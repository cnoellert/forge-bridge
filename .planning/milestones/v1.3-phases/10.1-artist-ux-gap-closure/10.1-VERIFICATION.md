---
phase: 10.1-artist-ux-gap-closure
verified: 2026-04-24T19:27:43Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 2
overrides:
  - must_have: "Preset chips (Active synth / Quarantined / Builtin only) are visually discoverable on first paint"
    reason: "Chip roster amended to two variants (Synth only / Builtin only) per Plan 10.1-01 contract — the original three-chip roster named the middle chip Quarantined, which filtered against a nonexistent status field. Shipped chip roster matches the 2-variant contract locked by tests/test_tool_quarantine_surface.py. Also, Active synth was renamed to Synth only in Plan 10.1-06 because its tokens (origin:synthesized) delivered all synthesized tools regardless of status — label now matches tokens. ROADMAP SC#2 was amended in lockstep; SC#4 chip-name list was not, but SC#4's intent (chips discoverable, caption, UI-SPEC coverage) is fully met."
    accepted_by: "CN"
    accepted_at: "2026-04-24T10:55:00-07:00"
  - must_have: "Fresh operator (not CN/dev, not ET/tester) identifies the three most recently synthesized tools and their status within 30 seconds"
    reason: "UAT ran 2026-04-24 10:50; ET was used (not truly fresh) and answered correctly in ~20s, but inferred sort order from list position rather than reading the Created column header. Both deviations are honest-recorded in 10.1-UAT.md Deviations section, 10.1-05-SUMMARY.md, and the UAT Decision block explicitly captures developer judgement to mark PASS with deviations. Transparent audit trail; the deviation is NOT hidden."
    accepted_by: "CN"
    accepted_at: "2026-04-24T10:55:00-07:00"
human_verification:
  - test: "Run re-UAT with a truly fresh non-developer operator (not CN, not ET) on :9996 with the D-44 fixture set"
    expected: "Operator identifies the 3 most-recently-synthesized tools and their status in ≤30s on first glance, reading sort order from the Created column header rather than guessing from list position"
    why_human: "D-44 requires human observation — no CLI/API substitute. The 2026-04-24 UAT used ET with documented deviations; a truly fresh operator is the only way to close the deviation without requalifying the existing record"
  - test: "Assess whether the Created column header's descending-sort affordance is legible without hovering or guessing"
    expected: "Operator identifies the sort order from the header alone (small caret, relative time like '2h ago', or a label rename like 'Synthesized') — not from inferring via list position"
    why_human: "UX signal was the primary residual concern in 10.1-UAT.md observations; requires a human to judge glanceability vs guessed inference. Addressed in 10.1-UAT.md §Decision as a Phase 11 follow-up"
  - test: "Visual smoke-check of http://localhost:9996/ui/tools after a chip click and a nav click in Firefox/Safari (Chromium regression is automated)"
    expected: "Single .top-nav, single #health-strip, caption + 2 chips visible, status chip colors render correctly in non-Chromium engines"
    why_human: "Playwright tests only drive Chromium; Firefox/Safari parity is not automated in this phase"
---

# Phase 10.1: Artist-UX Gap Closure Verification Report

**Phase Goal:** The Web UI passes the D-36 non-developer dogfood re-UAT — a fresh operator opens http://localhost:9996/ui/ cold and identifies the three most recently synthesized tools and their status (active / quarantined) within 30 seconds. The nav swap contract is fixed (no duplicate .top-nav / #health-strip render on click), the Tools table surfaces an explicit Status chip, column headers use artist-facing language, developer telemetry is demoted or hidden, and preset chips are discoverable on first paint. A new in-browser swap-contract test guards against regression of the shell.html bug class.

**Verified:** 2026-04-24T19:27:43Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to ROADMAP Success Criteria)

| # | Truth (SC#) | Status | Evidence |
|---|-------------|--------|----------|
| 1 | SC#1 — Nav swap contract: one .top-nav and one #health-strip after click; UI-SPEC updated | VERIFIED | shell.html line 7 is `  <nav class="top-nav" hx-boost="true" hx-push-url="true">` (hx-target/hx-swap removed per D-37 Option A). UI-SPEC.md §Interaction Contracts row rewritten to `default (body)` / `default (outerHTML of body)`; "Nav swap note (Phase 10.1, D-37 Option A)" paragraph present. Playwright `test_nav_click_yields_single_nav_and_single_health_strip` passes on post-fix, fails on pre-fix (FAIL→PASS evidence in 10.1-04-SUMMARY.md Steps 2 + 4). |
| 2 | SC#2 — Tools table has explicit Status column with active/loaded chip; quarantined deferred | VERIFIED | `<th>Status</th>` present in tools_table.html; `<span class="status-chip status-chip--{{ tool.status }}">` renders chip. `_derive_tool_status(ToolRecord)` (ui_handlers.py:85) returns `active` or `loaded`. Live spot-check: `_derive_tool_status(ToolRecord(..., origin='synthesized', code_hash='a'*64, observation_count=5))` → `'active'`. ROADMAP SC#2 amended in lockstep (line 107) to 2-variant contract; `tests/test_tool_quarantine_surface.py` (7 tests) pins the contract. |
| 3 | SC#3 — Artist-facing column headers; dev telemetry demoted | VERIFIED | Four primary `<th>` elements: Name, Status, Type, Created. Zero occurrences of `<th>Code hash</th>`, `<th>Obs count</th>`, `<th>Namespace</th>`. Telemetry (namespace · code_hash[:8] · N obs) lives in `.tool-name-cell .tool-name-secondary` (12px/muted via forge-console.css:67). Empty-state colspan=4. |
| 4 | SC#4 — Preset chips discoverable on first paint; caption; UI-SPEC captures pattern | PASSED (override) | Override: Chip roster amended to 2 variants (Synth only / Builtin only) per Plan 10.1-01 contract — accepted by CN on 2026-04-24. Caption `<p class="chip-caption">Click a chip to narrow the list</p>` present in tools/list.html line 7. `.chip-caption` CSS (forge-console.css:65) provides 8px bottom margin (D-42 sm gap). Chip label/token alignment resolved by Plan 10.1-06 rename (Active synth → Synth only). UI-SPEC §Interaction Contracts nav row captures the swap contract; chip-click row update is deferred (tracked in 10.1-UAT.md §Decision as Phase 11 follow-up #2). |
| 5 | SC#5 — In-browser Playwright test guards swap contract; fails on pre-fix shell.html | VERIFIED | `tests/test_ui_nav_swap_regression.py` asserts D-39 invariant (1× nav.top-nav, 1× #health-strip). FAIL→PASS evidence captured verbatim in 10.1-04-SUMMARY.md §Steps 2 + 4. Bonus `tests/test_ui_chip_click_regression.py` covers chip-click path (Plan 10.1-06); FAIL→PASS evidence in 10.1-06-SUMMARY.md §Steps 2 + 4. pytest-playwright wired via `[project.optional-dependencies].test-e2e` in pyproject.toml line 37-38; tests auto-discovered by default `pytest tests/`. Full suite: 481 passed. |
| 6 | SC#6 — Non-developer dogfood re-UAT passes with fresh operator in <30s | PASSED (override) | Override: UAT ran 2026-04-24 10:50 with ET in ~20s; partial-understanding (sort inferred from list position, not Created header). Both deviations honest-recorded in 10.1-UAT.md §Deviations. Developer (CN) made documented judgement call to mark PASS with deviations; audit trail is transparent across 10.1-UAT.md + 10.1-05-SUMMARY.md. Accepted by CN on 2026-04-24. Followup: truly fresh operator recommended in Phase 11 (see human_verification block). |

**Score:** 6 / 6 truths verified (4 VERIFIED + 2 PASSED via override)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `forge_bridge/console/templates/shell.html` | Nav without hx-target/hx-swap; hx-boost + hx-push-url retained | VERIFIED | Line 7 exact match. 53 lines (same as pre-edit). |
| `forge_bridge/console/templates/fragments/tools_table.html` | 4 primary columns; Status chip; empty-state colspan=4 | VERIFIED | `<th>Name/Status/Type/Created</th>` each count 1; `status-chip status-chip--` interpolation present; `colspan="4"` present; no old headers. |
| `forge_bridge/console/templates/tools/list.html` | Caption above query console include | VERIFIED | `<p class="chip-caption">Click a chip to narrow the list</p>` on line 7, above `{% include "fragments/query_console.html" %}`. |
| `forge_bridge/console/templates/fragments/query_console.html` | submit() and clearAndReset() use body/outerHTML swap; explicit history.pushState | VERIFIED | `target: 'body'` count = 2; `target: '#view-main'` count = 0; `history.pushState` count = 3 (1 comment + 2 call sites). Plan 10.1-06 fix landed. |
| `forge_bridge/console/ui_handlers.py` | `_derive_tool_status` helper + handler decoration; `_tools_preset_chips` returns 2 chips (Synth only + Builtin only) | VERIFIED | `def _derive_tool_status(tool: "ToolRecord") -> str` at line 85; handler decoration `{**t.to_dict(), "status": _derive_tool_status(t)}` at line 200; preset chips returns `[{'label': 'Synth only', 'tokens': 'origin:synthesized'}, {'label': 'Builtin only', 'tokens': 'origin:builtin'}]`. Zero `Active synth` / `Quarantined` references. |
| `forge_bridge/console/static/forge-console.css` | Status chip + caption + tool-name-cell CSS rules | VERIFIED | `.status-chip{`, `.status-chip--active{`, `.status-chip--loaded{`, `.status-chip--quarantined{` (forward-ready), `.chip-caption{`, 4 `.tool-name-cell` rules — all present. |
| `tests/test_tool_quarantine_surface.py` | 3 pinning + 4 derivation tests | VERIFIED | 7 test functions found; runs green (verified via targeted pytest run). |
| `tests/test_ui_nav_swap_regression.py` | Playwright nav-swap test | VERIFIED | `async def test_nav_click_yields_single_nav_and_single_health_strip` at line 204; runs green (verified live: 1 passed in targeted run). |
| `tests/test_ui_chip_click_regression.py` | Playwright chip-click test (Plan 10.1-06 bonus) | VERIFIED | `async def test_chip_click_yields_single_nav_and_single_health_strip` at line 193; runs green (verified live: 1 passed in targeted run). |
| `tests/test_ui_tools_view.py` | Lockstep `Synth only` assertion | VERIFIED | Fixture assertion uses `"Synth only" in r.text`; `Active synth` absent. |
| `pyproject.toml` | `[project.optional-dependencies].test-e2e` with pytest-playwright; not in base deps | VERIFIED | `test-e2e = [\n    "pytest-playwright>=0.5",\n]` at lines 37-38. `awk '/^dependencies = \[/,/^\]/' \| grep -c playwright` = 0. |
| `.planning/ROADMAP.md` | SC#2 amended to 2-variant contract; quarantined deferred to Phase 11 / v1.4 | VERIFIED | Line 107 contains `one of \`active\` or \`loaded\`` and `Quarantined-tool surfacing deferred to Phase 11 / v1.4`. Old 3-variant text gone (grep count 0). |
| `.planning/phases/10-web-ui/10-UI-SPEC.md` | Nav-row rewrite + "Nav swap note" paragraph | VERIFIED | `Nav link click (hx-boost) \| default (body) \| default (outerHTML of body)` row present (count 1); `Nav swap note (Phase 10.1, D-37 Option A)` paragraph present (count 1). |
| `.planning/phases/10.1-artist-ux-gap-closure/10.1-UAT.md` | UAT record with Outcome / Duration / Operator | VERIFIED | All three required fields present; PASS with documented deviations; automated-gate confirmation block present. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| tests/test_tool_quarantine_surface.py | forge_bridge/console/manifest_service.py | ms.remove() + ToolRecord field shape | WIRED | Test 2 calls `ms.remove("synth_loaded")` and asserts absence; Test 3 asserts `fields(ToolRecord)` has no `status`/`quarantined`. |
| forge_bridge/console/ui_handlers.py | forge_bridge/console/templates/fragments/tools_table.html | Handler decorates tool dict with derived `status`; template renders `status-chip--{{ tool.status }}` | WIRED | Verified via live import spot-check — `_derive_tool_status` returns `"active"` for synth+code_hash+obs>0; decoration at ui_handlers.py:200 passes status into the template context. |
| forge_bridge/console/templates/fragments/tools_table.html | forge_bridge/console/static/forge-console.css | Chip class names match CSS selectors | WIRED | Template emits `.status-chip .status-chip--active` / `.status-chip--loaded`; CSS defines all 3 variants + base; `.tool-name-cell .tool-name-primary/--secondary` classes match primary/secondary cells. |
| forge_bridge/console/templates/tools/list.html | forge_bridge/console/static/forge-console.css | `.chip-caption` utility class | WIRED | Template `<p class="chip-caption">` matches `.chip-caption{color:var(--color-text-muted);...margin:0 0 8px 0}` in CSS. |
| forge_bridge/console/templates/fragments/query_console.html | htmx 2.0.10 + browser history API | `htmx.ajax({target:'body', swap:'outerHTML'})` + explicit `history.pushState()` | WIRED | Both submit() and clearAndReset() use the body-swap pattern; pushState precedes ajax call; covered by test_ui_chip_click_regression.py which asserts post-click URL pushed to `/ui/tools?origin=synthesized`. |
| tests/test_ui_nav_swap_regression.py | forge_bridge/console/app.py + mcp/server.py | `_start_console_task` on 127.0.0.1 ephemeral port | WIRED | Live uvicorn harness pattern from `tests/test_console_http_transport.py:24-75` replicated; `_find_free_port` binds to 127.0.0.1 only (T-10.1-11 mitigation; `0.0.0.0` count = 0). |
| tests/test_ui_chip_click_regression.py | same harness | Same fixture pattern (module-local) | WIRED | Plan 10.1-06 forward-references fixture extraction to conftest as Phase 11 cleanup. |
| .planning/ROADMAP.md SC#2 | tests/test_tool_quarantine_surface.py | SC#2 amended text cites the test as contract source of truth | WIRED | Grep on ROADMAP.md for `test_tool_quarantine_surface` returns ≥ 1 match in the amended SC#2 text. |
| .planning/phases/10-web-ui/10-UI-SPEC.md Nav row | forge_bridge/console/templates/shell.html | Attribute set matches verbatim | WIRED | UI-SPEC row: "default (body) / default (outerHTML of body) / true"; shell.html has hx-boost + hx-push-url + no override. |

### Data-Flow Trace (Level 4)

Dynamic-data artifact in this phase is the Tools table. Traced end-to-end:

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| fragments/tools_table.html | `{% for tool in tools %}` | `ui_tools_handler` ConsoleReadAPI.get_tools() → decorator adds `status` key | Yes (when ManifestService has records); spot-checked via `_derive_tool_status` returning real `active` | FLOWING |
| fragments/tools_table.html | `tool.status` | `_derive_tool_status(t)` pure function over ToolRecord fields | Yes — deterministic from origin/code_hash/observation_count | FLOWING |
| tools/list.html caption | static literal `Click a chip to narrow the list` | N/A (static copy) | N/A (intentionally static) | N/A |
| fragments/query_console.html chips | `preset_chips` context var | `_tools_preset_chips()` | Yes — returns 2 chips with stable labels/tokens | FLOWING |

The only artifact rendering genuinely dynamic data (tools_table.html) is fully wired end-to-end: ManifestService → ConsoleReadAPI.get_tools() → ui_tools_handler decoration → template context → chip class + label. No HOLLOW_PROP, no STATIC fallback.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `_derive_tool_status` returns "active" for synth+hash+obs | `python -c "from forge_bridge.console.ui_handlers import _derive_tool_status; from forge_bridge.console.manifest_service import ToolRecord; print(_derive_tool_status(ToolRecord(name='x', origin='synthesized', namespace='synth', code_hash='a'*64, observation_count=5)))"` | `active` | PASS |
| `_tools_preset_chips` returns 2-chip roster with `Synth only` | `python -c "from forge_bridge.console.ui_handlers import _tools_preset_chips; print(_tools_preset_chips())"` | `[{'label': 'Synth only', 'tokens': 'origin:synthesized'}, {'label': 'Builtin only', 'tokens': 'origin:builtin'}]` | PASS |
| Targeted phase-10.1 tests run green | `pytest tests/test_tool_quarantine_surface.py tests/test_ui_nav_swap_regression.py tests/test_ui_chip_click_regression.py tests/test_ui_tools_view.py tests/test_ui_js_disabled_graceful_degradation.py -q` | `32 passed, 30 warnings in 2.57s` | PASS |
| Full regression suite | `pytest tests/ -q` | `481 passed, 70 warnings in 17.90s` | PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| CONSOLE-02 | 10.1-02 (UI-SPEC), 10.1-05 (closure), 10.1-06 | Web UI design follows UI-SPEC.md palette contract | SATISFIED | UI-SPEC.md §Interaction Contracts updated (nav row + note); CSS reuses `:root` tokens (amber + #242424 LOGIK-PROJEKT palette) via `var(--color-accent)`, `var(--color-status-ok)`, etc. |
| CONSOLE-04 | 10.1-02, 10.1-04, 10.1-05, 10.1-06 | Navigation covers five views plus drilldowns with URL-addressable state | SATISFIED | Nav anchors for all 5 views in shell.html (Tools/Executions/Manifest/Health/Chat); hx-push-url="true" preserves URL; chip click uses explicit `history.pushState` for bookmarkability. Playwright tests assert URL push post-click. |
| TOOLS-01 | 10.1-01, 10.1-03, 10.1-05, 10.1-06 | Browse registered tools with filters for origin/namespace/readonly | SATISFIED | tools_table.html renders all tools from get_tools(); 2-chip preset filter (Synth only / Builtin only) + query-console grammar supports origin, namespace, q, readonly filters. |
| TOOLS-02 | 10.1-01, 10.1-03, 10.1-05, 10.1-06 | Drilldown with canonical _meta provenance | SATISFIED | Row-link `hx-get="/ui/tools/{{ tool.name }}"` retained (drilldown handler unchanged from Phase 10); demoted telemetry in Name cell exposes code_hash[:8], obs count, namespace on list view. |

All four requirement IDs declared in PLAN frontmatter are SATISFIED. No orphaned requirements in REQUIREMENTS.md for Phase 10.1 (re-verification under D-36 gate; automated coverage already closed in Phase 10).

### Anti-Patterns Found

Scanned all files modified across Plans 10.1-01 → 10.1-06:

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| forge_bridge/console/templates/fragments/query_console.html | 33 | `@click='stuffAndSubmit("{{ chip.tokens }}")'` — attribute-context JS escaping | Warning (latent) | Code review flagged as WR-01 in 10.1-REVIEW.md; not exploitable at current call sites (all chip tokens hardcoded safe); deferred to Phase 11 hardening |
| forge_bridge/console/static/forge-console.css | 62, 64 | `rgba(47,95,0,.15)` / `rgba(102,78,0,.2)` hex-derived tints | Info | Pre-existing file pattern; tints are visual-only; not a security concern |
| (code review, see 10.1-REVIEW.md) | various | 1 unused variable + 5 cleanup items | Info | 0 critical, 2 warnings, 5 info — none block the phase goal |

**Blocker anti-patterns: 0.** All flagged items are latent or informational; none affects the Phase 10.1 goal achievement.

### Human Verification Required

Three items that automated verification cannot close. See `human_verification` frontmatter for full list. Summary:

1. **Truly-fresh-operator re-UAT** — D-44 fresh-operator criterion was not strictly met (ET re-used). Recommend running one more re-UAT with a genuinely uncontaminated operator in Phase 11 to requalify the record without affecting current closure.
2. **Default-sort affordance legibility** — ET inferred recency from list position, not from the Created column header. A human must judge whether a small caret / relative time / header rename would make glance-reading work on first try. Captured in 10.1-UAT.md §Decision #1 as Phase 11 follow-up.
3. **Non-Chromium browser parity smoke** — Playwright regression only drives Chromium; Firefox/Safari rendering of the Status chip + nav swap + chip click is not automated.

None of these items block Phase 10.1 closure; they are forward-pointers for Phase 11 / v1.4.

### Gaps Summary

No actionable gaps. Deliverables for all six ROADMAP success criteria are present and correctly wired in the codebase. Two items required overrides (SC#4 chip-roster amendment; SC#6 UAT deviation) — both are documented deliberate deviations with transparent audit trails (ROADMAP SC#2 amendment, 10.1-UAT.md Deviations block, 10.1-05-SUMMARY.md, and the Decision block explicitly capturing developer judgement to mark PASS with recorded deviations).

Phase 10.1 achieves its goal: the D-36 artist-UX gate is closed with a complete fix set (nav swap, chip swap, Status chip, artist-facing headers, demoted telemetry, caption, Playwright regression tests on both interaction paths, and amended ROADMAP SC#2 reflecting the 2-variant contract).

The `status: human_needed` reflects the three forward-pointing human follow-ups — a truly fresh operator should exercise the UI again in Phase 11 to close the documented deviation cleanly, and the default-sort affordance UX signal should be addressed. These are not blockers; they are quality-improvement items that require a human to evaluate.

---

_Verified: 2026-04-24T19:27:43Z_
_Verifier: Claude (gsd-verifier)_
