---
phase: 10.1-artist-ux-gap-closure
plan: 05
status: complete
outcome: PASS (with deviations — see 10.1-UAT.md)
date: 2026-04-24
---

# Plan 10.1-05 — Dogfood Re-UAT Summary

## Pre-UAT automated-gate baseline (Task 1)

- **pytest tests/ -x**: exit 0, 481 passed / 22 skipped (forge conda env). Well above the ≥160 plan criterion.
- **ruff check forge_bridge/ tests/**: 119 pre-existing findings, zero regressions from Phase 10.1. Verified by checking out `c923991` (pre-10.1 base) — identical count. All Phase 10.1-touched files pass scoped ruff. Plan 10.1-05 Task 1's absolute-zero acceptance was over-specified relative to repo baseline; tracked as plan-authoring finding.

## Fixture server (Task 2)

- **Mode**: Option A — local dev script `scripts/dogfood_uat_server.py` (gitignored; delete after phase closes). Reuses `_make_fake_read_api()` from `tests/test_ui_nav_swap_regression.py` verbatim so the fixture is identical to the Playwright contract.
- **Boot**: `python scripts/dogfood_uat_server.py` binds 127.0.0.1:9996.
- **Smokes**: HTTP 200 on `/ui/tools`, 5 tools on `/api/v1/tools`, visual confirmation of 4 columns / caption / 2 chips / no quarantined / clean nav swap.

## Bug discovered during smoke — triggered gap-closure Plan 10.1-06

Smoke check (2026-04-24 10:42) surfaced two bugs that the Wave 1/2 tests did not cover:

1. **Chip click duplicates shell inside `#view-main`.** Same D-39 invariant violation as the 2026-04-23 D-37 nav bug, but via `query_console.html`'s JS `htmx.ajax()` path instead of the `hx-boost` nav-link path. Playwright regression in 10.1-04 only tested nav-link clicks, not chip clicks.
2. **"Active synth" chip label mismatch with tokens.** Label promised "active", tokens (`origin:synthesized`) delivered all synthesized tools regardless of status.

Both fixed by Plan 10.1-06 before the operator was brought in. See `10.1-06-SUMMARY.md`.

Bonus finding surfaced by 10.1-06: `pushUrl: true` in `htmx.ajax()` options is a no-op in htmx 2.0.10; replaced with explicit `history.pushState()` for URL bookmarkability. Was latent pre-10.1.

## UAT outcome (Task 3 — human checkpoint)

- **Operator**: ET (same operator as 2026-04-23 run — see UAT record deviation #1).
- **Duration**: ~20s (wall-clock observed; under 30s D-44 criterion).
- **Answer correctness**: 3 correct tools named with "active" status vocabulary.
- **Partial-understanding**: operator inferred sort order from list position, not from the `Created` column header (UAT record deviation #2).
- **Outcome**: PASS with deviations documented verbatim in `10.1-UAT.md`.

## UAT record committed (Task 4)

- `.planning/phases/10.1-artist-ux-gap-closure/10.1-UAT.md` — full record, including both deviations, observations, automated-gate post-confirmation, and three Phase 11 follow-up items (default-sort affordance legibility; UI-SPEC chip-click lockstep; Playwright fixture extraction).
- STATE.md + ROADMAP.md updates applied via `gsd-tools phase complete` (orchestrator step).

## Deviations summary

Plan 10.1-05 had three places where execution deviated from the plan text:

1. **Task 1 ruff gate — adjusted to scoped-clean**: 119 pre-existing findings verified at the pre-Phase-10.1 base, not introduced by this phase. Recorded in UAT; not blocking.
2. **Task 2 fixture mode — chose Option A (dev script)**: user's local `~/.forge-bridge/synthesized/` only has 1 synth tool, so Option B (live stack with real ManifestService preload) was not available. Option A was documented in the plan as an alternative.
3. **Task 3 operator identity and partial understanding — accepted with documented deviation**: no fresh operator available on-site, ET used. Answer was partially inferred rather than cleanly read off the UI. Both deviations honest-recorded in `10.1-UAT.md` §Deviations.

## Forward pointer

- Phase 10 unblocks — D-36 artist-UX gate closed (with deviations documented).
- Phase 10.1 closes at 6/6 plans complete (5 original + 1 gap-closure for the smoke finding).
- Next action: `/gsd-plan-phase 11` — CLI Companion phase.
- Three deferred items captured for Phase 11 / v1.4 (see UAT record §Decision).
