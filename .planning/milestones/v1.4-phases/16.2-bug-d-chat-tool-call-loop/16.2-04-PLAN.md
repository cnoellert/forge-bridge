---
phase: 16.2
plan: 04
type: execute
wave: 3
depends_on: ["16.2-02", "16.2-03"]
files_modified:
  - .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md
autonomous: false
requirements:
  - CHAT-04
must_haves:
  truths:
    - "D-08 #1: Strategy B E2E (Plan 03's strengthened test) PASSES on assist-01 with FB_INTEGRATION_TESTS=1 + live qwen2.5-coder:32b"
    - "D-08 #2: Adapter unit tests (Plan 01's TestOllamaToolAdapterBugDFallback + the rest of test_ollama_adapter.py) PASS on dev AND on assist-01"
    - "D-08 #3: CN/dev manual walkthrough at http://localhost:9996/ui/chat with the canonical CHAT-04 prompt produces a useful natural-language answer (NOT raw JSON, NOT refusal, NOT loop_budget_exceeded)"
    - "16.2-HUMAN-UAT.md records the technical-floor evidence + walkthrough capture in the Phase 10.1 / 16.1 D-14 format"
    - "Outcome: PASS with deviations IF AND ONLY IF all three D-08 conditions hold; otherwise Outcome: FAIL and Phase 16.3 is required"
    - "D-10 ratification path is documented as a post-close annotation hook (NOT a gating criterion)"
  artifacts:
    - path: ".planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md"
      provides: "Operator-recorded UAT outcome closing CHAT-04 (PASS with deviations per D-08, or FAIL routing to Phase 16.3)"
      contains: "Outcome:"
  key_links:
    - from: "16.2-HUMAN-UAT.md"
      to: ".planning/REQUIREMENTS.md::CHAT-04"
      via: "D-08 PASS-with-deviations satisfies CHAT-04 per D-14 precedent"
      pattern: "CHAT-04"
    - from: "16.2-HUMAN-UAT.md"
      to: ".planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-01..03-SUMMARY.md"
      via: "Pre-flight automated gates table cites the SUMMARY commits"
      pattern: "Pre-flight automated gates"
---

<objective>
Run the fresh-operator UAT path that closes CHAT-04 and unblocks v1.4 milestone close. Per CONTEXT D-08..D-11: Phase 16.2 closes with `Outcome: PASS with deviations` per the Phase 10.1 / 16.1 D-14 precedent IF AND ONLY IF the three technical-floor conditions all hold. Conditions 1 (Strategy B PASSES live), 2 (adapter unit tests PASS), and 3 (CN/dev manual walkthrough produces a useful natural-language answer) cannot be skipped — they form the technical floor that Phase 16.1's deviation lacked.

Purpose: Provide reproduce-from-disk evidence that Bug D is closed end-to-end. Plans 01-03 prove the fix at the unit-test + E2E-test layer; Plan 04 proves it at the artist-facing chat surface. Without this gate Phase 16.2 reduces to "the tests pass" without proof that the user-visible bug is fixed — which is exactly the failure mode Phase 16.1 closed-with-known-gap on.

The D-10 ratification path (fresh-artist re-run after Phase 16.2 close, observation appended to the same HUMAN-UAT.md) is a POST-CLOSE annotation hook — it does NOT gate this plan. Per CONTEXT D-09: "The deviation acceptable here is 'no fresh artist; CN/dev re-test against the strengthened automated floor' — and the floor must hold."

Output: A single `16.2-HUMAN-UAT.md` file in the phase directory with operator-recorded evidence following the Phase 10.1 / 16.1 format precedent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-CONTEXT.md
@.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-01-SUMMARY.md
@.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-02-SUMMARY.md
@.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-03-SUMMARY.md
@.planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md
@.planning/phases/10.1-artist-ux-gap-closure/10.1-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run D-08 technical floor (#1 + #2 + #3) on assist-01 and record HUMAN-UAT</name>
  <files>.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md</files>

  <read_first>
    - .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-CONTEXT.md (D-08 — three technical-floor conditions; D-09 — none can be skipped; D-10 — ratification path; D-11 — rejected paths)
    - .planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md (the format precedent — same Fixture State / Pre-flight gates / Test / Observations / Outcome / Action / Sign-off skeleton, EXCEPT this one MUST land PASS-with-deviations not FAIL)
    - .planning/phases/10.1-artist-ux-gap-closure/10.1-UAT.md (the original D-13 fresh-operator format precedent)
    - .planning/REQUIREMENTS.md (CHAT-04 verbatim — the requirement this UAT closes)
    - .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-01-SUMMARY.md (commit SHA of RED test)
    - .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-02-SUMMARY.md (commit SHA of GREEN fix)
    - .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-03-SUMMARY.md (commit SHA of strengthened E2E test)
  </read_first>

  <what-built>
    Plans 01–03 are on disk with all unit tests green. The technical fix for Bug D is committed. This checkpoint runs the three technical-floor checks per D-08 and records the result in `16.2-HUMAN-UAT.md`.

    Nothing automated to "build" in this task — it is a coordinated execution of three pre-existing checks, with the artifact being the operator's signed report.
  </what-built>

  <how-to-verify>
    Operator (CN/dev) on assist-01:

    **Pre-flight (run ONCE before D-08 #3):**

    Confirm the fix is on the running branch:
    ```bash
    git log --oneline -5 | head -10
    # Should show the GREEN feat(16.2) commit on top of the RED test(16.2) commit + strengthen test(16.2) commit
    ```

    Warm Ollama qwen2.5-coder:32b (avoids the 10–30s cold-start that can blow the <60s budget — Pitfall 9 from Phase 16.1):
    ```bash
    ollama run qwen2.5-coder:32b "warm" >/dev/null 2>&1
    ```

    Restart `python -m forge_bridge` so the live chat panel runs the new code:
    ```bash
    pkill -f 'python -m forge_bridge' 2>/dev/null || true
    sleep 1
    python -m forge_bridge >/tmp/forge-bridge.log 2>&1 &
    sleep 3
    curl -s http://localhost:9996/api/health | head -c 200
    # Expect a JSON response (degraded is OK on assist-01 without Flame)
    ```

    ---

    **D-08 #1 — Strategy B E2E PASSES on assist-01 with live Ollama:**

    ```bash
    FB_INTEGRATION_TESTS=1 pytest tests/integration/test_chat_endpoint_live.py::test_chat_canonical_uat_prompt_under_60s -v --tb=long 2>&1 | tee /tmp/16.2-d08-1.log
    ```

    Required outcome: `1 passed`. Capture elapsed time from the test output. If FAILED, STOP — Phase 16.2 close blocked. Failure mode means either (a) Plan 02 GREEN fix did not actually close Bug D end-to-end, or (b) qwen2.5-coder is emitting a different failure shape than the captured fixture handles. Either way, do NOT record PASS-with-deviations.

    ---

    **D-08 #2 — Adapter unit tests PASS on dev AND on assist-01:**

    On assist-01 (already running there for #1):
    ```bash
    pytest tests/llm/test_ollama_adapter.py -v --tb=short 2>&1 | tee /tmp/16.2-d08-2-assist.log
    # Required: 0 failed
    ```

    On dev machine (separate run — this is the "AND on dev" half of D-08 #2):
    ```bash
    pytest tests/llm/test_ollama_adapter.py -v --tb=short 2>&1 | tee /tmp/16.2-d08-2-dev.log
    # Required: 0 failed
    ```

    Required outcome: 0 failures on BOTH machines. The captured fixture is portable (no live Ollama needed for the adapter unit tests), so dev and assist-01 should produce identical results.

    ---

    **D-08 #3 — CN/dev manual walkthrough at http://localhost:9996/ui/chat:**

    1. Open `http://localhost:9996/ui/chat` in a browser on assist-01.
    2. Type the canonical CHAT-04 prompt verbatim (Phase 16 D-12):
       > what synthesis tools were created this week?
    3. Press Enter. Wait for the response.
    4. Take a screenshot OR copy the verbatim assistant message text.

    Required outcome shape — a useful natural-language answer that:
    - Is **NOT** raw tool-call JSON (e.g., NOT `{"name": ...}`).
    - Is **NOT** a refusal ("I can't help with that").
    - Is **NOT** a `loop_budget_exceeded` error banner.
    - Mentions specific tool names from the manifest, OR explains there are no synthesis tools created this week (both are useful answers — the LLM can correctly report "none" if the manifest is empty on assist-01).
    - Returns within ~60 seconds (informally — the strict 60s gate is in the automated D-08 #1 test).

    Acceptable variant: if the manifest on assist-01 is empty (no synthesis tools created this week), an answer like "There are no synthesis tools created this week" or "The manifest currently lists no tools" IS a useful natural-language answer and satisfies D-08 #3. The point is the LLM executed the tool, got a result, and synthesized in English — Bug D is closed.

    Unacceptable variants — record as FAIL and route to Phase 16.3:
    - Raw tool-call JSON `{"name": "forge_tools_read", ...}` (Bug D recurs)
    - HTTP 500 or 504 error banner
    - Empty assistant message
    - Loop_budget_exceeded message

    ---

    **Author `16.2-HUMAN-UAT.md`** following the 16.1-HUMAN-UAT.md format with these adjustments:

    - Front-matter: `status: passed` (or `status: failed` if any of D-08 #1/#2/#3 fails) — NOT `status: failed` like 16.1's was.
    - Header: same Date / Operator / Host / Commit / Branch shape.
    - **Operator note:** "DEVIATION from D-13 fresh-operator requirement (no fresh artist available; CN/dev re-test against the strengthened technical floor per D-08/D-09/D-11). Ratification path D-10 stays open for post-close fresh-artist re-run."
    - **Fixture State** section: same shape as 16.1 (forge-bridge running on :9996, Flame :9999 unreachable expected, Ollama qwen2.5-coder:32b loaded, etc.)
    - **Pre-flight automated gates** table: include the three rows below + any pre-existing 16.1 gates that were re-run (e.g., the four console-test files).

      | Gate | Result |
      |------|--------|
      | D-08 #1: Strategy B E2E `test_chat_canonical_uat_prompt_under_60s` (FB_INTEGRATION_TESTS=1, live Ollama) | PASS / FAIL — record actual elapsed seconds |
      | D-08 #2a: Adapter unit tests on assist-01 (`pytest tests/llm/test_ollama_adapter.py`) | PASS — N tests, 0 failures |
      | D-08 #2b: Adapter unit tests on dev (`pytest tests/llm/test_ollama_adapter.py`) | PASS — N tests, 0 failures |
      | TestOllamaToolAdapterBugDFallback (Plan 01's RED→GREEN flip evidence) | PASS — both methods green after Plan 02 |
      | (any 16.1 regression guards re-run) | PASS — confirm no regressions |

    - **Test (D-08 #3 walkthrough)** section: numbered steps + verbatim prompt + verbatim or screenshot of the response.
    - **Observations** section: response time, response content (verbatim or pasted from screenshot), tool-call trace blocks visible/absent, error banners absent, subjective assessment, follow-up questions tested if any.
    - **Cross-Phase 16 must-have regression check**: same as 16.1.
    - **Bug D closure assessment** section: explicit statement that the response IS a useful natural-language answer (or IS NOT — and route to Phase 16.3).
    - **Deviations** section: list operator deviation (CN/dev not fresh artist), explain how the strengthened technical floor (D-08 #1 + #2) compensates per D-09. Cite Phase 16.1 D-14 precedent.
    - **Outcome:** `PASS with deviations` IF AND ONLY IF all three D-08 conditions hold. `FAIL` otherwise (and route to Phase 16.3 in the Action section).
    - **Action** section: if PASS-with-deviations, "Phase 16.2 closes-with-deviation. CHAT-04 satisfied. v1.4 milestone close unblocked. D-10 ratification path stays open as post-close annotation hook." If FAIL, "Phase 16.2 closes-with-known-gap. Open Phase 16.3 to investigate <specific failure mode>. v1.4 milestone close stays blocked."
    - **D-10 ratification annotation hook** subsection (always present, distinct from the close): "If a fresh artist becomes available before v1.4 milestone close, replay the canonical CHAT-04 prompt at http://localhost:9996/ui/chat and append the observation here. Per D-10, ratification does NOT re-open Phase 16.2; it strengthens the close-with-deviation record."
    - **Operator sign-off:** CN/dev — date.
  </how-to-verify>

  <acceptance_criteria>
    - File exists: `test -f .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md`
    - Front-matter present: `head -8 .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md | grep -E '^status:'` returns one line.
    - All three D-08 conditions are explicitly recorded:
      - `grep -c 'D-08 #1' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` returns at least 1
      - `grep -c 'D-08 #2' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` returns at least 1
      - `grep -c 'D-08 #3' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` returns at least 1
    - Outcome line present: `grep -E '^.{0,5}Outcome:' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` returns one line.
    - If Outcome is "PASS with deviations", all three D-08 gates must be PASS in the report:
      ```bash
      OUTCOME=$(grep -E 'Outcome:' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md)
      if echo "$OUTCOME" | grep -qi 'PASS'; then
        # All three D-08 conditions must show PASS in the table
        grep 'D-08 #1' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md | grep -qi 'PASS' || { echo 'D-08 #1 not PASS'; exit 1; }
        grep 'D-08 #2' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md | grep -qi 'PASS' || { echo 'D-08 #2 not PASS'; exit 1; }
        grep 'D-08 #3' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md | grep -qi 'PASS\|useful\|natural' || { echo 'D-08 #3 not satisfied'; exit 1; }
        echo "All three D-08 conditions satisfied for PASS-with-deviations"
      fi
      ```
    - D-10 ratification annotation hook is present (and is documented as POST-close, not a gate): `grep -c 'D-10' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` returns at least 1, AND `grep -i 'ratification' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md | grep -qi 'does not re-open\|stays open\|post-close'` succeeds.
    - CHAT-04 referenced: `grep -c 'CHAT-04' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` returns at least 1.
    - Operator sign-off line: `grep -E 'Operator sign-off|CN/dev' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` returns at least one line.
    - Verbatim assistant response is included (either pasted text or screenshot reference): `grep -E 'Response content|verbatim|screenshot' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` returns at least 1.
    - Action section routes correctly: if Outcome is PASS-with-deviations, the Action mentions "v1.4 milestone close unblocked"; if FAIL, mentions "Phase 16.3" and "v1.4 milestone close stays blocked":
      ```bash
      if grep -qi 'Outcome:.*PASS' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md; then
        grep -qi 'v1.4 milestone close unblocked\|unblocks v1.4' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md
      elif grep -qi 'Outcome:.*FAIL' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md; then
        grep -qi 'Phase 16.3' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md && grep -qi 'stays blocked' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md
      fi
      ```
  </acceptance_criteria>

  <action>See &lt;how-to-verify&gt; below — the operator runs all three D-08 technical-floor checks on assist-01 (D-08 #1: `FB_INTEGRATION_TESTS=1 pytest tests/integration/test_chat_endpoint_live.py::test_chat_canonical_uat_prompt_under_60s`; D-08 #2: `pytest tests/llm/test_ollama_adapter.py` on both assist-01 AND dev; D-08 #3: manual walkthrough at http://localhost:9996/ui/chat with the canonical CHAT-04 prompt) and writes `.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` following the Phase 10.1 / 16.1 D-14 format. Outcome is `PASS with deviations` IF AND ONLY IF all three D-08 conditions PASS; otherwise `FAIL` and Phase 16.3 is required.</action>
  <verify>
    <automated>test -f .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md &amp;&amp; grep -qE '^.{0,5}Outcome:' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md &amp;&amp; grep -q 'D-08 #1' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md &amp;&amp; grep -q 'D-08 #2' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md &amp;&amp; grep -q 'D-08 #3' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md &amp;&amp; grep -q 'CHAT-04' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md &amp;&amp; grep -q 'D-10' .planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md</automated>
  </verify>
  <done>`16.2-HUMAN-UAT.md` exists with the Phase 10.1 / 16.1 D-14 format, all three D-08 conditions explicitly recorded with evidence, an Outcome line consistent with the recorded evidence (`PASS with deviations` if all gates green, else `FAIL` routing to Phase 16.3), the D-10 ratification annotation hook documented as post-close (not gating), CHAT-04 referenced, and operator sign-off line present.</done>
  <resume-signal>Type "uat-passed" if all three D-08 conditions held and `16.2-HUMAN-UAT.md` records `Outcome: PASS with deviations`, OR "uat-failed" if any of D-08 #1/#2/#3 failed and the report records `Outcome: FAIL` with the Action routing to Phase 16.3. Either signal completes this checkpoint; the orchestrator decides whether to mark the phase as closed-with-deviation (PASS) or closed-with-known-gap (FAIL).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → committed UAT artifact | The HUMAN-UAT.md is operator-authored evidence committed to the repo |
| operator browser → live chat surface | The CN/dev walkthrough exercises the live chat panel on assist-01 — no new auth/secret surface beyond the existing localhost:9996 binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.2.04-01 | Repudiation | Operator records PASS-with-deviations without actually running all three D-08 gates | mitigate | Acceptance criteria include shell guards that grep for explicit `D-08 #1` / `#2` / `#3` PASS markers; if Outcome is PASS but any gate is missing/FAIL, the gate fails. Phase 16.1 D-14 precedent format is the template — operator follows the same explicit-evidence pattern. |
| T-16.2.04-02 | Spoofing | Operator pastes a synthetic "useful answer" without actually running the walkthrough | accept | This is a trust-the-operator boundary by design — the same way 16.1-HUMAN-UAT.md trusts the CN/dev sign-off. The acceptance criteria require the verbatim response text or screenshot reference; pure fabrication is detectable in audit but not gate-enforceable. The cost of formal anti-fraud measures (recorded video, third-party witness) exceeds the cost of trust here. |
| T-16.2.04-03 | DoS / Information Disclosure | Operator runs walkthrough on a host other than assist-01 with mismatched manifest data | accept | Acceptance criteria require "Host: assist-01" in the report header. Mismatched-host runs produce honest disagreement signals (different tool list) but don't compromise the close decision — the gate is "useful answer," not "specific tool list." |

This plan adds NO new attack surface to running code — it only adds an operator-authored documentation artifact to the planning tree.
</threat_model>

<verification>
After Task 1 lands:

1. `16.2-HUMAN-UAT.md` exists in the phase directory with the Phase 10.1 / 16.1 D-14 format.
2. All three D-08 conditions are explicitly recorded with evidence (test output, screenshot/verbatim response).
3. Outcome is `PASS with deviations` (technical floor held) OR `FAIL` (and routes to Phase 16.3).
4. CHAT-04 is referenced; D-10 ratification path is documented as a post-close annotation hook (not a gate).
5. Operator sign-off line is present.
6. Action section correctly routes the v1.4 milestone close decision.

If Outcome is PASS-with-deviations: Phase 16.2 closes-with-deviation, CHAT-04 satisfied, v1.4 milestone close unblocked.
If Outcome is FAIL: Phase 16.2 closes-with-known-gap, Phase 16.3 needed, v1.4 milestone close stays blocked.
</verification>

<success_criteria>
- `.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` exists with the Phase 10.1 / 16.1 D-14 format.
- All three D-08 technical-floor conditions are explicitly recorded with evidence.
- Outcome line present and consistent with the recorded evidence.
- D-10 ratification annotation hook documented as post-close (not gating).
- CHAT-04 referenced.
- Operator sign-off line.
- Action section correctly routes v1.4 milestone close decision based on Outcome.
- Single docs commit: `docs(16.2): record fresh-operator UAT — Outcome: <PASS with deviations | FAIL>`.
</success_criteria>

<output>
After completion, create `.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-04-SUMMARY.md` with:
- The Outcome line from `16.2-HUMAN-UAT.md`
- Whether v1.4 milestone close is unblocked or still blocked
- Whether CHAT-04 is satisfied
- Whether Phase 16.3 is required
- Pointer to the HUMAN-UAT.md path for the orchestrator to read in close-phase
</output>
