---
phase: 16-fb-d-chat-endpoint
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - forge_bridge/console/static/forge-console.css
autonomous: true
requirements:
  - CHAT-04
user_setup: []

must_haves:
  truths:
    - "forge-console.css exposes a .spinner-amber class using existing --color-accent token (D-08)"
    - "@keyframes spin animation rule is added (single new keyframe)"
    - "New chat-message bubble classes use existing --color-* tokens — zero new color values introduced"
    - ".chat-transcript flex layout supports the chat panel scroll container"
    - "All additions are appended to forge-console.css (no edits to existing rules — additive only)"
  artifacts:
    - path: "forge_bridge/console/static/forge-console.css"
      provides: "LOGIK-PROJEKT amber spinner + chat-panel layout classes (D-07/D-08)"
      contains: "spinner-amber"
  key_links:
    - from: "forge_bridge/console/static/forge-console.css"
      to: "panel.html (plan 16-05)"
      via: "panel.html applies these classes to chat transcript + spinner + tool-trace blocks"
      pattern: "spinner-amber|chat-transcript|chat-message--"
---

<objective>
Append CSS rules to `forge_bridge/console/static/forge-console.css` for the FB-D chat panel:
LOGIK-PROJEKT amber `.spinner-amber` (D-08), chat transcript + message-bubble layout classes
(D-07), and chat-input textarea styling (D-10). All values reuse existing `--color-*` and
`--space-*` tokens — zero new colors introduced. Approximately 12-15 lines of CSS.

Purpose: Wave 1 foundation for the chat panel. Plan 16-05 (Web UI panel template) consumes
these classes in `panel.html` + `forge-chat.js`. Splitting this into its own plan lets the
panel template plan stay focused on Jinja + Alpine wiring without CSS scope creep.

Output: forge-console.css extended with `@keyframes spin`, `.spinner-amber`, `.chat-transcript`,
`.chat-message{,--user,--assistant,--tool}`, `.chat-tool-trace`, `#chat-input{,:focus}` rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-fb-d-chat-endpoint/16-CONTEXT.md
@.planning/phases/16-fb-d-chat-endpoint/16-PATTERNS.md
@.planning/phases/16-fb-d-chat-endpoint/16-RESEARCH.md
@forge_bridge/console/static/forge-console.css
@CLAUDE.md

<interfaces>
Existing tokens in forge-console.css line 1 (LOGIK-PROJEKT dark+amber palette):

```
--color-bg: #242424
--color-surface: #2a2a2a
--color-surface-deep: #1a1a1a
--color-accent: #cc9c00            (amber, used by spinner highlight + assistant border)
--color-accent-muted: #664e00      (darker amber, used for spinner track)
--color-text: #cccccc
--color-text-muted: #999999
--color-border: #333333
--color-border-md: #555555
--color-status-error: #660000
--font-mono: "Consolas","Monaco","Courier New",monospace
--space-xs:4px  --space-sm:8px  --space-md:16px  --space-lg:24px
--text-label:12px
```

Pattern reuse — existing classes the chat panel piggybacks on (NO changes needed):

```
.card / .health-card / .error-card    line 38, surface backgrounds
.amber                                line 43, amber color helper
.font-mono                            font-family helper
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append chat-panel CSS rules to forge-console.css</name>
  <read_first>
    - forge_bridge/console/static/forge-console.css (read entire file — confirm line count, confirm the file ends with a complete rule, confirm `@keyframes spin` does NOT already exist)
    - .planning/phases/16-fb-d-chat-endpoint/16-CONTEXT.md (D-07 collapsed details, D-08 amber spinner, D-09 error banner, D-10 textarea input)
    - .planning/phases/16-fb-d-chat-endpoint/16-PATTERNS.md (lines 408-428 — exact CSS pattern with token mapping table)
  </read_first>
  <action>
Append a CSS block to the END of `forge_bridge/console/static/forge-console.css`. Single append — do NOT modify any existing rule. Each rule reuses an existing `--*` token.

Before appending, run `grep -c "@keyframes spin\|spinner-amber\|chat-transcript\|chat-message" forge_bridge/console/static/forge-console.css` and verify the count is `0` (these classes do not yet exist).

Append the following block. Preserve formatting verbatim — single-line rules match the file's existing style at line 38+. The leading blank line + `/* === ... === */` comment is the existing section-divider convention in this CSS file:

```
[BLANK LINE]
/* === Phase 16 (FB-D) -- chat panel === */
.chat-transcript{display:flex;flex-direction:column;gap:var(--space-md);max-height:60vh;overflow-y:auto;padding:var(--space-md)}
.chat-message{padding:var(--space-md);border-radius:4px;background:var(--color-surface)}
.chat-message--user{border-left:2px solid var(--color-text-muted)}
.chat-message--assistant{border-left:2px solid var(--color-accent)}
.chat-message--tool{font-size:var(--text-label);color:var(--color-text-muted);font-family:var(--font-mono)}
.chat-tool-trace{margin-top:var(--space-sm);background:var(--color-surface-deep);border:1px solid var(--color-border);border-radius:4px;padding:var(--space-sm)}
.chat-tool-trace summary{cursor:pointer;color:var(--color-accent);font-family:var(--font-mono);font-size:var(--text-label)}
.chat-tool-trace pre{margin:var(--space-sm) 0 0;white-space:pre-wrap;word-break:break-word;font-family:var(--font-mono);font-size:var(--text-label);color:var(--color-text)}
.spinner-amber{display:inline-block;width:12px;height:12px;border:2px solid var(--color-accent-muted);border-top-color:var(--color-accent);border-radius:50%;animation:spin 0.7s linear infinite;vertical-align:middle}
@keyframes spin{to{transform:rotate(360deg)}}
#chat-input{width:100%;font-family:var(--font-mono);background:var(--color-surface-deep);border:1px solid var(--color-border-md);padding:var(--space-sm) var(--space-md);min-height:32px;color:var(--color-text);resize:vertical}
#chat-input:focus{outline:none;border-color:var(--color-accent)}
```

Implementation notes:
- The leading blank line + `/* === ... === */` comment is the existing section-divider convention in this CSS file.
- `.chat-message--assistant` uses the amber `--color-accent` border to visually distinguish the LLM's voice from the user (LOGIK-PROJEKT amber-on-dark per project memory).
- `.chat-tool-trace` uses `--color-surface-deep` (#1a1a1a — already in use) for a slightly recessed look on the collapsible block.
- `.chat-tool-trace summary` is amber + monospace — clickable affordance for the D-07 expandable transparency block.
- `.spinner-amber` uses the existing `--color-accent-muted` (#664e00) for the spinner track and `--color-accent` (#cc9c00) for the highlighted leading edge — pure CSS animation, no SVG, no new asset.
- `@keyframes spin` is the only new at-rule; the file does not currently ship one (verified via grep).
- `#chat-input` ID matches the textarea ID in the panel.html that plan 16-05 will create.
- DO NOT introduce any new `--color-*` custom property — the LOGIK-PROJEKT palette is locked at line 1 of the file.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-bridge && python -c "import re,pathlib; css = pathlib.Path('forge_bridge/console/static/forge-console.css').read_text(); assert 'spinner-amber' in css and '@keyframes spin' in css and 'chat-transcript' in css and 'chat-message--assistant' in css and 'chat-tool-trace' in css and '#chat-input' in css; print('OK: all 6 classes present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spinner-amber" forge_bridge/console/static/forge-console.css` returns at least `1`
    - `grep -c "@keyframes spin" forge_bridge/console/static/forge-console.css` returns `1`
    - `grep -c "chat-transcript" forge_bridge/console/static/forge-console.css` returns at least `1`
    - `grep -c "chat-message--assistant" forge_bridge/console/static/forge-console.css` returns at least `1`
    - `grep -c "chat-message--user" forge_bridge/console/static/forge-console.css` returns at least `1`
    - `grep -c "chat-tool-trace" forge_bridge/console/static/forge-console.css` returns at least `1`
    - `grep -c "#chat-input" forge_bridge/console/static/forge-console.css` returns at least `1`
    - `grep -c "var(--color-accent)" forge_bridge/console/static/forge-console.css` returns at least `2` more than the pre-edit count (multiple chat rules use this token)
    - No new `--color-*` custom property added — `grep -c "^--color-\|: #" forge_bridge/console/static/forge-console.css | head -1` count unchanged from pre-edit
    - File is still valid CSS — `python -c "import pathlib; css = pathlib.Path('forge_bridge/console/static/forge-console.css').read_text(); assert css.count('{') == css.count('}'), 'unbalanced braces'"` exits 0
  </acceptance_criteria>
  <done>forge-console.css extended with 12-13 new CSS rules: spinner amber + keyframes spin animation, chat-transcript flex container, four chat-message variants (user/assistant/tool/base), chat-tool-trace details/summary/pre, and #chat-input textarea styling. All values use existing tokens; brace count balanced; no new --color-* custom properties.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser CSS render → DOM | Static CSS file, no untrusted input. Defense-in-depth from SRI on the script tag is plan 16-05's concern, not this CSS file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | I (Information disclosure) | static CSS asset | accept | CSS file is served via the existing `/ui/static` mount with no auth (Phase 10 posture). No sensitive data in CSS — pure presentation. |
| T-16-03-02 | T (Tampering) | brace-balance / palette-token leak | mitigate | Acceptance criteria assert `{` count == `}` count and no new `--color-*` tokens. A future contributor cannot accidentally split the LOGIK-PROJEKT palette. |
</threat_model>

<verification>
- Task acceptance criteria pass (all 9 grep + brace + token-count checks).
- Visual verification deferred to plan 16-05 (template + JS) — that plan is the first surface where the rules render.
- `git diff forge_bridge/console/static/forge-console.css` shows ONLY appended rules (no modifications to lines 1-69).
</verification>

<success_criteria>
LOGIK-PROJEKT amber spinner CSS ships; chat-message bubble layout classes ship; all values reuse
existing --color-* and --space-* tokens. Plan 16-05 can apply these classes in panel.html and
forge-chat.js without re-declaring colors.
</success_criteria>

<output>
After completion, create `.planning/phases/16-fb-d-chat-endpoint/16-03-SUMMARY.md` documenting:
- Lines appended to forge-console.css (count + section comment)
- Confirmation that no existing rules were modified
- Note that plan 16-05 (panel.html + forge-chat.js) consumes .spinner-amber, .chat-transcript, .chat-message--*, .chat-tool-trace, #chat-input classes
</output>
</content>
</invoke>