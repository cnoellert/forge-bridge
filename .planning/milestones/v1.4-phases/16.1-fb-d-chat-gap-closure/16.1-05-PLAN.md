---
phase: 16.1
plan: 05
type: execute
wave: 3
depends_on: ["16.1-01", "16.1-02", "16.1-03", "16.1-04"]
files_modified:
  - .planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md
autonomous: false
requirements: [CHAT-04]
nyquist_compliant: true
must_haves:
  truths:
    - "A fresh operator (NOT CN/dev, NOT anyone who participated in Phase 16's plan-16-05 in-loop UAT) runs the canonical CHAT-04 prompt on assist-01"
    - "The artist receives a useful, plain-English answer in `<60s` with no error banner, no timeout, no rate-limit fallback"
    - "The outcome is recorded verbatim in `.planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md` mirroring the Phase 10/10.1 UAT format (operator ID, fixture state, duration, observations, Outcome: PASS or PASS-with-deviations or FAIL)"
    - "Pure PASS preferred (D-13). PASS-with-deviations acceptable per D-14 IF deviations honestly recorded and none mask a Phase-16 must-have regression. FAIL routes to Phase 16.2."
  artifacts:
    - path: ".planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md"
      provides: "Operator-recorded UAT outcome for CHAT-04 re-attempt; canonical evidence the v1.4 milestone close cites"
      min_lines: 30
  key_links:
    - from: ".planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md"
      to: "ROADMAP §Phase 16.1 success criterion 6 + REQUIREMENTS.md CHAT-04"
      via: "Outcome field + v1.4 milestone close cite"
      pattern: "Outcome: PASS"
---

<objective>
Implement D-11..D-15 — the fresh-operator artist re-UAT that closes Phase 16.1's verification. Deploy the 16.1 work (Plans 01..04) to assist-01, preload the model, hand the Web UI to a fresh artist, run the canonical CHAT-04 prompt, and record the outcome verbatim in `16.1-HUMAN-UAT.md`. This is the Phase 10/10.1-precedent hard fresh-operator gate that v1.4 milestone close depends on.

Purpose: The whole phase exists to close this gap. Plans 01..04 ship the technical fix and three regression guards; Plan 05 verifies the fix actually works for a non-developer artist on real hardware with a live LLM. Without this UAT recording `Outcome: PASS` (or `PASS-with-deviations` per D-14), CHAT-04 is not satisfied.

Output:
- New `.planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md` mirroring the Phase 10 / 10.1 UAT format
- Recorded operator ID (NOT `CN/dev`, NOT the plan-16-05 participant), fixture state (which backends reachable on assist-01), duration, observations, decision
- A `## Outcome:` line with one of `PASS`, `PASS with deviations`, or `FAIL`
- Git commit with the populated UAT file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16.1-fb-d-chat-gap-closure/16.1-CONTEXT.md
@.planning/phases/16.1-fb-d-chat-gap-closure/16.1-RESEARCH.md
@.planning/phases/16.1-fb-d-chat-gap-closure/16.1-PATTERNS.md
@.planning/phases/16.1-fb-d-chat-gap-closure/16.1-04-PLAN.md
@.planning/phases/16-fb-d-chat-endpoint/16-HUMAN-UAT.md
@./CLAUDE.md

<uat_format_template>
<!-- Mirror the Phase 10 / 10.1 UAT format (verified per CONTEXT D-13).
     Format pulled from `.planning/milestones/v1.3-phases/10.1-artist-ux-gap-closure/10.1-UAT.md`. -->

# Phase 16.1 (FB-D gap closure) — CHAT-04 Re-UAT

**Date:** YYYY-MM-DD
**Operator:** {anonymized handle, e.g., AT/artist} — fresh operator (NOT CN/dev, NOT anyone in Phase 16 plan-16-05 in-loop UAT)
**Host:** assist-01
**Phase 16.1 commit:** {git rev-parse --short HEAD on the deployed branch}
**Branch:** gsd/v1.4-staged-ops-platform

## Fixture State (which backends reachable at run time)

- forge-bridge process: ✅ running on `:9996` (Web UI) + `:9999` (Flame HTTP bridge — N/A on assist-01) + MCP stdio
- Flame HTTP bridge `:9999`: {reachable / unreachable — assist-01 default: unreachable}
- projekt-forge catalog `:9998`: {reachable / unreachable — assist-01 default: unreachable}
- Ollama: ✅ qwen2.5-coder:32b loaded
- Database: ✅ {empty / pre-seeded — describe}
- Backend-aware filter result (from chat handler logs): {N tools offered after filter, e.g., "7 in-process forge_* + synth_*"}

## Test

1. Navigate to `http://assist-01:9996/ui/chat`
2. Type the canonical CHAT-04 prompt VERBATIM:
   > what synthesis tools were created this week?
3. Press Enter
4. Observe response

## Observations

- Response time: {N seconds, observed via stopwatch or browser network tab}
- Spinner behavior: {amber spinner appears + disappears cleanly / spinner stalls / no spinner}
- Response content (paste verbatim or summarize):
  > {The LLM's actual response text}
- Tool-call trace blocks ("which tools the LLM picked"): {names + how many — D-07 expand-on-click works?}
- Error banners: {none / 429 / 504 / 422 / 500 — describe}
- Subjective assessment: {does the answer actually answer the question?}
- Follow-up questions tested (D-06 per-tab history): {yes/no — describe}

## Cross-Phase 16 must-have regression check

- CHAT-01 (rate limit): {ran 11+ rapid requests; 11th returned 429? — yes/no/not tested}
- CHAT-02 (timeout): {if any prompt blocked >125s, did it return 504? — yes/no/not tested}
- CHAT-03 (sanitization): N/A for this UAT (covered by automated tests)
- CHAT-05 (parity): N/A for this UAT (covered by automated parity test)

## Deviations (if any — D-14 soft-PASS criterion)

- {Deviation 1: e.g., "Response was 4 sentences, not the depth the artist expected — but it was factually correct"}
- {Deviation 2: ...}

(Each deviation must include: what diverged, why it doesn't mask a Phase 16 must-have regression, whether it would block v1.4 milestone close.)

## Outcome: {PASS | PASS with deviations | FAIL}

**Reasoning:** {One paragraph}

## Action

- {If PASS: "16.1 verification proceeds; v1.4 milestone close path open. CHAT-04 satisfied per ROADMAP."}
- {If PASS with deviations: list what's deferred — typically v1.4.x or v1.5 follow-up.}
- {If FAIL: "Open Phase 16.2 with the gap captured here. v1.4 milestone close blocked."}

## Operator sign-off

{Anonymized handle} — {YYYY-MM-DD}
</uat_format_template>

</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5.1 (CHECKPOINT): Deploy Phase 16.1 to assist-01 + verify automated tests pass before fresh-operator UAT</name>

  <what-built>
    Plans 01..04 are merged on the `gsd/v1.4-staged-ops-platform` branch. The full Phase 16.1 surface is ready for re-UAT:
    - Plan 01: Backend-aware tool-list filter (`forge_bridge/console/_tool_filter.py`) wired into `chat_handler`
    - Plan 02: TemplateResponse migration complete (13 sites); `starlette<0.53` pin dropped
    - Plan 03: Boot-wiring smoke test catches Bug B class
    - Plan 04 Task 4.2: Bisection sweep recorded; threshold locked
    - Plan 04 Task 4.3: Strategy B chat E2E test passes on assist-01
  </what-built>

  <how-to-verify>
    Pre-flight on assist-01 (the operator OR the executor SSHing in):

    ```bash
    # 1. Sync the branch
    cd ~/forge-bridge   # or wherever the repo is
    git fetch origin
    git checkout gsd/v1.4-staged-ops-platform
    git pull origin gsd/v1.4-staged-ops-platform

    # 2. Reinstall (Plan 02 dropped the starlette pin; pip will resolve >= 1.0.0 fresh)
    pip install -e ".[dev,test,llm]" --upgrade-strategy eager

    # 3. Confirm Starlette 1.0.0+ resolved (Plan 02 verification)
    python -c "import starlette; print(starlette.__version__)"
    # Expected: 1.0.0 or higher

    # 4. Run the always-on regression guards (catch Bug A + Bug B + filter behavior)
    pytest tests/console/test_lifespan_wiring.py tests/console/test_ui_handlers_render.py tests/console/test_chat_tool_filter.py tests/console/test_chat_handler.py -q
    # Expected: all PASS, no skips

    # 5. Preload qwen2.5-coder:32b (Plan 04 Pitfall 9)
    ollama run qwen2.5-coder:32b "warm" >/dev/null 2>&1

    # 6. Run the Strategy B chat E2E (Plan 04 Task 4.3) — the live sanity check
    FB_INTEGRATION_TESTS=1 pytest tests/integration/test_chat_endpoint_live.py -v --tb=short
    # Expected: PASS in <60s

    # 7. Start the bridge process (UI server)
    python -m forge_bridge   # or however the bridge is launched on assist-01

    # 8. Confirm the chat panel renders
    curl -s http://localhost:9996/ui/chat | grep "id=\"health-strip\""
    # Expected: at least one match (Plan 02 sentinel; UI is live)

    # 9. Confirm chat handler smoke test
    curl -s -X POST http://localhost:9996/api/v1/chat \
         -H "Content-Type: application/json" \
         -d '{"messages":[{"role":"user","content":"ping"}]}' | head -c 500
    # Expected: a JSON response with "messages" + "stop_reason" + "request_id" — NOT 500/504/429
    ```

    If ANY pre-flight step fails, STOP — file the failure as a Phase 16.1 sub-bug and re-plan, do NOT hand the Web UI to an artist with broken plumbing.

    Once all 9 pre-flight steps PASS, ask the user to:
    1. Identify a fresh operator (artist on the team — NOT CN, NOT the implementer of plan-16-05). Get their anonymized handle (e.g., "AT/artist", "EM/colorist").
    2. Schedule a 15-minute window where the artist can sit at assist-01.
    3. Reply with "operator ready: {handle}" — then proceed to Task 5.2.
  </how-to-verify>

  <resume-signal>
    Reply with "operator ready: {handle}" once a fresh artist is identified and scheduled for the assist-01 session, OR "deferred" if scheduling is blocked. The plan does not advance to Task 5.2 until the operator is ready.
  </resume-signal>


  <files>(no source changes — deployment + automated-guard pre-flight only)</files>
  <action>See <how-to-verify> above. Operator (or executor SSHing in) syncs branch `gsd/v1.4-staged-ops-platform` on assist-01, reinstalls deps (`pip install -e .[dev,test,llm] --upgrade-strategy eager`), confirms Starlette >=1.0.0 is resolved, runs the 4 always-on regression guard suites + the Strategy B chat E2E (Plan 04 Task 4.3), preloads `qwen2.5-coder:32b`, starts the bridge process, smoke-tests the chat panel + chat HTTP endpoint, then asks the user to identify a fresh artist operator.</action>
  <verify>`python -c 'import starlette; assert starlette.__version__ >= "1.0"'` exits 0; `pytest tests/console/test_lifespan_wiring.py tests/console/test_ui_handlers_render.py tests/console/test_chat_tool_filter.py tests/console/test_chat_handler.py -q` exits 0; `FB_INTEGRATION_TESTS=1 pytest tests/integration/test_chat_endpoint_live.py -q` on assist-01 exits 0; `curl -s http://localhost:9996/ui/chat | grep 'id="health-strip"' | wc -l` returns >= 1</verify>
  <done>All 9 pre-flight steps passed; bridge process running on :9996; fresh operator handle confirmed by user (NOT CN/dev, NOT plan-16-05 participant); operator scheduled for assist-01 session.</done>
  <acceptance_criteria>
    All 9 pre-flight steps passed; bridge process running on :9996; fresh operator handle confirmed by user (NOT CN/dev, NOT plan-16-05 participant); operator scheduled for assist-01 session.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.2 (CHECKPOINT): Fresh-operator runs the canonical CHAT-04 prompt + records outcome in 16.1-HUMAN-UAT.md</name>

  <what-built>
    Phase 16.1 deployed to assist-01 (Plans 01..04). All automated regression guards green. The bridge process is running on `:9996`. The fresh artist operator is ready.
  </what-built>

  <how-to-verify>
    Operator (the fresh artist, NOT the implementer):

    1. Open a browser on assist-01. Navigate to `http://localhost:9996/ui/chat` (or `http://<assist-01-ip>:9996/ui/chat` from a teammate's machine if assist-01 is headless).

    2. Type the canonical CHAT-04 prompt VERBATIM (do NOT rephrase — the prompt is locked per CONTEXT D-11):
       > what synthesis tools were created this week?

    3. Press Enter (D-10 — Enter sends, Shift+Enter inserts newline).

    4. Time the response. Watch for:
       - Amber spinner appears on Send button
       - Within `<60s`, an assistant message bubble appears with amber left-border
       - Bubble content is plain English, factually responsive
       - NO error banner (no 429, no 504, no 422, no generic "Chat error")
       - NO timeout
       - The artist subjectively confirms "this answers what I asked"

    5. (Optional but recommended) Try a follow-up question to check D-06 per-tab history:
       > can you tell me more about the most recent one?
       Confirm prior context is preserved.

    6. (Optional) Check the tool-call trace blocks (D-07): click the collapsed `<details>` blocks under the assistant message to see which tools the LLM picked. With Plan 01's filter on assist-01 reality (no Flame), expected tools are `synth_*` + in-process `forge_*` (e.g., `forge_manifest_read`, `forge_list_staged`).

    7. If the artist asks for a fallback prompt (D-15) because the canonical prompt no longer makes sense (e.g., tool naming evolved), CHECKPOINT THE EXECUTOR — do not pre-author a backup. The implementer (or the orchestrator) decides the fallback in real time.

    Implementer (or executor):

    8. Create `.planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md` using the format in the `<uat_format_template>` block above. Fill EVERY field:
       - Date, Operator handle, Host, commit, branch
       - Fixture State (run `curl http://localhost:9996/api/v1/health` and read which services are reachable; copy verbatim into the fixture-state section)
       - Test step list (verbatim from above)
       - Observations (paste the LLM response, time it, note tool-trace contents)
       - Cross-Phase-16 must-have regression check (rate-limit smoke if time allows; otherwise mark "not tested")
       - Deviations (if any — D-14)
       - **Outcome:** one of `PASS`, `PASS with deviations`, or `FAIL`
       - Reasoning paragraph
       - Action paragraph
       - Operator sign-off line

    9. Commit:
       ```bash
       git add .planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md
       git commit -m "uat(16.1): {Outcome} — fresh-operator CHAT-04 re-UAT recorded"
       ```

    10. Three terminal states:
        - **PASS**: 16.1 verification proceeds. CHAT-04 satisfied. v1.4 milestone close path open. Plan checker / verify-work gates can now run.
        - **PASS with deviations** (D-14): IF deviations are honestly recorded AND none mask a Phase-16 must-have regression (CHAT-01/02/03/05 + LLMTOOL-06). Otherwise treat as FAIL. List deferred items as v1.4.x or v1.5 follow-ups.
        - **FAIL**: Open Phase 16.2 with the gap captured. v1.4 milestone close blocked. Re-plan.
  </how-to-verify>

  <resume-signal>
    Reply with the recorded `Outcome:` value: `Outcome: PASS`, `Outcome: PASS with deviations`, or `Outcome: FAIL`. The orchestrator advances to `/gsd-verify-work` only on PASS or PASS-with-deviations; FAIL triggers Phase 16.2 planning.
  </resume-signal>


  <files>.planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md</files>
  <action>See <how-to-verify> above. Fresh artist runs canonical CHAT-04 prompt 'what synthesis tools were created this week?' at http://localhost:9996/ui/chat on assist-01. Implementer creates 16.1-HUMAN-UAT.md following the <uat_format_template> in the plan context — fills every section (operator handle, fixture state, observations, deviations if any, Outcome line, sign-off), then commits with `git add .planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md && git commit -m 'uat(16.1): {Outcome} — fresh-operator CHAT-04 re-UAT recorded'`.</action>
  <verify>`test -f .planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md` exits 0; `grep -E '^## Outcome: (PASS|PASS with deviations|FAIL)$' .planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md | wc -l` returns 1; `grep -c 'CN/dev' .planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md` returns 0 (fresh-operator gate honored)</verify>
  <done>16.1-HUMAN-UAT.md committed with one of (PASS / PASS with deviations / FAIL) and operator NOT being CN/dev or the plan-16-05 participant; on PASS or PASS-with-deviations the orchestrator advances to /gsd-verify-work; on FAIL Phase 16.2 is opened.</done>
  <acceptance_criteria>
    16.1-HUMAN-UAT.md committed with one of (PASS / PASS with deviations / FAIL) and operator NOT being CN/dev or the plan-16-05 participant; on PASS or PASS-with-deviations the orchestrator advances to /gsd-verify-work; on FAIL Phase 16.2 is opened.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh artist → Web UI on assist-01 | Untrusted-but-loyal operator; the chat path runs through Phase 16's rate limit + Phase 15's sanitization + Phase 16.1's tool filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-12 | Information Disclosure | Artist's prompt or LLM response logged in UAT artifact | mitigate | UAT artifact lives in `.planning/phases/...` — git-tracked, project-local, no external transmission. Operator handle is anonymized (e.g., "AT/artist", not real name). The LLM response is paste-verbatim, but the canonical prompt is non-sensitive. |
| T-16.1-13 | Tampering | Operator could falsify outcome | accept | Trust model: artists on the forge-bridge team are the bridge's intended users; falsifying the UAT defeats their own quality gate. The verifier (`/gsd-verify-work`) checks the structure of the artifact, not its truthfulness. |
</threat_model>

<verification>
- `.planning/phases/16.1-fb-d-chat-gap-closure/16.1-HUMAN-UAT.md` exists
- The file contains a line matching `^## Outcome: (PASS|PASS with deviations|FAIL)$`
- The Operator field is NOT `CN/dev` and NOT the plan-16-05 participant
- The Phase 16.1 commit referenced in the UAT is present in `git log` on the `gsd/v1.4-staged-ops-platform` branch
- If `Outcome: PASS` or `Outcome: PASS with deviations`, all Phase 16 must-haves (CHAT-01/02/03/05 + LLMTOOL-06) are still passing per `pytest tests/ -q`
- If `Outcome: FAIL`, a Phase 16.2 plan-spawn is queued (orchestrator activity)
</verification>

<success_criteria>
- ROADMAP §16.1 success criterion 6 satisfied — fresh operator runs CHAT-04 on assist-01, records outcome
- v1.4 milestone close path is open (PASS) or routed to next remediation phase (FAIL)
- Hard fresh-operator gate honored (D-12 / Phase 10/10.1 precedent)
- The UAT record is canonical evidence the milestone close cites
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-fb-d-chat-gap-closure/16.1-05-SUMMARY.md` referencing:
- The `16.1-HUMAN-UAT.md` outcome (PASS / PASS with deviations / FAIL)
- The operator handle (anonymized)
- Whether any deviations were recorded
- Action taken (proceed to verify / open 16.2 / defer items to v1.4.x)
</output>
