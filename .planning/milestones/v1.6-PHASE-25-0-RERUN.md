# Phase 25.0 — Ecological Operator Probes

Commit under test: `5046aa25`

Temporary daemon:

- Console: `http://127.0.0.1:10021`
- MCP HTTP: `http://127.0.0.1:10020`
- Provenance: daemon `5046aa25` matched operator CWD
- Flame bridge: reachable at `http://127.0.0.1:9999`

## Primary Probe

```bash
fbridge chat "preview rename shots on 30sec 21 with prefix genesis -> format as table"
```

Disposition: not executed in this sandbox.

Reason: the app egress guard rejected the probe because the terminal
`format_result` step would send condensed Flame production data to
Anthropic cloud. This is the correct policy boundary for this execution
environment. The probe remains operator-runnable under explicit
data-egress approval.

## Secondary Probe A — Preview

```bash
fbridge chat "preview rename shots on 30sec 21 with prefix genesis"
```

Result: PASS.

Observed:

```json
{
  "tool": "flame_rename_shots",
  "arguments": {
    "params": {
      "dry_run": true,
      "sequence_name": "30sec_21",
      "prefix": "genesis"
    }
  },
  "result": {
    "dry_run": true,
    "count": 25,
    "first_change": {
      "index": 0,
      "current": "genesis_0010_source_L01",
      "proposed": "genesis_010_source_L01",
      "type": "shot_name"
    }
  }
}
```

The same preview was fired twice. The first `current` value remained
`genesis_0010_source_L01`, confirming the dry_run path did not write
Flame state.

## Secondary Probe B — Mutation

```bash
fbridge chat "rename shots on 30sec 21 with prefix genesis 4-digit padding increment 10 starting at 10"
```

Result: PASS.

Observed:

```json
{
  "tool": "flame_rename_shots",
  "arguments": {
    "params": {
      "sequence_name": "30sec_21",
      "prefix": "genesis",
      "padding": 4,
      "increment": 10,
      "start": 10
    }
  },
  "renamed": 25,
  "skipped": 7,
  "first_change": {
    "seg_idx": 0,
    "old": "genesis_0010_source_L01",
    "new": "genesis_0010_source_L01"
  }
}
```

This mutation probe was intentionally idempotent against the current
sequence naming state. It validates the non-preview path still executes
through the same resolved scope and arithmetic without introducing a
new visible state delta.

## Disposition

25.0 establishes the preview/dry_run bridge:

- operator language: `preview`
- execution modifier: `dry_run=True`
- selected operation: `flame_rename_shots`
- output: structured proposed-change list

The mutation-gate substrate is working for the highest-value target.
The remaining primary preview -> format proof requires explicit cloud
egress approval because `format_result` is a sensitive-to-cloud chain
terminal.
