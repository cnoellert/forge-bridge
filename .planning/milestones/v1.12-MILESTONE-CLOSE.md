---
milestone: v1.12
milestone_name: Mutation Delegation
status: closed
opened: 2026-06-02
closed: 2026-06-02
audit_shape: writer's-room milestone-arc archaeology synthesis (detail carried by C2-CLOSE; this is the milestone-level reckoning, not a re-statement)
preceded_by: v1.11 Source-of-Truth Routing (SR.1 closed 2026-06-01)
succeeded_by: v1.13 Translation Fidelity (the E2E-01 param-resolution defect, reframed — language→substrate is the next frontier)
voice_constraint: operational-maturity-for-handoff (no architectural-completeness shapes) [[feedback-operational-maturity-not-completeness]]
stats: "6 commits (1 code: 9ecd503, 6 files +296/-6); __all__=19, version 1.5.1 throughout; suite 2688→2697 (+9). Single day, 2026-06-02. Live-verified via cross-team E2E-01."
---

# v1.12 Mutation Delegation — Milestone Close

> **What v1.12 was.** The keystone that fires ADR-003 / Door-C end-to-end: chat mutations
> reach the operator-ratified, executor-delegated apply path instead of being hard-blocked
> at the DI.1 gate. v1.10 made authority routing-invariant; v1.12 gives a ratified mutation
> a path to actually execute. Delivered as **phase 1 (C2)**; phase 2 (Shape B) deferred.

**Phases:** 1 delivered (**C2** — Compile → Commit-Bearing Executor Chain), full writer's-room cadence
(framing → discuss → plan → plan-check → lock → implement → verify → close). Shape B (phase 2) deferred.
**Release tag:** none — patch-equivalent (`__all__` at 19; `pyproject.toml` 1.5.1; no public API change).
**Stats:** 6 commits (1 code: `9ecd503`, 6 files, +296 / −6). Suite 2688 → 2697 (+9). `__all__`==19.
**Timeline:** 2026-06-02, single day. **Live-verified:** cross-team E2E-01.

## Theme — a ratified mutation can execute

Pre-C2, a mutating intent compiled to a *bare* host-mutation step that the DI.1 gate correctly hard-blocked
pre-ratify — the mutation had authority machinery but no ratify path to flow through. C2 closes that.

## Key accomplishment — C2 (Shape A+ intent-ratification)

A rename intent now compiles to a **commit-bearing executor chain** (`flame_rename_shots` →
`forge_apply_rename <args> -> commit`) via a deterministic post-compile transform
(`console/_executor_route.py`), reaching preview / `AssentRecord` / ratify instead of the DI.1 block.
Shipped **Shape A+**: operator assents to the mutation *capability + intent*; the resolved `count`
(`len(manifest.resolved_plan)`, computed bridge-side) surfaces at apply as *"Renamed N shots."* The rewrite
guard enforces the *mapped executor's own* mutating authority (Finding 1) — turning a one-time annotation
grounding into a durable runtime invariant. Map is rename-only (publish excluded — no arg parity). Detail in
`C2-CLOSE.md`.

## Verification & regression

- Bridge-side acceptance met: preview emits a commit-bearing chain; canonical ratify path clean.
- **Live-verified (E2E-01, cross-team):** real `013_…portofino` timeline, `preview_emitted`
  (`graph_intent_id 111d44f0560a`), ratify gate held, **zero mutation**, not PR20-shadowed.
- Suite 2688 → 2697 (+9); `__all__`==19; ruff clean; DT sign-off, no reservations.

## Honest scope (no overclaim)

v1.12 made a ratified mutation *executable* via delegation; it did **not** make the translation correct.
E2E-01 proved the substrate side and exposed the translation side: the graph shape was right and the
substrate would have executed faithfully, but the *params* were wrong (`prefix=noise`, sequence mangle).
That is the v1.13 frontier, not a v1.12 gap — the substrate behaved exactly as designed.

## Deferred at close (named, not rejected)

- **Shape B — manifest-ratification (Window-2 preview→apply drift)** was v1.12's planned phase 2. Deferred
  as a **named future motion with its maturation condition explicitly unmet**: it ships only if the room
  judges Window-2 drift unacceptable in production. NLT (v1.13) outranks it on live evidence
  (language→substrate correctness is the more pressing pain). Deferral, not rejection
  (`[[feedback-explicitly-unbound-vs-implicitly-rejected]]`).
- **26-04 live E2E** — substantially achieved via E2E-01 on the manual `serve_chat` console; the
  **bootstrap-console-executor-gap** (supervised daemon runs executor-less) remains an operational
  follow-up (`[[project-bootstrap-console-executor-gap]]`).
- **`/gsd-secure-phase 26`** before the forge-pipeline executor branch merges.
- **Publish executor delegation** — its own future motion (G2: no `ApplyPublishInput`↔`PublishSequence`
  arg parity).

## Lessons learned

- **Grounding shrinks scope.** The executor contract grounding made C2 *smaller* — no `mode` token, no arg
  reshaping, publish dropped. Scope-shrinks-under-grounding as a convergence signal.
- **Boundary invariant > one-time grounding** (Finding 1). G1 proved the annotation today; the rewrite gate
  enforces it every time — `[[feedback-baseline-drift-invalidates-controls]]` applied prospectively.
- **Grounding-flip relocates a finding** (Finding 2: C2-hook → pre-existing apply-replay surface → T5).
  Fired repeatedly across the arc; candidate amendment to `[[feedback-ground-specs-in-actual-files]]`.
