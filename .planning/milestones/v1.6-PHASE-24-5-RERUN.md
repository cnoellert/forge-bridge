# Phase 24.5 canonical-probe re-fire — 2026-05-18

**Commit 4 of 4 per `.planning/milestones/v1.6-PHASE-24-5-FRAMING.md` §13.**

Forcing-function validation of the consumer-side projection of
`orchestration_terminated` at the two operator surfaces (CLI `fbridge
chat` + Console UI `/ui/chat`) shipped in `e8d82cd` (CLI projection)
and `2a74c7a` (Console UI projection). Per framing §7 + §8, this rerun
is the empirical close of Phase 24.5 — the architectural shift from
"the 24.4 envelope encodes truth that operators cannot perceive" to
"both consumer surfaces project the same five-fact contract verbatim,
truthfully, without impersonation."

**Single perturbation:** the only mutation since the 24.4 rerun bundle
(`2026-05-18-phase-24-4-rerun/`) is the 24.5 consumer-side code from
this branch's 3 commits. **No router/handler/envelope/SSE changes** —
the 24.4 substrate is byte-identical pre- and post-24.5 (verified by
the §7.4 anti-scope falsification suite).

Two probes fired against each surface:

1. **CLI cold** — first canonical-query invocation after daemon
   editable-install re-anchoring
2. **CLI warm** — immediate repeat (KV cache primed)
3. **CLI `--json`** — envelope passthrough verification (no projection)
4. **Console warm** — canonical query against `/ui/chat` post-CLI
   warming (browser captured + DOM exported)

## §7 per-layer success-criterion evaluation

| Layer | Criterion | CLI cold | CLI warm | Console warm |
|---|---|---|---|---|
| **L_CLI** operator-visibility | Operator can answer the 5 questions (what/why/which authority/what repeated/iterations) from stdout/stderr alone | n/a (cold ran to budget; no K=2 trigger; existing `done`/`error` rendering paths unchanged) | ✅ Full 5-fact contract rendered with `[orchestration termination]` taxon prefix | n/a |
| **L_CONSOLE** Console-visibility | Operator can answer the same 5 questions from rendered chat panel alone; visually distinct chrome from assistant completion | n/a | n/a | ✅ `<section class="orchestration-termination">` populated with all 5 facts via `x-text` bindings; distinct CSS class namespace; sibling of message list, not nested |
| **L_PARITY** cross-surface contract | Both surfaces render same 5 facts from same envelope | n/a | ✅ trigger / reason / iterations / accumulated_results all sourced from same `termination.*` keys; consumer contract documented inline in framing §4 | ✅ same five facts surfaced via independent Alpine bindings against the same envelope fields |
| **C** anti-scope verification | 24.4 envelope shape + SSE event taxa + router log line byte-identical pre/post-24.5 | ✅ envelope unchanged; 24.4 falsification suite (3 SSE termination tests + K-fold router tests) all pass | ✅ same | ✅ same |

**Framing §8.1 + §8.2 expected shapes satisfied verbatim across both
consumer surfaces under live policy termination.**

## CLI cold probe (~120s wall-clock)

**Result:** HTTP 200, single status=ok dispatch within budget,
**130.55s wall-clock** (budget exhausted). No K=2 trigger reached.

```
fbridge chat "What are the clips on Reel 1"   0.50s user 0.06s system 0% cpu 2:10.55 total
```

**Cold-prefix amplifier still present** — consistent with 24.4 rerun's
cold probe shape (also exhausted budget). K=2 trigger fires only when
the model gets to a 2nd identical successful canonical dispatch within
budget; cold-prefix doesn't allow enough iterations for recurrence on
the first run. This is the **expected and unchanged** cold-probe
behavior; 24.5 does not modify any path that affects cold-probe
runtime economics. (Layer A optimization is closed at 24.3 commit 3;
further cold-prefix work is a separate seed.)

## CLI warm probe (51.3s wall-clock — the load-bearing rendering)

**Result:** HTTP 200, terminal envelope = `orchestration_terminated`,
**51.3s wall-clock** (terminated ~69s before budget). Exit 0.

**CLI stderr (operator-visible progress):**
```
Sending request...
Response received (51.3s)
exit=0
```

**CLI stdout (the load-bearing §7.1 L_CLI evidence — verbatim):**
```
[orchestration termination]
  trigger:     k_fold_canonical
  reason:      Tool flame_execute_python dispatched successfully 2 times with identical canonical arguments and identical canonical result. Loop terminated by orchestration policy.
  iterations:  4
  canonical results (2 successful):
    1. flame_execute_python [iter=2]
       args_hash:   c4e1c1a3
       result_hash: 8ca14d1e
       content:
         | ([TextContent(type='text', text='{\n  "stdout": "{\\"reel\\": \\"Reel 1\\", \\"clips\\": [\\"A001C001_130101_NQ96\\", \\"A001C002_130101_NQ96\\", \\"A001C003_130101_NQ96\\", \\"A001C004_130101_NQ96\\", \\"A001C005_130101_NQ96\\", \\"A001C006_130101_NQ96\\", \\"A001C007_130101_NQ96\\", \\"A001C008_130101_NQ96\\", \\"A001C009_130101_NQ96\\", \\"A001C010_130101_NQ96\\", ⟨several hundred clip names elided in this doc — full body verbatim at cli-warm-stdout.txt⟩, \\"B005C014_260303_RNTM\\"
         | [...truncated, full result was 21682 bytes]
    2. flame_execute_python [iter=4]
       args_hash:   c4e1c1a3
       result_hash: 8ca14d1e
       content:
         | ([TextContent(type='text', text='{\n  "stdout": "{\\"reel\\": \\"Reel 1\\", \\"clips\\": [\\"A001C001_130101_NQ96\\", \\"A001C002_130101_NQ96\\", \\"A001C003_130101_NQ96\\", \\"A001C004_130101_NQ96\\", \\"A001C005_130101_NQ96\\", \\"A001C006_130101_NQ96\\", \\"A001C007_130101_NQ96\\", \\"A001C008_130101_NQ96\\", \\"A001C009_130101_NQ96\\", \\"A001C010_130101_NQ96\\", ⟨several hundred clip names elided in this doc — full body verbatim at cli-warm-stdout.txt⟩, \\"B005C014_260303_RNTM\\"
         | [...truncated, full result was 21682 bytes]
```

Both entries rendered identically because their content IS byte-identical
— that recurrence is the K=2 trigger condition (same `args_hash` +
same `result_hash` + `status=ok` dispatched twice → trigger fires).
Showing both entries with the same first-10 / last-1 clip names + the
same elision marker preserves the architectural truth at the read
surface: the operator sees the matching `args_hash c4e1c1a3` and
matching `result_hash 8ca14d1e` lines directly above the elision, and
the rendering itself encodes the recurrence pattern.

The `⟨...⟩` markers are doc-authoring elisions, NOT consumer-side
synthesis or smoothing — the CLI's actual stdout has the full ~8KB
post-sanitization clip list inside each entry's content block (the
daemon sanitizes the original 21,682-byte tool result down to 8192
bytes before it reaches the consumer, per the `_SANITIZE_MAX_BYTES`
boundary documented at the router; the CLI then renders those 8KB
verbatim with the trailing `[...truncated, full result was 21682
bytes]` marker the sanitizer appends). The full bundle stdout is
preserved verbatim at `cli-warm-stdout.txt` if archaeology requires
exact-bytes verification of the elided middle.

**Operator can answer all five §7.1 questions from this stdout alone:**

1. *What happened?* — the conversation ended (taxon prefix tells them
   it ended, and the block following tells them how)
2. *Why?* — `reason:` line verbatim: "Loop terminated by orchestration
   policy" (NOT "the model finished" — distinct from model completion)
3. *Which authority fired?* — "orchestration policy" named explicitly
   in the reason; the `[orchestration termination]` taxon prefix
   distinguishes from `done` (model-authored) and `error` (transport)
4. *What canonical result repeated?* — both entries show the same
   tool (`flame_execute_python`), the same `args_hash` (`c4e1c1a3`)
   and the same `result_hash` (`8ca14d1e`); the recurrence pattern is
   directly readable
5. *How many iterations?* — `iterations: 4` rendered explicitly

**§10.1 anti-impersonation guard held empirically:** content is
rendered as data inside the structured block with `|` pipe-prefix
demarcation, NOT extracted via `_extract_reply` and rendered as bare
reply text. The K-th tool result's content is data, not assistant
speech. Test
`test_chat_orchestration_terminated_does_not_impersonate_via_extract_reply`
codifies this guard; under live execution the guard holds.

## Console warm probe (DOM-captured — the load-bearing §7.2 evidence)

**Result:** HTTP 200 (default JSON path; SSE consumer adoption
deferred per framing §9), terminal envelope rendered as
`<section class="orchestration-termination">` populated by Alpine
bindings.

**Live DOM excerpt (`console-dom.html`, 47KB total):**
```html
<section class="orchestration-termination"
         x-show="termination"
         role="region"
         aria-label="Orchestration termination">
  <header class="orchestration-termination__header">
    <span class="orchestration-termination__taxon">Orchestration Termination</span>
    <span class="orchestration-termination__sub">policy-decided — not model completion</span>
  </header>
  <dl class="orchestration-termination__facts">
    <dt>trigger</dt>
    <dd x-text="termination &amp;&amp; termination.trigger">k_fold_canonical</dd>
    <dt>reason</dt>
    <dd x-text="termination &amp;&amp; termination.reason">Tool flame_execute_python dispatched successfully 2 times with identical canonical arguments and identical canonical result. Loop terminated by orchestration policy.</dd>
    <dt>iterations</dt>
    <dd x-text="termination &amp;&amp; termination.iterations">4</dd>
  </dl>
  <details class="orchestration-termination__results" ...>
    <summary>canonical results (2 successful)</summary>
    <!-- entry 1: flame_execute_python [iter=2] args_hash 9e949ebc result_hash 8ca14d1e -->
    <!-- entry 2: flame_execute_python [iter=4] args_hash 9e949ebc result_hash 8ca14d1e -->
  </details>
</section>
```

**§7.2 verifications against this DOM:**

- **Distinct chrome:** `.orchestration-termination` class (not
  `.chat-message--*`); the section is a DOM sibling of the message
  `x-for` template, not a styled message variant
- **5 facts rendered verbatim through `x-text`** (NOT `x-html`, NOT
  `renderContent`) — the markdown renderer is never invoked on
  envelope strings (the §10.1 chrome-layer anti-impersonation guard)
- **Both `accumulated_results` entries' `args_hash` MATCH** (`9e949ebc`)
  and **both `result_hash` MATCH** (`8ca14d1e`) — the K=2 recurrence
  condition is operator-readable at the chrome surface
- **No empty assistant turn is laundered into a synthesized completion**
  — assistant chrome divs above the termination section render empty
  content because the model never authored a textual completion; the
  consumer honestly renders that emptiness rather than synthesizing
  one

The terminal assistant turn renders as empty `.chat-message--assistant`
chrome above the termination section. This is **architecturally
correct** — the model authored only tool_calls during iters 1-4, never
a textual completion. The orchestration termination block carries the
truth that the loop ended; no consumer-side synthesis bridges the gap.
This is the most operator-visible empirical demonstration of the 24.5
invariant: *"the consumer may project, but does not synthesize."*

## CLI `--json` envelope passthrough (anti-scope §3.2 item 10)

`fbridge chat --json "What are the clips on Reel 1"` captured at
`cli-json.txt`. P-01 stdout purity preserved — envelope passes through
verbatim; no taxonomic `[orchestration termination]` prefix leaks
into `--json` stdout. The new 24.5 code path (detection +
rendering) executes ONLY on the non-`--json` path; `--json` byte-
equivalent passthrough is preserved per
`test_chat_orchestration_terminated_json_mode_byte_identical_envelope`.

## Cross-rerun archaeology vs 24.4 bundle

**Stable across both reruns** (substrate truth invariant):
- `result_hash`: **`8ca14d1e`** in both 24.4-rerun and 24.5-rerun —
  Flame deterministically returns the same Reel 1 contents; the
  trigger correctly detects substrate-observable recurrence
- `tool_name`: `flame_execute_python` for both K-th matched entries
- `trigger`: `k_fold_canonical` (24.4 framing §5 archaeological
  naming convention preserved)
- `iterations`: 4 (iter 1 affordance regression → iter 2 ok → iter 3
  affordance regression → iter 4 ok = K=2 fires)
- Cold probe wall-clock topology: cold exhausts budget; warm
  terminates before budget (~50–55s window across both reruns)

**Differs across reruns** (expected — model emits different
canonically-equivalent Python each invocation):
- `args_hash`: 24.4 used `5be50d5d` (per framing §14 archaeology); CLI
  warm here used `c4e1c1a3`; Console warm here used `9e949ebc`. All
  three are different bytes; all three represent canonically-
  equivalent Python that produces the same Reel 1 result. **Within
  each probe, the K-th match args_hash equals the (K-1)th args_hash**
  — that's the recurrence condition the trigger detects.

This is the cleanest possible cross-rerun evidence: the **substrate
truth is invariant** (result_hash byte-identical across days), and the
**orchestration trigger fires deterministically** against
substrate-observable recurrence regardless of args-side variance.

## Server-side telemetry (§7.4 anti-scope)

`server-log-key-lines.txt` captures multiple structured log lines from
`handlers.py:1727` across the probes:

```
status=orchestration_terminated trigger=k_fold_canonical k_count=2
                                reason=orchestration_terminated
INFO     chat orchestration_terminated      handlers.py:1727
stop_reason=orchestration_terminated trigger=k_fold_canonical
```

Five identical-signature emissions across the CLI cold + CLI warm +
CLI `--json` + Console warm probes. **The substrate emission path is
byte-identical pre- and post-24.5** — the 24.4 router/handler code
fires the same lines under live policy termination as it did at
24.4-rerun time.

## Tests — full regression on portofino

```
$ python -m pytest tests/test_cli_chat.py tests/console/ tests/llm/ -q
542 passed, 1 warning in 15.43s
```

- 29 tests in `tests/test_cli_chat.py` (20 pre-existing + 9 new in
  commit 2 covering CLI orchestration_terminated projection)
- 66 tests in `tests/console/test_chat_handler.py` (54 pre-existing +
  12 new in commit 3 covering Console panel/JS/CSS projection)
- All `tests/console/test_chat_handler_sse.py` × 3 orchestration_
  terminated tests still green (anti-scope §7.4)
- All `tests/llm/test_complete_with_tools.py` K-fold trigger + envelope
  tests still green (anti-scope §7.4)
- **Zero failures, zero regressions.**

## Doctor substrate state (§7.4 anti-scope)

Substrate green throughout the probe window — `fbridge doctor` reported
all surfaces ok at `doctor-pre.txt` (before probes) and `doctor-post.txt`
(after both probes). Bundle holds both verbatim; no surface changes
state across the rerun.

## What this rerun proves

1. **Both consumer surfaces project the §4 contract truthfully under
   live policy termination.** CLI stdout and Console DOM both render
   the five facts (trigger / reason / iterations / accumulated_results /
   orchestration provenance) from the verbatim envelope. Operators can
   distinguish "model finished" from "orchestrator stopped this" at
   either surface without log archaeology.

2. **The 24.5 anti-impersonation invariant holds empirically.**
   - CLI: K-th `accumulated_results[].content` rendered as `|` pipe-
     prefixed data in a structured block, NOT extracted as bare reply
     text via `_extract_reply`. Structural detection ordering held —
     no impersonation possible because the orchestration_terminated
     path runs BEFORE `_extract_reply` is ever called.
   - Console: K-th content rendered inside `<pre x-text="entry.content">`,
     NOT through `renderContent` / `x-html` (which would route through
     the markdown renderer). Empty assistant turns rendered honestly
     as empty — no consumer-side synthesis bridges the gap.

3. **Sibling architecture validated.** CLI and Console implement the
   same §4 contract independently against the same envelope shape.
   No shared rendering code, no shared abstraction — parity-by-contract,
   not parity-by-shared-implementation. The args_hash variance between
   the two probes (`c4e1c1a3` vs `9e949ebc`) and the result_hash
   stability (`8ca14d1e` across both) confirms both surfaces consume
   the envelope identically while the underlying probe data varies as
   expected per LLM invocation.

4. **24.4 substrate byte-identical pre- and post-24.5.** §7.4
   falsification suite (542 tests including the 3 SSE orchestration_
   terminated tests + K-fold trigger tests) all pass. The router,
   handler, envelope schema, SSE event taxonomy, and telemetry log
   line are unchanged. The 24.5 consumer projection added zero
   surface to the substrate.

5. **Cross-rerun archaeology validates substrate invariants.**
   `result_hash 8ca14d1e` is byte-identical across the 24.4-rerun
   and this 24.5-rerun bundles — Flame's deterministic return shape
   on the canonical Reel 1 query is operationally stable across days.
   The K=2 trigger fires against substrate-observable recurrence
   correctly regardless of args-side LLM variance.

6. **The operator loop the 24.x arc was scoped to deliver is closed
   end-to-end.** Substrate truth (24.4) + transport coherence (24.4) +
   telemetry coherence (24.4) + **operator-visible truthfulness at
   both consumer surfaces (24.5)** — three architectural surfaces,
   three operator surfaces, one consistent invariant: orchestration
   may terminate; orchestrator does not impersonate the model;
   consumer does not synthesize.

## Bundle contents

```
README.md                      this file
doctor-pre.txt                 doctor state before any probe (all surfaces ok)
doctor-post.txt                doctor state after both probes (all surfaces ok)
cli-cold-meta.txt              cold probe wall-clock timing (130.55s)
cli-cold-stdout.txt            cold probe stdout (existing done/error paths unchanged)
cli-cold-stderr.txt            cold probe stderr (progress messages + exit code)
cli-warm-meta.txt              warm probe wall-clock timing (51.3s)
cli-warm-stdout.txt            warm probe stdout — the load-bearing CLI rendering
cli-warm-stderr.txt            warm probe stderr (51.3s response, exit=0)
cli-json.txt                   --json envelope passthrough capture (byte-equivalent)
console-dom.html               Console UI DOM after warm probe (47KB; the load-bearing Console rendering)
server-log-key-lines.txt       server-side telemetry — orchestration_terminated emissions
tests-post.txt                 full pytest run on portofino (542 passed)
```

## Methodology notes

- **Single-perturbation discipline maintained.** The only mutation
  since 24.4 rerun is the consumer-side code from this branch's 3
  commits (CLI projection, Console UI projection, this rerun docs).
  No router/handler/envelope/SSE/telemetry changes. The daemon's
  in-memory Python is byte-identical 24.4 code (the substrate behavior
  is therefore directly comparable to the 24.4 rerun bundle).
- **Editable install re-anchored** mid-bundle to `/Users/cnoellert/
  GitHub/forge-bridge` (was pointing at deleted `.claude/worktrees/
  admiring-gould-19e45a/` post-housekeeping; `pip install -e .` from
  the main checkout corrected it). Cleanup-side-effect archaeology
  noted as `feedback-editable-install-vs-worktree-removal` candidate
  for memory after this phase closes.
- **Measurement-first sequencing.** Bundle artifacts captured first;
  this README authored AFTER all artifacts complete; classification
  precedes narrative.
- **Cold-then-warm probe order** matches 24.4-rerun discipline. Cold
  exercises the budget-exhaustion path (no K=2 trigger reached); warm
  exercises the trigger-fires path. Both needed for complete §7 +
  cross-rerun comparison.
- **Same canonical query as 24.4-rerun** — `"What are the clips on
  Reel 1"` — verbatim. Forces direct comparability.
- **Same daemon, same Flame, same Ollama backend, same env file** as
  24.4-rerun. The only difference is the 24.5 consumer-side code.
- **Console DOM captured via DevTools** — operator copied
  `<section id="chat-panel">`'s outerHTML; full DOM (47KB) preserved
  at `console-dom.html`.
- **Server-side telemetry copied verbatim** from
  `/var/log/forge-bridge/console.log` byte-delta extraction filtered
  for `(orchestration_terminated|k_fold_canonical|stop_reason)`.
- **Tests-post.txt validated** via inline `pytest -q` rerun
  (`542 passed`) after initial bundle had an empty test file from
  pytest running in wrong directory.

## Status

Phase 24.5 commit 4 complete. **Phase 24.5 CLOSES against this
measurement** per framing §7 + §8:

- **§7.1 L_CLI**: ✅ CLI warm probe rendered all 5 contract facts
  with taxonomic prefix; operator can answer all five
  framing-named questions from stdout/stderr alone
- **§7.2 L_CONSOLE**: ✅ Console DOM rendered the same 5 facts via
  `x-text` bindings in distinct `.orchestration-termination` chrome;
  sibling section (not nested), no `x-html` / `renderContent`
  inside termination subtree
- **§7.3 L_PARITY**: ✅ both surfaces sourced same envelope keys;
  `result_hash` matched across surfaces (substrate invariant); args
  variance across probes preserves contract-parity verification
- **§7.4 C**: ✅ 24.4 envelope shape byte-identical pre/post-24.5;
  542/542 tests pass; falsification suite (3 SSE termination tests +
  K-fold trigger tests) all green; substrate telemetry log line
  unchanged
- **§8 forcing function**: ✅ canonical-probe re-fire on warm produces
  the predicted shape verbatim on both surfaces (CLI stdout block +
  Console DOM section)
- **§10.1 anti-scope binding**: ✅ no consumer-side synthesis observed;
  no markdown rendering of envelope strings; no impersonation via
  `_extract_reply` fallback; no compound CSS selector laundering;
  empty assistant turns rendered honestly without synthesis

The architectural shift Phase 24.5 was scoped to deliver is
empirically present, archaeologically legible, and operationally
observable at both consumer surfaces. The operator loop the 24.x
arc was scoped to deliver is closed end-to-end. Layer projection
(consumer-side) under successful policy termination is now the
consumer surfaces' authority — and the consumer surfaces project
without synthesizing.

**24.x A/B/C decomposition + consumer projection: COMPLETE.**
