---
phase: 17-default-model-bumps
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md
autonomous: true
requirements: [MODEL-02]

must_haves:
  truths:
    - "`_DEFAULT_LOCAL_MODEL` value remains `qwen2.5-coder:32b` (NO source change in this plan — the conservative-bump-first pattern holds)."
    - "`SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` is re-targeted to v1.5 and amended with the empirical evidence from the 2026-04-28 assist-01 pre-run UAT."
    - "The seed's amended evidence section captures the cold-run failure mode, the warm-run extended-budget pass, the qwen3:32b completion-token verbosity diagnosis, and three candidate v1.5 fixes."
    - "No regression unit test is added asserting `local_model == 'qwen2.5-coder:32b'` (per CONTEXT.md D-04: the local_model assertion is NOT added when MODEL-02 defers — adding it would lock in the deferred state)."
    - "MODEL-02 acceptance branch (b) is closed: 'bump deferred with a phase-17 SUMMARY note citing specific qwen3:32b failure modes that block the conservative-bump-first pattern' (the empirical evidence lives in the bumped seed AND is referenced from the phase SUMMARY)."
  artifacts:
    - path: ".planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md"
      provides: "Re-targeted v1.5 seed with the 2026-04-28 assist-01 empirical evidence appended."
      contains: "v1.5"
  key_links:
    - from: "MODEL-02 acceptance branch (b)"
      to: "SEED-DEFAULT-MODEL-BUMP-V1.4.x.md (re-targeted)"
      via: "empirical-evidence section + v1.5 retargeting"
      pattern: "FORGE-INTEGRATION-SENTINEL-XJK29Q|qwen3:32b"
    - from: "Phase 17 SUMMARY"
      to: "SEED-DEFAULT-MODEL-BUMP-V1.4.x.md"
      via: "carry-forward addendum reference"
      pattern: "SEED-DEFAULT-MODEL-BUMP"
---

<objective>
MODEL-02 deferral: the 2026-04-28 assist-01 pre-run UAT (CONTEXT.md D-02) returned a non-deterministic borderline result against the production 60s `max_seconds` budget. Specifically: the salvage helper, tool dispatch, and 2-step loop terminate cleanly; but qwen3:32b's thinking-mode emits 400-525 completion tokens per turn (vs ~50 for qwen2.5-coder), driving total elapsed to 55-75s on assist-01 and exceeding the default 60s budget on cold runs. Therefore MODEL-02 takes the deferral branch (REQUIREMENTS.md MODEL-02 acceptance (b)).

This plan ships **NO source code change**:
- `forge_bridge/llm/router.py` is NOT touched. `_DEFAULT_LOCAL_MODEL` stays at `"qwen2.5-coder:32b"` (its post-P-01 value).
- `tests/test_llm.py` is NOT touched. No `local_model == "qwen3:32b"` assertion is added (would falsely lock in the deferred state); no `local_model == "qwen2.5-coder:32b"` assertion is added (the existing `test_default_fallback` already covers it after P-01).

This plan ships **one documentation change**:
- `.planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` is re-targeted to v1.5 and amended with the empirical evidence section capturing the cold-run failure mode, the warm-run extended-budget pass, the completion-token-verbosity diagnosis, and three candidate v1.5 fixes (max_seconds bump, `/no_think` directive in OllamaToolAdapter, router-init warmup ping).

Purpose: close MODEL-02 against acceptance branch (b) — "bump deferred with a phase-17 SUMMARY note citing specific qwen3:32b failure modes" — and durably plant the empirical evidence in `.planning/seeds/` where v1.5 planning will find it. The phase SUMMARY (a workflow artifact, not a plan deliverable) cites the bumped seed.

Output: One docs commit on `.planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-default-model-bumps/17-CONTEXT.md
@.planning/phases/17-default-model-bumps/17-01-SUMMARY.md
@.planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md
@./CLAUDE.md

<interfaces>
<!-- Existing seed file the executor MUST extend (not rewrite). Frontmatter `trigger_when` flips from v1.4.x to v1.5; -->
<!-- the body keeps the original "Idea" / "Why This Matters" sections and gains an "Empirical Evidence (Phase 17 -->
<!-- pre-run UAT, 2026-04-28)" section + a "Candidate v1.5 Fixes" section. -->

From .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md (current frontmatter — flip `trigger_when`):
```yaml
---
name: SEED-DEFAULT-MODEL-BUMP-V1.4.x          # ← keep name (file slug stays for git history)
description: Bump default Ollama tool-call model qwen2.5-coder:32b → qwen3:32b after assist-01 UAT
type: forward-looking-idea
planted_during: v1.4 FB-C planning (2026-04-26)
trigger_when: A v1.4.x patch milestone opens after FB-C ships AND the assist-01 UAT for the FB-C tool-call loop on qwen2.5-coder:32b has produced a baseline reading
---
```

Pre-run UAT result to encode (from CONTEXT.md and the planning brief):
- Run 1 (cold, default 60s budget): iter 1 took 55.2s and 522 completion tokens; iter 2 never reached due to LLMLoopBudgetExceeded reason=max_seconds. Sentinel NOT returned.
- Run 2 (warm, extended 180s budget via `tmp/qwen3_extended_uat.py` script): iter 1 took 39.6s / 445 tokens; iter 2 took 18.4s / 195 tokens; total 58.0s; sentinel `FORGE-INTEGRATION-SENTINEL-XJK29Q` returned verbatim.
- Diagnosis: salvage helper handles qwen3 tool-call shape correctly (no 6th shape, no widening required); 2-step loop completes end-to-end on qwen3:32b. Failure mode is completion-token verbosity (qwen3 thinking-mode: 400-525 tokens/turn vs ~50 for qwen2.5-coder); ~10 tok/s on assist-01 puts iter 1 alone at 40-55s. Borderline against the 60s default; non-deterministic on cold start.
- Verdict: DEFER. Bump is not a clean win at the v1.4.x default `max_seconds=60` budget.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-target SEED-DEFAULT-MODEL-BUMP-V1.4.x to v1.5 and append empirical-evidence section</name>
  <files>.planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md</files>
  <read_first>
    - .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md (current state — frontmatter + body sections; the executor MUST preserve the original Idea/Why-This-Matters/When-To-Surface/Cross-References sections and only flip the trigger + append two new sections)
    - .planning/phases/17-default-model-bumps/17-CONTEXT.md §"D-03 P-03" + §"D-02" (deferral mandate + the pre-run UAT command + the empirical-evidence requirement)
    - .planning/REQUIREMENTS.md §MODEL-02 (acceptance branch (b) verbatim)
    - .planning/seeds/SEED-CLOUD-MODEL-BUMP-V1.4.x.md (style reference for the closing/retarget convention used elsewhere in `.planning/seeds/`)
  </read_first>
  <action>
    Edit `.planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md`. Two edits in one atomic commit. **Preserve** the existing Idea / Why This Matters / When to Surface / How to Apply / Cross-References sections — they're still accurate; the qwen3 candidacy isn't dead, just deferred to v1.5 with concrete evidence about the failure mode.

    **Edit 1 — Flip the frontmatter `description` and `trigger_when`** (top of file):

    Change `description:` line FROM:
    ```yaml
    description: Bump default Ollama tool-call model qwen2.5-coder:32b → qwen3:32b after assist-01 UAT
    ```
    TO:
    ```yaml
    description: Bump default Ollama tool-call model qwen2.5-coder:32b → qwen3:32b in v1.5 (deferred from v1.4.x Phase 17 — see empirical evidence below)
    ```

    Change `trigger_when:` line FROM:
    ```yaml
    trigger_when: A v1.4.x patch milestone opens after FB-C ships AND the assist-01 UAT for the FB-C tool-call loop on qwen2.5-coder:32b has produced a baseline reading
    ```
    TO:
    ```yaml
    trigger_when: A v1.5 milestone planning session AND one of (a) the default complete_with_tools `max_seconds` budget has been bumped to ≥120s, (b) `OllamaToolAdapter` gains a per-model `think=False` / qwen3 `/no_think` directive, OR (c) router-init adds a warmup-ping path that absorbs cold-start cost
    ```

    Do NOT change the `name:` field — the slug `SEED-DEFAULT-MODEL-BUMP-V1.4.x` stays so `git log` and back-references remain stable. (Convention precedent: the seed describes a v1.4.x-planted concern even after retargeting.)

    Do NOT delete the existing `Idea`, `Why This Matters`, `When to Surface`, `How to Apply`, or `Cross-References` sections. They remain accurate — qwen3:32b is still the v1.5 candidate.

    **Edit 2 — Append two new sections** at the end of the file (after the existing `## Cross-References` section):

    ```markdown
    ## Empirical Evidence (Phase 17 pre-run UAT, 2026-04-28)

    Per CONTEXT.md D-02, the qwen3:32b live UAT was run on assist-01 BEFORE
    Phase 17 plan-phase locked. The empirical result was non-deterministic
    against the v1.4.x default `max_seconds=60` budget, so MODEL-02 took
    acceptance branch (b) — defer with a phase-17 SUMMARY note.

    **Test:** `test_ollama_tool_call_loop_live` against
    `FORGE_LOCAL_MODEL=qwen3:32b` on assist-01 (M-series GPU, 32 GB) at HEAD
    `fad8615` (v1.4.0; identical source as current main for `forge_bridge/`
    and `tests/`).

    ### Run 1 — cold start, default 60 s budget

    - iter 1: 55.2 s, 522 completion tokens, `status=continuing` (tool dispatched correctly)
    - iter 2: never reached — `LLMLoopBudgetExceeded reason=max_seconds`
    - Sentinel `FORGE-INTEGRATION-SENTINEL-XJK29Q`: NOT returned

    ### Run 2 — warm start, extended 180 s budget (`tmp/qwen3_extended_uat.py` script, same prompt + tools + executor as the test, `max_seconds=180`)

    - iter 1: 39.6 s, 445 completion tokens, `status=continuing`
    - iter 2: 18.4 s, 195 completion tokens, `status=terminal`
    - Total elapsed: 58.0 s
    - Sentinel returned verbatim: `FORGE-INTEGRATION-SENTINEL-XJK29Q` ✅

    ### Diagnosis

    The salvage helper (`forge_bridge/llm/_adapters.py::_try_parse_text_tool_call`)
    handles the qwen3 tool-call shape correctly — **no 6th shape, no widening
    required**. The 2-step LLMTOOL-01 loop completes end-to-end on qwen3:32b
    when given enough wall-clock budget.

    The failure mode is **completion-token verbosity**: qwen3 thinking-mode
    emits 400-525 tokens per turn (vs ~50 for qwen2.5-coder). At ~10-11 tok/s
    on assist-01, iter 1 alone consumes 40-55 s. This puts the total run at
    55-75 s — borderline against the 60 s default, with cold-start sensitivity
    pushing the cold path past budget.

    **Bottom line:** zero regression risk to MODEL-02 itself (mechanics work);
    the bump is gated on either widening the budget or quieting qwen3's
    thinking-mode emit.

    ## Candidate v1.5 Fixes (pick one or stack)

    1. **Bump default `max_seconds`** in `forge_bridge/llm/router.py`
       `complete_with_tools` from 60 → ~120 s. Decision is whether the same
       budget bump is acceptable for the cloud (Anthropic) path too. Lowest-
       implementation-cost option; trades latency budget for model coverage.

    2. **Add qwen3 `/no_think` (or `think=False`) directive support** in
       `forge_bridge/llm/_adapters.py::OllamaToolAdapter`. qwen3 supports a
       directive that suppresses chain-of-thought emit. Cleanest fix —
       removes the verbosity at the source — but requires per-model adapter
       awareness similar to the cloud-side `temperature`-elision pattern in
       SEED-OPUS-4-7-TEMPERATURE-V1.5.

    3. **Router-init warmup ping**. Cold start dominates the borderline cases
       (Run 1 vs Run 2). A small warmup request at `LLMRouter` instantiation
       (or first `acomplete()` call) absorbs the cold cost out of the
       tool-call loop's measured budget. Lightest behavioral change; doesn't
       solve the verbosity issue but materially reduces cold-path failure
       rate.

    Pick during v1.5 planning based on whether a default-budget bump is
    acceptable for the cloud path too.

    ## Phase 17 Closure (2026-04-28)

    - MODEL-02 closed against acceptance branch (b): "bump deferred with a
      phase-17 SUMMARY note citing specific qwen3:32b failure modes that
      block the conservative-bump-first pattern."
    - `forge_bridge/llm/router.py` `_DEFAULT_LOCAL_MODEL` remains
      `qwen2.5-coder:32b` (preserved through Phase 17 P-01 + P-03).
    - This seed is the durable home for the empirical evidence; the Phase 17
      SUMMARY references this section.
    ```

    Do NOT change `forge_bridge/llm/router.py`. Do NOT change `tests/test_llm.py`. Do NOT add a regression test for the `local_model` default value (CONTEXT.md D-04 explicitly excludes this when MODEL-02 defers).

    Commit message:
    ```
    docs(seeds): retarget SEED-DEFAULT-MODEL-BUMP-V1.4.x to v1.5 with Phase 17 UAT evidence

    MODEL-02 took acceptance branch (b) — deferred. The 2026-04-28 assist-01
    pre-run UAT showed qwen3:32b's mechanics work end-to-end, but thinking-
    mode completion-token verbosity (400-525 tok/turn vs ~50 for
    qwen2.5-coder) pushes the loop borderline against the 60s default
    budget. Three candidate v1.5 fixes captured: max_seconds bump, qwen3
    /no_think directive, router-init warmup.

    Phase 17 P-03 of 3 (deferral commit; no source change).
    ```
  </action>
  <acceptance_criteria>
    - `grep -c '^description: Bump default Ollama tool-call model qwen2.5-coder:32b → qwen3:32b in v1.5' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `1` (frontmatter description re-targeted)
    - `grep -c 'A v1.5 milestone planning session' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `1` (trigger_when re-targeted)
    - `grep -c '^## Empirical Evidence (Phase 17 pre-run UAT, 2026-04-28)' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `1` (new section appended)
    - `grep -c '^## Candidate v1.5 Fixes' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `1` (new section appended)
    - `grep -c 'FORGE-INTEGRATION-SENTINEL-XJK29Q' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `>= 1` (sentinel from Run 2 captured verbatim)
    - `grep -c '522 completion tokens\|55.2 s' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `>= 1` (Run 1 numerics encoded)
    - `grep -c 'Total elapsed: 58.0 s\|58.0 s' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `>= 1` (Run 2 total elapsed encoded)
    - `grep -c '/no_think\|think=False' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `>= 1` (candidate fix #2 captured)
    - `grep -c 'warmup' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `>= 1` (candidate fix #3 captured)
    - `grep -c '## Idea' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `1` (original "Idea" section preserved)
    - `grep -c '## Why This Matters' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `1` (original section preserved)
    - `grep -c '## How to Apply' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `1` (original section preserved)
    - `grep -c '## Cross-References' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` returns `1` (original section preserved)
    - `git diff --name-only HEAD~1 HEAD` shows exactly one file: `.planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` (commit isolation; NO source-file change)
    - `git diff HEAD~1 HEAD -- forge_bridge/llm/router.py` is empty (no source change in P-03)
    - `git diff HEAD~1 HEAD -- tests/` is empty (no test change in P-03)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Empirical Evidence (Phase 17 pre-run UAT, 2026-04-28)' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md && grep -q 'FORGE-INTEGRATION-SENTINEL-XJK29Q' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md && grep -q 'A v1.5 milestone planning session' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md && grep -q '## Candidate v1.5 Fixes' .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md && echo OK</automated>
  </verify>
  <done>
    - Seed re-targeted to v1.5 (frontmatter `description` + `trigger_when` flipped; original sections preserved)
    - Empirical Evidence section appended with verbatim Run 1 / Run 2 numerics + diagnosis
    - Candidate v1.5 Fixes section appended with three concrete options (max_seconds bump, qwen3 /no_think, warmup ping)
    - Phase 17 Closure paragraph appended naming acceptance branch (b)
    - One docs commit on the seed file only — no source change, no test change
    - MODEL-02 acceptance branch (b) closed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change — no source code, no API surface, no I/O. No trust boundary involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.03-01 | (n/a) | .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md | accept | No new security surface — planning-tree documentation update only. ASVS L1 sufficient. |
</threat_model>

<verification>
- `grep` checks confirm the seed's frontmatter `description` + `trigger_when` are flipped to v1.5
- `grep` checks confirm the new Empirical Evidence and Candidate v1.5 Fixes sections exist with the prescribed content
- `git diff --name-only HEAD~1 HEAD` shows exactly one file changed (the seed)
- `git diff HEAD~1 HEAD -- forge_bridge/llm/router.py` is empty (no source change — `_DEFAULT_LOCAL_MODEL` stays at `qwen2.5-coder:32b`)
- `git diff HEAD~1 HEAD -- tests/` is empty (no test change — no `local_model` regression assertion added per D-04 deferral branch)
- The phase SUMMARY (workflow artifact) cites this seed as the durable home for the empirical evidence
</verification>

<success_criteria>
- `_DEFAULT_LOCAL_MODEL` remains `"qwen2.5-coder:32b"` in `forge_bridge/llm/router.py` (preserved through P-01 and P-03)
- `SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` re-targeted to v1.5 with Phase 17 empirical evidence appended
- Three candidate v1.5 fixes documented (max_seconds bump, qwen3 `/no_think`, router-init warmup ping)
- One atomic docs commit; no source or test files touched
- MODEL-02 acceptance branch (b) closed
</success_criteria>

<output>
After completion, create `.planning/phases/17-default-model-bumps/17-03-SUMMARY.md` documenting:
- File modified: `.planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md` (one diff)
- Acceptance criteria evidence (grep output)
- Confirmation that no source or test file was touched (`git diff` empty for those paths)
- Explicit closure of MODEL-02 against acceptance branch (b) with a one-line summary of the deferral rationale (cold-run budget exceedance + qwen3 thinking-mode token verbosity)
- Explicit reference to the bumped seed as the durable home for the empirical evidence so the phase SUMMARY can cite it
</output>
