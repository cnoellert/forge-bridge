# Phase 24.6 canonical-probe rerun — 2026-05-18

**Bundle:** `~/.forge-bridge/measurements/2026-05-18-phase-24-6-rerun/`
**Branch:** `phase-24-6-affordance-selection-sharpening`
**Base:** `main @ 6b24983` + framing `a472cad` + edit `cf5c7bf` + test-pin update `0011f1d`
**Canonical query:** `"What are the clips on Reel 1"` (verbatim from 24.4 + 24.5)
**Daemon:** PID 34713, started 2026-05-18 13:31:37 (post-edit restart)

---

## §0 What this rerun proves

The §5 multi-dimensional pass criterion fires across **all three dimensions** simultaneously, with the failure mode the §1 diagnosis named ("unstable affordance prior; iter 1 AND iter 3 both pick `flame_find_media`") eliminated entirely.

The model now picks `flame_execute_python` at iter 1 in 5/5 warm probes, dispatches the same canonical tool again at iter 2, and the K=2 termination trigger fires deterministically at iter 2 (down from iter 4 in 24.4 and 24.5). Two wasted iterations per probe disappear; recovery integrity holds across all probes; substrate result_hash byte-identical to 24.4 + 24.5.

The §0 framing reframe ("unstable affordance prior" vs "wrong first guess") is honored at the measurement layer: the prior is now stable enough that the model never enters the relapse state in 5 independent reruns. The §5 prior-stabilization criterion is satisfied vacuously rather than via observed recovery — a stronger outcome than the framing anticipated.

---

## §1 Pass criterion evaluation (multi-dimensional)

Per FRAMING §5 the pass criterion has three dimensions; any-of-three is a win, all-of-three is the full success. **All three fire.**

### §1.1 Initial-prior shift — ✓ FULL

> *Criterion:* iter-1 picks `flame_execute_python` in ≥4 of 5 warm canonical reruns

| Probe | iter-1 tool | Status |
|---|---|---|
| warm-1 | `flame_execute_python` | ok |
| warm-2 | `flame_execute_python` | ok |
| warm-3 | `flame_execute_python` | ok |
| warm-4 | `flame_execute_python` | ok |
| warm-5 | `flame_execute_python` | ok |

**5 of 5 warm probes.** Exceeds the ≥4/5 bar.

Evidence: `server-log-full.txt` per-iter dispatch lines (router.py:949 `tool-call iter=1 tool=flame_execute_python status=ok code_len=581`) appear identically in all 5 warm probes.

Comparison to 24.4 rerun: iter-1 was `flame_find_media` `status=tool_error` (0 of 1 picked correctly). 24.6 inverts this completely.

### §1.2 Prior stabilization across the loop — ✓ VACUOUSLY satisfied

> *Criterion:* in any probe where iter-1 still picks `flame_find_media`, iter-3 does NOT also pick `flame_find_media` (the prior updates after iter-2 recovery)

There were **zero** probes where iter-1 picked `flame_find_media`. The iter-3 stabilization check has no cases to evaluate because the model never enters the relapse state.

This is a **stronger** outcome than the framing anticipated. The §0 reframe correctly named the pathology as "unstable affordance prior" — the framing assumed sharpening might either improve initial selection OR stabilize the prior across the loop. The actual outcome: the initial-prior shift is so complete that prior-stabilization is irrelevant. The affordance prior is now correct at iter 1 and remains correct at iter 2 (where K=2 fires).

### §1.3 Substrate integrity — ✓ FULL

> *Criterion:* `result_hash 8ca14d1e` is stable across all 5 probes AND recovery path remains intact in any probe where selection is still wrong

| Signal | 24.6 value | 24.5 value | 24.4 value |
|---|---|---|---|
| `result_hash` | `8ca14d1e` | `8ca14d1e` | `8ca14d1e` |
| `args_hash` | `c4e1c1a3` | `c4e1c1a3` | `c4e1c1a3` |
| `code_len` per dispatch | 581 | 581 | 581 |
| MD5 of warm stdouts (all 5 identical) | `d3126454...` | (single warm) | (single warm) |

All 5 warm probe stdouts are **byte-identical** (`md5 warm-*-stdout.txt` returns same hash 5 times). Result hash matches 24.4 and 24.5 byte-for-byte. Substrate produced canonical Flame query results without disturbance.

Recovery integrity: all 5 warm probes returned `exit=0` with the orchestration_terminated envelope rendered correctly. No probe required recovery from a wrong selection because no probe had a wrong selection.

### §1.4 Failure criteria — NONE triggered

The framing named three failure conditions; none fired:

- ❌ "iter-1 AND iter-3 selection unchanged after edit" — selection completely shifted at iter-1
- ❌ "recovery degrades in any probe where selection is still wrong" — no probe had iter-1 wrong
- ❌ "result_hash drifts from 8ca14d1e" — held byte-stable across all 5 probes

---

## §2 Per-probe evidence

### §2.1 Cold probe (anti-scope verification)

```
cold-1   exit=3   elapsed=130.578s   stop_reason=loop_budget_exceeded
         iter=1   tool=flame_execute_python   status=ok   code_len=581
```

Cold probe budget-exhausted at 130s before iter-2 could complete — same shape as 24.4 cold (~130s timeout) and 24.5 cold (~130.55s timeout). **Anti-scope verification passes:** no behavioral drift; if anything, the cold path now picks `flame_execute_python` at iter-1 (compared to 24.4 cold which picked `flame_find_media`) — slight improvement, still doesn't fit in 120s budget because the cold-start LLM inference + canonical-result tokenization for iter 1 alone consumes the entire budget.

Per FRAMING §5: *"Cold probes: Anti-scope verification only, same as 24.4 and 24.5. Cold path must show no behavioral drift. Pass criteria below are not evaluated on cold probes."*

The cold-prefix budget gap is a separate concern (operator-time on cold start) that out-of-scope for 24.6 and unchanged by description sharpening — the model still needs ~120s to produce iter-1 inference + canonical dispatch on cold start. If 24.6 had eliminated iter-1 mis-selection AND fit within cold budget, that would have implied a runtime-economics shift the description edit cannot mechanically produce.

### §2.2 Warm probes (M=5 forcing function)

| Probe | exit | elapsed | iter-1 | iter-2 | K=2 fires | result_hash |
|---|---|---|---|---|---|---|
| warm-1 | 0 | 118.463s | `flame_execute_python` ok | `flame_execute_python` ok | iter=2 | `8ca14d1e` |
| warm-2 | 0 | 61.419s | `flame_execute_python` ok | `flame_execute_python` ok | iter=2 | `8ca14d1e` |
| warm-3 | 0 | 61.700s | `flame_execute_python` ok | `flame_execute_python` ok | iter=2 | `8ca14d1e` |
| warm-4 | 0 | 59.514s | `flame_execute_python` ok | `flame_execute_python` ok | iter=2 | `8ca14d1e` |
| warm-5 | 0 | 53.866s | `flame_execute_python` ok | `flame_execute_python` ok | iter=2 | `8ca14d1e` |

**warm-1 anomaly noted, not concerning:** warm-1 took ~2× the other warms (118s vs ~60s) because the preceding cold probe burned the 130s budget without fully warming the model's KV cache; warm-1 effectively paid a partial cold-start cost on its iter-1 inference. Warm-2 through warm-5 reflect steady-state warm performance (~60s for 2 iters = ~30s/iter, consistent with `flame_execute_python` against the 58-tool prefix per Phase 24.1 measurement).

Iter sequence verbatim from `server-log-key-lines.txt` (representative warm probe):

```
INFO  tool-call iter=1   router.py:949   tool=flame_execute_python   status=ok code_len=581
INFO  tool-call iter=2   router.py:949   tool=flame_execute_python
INFO  tool-call iter=2   router.py:978   tool=flame_execute_python
                         status=orchestration_terminated
                         trigger=k_fold_canonical k_count=2
                         reason=orchestration_terminated
                         stop_reason=orchestration_terminated trigger=k_fold_canonical
                         status=ok code_len=581
```

### §2.3 CLI rendering of orchestration_terminated envelope (excerpt, warm-1)

```
[orchestration termination]
  trigger:     k_fold_canonical
  reason:      Tool flame_execute_python dispatched successfully 2 times with
               identical canonical arguments and identical canonical result.
               Loop terminated by orchestration policy.
  iterations:  2
  canonical results (2 successful):
    1. flame_execute_python [iter=1]
       args_hash:   c4e1c1a3
       result_hash: 8ca14d1e
       content:     ⟨21,682-byte JSON with reel="Reel 1" + clips array — verbatim
                    Flame query result; first clip "A001C001_130101_NQ96"⟩
    2. flame_execute_python [iter=2]
       args_hash:   c4e1c1a3
       result_hash: 8ca14d1e
       content:     ⟨byte-identical to (1) — substrate confirms canonical
                    recurrence triggered K=2⟩
```

The CLI projection per Phase 24.5 consumer-surfacing renders the envelope honestly: 2 iterations, both canonical, both flame_execute_python, both with matching args_hash + result_hash. The empty `.chat-message--assistant` chrome above the termination section (per 24.5 §10.4 / §10.9) renders honestly empty — the model never authored a textual completion (only tool_calls); consumer does not synthesize one.

---

## §3 Cross-rerun archaeology

### §3.1 Iter-sequence comparison

| | 24.4 rerun | 24.5 rerun | **24.6 rerun** |
|---|---|---|---|
| iter 1 tool | `flame_find_media` | `flame_find_media` | **`flame_execute_python`** |
| iter 1 status | `tool_error` | `tool_error` | **`ok`** |
| iter 2 tool | `flame_execute_python` | `flame_execute_python` | `flame_execute_python` |
| iter 3 tool | `flame_find_media` | `flame_find_media` | **(no iter 3 — K=2 fired)** |
| iter 3 status | `tool_error` | `tool_error` | — |
| iter 4 tool | `flame_execute_python` | `flame_execute_python` | — |
| K=2 fires at | iter 4 | iter 4 | **iter 2** |
| Total iters to termination | 4 | 4 | **2** |
| Wasted iters | 2 | 2 | **0** |

### §3.2 What stayed byte-identical

- **`result_hash`:** `8ca14d1e` across all three reruns. The canonical Flame query result has not drifted; the substrate executes identically.
- **`args_hash`:** `c4e1c1a3` across all three reruns. The model emits identical canonical arguments to `flame_execute_python`.
- **`code_len`:** 581 across all three reruns. The Python code dispatched into Flame is unchanged in length (likely byte-identical).
- **Trigger taxon:** `k_fold_canonical` in all three. The K=2 termination policy is the only termination mechanism observed.
- **Stop reason:** `orchestration_terminated` in all three (24.4/24.5/24.6 warm). The envelope semantics are identical; only the iteration count differs.

### §3.3 What changed

- **iter-1 tool selection:** `flame_find_media` → `flame_execute_python` (the load-bearing perturbation).
- **iter count:** 4 → 2. The model converges to canonical recurrence at the earliest possible iteration where K=2 can fire.
- **iter-1 status:** `tool_error` → `ok`. The first dispatch is now substantively useful, not a wasted error-return.

### §3.4 What 24.6 does NOT change relative to 24.5

- Wall-clock per probe is **roughly comparable**, not dramatically faster (see §5). The work pattern shifted from "4 cheap iters, 2 useful" to "2 useful iters." Per-iter cost is similar.
- Substrate behavior is unchanged. Same Flame execution, same result, same envelope.
- Consumer projection (CLI + Console per 24.5) is unchanged. The rendered output differs only in the `iterations: 2` field vs `iterations: 4` field.

---

## §4 Substrate integrity (anti-scope §3 + §10 verification)

### §4.1 Doctor state — pre vs post

```diff
- │ graph_store  │ ok     │ 50 graphs, last 2026-05-18T18:24:53.451Z             │
+ │ graph_store  │ ok     │ 63 graphs, last 2026-05-18T20:42:26.763Z             │
```

The only doctor delta across the rerun is the `graph_store` row — +13 graphs from 6 probes (1 cold + 5 warm). Cold contributed 1 dispatch (iter 1 only); each warm contributed 2 dispatches (iter 1 + iter 2) = 1 + 10 = 11 expected; +2 background graphs accounted for. All other doctor surfaces (console / mcp_http / flame_bridge / state_ws / `flame_bridge` daemon-routed probe) unchanged and `ok`.

Anti-scope §10 verification: no policy / model / orchestration / substrate state shifted during the rerun. The only state mutation was append-only graph emission at the expected rate.

### §4.2 Anti-scope items from FRAMING §10 — verbatim verification

| Anti-scope item | Verified state |
|---|---|
| Tool inventory unchanged | ✓ no add, no remove (test_pr22_every_registered_tool_satisfies_canonical_contract pre-existing failure unrelated; not from 24.6) |
| Tool signatures unchanged | ✓ `execute_python(code, main_thread=False)` + `find_media(params)` byte-identical pre-/post-edit |
| Orchestration policy unchanged | ✓ K=2 trigger fires identically; router.py:639-662 untouched |
| Termination semantics unchanged | ✓ `orchestration_terminated` envelope byte-equivalent to 24.4/24.5 |
| System message unchanged | ✓ no system prompt edits in commit cf5c7bf |
| Model + runtime substrate unchanged | ✓ same qwen2.5-coder:14b, same Ollama, no router config changes |
| No new comparative routing language | ✓ `find_media` boundary clarification names capability LIMIT not other tools; `execute_python` paragraph 4 rewords inherited baseline within §3 inherited-baseline clause |

### §4.3 Envelope passthrough (anti-scope §3.2 item 10 from 24.5)

CLI `--json` short-circuit is not exercised in this rerun bundle (no `--json` probes captured). The standard `fbridge chat` invocation renders the full envelope verbatim per 24.5 projection contract. CLI exit codes locked at 0=success / 3=budget-exhausted (cold) per existing CLI contract.

---

## §5 Wall-clock honest analysis

**Operator-perceived total wait per warm probe:**

| Rerun | Warm wall-clock | Iter count | Per-iter avg |
|---|---|---|---|
| 24.4 | 54.7s | 4 | 13.7s |
| 24.5 | 51.3s | 4 | 12.8s |
| 24.6 (warm-2..5 avg) | 59.1s | 2 | 29.6s |

24.6 is **not dramatically faster** in raw wall-clock terms. Per-iter cost actually rises (~30s vs ~13s) because:

- 24.4/24.5 had 4 iters where 2 were cheap `flame_find_media` `tool_error` returns (~3-5s each — fast OS-level rejection on a query-string-required input) and 2 were expensive `flame_execute_python` canonical dispatches (~25-30s each).
- 24.6 has 2 iters, both expensive `flame_execute_python` canonical dispatches (~30s each) — no cheap fail-iters to bring the per-iter average down.

Net result: **operator wall-clock per warm probe is roughly comparable** between 24.6 and 24.5 (~60s vs ~51s in steady state). 24.6 trades 4 cheap+expensive iters for 2 expensive iters. The work done is the same; the model's first guess is now correct.

**Where the operator-visible win actually lives:**

- **Determinism.** K=2 fires at iter 2 every time, not at iter 4. The termination shape is predictable, which matters for operator mental model and for cold-budget headroom.
- **Affordance correctness.** The model picks the right tool first, which matters for any future scenario where iter-1 selection has higher cost than a tool-error rejection (e.g., a future Anthropic-routed path where iter-1 mis-selection costs more than ~5s).
- **Visible coherence.** The CLI shows "iterations: 2" with both canonical results matching — operators can see at a glance that the model behaved deterministically rather than recovering from a mis-select.

**Where the operator-visible win does NOT live:**

- Raw "fewer seconds per chat call." 24.6 is not the wall-clock win it might naively appear to be.
- Cold-start fit. The cold probe still budget-exhausts at 130s — same as 24.4 and 24.5. Description sharpening cannot mechanically shrink cold-start LLM inference cost.

This honest framing matters for §11 carry-forward decisions: 24.6 closes the affordance pathology; it does NOT close cold-start budget gaps or per-iter inference cost. Those remain separate concerns with their own re-eval triggers in the §11 register.

---

## §6 What this rerun does NOT prove

### §6.1 The three-mechanism decomposition

When an LLM affordance behavior changes after a perturbation, three distinct mechanisms could explain the shift. Each has a different falsification surface; conflating them produces overclaim.

| # | Mechanism | Plausibility for 24.6 setup |
|---|---|---|
| 1 | **Session-local KV/cache familiarity.** The model's effective behavior is shaped by recent context — same query repeated, same tool outputs ingested, same recovery paths observed. The "prior" the model expresses is partly cache-conditioned. | **Strongest confound.** Warm-state caching is real; M=5 probes ran in quick succession; the canonical query has been repeated many times across 24.4 + 24.5 + 24.6 cycles. |
| 2 | **Backend/provider adaptive behavior.** Some providers do online adaptation between calls (request-level fine-tuning, RLHF feedback loops, etc.). | **Low.** Production hardcodes `sensitive=True` → local Ollama; Ollama does NOT update model weights between calls. The only "adaptation" mechanism in the loop is mechanism (1). |
| 3 | **Actual docstring-induced prior shift.** The intentional 24.6 perturbation changes what the model reads when constructing its tool-selection prior; the rhetorical inversion shifts which token sequences win the selection competition. | **The hypothesis 24.6 is built to test.** |

### §6.2 What the historical continuity argument shows

If mechanism (1) alone were sufficient to retrain the local affordance prior, we'd expect to see drift accumulate across the 24.4 + 24.5 warm-rerun cycles. The model had already seen this canonical query many times, the successful recovery pattern many times, the tool outputs many times — across multiple reruns and phase boundaries — yet the pathology was *remarkably stable*:

```
24.4 rerun:  iter 1 → flame_find_media   iter 2 → execute_python
             iter 3 → flame_find_media   iter 4 → execute_python  → K=2

24.5 rerun:  iter 1 → flame_find_media   iter 2 → execute_python
             iter 3 → flame_find_media   iter 4 → execute_python  → K=2
```

Same shape across both reruns despite repeated exposure. Then **immediately** after the rhetorical inversion landed:

```
24.6 rerun:  iter 1 → execute_python     iter 2 → execute_python  → K=2  (× 5)
```

The temporal correlation between the perturbation landing and the topology shift is strong. The null hypothesis (mechanism (1) alone explains the shift) predicts gradual drift, fewer iter-1 mis-selections, or mixed distributions emerging organically across the warm-probe history. The observed pattern — stable pathology across multiple phases, then sharp collapse at the exact intervention point — is inconsistent with the null.

### §6.3 The topology-collapse-vs-memorization signature

A memorized trajectory typically looks like: **correct first step, then residual oscillation later.** The model learns "for this query, the answer path starts with X" but doesn't durably update higher-order affordance weighting; subsequent iterations can drift back to the original prior.

What this rerun actually observed:

- iter-1 picks `flame_execute_python` (initial selection corrected)
- iter-2 picks `flame_execute_python` again (NO relapse to `flame_find_media`)
- substrate result_hash byte-stable
- stdout MD5 byte-identical across all 5 warm probes
- termination shape deterministic (K=2 at iter 2, every time)
- iter-3 NEVER ENTERED (model didn't relapse, so K=2 fired before iter-3 was needed)

This is **topology collapse**, not memorized-trajectory shape. There is no relapse state being recovered from; the entire mis-selection sub-graph is absent from the observed runs. That's harder to explain by simple repetition learning alone — repetition learning typically produces shifted-first-step + residual-instability, not whole-sub-graph elimination.

### §6.4 The honest epistemic state

Combining §6.1–§6.3:

> The evidence **strongly supports** the docstring-ordering hypothesis (mechanism 3).
>
> The evidence **does not yet fully isolate** mechanism (3) from accumulated interaction familiarity (mechanism 1).
>
> Topology collapse (vs memorized-trajectory shape) is *differentiating signal beyond raw first-step correctness* — it weakens but does not eliminate the (1)-only null hypothesis.
>
> Query-class generalization under colder conditions is the natural next falsification surface if stronger causal confidence is wanted. See §12 for the discriminator design.

### §6.5 Limitations the framing already named

- **Does not prove the same shift holds for other query classes.** The forcing function tested one canonical query ("What are the clips on Reel 1"). Other query shapes the model might mis-select on (e.g., "list the segments on the current sequence", "show me the batch graph") were not probed. Generalizing 24.6's win to those query classes is intentionally unbound — see §12 discriminator design.
- **Does not prove the catalogue sweep is unnecessary.** §3 explicitly deferred the full `flame_*` catalogue sweep. 24.6's narrow two-tool edit is sufficient for this query class; whether other latent affordance ambiguities exist in the 12 unmodified `flame_*` tools is a separate empirical question.
- **Does not prove M=5 is statistically definitive.** 5 reruns is a small sample. The 5/5 win is striking but a future operator could observe a 4/5 or 3/5 mix under different cache states, prompt-context shifts, or model-side stochasticity. The forcing function intentionally set the bar at ≥4/5 to leave room for one-shot variance; the 5/5 observed result exceeds that bar but does not eliminate the possibility of future selection drift.
- **Does not prove cold-prefix budget improves.** Cold probe still budget-exhausts at 130s. Description sharpening is orthogonal to cold-start inference cost.

---

## §7 Test suite snapshot (post-commit-2.5)

Full suite at HEAD `0011f1d`, ignoring known pre-existing port-binding flake in `test_console_stdio_cleanliness.py`:

```
1902 passed, 19 skipped, 6 failed in 55.5s
```

**The 6 failures are pre-existing on main**, verified by reverting 24.6 docstring edits (via `git checkout HEAD~2 -- forge_bridge/tools/utility.py forge_bridge/tools/project.py`) and re-running tests — same 6 failures, same count. They are:

| Test | Surface | Pre-existing on main? |
|---|---|---|
| `test_public_api.py::test_no_forge_specific_strings` | public API string check | yes |
| `test_tool_contract_enforcement.py::test_pr22_every_registered_tool_satisfies_canonical_contract` | tool contract | yes |
| `test_typer_entrypoint.py::test_bare_forge_bridge_boots_mcp_not_help` | CLI bootstrap | yes |
| `test_typer_entrypoint.py::test_console_port_flag_sets_env` | CLI env wiring | yes |
| `test_utility_ping.py::test_ping_failure_path_echoes_bridge_url` | ping URL state | yes |
| `test_utility_ping.py::test_ping_failure_path_bridge_url_reflects_current_bridge_module_state` | ping URL state | yes |

None of these are 24.6 scope. They predate this phase; they remain after this phase; they're separate concerns for their respective subsystems.

**2 docstring-pin tests previously failing from 24.6 (cf5c7bf):**

| Test | Status pre-2.5 | Status post-2.5 |
|---|---|---|
| `test_docstring_teaches_canonical_positioning_not_escape_hatch` | failed (OLD-string pin) | passing (24.6 perturbation absorbed) |
| `test_docstring_teaches_when_to_use_and_when_not_to` | failed (line-wrap split phrase) | passing (whitespace-normalized substring check) |

Both updated in commit `0011f1d` to match the intentional 24.6 perturbation; SPIRIT of the 23.1 pins preserved (canonical-surface framing + escalation rule both still asserted, just adapted to the new prose shape).

---

## §8 Forcing-function evidence — summary

Per FRAMING §5 forcing-function metric: *"Across M=5 warm canonical reruns post-edit, the affordance prior shifts toward `flame_execute_python` and/or stabilizes across the loop, without any other architectural mutation."*

| Dimension | Required | Observed | Verdict |
|---|---|---|---|
| Initial-prior shift | iter-1 picks `flame_execute_python` in ≥4/5 warms | 5/5 | ✓ FULL |
| Prior stabilization | when iter-1 wrong, iter-3 not also wrong | vacuously — no iter-1 was wrong | ✓ vacuously satisfied |
| Substrate integrity | `result_hash 8ca14d1e` stable; recovery intact | byte-identical hash across 5 probes; recovery vacuous | ✓ FULL |
| Failure: no effect | unchanged selection | clearly changed | ✗ not triggered |
| Failure: recovery degrades | recovery fails when iter-N wrong | no iter-N wrong; envelope correct on all | ✗ not triggered |
| Failure: substrate drift | `result_hash` shifts | byte-identical | ✗ not triggered |

**Verdict:** all three pass dimensions fire; zero failure conditions triggered; pathology eliminated rather than mitigated.

---

## §9 Bundle contents

```
2026-05-18-phase-24-6-rerun/
├── run-probes.sh           — execution script (pre-flight check + probe runner)
├── run-log.txt             — script stdout (probe timings + log baseline tracking)
├── doctor-pre.txt          — fbridge doctor before probes (50 graphs)
├── doctor-post.txt         — fbridge doctor after probes (63 graphs; only delta)
├── cold-1-{meta,stderr,stdout}.txt   — cold probe (anti-scope; exit=3, 130s)
├── warm-1-{meta,stderr,stdout}.txt   — first warm (exit=0, 118s — partial cold-warm)
├── warm-2-{meta,stderr,stdout}.txt   — exit=0, 61s
├── warm-3-{meta,stderr,stdout}.txt   — exit=0, 62s
├── warm-4-{meta,stderr,stdout}.txt   — exit=0, 60s
├── warm-5-{meta,stderr,stdout}.txt   — exit=0, 54s
├── server-log-full.txt     — 440 daemon log lines bracketing the rerun window
└── server-log-key-lines.txt — filtered to per-iter dispatch + termination signals
```

All 5 warm-*-stdout.txt files MD5-identical: `d312645429693ba92c2fd7d97d30e18a`.

---

## §10 Methodology notes

### §10.1 Daemon restart was load-bearing

The MCP daemon caches tool schemas at registration time (server startup). The first rerun attempt (`bbzoh0ny5`) aborted at pre-flight because the daemon held OLD docstrings — pre-flight check (originally `fbridge actions show <name>` grep) initially used incorrect CLI syntax; once corrected to direct Python module introspection (`from forge_bridge.tools.utility import execute_python; assert "canonical introspection surface" in execute_python.__doc__`), the pre-flight passed and the rerun executed. Pre-flight discipline saved a noisy rerun cycle that would have produced 24.5-shaped data.

### §10.2 The warm-1 partial-cold-warm signature

warm-1 elapsed 118s vs warm-2..5's ~60s. The preceding cold probe (130s budget exhaust) did NOT successfully warm the model's KV cache — the cold probe's iter-1 inference completed but loop budget ran out before iter-2 could start, leaving the cache in a partial-warm state. warm-1 paid the remaining warm-up cost on its iter-1 inference, then warm-2..5 hit the steady-state cache. This is a real signature of Phase 24.1 KV-cache dynamics; not a 24.6 anomaly.

### §10.3 Vacuous satisfaction is a stronger outcome than the framing anticipated

FRAMING §5 expected a measurement scenario where iter-1 selection might shift partially (e.g., 3/5) with iter-3 stabilization as the secondary win. The observed 5/5 makes iter-3 stabilization a vacuous question. Per [[feedback-distinct-success-criteria-per-adjacent-layer]], it's worth naming that vacuous satisfaction at the prior-stabilization dimension reflects a stronger-than-expected initial-prior shift, not an absence of evidence at that dimension. The §5 framing was structured to handle the partial case; the actual outcome simply doesn't require it.

### §10.4 The 2 broken tests as an in-scope D-04 consequence

Phase 23.1 pinned exact docstring strings in `test_flame_execute_python.py` to prevent regression. 24.6's intentional perturbation of those exact strings caused the pins to fail. Per FRAMING §13 footnote, this is a D-04 fix-as-discovered consequence of the in-scope edit, not a scope expansion. Commit 2.5 (`0011f1d`) updates the pins to match 24.6's perturbed language; the SPIRIT of the 23.1 pins (canonical-surface framing + escalation rule) is preserved at the new shape.

### §10.5 Cold path improvement is incidental, not load-bearing

Cold probe iter-1 picked `flame_execute_python` (24.4/24.5 cold picked `flame_find_media`). This is a small win — cold-path affordance is also corrected by the same docstring edit — but it does NOT make the cold probe fit in budget. Per §5 cold probes are anti-scope verification only; this improvement is recorded for archaeology but does not factor into pass-criterion evaluation.

---

## §11 Status

**Phase 24.6 forcing function: PASSED across all three pass dimensions.**

- Initial-prior shift: 5/5 (criterion ≥4/5) — exceeded
- Prior stabilization: vacuously satisfied (no iter-1 mis-selection occurred)
- Substrate integrity: result_hash byte-stable; warm stdouts MD5-identical

The unstable-affordance-prior pathology surfaced at framing §1 (iter 1 AND iter 3 both pick `flame_find_media` for the canonical query class) is eliminated. The model now selects `flame_execute_python` at iter 1 deterministically across 5 independent warm probes; K=2 fires at the earliest possible iteration (iter 2); two wasted iterations per probe disappear.

Anti-scope §10 honored verbatim throughout: no orchestrator-side synthesis, no prompt injection, no system-message changes, no tool-list narrowing, no semantic-equivalence result-hashing, no cross-provider reach. The single perturbation was the two tool docstrings + a Phase 23.1 test-pin update consequence (commit 2.5).

Phase status: ready for PR open against `main`. Awaiting commit 3 (this document) + PR creation.

Disposition of §11 register item (1) updated per §6.4 honest epistemic state — see §12 below.

---

## §12 §11 deferral register — disposition update

Per §6.4, the evidence-base for 24.6 strongly supports the docstring-ordering hypothesis but does not yet fully isolate it from accumulated interaction familiarity. This produces a **partial-closure** disposition for item (1) of the 24.5 inherited register, not a full-closure: the pathology is empirically eliminated under the conditions tested, but the *causal* claim that the docstring-perturbation was solely responsible is one falsification surface away from clean isolation.

### §12.1 Item (1) disposition: PARTIAL CLOSURE

| Item | 24.5 disposition | 24.6 disposition |
|---|---|---|
| (1) Affordance-selection regression | Deferred-not-rejected; trigger: future phase under separate framing | **PARTIAL CLOSURE** under warm canonical-query conditions. Pathology eliminated in 5/5 warm probes; topology collapse (vs memorized-trajectory shape) is differentiating signal that weakens but does not eliminate the mechanism-(1) null. Re-eval trigger for *full* closure: query-class generalization probe under cold conditions (see §12.3). |

The honest framing matters because:

- A "closed" disposition would imply the docstring-ordering hypothesis is established. It isn't yet — it has strong evidence but isn't isolated from session-familiarity confound.
- A "still-deferred" disposition would imply 24.6 didn't move the needle. It did — pathology was eliminated under tested conditions, topology collapsed cleanly, anti-scope was honored.
- "Partial closure" is the truthful intermediate state. The next phase's design follows from naming it as such.

### §12.2 Item (8) status note

Item (8) — SSE consumer adoption in CLI + Console — was added at 24.5 close. Disposition unchanged at 24.6 (trigger: dedicated Layer C consumer-perception phase opens). 24.6 did not touch SSE consumer surfaces; this item remains explicitly unbound per `[[feedback-explicitly-unbound-vs-implicitly-rejected]]`.

### §12.3 New item (9): Query-class generalization probe under cold conditions

| | |
|---|---|
| **Item** | (9) Query-class generalization for affordance-selection sharpening |
| **Status** | NEW at 24.6 close; explicitly unbound pending evidence |
| **Re-eval trigger** | A dedicated phase (24.7 candidate) opens to run the discriminator probe; alternatively, if a future operator surfaces affordance-relapse evidence on a novel query class, that's a trigger to open immediately. |
| **Design (the clean discriminator)** | Cold daemon, cold model state, unseen-but-structurally-similar query class, same docstrings as 24.6 cf5c7bf, same tool inventory. Candidate queries: "List the clips in Reel 3", "What clips are inside the current desktop reel?", "Enumerate the sequence contents of Reel A." If iter-1 picks `flame_execute_python` on novel-but-homologous queries from a cold daemon (cache-empty), that strongly favors mechanism (3) over mechanism (1). |
| **What it would prove if positive** | The 24.6 affordance prior shift generalizes to homologous queries the model hasn't seen recently — strong evidence that the docstring-ordering perturbation produced a genuine prior shift, not session-conditioned familiarity. |
| **What it would prove if negative** | The model picks `flame_find_media` on novel queries despite the new docstrings — evidence that 24.6's win was substantially session-familiarity, and the descriptive perturbation alone is insufficient. That would trigger a different intervention class (catalogue sweep, signature changes, or accepting affordance-selection as an inference-time concern rather than a description-time one). |

### §12.4 New item (10): Full `flame_*` catalogue sweep

| | |
|---|---|
| **Item** | (10) Description sharpening sweep across remaining `flame_*` tools |
| **Status** | NEW at 24.6 close (was deferred-implicit at framing §3); explicitly named here for register hygiene |
| **Re-eval trigger** | Affordance-relapse evidence emerges on a query class targeting one of the 12 unmodified `flame_*` tools (e.g., timeline traversal mis-selecting against `flame_get_sequence_segments`, batch graph traversal mis-selecting against `flame_inspect_batch_xml`). |
| **Why deferred at 24.6** | Single-perturbation discipline per FRAMING §3; sweeping the catalogue at 24.6 would weaken causal clarity. The remaining 12 tool descriptions are inherited baseline at 24.6 close. |
| **Relationship to item (9)** | (9) tests whether 24.6's edits generalize to homologous queries; (10) tests whether the rhetorical inversion pattern needs to extend to the catalogue. Either could surface independently. |

### §12.5 Items (2)–(7): unchanged at 24.6

All other inherited items from 24.5's register remain at their 24.5 dispositions, verbatim trigger wording preserved for archaeology fidelity per `[[feedback-counts-are-archaeology-grade]]`.

| Item | 24.5 disposition (preserved) |
|---|---|
| (2) Orchestrator-side terminal-content synthesis | Partial closure at 24.5 (consumer-side surfacing ships); orchestrator-side synthesis still deferred. Trigger: new consumer surface (e.g. MCP resource consumer) emerges. |
| (3) D-07 unification | Deferred-not-rejected. Trigger: cross-pathology evidence emerges. |
| (4) Cross-provider Anthropic termination semantics | Deferred-not-rejected. Trigger: Anthropic-routing daily path activation. |
| (5) Token-delta streaming | Deferred-not-rejected. Trigger: native `tool_calls` reliability change in adapter layer. |
| (6) Adaptive / per-tool K | Deferred-not-rejected. Trigger: evidence K=2 is wrong for some tool class. |
| (7) Result-hash normalization | Deferred-not-rejected. Trigger: evidence exact-bytes insufficient. |

### §12.6 Register hygiene note

The §11 register at 24.6 close has **10 items** (8 inherited from 24.5 + 2 new from 24.6 close). Item (1) is in partial-closure status — neither fully closed nor active scope. This is the first §11 register entry with a partial-closure disposition; the partial-closure language is load-bearing per §6.4 and should not be collapsed into either "closed" or "deferred" at future syntheses without re-reading the §6 epistemic argument.

