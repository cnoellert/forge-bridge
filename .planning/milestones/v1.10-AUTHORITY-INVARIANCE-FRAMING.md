---
milestone: v1.10
milestone_name: Authority Invariance — operator authority and execution eligibility must not depend on routing path
status: cycle-3-draft
drafted: 2026-06-01
type: milestone-framing
preceded_by: v1.9 Conversational Reads (CR.1 closed 2026-06-01 — mechanism shipped, corpus frozen, seam-mismatch finding handed forward)
opens_when: the room ratifies this framing; the phase framing(s) derive from it
artifact_role: load-bearing architectural framing — the v1.10 phase framing(s) derive from this. Re-aims the work the CR.1 corpus surfaced and, through the room's cross-voice fold, reframes it: the failures are not "dispatch is broken" but "the routing layer has accumulated responsibilities that exceed routing" — it now decides authority and eligibility, which must be invariant to it.
prior_art:
  - .planning/phases/CR.1-conversational-reads-answer-pass-spike/CR.1-CLOSE.md (the close that handed this forward; §Ranked-input ①②)
  - UAT/CR.1-dogfood-findings.md (the wire ledger; the 7/9-compile-layer failure distribution; §7 ranked input)
  - .planning/phases/A.2-thread-a-ratification-enforced-apply/A.2-CLOSE.md (Thread A: operator authority via AssentRecord — the invariant Hole B silently broke)
  - forge_bridge/console/_chat_compile.py:158 (graph_contains_commit_node — the SOLE mutating gate) + :100-112 (would_mutate/requires_ratification derive from commit token) + :62-86 (build_compile_system_prompt — the loose `commit` instruction)
  - forge_bridge/console/_step.py:326-337 (the exactly-one-or-die resolver; candidates already in hand at :334)
  - forge_bridge/console/handlers.py:528 (_execute_forced_tool def) + :720-722 (the unmediated call_tool — NO gate; the exact insertion site) + :603/:678 (DISAMBIGUATION_KEY/UNRESOLVED_KEY — param stops, NOT safety guards)
  - forge_bridge/graph/commit.py:75/93 (is_commit_step / graph_contains_commit_node — the commit-token detection behind Symptom 1A)
  - forge_bridge/server.py:39-69 (readOnlyHint/destructiveHint live ONLY in this orphan; its first import is `from forge_mcp.tools` — the DEAD package; live is forge_bridge.mcp — so the annotation surface is not runtime-reachable)
  - memory: feedback-pre-orchestration-resolution-paralysis (the Symptom 2 shape — relax discovery, tighten action)
  - memory: feedback-orchestrator-control-flow-not-meaning (the guard the arbitration layer must stay inside)
  - memory: project-substrate-to-usability-crossing (why these are the right failures to chase now)
  - memory: feedback-baseline-drift-invalidates-controls (contemporaneous baseline re-run before measuring intervention)
  - .planning/milestones/v1.9-CONVERSATIONAL-READS-FRAMING.md (structural mirror + §consequence-analysis convention)
---

# v1.10 Authority Invariance — Routing Must Not Alter Authority or Eligibility

> **What this artifact is.** Cycle-3 writing-room framing for v1.10, drafted
> from the CR.1 close + dogfood corpus and reframed through the room's
> cross-voice fold (DT grounding / Creative experience / Orch synthesis).
> Compact per [[feedback-cadence-artifacts-shrink-to-load-bearing]].
>
> **What this artifact is not.** Not a roadmap, not a phase plan. Not a
> reopening of substrate authority, the ratify/assent discipline, or the CR.1
> answer-pass — all stay closed and unregressed. v1.10 touches the **routing
> layer at the compile→dispatch seam**, nothing downstream of a
> correctly-routed step.
>
> **The one-line thesis.** There is **one seam, two symptoms, one invariant.**
> The CR.1 dogfood read as "dispatch is broken" — but the room's fold found the
> deeper shape: **the routing layer has accumulated responsibilities that
> exceed routing.** It now silently decides whether a mutation reaches the
> authority chain, whether a read reaches execution, and whether a user sees
> progress or paralysis. Both failures violate a single invariant:
>
> > **Operator authority and execution eligibility must not depend on which
> > routing path was selected.**
>
> Hole B says *authority changes when routing changes.* Resolver paralysis says
> *eligibility changes when routing cannot establish a path.* v1.10 restores
> the invariant.

## The transition

v1.6 made the substrate operable; v1.7 shipped the artist surfaces; v1.8
projected authority to the Console; v1.9 stood up the read answer-pass as a
pressure instrument and **ran it** (CR.1 dogfood, 2026-06-01). The instrument
did its job: the corpus's dominant signal is not answer quality. Every tool
that *executed* returned correct data (iteration `6`, batch groups, reels). The
failures cluster at the compile→dispatch seam — and when the room looked at
*why two unlike failures (an authority breach and a usability dead-end) emerge
from the same physical location*, the answer was the routing layer doing more
than routing. That diagnosis, not "dispatch integrity," is the milestone.

## One seam, two symptoms (grounded — live reads 2026-06-01)

Both symptoms originate at the **PR20 narrowing decision** — the point where the
candidate-tool set is filtered toward execution. A single survivor short-circuits
to the forced-tool path; multiple survivors hit the resolver. Same decision
point, two failure modes.

### Symptom 1 — authority changes when routing changes (SAFETY)

Two routing paths let a read reach a mutation; the second is the severe one.

- **1A — the commit gate (R4/R5).** `run_compile_branch` routes to the
  mutating-preview regime on one condition: does compiled step text contain a
  `commit` token? (`_chat_compile.py:158`, `graph_contains_commit_node`, def at
  `graph/commit.py:75/93`; `would_mutate`/`requires_ratification` derive at
  `:100-112`.) The classification is *correct*; the model emitted `commit` for a
  read. Result: an artist sees a **"Ratify & Apply" prompt** for a question.
  Gated, but mis-routed.
- **1B — the forced-tool path (R11), NO gate at all.** When narrowing yields one
  survivor, `_execute_forced_tool` (`handlers.py:528`) invokes it **directly** —
  *"the LLM router is not called on this path"* — and the unmediated call is at
  **`:720-722`** (`params = normalize_tool_args(...)` → `raw = await
  _mcp_server.mcp.call_tool(...)`). **No commit gate, no `would_mutate`, no
  preview, no ratify** between resolution and that call. The only things that
  can stop it first are param-resolution failures (`DISAMBIGUATION_KEY :603`,
  `UNRESOLVED_KEY :678`) — *"I couldn't fill the args"* stops, **not** safety
  guards. R11 (`"duration in frames"`) mis-selected `flame_set_start_frames`
  (a mutation) and the forced path **executed it directly**; only a missing
  required param stopped the write.

  **Reachable, not theoretical (DT, pre-empting the Orch challenge).** 1B fires
  in a narrow window — narrowing must yield a *single* survivor, it must be a
  *mutation* tool, and its params must *resolve*. That window is real: R11
  demonstrated the selection and the execution; only a missing param — not any
  mutation check — stopped host state from changing. The narrowness is exactly
  why the §asymmetry ranks 1B non-negotiable and 1A "one careless ratify away."

**This is the invariant broken.** `_execute_forced_tool` was conceived as a
routing *optimization* (skip the LLM loop when the answer is unambiguous) — born
for single-result *reads*. The moment a mutation tool can be the sole survivor,
that optimization became **an alternate authority path with no authority** (the
A.2 AssentRecord chain is bypassed entirely). The fix is not new policy bolted
on; it is **enforcing an invariant that was always supposed to hold** — operator
authority is invariant to execution routing (Thread A) — and was never enforced
at this site. **`:720-722` is where the guarantee inserts.**

### Symptom 2 — eligibility changes when routing cannot establish a path (USABILITY)

When a compiled step token matches ≠1 tool, the resolver hard-stops
(`_step.py:326-337`) with `tool_selection_ambiguous` and leaks an internal hint
(*"Step matched 4 tools… use a more specific verb/noun"*). It **already carries
the candidate list** (`:334`) — the material to disambiguate — but instead of
arbitrating, it dies. Three dogfood reads (path / timewarp / duration of a shot)
hit this. The documented [[feedback-pre-orchestration-resolution-paralysis]]
shape: a strict exact-match resolver downstream of an ambiguity-prone compile.
Whether a read is *eligible to execute at all* depends on whether routing could
confidently choose — which is the invariant violated from the other side.

## Doctrine (carried, sharpened — not reopened)

- **The invariant is the milestone's law.** *Operator authority and execution
  eligibility must not depend on which routing path was selected.* Authority
  (who may authorize a mutation) and eligibility (whether a read may execute)
  are properties of the **request**, never of the **route**. Enforced at every
  routing site — the commit gate (1A), the forced-tool call at `:720-722` (1B),
  and the resolver (Symptom 2).
- **The arbitration layer is control-flow, not meaning**
  ([[feedback-orchestrator-control-flow-not-meaning]]). Selecting among
  candidate tools — or asking the operator a *structured* choice — is dispatch
  arbitration. It MUST NOT author prose, synthesize intent, or invent a tool
  outside the candidate set. The 24.4 guard stands.
- **The guard fails toward the read, not toward a dead-end** (Creative). When
  the authority guard blocks a mis-routed read from mutating, the artist must
  not hit a blocked-error wall — re-route to the *read* answer where one exists
  (a mis-selected `flame_set_start_frames` on "duration in frames" should fall
  back to answering the duration), or at minimum say plainly "I read that as a
  question and won't change anything." A safety guard that manufactures a new
  dead-end trades one bad experience for another.
- **The cut line is unchanged** (v1.9): explain facts that exist; invent
  nothing. v1.10 adds no new model authority; if it uses the model at all
  (Symptom 2 disambiguation), the model *selects from a provided set*.
- **Measure-first.** The intervention's effect is only legible against a
  *contemporaneous* failure-shape baseline
  ([[feedback-baseline-drift-invalidates-controls]]) — re-run the dogfood reads
  on current code before any fix; capture failure-shape, not just counts
  ([[feedback-failure-shape-stability-as-disposition-evidence]]).

## One invariant, two enforcement bars (§consequence-analysis)

The two symptoms share the invariant but are held to different bars — and the
room must vote on that:

| | Symptom 1 (authority) | Symptom 2 (eligibility) |
|---|---|---|
| Violation | authority changes with route | eligibility changes with route |
| Worst case | 1B: a read **executes a mutation directly, no ratify** (1A: one careless ratify away) | an artist gets a confusing dead-end and gives up |
| Bar | a **guarantee** — deterministic, tested invariant; "best-effort" is unacceptable for a safety property | **best-effort** — measurably fewer dead-ends; arbitration may legitimately still miss |
| Trust model | must NOT depend on a second LLM pass (don't guard a safety property with a probabilistic classifier) | MAY use a bounded LLM selection among candidates (control-flow) |

**Consequence:** Symptom 1 leads and is non-negotiable. Prompt-hardening of
`build_compile_system_prompt` (the loose `commit` instruction at `:82-86`) is a
**mitigation, not the guarantee** — it reduces bad compiles probabilistically;
the guarantee is deterministic and lives below the model, at the routing sites.

## The spine

**v1.10 restores authority invariance at the compile→dispatch seam: operator
authority and execution eligibility become properties of the request, enforced
identically regardless of which routing path was selected.** Symptom 1 (the
authority guarantee at 1A + 1B) is the load-bearing half; Symptom 2 (eligibility
arbitration) is the usability half. The answer-pass (CR.1) is left intact; v1.10
ensures more reads *reach* it correctly-routed and unambiguous.

The remaining CR.1 §7 items (session/project scope; answer-pass
refuse-on-missing-field + error triage) are **not** in the headline. The
corpus-instrument capture-seam fix is named as *enabling tooling* (Q-DI4)
because measuring either symptom honestly needs the abort/preview seams the
current instrument is blind to.

## Open questions for discuss (leans given)

- **Q-DI1 — where does mutation knowledge canonically live? (the reframed
  question — Creative).** Not *"how do we recover the annotation logic from
  `server.py`?"* — that framing almost guarantees the orphan becomes canon. The
  healthy question is *"what is the canonical owner of mutation knowledge after
  v1.10?"*, which leaves room to conclude **the orphan was wrong.** This guards
  against a failure mode the room has hit repeatedly: discover missing behavior
  → find surviving implementation → unconsciously elevate the survivor into
  authority (*archaeology becoming architecture*). **DT grounding:** the
  annotation surface is genuinely not runtime-reachable — `readOnlyHint`/
  `destructiveHint` exist only in `server.py:39-69`, whose first import is from
  the dead `forge_mcp.tools`; synth tools carry `readOnlyHint=False` always. So
  a deterministic read/mutate classification **must be built**, not read off.
  *Lean on the canonical home:* a declared property at the **tool-definition
  layer** (or a trait in `core/`), enforced at all three routing sites — not a
  lookup table inherited from the orphan. Establishing this is the milestone's
  first build dependency.
- **Q-DI2 — Symptom 2 arbitration mechanism.** *Lean: extend the existing
  narrowing machinery (reachability / PR14 / PR20 at `_step.py`); add a bounded
  candidate-selection step (deterministic ranking first; one LLM selection among
  the ≤5 candidates only if needed); NEVER leak "matched N tools."* **Creative:**
  the disambiguation must not leak **tool identifiers** either — `flame_get_clip`
  vs `forge_get_shot` is the same developer-facing disease one layer up. If the
  artist must choose, the choice is in task terms, not tool names.
- **Q-DI3 — does v1.10 touch the compile prompt?** *Lean: yes, as secondary
  mitigation only* — tighten the `commit` instruction opener
  ([[feedback-rhetorical-position-as-architectural-control-surface]]) — but the
  ratifiable deliverable is the deterministic guarantee, not the prompt.
- **Q-DI4 — corpus-instrument capture seam.** Fold the capture-on-abort/preview
  fix in as measurement tooling, or hold it? *Lean: fold the minimal version* —
  without it the milestone cannot measure Symptom 1's "reads no longer reach
  mutation" or Symptom 2's "fewer dead-ends" against the corpus.
- **Q-DI5 — baseline.** Re-run the 11 dogfood reads on current `main` for a
  contemporaneous failure-shape baseline before any change. *Lean: yes,
  prerequisite.* (Daemon torn down at CR.1 close; same stdio-held-open bring-up.)

## Constraints (binding)

- **Public surface frozen:** `forge_bridge.__all__` stays **19**; no new external
  libraries. **Milestone number vs package version are decoupled** (Orch): the
  *milestone* is `v1.10`; the *package* stays on the `1.5.x` line — consistent
  with v1.5–v1.9 all riding package `1.4.1 → 1.5.1`. `v1.10` implies no `1.10.0`
  bump; discuss sets the actual patch/minor.
- **Authority invariance becomes a *tested invariant*** at the routing sites —
  the milestone's acceptance criterion. A read cannot reach a mutation by *any*
  route; eligibility does not depend on routing confidence.
- **No regression of the CR.1 answer-pass** or the preview→ratify→apply path; the
  guarantee *prevents reads from entering* the mutating path, it does not modify
  the mutating path.
- **No new model authority.** Arbitration selects; it never authors.
- **Reads-only stays reads-only.** Nothing here touches `AssentRecord`, ratify,
  or apply semantics.

## Status

**Cycle-3 milestone-framing draft, 2026-06-01** (DT grounding + Creative
experience + Orch synthesis converged). The room resolved the identity question:
**not a dispatch milestone, not split into two — one seam-invariance milestone
whose most important invariant is authority.** Cycle-3 changes:

- **Reframed (Creative→Orch convergence):** "dispatch is broken" → "routing has
  accumulated responsibilities that exceed routing." Renamed **Dispatch
  Integrity → Authority Invariance**. Both failures recast as symptoms of one
  invariant: *operator authority and execution eligibility must not depend on
  routing path.* Not split — DT's grounding (both symptoms originate at the PR20
  narrowing decision, one physical seam) makes splitting a fracture of one code
  seam.
- **DT — exact insertion site:** 1B's no-gate call is `handlers.py:720-722`
  (cited now, not just the `:528` def); the only short-circuits are param stops
  (`:603/:678`), not safety guards. The reachability pre-emption ("reachable, not
  theoretical") is stated to convert the likely Orch objection into a
  strengthened claim.
- **DT — Q-DI1 confirmed:** `server.py` imports the dead `forge_mcp.tools`;
  annotation surface not runtime-reachable; classifier must be built.
- **Creative — Q-DI1 reframed to ownership:** *where does mutation knowledge
  live* (orphan as evidence, not authority), naming the archaeology-becomes-
  architecture failure mode. Plus the two guard constraints (no tool-identifier
  leak; fail-toward-the-read).

Five discuss questions carry leans, not rulings. Ready for ratify, then
v1.10-discuss (Q-DI1 — canonical ownership of mutation knowledge — is the first
decision; it carries the build dependency).

---

*Drafted 2026-06-01 from the CR.1 close + dogfood corpus; reframed through the
cross-voice fold. Grounded against live `_chat_compile.py` / `_step.py` /
`handlers.py` / `server.py`; no claims from the findings doc alone — every
routing site was read. The re-aim is the asset CR.1 paid for.*
