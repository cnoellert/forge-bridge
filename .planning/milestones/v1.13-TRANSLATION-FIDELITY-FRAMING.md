# v1.13 — Translation Fidelity (MILESTONE FRAMING)

**Opens:** active milestone, after **v1.12 Mutation Delegation CLOSED** (2026-06-02; C2 delivered +
live-verified, Shape B deferred). **Foundation:** `.planning/phases/NLT-nl-translation-layer/
NLT-DISCOVERY-CLOSE.md` (discovery done; spine grounded; Q0/Q2 resolved).
**Doctrine:** `[[feedback-substrate-pass-translation-open]]`.
*("Fidelity" = does the chain-step graph faithfully represent operator intent — exactly what Phase 3
validation measures.)*

---

## Thesis

C2's live E2E proof relocated the frontier: the substrate is trustworthy and execution is solved, so the
weak point is the path from human language into the substrate. The NLT discovery established that **the
translation layer is not missing — it exists, scattered and implicit, and runs today** (`compile_intent`,
tool filter/narrow, `resolve_query_entities`, `extract_explicit_params`, source/executor routing). v1.13's
job is to **formalize, bound, and validate** that machinery as a first-class subsystem — **not** a greenfield
build, and **not** "make chat smarter." The translation/substrate boundary is the chain-step graph;
translation success and substrate success become *separately measurable* verdicts.

Grounded by evidence, not intuition: the D-series partitioned "rename via chat" into **three defects /
three classes / three owners** (grounding · routing+extraction · contextual-resolution), all visible at the
graph boundary, none in the substrate.

---

## Relationship to v1.12 (sibling, not fold)

NLT spans **reads and mutations**; v1.12 (Mutation Delegation) was mutation-authority-scoped. Folding NLT
under v1.12 would recreate the arbitration→everything drift the boundary discipline prevents (DT), so v1.13
is a separate milestone. **v1.12 CLOSED 2026-06-02** (`v1.12-MILESTONE-CLOSE.md`): C2 delivered +
live-verified; **Shape B deferred** as a named future motion with its maturation condition explicitly unmet
(ships only if the room judges Window-2 preview→apply drift unacceptable in production). NLT outranks Shape
B on live evidence — language→substrate correctness is the more pressing pain. One active milestone on the
board: v1.13.

---

## Phases (measure-first-gated; DT/Creative review hardenings folded)

1. **Formalize** — lock the boundary contract + bound the ~6-component inventory. **Contract axis (TF.1
   discuss, reframed by DT+Creative): compile-resolved vs context-resolved, NOT reads-vs-mutations.**
   Compile-resolved params → the chain-step graph IS the full contract. Context-resolved params (contextual
   refs needing dispatch/desktop state — "this sequence", "last 013 shot", derived step-text) → **two-point
   contract** (graph captures intent + dispatch resolves), and this cuts across reads *and* mutations (a
   contextual mutation straddles too — resolution lands post-ratify). Two clauses ride with it: the **Shape-A
   coupling** (resolving a contextual mutation target after ratify *is* Shape A — preview shows an unresolved
   ref; desktop-at-compile is the Shape-B-era concern) and the **integrity invariant** (below). Phase 3
   cannot validate what Phase 1 never defines. (Discovery pre-delivered the compile-resolved half.)
2. **Taxonomy** — formalize the five translation failure classes: grounding · routing · extraction ·
   entity-resolution · **contextual/stateful resolution**. (Five is complete; capability-gap stays OUT —
   substrate work.) **But name the capability-gap verdict-pair** (Q1): the oracle must recognize *honest
   decline because capability is absent* as **translation-PASS + substrate-GAP** (R9 = the canonical case),
   distinct from a mis-route (translation-FAIL). "Detect-no-capability-and-decline-honestly" is a translation
   **success criterion** — so Phase 4 never optimizes an honest can't into a hallucinated route.
3. **Validation — split 3a / 3b** (DT Q3; you can't measure-first with an unvalidated instrument,
   `[[feedback-audit-before-overfit]]`; v1.10 carried a 3× measurement-debt pattern):
   - **3a — ground the oracle (the milestone KEYSTONE).** Build a **labeled reference corpus** (correct
     graph + resolved params per NL input — CR.1/E2E are *unlabeled* seeds); **build + validate the
     example-fill detector**, measuring its **false-positive rate** (a value matching a docstring example is
     NOT proof it was lifted — a real sequence may legitimately be `30sec_21`); resolve the **reads two-part
     contract** (Q2) so reads are scored at both points. 3a must land before 3b can gate anything.
   - **3b — run the oracle** → independent **translation-pass / substrate-pass** verdicts + the
     example-fill-vs-grounded frequency that ranks Phase 4.
4. **Quality** — evidence-ranked **smallest slices**, each independent, ordered by 3b's measured frequency:
   - strip liftable *value* examples (keep vocab as enum) — D3's 3-site prefix class + sequence-name sites +
     the cross-tool read-docstring bleed (`[[feedback-rhetorical-position-as-architectural-control-surface]]`).
     **Ranking note (T1 finding B): this slice RESTORES a safety net, not just removes a defect** — a lifted
     example masquerades as explicit and bypasses the existing honest-decline guard (`_step.py:407`); with no
     example to lift, the param stays unresolved and the guard fires ("specify the exact sequence"). Rank it
     on *uncertainty-preservation*, not just bad-fill rate;
   - PR20-shadows-compile + `extract_explicit_params` parse fix (defect #2);
   - contextual world-state injection at **dispatch** (defect #3 — wire `desktop` into the dispatch instance
     of `resolve_query_entities`, where live state is reachable; NOT compile, per the reframe);
   - the per-param provenance signal (see "one artifact" below) at the existing gate — the *detection*
     complement to example-strip's *prevention* of the same false-certainty failure (TF.1-CONTRACT §5).

---

## The oracle is ONE artifact (DT ↔ Creative reconciliation)

DT's Q3 reference-corpus, Creative's operator-facing validation surface, and the Q2 provenance flag are the
**same artifact seen from different sides.** A per-param provenance signal — **`grounded-from-intent` /
`grounded-from-context` / `filled-from-example` / `unresolved`** — is *simultaneously*: validation data
(3b's measurement), oracle output, debugging information, and operator-facing feedback at the preview gate.
**Build one surface, not four.** That one signal is what the example-fill detector emits, what the labeled
corpus scores against, what ranks Phase 4, and what shows the operator *"sequence_name: filled from example,
not grounded"* before they ratify — which is exactly the cut-line doctrine (a value filled from an example
is a fact-that-doesn't-exist presented as grounded). This convergence — one artifact serving measurement,
diagnosis, and operator understanding — is the milestone's healthiest structural sign.

## Guardrails (binding)

- **Decompose → rank → measure-first; smallest useful slices; reject the monolith.** The room's record is
  that grounding *shrinks* scope (C2 got smaller; DI.2 T4 gated out). Phase 3 before Phase 4 is the antidote
  to a never-landing grand framing.
- **Ground the instrument before it gates** (`[[feedback-audit-before-overfit]]`). 3a validates the oracle —
  including the example-fill detector's false-positive rate — before 3b's numbers rank anything. "Build
  validation → *validate the validation* → rank quality," not "build validation → rank quality" (Creative).
  The oracle is itself an instrument that can carry measurement debt (v1.10's 3× pattern); 3a is where that
  debt is paid, and it is the milestone keystone — the measure-first spine is only as trustworthy as 3a.
- **Ratification-integrity invariant (first-class contract clause):** *dispatch may resolve refs the graph
  left unresolved (contextual → concrete); it may NEVER override a param the operator explicitly ratified.*
  This is the line between Shape A working (resolving "this sequence" at dispatch) and a hidden mutation of
  the ratified graph. TF.1 audits the `_step.py:254` merge against it specifically (resolves-unresolved-yes /
  overrides-explicit-no).
- **Q2 provenance principle:** shallowest-useful representation (per-param provenance at the existing
  preview gate); measure-first any depth beyond it; reject a speculative confidence-scoring /
  multi-candidate framework built before evidence.
- **Formalize, don't rewrite** (`[[feedback-brief-examples-as-behavioral-reference-shapes]]`): preserve the
  battle-tested topology; harden the weak spots.

---

## Scope boundaries

- **IN:** formalize + bound + validate the existing translation layer (Phases 1–3); evidence-ranked quality
  slices (Phase 4).
- **DEFERRED (named future motions):** **Shape B** (manifest-ratification, Window-2 drift — re-rankable
  against NLT evidence); **tool-authoring contract** (D3's "translation reasons over the whole tool corpus";
  descriptions serve 3 conflated roles — own grounding, don't open now).
- **OUT:** Layer C / substrate (done — C2); bootstrap-console-executor-gap (operational); publish executor
  (own motion).

---

## Carry-forwards folded in

- **R7 session/project scope** and DI.2's dormant **context-eligibility seam** are adjacent to the
  contextual/stateful-resolution class (Phase 2/4) — fold into that class rather than carry separately.
- **R9 timewarp** capability tool stays a separate forge-pipeline motion (not translation).

---

## Status / open questions

- **Room review COMPLETE (2026-06-02).** DT + Creative both endorsed for Phase 1 planning; three hardenings
  folded above (Phase 1 reads-contract decision · Phase 2 capability-gap verdict-pair · Phase 3 split
  3a/3b + the one-artifact reconciliation). No scope change, no discovery reopened, formalize-not-build
  posture intact. **Phase 1 planning may now proceed.**
- **Phase 1 sizing → milestone shape (open, resolved by Phase 1).** Discovery sized the surface (~6
  components, ≥3 sites, 3+ classes); Phase 1 ratifies whether all four phases land in v1.13 or Phase 4's
  slices spill to a successor. Phase 3a's keystone weight may itself argue for a tighter v1.13 (e.g. land
  through 3b + a first Phase-4 slice, spill the rest).

*(Resolved: version/name = v1.13 Translation Fidelity; v1.12 disposition = closed with C2 + Shape B
deferred — both at the v1.12 close, 2026-06-02.)*
