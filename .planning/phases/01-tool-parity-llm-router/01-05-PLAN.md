---
phase: 01-tool-parity-llm-router
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - forge_bridge/tools/batch.py
  - forge_bridge/tools/reconform.py
  - forge_bridge/tools/switch_grade.py
autonomous: true
requirements: [TOOL-02, TOOL-06, TOOL-07]

must_haves:
  truths:
    - "batch.py exports inspect_batch_xml and prune_batch_xml functions"
    - "reconform.py exists as a new tool module with reconform_sequence function"
    - "switch_grade.py exists as a new tool module with switch_grade function"
    - "External dependencies (forge_batch_xml, catalog WebSocket) are stubbed with clear error messages"
  artifacts:
    - path: "forge_bridge/tools/batch.py"
      provides: "Two new batch XML inspection/pruning tools"
      contains: "async def inspect_batch_xml"
    - path: "forge_bridge/tools/reconform.py"
      provides: "Reconform sequence tool ported from projekt-forge"
      contains: "async def reconform_sequence"
    - path: "forge_bridge/tools/switch_grade.py"
      provides: "Switch grade tool with catalog stub"
      contains: "async def switch_grade"
  key_links:
    - from: "forge_bridge/tools/batch.py"
      to: "forge_mcp.bridge"
      via: "import"
      pattern: "from forge_mcp import bridge"
    - from: "forge_bridge/tools/reconform.py"
      to: "forge_mcp.bridge"
      via: "import"
      pattern: "from forge_mcp import bridge"
---

<objective>
Port batch.py additions (inspect_batch_xml, prune_batch_xml), create reconform.py, and create switch_grade.py from projekt-forge, stubbing external dependencies that don't exist in the standalone repo.

Purpose: Completes Flame tool parity for batch, reconform, and grade switching operations.
Output: Updated batch.py, new reconform.py, new switch_grade.py.
</objective>

<execution_context>
@/Users/cnoellert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cnoellert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tool-parity-llm-router/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port batch.py additions and create reconform.py</name>
  <read_first>
    - forge_bridge/tools/batch.py (full current file)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/batch.py (full source)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/reconform.py (full source)
    - .planning/phases/01-tool-parity-llm-router/01-RESEARCH.md (Pitfall 3: external script deps for batch XML)
  </read_first>
  <files>forge_bridge/tools/batch.py, forge_bridge/tools/reconform.py</files>
  <action>
1. **batch.py additions:**
   Port `inspect_batch_xml` and `prune_batch_xml` from projekt-forge's batch.py.
   
   CRITICAL: These functions in projekt-forge import from `forge_batch_xml` and `forge_batch_prune` which are external scripts not present in this repo. Handle this by:
   - Port the function signatures, Pydantic models, and docstrings as-is
   - For the implementation body, if it relies on `forge_batch_xml` or `forge_batch_prune`, wrap the import in try/except and raise a clear RuntimeError:
     ```python
     raise RuntimeError(
         "inspect_batch_xml requires forge_batch_xml script from flame_hooks. "
         "This dependency is not yet available in standalone forge-bridge."
     )
     ```
   - If the function uses only `bridge.execute()` with inline Python code (no external imports), port it directly
   - Change import from `from forge_bridge import bridge` to `from forge_mcp import bridge`

2. **reconform.py (new file):**
   Port the entire `reconform.py` from projekt-forge.
   - Copy all Pydantic models and async functions
   - Change `from forge_bridge import bridge` to `from forge_mcp import bridge`
   - The reconform tool should be standalone-compatible (it uses Flame API directly via bridge.execute, no external script deps according to research)
   - If any import from forge_bridge.client or forge_bridge.tools.catalog is found, stub it with a clear error
   - Include the module docstring from projekt-forge
  </action>
  <verify>
    <automated>python -c "from forge_bridge.tools.batch import inspect_batch_xml, prune_batch_xml; from forge_bridge.tools.reconform import reconform_sequence; print('batch+reconform OK')"</automated>
  </verify>
  <acceptance_criteria>
    - forge_bridge/tools/batch.py contains "async def inspect_batch_xml" and "async def prune_batch_xml"
    - forge_bridge/tools/reconform.py exists and contains "async def reconform_sequence"
    - Both files use "from forge_mcp import bridge" (NOT "from forge_bridge import bridge")
    - `python -c "from forge_bridge.tools.batch import inspect_batch_xml, prune_batch_xml"` succeeds
    - `python -c "from forge_bridge.tools.reconform import reconform_sequence"` succeeds
  </acceptance_criteria>
  <done>batch.py has 2 new XML tools (stubbed if external deps required), reconform.py is a new tool module with reconform_sequence.</done>
</task>

<task type="auto">
  <name>Task 2: Create switch_grade.py with catalog stub</name>
  <read_first>
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/switch_grade.py (full source)
    - .planning/phases/01-tool-parity-llm-router/01-RESEARCH.md (Pitfall 4: switch_grade catalog WebSocket + openclip writer deps)
  </read_first>
  <files>forge_bridge/tools/switch_grade.py</files>
  <action>
Port switch_grade.py from projekt-forge with the following approach:

1. Copy the full file structure: module docstring, imports, Pydantic models, function signatures.

2. Change `from forge_bridge import bridge` to `from forge_mcp import bridge`.

3. For the `switch_grade` function (the core Flame-side media swap):
   - If it only uses bridge.execute() with Flame Python code, port it fully
   - If it calls `_catalog_request()` or `_write_openclip_server_side()`, isolate those calls behind a try/except or conditional check

4. For `query_alternatives` (catalog WebSocket query):
   - Stub this function to return a clear error:
     ```python
     async def query_alternatives(params: QueryAlternatives) -> str:
         """Query available grade alternatives for a shot. Requires catalog server."""
         return json.dumps({
             "error": "query_alternatives requires the catalog WebSocket server (ws://127.0.0.1:9998) "
                      "which is part of projekt-forge infrastructure. "
                      "Use switch_grade directly if you know the target media path."
         })
     ```

5. For `_write_openclip_server_side` or similar helper:
   - If called by switch_grade, wrap in try/except with clear error message
   - The switch_grade function should work for direct media path swaps without openclip

6. Remove any imports of `forge_bridge.client.async_client` or other modules that don't exist in standalone.
  </action>
  <verify>
    <automated>python -c "from forge_bridge.tools.switch_grade import switch_grade; print('switch_grade OK')"</automated>
  </verify>
  <acceptance_criteria>
    - forge_bridge/tools/switch_grade.py exists
    - Contains "async def switch_grade" with a Pydantic input model
    - Uses "from forge_mcp import bridge" (NOT "from forge_bridge import bridge")
    - Does NOT import from "forge_bridge.client.async_client" or "forge_bridge.tools.catalog"
    - `python -c "from forge_bridge.tools.switch_grade import switch_grade"` succeeds without errors
  </acceptance_criteria>
  <done>switch_grade.py exists with the core switch_grade function ported. Catalog query and openclip writer are stubbed with clear error messages.</done>
</task>

</tasks>

<verification>
- `python -c "from forge_bridge.tools.batch import inspect_batch_xml, prune_batch_xml; print('OK')"` succeeds
- `python -c "from forge_bridge.tools.reconform import reconform_sequence; print('OK')"` succeeds
- `python -c "from forge_bridge.tools.switch_grade import switch_grade; print('OK')"` succeeds
- `grep -r 'from forge_bridge import bridge' forge_bridge/tools/` returns 0 matches
</verification>

<success_criteria>
batch.py has inspect_batch_xml and prune_batch_xml. reconform.py is a new complete tool module. switch_grade.py is a new tool module with core functionality and stubbed external dependencies. All use correct import paths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-parity-llm-router/01-05-SUMMARY.md`
</output>
