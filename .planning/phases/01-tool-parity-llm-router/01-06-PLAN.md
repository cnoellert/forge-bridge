---
phase: 01-tool-parity-llm-router
plan: 06
type: execute
wave: 3
depends_on: [03, 04, 05]
files_modified:
  - forge_bridge/tools/project.py
  - forge_bridge/tools/utility.py
  - forge_bridge/server.py
autonomous: true
requirements: [TOOL-04, TOOL-05, TOOL-08]

must_haves:
  truths:
    - "project.py Pydantic models are verified complete for all parameterized functions"
    - "utility.py Pydantic models are verified complete for all parameterized functions"
    - "All new tool functions (from Plans 04 and 05) are registered as MCP tools in server.py"
    - "All parameterized MCP tools across all tool modules have Pydantic input models"
    - "server.py bulk_rename_segments registration points to a valid function"
  artifacts:
    - path: "forge_bridge/server.py"
      provides: "Complete MCP tool registrations for all new functions"
      contains: "flame_disconnect_segments"
    - path: "forge_bridge/tools/project.py"
      provides: "Verified Pydantic model coverage"
    - path: "forge_bridge/tools/utility.py"
      provides: "Verified Pydantic model coverage"
  key_links:
    - from: "forge_bridge/server.py"
      to: "forge_bridge/tools/timeline.py"
      via: "mcp.tool() registrations for new functions"
      pattern: "timeline\\.disconnect_segments"
    - from: "forge_bridge/server.py"
      to: "forge_bridge/tools/reconform.py"
      via: "import and mcp.tool() registration"
      pattern: "reconform\\.reconform_sequence"
    - from: "forge_bridge/server.py"
      to: "forge_bridge/tools/switch_grade.py"
      via: "import and mcp.tool() registration"
      pattern: "switch_grade\\.switch_grade"
---

<objective>
Verify project.py and utility.py Pydantic model coverage, register all new tool functions as MCP tools in server.py, and fix the bulk_rename_segments registration mismatch.

Purpose: Wires all new tools into the MCP server so they are callable by LLM agents. Completes the Pydantic model coverage requirement.
Output: Updated server.py with all new tool registrations, verified project.py and utility.py.
</objective>

<execution_context>
@/Users/cnoellert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cnoellert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tool-parity-llm-router/01-RESEARCH.md
@.planning/phases/01-tool-parity-llm-router/01-03-SUMMARY.md
@.planning/phases/01-tool-parity-llm-router/01-04-SUMMARY.md
@.planning/phases/01-tool-parity-llm-router/01-05-SUMMARY.md

<interfaces>
<!-- New functions available after Plans 04 and 05 -->

From forge_bridge/tools/timeline.py (8 new functions):
- disconnect_segments(params: DisconnectSegments) -> str
- inspect_sequence_versions(params: InspectSequenceVersions) -> str
- create_version(params: CreateVersion) -> str
- reconstruct_track(params: ReconstructTrack) -> str
- clone_version(params: CloneVersion) -> str
- replace_segment_media(params: ReplaceSegmentMedia) -> str
- scan_roles(params: ScanRoles) -> str
- assign_roles(params: AssignRoles) -> str

From forge_bridge/tools/batch.py (2 new functions):
- inspect_batch_xml(params: InspectBatchXml) -> str
- prune_batch_xml(params: PruneBatchXml) -> str

From forge_bridge/tools/reconform.py (new module):
- reconform_sequence(params: ReconformSequence) -> str

From forge_bridge/tools/switch_grade.py (new module):
- switch_grade(params: SwitchGrade) -> str
- query_alternatives(params: QueryAlternatives) -> str  (stubbed)

From forge_bridge/server.py (existing registration pattern):
```python
mcp.tool(
    name="flame_<tool_name>",
    annotations={
        "title": "<Human-Readable Title>",
        "readOnlyHint": True/False,
        "destructiveHint": True/False,
        "idempotentHint": True/False,
        "openWorldHint": False,
    },
)(module.function_name)
```

<!-- From Plan 03: register_llm_resources(mcp) is already called in server.py -->
<!-- This plan must NOT re-add that call. It should already be present. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify project.py and utility.py Pydantic coverage</name>
  <read_first>
    - forge_bridge/tools/project.py (full file)
    - forge_bridge/tools/utility.py (full file)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/project.py (for comparison)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/utility.py (for comparison)
    - .planning/phases/01-tool-parity-llm-router/01-RESEARCH.md (TOOL-04 and TOOL-05 notes)
  </read_first>
  <files>forge_bridge/tools/project.py, forge_bridge/tools/utility.py</files>
  <action>
1. **project.py verification:**
   - Read the standalone project.py and the projekt-forge project.py
   - Research says they are "structurally identical" -- verify this
   - Check that all parameterized functions (functions that take user input) have Pydantic BaseModel input types
   - Zero-arg functions like get_project() do NOT need Pydantic models
   - If any parameterized function lacks a Pydantic model, add one following the existing pattern
   - Ensure import is `from forge_mcp import bridge`

2. **utility.py verification:**
   - Research says standalone "already has Pydantic models for all utility tools" -- verify this
   - Check that execute_python has an input model (it takes `code` as a string param)
   - Check that execute_shortcut has an input model
   - Check that ping() has no model needed (zero-arg)
   - If any gap, add the missing model
   - Ensure import is `from forge_mcp import bridge`

If both are already complete (research suggests they are), document this with a brief comment and move on. No unnecessary changes.
  </action>
  <verify>
    <automated>python -c "
from forge_bridge.tools import project, utility
import inspect
# Check parameterized functions have type-annotated first args
for mod in [project, utility]:
    for name, fn in inspect.getmembers(mod, inspect.isfunction):
        if name.startswith('_'):
            continue
        sig = inspect.signature(fn)
        params = list(sig.parameters.values())
        if params:
            ann = params[0].annotation
            if ann != inspect.Parameter.empty:
                print(f'{mod.__name__}.{name}: {ann.__name__}')
            else:
                print(f'WARNING: {mod.__name__}.{name} has untyped param')
print('check done')
"</automated>
  </verify>
  <acceptance_criteria>
    - forge_bridge/tools/project.py uses "from forge_mcp import bridge"
    - forge_bridge/tools/utility.py uses "from forge_mcp import bridge"
    - All parameterized functions in both files have Pydantic BaseModel first argument annotations
    - `python -c "from forge_bridge.tools import project, utility"` succeeds
  </acceptance_criteria>
  <done>project.py and utility.py Pydantic model coverage is verified complete. Any gaps filled.</done>
</task>

<task type="auto">
  <name>Task 2: Register all new tools in server.py</name>
  <read_first>
    - forge_bridge/server.py (full current file with existing registrations -- note: Plan 03 will have already added register_llm_resources(mcp) and the LLM health import)
    - forge_bridge/tools/timeline.py (first 50 lines to confirm function names)
    - forge_bridge/tools/batch.py (last 100 lines to confirm new function names)
    - forge_bridge/tools/reconform.py (to confirm function names)
    - forge_bridge/tools/switch_grade.py (to confirm function names)
  </read_first>
  <files>forge_bridge/server.py</files>
  <action>
1. Add imports for new tool modules at the top of server.py, alongside existing imports:
   ```python
   from forge_mcp.tools import project, timeline, batch, utility, publish, reconform, switch_grade
   ```

2. Fix the `flame_bulk_rename_segments` registration if the function name doesn't match (Pitfall 7 from research). Check what the actual function name is in timeline.py and update the registration.

3. Add MCP tool registrations for ALL new functions. Follow the exact existing pattern. Use these names and annotations:

   **Timeline new tools:**
   - `flame_disconnect_segments` — title: "Disconnect Segments from Source Media", readOnly: False, destructive: False, idempotent: True
   - `flame_inspect_sequence_versions` — title: "Inspect Sequence Versions", readOnly: True, destructive: False, idempotent: True
   - `flame_create_version` — title: "Create Sequence Version", readOnly: False, destructive: False, idempotent: False
   - `flame_reconstruct_track` — title: "Reconstruct Track from Segments", readOnly: False, destructive: False, idempotent: True
   - `flame_clone_version` — title: "Clone Sequence Version", readOnly: False, destructive: False, idempotent: False
   - `flame_replace_segment_media` — title: "Replace Segment Media", readOnly: False, destructive: False, idempotent: True
   - `flame_scan_roles` — title: "Scan Track Roles", readOnly: True, destructive: False, idempotent: True
   - `flame_assign_roles` — title: "Assign Roles to Tracks", readOnly: False, destructive: False, idempotent: True

   **Batch new tools:**
   - `flame_inspect_batch_xml` — title: "Inspect Batch XML", readOnly: True, destructive: False, idempotent: True
   - `flame_prune_batch_xml` — title: "Prune Batch XML", readOnly: False, destructive: True, idempotent: True

   **Reconform:**
   - `flame_reconform_sequence` — title: "Reconform Sequence", readOnly: False, destructive: False, idempotent: False

   **Switch Grade:**
   - `flame_switch_grade` — title: "Switch Grade on Shot", readOnly: False, destructive: False, idempotent: True
   - `flame_query_alternatives` — title: "Query Grade Alternatives", readOnly: True, destructive: False, idempotent: True

   All tools have `openWorldHint: False`.

4. Place new tool registrations in logical sections:
   - Timeline tools section: add after existing timeline registrations
   - Batch tools section: add after existing batch registrations
   - New sections: "Reconform Tools" and "Grade Tools" before the Entry Point section

5. Do NOT re-add the register_llm_resources(mcp) call -- Plan 03 already added it. Verify it is present and leave it as-is.
  </action>
  <verify>
    <automated>python -c "
import forge_bridge.server as s
# Check that all new tool names are registered
expected = [
    'flame_disconnect_segments', 'flame_inspect_sequence_versions',
    'flame_create_version', 'flame_reconstruct_track', 'flame_clone_version',
    'flame_replace_segment_media', 'flame_scan_roles', 'flame_assign_roles',
    'flame_inspect_batch_xml', 'flame_prune_batch_xml',
    'flame_reconform_sequence', 'flame_switch_grade', 'flame_query_alternatives',
]
print(f'server.py imports: OK')
# The registrations are done at module level so if import succeeds, they registered
" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - forge_bridge/server.py imports reconform and switch_grade modules: "from forge_mcp.tools import project, timeline, batch, utility, publish, reconform, switch_grade"
    - forge_bridge/server.py contains `name="flame_disconnect_segments"`, `name="flame_inspect_sequence_versions"`, `name="flame_create_version"`, `name="flame_reconstruct_track"`, `name="flame_clone_version"`, `name="flame_replace_segment_media"`, `name="flame_scan_roles"`, `name="flame_assign_roles"`
    - forge_bridge/server.py contains `name="flame_inspect_batch_xml"`, `name="flame_prune_batch_xml"`
    - forge_bridge/server.py contains `name="flame_reconform_sequence"`, `name="flame_switch_grade"`, `name="flame_query_alternatives"`
    - forge_bridge/server.py contains "register_llm_resources(mcp)" (from Plan 03, should already be there)
    - `python -c "import forge_bridge.server"` succeeds without errors
  </acceptance_criteria>
  <done>All 13 new tool functions are registered as MCP tools in server.py. The bulk_rename_segments mismatch is fixed. LLM health resource is confirmed wired in (from Plan 03).</done>
</task>

</tasks>

<verification>
- `python -c "import forge_bridge.server"` succeeds (all registrations valid)
- `grep -c 'mcp.tool' forge_bridge/server.py` returns count >= 26 (original ~13 + 13 new)
- `grep 'flame_reconform_sequence\|flame_switch_grade\|flame_disconnect_segments' forge_bridge/server.py` returns 3 matches
- `python -m pytest tests/test_tools.py -x -q --timeout=10` -- unskip and run the export tests
</verification>

<success_criteria>
All 13 new tool functions are registered as MCP tools in server.py with correct names, annotations, and module references. project.py and utility.py Pydantic model coverage is verified complete. The server module imports cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-parity-llm-router/01-06-SUMMARY.md`
</output>
