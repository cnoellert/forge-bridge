---
phase: 01-tool-parity-llm-router
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - forge_bridge/mcp/server.py
  - tests/test_tools.py
  - tests/test_llm.py
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-04, TOOL-05, TOOL-06, TOOL-07, TOOL-08, TOOL-09, LLM-01, LLM-02, LLM-03, LLM-04, LLM-05, LLM-06, LLM-07, LLM-08]
gap_closure: true

must_haves:
  truths:
    - "All 13 Flame operations named in the ROADMAP are callable as MCP tools including flame_rename_segments"
    - "Wave 0 test stubs are unskipped and pass against implemented code"
    - "pytest tests/test_tools.py tests/test_llm.py exits 0 with no skipped tests"
  artifacts:
    - path: "forge_bridge/mcp/server.py"
      provides: "flame_rename_segments, flame_publish_sequence, flame_assemble_published_sequence MCP tool registrations"
      contains: "flame_rename_segments"
    - path: "tests/test_tools.py"
      provides: "Unskipped tool tests with corrected function names"
      contains: "def test_timeline_exports"
    - path: "tests/test_llm.py"
      provides: "Unskipped LLM tests with corrected imports and variable names"
      contains: "from forge_bridge.llm.health import register_llm_resources"
  key_links:
    - from: "forge_bridge/mcp/server.py"
      to: "forge_bridge/tools/publish.py"
      via: "flame_publish.rename_segments registration"
      pattern: "flame_rename_segments.*flame_publish\\.rename_segments"
    - from: "tests/test_llm.py"
      to: "forge_bridge/llm/health.py"
      via: "import register_llm_resources"
      pattern: "from forge_bridge\\.llm\\.health import register_llm_resources"
---

<objective>
Close two verification gaps from Phase 1: (1) register missing publish tools in the live MCP server, and (2) unskip all Wave 0 test stubs now that implementation has landed, fixing test bugs in the process.

Purpose: Phase 1 verification scored 4/5 — flame_rename_segments is not registered as an MCP tool, and 15 test stubs remain skipped providing zero automated validation of the implemented code.
Output: All 13 Flame operations callable via MCP, all Wave 0 tests passing green.
</objective>

<execution_context>
@/Users/cnoellert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cnoellert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tool-parity-llm-router/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register missing publish tools in live MCP server</name>
  <files>forge_bridge/mcp/server.py</files>
  <read_first>
    - forge_bridge/mcp/server.py (current server — see lines 196-399 for Flame tool registration block; note line 231 already registers flame_rename_shots from flame_timeline.rename_shots)
    - forge_bridge/tools/publish.py (source functions: rename_segments, publish_sequence, assemble_published_sequence)
  </read_first>
  <action>
In forge_bridge/mcp/server.py, inside the `try:` block that imports flame tool modules (starts around line 196), add publish tool registrations AFTER the existing grade tools block (after line 396, before the `except ImportError:` on line 398). Add the following THREE registrations only. Do NOT register flame_rename_shots — it is already registered at line 231 from flame_timeline.rename_shots and adding a duplicate would silently override or error.

```python
    # ── Publish Tools ──────────────────────────────────────────

    mcp.tool(
        name="flame_rename_segments",
        annotations={
            "title": "Rename Segments on Sequence",
            "readOnlyHint": False,
            "destructiveHint": False,
            "idempotentHint": True,
            "openWorldHint": False,
        },
    )(flame_publish.rename_segments)

    mcp.tool(
        name="flame_publish_sequence",
        annotations={
            "title": "Publish Sequence",
            "readOnlyHint": False,
            "destructiveHint": False,
            "idempotentHint": False,
            "openWorldHint": True,
        },
    )(flame_publish.publish_sequence)

    mcp.tool(
        name="flame_assemble_published_sequence",
        annotations={
            "title": "Assemble Published Sequence",
            "readOnlyHint": False,
            "destructiveHint": False,
            "idempotentHint": False,
            "openWorldHint": False,
        },
    )(flame_publish.assemble_published_sequence)
```

The `flame_publish` module is already imported on line 201. No new imports needed.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-bridge && python -c "import forge_bridge.mcp.server; print('server imports OK')" && grep -c "flame_publish\.rename_segments" forge_bridge/mcp/server.py && grep -c "flame_publish\.publish_sequence" forge_bridge/mcp/server.py && grep -c "flame_publish\.assemble_published_sequence" forge_bridge/mcp/server.py</automated>
  </verify>
  <acceptance_criteria>
    - forge_bridge/mcp/server.py contains the string `flame_rename_segments`
    - forge_bridge/mcp/server.py contains `flame_publish.rename_segments`
    - forge_bridge/mcp/server.py contains `flame_publish.publish_sequence`
    - forge_bridge/mcp/server.py contains `flame_publish.assemble_published_sequence`
    - forge_bridge/mcp/server.py does NOT contain a second `flame_rename_shots` registration (the one at line 231 from flame_timeline is the only one)
    - `python -c "import forge_bridge.mcp.server"` exits 0
  </acceptance_criteria>
  <done>Three missing publish functions (rename_segments, publish_sequence, assemble_published_sequence) registered as MCP tools in the live server. flame_rename_shots left untouched — already registered from timeline module.</done>
</task>

<task type="auto">
  <name>Task 2: Unskip Wave 0 tests and fix test bugs</name>
  <files>tests/test_tools.py, tests/test_llm.py</files>
  <read_first>
    - tests/test_tools.py (all skip decorators to remove)
    - tests/test_llm.py (skip decorators to remove, plus two bugs to fix)
    - forge_bridge/llm/router.py (confirm module-level constant is SYSTEM_PROMPT not FORGE_SYSTEM_PROMPT)
    - forge_bridge/llm/health.py (confirm register_llm_resources lives here, not in router.py)
    - forge_bridge/tools/reconform.py (confirm it exports reconform_sequence for pydantic coverage test)
    - forge_bridge/tools/switch_grade.py (confirm it exports switch_grade for pydantic coverage test)
  </read_first>
  <action>
**In tests/test_tools.py:**

1. Remove ALL `@pytest.mark.skip(...)` decorators from these functions:
   - test_timeline_exports (line 31)
   - test_batch_exports (line 57)
   - test_publish_exports (line 81)
   - test_reconform_exports (line 99)
   - test_switch_grade_exports (line 111)
   - test_project_models (line 123)
   - test_utility_models (line 150)
   - test_pydantic_coverage (line 176)

2. In `test_timeline_exports`, fix TWO stale function names in the `expected` list:
   - Replace `"get_sequence_info"` with `"get_sequence_segments"` (the function was renamed; `get_sequence_segments` exists at line 114 of timeline.py)
   - Replace `"bulk_rename_segments"` with `"rename_shots"` (the function was renamed; `rename_shots` exists at line 249 of timeline.py)

3. In `test_pydantic_coverage` (around line 184), update the module import list to include reconform and switch_grade for complete Pydantic coverage:
   Change:
   ```python
   from forge_bridge.tools import batch, project, publish, timeline, utility
   modules = [timeline, batch, publish, project, utility]
   ```
   To:
   ```python
   from forge_bridge.tools import batch, project, publish, reconform, switch_grade, timeline, utility
   modules = [timeline, batch, publish, project, utility, reconform, switch_grade]
   ```

**In tests/test_llm.py:**

1. Remove ALL `@pytest.mark.skip(...)` decorators from these functions:
   - test_llm_package_structure (line 25)
   - test_acomplete_is_coroutine (line 36)
   - test_complete_sync_wrapper (line 50)
   - test_env_var_override (line 65)
   - test_optional_import_guard (line 87)
   - test_health_check_shape (line 122)
   - test_health_resource_registered (line 137)

2. Fix bug in `test_env_var_override` (line 82): Change `router_mod.FORGE_SYSTEM_PROMPT` to `router_mod.SYSTEM_PROMPT`. The module-level constant in forge_bridge/llm/router.py is named `SYSTEM_PROMPT` (line 64 of router.py), not `FORGE_SYSTEM_PROMPT`. The env var that sets it is `FORGE_SYSTEM_PROMPT` but the Python constant is `SYSTEM_PROMPT`.

3. Fix bug in `test_health_resource_registered` (line 142): Change `from forge_bridge.llm.router import register_llm_resources` to `from forge_bridge.llm.health import register_llm_resources`. The function `register_llm_resources` is defined in `forge_bridge/llm/health.py` (line 7), not in `router.py`.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-bridge && python -m pytest tests/test_tools.py tests/test_llm.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tools.py contains zero occurrences of `@pytest.mark.skip`
    - tests/test_llm.py contains zero occurrences of `@pytest.mark.skip`
    - tests/test_tools.py test_timeline_exports expects `get_sequence_segments` (not `get_sequence_info`)
    - tests/test_tools.py test_timeline_exports expects `rename_shots` (not `bulk_rename_segments`)
    - tests/test_llm.py contains `router_mod.SYSTEM_PROMPT` (not `router_mod.FORGE_SYSTEM_PROMPT`)
    - tests/test_llm.py contains `from forge_bridge.llm.health import register_llm_resources`
    - tests/test_tools.py contains `from forge_bridge.tools import batch, project, publish, reconform, switch_grade, timeline, utility`
    - `python -m pytest tests/test_tools.py tests/test_llm.py -x -v` exits 0 with 0 skipped
  </acceptance_criteria>
  <done>All 15 Wave 0 test stubs unskipped, stale function names in test_timeline_exports corrected (get_sequence_segments, rename_shots), env var test references correct constant name SYSTEM_PROMPT, health resource test imports from correct module forge_bridge.llm.health, pydantic coverage test includes reconform and switch_grade, all tests pass green</done>
</task>

</tasks>

<verification>
1. `python -c "import forge_bridge.mcp.server"` exits 0
2. `grep -c "flame_publish\.rename_segments" forge_bridge/mcp/server.py` returns >= 1
3. `grep -c "flame_publish\.publish_sequence" forge_bridge/mcp/server.py` returns >= 1
4. `grep -c "flame_publish\.assemble_published_sequence" forge_bridge/mcp/server.py` returns >= 1
5. `grep -c "flame_rename_shots" forge_bridge/mcp/server.py` returns exactly 1 (no duplicate)
6. `grep -c "@pytest.mark.skip" tests/test_tools.py` returns 0
7. `grep -c "@pytest.mark.skip" tests/test_llm.py` returns 0
8. `grep "get_sequence_segments" tests/test_tools.py` returns a match
9. `grep "rename_shots" tests/test_tools.py` returns a match (in expected list, not bulk_rename_segments)
10. `python -m pytest tests/test_tools.py tests/test_llm.py -v` exits 0 with 0 skipped, 0 failed
</verification>

<success_criteria>
- All 13 Flame operations from ROADMAP Success Criterion 1 are registered as MCP tools in forge_bridge/mcp/server.py (was 12/13, now 13/13)
- No duplicate tool registrations (flame_rename_shots appears exactly once)
- All 15 Wave 0 test stubs unskipped and passing
- Zero test bugs remaining (stale function names fixed, SYSTEM_PROMPT reference fixed, register_llm_resources import fixed)
- Phase 1 verification score moves from 4/5 to 5/5
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-parity-llm-router/01-07-SUMMARY.md`
</output>
