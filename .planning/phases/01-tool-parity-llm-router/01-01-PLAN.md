---
phase: 01-tool-parity-llm-router
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - forge_bridge/bridge.py
  - tests/test_tools.py
  - tests/test_llm.py
  - tests/conftest.py
autonomous: true
requirements: [TOOL-09, LLM-05, LLM-08]

must_haves:
  truths:
    - "pip install forge-bridge works without openai or anthropic as hard deps"
    - "bridge.py default timeout is 60 seconds"
    - "pyproject.toml has no duplicate dependency declarations"
    - "Test scaffolds exist for all phase requirements"
  artifacts:
    - path: "pyproject.toml"
      provides: "Clean dependency declarations with [llm] optional extra"
      contains: "[project.optional-dependencies]"
    - path: "forge_bridge/bridge.py"
      provides: "Updated default timeout"
      contains: 'FORGE_BRIDGE_TIMEOUT", "60"'
    - path: "tests/test_tools.py"
      provides: "Test stubs for TOOL-01 through TOOL-09"
    - path: "tests/test_llm.py"
      provides: "Test stubs for LLM-01 through LLM-08"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
  key_links:
    - from: "pyproject.toml"
      to: "forge_bridge/llm/"
      via: "optional [llm] extra"
      pattern: 'llm = \[.*"openai'
---

<objective>
Fix pyproject.toml (remove duplicate deps, create [llm] optional extra), bump bridge.py timeout to 60s, and create Wave 0 test scaffolds for all phase requirements.

Purpose: Establishes the foundation for all subsequent plans -- clean packaging and test infrastructure.
Output: Fixed pyproject.toml, updated bridge.py, test scaffold files (test_tools.py, test_llm.py, conftest.py).
</objective>

<execution_context>
@/Users/cnoellert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cnoellert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tool-parity-llm-router/01-RESEARCH.md
@.planning/phases/01-tool-parity-llm-router/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pyproject.toml and bump bridge timeout</name>
  <read_first>
    - pyproject.toml (current state with duplicate deps)
    - forge_bridge/bridge.py (current BRIDGE_TIMEOUT default)
  </read_first>
  <files>pyproject.toml, forge_bridge/bridge.py</files>
  <action>
1. Edit pyproject.toml:
   - Remove ALL four occurrences of `"openai>=1.0"` and `"anthropic>=0.25"` from `[project.dependencies]`
   - The remaining dependencies list should be:
     ```
     dependencies = [
         "httpx>=0.27",
         "websockets>=13.0",
         "mcp[cli]>=1.0",
         "sqlalchemy[asyncio]>=2.0",
         "asyncpg>=0.29",
         "alembic>=1.13",
         "psycopg2-binary>=2.9",
     ]
     ```
   - Add `[project.optional-dependencies]` section (merging with existing `dev` extra):
     ```
     [project.optional-dependencies]
     llm = [
         "openai>=1.0",
         "anthropic>=0.25",
     ]
     dev = [
         "pytest",
         "pytest-asyncio",
         "ruff",
     ]
     ```

2. Edit forge_bridge/bridge.py:
   - Change line `BRIDGE_TIMEOUT = int(os.environ.get("FORGE_BRIDGE_TIMEOUT", "30"))` to `BRIDGE_TIMEOUT = int(os.environ.get("FORGE_BRIDGE_TIMEOUT", "60"))`
  </action>
  <verify>
    <automated>grep -c '"openai' pyproject.toml && grep -c '"anthropic' pyproject.toml && grep 'FORGE_BRIDGE_TIMEOUT.*60' forge_bridge/bridge.py</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml [project.dependencies] contains exactly 0 lines with "openai" or "anthropic"
    - pyproject.toml [project.optional-dependencies] section exists with `llm = [` containing "openai>=1.0" and "anthropic>=0.25"
    - pyproject.toml [project.optional-dependencies] section has `dev = [` with pytest, pytest-asyncio, ruff
    - forge_bridge/bridge.py line reads: BRIDGE_TIMEOUT = int(os.environ.get("FORGE_BRIDGE_TIMEOUT", "60"))
  </acceptance_criteria>
  <done>pyproject.toml has zero duplicate deps, openai/anthropic in [llm] extra only, bridge timeout defaults to 60s</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test scaffolds</name>
  <read_first>
    - tests/test_core.py (existing test patterns in the project)
    - forge_bridge/tools/__init__.py (current exports)
    - forge_bridge/tools/timeline.py (first 50 lines for import pattern)
    - forge_bridge/tools/batch.py (first 30 lines for import pattern)
    - .planning/phases/01-tool-parity-llm-router/01-VALIDATION.md (test map)
  </read_first>
  <files>tests/test_tools.py, tests/test_llm.py, tests/conftest.py</files>
  <action>
1. Create tests/conftest.py with:
   - A `monkeypatch_bridge` fixture that patches `forge_mcp.bridge.execute` to return a mock BridgeResponse
   - A `mock_openai` fixture that patches openai imports
   - A `mock_anthropic` fixture that patches anthropic imports

2. Create tests/test_tools.py with test stubs (all initially marked `@pytest.mark.skip(reason="Wave 0 stub")` to be unskipped as implementation lands):
   - `test_timeline_exports`: Verify timeline module exports: get_sequence_info, set_segment_attribute, bulk_rename_segments, get_sequence_editing_guide, disconnect_segments, inspect_sequence_versions, create_version, reconstruct_track, clone_version, replace_segment_media, scan_roles, assign_roles
   - `test_batch_exports`: Verify batch module exports: list_batch_nodes, get_node_attributes, create_node, connect_nodes, set_node_attribute, render_batch, batch_setup, get_write_file_path, inspect_batch_xml, prune_batch_xml
   - `test_publish_exports`: Verify publish module exports: rename_shots, rename_segments, publish_sequence, assemble_published_sequence
   - `test_reconform_exports`: Verify reconform module exists and exports: reconform_sequence
   - `test_switch_grade_exports`: Verify switch_grade module exists and exports: switch_grade
   - `test_project_models`: Verify project.py Pydantic models exist for all parameterized functions
   - `test_utility_models`: Verify utility.py Pydantic models exist for all parameterized functions
   - `test_pydantic_coverage`: Verify all parameterized tool functions have a Pydantic BaseModel as first argument type annotation
   - `test_bridge_timeout`: `from forge_mcp.bridge import BRIDGE_TIMEOUT; assert BRIDGE_TIMEOUT == 60` (NOT skipped -- can verify immediately)
   - `test_pyproject_no_duplicates`: Read pyproject.toml, parse dependencies, assert no duplicate package names and openai/anthropic NOT in [project.dependencies] (NOT skipped -- can verify immediately)

3. Create tests/test_llm.py with test stubs:
   - `test_llm_package_structure`: `from forge_bridge.llm.router import LLMRouter, get_router; assert callable(LLMRouter)`
   - `test_acomplete_is_coroutine`: `import asyncio; router = LLMRouter(); assert asyncio.iscoroutinefunction(router.acomplete)`
   - `test_complete_sync_wrapper`: Verify `router.complete` exists and is not a coroutine
   - `test_env_var_override`: Set FORGE_LOCAL_LLM_URL, FORGE_LOCAL_MODEL, FORGE_CLOUD_MODEL, FORGE_SYSTEM_PROMPT env vars via monkeypatch, verify router reads them
   - `test_optional_import_guard`: Verify importing forge_bridge.llm.router does NOT fail even without openai/anthropic at module level (lazy import guard)
   - `test_health_check_shape`: Verify ahealth_check() returns dict with keys "local", "cloud", "local_model", "cloud_model"
   - `test_health_resource_registered`: Verify register_llm_resources(mcp) registers a resource at "forge://llm/health"
   - `test_llm_shim_import`: Verify `from forge_bridge.llm_router import LLMRouter, get_router` still works (backwards compat shim)

   All LLM tests marked `@pytest.mark.skip(reason="Wave 0 stub")` except test_llm_shim_import which can test once the shim is created.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tools.py tests/test_llm.py -x -q --timeout=10 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py exists and contains "def monkeypatch_bridge"
    - tests/test_tools.py exists and contains "def test_timeline_exports", "def test_batch_exports", "def test_reconform_exports", "def test_switch_grade_exports", "def test_bridge_timeout", "def test_pyproject_no_duplicates"
    - tests/test_llm.py exists and contains "def test_llm_package_structure", "def test_acomplete_is_coroutine", "def test_health_check_shape", "def test_health_resource_registered"
    - `python -m pytest tests/test_tools.py::test_bridge_timeout tests/test_tools.py::test_pyproject_no_duplicates -x -q` passes (non-skipped tests green)
  </acceptance_criteria>
  <done>Wave 0 test scaffolds exist for all 17 phase requirements. Non-skipped tests (bridge_timeout, pyproject_no_duplicates) pass green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_tools.py::test_bridge_timeout -x` passes
- `python -m pytest tests/test_tools.py::test_pyproject_no_duplicates -x` passes
- `grep -c 'openai\|anthropic' pyproject.toml` in [project.dependencies] section returns 0
- `grep 'llm = \[' pyproject.toml` returns a match
</verification>

<success_criteria>
pyproject.toml is clean (no duplicate deps, [llm] extra exists), bridge timeout defaults to 60s, and test scaffold files exist for all 17 phase requirement IDs with stubs ready to be unskipped as implementation lands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-parity-llm-router/01-01-SUMMARY.md`
</output>
