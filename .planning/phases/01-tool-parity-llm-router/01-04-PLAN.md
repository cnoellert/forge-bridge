---
phase: 01-tool-parity-llm-router
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - forge_bridge/tools/timeline.py
  - forge_bridge/tools/publish.py
autonomous: true
requirements: [TOOL-01, TOOL-03]

must_haves:
  truths:
    - "timeline.py exports all 8 new functions from projekt-forge: disconnect_segments, inspect_sequence_versions, create_version, reconstruct_track, clone_version, replace_segment_media, scan_roles, assign_roles"
    - "publish.py rename_segments matches projekt-forge behavior"
    - "All ported functions use 'from forge_mcp import bridge' (not forge_bridge)"
    - "All new functions have Pydantic input models"
  artifacts:
    - path: "forge_bridge/tools/timeline.py"
      provides: "8 new Flame timeline functions ported from projekt-forge"
      contains: "async def disconnect_segments"
    - path: "forge_bridge/tools/publish.py"
      provides: "Verified rename_segments implementation"
      contains: "async def rename_segments"
  key_links:
    - from: "forge_bridge/tools/timeline.py"
      to: "forge_mcp.bridge"
      via: "import at top of file"
      pattern: "from forge_mcp import bridge"
---

<objective>
Port 8 new timeline functions from projekt-forge into forge_bridge/tools/timeline.py and verify publish.py rename_segments matches projekt-forge.

Purpose: Brings Flame tool coverage to projekt-forge parity for timeline and publish operations.
Output: Updated timeline.py with 8 new functions, verified publish.py.
</objective>

<execution_context>
@/Users/cnoellert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cnoellert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tool-parity-llm-router/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port 8 timeline functions from projekt-forge</name>
  <read_first>
    - forge_bridge/tools/timeline.py (full current file -- the target)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/timeline.py (full source -- the origin)
    - .planning/phases/01-tool-parity-llm-router/01-RESEARCH.md (Pitfall 1: wrong bridge import path, Pitfall 7: bulk_rename_segments mismatch)
  </read_first>
  <files>forge_bridge/tools/timeline.py</files>
  <action>
Port these 8 functions from projekt-forge's timeline.py into the standalone forge_bridge/tools/timeline.py:

1. **disconnect_segments** — Disconnect segments from source media
2. **inspect_sequence_versions** — List all versions of a sequence
3. **create_version** — Create a new version of a sequence
4. **reconstruct_track** — Reconstruct a track from segments
5. **clone_version** — Clone a version of a sequence
6. **replace_segment_media** — Replace segment media with new source
7. **scan_roles** — Scan sequence tracks to detect roles from file paths
8. **assign_roles** — Assign roles to tracks based on scan results

For each function:
- Copy the full function body from projekt-forge
- Copy the Pydantic input model (BaseModel subclass) for each function
- Change ALL import references from `from forge_bridge import bridge` to `from forge_mcp import bridge` (matching the existing standalone pattern)
- Preserve the async function signatures
- Preserve all docstrings and type annotations
- Add each function after the existing functions in timeline.py

CRITICAL IMPORT: The standalone repo uses `from forge_mcp import bridge`, NOT `from forge_bridge import bridge`. Every ported function must use the standalone import.

Also check: if projekt-forge has a function named `bulk_rename_segments`, verify whether it's the same as the existing standalone's function or different. The server.py registers `timeline.bulk_rename_segments` -- ensure this name matches an actual exported function.
  </action>
  <verify>
    <automated>python -c "from forge_bridge.tools import timeline; funcs = ['disconnect_segments', 'inspect_sequence_versions', 'create_version', 'reconstruct_track', 'clone_version', 'replace_segment_media', 'scan_roles', 'assign_roles']; missing = [f for f in funcs if not hasattr(timeline, f)]; assert not missing, f'Missing: {missing}'; print('All 8 functions present')"</automated>
  </verify>
  <acceptance_criteria>
    - forge_bridge/tools/timeline.py contains "async def disconnect_segments", "async def inspect_sequence_versions", "async def create_version", "async def reconstruct_track", "async def clone_version", "async def replace_segment_media", "async def scan_roles", "async def assign_roles"
    - forge_bridge/tools/timeline.py contains "from forge_mcp import bridge" (NOT "from forge_bridge import bridge")
    - Each new function has a corresponding Pydantic BaseModel input class
    - `python -c "from forge_bridge.tools.timeline import disconnect_segments, scan_roles, assign_roles"` succeeds
  </acceptance_criteria>
  <done>All 8 new timeline functions are present in timeline.py, use correct import path, and have Pydantic input models.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and reconcile publish.py rename_segments</name>
  <read_first>
    - forge_bridge/tools/publish.py (full standalone version)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/publish.py (full projekt-forge version)
    - .planning/phases/01-tool-parity-llm-router/01-RESEARCH.md (Pitfall 5: rename_segments already present)
  </read_first>
  <files>forge_bridge/tools/publish.py</files>
  <action>
Diff the standalone forge_bridge/tools/publish.py rename_segments function against the projekt-forge version.

Scenarios:
A. If they are identical (same logic, same Pydantic model) — no changes needed. Add a comment at the top of the function: `# Verified identical to projekt-forge 2026-04-14`
B. If the projekt-forge version has improvements/fixes — port those improvements. Change import from `from forge_bridge import bridge` to `from forge_mcp import bridge` if needed.
C. If they differ in ways that are both valid — prefer the standalone version (it's the one deployed) and document the difference in a comment.

In all cases, verify the RenameSegments Pydantic model is complete and the function signature is consistent with the other tools.

If no code changes are needed (scenario A), the measurable artifact is the verification comment added to the function. This satisfies TOOL-03 -- the requirement is parity confirmation, not necessarily code change.
  </action>
  <verify>
    <automated>python -c "from forge_bridge.tools.publish import rename_segments, RenameSegments; print('publish OK')"</automated>
  </verify>
  <acceptance_criteria>
    - forge_bridge/tools/publish.py contains "async def rename_segments" and "class RenameSegments(BaseModel)"
    - forge_bridge/tools/publish.py uses "from forge_mcp import bridge"
    - forge_bridge/tools/publish.py contains a verification comment (e.g., "Verified identical to projekt-forge" or "Ported improvements from projekt-forge" or "Differs from projekt-forge: ...")
    - `python -c "from forge_bridge.tools.publish import rename_segments"` succeeds
  </acceptance_criteria>
  <done>publish.py rename_segments is verified against projekt-forge. Parity status documented via comment in source. Any differences explained.</done>
</task>

</tasks>

<verification>
- `python -c "from forge_bridge.tools.timeline import disconnect_segments, inspect_sequence_versions, create_version, reconstruct_track, clone_version, replace_segment_media, scan_roles, assign_roles; print('all 8 OK')"` succeeds
- `python -c "from forge_bridge.tools.publish import rename_segments; print('publish OK')"` succeeds
- `grep -c 'from forge_bridge import bridge' forge_bridge/tools/timeline.py` returns 0
- `grep 'from forge_mcp import bridge' forge_bridge/tools/timeline.py` returns a match
</verification>

<success_criteria>
timeline.py exports all 8 new functions with Pydantic models, using correct import path. publish.py rename_segments is verified against projekt-forge and any divergence is documented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tool-parity-llm-router/01-04-SUMMARY.md`
</output>
