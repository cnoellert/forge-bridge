---
phase: 20.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packaging/forge-bridge.env.example
  - tests/test_packaging.py
autonomous: true
requirements: [INSTALL-01, INSTALL-04]
tags: [install, packaging, env-file, structural]

must_haves:
  truths:
    - "An env-file template exists in-tree that lists every operator-tunable env var the daemons read"
    - "The template ships with code-default values that match forge_bridge/llm/router.py, forge_bridge/store/session.py, and forge_bridge/__main__.py"
    - "The template is the SINGLE source of truth that systemd EnvironmentFile= and macOS wrapper scripts will source"
    - "The template ships parseable as KEY=VALUE (no `export`), so both `set -a; . FILE; set +a` and systemd's EnvironmentFile= read it identically"
  artifacts:
    - path: "packaging/forge-bridge.env.example"
      provides: "Env-file template installed to /etc/forge-bridge/forge-bridge.env at install time"
      contains: "FORGE_DB_URL=, FORGE_LOCAL_LLM_URL=, FORGE_LOCAL_MODEL=qwen2.5-coder:32b, FORGE_CLOUD_MODEL=claude-sonnet-4-6, FORGE_CONSOLE_PORT=9996, FORGE_BRIDGE_PORT=9998, ANTHROPIC_API_KEY="
  key_links:
    - from: "packaging/forge-bridge.env.example"
      to: "forge_bridge/llm/router.py:181-210"
      via: "env-var defaults must match code defaults"
      pattern: "FORGE_LOCAL_LLM_URL=http://localhost:11434/v1"
    - from: "packaging/forge-bridge.env.example"
      to: "forge_bridge/store/session.py:44-54"
      via: "FORGE_DB_URL default must match code default"
      pattern: "postgresql\\+asyncpg://forge:forge@localhost:5432/forge_bridge"
    - from: "packaging/forge-bridge.env.example"
      to: "forge_bridge/__main__.py:49-61"
      via: "FORGE_CONSOLE_PORT default must match code default"
      pattern: "FORGE_CONSOLE_PORT=9996"
---

<objective>
Create `packaging/forge-bridge.env.example` — the single source of truth for forge-bridge daemon configuration.

Purpose: Both systemd `EnvironmentFile=` (Linux) and the macOS launchd wrapper scripts will source this file at daemon start. Without this template, every other Phase 20.1 artifact (units, plists, wrappers, install-bootstrap.sh) has nothing to point at. This is the structural Wave 1 prerequisite for everything else.

Output: A KEY=VALUE template with no `export` keyword (so systemd parses it directly), values matching the in-code defaults at `forge_bridge/llm/router.py:181-210`, `forge_bridge/store/session.py:44-54`, and `forge_bridge/__main__.py:49-61`. Locked seed defaults preserved (qwen2.5-coder:32b NOT qwen3:32b; claude-sonnet-4-6 NOT opus-4-7).

Per D-23 decoupled-commit purity: this plan ships a structural artifact only. No value flips, no doc reshape — those land in P6.
Per D-10/D-11: env file at `/etc/forge-bridge/forge-bridge.env` (set at install time by P4); template ships in `packaging/` in-tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-CONTEXT.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-RESEARCH.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md

<interfaces>
<!-- Code defaults the env-file template MUST mirror exactly. Extracted from the codebase. -->
<!-- Executor: copy these values verbatim into the template. -->

From forge_bridge/llm/router.py:181-210 (env vars):
  FORGE_LOCAL_LLM_URL    default: http://localhost:11434/v1
  FORGE_LOCAL_MODEL      default: qwen2.5-coder:32b   ← LOCKED (SEED-DEFAULT-MODEL-BUMP-V1.4.x)
  FORGE_CLOUD_MODEL      default: claude-sonnet-4-6   ← LOCKED (SEED-OPUS-4-7-TEMPERATURE-V1.5)
  FORGE_SYSTEM_PROMPT    default: built-in (NOT exposed in env file — internal)

From forge_bridge/store/session.py:44-54:
  FORGE_DB_URL           default: postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge

From forge_bridge/__main__.py:49-61:
  FORGE_CONSOLE_PORT     default: 9996

From flame_hooks/forge_bridge/scripts/forge_bridge.py (top-of-file env overrides):
  FORGE_BRIDGE_HOST      default: 127.0.0.1
  (FORGE_BRIDGE_PORT for the Flame hook is 9999 — NOT in this env file; Flame hook is OS-launched, not daemon-launched)

NEW for 20.1 (consumed by systemd unit + wrapper script):
  FORGE_BRIDGE_PORT      default: 9998   ← WS bus port (forge_bridge.server)
                                           Used by macOS wrapper script as the readiness-gate target

ANTHROPIC_API_KEY: empty by default — chat hardcodes sensitive=True (local Ollama path); cloud routing is opt-in only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create packaging/ directory and env-file template</name>
  <files>packaging/forge-bridge.env.example, tests/test_packaging.py</files>
  <read_first>
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-RESEARCH.md (Pattern 4, lines 590-660 — verbatim env-file template body)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md (§ "packaging/forge-bridge.env.example", lines 328-380)
    - tests/test_install_hook_version_consistency.py (the analog for tests/test_packaging.py — pure-Python file-content regex, no fixtures, no asyncio)
    - forge_bridge/llm/router.py (lines 181-210 to confirm env-var contracts; do NOT modify)
    - forge_bridge/store/session.py (lines 44-54 to confirm FORGE_DB_URL default; do NOT modify)
    - forge_bridge/__main__.py (lines 49-61 to confirm FORGE_CONSOLE_PORT default; do NOT modify)
    - .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md (LOCK FORGE_LOCAL_MODEL=qwen2.5-coder:32b)
    - .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md (LOCK FORGE_CLOUD_MODEL=claude-sonnet-4-6)
  </read_first>
  <behavior>
    Test 1: packaging/forge-bridge.env.example exists and contains every required key (FORGE_DB_URL, FORGE_LOCAL_LLM_URL, FORGE_LOCAL_MODEL, FORGE_CLOUD_MODEL, FORGE_CONSOLE_PORT, FORGE_BRIDGE_PORT, ANTHROPIC_API_KEY)
    Test 2: FORGE_LOCAL_MODEL value is exactly "qwen2.5-coder:32b" (NOT qwen3:32b — SEED guard)
    Test 3: FORGE_CLOUD_MODEL value is exactly "claude-sonnet-4-6" (NOT opus-4-7 — SEED guard)
    Test 4: FORGE_DB_URL value is exactly "postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge"
    Test 5: Template parses cleanly under `set -a; . FILE; set +a` (no `export` keyword, no shell expansion required)
  </behavior>
  <action>
Create the directory and file:

  mkdir -p packaging

Then write `packaging/forge-bridge.env.example` with this exact content (copy verbatim from RESEARCH.md Pattern 4 lines 597-631 / PATTERNS.md lines 334-365):

  # packaging/forge-bridge.env.example
  # This file is the SINGLE source of truth for forge-bridge daemon configuration.
  # Both systemd (via EnvironmentFile=) and launchd wrapper scripts source it.
  # Edit and run:
  #   sudo systemctl restart forge-bridge   (Linux)
  #   sudo launchctl kickstart -k system/com.cnoellert.forge-bridge   (macOS)
  #
  # See docs/INSTALL.md for end-to-end install path.

  # ─── Postgres ─────────────────────────────────────────────────────────────
  # Default matches alembic.ini hardcoded URL + forge_bridge/store/session.py default.
  FORGE_DB_URL=postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge

  # ─── Local LLM (Ollama) ───────────────────────────────────────────────────
  # Most operators run Ollama on a SEPARATE LLM service host on the same network.
  # Set this to your LLM host's URL, e.g.:
  #   FORGE_LOCAL_LLM_URL=http://192.168.86.15:11434/v1
  # Default below assumes Ollama is local — only use that on hosts with dedicated GPU/RAM.
  FORGE_LOCAL_LLM_URL=http://localhost:11434/v1

  # Locked default per SEED-DEFAULT-MODEL-BUMP-V1.4.x — DO NOT change to qwen3:32b
  # (cold-start LLMLoopBudgetExceeded driven by thinking-mode token verbosity).
  FORGE_LOCAL_MODEL=qwen2.5-coder:32b

  # ─── Cloud LLM (Anthropic) — OPTIONAL ─────────────────────────────────────
  # Chat hardcodes sensitive=True (routes through local Ollama). Cloud is only used
  # for sensitive=False routing — not the daily operator workflow.
  ANTHROPIC_API_KEY=
  # Locked default per SEED-OPUS-4-7-TEMPERATURE-V1.5 — DO NOT bump to opus-4-7
  # (AnthropicAdapter unconditionally sends temperature; opus-4-7 rejects it).
  FORGE_CLOUD_MODEL=claude-sonnet-4-6

  # ─── Ports ────────────────────────────────────────────────────────────────
  # MCP server + Artist Console (forge_bridge.__main__).
  FORGE_CONSOLE_PORT=9996
  # WebSocket bus (forge_bridge.server).
  FORGE_BRIDGE_PORT=9998

  # ─── Flame hook bind host (Track A only) ──────────────────────────────────
  # Flame hook listens on FORGE_BRIDGE_HOST:9999 — daemons do NOT consume this var
  # but the env file documents it for operator visibility.
  FORGE_BRIDGE_HOST=127.0.0.1

Then create `tests/test_packaging.py` (mirror tests/test_install_hook_version_consistency.py style — pure-Python, no fixtures). Module docstring: "Regression guard: packaging/ artifacts. Covers INSTALL-01 / INSTALL-04 from Phase 20.1. Fails immediately when env-template default values drift from the codebase defaults at forge_bridge/llm/router.py / store/session.py / __main__.py. Mirrors tests/test_install_hook_version_consistency.py style: pure file reads + regex, no fixtures, no asyncio."

Test functions to write (pure-Python; subsequent plans will append more tests for systemd/launchd):

  from __future__ import annotations
  from pathlib import Path

  _REPO_ROOT = Path(__file__).parent.parent
  _PACKAGING = _REPO_ROOT / "packaging"

  def _read(rel: str) -> str:
      return (_REPO_ROOT / rel).read_text()

  def test_env_template_exists():
      assert (_PACKAGING / "forge-bridge.env.example").is_file(), \
          "packaging/forge-bridge.env.example missing — Phase 20.1 P1 deliverable"

  def test_env_template_has_all_required_keys():
      """Every key the daemons read MUST appear in the template."""
      required = {
          "FORGE_DB_URL", "FORGE_LOCAL_LLM_URL", "FORGE_LOCAL_MODEL",
          "FORGE_CLOUD_MODEL", "FORGE_CONSOLE_PORT", "FORGE_BRIDGE_PORT",
          "ANTHROPIC_API_KEY",
      }
      content = _read("packaging/forge-bridge.env.example")
      for key in required:
          assert f"\n{key}=" in content or content.startswith(f"{key}="), \
              f"env template missing {key}"

  def test_env_template_locked_local_model():
      """SEED-DEFAULT-MODEL-BUMP-V1.4.x: FORGE_LOCAL_MODEL stays at qwen2.5-coder:32b."""
      content = _read("packaging/forge-bridge.env.example")
      assert "FORGE_LOCAL_MODEL=qwen2.5-coder:32b" in content
      assert "FORGE_LOCAL_MODEL=qwen3:32b" not in content

  def test_env_template_locked_cloud_model():
      """SEED-OPUS-4-7-TEMPERATURE-V1.5: FORGE_CLOUD_MODEL stays at claude-sonnet-4-6."""
      content = _read("packaging/forge-bridge.env.example")
      assert "FORGE_CLOUD_MODEL=claude-sonnet-4-6" in content
      assert "opus-4-7" not in content

  def test_env_template_db_url_matches_alembic():
      """FORGE_DB_URL default must match the role+db pair install-bootstrap.sh creates."""
      content = _read("packaging/forge-bridge.env.example")
      assert "FORGE_DB_URL=postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge" in content

  def test_env_template_no_export_keyword():
      """systemd EnvironmentFile= and `set -a; . FILE; set +a` both reject `export`."""
      content = _read("packaging/forge-bridge.env.example")
      for line in content.splitlines():
          stripped = line.strip()
          if stripped.startswith("#") or not stripped:
              continue
          assert not stripped.startswith("export "), \
              f"env template line uses `export` (breaks systemd EnvironmentFile=): {line!r}"

  def test_env_template_parses_with_set_a():
      """Sanity: every non-comment, non-blank line is `KEY=VALUE` shape."""
      import re
      content = _read("packaging/forge-bridge.env.example")
      for line in content.splitlines():
          stripped = line.strip()
          if not stripped or stripped.startswith("#"):
              continue
          assert re.match(r"^[A-Z_][A-Z0-9_]*=.*$", stripped), \
              f"env template has non-KEY=VALUE line: {line!r}"

NOTE: This task creates the test file but does NOT YET add the systemd/launchd/install-bootstrap.sh assertions — those land in their own plans (P2/P3/P4). The pattern is "tests grow with the plans that ship the artifacts they cover."

Run the tests:

  pytest tests/test_packaging.py -x -v

All 7 tests must pass.
  </action>
  <verify>
    <automated>pytest tests/test_packaging.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `packaging/forge-bridge.env.example` exists (test: `test -f packaging/forge-bridge.env.example`)
    - File `tests/test_packaging.py` exists (test: `test -f tests/test_packaging.py`)
    - All 7 tests pass: `pytest tests/test_packaging.py -x -v` exits 0
    - File contains exact string `FORGE_LOCAL_MODEL=qwen2.5-coder:32b` (test: `grep -F 'FORGE_LOCAL_MODEL=qwen2.5-coder:32b' packaging/forge-bridge.env.example`)
    - File contains exact string `FORGE_CLOUD_MODEL=claude-sonnet-4-6` (test: `grep -F 'FORGE_CLOUD_MODEL=claude-sonnet-4-6' packaging/forge-bridge.env.example`)
    - File contains exact string `FORGE_DB_URL=postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge` (test: `grep -F 'FORGE_DB_URL=postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge' packaging/forge-bridge.env.example`)
    - File contains exact string `FORGE_CONSOLE_PORT=9996` and `FORGE_BRIDGE_PORT=9998` (test: `grep -E '^FORGE_CONSOLE_PORT=9996$' && grep -E '^FORGE_BRIDGE_PORT=9998$'`)
    - File contains exact string `ANTHROPIC_API_KEY=` (empty value; test: `grep -E '^ANTHROPIC_API_KEY=$' packaging/forge-bridge.env.example`)
    - File does NOT contain `qwen3:32b` (test: `! grep -F 'qwen3:32b' packaging/forge-bridge.env.example`)
    - File does NOT contain `opus-4-7` (test: `! grep -F 'opus-4-7' packaging/forge-bridge.env.example`)
    - File does NOT contain any line starting with `export ` (test: `! grep -E '^export ' packaging/forge-bridge.env.example`)
    - Template parses with `set -a; . packaging/forge-bridge.env.example; set +a` exit 0 (test: `bash -c 'set -a; . packaging/forge-bridge.env.example; set +a; echo "$FORGE_LOCAL_MODEL"' == 'qwen2.5-coder:32b'`)
  </acceptance_criteria>
  <done>
    Env-file template exists in tree at packaging/forge-bridge.env.example with exact code-default values for every operator-tunable env var. Locked seed values (qwen2.5-coder:32b, claude-sonnet-4-6) confirmed by regression tests. tests/test_packaging.py exists and all 7 tests pass. No `export` keyword anywhere — systemd-EnvironmentFile-compatible.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → in-tree config template | The template is read-only in tree (mode 0644 standard). No secrets ship here — `ANTHROPIC_API_KEY=` is empty. The PRODUCTION env file at `/etc/forge-bridge/forge-bridge.env` (P4) is the secret-bearing surface; THIS template is its source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-01 | I (Information Disclosure) | packaging/forge-bridge.env.example | mitigate | Template ships ANTHROPIC_API_KEY= empty; no secrets in tree. Regression test (test_env_template_no_export_keyword) ensures no `export FOO=secret` paste-leak pattern lands by accident. |
| T-20.1-02 | T (Tampering) | env-file values vs code defaults | mitigate | Three lock tests (test_env_template_locked_local_model, test_env_template_locked_cloud_model, test_env_template_db_url_matches_alembic) catch any drift from SEED-locked values or alembic.ini-hardcoded URL. |
| T-20.1-03 | R (Repudiation) / D (Denial of Service) | template parse failure at install time | mitigate | test_env_template_parses_with_set_a + test_env_template_no_export_keyword guarantee both `set -a; . FILE; set +a` AND systemd `EnvironmentFile=` will parse the template. Catches file-format drift before it lands in /etc/forge-bridge/. |
| T-20.1-04 | I (Information Disclosure) | future operator copy-pastes secrets into the in-tree example | accept | Operator owns the example-vs-real distinction. The filename `.env.example` follows the dotenv convention; .gitignore-style protection of `/etc/forge-bridge/forge-bridge.env` (the live, secret-bearing copy) is P4's responsibility (mode 0640 root:$SUDO_USER). |
</threat_model>

<verification>
Run quick suite:
  pytest tests/test_packaging.py -x -v

Run full suite (must remain green; 20.1 adds tests, doesn't refactor existing):
  pytest tests/ -x

Manual sanity:
  bash -c 'set -a; . packaging/forge-bridge.env.example; set +a; env | grep -E "^FORGE_|^ANTHROPIC_API_KEY"'
  # → prints all 7 vars with locked values; FORGE_LOCAL_MODEL=qwen2.5-coder:32b
</verification>

<success_criteria>
1. packaging/forge-bridge.env.example exists with all 7 required keys.
2. Locked seed defaults present: FORGE_LOCAL_MODEL=qwen2.5-coder:32b, FORGE_CLOUD_MODEL=claude-sonnet-4-6.
3. FORGE_DB_URL matches alembic.ini hardcoded URL exactly.
4. tests/test_packaging.py exists with 7 passing tests.
5. Full pytest suite remains green (no regressions).
6. Template parses with `set -a; . FILE; set +a` (systemd EnvironmentFile parity).
7. Single git commit per D-23 (structural, no value flips, no doc reshape).
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-01-SUMMARY.md` documenting:
- Files created (packaging/forge-bridge.env.example, tests/test_packaging.py).
- Lock test coverage (qwen2.5-coder:32b + claude-sonnet-4-6 anti-drift).
- Confirmation that all 7 keys are present and parseable.
- Foundation laid for P2/P3/P4/P5 to extend tests/test_packaging.py with their own assertions.
</output>
