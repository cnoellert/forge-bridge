# Phase 20.1: Install Bootstrap Script + Systemd Daemon (v1.5 ship blocker) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-01
**Phase:** 20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker
**Areas discussed:** Service ownership model (auto-rec'd), Postgres install scope (auto-rec'd), Distro support stance (discussed), Partial-install flag matrix (discussed), Env-file edit UX (auto-rec'd), `forge doctor` integration (auto-rec'd), Non-author UAT logistics (discussed), Service `User=` identity (auto-rec'd)

---

## Service ownership model (auto-recommended; user accepted)

| Option | Description | Selected |
|--------|-------------|----------|
| System service | `/etc/systemd/system/` with `User=cnoellert`; sudo for daily ops; survives logout | ✓ |
| User service | `~/.config/systemd/user/` with `loginctl enable-linger`; no sudo; conda-native | |
| Hybrid | system unit but unprivileged operator user via `User=` | |

**User's choice:** System service.
**Notes:** Matches candidate's `/etc/systemd/system/` pick. Survives logout. Env-file editing follows artist mental model. `journalctl -u forge-bridge` works without `--user`.

---

## Postgres install scope (auto-recommended; user accepted)

| Option | Description | Selected |
|--------|-------------|----------|
| Bootstrap-only | Assume `postgresql-server` package present; handle initdb/pg_hba/role/db/alembic only | |
| All-in-one Rocky/RHEL | Also runs `dnf install` if package missing | ✓ |
| All-in-one + apt fallback | Rocky/RHEL via dnf + Debian/Ubuntu via apt | |

**User's choice:** All-in-one (Rocky/RHEL only).
**Notes:** Walk gap #3 was "package present but cluster uninitialized" — but the artist also has to install the package, so script ownership is consistent. apt fallback expands blast radius without target-user evidence.

---

## Distro support stance for 20.1 (DISCUSSED)

| Option | Description | Selected |
|--------|-------------|----------|
| Rocky/RHEL only | macOS launchd → Phase 20.2 if surfaced | |
| Rocky/RHEL + macOS docs-only appendix | No launchd; manual `python -m forge_bridge.server` + `python -m forge_bridge` via tmux | |
| Rocky/RHEL + macOS launchd plist in 20.1 | Real macOS daemon parity | ✓ |

**User's choice:** Option 3 (Rocky/RHEL + macOS launchd plist in 20.1) with the clarification "we can use portofino" (personal Mac available for testing).
**Notes:** Made macOS support concrete — user has a real test host (portofino). Cascading sub-questions surfaced in follow-up below.

### Follow-up Q3.1 — what is portofino?

| Option | Description | Selected |
|--------|-------------|----------|
| Your personal Mac (dev box) | macOS UAT is therefore an author-walk | ✓ |
| A colleague's Mac (Flame artist or designer) | macOS UAT could be non-author | |
| A throwaway VM/test box | fresh-state UAT possible | |

**User's choice:** Personal Mac.
**Implication:** macOS UAT is structurally an author-walk by definition. Combines with Area 7 logistics.

### Follow-up Q3.2 — macOS Postgres backend the script handles

| Option | Description | Selected |
|--------|-------------|----------|
| Homebrew `postgresql@16` only | Script runs `brew install postgresql@16 && brew services start postgresql@16` if missing | |
| Postgres.app only | Detect `/Applications/Postgres.app` and use bundled CLI tools | |
| Detect both; install Homebrew variant if neither present | (recommended) | ✓ |
| Detect existing only; do NOT auto-install Postgres on macOS | Operator must install before running script | |

**User's choice:** Detect both; install Homebrew variant if neither present.
**User's commentary:** "All flame workstation on Mac will have postgres already installed." Real-world claim — the install branch is a rare edge case but must work for parity.

### Follow-up Q3.3 — launchd plist scope and location

| Option | Description | Selected |
|--------|-------------|----------|
| `/Library/LaunchDaemons/...plist` (system daemons, parity with Linux system service) | (recommended) | ✓ |
| `~/Library/LaunchAgents/...plist` (per-user agents) | | |
| Split system daemon for bus + LaunchAgent for console | | |

**User's choice:** `/Library/LaunchDaemons/`. Parity with Linux system-service pick (Area 1).

### Follow-up Q3.4 — macOS install-bootstrap pre-reqs

| Option | Description | Selected |
|--------|-------------|----------|
| Just Python 3.11 + git | Script handles brew install + everything else | |
| Python + git + Homebrew already installed | Script handles brew commands but assumes brew is present | ✓ (with explicit detect-and-inform) |
| Python + git + Homebrew + Postgres already installed | Script handles only initdb/role/db/alembic + launchd | |

**User's choice:** Option 2, with the refinement: "the last one should check and if not installed inform."
**Implementation:** Script detects Homebrew presence; if missing, emits friendly error pointing at https://brew.sh and exits non-zero. No auto-install of Homebrew (operator-trust concern).

---

## Partial-install flag matrix (DISCUSSED)

User flagged comprehension confusion: "Discuss, I don't know that I understand."

### Q4.1 — remote Postgres workflow?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — real workflow I've hit | | |
| No — always local | | |
| Not yet, but expect to within v1.5–v1.6; plant `--no-postgres` flag now | | ✓ |
| Other | | |

**User's choice:** Plant the flag now. Implies `--no-postgres` must be implemented in 20.1, not a future deferral.

### Q4.2 — chat-only / MCP-only deploy?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — real deploy shape I've used | | ✓ |
| No — staged ops + SQL mirror are part of forge-bridge's value to me | | |
| Not yet, but plant | | |
| Other | | |

**User's choice:** Yes, real deploy shape. `--mcp-only` is mandatory in 20.1, not a future deferral.

### Q4.3 — auto-detect macOS for `--track-b`?

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-detect silently | Script just does the right thing | |
| Stay explicit | Operator must pass `--track-b` even on macOS | |
| Auto-detect + emit clear log line + override flag (`--with-flame-mac`) | (recommended) | ✓ |
| Other | | |

**User's choice:** Option 3. Auto-detect is correct; explicit log line preserves discoverability for the rare Mac-Flame artist; override flag handles the edge case.

### Q4.4 — final flag surface (synthesized)

**User implied:** Option 3 — `--track-b` + `--no-postgres` + `--mcp-only` + `--with-flame-mac` + `--non-interactive`. macOS auto-applies `--track-b` unless `--with-flame-mac` is passed.
**Notes:** All flags MUST work in 20.1; no "planted but unimplemented" lazy paths. Test matrix expands to 4 sane combos: Linux-full, Linux-`--mcp-only`, macOS-default, Linux-`--no-postgres`.

---

## Env-file edit UX (auto-recommended; user accepted)

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-launch `${EDITOR:-vi}` on `/etc/forge-bridge/forge-bridge.env` after copy | | |
| Interactive prompt: read `FORGE_LOCAL_LLM_URL` only, sed-write into env file | (recommended) | ✓ |
| Stop and instruct: print "now edit `/etc/forge-bridge/forge-bridge.env`" | | |

**User's choice:** Interactive prompt for `FORGE_LOCAL_LLM_URL` only.
**Notes:** Artist has one decision ("where does my LLM run?"); putting them in vi is a comprehension cliff. Defaults handle everything else. `--non-interactive` for unattended re-runs.

---

## `forge doctor` integration / final verification (auto-recommended; user accepted)

| Option | Description | Selected |
|--------|-------------|----------|
| Script exits after `systemctl start`; operator runs doctor separately | (current INSTALL.md Step 8) | |
| Script auto-runs `forge doctor` at end and emits PASS/FAIL/PARTIAL inline | | |
| Same as above + `forge doctor` gets new sub-checks for both daemon states | (recommended) | ✓ |

**User's choice:** Auto-run + new doctor sub-checks for daemon states.
**Notes:** Daemon state is the install's success signal. Phase 23 DIAG-05 parity work pulls forward into 20.1 because the daemon model needs the verification path to land together.

---

## Non-author UAT logistics (DISCUSSED)

User picked "Author-walk acceptable" at gray-area selection; sub-questions clarified shape.

### Q7.1 — when does the author-walk fallback trigger?

| Option | Description | Selected |
|--------|-------------|----------|
| Immediately — no artist recruiting attempt | | ✓ |
| 1 week — try to schedule, fall back if blocked | (recommended) | |
| 2 weeks — same as above, longer recruiting window | | |
| v1.5 ship deadline — author-walk only when ship pressure forces close | | |

**User's choice:** Immediately. Author walks both Linux + macOS as part of phase execution; no recruiting window.

### Q7.2 — discipline contract for the author-walk

| Option | Description | Selected |
|--------|-------------|----------|
| Same as Phase 20 D-02.1 (verbatim copy-paste, log every author-context dependence) | | |
| Stricter: fresh VM/throwaway box where author has no prior conda, postgres, or shell history | (recommended) | ✓ |
| Strictest: `curl | bash` form (no clone) on a fresh VM | | |

**User's choice:** Fresh VM. Phase 20's flame-01 had postgresql package pre-installed; gap #3 only surfaced from that incidental state. Fresh VM forces every branch of `install-bootstrap.sh` to fire honestly.

### Q7.3 — outcome semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Author-walk PASS = phase closes clean; artist UAT becomes optional | | |
| Author-walk PASS = phase closes PASS-with-deviations; plant `SEED-PHASE-20.1-ARTIST-UAT-V1.6+` for future | (recommended) | ✓ |
| Author-walk PASS = phase closes clean ONLY IF parallel artist screenshare also runs | | |

**User's choice:** PASS-with-deviations + planted seed. Mirrors Phase 20 D-02.1 honesty. Closing 20.1 the same way Phase 20 closed (clean PASS off author-walk) would re-burn the gate.

### Q7.4 — two walks: both gates, or one primary?

| Option | Description | Selected |
|--------|-------------|----------|
| Both gates — phase closes only when BOTH paths clean-walk | | ✓ |
| Linux-flame-01 primary; portofino macOS parity check (informational) | | |
| Linux-flame-01 primary; macOS sanity sniff (script runs, surfaces 1–4 reachable) | (recommended) | |
| Whichever walks first closes the gate; the other is a follow-up | | |

**User's choice:** Both gates. Phase doesn't close until both flame-01 (Linux fresh-VM) AND portofino (macOS) report clean.

---

## Service `User=` identity (auto-recommended; user accepted)

| Option | Description | Selected |
|--------|-------------|----------|
| `User=$SUDO_USER` (operator's login user) | (recommended) | ✓ |
| Dedicated `forge-bridge` system user; `~/.forge-bridge/` becomes `/var/lib/forge-bridge/` | | |
| Variable `FORGE_BRIDGE_USER` in env file; default `$SUDO_USER` | | |

**User's choice:** `$SUDO_USER`. Operator's `~/.forge-bridge/executions.jsonl` and synthesized tools stay operator-owned; dedicated system user breaks the Phase 8 STORE-06 JSONL-authoritative invariant. Multi-operator is v1.6+ (auth) anyway.

---

## Claude's Discretion

Areas where the user explicitly deferred to planner judgment:
- Exact systemd ordering directives beyond `Requires=`/`After=` (`Wants=`, `BindsTo=`)
- launchd plist `KeepAlive` / `RunAtLoad` / `WorkingDirectory` directives
- `pg_hba.conf` flip method (sed-in-place vs full file rewrite)
- Whether env-file's interactive prompt uses bash `read -p` or `select`/`whiptail`/`dialog`
- Plan numbering inside 20.1 (likely 5–7 plans)
- Whether `daemon_state` `forge doctor` sub-check is one row with branching or two rows

---

## Deferred Ideas

(Captured in CONTEXT.md `<deferred>` section; surfaced during discussion):
- `SEED-PHASE-20.1-ARTIST-UAT-V1.6+` — real-artist UAT walk, planted at phase close per D-19
- Auto-installing Homebrew on macOS (operator-trust concern)
- Apt/Debian/Ubuntu Linux distro support
- `curl | bash` public-distribution form for `install-bootstrap.sh`
- Multi-machine / multi-user / authenticated deploys (`SEED-AUTH-V1.5`)
- `forge doctor --print-example-config` flag (env-file template covers it instead)
- Logrotate / newsyslog log rotation
- systemd env-vs-file consistency check in `forge doctor`
- macOS launchd `Wants=`/`BindsTo=` equivalents
- Phase 20.2 trigger if single 20.1 gap balloons beyond 1 plan
