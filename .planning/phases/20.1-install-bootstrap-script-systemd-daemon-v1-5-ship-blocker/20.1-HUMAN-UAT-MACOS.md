---
status: partial
phase: 20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker
plan: 07
platform: macOS (Sequoia 15.x)
host: portofino
source:
  - docs/INSTALL.md
  - scripts/install-bootstrap.sh
  - packaging/launchd/com.cnoellert.forge-bridge-server.plist
  - packaging/launchd/com.cnoellert.forge-bridge.plist
  - /etc/forge-bridge/forge-bridge.env
walker_discipline:
  - verbatim-copy-paste
  - verbatim-transcription
  - no-silent-route-around
  - log-every-author-context-dependence
fresh_state_contract: true
started: ~PENDING~
updated: ~PENDING~
outcome: ~PENDING~
---

# Phase 20.1 — macOS Fresh-State Walk on portofino (Gate per D-18)

> Walker: cnoellert — project author. Fresh-state discipline per D-17 (no prior conda env in
> `forge`, no prior /etc/forge-bridge, no prior /Library/LaunchDaemons/com.cnoellert.forge-bridge*).
> Outcome recorded as `PASS-with-deviations` per D-19.
>
> macOS path details: auto-applies `--track-b` (D-08) — no Flame hook on portofino. Validates 4
> of 5 surfaces (Flame hook surface skipped by design). Confirms Postgres pre-installed (user-confirmed
> expectation for all Mac Flame workstations per CONTEXT.md `<specifics>`).
>
> **Status: PENDING — walk not yet conducted.**
>
> This scaffold was pre-created by the executor agent to define the document shape. The operator
> must complete the walk on portofino per the discipline contract below and fill in each section.
> Do NOT mark outcome as PASS until the walk is complete.

**Date:** ~PENDING~
**Walker:** cnoellert — project orchestrator; non-author of the bootstrap script macOS branch
(first end-to-end physical execution on macOS). See Deviations, item 0.
**Host:** portofino (macOS Sequoia 15.x, Apple Silicon arm64)
**LLM service host (separate):** assist-01 @ `192.168.86.15` — Ollama + `qwen2.5-coder:32b`
**Branch / HEAD commit:** ~PENDING~
**INSTALL.md commit:** ~PENDING~

**Discipline contract (D-17 fresh-state + D-02.1 honesty model):**
1. Copy-paste every command verbatim from `docs/INSTALL.md`. Do NOT mentally substitute corrections.
2. Transcribe observed output verbatim (elide with `[...]` only for length, mark all elisions).
3. NO silent route-around. If a command fails or doc is ambiguous, STOP, log the gap, then continue.
4. Log every author-context dependence (SSH, IDE, prior-state knowledge) as a deviation.
5. Fresh-state contract: no prior `forge` conda env, no prior /etc/forge-bridge, no prior
   /Library/LaunchDaemons/com.cnoellert.forge-bridge*.plist, no prior /var/log/forge-bridge.
   Teardown steps performed to reach fresh state must be documented below.

---

## Pre-walk fixture state

> Record the box's exact state BEFORE any walk step. If prior plists / env-file existed,
> document the bootout + removal steps performed to reach fresh-state condition.

```
# Commands to run verbatim and paste output:
$ sw_vers -productVersion
~PENDING~

$ uname -m
~PENDING~

$ command -v brew && brew --version | head -1
~PENDING~

$ command -v psql && psql --version
~PENDING~

$ [ -d /etc/forge-bridge ] && ls -la /etc/forge-bridge || echo "no /etc/forge-bridge"
~PENDING~

$ [ -f /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist ] && \
    echo "prior install present" || echo "no prior plists"
~PENDING~

$ [ -d /var/log/forge-bridge ] && ls -la /var/log/forge-bridge || echo "no log dir"
~PENDING~
```

**Teardown steps performed (if prior state existed):**
~NONE OR DOCUMENT STEPS~
(e.g. `sudo launchctl bootout system /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist` +
 `sudo launchctl bootout system /Library/LaunchDaemons/com.cnoellert.forge-bridge-server.plist` +
 `sudo rm /Library/LaunchDaemons/com.cnoellert.forge-bridge*.plist` +
 `sudo rm -rf /etc/forge-bridge /var/log/forge-bridge`)

---

## Reference versions (D-22)

> Captured at walk-time per D-22. Record AFTER Step 3 (bootstrap) completes.

| Dependency | Host | Version found |
|------------|------|---------------|
| Python (after `conda activate forge`) | portofino | ~PENDING~ |
| conda | portofino | ~PENDING~ |
| PostgreSQL (pre-installed) | portofino | ~PENDING~ |
| launchctl (Darwin Bootstrapper) | portofino | ~PENDING~ |
| bash (system vs Homebrew) | portofino | ~PENDING~ |
| forge-bridge `__version__` (after Step 2) | portofino | ~PENDING~ |
| Ollama | assist-01 | ~PENDING~ |
| qwen2.5-coder:32b pulled | assist-01 | ~PENDING~ |

---

## Walk transcript

> Verbatim command + verbatim output for each INSTALL.md step. Elide with `[...]` only for length;
> always mark elisions.

### Step 1: Prepare the conda environment

```
$ conda create -n forge python=3.11 -y
~PENDING~

$ conda activate forge
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 2: Install forge-bridge

```
$ pip install -e ".[dev,llm]"
~PENDING~

$ python -c "import forge_bridge; print(forge_bridge.__version__)"
~PENDING~

$ forge-bridge --help
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 3: Run the bootstrap script (macOS branch)

> macOS branch: auto-applies `--track-b`, detects pre-installed Postgres (expected on portofino),
> creates /var/log/forge-bridge before launchctl bootstrap (Pitfall 2 fix).

```
$ sudo ./scripts/install-bootstrap.sh
~PENDING~
```

**Confirm:**
- [ ] Script printed `macOS detected — auto-applying --track-b` (D-08 log line)
- [ ] Script detected pre-installed Postgres (skip brew install branch fired)
- [ ] Script created /var/log/forge-bridge/ BEFORE `launchctl bootstrap`
- [ ] `launchctl bootstrap system /Library/LaunchDaemons/com.cnoellert.forge-bridge*.plist` succeeded
- [ ] Script printed `[forge-bridge] Running forge-bridge console doctor...`
- [ ] `forge doctor` reported PASS or PARTIAL with `daemon_state` row visible
- [ ] Script exited 0

**macOS plist lint check (Pitfall 5 regression — run after Step 3):**

```
$ plutil -lint /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist
~PENDING~

$ plutil -lint /Library/LaunchDaemons/com.cnoellert.forge-bridge-server.plist
~PENDING~
```

Both must return "OK". If either fails, STOP and log the gap.

**If FAIL — STOP, log the gap below, do not continue.**

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 4: (Track B — macOS auto-skips Flame hook per D-08)

> Step 4 is auto-skipped on portofino because the bootstrap script applied `--track-b`.
> The Flame hook surface is skipped on this walk (documented skip, not a gap).
> Phase 20.1 validates 4/5 surfaces on macOS per D-09.

**Confirmed skip:** auto-`--track-b` applied — Flame hook surface excluded from this walk.

---

### Step 5: Configure environment

```
$ sudo nano /etc/forge-bridge/forge-bridge.env
# (set FORGE_LOCAL_LLM_URL=http://192.168.86.15:11434/v1)
~PENDING~

$ sudo launchctl kickstart -k system/com.cnoellert.forge-bridge
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 6: Verify the daemons are running

```
$ sudo launchctl print system/com.cnoellert.forge-bridge-server | grep state
~PENDING~

$ sudo launchctl print system/com.cnoellert.forge-bridge | grep state
~PENDING~

$ tail -20 /var/log/forge-bridge/console.log
~PENDING~

$ tail -20 /var/log/forge-bridge/server.log
~PENDING~
```

**Confirm:**
- [ ] `forge-bridge-server` → `state = running`
- [ ] `forge-bridge` → `state = running`
- [ ] console.log readable; no fatal errors
- [ ] server.log readable; no fatal errors

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 7: Verify reachable surfaces (4/5 — Flame hook auto-skipped)

> Surface 5 (Flame hook :9999) is intentionally skipped on portofino per D-08 + D-09.

```
# Surface 1: Web UI
$ curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"
~PENDING~

# Surface 2: CLI
$ forge-bridge console doctor
~PENDING~

# Surface 3: Chat endpoint
$ curl -s -X POST http://localhost:9996/api/v1/chat \
    -H 'Content-Type: application/json' \
    -d '{"prompt":"what tools do you have"}'
~PENDING~

# Surface 4: MCP server stdio (manual ctrl-C OK)
$ python -m forge_bridge
# (confirm starts, then ctrl-C)
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 8: Post-install diagnostic

```
$ forge-bridge console doctor
~PENDING~
```

---

### Idempotency check (RESEARCH.md OQ #3 + D-21)

```
$ sudo ./scripts/install-bootstrap.sh --non-interactive
~PENDING~
```

**Confirm:**
- [ ] Script ran without error
- [ ] Env-file edits preserved (FORGE_LOCAL_LLM_URL unchanged)
- [ ] `launchctl bootout` + `bootstrap` cycle left daemons running intact
- [ ] `forge doctor` still PASS or PARTIAL after re-run

**Gap surfaced:** None / ~LIST IF ANY~

---

## Surfaces table

| # | Surface | Smoke command | Result | Notes |
|---|---------|--------------|--------|-------|
| 1 | Web UI on :9996/ui/ | `curl ... -w "%{http_code}\n"` | ~PENDING~ | |
| 2 | CLI `forge-bridge` | `forge-bridge console doctor` | ~PENDING~ | |
| 3 | HTTP /api/v1/chat | `curl -s -X POST ...` | ~PENDING~ | |
| 4 | MCP server stdio | `python -m forge_bridge` (ctrl-C) | ~PENDING~ | |
| 5 | Flame hook on :9999 | SKIPPED (auto-`--track-b` per D-08) | N/A | Expected skip on portofino |

---

## Gap log

> Every gap surfaced during the walk. Disposition per D-20 (aggressive in-flight fix policy).
> If NO gaps: "No gaps surfaced — all steps walked cleanly."

| # | Step | Gap description | Disposition | Plan / seed |
|---|------|----------------|-------------|-------------|
| ~N~ | ~STEP~ | ~DESCRIPTION~ | ~in-flight-fix / doc-only / v1.6+ seed~ | ~20.1-08+ or seed reference~ |

---

## Deviations

> Any step where the walker drew on knowledge not in the doc, used SSH/IDE triage, or deviated
> from verbatim. Even benign deviations get logged per D-02.1.

0. **D-19 deviation — author-walked on fresh-state Mac (pre-recorded; required acknowledgment).**
   The walker (cnoellert) is the author of the Phase 20.1 planning artifacts. portofino is their
   personal Mac. Per D-19, outcome is recorded as `PASS-with-deviations`, never clean `PASS`. A
   future real-artist UAT is seeded as
   `.planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md`.

1. **Flame hook surface auto-skipped (expected per D-08).**
   portofino does not have Flame installed. The bootstrap script auto-applied `--track-b`, which
   excludes the Flame hook surface. This is intentional behavior validated in this walk (confirms
   D-08 auto-skip fires correctly and emits the expected log line).

~LIST ADDITIONAL DEVIATIONS HERE~

---

## Outcome

> One of: PASS-with-deviations | FAIL (with gap reference)

**Status:** ~PENDING~

~PENDING — fill in after walk is complete~

**Reasoning:**
~PENDING~

**D-18 gate status after this walk:**
~PENDING~ (both Linux + macOS walks must report PASS-with-deviations for D-18 gate to clear)

---

## Walker sign-off

~PENDING — cnoellert, YYYY-MM-DD~
