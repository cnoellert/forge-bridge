---
status: passed
phase: 20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker
verified: 2026-05-01
verifier: inline (Linux + macOS UAT walks are the primary verification artifacts; this file consolidates the goal-backward check)
phase_requirements: [INSTALL-01, INSTALL-02, INSTALL-03, INSTALL-04]
plans: [20.1-01, 20.1-02, 20.1-03, 20.1-04, 20.1-05, 20.1-06, 20.1-07, 20.1-08]
---

# Phase 20.1 Verification — Goal Achievement Check

## Phase goal (verbatim from ROADMAP.md)

> A fresh-VM Linux walk on flame-01 (Rocky/RHEL 9) AND a fresh-state macOS walk on portofino
> can complete the install end-to-end via `sudo ./scripts/install-bootstrap.sh`, edit
> `/etc/forge-bridge/forge-bridge.env`, and reach all reachable surfaces with `forge doctor`
> PASS or PARTIAL — closing Phase 20's 13-gap log via the daemon model.

## Goal-backward check

| Goal element | Verified | Evidence |
|--------------|---------|----------|
| Fresh-VM Linux walk on flame-01 completes end-to-end | ✅ PASS-with-deviations (D-19) | `20.1-HUMAN-UAT-LINUX.md` re-walk transcript at HEAD `7fe9779`. Daemons `active`, all three ports bound, `forge doctor` PARTIAL with `daemon_state ok`. Fresh-VM contract acknowledged as Deviation 1 (real-artist UAT seeded for v1.6+). |
| Fresh-state macOS walk on portofino completes end-to-end | ✅ PASS-with-deviations (D-19) | `20.1-HUMAN-UAT-MACOS.md` walk transcript at HEAD `cb687f8`. Both daemons `running`, all three ports bound, `forge doctor` PARTIAL with `daemon_state ok`, chat round-trip verified. Walked against Autodesk-bundled Postgres on `:7533`. |
| `sudo ./scripts/install-bootstrap.sh` is the install entry point | ✅ | `scripts/install-bootstrap.sh` exists, is executable, walked end-to-end on both gates. macOS path required `--no-postgres` flag due to Autodesk-Postgres-on-:7533 reality (deferred to `SEED-PHASE-20.1-POSTGRES-OWNERSHIP-V1.6+`). |
| `/etc/forge-bridge/forge-bridge.env` is the canonical config surface | ✅ | `packaging/forge-bridge.env.example` template ships 8 keys (added `FORGE_MCP_PORT` in 20.1-08); installed to `/etc/forge-bridge/forge-bridge.env` with mode `0640 root:${SUDO_USER_GROUP}` on both Linux + macOS. |
| All reachable surfaces green with `forge doctor` PASS or PARTIAL | ✅ | Both gates report PARTIAL (warns only, no fails). Reachable surfaces:<br>- Linux: 4/5 (Surface 5 Flame :9999 not exercised — Flame not running, Deviation 3)<br>- macOS: 4/5 (Surface 1 Flame :9999 not applicable — Track B per D-08) |
| `daemon_state` sub-check in `forge doctor` reports liveness | ✅ | New row `daemon_state ok forge-bridge-server=active/running, forge-bridge=active/running` on both gates. Plan 20.1-05 deliverable. |
| Phase 20's 13-gap log closed | ✅ | All 12 functional gaps closed (gap #13 was a doc-only nuance covered by the INSTALL.md reshape):<br>- #1-#9: Postgres bootstrap, env-file install, alembic, unit registration, doctor verification (Plans 01-04)<br>- #10: stdin-close exit (Plan 20.1-08 `--transport streamable-http`)<br>- #11: single-process doc lie (Plan 20.1-06 INSTALL.md reshape + 20.1-08 lifespan composition)<br>- #12: clean stop semantics (systemd `Requires=` cascade + launchd KeepAlive=SuccessfulExit:false) |
| Daemon model holds (no flap, no clean exit on stdio EOF) | ✅ | After Plan 20.1-08 + lifespan composition fix (commit `7fe9779`), `forge-bridge.service` / `com.cnoellert.forge-bridge.plist` stay `active`/`running` continuously. No `Deactivated successfully` cycles in journal. Same PID hosts `:9996` + `:9997` (lifespan composition). Idempotent re-runs don't disrupt the daemon. |

## Requirement traceability

| Req | Description | Plans | Status |
|-----|-------------|-------|--------|
| INSTALL-01 | Operator-runnable install via single bootstrap script | 01, 02, 03, 04, 05, 08 | ✅ |
| INSTALL-02 | Daemon-supervised on Linux + macOS (systemd / launchd) | 02, 03 | ✅ |
| INSTALL-03 | Operator-facing daemon model documented | 06 | ✅ |
| INSTALL-04 | Env-file is the single source of truth for daemon config | 01, 04, 08 | ✅ |

All 4 phase requirements verified. No requirement IDs in any plan frontmatter are unaccounted for.

## Test surface

- `tests/test_packaging.py` — 36 tests, all passing (env-file, systemd, launchd, install-bootstrap.sh, daemon-persistence)
- `tests/test_cli_main.py` — 10 tests, all passing (`--transport` + `--mcp-port` flag parsing)
- `tests/test_cli_doctor.py` — `daemon_state` sub-check tests passing
- `tests/test_typer_entrypoint.py` — stub widened for forward-compat with `transport` kwarg
- Full suite: 893 passed, 41 skipped, no failures (verified locally on portofino at HEAD `7fe9779` post-Wave 7)

## Walks-as-evidence

The Linux + macOS UAT files are the primary verification artifacts:
- `.planning/phases/20.1-.../20.1-HUMAN-UAT-LINUX.md` — first-pass FAIL preserved per D-02.1, re-walk PASS-with-deviations after 20.1-08 lands
- `.planning/phases/20.1-.../20.1-HUMAN-UAT-MACOS.md` — full multi-attempt walk with 5 in-flight gaps logged + dispositions

Combined Gap log: 6 distinct gaps surfaced, 5 patched in-flight (4 commits to `scripts/install-bootstrap.sh`, 1 commit to `forge_bridge/mcp/server.py`), 1 deferred to a SEED. All on the public record in the UAT files.

## In-flight fix lineage (D-20 aggressive in-flight policy)

| # | Gap | Surface | Fix commit |
|---|-----|---------|------------|
| 0 | `python -m forge_bridge.cli` not executable (sub-package without `__main__`) | Linux first-pass | `aaf6965` |
| 1 | FastMCP stdio + `StandardInput=null` exits cleanly; `:9996` dies with lifespan | Linux first-pass | `7fe9779` (Plan 20.1-08 + lifespan composition) |
| 2 | `bootstrap_pg_macos` only knew Homebrew; Autodesk pgsql-* not detected | macOS Step 3 | `c73227f` |
| 3 | `chown root:${SUDO_USER}` fails on macOS (no user-private group) | macOS Step 3 | `c9aac41` |
| 4 | Log dir `root:wheel 755`; daemon user can't write → launchd EX_CONFIG=78 | macOS Step 6 | `cb687f8` |
| 5 | Stale `forge_bridge==1.3.0` in conda site-packages shadowed editable install | macOS Step 6 | walker action (`pip uninstall && pip install -e`) |
| 6 | Default port collision `:9996` between forge-bridge and projekt-forge | macOS pre-walk | deferred (informal SEED candidate) |

## SEEDs planted at close

- `SEED-PHASE-20.1-ARTIST-UAT-V1.6+` — author-walks logged Deviation 0 on both gates; commit a real-artist fresh-machine UAT at v1.6+ to earn clean PASS.
- `SEED-PHASE-20.1-POSTGRES-OWNERSHIP-V1.6+` — bootstrap ships an opinionated cluster install that doesn't fit the Autodesk-Postgres-on-:7533 reality. Decouple cluster ownership from schema ownership at v1.6+.
- `SEED-CHAT-STREAMING-V1.4.x` — re-validated by macOS chat verification (slow but works); already planted at v1.4.x for v1.6+.

## Verification verdict

**PASSED.** Phase goal achieved on both gates per D-19 (PASS-with-deviations is the highest outcome an author-walk can earn). Daemon model holds; install path is operator-runnable end-to-end on both Linux and macOS; all reachable surfaces green; all 12 Phase 20 functional gaps closed; requirement traceability complete.

**Outstanding human-verification items:** None blocking phase close. The artist-UAT seed defers a fresh-machine non-author walk to v1.6+, which is the appropriate next step in the validation arc, not a phase-close gate.
