---
status: partial
phase: 20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker
plan: 07
platform: Linux (Rocky/RHEL 9)
host: flame-01
source:
  - docs/INSTALL.md
  - scripts/install-bootstrap.sh
  - packaging/systemd/forge-bridge-server.service
  - packaging/systemd/forge-bridge.service
  - /etc/forge-bridge/forge-bridge.env
walker_discipline:
  - verbatim-copy-paste
  - verbatim-transcription
  - no-silent-route-around
  - log-every-author-context-dependence
fresh_vm_contract: true
started: ~PENDING~
updated: ~PENDING~
outcome: ~PENDING~
---

# Phase 20.1 — Linux Fresh-VM Walk on flame-01 (Gate per D-18)

> Walker: cnoellert — project author. Fresh-VM discipline per D-17 (no prior conda env, no prior
> Postgres role, no prior /etc/forge-bridge). Outcome recorded as `PASS-with-deviations` per D-19
> (author-walk cannot earn clean PASS per D-02.1 honesty model).
>
> **Status: PENDING — walk not yet conducted.**
>
> This scaffold was pre-created by the executor agent to define the document shape. The operator
> must complete the walk on flame-01 per the discipline contract below and fill in each section.
> Do NOT mark outcome as PASS until the walk is complete.

**Date:** ~PENDING~
**Walker:** cnoellert — project orchestrator / author of planning artifacts; non-author of the
bootstrap script itself (script was synthesized by Claude from CONTEXT.md + RESEARCH.md; this
walk is the first end-to-end physical execution). See Deviations, item 0.
**Host:** flame-01 (Rocky Linux 9.x, x86_64)
**LLM service host (separate):** assist-01 @ `192.168.86.15` — Ollama + `qwen2.5-coder:32b`
**Branch / HEAD commit:** ~PENDING~
**INSTALL.md commit:** ~PENDING~

**Discipline contract (D-17 fresh-VM + D-02.1 honesty model):**
1. Copy-paste every command verbatim from `docs/INSTALL.md`. Do NOT mentally substitute corrections.
2. Transcribe observed output verbatim (elide with `[...]` only for length, mark all elisions).
3. NO silent route-around. If a command fails or doc is ambiguous, STOP, log the gap, then continue.
4. Log every author-context dependence (SSH, IDE, prior-state knowledge) as a deviation.
5. Fresh-VM contract: no prior conda env, no prior Postgres role, no prior /etc/forge-bridge, no
   prior systemd units for forge-bridge. Tear down any prior state BEFORE starting (document teardown).

---

## Pre-walk fixture state

> Record the box's exact state BEFORE any walk step. If prior state existed, document the teardown
> steps performed to reach fresh-VM condition.

```
# Commands to run verbatim and paste output:
$ cat /etc/redhat-release
~PENDING~

$ command -v psql || echo "no psql"
~PENDING~

$ [ -d /var/lib/pgsql/data/base ] && echo "cluster initialized" || echo "no cluster"
~PENDING~

$ [ -d /etc/forge-bridge ] && ls -la /etc/forge-bridge || echo "no /etc/forge-bridge"
~PENDING~

$ systemctl list-units --all | grep forge-bridge || echo "no forge-bridge units"
~PENDING~

$ ls /Library/LaunchDaemons/com.cnoellert.* 2>/dev/null || echo "no launchd plists"
~PENDING~
```

**Teardown steps performed (if any prior state existed):**
~NONE OR DOCUMENT STEPS~

---

## Reference versions (D-22)

> Captured at walk-time per D-22. Record AFTER Step 3 (bootstrap) completes, when Postgres is installed.

| Dependency | Host | Version found |
|------------|------|---------------|
| Python (after `conda activate forge`) | flame-01 | ~PENDING~ |
| conda | flame-01 | ~PENDING~ |
| PostgreSQL (post-Step-3) | flame-01 | ~PENDING~ |
| systemd | flame-01 | ~PENDING~ |
| forge-bridge `__version__` (after Step 2) | flame-01 | ~PENDING~ |
| Ollama | assist-01 | ~PENDING~ |
| qwen2.5-coder:32b pulled | assist-01 | ~PENDING~ |

---

## Walk transcript

> Verbatim command + verbatim output for each INSTALL.md step. Elide with `[...]` only for length;
> always mark elisions.

### Step 1: Prepare the conda environment

```
$ conda create -n forge python=3.11 -y
~PENDING~

$ conda activate forge
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 2: Install forge-bridge

```
$ pip install -e ".[dev,llm]"
~PENDING~

$ python -c "import forge_bridge; print(forge_bridge.__version__)"
~PENDING~

$ forge-bridge --help
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 3: Run the bootstrap script

> This is the new P4 script. The D-18 gate turns on how this step runs.

```
$ sudo ./scripts/install-bootstrap.sh
~PENDING~
```

**Confirm:**
- [ ] Script prompted for Ollama URL (or `--non-interactive` defaults applied)
- [ ] Script printed `[forge-bridge] Running forge-bridge console doctor...`
- [ ] `forge doctor` reported PASS or PARTIAL with `daemon_state` row visible
- [ ] Script exited 0

**If FAIL — STOP, log the gap below, do not continue.**

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 4: Install the Flame hook (Track A — Linux full path)

```
$ ./scripts/install-flame-hook.sh
~PENDING~

$ curl -s http://localhost:9999/status | python3 -m json.tool
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 5: Configure environment

```
$ sudo nano /etc/forge-bridge/forge-bridge.env
# (set FORGE_LOCAL_LLM_URL=http://192.168.86.15:11434/v1 — confirm URL is correct)
~PENDING~

$ sudo systemctl restart forge-bridge
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 6: Verify the daemons are running

```
$ sudo systemctl status forge-bridge-server
~PENDING~

$ sudo systemctl status forge-bridge
~PENDING~

$ sudo journalctl -u forge-bridge -n 20
~PENDING~
```

**Confirm:**
- [ ] `forge-bridge-server` → active (running) on `:9998`
- [ ] `forge-bridge` → active (running) on `:9996`
- [ ] No FATAL / CRITICAL lines in journalctl output

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 7: Verify all five surfaces

> Exercise all 5 surfaces per INSTALL.md Step 7 discipline.

```
# Surface 1: Web UI
$ curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"
~PENDING~

# Surface 2: CLI
$ forge-bridge console doctor
~PENDING~

# Surface 3: Chat endpoint
$ curl -s -X POST http://localhost:9996/api/v1/chat \
    -H 'Content-Type: application/json' \
    -d '{"prompt":"what tools do you have"}'
~PENDING~

# Surface 4: MCP server stdio (manual ctrl-C OK)
$ python -m forge_bridge
# (confirm starts, then ctrl-C)
~PENDING~

# Surface 5: Flame hook on :9999
$ curl -s http://localhost:9999/status | python3 -m json.tool
~PENDING~
```

**Gap surfaced:** None / ~LIST IF ANY~

---

### Step 8: Post-install diagnostic

```
$ forge-bridge console doctor
~PENDING~
```

---

### Idempotency check (RESEARCH.md OQ #3)

```
$ sudo ./scripts/install-bootstrap.sh --non-interactive
~PENDING~
```

**Confirm:**
- [ ] Script ran without error
- [ ] Env-file edits preserved (FORGE_LOCAL_LLM_URL unchanged)
- [ ] Both daemons still active after re-run
- [ ] `forge doctor` still PASS or PARTIAL

**Gap surfaced:** None / ~LIST IF ANY~

---

## Surfaces table

| # | Surface | Smoke command | Result | Notes |
|---|---------|--------------|--------|-------|
| 1 | Flame hook on :9999 | `curl -s http://localhost:9999/status` | ~PENDING~ | |
| 2 | CLI `forge-bridge` | `forge-bridge console doctor` | ~PENDING~ | |
| 3 | Web UI on :9996/ui/ | `curl ... -w "%{http_code}\n"` | ~PENDING~ | |
| 4 | HTTP /api/v1/chat | `curl -s -X POST ...` | ~PENDING~ | |
| 5 | MCP server stdio | `python -m forge_bridge` (ctrl-C) | ~PENDING~ | |

---

## Gap log

> Every gap surfaced during the walk. Disposition per D-20 (aggressive in-flight fix policy).
> If NO gaps: "No gaps surfaced — all steps walked cleanly."

| # | Step | Gap description | Disposition | Plan / seed |
|---|------|----------------|-------------|-------------|
| ~N~ | ~STEP~ | ~DESCRIPTION~ | ~in-flight-fix / doc-only / v1.6+ seed~ | ~20.1-08+ or seed reference~ |

---

## Deviations

> Any step where the walker drew on knowledge not in the doc, used SSH/IDE triage, or deviated
> from verbatim. Even benign deviations get logged per D-02.1.

0. **D-19 deviation — author-walked on fresh VM (pre-recorded; required acknowledgment).**
   The walker (cnoellert) is the author of the Phase 20.1 planning artifacts. This walk is conducted
   under the D-17 fresh-VM discipline (no prior conda env, no prior Postgres) but the walker has full
   context that a real artist would not. Per D-19, outcome is recorded as `PASS-with-deviations`,
   never clean `PASS`. A future real-artist UAT is seeded as
   `.planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md`.

~LIST ADDITIONAL DEVIATIONS HERE~

---

## Outcome

> One of: PASS-with-deviations | FAIL (with gap reference)

**Status:** ~PENDING~

~PENDING — fill in after walk is complete~

**Reasoning:**
~PENDING~

---

## Walker sign-off

~PENDING — cnoellert, YYYY-MM-DD~
