---
phase: 20.1
plan: 07
type: execute
wave: 4
depends_on: [20.1-04, 20.1-05, 20.1-06]
files_modified:
  - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md
  - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md
  - .planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md
autonomous: false
requirements: [INSTALL-01, INSTALL-02, INSTALL-03, INSTALL-04]
tags: [uat, manual-walk, gate, phase-close]

must_haves:
  truths:
    - "20.1-HUMAN-UAT-LINUX.md exists and records a fresh-VM Linux walk on flame-01 (Rocky/RHEL 9) per D-17 fresh-VM contract"
    - "20.1-HUMAN-UAT-MACOS.md exists and records a fresh-state macOS walk on portofino per D-17"
    - "Both walks reach all reachable surfaces with `forge doctor` PASS or PARTIAL per D-12"
    - "Both walks are gates per D-18 — phase does NOT close until BOTH walks report clean (zero gaps surface, all expected surfaces green)"
    - "Outcome of each walk recorded as `PASS-with-deviations` per D-19 (matches Phase 20 D-02.1 honesty model — never claim clean PASS off an author-walk)"
    - "`.planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md` is planted at phase close per D-19 (real-artist UAT walk against the 20.1 install path; deferred until a non-author Flame artist or pipeline operator without sysadmin training is available)"
    - "Reference versions (D-22) captured in each HUMAN-UAT.md: Linux fresh-VM Postgres/Python/conda + Ollama-on-assist-01; macOS portofino brew/postgresql/Python/conda"
    - "Any gap surfaced during either walk gets a disposition per D-20 (in-flight gap-fix policy = aggressive); a gap that balloons beyond ~1 plan triggers a 20.1.1 / 20.2 fractal per D-21"
  artifacts:
    - path: ".planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md"
      provides: "Verbatim Linux fresh-VM walk transcript on flame-01 with reference-versions table + gap-disposition table"
      contains: "## Pre-walk fixture state, ## Reference versions, ## Walk transcript, ## Gap log, ## Outcome"
      min_lines: 100
    - path: ".planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md"
      provides: "Verbatim macOS fresh-state walk transcript on portofino"
      contains: "## Pre-walk fixture state, ## Reference versions, ## Walk transcript (auto---track-b path validated), ## Gap log, ## Outcome"
      min_lines: 100
    - path: ".planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md"
      provides: "Planted seed for real-artist UAT walk in v1.6+"
      contains: "Trigger condition, walker discipline, mirror of Phase 20's SEED-PHASE-20-NON-AUTHOR-UAT-V1.6+ shape"
  key_links:
    - from: "20.1-HUMAN-UAT-LINUX.md"
      to: "scripts/install-bootstrap.sh, packaging/systemd/*.service, /etc/forge-bridge/forge-bridge.env"
      via: "walk transcript records each command + observed output"
      pattern: "sudo \\./scripts/install-bootstrap\\.sh|systemctl is-active|forge-bridge console doctor"
    - from: "20.1-HUMAN-UAT-MACOS.md"
      to: "scripts/install-bootstrap.sh, packaging/launchd/*.plist, /etc/forge-bridge/forge-bridge.env"
      via: "walk transcript on portofino"
      pattern: "launchctl bootstrap system|launchctl print system/com\\.cnoellert\\.forge-bridge"
---

<objective>
Phase 20.1's two gate walks — Linux on flame-01 (fresh VM / Rocky 9) AND macOS on portofino (fresh-state) per D-18. These are the v1.5 ship-blocker validation. Both walks must report clean (PASS-with-deviations per D-19, all 5 reachable surfaces green, `forge doctor` PASS or PARTIAL) before Phase 20.1 closes. Plus the close-time SEED plant per D-19.

Purpose: Phase 20 closed `PASS-with-deviations` because the only available walker was the author. Phase 20.1 inherits that constraint at the same level (D-19 — never claim clean PASS off an author-walk) but raises the bar in two ways:
1. **Fresh-VM contract** (D-17) — no prior conda env, no prior Postgres, no prior shell history. The script must handle a true cold start, exercising every branch honestly.
2. **Both OSes are gates** (D-18) — Linux PASS alone does not close 20.1; macOS PASS alone does not close 20.1; one-of-two is rejected.

Output:
- `20.1-HUMAN-UAT-LINUX.md` (fresh-VM Linux walk on flame-01)
- `20.1-HUMAN-UAT-MACOS.md` (fresh-state macOS walk on portofino)
- `.planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md` (planted at close per D-19)

Per CONTEXT.md D-23 + D-21: This plan ships ONLY documentation artifacts (walks + seed). Any in-flight gap-fix surfaced during the walks lands as a SEPARATE plan 20.1-08 / 20.1-09 / etc. before the phase closes (D-20 aggressive fix policy). If a gap balloons beyond ~1 plan, fractal to 20.1.1 / 20.2 per D-21.

This plan is `autonomous: false` because the walks are physical hands-on-keyboard walks on real machines (flame-01 + portofino). No automated CI substitute; the entire Phase 20.1 forcing-function premise is "the install procedure is a script with a verification checklist; the only honest verification is to run it on a fresh machine."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-CONTEXT.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-RESEARCH.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-04-SUMMARY.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-05-SUMMARY.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-06-SUMMARY.md
@.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md

<interfaces>
<!-- Walker discipline (carried from Phase 20 D-02.1, hardened by D-17 fresh-VM contract): -->
1. Verbatim copy-paste from docs/INSTALL.md — type nothing the doc doesn't say to type
2. Verbatim observation transcription — paste exact command output (truncate only for log brevity, marked with `[...]`)
3. NO silent route-around — if a command fails or output diverges, STOP and log the gap
4. Log every author-context dependence (SSH, IDE, prior-state knowledge) — these are deviations
5. NO prior conda env / NO prior Postgres / NO prior /etc/forge-bridge / NO prior shell history (D-17 fresh-VM)

<!-- Walk shape (Phase 20.1's own UAT — RESEARCH.md + Phase 20 precedent): -->
1. Fresh Rocky 9 box / fresh-state portofino with: postgresql package may or may not be present;
   cluster definitely uninitialized; no forge user; no forge_bridge db; no prior /etc/forge-bridge.
2. Walker clones the repo (or in fresh-VM case, transfers the repo from the host).
3. Walker completes Steps 1 + 2 of INSTALL.md (conda env + pip install — these are unchanged from
   Phase 20).
4. Walker runs `sudo ./scripts/install-bootstrap.sh` (Step 3, the new bootstrap-script invocation).
5. Walker confirms it ran cleanly OR logs the gap.
6. Walker edits /etc/forge-bridge/forge-bridge.env (Step 5 — minimum: confirm `FORGE_LOCAL_LLM_URL`
   is correct for assist-01).
7. Walker confirms `systemctl status` (Linux) / `launchctl print system/...` (macOS) shows both
   daemons active.
8. Walker runs `forge-bridge console doctor` and pastes the output table.
9. Walker exercises all 5 reachable surfaces per docs/INSTALL.md Step 7 (Web UI, CLI, /api/v1/chat,
   MCP server stdio, Flame hook on `:9999` — the last is Linux flame-01 only; macOS portofino
   walks --track-b auto-applied per D-08).

<!-- D-09 minimum coverage: combos (a) Linux full + (c) macOS default. -->
<!-- (b) Linux --mcp-only and (d) Linux --no-postgres are SMOKE checks — planner discretion whether
     to fold them into the same flame-01 fresh-VM walk or skip. The D-18 phase-close gate is (a) + (c). -->

<!-- Outcome semantics (D-19): -->
PASS-with-deviations  = phase closes; SEED planted for v1.6+ real-artist walk
PASS                  = author cannot claim this; Phase 20 D-02.1 precedent locks the honesty model
FAIL (any gap surfaces, doesn't auto-fix in-flight) = trigger 20.1-08+ in-flight fix per D-20, OR
                                                     spin 20.1.1 / 20.2 per D-21 if gap balloons

<!-- HUMAN-UAT.md template inherited from Phase 20: -->
##  Pre-walk fixture state    — what existed BEFORE the walk (conda? Postgres? prior /etc/?)
##  Reference versions (D-22) — Postgres/Python/conda/Ollama versions captured at walk-time
##  Walk transcript           — verbatim cmd + verbatim output, with [...] for elision
##  Surfaces table            — 5 surfaces × {smoke command, result, notes}
##  Gap log                   — table: # | step | description | disposition | plan/seed
##  Deviations                — author-context dependencies (SSH, prior knowledge)
##  Outcome                   — PASS-with-deviations / FAIL + reasoning
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Walk the fresh-VM Linux install on flame-01 (gate per D-18)</name>
  <files>.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md</files>
  <read_first>
    - .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md (the Phase 20 walk transcript — use as the document-shape template; Phase 20.1 walk should mirror its sections + voice)
    - docs/INSTALL.md (the reshaped doc, P6) — walker MUST copy-paste each command verbatim; if doc command fails or doc text is ambiguous, STOP and log the gap before re-walking that step
    - scripts/install-bootstrap.sh (the script being walked) — read for awareness, but during the walk DO NOT route around the script; let it surface its own gaps
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-CONTEXT.md (D-17 fresh-VM contract; D-18 both-walks gate; D-19 outcome semantics; D-20 in-flight gap-fix; D-22 reference versions)
  </read_first>
  <what-built>
    Phase 20.1 P1-P6 shipped:
    - packaging/forge-bridge.env.example (P1)
    - packaging/systemd/forge-bridge-server.service + forge-bridge.service (P2)
    - packaging/launchd/com.cnoellert.forge-bridge*.plist + wrapper scripts (P3)
    - scripts/install-bootstrap.sh (P4)
    - forge_bridge/cli/doctor.py daemon_state sub-check (P5)
    - docs/INSTALL.md reshape — Steps 3, 5, 6 (P6)

    flame-01 is a Rocky/RHEL 9 box; the walk validates that this artifact set actually walks an
    operator (or author-as-operator-on-fresh-VM) from "fresh machine" to "5 reachable surfaces with
    `forge doctor` PASS or PARTIAL" without re-deriving the topology.
  </what-built>
  <how-to-verify>
On flame-01, in a FRESH state per D-17 (no prior conda env, no prior Postgres role, no prior /etc/forge-bridge directory; if these exist from prior walks, tear them down first OR use a fresh VM):

1. **Pre-walk fixture capture** — record the box's pre-walk state in `20.1-HUMAN-UAT-LINUX.md`:
   - `cat /etc/redhat-release`
   - `command -v psql || echo "no psql"`
   - `[ -d /var/lib/pgsql/data/base ] && echo "cluster initialized" || echo "no cluster"`
   - `[ -d /etc/forge-bridge ] && ls -la /etc/forge-bridge || echo "no /etc/forge-bridge"`
   - `systemctl list-units --all | grep forge-bridge || echo "no forge-bridge units"`
   - `ls /Library/LaunchDaemons/com.cnoellert.* 2>/dev/null || echo "no launchd plists"` (sanity on Linux — should be 'no launchd plists')

2. **Reference versions (D-22)** — record:
   - `python --version` (operator's user shell, AFTER conda activate forge — should be 3.11.x)
   - `conda --version`
   - `psql --version` (post-Step-3, AFTER bootstrap script's `dnf install`)
   - `systemctl --version | head -1`
   - assist-01 reference: `curl -s http://192.168.86.15:11434/api/version` (or whatever your LLM host URL resolves to — the env file's FORGE_LOCAL_LLM_URL)

3. **Walk INSTALL.md Steps 1 → 8 verbatim**, transcribing:
   - Each command (verbatim copy-paste from INSTALL.md)
   - Each observed output (verbatim — elide with `[...]` only for brevity)
   - Any deviation (with justification — e.g. "I had to SSH to triage X" is a deviation per D-02.1)

4. **Step 3 specifically** — the new bootstrap script:
   - Run `sudo ./scripts/install-bootstrap.sh` (no flags)
   - Confirm it asked you for Ollama URL (or that --non-interactive defaults applied)
   - Confirm `forge doctor` auto-ran at end and reported PASS or PARTIAL
   - If FAIL — STOP, log the gap, propose disposition (in-flight fix per D-20 or fractal per D-21)

5. **Step 6 verification** (the daemon model):
   - `sudo systemctl status forge-bridge-server` → active (running) on `:9998`
   - `sudo systemctl status forge-bridge` → active (running) on `:9996`
   - `sudo journalctl -u forge-bridge -n 20` → readable; no FATAL / CRITICAL lines

6. **Step 7 surface check** — exercise all 5 surfaces:
   - Web UI: `curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"` → 200
   - CLI: `forge-bridge console doctor` → PASS or PARTIAL with daemon_state row visible
   - Chat: `curl -s -X POST http://localhost:9996/api/v1/chat -H 'Content-Type: application/json' -d '{"prompt":"what tools do you have"}'` → JSON with non-empty response (under 60s)
   - MCP server stdio: spawn a fresh `python -m forge_bridge` and confirm it starts (manual ctrl-C OK)
   - Flame hook on :9999: `curl -s http://localhost:9999/status | python3 -m json.tool` → `flame_available: true|false`

7. **Outcome (D-19)**:
   - All clean → PASS-with-deviations (author-walk on fresh VM)
   - Any gap → Disposition: in-flight fix (20.1-08+) OR doc-only patch OR fractal (20.1.1)
   - SSH triage / IDE / prior-knowledge dependencies → log as deviations

8. **Idempotency check** (RESEARCH.md Open Question 3):
   - Re-run `sudo ./scripts/install-bootstrap.sh --non-interactive`
   - Expected: no-op; doctor still PASS; env-file edits preserved

Write `20.1-HUMAN-UAT-LINUX.md` mirroring `.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md` section structure: Pre-walk / Reference versions / Walk transcript / Surfaces table / Gap log / Deviations / Outcome.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "linux PASS" — Phase 20.1 Linux gate clean (zero gaps); proceed to Task 2 (macOS walk)
    - "linux PASS-with-deviations: <list>" — gaps logged but disposition is "doc-only patch I will apply inline" or "deferred to v1.6+ via seed"; proceed to Task 2
    - "linux FAIL: <gap>" — gap surfaced that needs in-flight fix; STOP this plan, spin 20.1-08 (or higher) for the fix, re-walk after fix lands
    - "fractal: 20.1.1 needed: <reason>" — gap balloons beyond ~1 plan per D-21; spin Phase 20.1.1
  </resume-signal>
  <action>
This is a checkpoint:human-action task — the walker performs the walk as described in <how-to-verify> on flame-01 (Rocky/RHEL 9 fresh VM per D-17). The walker writes the verbatim transcript to .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md mirroring the Phase 20 HUMAN-UAT.md document shape (Pre-walk / Reference versions / Walk transcript / Surfaces table / Gap log / Deviations / Outcome). Walker discipline applies (CONTEXT.md walker contract): verbatim copy-paste, verbatim observation transcription, no silent route-around, log every author-context dependence as a deviation, fresh-state contract (no prior conda env, no prior Postgres, no prior /etc/forge-bridge). The walk is a manual gate per D-18; orchestrator pauses on the resume-signal.
  </action>
  <verify>
    <automated>test -f .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md && grep -F "## Outcome" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md && grep -F "PASS-with-deviations\|PASS\|FAIL" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md && grep -F "sudo ./scripts/install-bootstrap.sh" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md && grep -F "forge doctor" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md && grep -F "systemctl is-active\|systemctl status forge-bridge" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md</automated>
  </verify>
  <done>
    20.1-HUMAN-UAT-LINUX.md exists. Contains all required sections (Pre-walk fixture state, Reference versions per D-22, Walk transcript, Surfaces table, Gap log with dispositions, Deviations, Outcome). Outcome is `PASS-with-deviations` per D-19 (or FAIL with in-flight 20.1-08+ fix that subsequently lands). All 5 reachable surfaces exercised. forge doctor PASS or PARTIAL with daemon_state row visible. Idempotency check (re-run script) recorded.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Walk the fresh-state macOS install on portofino (gate per D-18)</name>
  <files>.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md</files>
  <read_first>
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md (just created in Task 1 — confirm doc shape; reuse where applicable)
    - docs/INSTALL.md (the reshaped doc, P6) — walker MUST copy-paste each command verbatim
    - scripts/install-bootstrap.sh (the script being walked, macOS branch)
    - packaging/launchd/com.cnoellert.forge-bridge*.plist (the macOS surface being validated)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-CONTEXT.md (D-04 macOS Postgres branch; D-08 auto-track-b; D-15 macOS log paths)
  </read_first>
  <what-built>
    Same artifact set as Task 1, walked on the macOS surface. The macOS walk validates:
    - launchd plists in /Library/LaunchDaemons/
    - wrapper scripts in /usr/local/bin/
    - /var/log/forge-bridge/{console,server}.log (mode 755 root:wheel)
    - auto-applied --track-b (no Flame hook on macOS)
    - macOS Postgres branch (D-04: detect existing OR brew install postgresql@16)

    portofino is the operator's personal Mac per CONTEXT.md `<specifics>`. Per user-confirmed
    "all Mac Flame workstations canonically have Postgres pre-installed," portofino likely has
    Postgres present; the script's "skip install" branch fires (rare-edge brew install branch
    of D-04 doesn't fire on portofino, but the script's idempotency must hold).
  </what-built>
  <how-to-verify>
On portofino, in a FRESH state per D-17 (no prior conda env, no prior /etc/forge-bridge directory, no prior /Library/LaunchDaemons/com.cnoellert.forge-bridge*.plist; if these exist from prior walks, `launchctl bootout` and `rm` them first):

1. **Pre-walk fixture capture** — record:
   - `sw_vers -productVersion` (macOS Sequoia 15.7.4 reference)
   - `uname -m` (arm64 — Apple Silicon)
   - `command -v brew && brew --version | head -1`
   - `command -v psql && psql --version` (verify pre-installed Postgres claim)
   - `[ -d /etc/forge-bridge ] && ls -la /etc/forge-bridge || echo "no /etc/forge-bridge"`
   - `[ -f /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist ] && echo "prior install present" || echo "no prior plists"`
   - `[ -d /var/log/forge-bridge ] && ls -la /var/log/forge-bridge || echo "no log dir"`

2. **Reference versions (D-22)** — record after Step 3 completes:
   - `python --version` (forge env, after conda activate)
   - `conda --version`
   - `psql --version`
   - `launchctl version 2>&1 | head -1` (Darwin Bootstrapper version)
   - `bash --version | head -1` (system bash 3.2 vs Homebrew bash 5.x)

3. **Walk INSTALL.md Steps 1 → 8 verbatim** — same discipline as Task 1.

4. **Step 3 specifically** (macOS branch):
   - `sudo ./scripts/install-bootstrap.sh` — confirm:
     - Auto-applies --track-b (logs "macOS detected — auto-applying --track-b")
     - Detects existing Postgres OR runs `brew install postgresql@16` (rare branch per D-04)
     - Skips pg_hba alignment (Homebrew Postgres ships with `trust` for localhost)
     - Creates /var/log/forge-bridge/ BEFORE `launchctl bootstrap` (Pitfall 2 fix)
     - `launchctl bootstrap system /Library/LaunchDaemons/com.cnoellert.forge-bridge*.plist` succeeds
     - `forge doctor` auto-runs and reports PASS/PARTIAL with daemon_state row visible

5. **Step 6 verification** (macOS daemon model):
   - `sudo launchctl print system/com.cnoellert.forge-bridge-server | grep state` → `state = running`
   - `sudo launchctl print system/com.cnoellert.forge-bridge | grep state` → `state = running`
   - `tail -20 /var/log/forge-bridge/console.log` → readable; no fatal errors
   - `tail -20 /var/log/forge-bridge/server.log` → readable; no fatal errors

6. **Step 7 surface check** — same 5-surface curl matrix as Task 1, with one variation:
   - **SKIP the Flame hook surface on portofino** (no Flame on portofino → expected behavior; D-08 auto-applied --track-b)
   - All other surfaces (Web UI / CLI / chat / MCP) must reach green

7. **Idempotency check** — re-run script per RESEARCH.md OQ #3 + D-21:
   - `sudo ./scripts/install-bootstrap.sh --non-interactive`
   - Expected: no-op; daemons stay running; env-file preserved; `launchctl bootout` + `bootstrap` cycle leaves the running daemons intact

8. **macOS-specific verification** — `plutil -lint` on installed plists:
   - `plutil -lint /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist` → "OK"
   - `plutil -lint /Library/LaunchDaemons/com.cnoellert.forge-bridge-server.plist` → "OK"
   - These confirm the substituted-placeholder plists are well-formed XML (Pitfall 5 regression check)

9. **Outcome (D-19)** — same disposition matrix as Task 1.

Write `20.1-HUMAN-UAT-MACOS.md` mirroring the Linux walk's section structure.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "macos PASS" — gate clean; proceed to Task 3 (seed plant)
    - "macos PASS-with-deviations: <list>" — gaps logged with disposition; proceed to Task 3
    - "macos FAIL: <gap>" — STOP, spin in-flight fix plan, re-walk
    - "fractal: 20.2 needed: <reason>" — gap balloons beyond ~1 plan; spin Phase 20.2
  </resume-signal>
  <action>
This is a checkpoint:human-action task — the walker performs the walk as described in <how-to-verify> on portofino (macOS Sequoia 15.7.4 fresh-state per D-17). The walker writes the verbatim transcript to .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md mirroring the Linux walk's document shape. Same walker discipline applies (verbatim, no route-around, fresh state). Per D-08 the macOS walk auto-applies --track-b (Flame hook surface skipped on portofino — 4 surfaces validated, not 5). Per D-04 the Postgres branch is the rare-edge brew install path; if portofino has Postgres pre-installed (per user-confirmed expectation), the script's "skip install" branch fires and the brew install code path is exercised on a different fresh VM if needed for D-09 coverage (planner discretion). The walk is a manual gate per D-18; orchestrator pauses on the resume-signal.
  </action>
  <verify>
    <automated>test -f .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md && grep -F "## Outcome" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md && grep -F "PASS-with-deviations\|PASS\|FAIL" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md && grep -F "launchctl bootstrap system\|launchctl print system/com.cnoellert.forge-bridge" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md && grep -F "/var/log/forge-bridge" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md && grep -F "forge doctor\|forge-bridge console doctor" .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md</automated>
  </verify>
  <done>
    20.1-HUMAN-UAT-MACOS.md exists. Contains all required sections including macOS-specific surface verification (launchctl print state lines, /var/log/forge-bridge/console.log + server.log readable, plutil -lint clean on installed plists). Outcome is `PASS-with-deviations` per D-19 (or FAIL with in-flight fix). 4 reachable surfaces exercised (Flame hook auto-skipped per D-08). Idempotency check recorded. Phase 20.1 D-18 gate satisfied (BOTH Linux and macOS walks reported clean by this point).
  </done>
</task>

<task type="auto">
  <name>Task 3: Plant SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md (D-19 close-time obligation)</name>
  <files>.planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md</files>
  <read_first>
    - .planning/seeds/SEED-PHASE-20-NON-AUTHOR-UAT-V1.6+.md (the parallel Phase 20 seed; use as the structural template — same naming convention, same content shape)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-LINUX.md (just created — reference the deviations the seed addresses)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-MACOS.md (just created — reference any macOS-specific deviations)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-CONTEXT.md (D-19 outcome semantics — "Outcome semantics — author-walk PASS = phase closes PASS-with-deviations and 20.1 plants `.planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md` for future real-artist verification")
  </read_first>
  <action>
Create `.planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md`. Mirror the structure of `.planning/seeds/SEED-PHASE-20-NON-AUTHOR-UAT-V1.6+.md` exactly.

Content body:

  # SEED — Phase 20.1 Artist UAT (v1.6+)

  **Status:** planted (Phase 20.1 close, 2026-05-DD)
  **Trigger condition:** A Flame artist or post-production pipeline operator without sysadmin training becomes available to walk the v1.5+ install path on a fresh machine.
  **Dispositioned during:** Phase 20.1 close per D-19

  ## Why this seed exists

  Phase 20.1 closed `PASS-with-deviations` from author-walks on flame-01 (Linux) + portofino (macOS). The walks validated that the install procedure (script + daemon + env file + reshaped INSTALL.md) walks cleanly on a fresh VM / fresh state. They did NOT validate the deeper Phase 20.1 acceptance criterion: **a Flame artist with no Linux/Postgres/sysadmin background can complete the install end-to-end and reach all 5 surfaces.**

  Author-walks under D-17 fresh-VM discipline catch the structural gaps (missing initdb, missing pg_hba alignment, missing daemon-state probe). They do not catch the artist-comprehension gaps that surface only when a non-author follows the doc verbatim:
  - Does the artist know what `sudo` means, why they need it, what to do if it fails?
  - Does the doc's mention of "your conda env" assume conda mental model the artist may lack?
  - Does the bootstrap script's interactive Ollama-URL prompt produce sensible default behavior when the artist hits Enter without thinking?
  - Does the daemon log path (`/var/log/forge-bridge/console.log` on macOS, `journalctl -u forge-bridge` on Linux) make sense to someone who has never read a service log?

  These questions can only be answered by an actual artist walking the install. Phase 20 D-02.1 set the precedent: never claim clean PASS off an author-walk; plant a seed for the real walk and address gaps when a real walker materializes.

  ## What triggers a follow-up phase

  When ANY of the following becomes available:

  1. A Flame artist (or post-production pipeline operator) with no prior forge-bridge knowledge, no Linux sysadmin training, no Postgres administration history.
  2. The artist is willing to spend ~30-60 minutes walking the install procedure verbatim.
  3. The artist has access to a fresh-state machine (Linux Rocky/RHEL or macOS) per D-17.

  ## What the follow-up phase ships

  Mirror Phase 20.1's UAT discipline:
  - `<follow-up-phase>-HUMAN-UAT-ARTIST-LINUX.md` and/or `<follow-up-phase>-HUMAN-UAT-ARTIST-MACOS.md` — verbatim transcript with gap log
  - In-flight fixes (D-20 aggressive fix policy) for any artist-comprehension gap surfaced
  - If the artist hits a substantive gap requiring more than ~1 plan to close, fractal to next decimal phase (analogous to D-21)
  - At close, the seed flips to "validated by `<artist-name>` on `<date>`" and is preserved as historical record

  ## Carry-forward context

  - Phase 20.1 deliverables (P1-P6): bootstrap script + 2 systemd units + 2 launchd plists + 2 wrapper scripts + env-file template + doctor `daemon_state` sub-check + reshaped INSTALL.md
  - Phase 20.1 walk evidence (P7): `20.1-HUMAN-UAT-LINUX.md` + `20.1-HUMAN-UAT-MACOS.md` — read these for "what the author walked correctly" baseline
  - Cross-reference Phase 20's parallel seed: `.planning/seeds/SEED-PHASE-20-NON-AUTHOR-UAT-V1.6+.md` (Phase 20 INSTALL.md authoring; same honesty model, different content surface)
  - Project memory `feedback_strong_recos_technical.md` — operator prefers strong recommendations on technical decisions; if the artist asks "should I use the default settings?" the doc / script should answer

  ## Out of scope (defer to v1.6+ proper)

  - Multi-artist UAT matrix (single-artist walk is sufficient for trigger)
  - Recording artist sessions (privacy + tooling concerns)
  - Building an artist-walk environment (script the fresh-VM provisioning) — out of legibility milestone
  - Pre-emptive fix-everything-the-author-imagined-an-artist-might-hit (yagni; let the real walk surface the real gaps)

  ## Closing this seed

  This seed closes when:
  - A real-artist walk completes (PASS or PASS-with-deviations of artist-specific kind)
  - All in-flight gap-fix plans land
  - 20.1's two HUMAN-UAT.md walks remain valid (the P1-P6 deliverables haven't drifted; if they have, the seed re-opens against the new state)

  **Filed:** Phase 20.1 close (P7), 2026-05-DD
  **Mirror of:** SEED-PHASE-20-NON-AUTHOR-UAT-V1.6+.md (Phase 20 close, 2026-05-01)
  **Owner:** v1.6+ planning

DO NOT include real artist names; the seed is a template that activates when the trigger condition lands.
  </action>
  <verify>
    <automated>test -f .planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md && grep -F 'SEED — Phase 20.1 Artist UAT' .planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md && grep -F 'PASS-with-deviations' .planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md && grep -F 'D-17' .planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md && grep -F 'SEED-PHASE-20-NON-AUTHOR-UAT-V1.6+.md' .planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md` exists
    - File contains exact title `SEED — Phase 20.1 Artist UAT` heading
    - File mentions `PASS-with-deviations` (D-19 outcome semantics carried forward)
    - File mentions `D-17` fresh-VM contract (carried forward as artist-walk fixture requirement)
    - File cross-references the Phase 20 parallel seed `SEED-PHASE-20-NON-AUTHOR-UAT-V1.6+.md`
    - File mentions Phase 20.1 walk artifacts `20.1-HUMAN-UAT-LINUX.md` AND `20.1-HUMAN-UAT-MACOS.md`
    - File contains `## Trigger condition` (or equivalent — what activates the v1.6+ follow-up phase)
    - File contains `## What the follow-up phase ships` section
    - File contains `## Closing this seed` section
    - File ends with `Owner: v1.6+ planning` (or equivalent owner attribution)
  </acceptance_criteria>
  <done>
    SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md planted at `.planning/seeds/`. Phase 20.1 close obligation per D-19 satisfied. The seed mirrors Phase 20's parallel seed and explicitly defers the artist-walk to v1.6+ when a real artist becomes available. The honesty model (never claim clean PASS off author-walk) preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-walker (operator account on flame-01 / portofino) → install-bootstrap.sh + system | The walker has root via sudo on both boxes. The walks deliberately exercise every install branch including system-level mutations (dnf install, postgresql-setup --initdb, pg_hba edit, /etc/systemd/system/* writes, /Library/LaunchDaemons/* writes). |
| HUMAN-UAT.md transcript → planning artifacts | Walks record verbatim output. Risk: command output may contain operator-specific paths (e.g. /Users/cnoellert/...) — those land in the planning tree but are NOT secrets. Real secrets (ANTHROPIC_API_KEY) NEVER appear in HUMAN-UAT transcripts because Step 5 default config leaves it empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-29 | I (Information Disclosure) | Walker pastes ANTHROPIC_API_KEY into transcript | mitigate | Walker discipline #4 (CONTEXT.md): log every author-context dependence including secret values. Walker MUST redact ANTHROPIC_API_KEY in the transcript if they set it. INSTALL.md Step 5 explicitly notes the key is OPTIONAL — most walks won't set it. |
| T-20.1-30 | T (Tampering) | Walker silently routes around a gap and claims PASS | mitigate | Walker discipline #3: NO silent route-around. Phase 20 D-02.1 precedent — author-walks specifically tracked deviations; same model applies. P7 outcome semantics REQUIRE PASS-with-deviations (never claim clean PASS off author-walk). |
| T-20.1-31 | D (Denial of Service) | Walk leaves box in broken state | accept (with rollback documentation) | The script is idempotent; re-running it heals partial-install state. If a walk catastrophically breaks the box (rare — unit-file syntax error not caught by P2 tests), the operator can `systemctl bootout system /Library/LaunchDaemons/...` and `rm /etc/systemd/system/forge-bridge*.service && systemctl daemon-reload` to clear state. Manual cleanup is acceptable for dev-VM walks; production wrenches into Phase 23 (TROUBLESHOOTING.md). |
| T-20.1-32 | R (Repudiation) | Walk transcript not actually verbatim | mitigate | Walker discipline #1+#2: verbatim copy-paste, verbatim transcription. Phase 20 D-02.1 honesty model. Audit risk is low because the walker IS the planner-agent; falsification has no benefit. |
</threat_model>

<verification>
End-of-phase audit (run after both walks PASS-with-deviations):

  ls .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-HUMAN-UAT-*.md
  # → Both files exist (LINUX + MACOS)

  ls .planning/seeds/SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md
  # → Seed planted

  pytest tests/ -x
  # → Full suite green (no regressions from Wave 1-3 changes)

  git log --oneline 20.1
  # → 7 commits visible (one per plan, decoupled per D-23)

Cross-reference all P1-P6 artifacts against the walk transcripts:

  grep -F 'sudo ./scripts/install-bootstrap.sh' .planning/phases/20.1*/20.1-HUMAN-UAT-LINUX.md
  grep -F 'launchctl bootstrap system' .planning/phases/20.1*/20.1-HUMAN-UAT-MACOS.md
  grep -F 'forge doctor' .planning/phases/20.1*/20.1-HUMAN-UAT-*.md
  # → Each transcript actually exercised the artifacts P1-P6 shipped
</verification>

<success_criteria>
1. `20.1-HUMAN-UAT-LINUX.md` exists; flame-01 fresh-VM walk PASS-with-deviations or auto-fixed in-flight (D-20).
2. `20.1-HUMAN-UAT-MACOS.md` exists; portofino fresh-state walk PASS-with-deviations or auto-fixed in-flight.
3. Both walks reached all reachable surfaces (Linux: 5/5; macOS: 4/5 — Flame hook auto-skipped).
4. `forge doctor` reports PASS or PARTIAL on both boxes with daemon_state row visible (D-12 + P5 wire-in confirmed).
5. Reference versions captured per D-22 in both transcripts.
6. SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md planted (D-19 close obligation).
7. Any in-flight gap-fix landed as 20.1-08+ before phase close (D-20).
8. Both walks honor the walker discipline: verbatim copy-paste, verbatim transcription, no silent route-around (D-02.1 / D-17).
9. Phase 20.1 closes PASS-with-deviations per D-19 (NOT clean PASS — author walks cannot earn that bar).
10. Final commit per D-23: planning artifacts only (HUMAN-UAT.md × 2 + seed).
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-07-SUMMARY.md` documenting:
- Both walks completed; outcome (PASS-with-deviations / FAIL).
- Reference versions table (D-22) for flame-01 (Linux) AND portofino (macOS).
- Gap log summary across both walks; disposition for each.
- In-flight fixes that landed (if any: 20.1-08+ plans).
- SEED-PHASE-20.1-ARTIST-UAT-V1.6+.md plant confirmation.
- Phase 20.1 close declaration: PASS-with-deviations.
- v1.5 milestone unblocked — Phase 20.1 ship-blocker gate cleared.
- Outstanding work: Phase 21 / 22 / 23 follow-on per ROADMAP.md.
</output>
