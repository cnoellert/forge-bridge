---
phase: 20.1
plan: 06
type: execute
wave: 5
depends_on: [20.1-04, 20.1-05]
files_modified:
  - docs/INSTALL.md
autonomous: true
requirements: [INSTALL-01, INSTALL-03, INSTALL-04]
tags: [docs, install, reshape]

must_haves:
  truths:
    - "docs/INSTALL.md Step 3 collapses to a single bootstrap-script invocation (`sudo ./scripts/install-bootstrap.sh`) — no manual psql / pg_hba / createuser / alembic ceremonies"
    - "docs/INSTALL.md Step 5 collapses to env-file edit (`/etc/forge-bridge/forge-bridge.env`) + restart command (systemctl / launchctl) — no `export FOO=bar` ephemeral shell-state guidance"
    - "docs/INSTALL.md Step 6 NO LONGER claims `python -m forge_bridge` boots all four surfaces in one shot (Phase 20 gap #11 doc lie removed); instead documents the two-daemon model and `systemctl status` / `launchctl print` verification commands"
    - "docs/INSTALL.md Steps 1, 2, 4, 7, 8 survive verbatim from Phase 20 close (UAT-validated; reshape scoped only to Steps 3, 5, 6 per CONTEXT.md line 113)"
    - "README.md install section continues to point unambiguously at docs/INSTALL.md (INSTALL-03 — README ↔ INSTALL.md consistency; Phase 20-01 D-17 grep-zero regression guard preserved)"
    - "INSTALL.md documents stop / restart / logs commands for both Linux (journalctl, systemctl restart) and macOS (tail -f /var/log/forge-bridge/*.log, launchctl kickstart -k)"
  artifacts:
    - path: "docs/INSTALL.md"
      provides: "Operator-facing canonical install narrative, reshaped to bootstrap-script + daemon model"
      contains: "sudo ./scripts/install-bootstrap.sh, /etc/forge-bridge/forge-bridge.env, sudo systemctl restart forge-bridge, sudo launchctl kickstart -k system/com.cnoellert.forge-bridge, journalctl -u forge-bridge -f, tail -f /var/log/forge-bridge/console.log, sudo systemctl status forge-bridge-server, sudo launchctl print system/com.cnoellert.forge-bridge"
      min_lines: 280
  key_links:
    - from: "docs/INSTALL.md (Step 3)"
      to: "scripts/install-bootstrap.sh"
      via: "the doc's imperative half is the script; doc describes, script does"
      pattern: "sudo \\./scripts/install-bootstrap\\.sh"
    - from: "docs/INSTALL.md (Step 5)"
      to: "packaging/forge-bridge.env.example → /etc/forge-bridge/forge-bridge.env"
      via: "doc points operator at /etc/ env file; bootstrap script installed it (P4)"
      pattern: "/etc/forge-bridge/forge-bridge\\.env"
---

<objective>
Reshape `docs/INSTALL.md` to match the daemon model that P1-P5 shipped. Collapse Steps 3, 5, 6 from "manual psql + manual exports + python -m forge_bridge with a tail keepalive lie" to "run the bootstrap script + edit the env file + check systemctl/launchctl status." Closes Phase 20 gap #11 (the substantive doc lie about single-process launch) and updates the operator-facing narrative to match shipped reality.

Purpose: P4 ships install-bootstrap.sh as the imperative half. Without an INSTALL.md that points operators at it, the script is invisible. The Phase 20 forcing function ("if INSTALL.md doesn't work end-to-end on a clean machine, we don't ship it") applies double-strength here: the reshape must walk cleanly on flame-01 (Linux) and portofino (macOS) per D-17 / D-18 in P7.

Output: Updated `docs/INSTALL.md`. Steps 1, 2, 4, 7, 8 untouched (Phase 20 close shipped them; Track A walk validated). Steps 3, 5, 6 reshaped per CONTEXT.md "Artifacts to refresh / extend" line 113 + PATTERNS.md § "docs/INSTALL.md MODIFIED" lines 714-796.

Per D-23 decoupled-commit purity: doc reshape ships as a SEPARATE commit from P4's script wiring. Phase 17 D-30 precedent — `git blame` on a future bump line should show "INSTALL.md reshape", not "INSTALL.md reshape + script wiring + ...". This is the doc-only commit.

Per CONTEXT.md "Out of scope" line 22: Phase 20.1 reshape is INSTALL.md only. `docs/GETTING-STARTED.md` (Phase 21), `docs/RECIPES.md` (Phase 22), `docs/TROUBLESHOOTING.md` (Phase 23) are out of scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-CONTEXT.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-RESEARCH.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-04-SUMMARY.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-05-SUMMARY.md
@.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md
@docs/INSTALL.md

<interfaces>
<!-- Phase 20 INSTALL.md voice + heading style — preserve exactly. Source: PATTERNS.md lines 717-732 -->
- Heading style: `## Step N: <Verb> <object>` (e.g. "## Step 3: Set up Postgres" → "## Step 3: Run the bootstrap script")
- Sub-heading: `### Na. <action phrase>` (within steps)
- Imperative voice
- Code blocks specify language: ` ```bash `, ` ```ini `, etc. — never bare ` ``` `
- Verification commands include EXPECTED OUTPUT inline as comment after `→` symbol
- Bold-emphasis for operative keys (`**mandatory**`, `**Skip this step if Track B / MCP-only**`)
- Three-dash separator (`---`) between steps
- "Note on X" callouts for edge cases

<!-- The Phase 20 doc-lie at line 234 (verified by Read in this plan's context-gathering): -->
"The single `python -m forge_bridge` process boots **all four hosted surfaces in one shot** —
 MCP server (stdio), Artist Console Web UI (`:9996`), chat endpoint (`:9996/api/v1/chat`),
 and the WebSocket event server (`:9998`)."
<!-- This is FALSE — `python -m forge_bridge` is a CLIENT of `:9998`. The bus runs as a SEPARATE
     process via `python -m forge_bridge.server`. Phase 20.1 deletes this line. -->

<!-- Step boundaries verified via grep (head of each section): -->
Line 62:  "## Step 1: Prepare the conda environment"     ← KEEP verbatim
Line 79:  "## Step 2: Install forge-bridge"              ← KEEP verbatim
Line 105: "## Step 3: Set up Postgres"                   ← RESHAPE (collapse to bootstrap script)
Line 166: "## Step 4: Install the Flame hook"            ← KEEP verbatim
Line 193: "## Step 5: Configure environment variables"   ← RESHAPE (env file + restart)
Line 232: "## Step 6: Start the server"                  ← RESHAPE (remove lie; systemctl/launchctl)
Line 261: "## Step 7: Verify all five surfaces"          ← KEEP verbatim
Line 292: "## Step 8: Post-install diagnostic"           ← KEEP verbatim (Step 8 references `forge doctor`; P5 adds daemon_state row to that table — no doc edit needed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape Step 3 — collapse to bootstrap script invocation</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - docs/INSTALL.md (FULL FILE — confirm current Step 3 boundaries: line 105 → line 163 / before Step 4 at line 166)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md (§ "Reshape target — Step 3", lines 731-743)
    - .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md (gaps #1-#6 — every Step 3 gap that this reshape closes)
    - scripts/install-bootstrap.sh (the script the new Step 3 points at; confirm `--help` and flag matrix exist before referencing them in the doc)
  </read_first>
  <action>
Replace the entire Step 3 section in docs/INSTALL.md with the new bootstrap-script narrative. Preserve the section's `---` separators (the one before line 105 and the one before line 166 stay; only the content between them changes).

NEW Step 3 body (replaces existing lines 105-163):

  ## Step 3: Run the bootstrap script

  The bootstrap script handles Postgres setup, env-file install, systemd unit registration (Linux) or launchd plist registration (macOS), and the post-install `forge doctor` verification — all in one shot. Closes the Phase 20 install gaps that demanded sysadmin-level Postgres + pg_hba + service-management knowledge from operators who shouldn't need it.

  **Skip this step if you are running Track B / MCP-only AND have an existing remote Postgres** — instead use the `--no-postgres` flag below.

  ### 3a. Run the script

  ```bash
  sudo ./scripts/install-bootstrap.sh
  ```

  The script auto-detects your OS (Rocky/RHEL Linux or macOS Darwin) and runs the right install path. It is **idempotent** — running it twice in a row is a no-op on the second run (operator env-file edits are preserved).

  ### 3b. Optional flags

  Pass any combination of these flags for partial-install scenarios:

  | Flag | Effect |
  |---|---|
  | `--track-b` | Skip Flame hook install (Track B / MCP-only deploy) |
  | `--no-postgres` | Skip Postgres bootstrap entirely (use existing remote DB) |
  | `--mcp-only` | `--track-b` + `--no-postgres` + skip Console daemon (BUS+MCP only) |
  | `--with-flame-mac` | macOS only — opt INTO Flame hook install (default skips on macOS) |
  | `--non-interactive` | Skip the FORGE_LOCAL_LLM_URL prompt; use defaults |

  Run `sudo ./scripts/install-bootstrap.sh --help` for the live flag matrix.

  ### 3c. What the script does

  On a fresh Rocky/RHEL machine:
  - Installs `postgresql-server` + `postgresql-contrib` if missing
  - Initializes the cluster via `postgresql-setup --initdb` if needed
  - Probes the cluster's `password_encryption` (md5 vs scram-sha-256) and aligns `/var/lib/pgsql/data/pg_hba.conf` to match
  - Creates the `forge` Postgres role + `forge_bridge` database (idempotent; default password `'forge'` — local-only, see Step 5 for hardening)
  - Runs `alembic upgrade head` against the new database
  - Installs `/etc/forge-bridge/forge-bridge.env` from the in-tree template (mode `0640 root:$YOUR_USER`)
  - Copies `packaging/systemd/*.service` → `/etc/systemd/system/`, runs `systemctl daemon-reload`, enables and starts both units
  - Auto-runs `forge-bridge console doctor` as the install-success gate

  On macOS:
  - Detects existing Postgres (`psql` on PATH, or Homebrew `postgresql@16`); installs Homebrew variant if neither present
  - Skips `pg_hba` alignment (Homebrew Postgres ships `trust` for localhost)
  - Same `forge` role + `forge_bridge` database creation + alembic
  - Auto-applies `--track-b` (skips Flame hook); pass `--with-flame-mac` to override
  - Creates `/var/log/forge-bridge/` (mode `755 root:wheel`) for daemon log files
  - Copies `packaging/launchd/*.plist` → `/Library/LaunchDaemons/`, copies wrappers → `/usr/local/bin/`
  - Runs `launchctl bootstrap system /Library/LaunchDaemons/com.cnoellert.forge-bridge*.plist` (modern syntax; replaces deprecated `launchctl load`)
  - Same auto-doctor verification

  ### 3d. Custom Postgres credentials

  If your Postgres role/DB is NOT the default `forge:forge@localhost:5432/forge_bridge` — for example on a shared development cluster — pass `--no-postgres` to skip the Postgres bootstrap and edit `/etc/forge-bridge/forge-bridge.env` to point `FORGE_DB_URL` at your cluster. Then run alembic manually:

  ```bash
  alembic -x url=postgresql+psycopg2://USER:PASS@HOST:PORT/DB upgrade head
  ```

  (The `+psycopg2` driver is required because Alembic's sync engine doesn't support `+asyncpg`. forge-bridge runtime uses `+asyncpg` per `FORGE_DB_URL` in the env file.)

CRITICAL — preserve the `---` separators on either side of Step 3. Steps 2 (above) and 4 (below) are not modified. Use `bash` language hint on every code block. Keep `**Skip this step if Track B**` style operative-key callout from the Phase 20 voice.
  </action>
  <verify>
    <automated>grep -F 'sudo ./scripts/install-bootstrap.sh' docs/INSTALL.md && grep -F '## Step 3: Run the bootstrap script' docs/INSTALL.md && grep -F -- '--track-b' docs/INSTALL.md && grep -F -- '--no-postgres' docs/INSTALL.md && grep -F -- '--mcp-only' docs/INSTALL.md && grep -F -- '--with-flame-mac' docs/INSTALL.md && grep -F -- '--non-interactive' docs/INSTALL.md && grep -F 'install-bootstrap.sh --help' docs/INSTALL.md && grep -F '/etc/forge-bridge/forge-bridge.env' docs/INSTALL.md && grep -F 'forge-bridge console doctor' docs/INSTALL.md && grep -F '## Step 1: Prepare the conda environment' docs/INSTALL.md && grep -F '## Step 2: Install forge-bridge' docs/INSTALL.md && grep -F '## Step 4: Install the Flame hook' docs/INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Step 3: Set up Postgres` heading replaced with `## Step 3: Run the bootstrap script` (test: `! grep -F '## Step 3: Set up Postgres' docs/INSTALL.md && grep -F '## Step 3: Run the bootstrap script' docs/INSTALL.md`)
    - Step 3 contains the literal `sudo ./scripts/install-bootstrap.sh` command
    - Step 3 references all 5 flags from D-07 (literal grep on each: `--track-b`, `--no-postgres`, `--mcp-only`, `--with-flame-mac`, `--non-interactive`)
    - Step 3 references `--help` for the live flag matrix
    - Step 3 mentions the env-file path `/etc/forge-bridge/forge-bridge.env`
    - Step 3 mentions `forge-bridge console doctor` (the auto-run verification)
    - Step 3 mentions Linux-specific commands (`postgresql-setup --initdb`, `alembic upgrade head`)
    - Step 3 mentions macOS-specific commands (`/Library/LaunchDaemons/`, `launchctl bootstrap system`)
    - Step 3 documents the custom-credentials path (`alembic -x url=...`) for non-default Postgres setups
    - Steps 1, 2, 4, 7, 8 headings UNCHANGED (test: `grep -F '## Step 1: Prepare the conda environment'`, `grep -F '## Step 2: Install forge-bridge'`, `grep -F '## Step 4: Install the Flame hook'`, `grep -F '## Step 7: Verify all five surfaces'`, `grep -F '## Step 8: Post-install diagnostic'` all match)
    - Step 3 does NOT contain `createuser -P` (the fragile interactive prompt the script replaces)
    - Step 3 does NOT contain `sudo -u postgres createuser` (gap #2 — the cwd warning issue)
  </acceptance_criteria>
  <done>
    Step 3 collapsed to bootstrap-script invocation + flag matrix + what-it-does narrative + custom-credentials addendum. Closes Phase 20 gaps #1-#6 in the doc surface (the script closes them in behavior; the doc points the operator at the script). Steps 1, 2, 4 still intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reshape Step 5 — collapse to env-file edit + restart command</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - docs/INSTALL.md (CURRENT Step 5: lines 193-229 — the `export FOO=bar` + Track B carveout)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md (§ "Reshape target — Step 5" lines 745-758)
    - packaging/forge-bridge.env.example (the env-file template — confirm the section comments are operator-actionable)
    - .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md (gaps #7, #8 — env persistence + conda re-init)
  </read_first>
  <action>
Replace the existing Step 5 section. Preserve the `---` separators on either side.

NEW Step 5 body (replaces existing lines 193-229):

  ## Step 5: Configure environment

  The bootstrap script (Step 3) installed `/etc/forge-bridge/forge-bridge.env` (mode `0640 root:$YOUR_USER`) — both daemons read it on start (systemd via `EnvironmentFile=`; macOS via the wrapper-script `set -a; . FILE; set +a` pattern). It is the single source of truth for forge-bridge configuration.

  Closes Phase 20 gaps #7 (env persistence — the env file IS the persistence path) and #8 (conda re-init — daemons run with absolute python paths, no shell sourcing).

  ### 5a. Edit the env file (optional)

  All defaults work for single-machine local-Ollama setups. Edit only if you need to change them — most operators set `FORGE_LOCAL_LLM_URL` once during the bootstrap script's interactive prompt and never touch the file again.

  ```bash
  sudo $EDITOR /etc/forge-bridge/forge-bridge.env
  ```

  Common changes:
  - `FORGE_LOCAL_LLM_URL` — point at your separate Ollama host (e.g. `http://192.168.86.15:11434/v1`)
  - `ANTHROPIC_API_KEY` — paste your Anthropic key if you need cloud-LLM routing (OPTIONAL — chat hardcodes `sensitive=True` and routes through local Ollama; cloud is NOT the daily operator workflow)

  ### 5b. Reload after editing

  After edits, restart the daemons so they pick up the new values:

  **Linux:**
  ```bash
  sudo systemctl restart forge-bridge
  ```

  **macOS:**
  ```bash
  sudo launchctl kickstart -k system/com.cnoellert.forge-bridge
  ```

  Both commands are idempotent — restart is safe even if no edits were made.

  ### 5c. About the `forge:forge` Postgres password

  The default `FORGE_DB_URL=postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge` ships with the literal password `'forge'` because the cluster is local-only on a single-operator workstation and forge-bridge has no auth surface at v1.4.x (caller-identity migration is `SEED-AUTH-V1.5`, deferred to v1.6+). If you later expose the cluster beyond localhost, rotate the role password via `psql -c "ALTER USER forge WITH PASSWORD '...';"` and update `FORGE_DB_URL` accordingly.

CRITICAL: do NOT add new headings or steps; this is a reshape of the existing Step 5 only. The 3-step sub-structure (5a / 5b / 5c) replaces the previous step's content but stays inside Step 5's boundaries.
  </action>
  <verify>
    <automated>grep -F '## Step 5: Configure environment' docs/INSTALL.md && grep -F '/etc/forge-bridge/forge-bridge.env' docs/INSTALL.md && grep -F 'sudo systemctl restart forge-bridge' docs/INSTALL.md && grep -F 'sudo launchctl kickstart -k system/com.cnoellert.forge-bridge' docs/INSTALL.md && grep -F 'FORGE_LOCAL_LLM_URL' docs/INSTALL.md && grep -F 'ANTHROPIC_API_KEY' docs/INSTALL.md && grep -F 'SEED-AUTH-V1.5' docs/INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Step 5: Configure environment` heading present (test: `grep -F '## Step 5: Configure environment' docs/INSTALL.md`)
    - Step 5 references the env-file path `/etc/forge-bridge/forge-bridge.env`
    - Step 5 contains exact Linux restart command `sudo systemctl restart forge-bridge`
    - Step 5 contains exact macOS reload command `sudo launchctl kickstart -k system/com.cnoellert.forge-bridge`
    - Step 5 mentions `FORGE_LOCAL_LLM_URL` AND `ANTHROPIC_API_KEY` (the two operator-tunable values)
    - Step 5 mentions `SEED-AUTH-V1.5` for password-rotation context
    - Step 5 does NOT contain `export FORGE_` (the gap #7 ephemeral-export pattern is removed)
    - Step 5 does NOT contain `~/.bashrc` (the gap #8 conda-re-init pitfall — the daemon model bypasses shell sourcing entirely)
  </acceptance_criteria>
  <done>
    Step 5 collapsed to env-file edit + Linux/macOS restart commands. Closes Phase 20 gaps #7 and #8 in the doc surface. The 5c addendum honestly describes the `forge:forge` local-only credential and points at SEED-AUTH-V1.5 for the v1.6+ hardening path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reshape Step 6 — remove the gap #11 doc lie; document daemon verification + stop/restart/logs</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - docs/INSTALL.md (CURRENT Step 6: lines 232-257 — contains the `python -m forge_bridge` "all four surfaces" lie at line 234)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md (§ "Reshape target — Step 6" lines 759-791)
    - .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md (gap #11 — the architectural driver; gap #12 — "how do I stop the server?")
    - packaging/systemd/forge-bridge.service (P2 — confirm `Requires=forge-bridge-server.service` matches the doc's two-process narrative)
  </read_first>
  <action>
Replace the existing Step 6 section in its entirety. The line containing `boots all four hosted surfaces in one shot` MUST be deleted. Preserve the `---` separators on either side.

NEW Step 6 body (replaces existing lines 232-257):

  ## Step 6: Verify the daemons are running

  The bootstrap script (Step 3) registered TWO services on your machine and started them in the right order. forge-bridge is a two-process system:

  - **`forge-bridge-server`** — the WebSocket bus on `:9998` (the canonical event-driven backplane; runs `python -m forge_bridge.server`)
  - **`forge-bridge`** — the MCP server + Artist Console + chat endpoint on `:9996` (runs `python -m forge_bridge`); depends on the bus

  Closes Phase 20 gap #11 — earlier versions of this doc claimed `python -m forge_bridge` boots all surfaces in one shot. That was incorrect; the bus is a separate process and must start first. systemd `Requires=` (Linux) and a wrapper-script `:9998` readiness gate (macOS) handle the ordering invisibly now.

  ### 6a. Linux — `systemctl`

  ```bash
  sudo systemctl status forge-bridge-server   # WS bus on :9998
  sudo systemctl status forge-bridge          # MCP + Console on :9996
  ```

  Live logs:

  ```bash
  sudo journalctl -u forge-bridge -f          # MCP + Console
  sudo journalctl -u forge-bridge-server -f   # bus
  ```

  Stop / restart / disable:

  ```bash
  sudo systemctl restart forge-bridge                 # restarts MCP+Console (bus stays up)
  sudo systemctl restart forge-bridge-server          # restarts bus (Console will follow due to Requires=)
  sudo systemctl stop forge-bridge forge-bridge-server   # full stop
  sudo systemctl disable forge-bridge forge-bridge-server   # do not start at next boot
  ```

  ### 6b. macOS — `launchctl`

  ```bash
  sudo launchctl print system/com.cnoellert.forge-bridge-server
  sudo launchctl print system/com.cnoellert.forge-bridge
  ```

  Live logs (macOS plists write to file, not stdout — `tail -f` directly):

  ```bash
  tail -f /var/log/forge-bridge/console.log
  tail -f /var/log/forge-bridge/server.log
  ```

  Stop / restart:

  ```bash
  sudo launchctl kickstart -k system/com.cnoellert.forge-bridge   # restart MCP+Console
  sudo launchctl bootout system /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist           # stop one daemon
  sudo launchctl bootout system /Library/LaunchDaemons/com.cnoellert.forge-bridge-server.plist    # stop bus too
  ```

  ### 6c. About the lifespan model

  Both daemons are managed by the OS init system — systemd on Linux, launchd on macOS. Process supervision (start at boot, restart on crash, journal logging) comes for free. There is no `tail -f /dev/null | python -m forge_bridge` keepalive ceremony anymore — the daemons run as supervised services with `StandardInput=null` (Linux) or detached stdin (macOS), so the FastMCP stdio handshake-exit issue from earlier versions is solved at the unit level.

CRITICAL — the line:
  "The single `python -m forge_bridge` process boots **all four hosted surfaces in one shot**..."
MUST be deleted from the doc. Verify post-write with: `! grep -F 'all four hosted surfaces in one shot' docs/INSTALL.md`.
  </action>
  <verify>
    <automated>grep -F '## Step 6: Verify the daemons are running' docs/INSTALL.md && ! grep -F 'all four hosted surfaces in one shot' docs/INSTALL.md && grep -F 'sudo systemctl status forge-bridge-server' docs/INSTALL.md && grep -F 'sudo systemctl status forge-bridge' docs/INSTALL.md && grep -F 'sudo journalctl -u forge-bridge -f' docs/INSTALL.md && grep -F 'sudo journalctl -u forge-bridge-server -f' docs/INSTALL.md && grep -F 'sudo launchctl print system/com.cnoellert.forge-bridge-server' docs/INSTALL.md && grep -F 'sudo launchctl print system/com.cnoellert.forge-bridge' docs/INSTALL.md && grep -F 'tail -f /var/log/forge-bridge/console.log' docs/INSTALL.md && grep -F 'tail -f /var/log/forge-bridge/server.log' docs/INSTALL.md && grep -F 'sudo launchctl kickstart -k system/com.cnoellert.forge-bridge' docs/INSTALL.md && ! grep -F 'tail -f /dev/null | python -m forge_bridge' docs/INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Step 6: Verify the daemons are running` heading replaces `## Step 6: Start the server` (test: `! grep -F '## Step 6: Start the server' docs/INSTALL.md && grep -F '## Step 6: Verify the daemons are running' docs/INSTALL.md`)
    - The literal Phase 20 doc-lie string `all four hosted surfaces in one shot` does NOT appear anywhere in docs/INSTALL.md (test: `! grep -F 'all four hosted surfaces in one shot' docs/INSTALL.md`)
    - The literal `tail -f /dev/null | python -m forge_bridge` keepalive prose does NOT appear (test: `! grep -F 'tail -f /dev/null | python -m forge_bridge' docs/INSTALL.md`)
    - Step 6 contains both Linux daemon-status commands (`systemctl status forge-bridge-server` AND `systemctl status forge-bridge`)
    - Step 6 contains both macOS daemon-status commands (`launchctl print system/com.cnoellert.forge-bridge-server` AND `launchctl print system/com.cnoellert.forge-bridge`)
    - Step 6 contains both Linux journalctl tail commands (`-u forge-bridge -f` AND `-u forge-bridge-server -f`)
    - Step 6 contains both macOS log-tail commands (`tail -f /var/log/forge-bridge/console.log` AND `tail -f /var/log/forge-bridge/server.log`)
    - Step 6 contains macOS reload command `sudo launchctl kickstart -k system/com.cnoellert.forge-bridge` (D-12 / Phase 20 gap #12 fix)
    - Step 6 contains Linux full-stop command `sudo systemctl stop forge-bridge forge-bridge-server` (Phase 20 gap #12 fix)
    - Steps 7 and 8 headings UNCHANGED (test: `grep -F '## Step 7: Verify all five surfaces' && grep -F '## Step 8: Post-install diagnostic'`)
  </acceptance_criteria>
  <done>
    Step 6 reshaped to two-daemon-verification narrative. Phase 20 gap #11 (the doc lie) deleted. Phase 20 gap #12 (no clean stop semantics) closed by full systemctl/launchctl coverage. The keepalive ceremony (`tail -f /dev/null | ...`) is gone — daemons handle stdin via unit/plist directives. Step 6c addendum explains the OS-init-system architectural choice.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → docs/INSTALL.md (read-only) | Doc is read-only narrative. No secrets, no config, no executable code. The threat surface is "operator follows wrong instructions and breaks their box" — Phase 20 gap #11 was exactly this category. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-26 | T (Tampering — operator misconfiguration via doc lie) | docs/INSTALL.md Step 6 "all four surfaces" lie | mitigate | Lie deleted in Task 3. Acceptance test enforces `! grep -F 'all four hosted surfaces in one shot' docs/INSTALL.md`. P7 HUMAN-UAT walks (flame-01 + portofino) re-validate the reshape. |
| T-20.1-27 | I (Information Disclosure — credential rotation guidance) | Step 5 documents `forge:forge` default password | accept (with documentation) | Step 5c explicitly addresses the local-only-credential reality + points at SEED-AUTH-V1.5 for v1.6+ hardening. Operators who later expose the cluster have a documented rotation path. |
| T-20.1-28 | T (Tampering — INSTALL.md ↔ README drift) | INSTALL-03 README ↔ INSTALL.md consistency | mitigate (already enforced) | Phase 20-01 D-17 grep-zero regression guard already in place. Step boundaries (Steps 1, 2, 4, 7, 8) preserved verbatim — no version drift introduced. |

(Doc-only plans have minimal threat surface; reasoning included for completeness per phase security_enforcement.)
</threat_model>

<verification>
Quick sanity:
  grep -c '^## Step' docs/INSTALL.md     # → 8 (Steps 1-8 all present)
  ! grep -F 'all four hosted surfaces in one shot' docs/INSTALL.md   # → exit 0 (lie deleted)
  grep -F 'sudo ./scripts/install-bootstrap.sh' docs/INSTALL.md     # → match (Step 3 references the script)

Cross-reference with shipped artifacts:
  grep -F '/etc/forge-bridge/forge-bridge.env' docs/INSTALL.md      # → matches; consistent with packaging/forge-bridge.env.example install target
  grep -F '/Library/LaunchDaemons/' docs/INSTALL.md                  # → matches; consistent with packaging/launchd/ install target
  grep -F 'forge-bridge console doctor' docs/INSTALL.md              # → matches; consistent with P5 daemon_state sub-check

Full pytest suite:
  pytest tests/ -x
  # → All existing tests still green; no test deltas (this is a doc-only plan).
</verification>

<success_criteria>
1. Steps 3, 5, 6 reshaped per PATTERNS.md targets.
2. Phase 20 gap #11 doc lie deleted (regression-tested via `! grep`).
3. Phase 20 gap #12 (stop/restart/logs) addressed in Step 6 for both Linux and macOS.
4. Steps 1, 2, 4, 7, 8 headings unchanged from Phase 20 close.
5. README ↔ INSTALL.md consistency preserved (no version drift; Step boundaries intact).
6. New Step 5c addendum documents the `forge:forge` local-only credential + SEED-AUTH-V1.5 v1.6+ hardening path.
7. Cross-references with P1 (env file), P4 (script), P5 (doctor) all consistent.
8. Full pytest suite remains green (doc-only; no code change).
9. Single git commit per D-23 (doc reshape only; no script wiring; no doctor sub-check).
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-06-SUMMARY.md` documenting:
- Steps 3, 5, 6 reshaped; Steps 1, 2, 4, 7, 8 unchanged.
- Phase 20 gap #11 (the doc lie) deleted; gap #12 (stop/restart) addressed.
- INSTALL-03 README ↔ INSTALL.md consistency preserved.
- Doc now matches shipped reality (P1-P5 artifacts).
- Foundation laid for P7 (HUMAN-UAT walks on flame-01 + portofino).
- Reading INSTALL.md end-to-end now describes the daemon model honestly — the install procedure IS a script + a daemon, not a wall of imperatives.
</output>
