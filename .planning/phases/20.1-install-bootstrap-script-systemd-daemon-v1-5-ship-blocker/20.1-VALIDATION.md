---
phase: 20.1
slug: install-bootstrap-script-systemd-daemon-v1-5-ship-blocker
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-01
---

# Phase 20.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `20.1-RESEARCH.md` § Validation Architecture (line 1166).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing — `tests/` already in tree) |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `pytest tests/test_packaging.py tests/cli/test_doctor_daemon_state.py -x` |
| **Full suite command** | `pytest tests/ -x` |
| **Estimated runtime** | ~30s for quick; ~3-5min for full |

Plus shell-syntax checks (planner integrates into pre-commit / CI surface):
- `bash -n scripts/install-bootstrap.sh`
- `plutil -lint packaging/launchd/*.plist`
- `systemd-analyze verify packaging/systemd/*.service` (best-effort — only fires on hosts with systemd)

---

## Sampling Rate

- **After every task commit:** Run quick command (subset that covers the modified file)
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite green + both HUMAN-UAT walks (Linux flame-01 + macOS portofino) clean per D-18
- **Max feedback latency:** 30 seconds for the quick subset

---

## Per-Task Verification Map

> Filled in during `/gsd-execute-phase` per-plan task expansion. Templates below; planner sets per-plan rows.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 20.1-P2-01 | 02 | 1 | INSTALL-02 | — | systemd unit valid + parses | unit | `systemd-analyze verify packaging/systemd/forge-bridge.service` | ❌ W0 | ⬜ pending |
| 20.1-P2-02 | 02 | 1 | INSTALL-02 | — | bus unit valid + parses | unit | `systemd-analyze verify packaging/systemd/forge-bridge-server.service` | ❌ W0 | ⬜ pending |
| 20.1-P3-01 | 03 | 1 | INSTALL-02 | — | launchd plists lint clean | unit | `plutil -lint packaging/launchd/com.cnoellert.forge-bridge.plist` | ❌ W0 | ⬜ pending |
| 20.1-P3-02 | 03 | 1 | INSTALL-02 | — | launchd bus plist lint clean | unit | `plutil -lint packaging/launchd/com.cnoellert.forge-bridge-server.plist` | ❌ W0 | ⬜ pending |
| 20.1-P4-01 | 04 | 2 | INSTALL-01 | — | install-bootstrap.sh syntax-clean | unit | `bash -n scripts/install-bootstrap.sh` | ❌ W0 | ⬜ pending |
| 20.1-P5-01 | 05 | 2 | INSTALL-04 | — | doctor daemon_state row appears Linux | unit | `pytest tests/cli/test_doctor_daemon_state.py::test_linux_active -x` | ❌ W0 | ⬜ pending |
| 20.1-P5-02 | 05 | 2 | INSTALL-04 | — | doctor daemon_state row appears macOS | unit | `pytest tests/cli/test_doctor_daemon_state.py::test_macos_active -x` | ❌ W0 | ⬜ pending |
| 20.1-P5-03 | 05 | 2 | INSTALL-04 | — | doctor daemon_state row OS-branch fallback | unit | `pytest tests/cli/test_doctor_daemon_state.py::test_other_os -x` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_packaging.py` — new file. Holds the 4 packaging-unit-validation tests:
  - systemd-unit syntax check (skip if `systemd-analyze` not on PATH — i.e. macOS dev box)
  - launchd plist lint check (skip if `plutil` not on PATH — i.e. Linux dev box)
  - bash syntax check on `scripts/install-bootstrap.sh`
  - env-file template parses with `set -a; . FILE; set +a` (skip if file missing)
- [ ] `tests/cli/test_doctor_daemon_state.py` — new file. Holds 3 doctor sub-check pytest cases per RESEARCH.md § Validation Architecture. Mocks `subprocess.run` for both `systemctl is-active` (Linux) and `launchctl print system/...` (macOS) outcomes. Covers: active-active, inactive-active, both-inactive, OS-other fallback.
- [ ] No new framework install — pytest already in tree.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end fresh-VM Linux walk on flame-01 (Rocky/RHEL) | INSTALL-01..04 | Requires throwaway VM with no prior conda / no prior Postgres / no prior shell history per D-17. Cannot run in CI. | Per Phase 20 D-02.1 walker discipline: copy-paste verbatim, transcribe verbatim. Produces `20.1-HUMAN-UAT-LINUX.md`. Gate per D-18. |
| End-to-end fresh-state macOS walk on portofino | INSTALL-01..04 | Requires throwaway state on physical Mac (portofino). Cannot run in CI. Validates launchd plists + Homebrew Postgres branch + auto-`--track-b`. | Same walker discipline. Produces `20.1-HUMAN-UAT-MACOS.md`. Gate per D-18. |
| `forge-bridge console doctor` → PASS or PARTIAL on the operator's machine after install | INSTALL-04 | Doctor is the auto-chained final-step verification per D-12 — verified inside the manual walks above. | Both walks include doctor table inspection. Operator screenshots / pastes output. |
| Daemon state survives reboot (systemd `enable --now` / launchd `bootstrap system`) | INSTALL-02 | Requires actual reboot of the test box. | Walks include reboot step + `systemctl is-active` / `launchctl print` on cold boot. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (P7 walks are manual gates, not feedback samples)
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s for quick / < 5min for full
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
