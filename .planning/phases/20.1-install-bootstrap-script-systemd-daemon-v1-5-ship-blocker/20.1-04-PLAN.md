---
phase: 20.1
plan: 04
type: execute
wave: 2
depends_on: [20.1-01, 20.1-02, 20.1-03]
files_modified:
  - scripts/install-bootstrap.sh
  - tests/test_packaging.py
autonomous: true
requirements: [INSTALL-01, INSTALL-04]
tags: [install, script, postgres, systemd, launchd, imperative]

must_haves:
  truths:
    - "scripts/install-bootstrap.sh exists, is executable, and passes `bash -n` syntax check"
    - "Script asserts running as root with $SUDO_USER set, validates $SUDO_USER matches `^[a-z][a-z0-9_-]*$`, and detects OS (Rocky/RHEL Linux or Darwin/macOS)"
    - "Script supports the 5 locked flags from D-07: --track-b, --no-postgres, --mcp-only, --with-flame-mac, --non-interactive (all five must work; no planted-but-unimplemented paths)"
    - "On Linux: script bootstraps Postgres (dnf install if missing → postgresql-setup --initdb if not initialized → enable+start → pg_isready loop → probe password_encryption → align pg_hba.conf idempotently → create role+db idempotently → alembic upgrade head)"
    - "On macOS: script detects Homebrew + postgresql@16 OR Postgres.app; brew installs postgresql@16 if neither present (D-04 rare branch); auto-applies --track-b unless --with-flame-mac; mkdir -p /var/log/forge-bridge BEFORE launchctl bootstrap (Pitfall 2 fix)"
    - "Script copies packaging/forge-bridge.env.example → /etc/forge-bridge/forge-bridge.env (only if absent; preserves operator edits on re-run); chmod 0640, chown root:$SUDO_USER (Pitfall 3 fix)"
    - "Script substitutes __SUDO_USER__ placeholders in every copied unit/plist/wrapper via the single substitute_placeholders helper (Pitfall 5 fix)"
    - "Linux: copies packaging/systemd/*.service → /etc/systemd/system/, daemon-reload, enable+start; macOS: copies packaging/launchd/*.plist → /Library/LaunchDaemons/, copies wrappers → /usr/local/bin/, launchctl bootstrap"
    - "Script auto-runs `forge-bridge console doctor` at end (D-12); exits 0 on PASS or PARTIAL, exits 1 on FAIL"
    - "Re-running the script is a no-op (idempotency contract per D-21 + RESEARCH.md Open Question 3); operator env-file edits preserved across re-runs"
  artifacts:
    - path: "scripts/install-bootstrap.sh"
      provides: "Imperative bootstrap that turns a fresh machine into a working forge-bridge install"
      contains: "set -euo pipefail, [forge-bridge] log prefix, EUID/SUDO_USER asserts, OS detection, flag parser, bootstrap_pg(), install_env_file(), substitute_placeholders(), install_systemd_units / install_launchd_plists, start_daemons, verify_doctor"
      min_lines: 250
  key_links:
    - from: "scripts/install-bootstrap.sh"
      to: "packaging/forge-bridge.env.example"
      via: "cp template → /etc/forge-bridge/forge-bridge.env (only if absent)"
      pattern: "/etc/forge-bridge/forge-bridge\\.env"
    - from: "scripts/install-bootstrap.sh"
      to: "packaging/systemd/*.service + packaging/launchd/*.plist"
      via: "cp + substitute_placeholders + daemon-reload / launchctl bootstrap"
      pattern: "substitute_placeholders|systemctl daemon-reload|launchctl bootstrap"
    - from: "scripts/install-bootstrap.sh"
      to: "forge-bridge console doctor"
      via: "auto-run as final verification step (D-12)"
      pattern: "forge-bridge console doctor"
---

<objective>
Ship `scripts/install-bootstrap.sh` — the imperative half of Phase 20.1's install model. This script is the operator-facing surface that turns a fresh Rocky 9 VM or a fresh-state portofino macOS box into a working forge-bridge install: Postgres bootstrap + auth alignment + role+db + alembic + env-file install + unit/plist registration + daemon start + doctor verification.

Purpose: Phase 20 Track A UAT proved that no Flame artist could complete `docs/INSTALL.md` as prose. Steps 3 (Postgres) and 6 (launch) demanded sysadmin knowledge the doc didn't supply. This script is the architectural fix: the install procedure becomes a script + a daemon, not a wall of imperatives. Closes 9 of the 13 Phase 20 gaps directly (#1, #2, #3, #4, #5, #6, #7, #8, #9; gap #10 closed by P2 unit StandardInput=null; #11 by P2 Requires= cascade; #12 by INSTALL.md reshape in P6; #13 is v1.4.x deferred chat hang).

Output: A 250+-line bash script in `scripts/` that takes a fresh machine and produces a working install end-to-end, plus 2 new tests in tests/test_packaging.py asserting bash syntax + flag matrix presence.

Per D-23 decoupled-commit purity: this plan is the IMPERATIVE wiring of the structural artifacts P1/P2/P3 shipped. No INSTALL.md doc reshape (P6) bundled here. No new doctor sub-check (P5).

Per D-07: ALL five flags must work — no planted-but-unimplemented lazy paths. Per D-09 UAT matrix: combos (a) Linux full, (b) Linux --mcp-only, (c) macOS default, (d) Linux --no-postgres must all complete cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-CONTEXT.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-RESEARCH.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-01-SUMMARY.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-02-SUMMARY.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-03-SUMMARY.md
@.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md
@scripts/install-flame-hook.sh

<interfaces>
<!-- Existing patterns (verbatim borrows). -->

FROM scripts/install-flame-hook.sh — top-matter pattern + log-prefix + error-with-remediation:
  - Header docstring with: subject / Usage / Environment overrides / What it does / What it does NOT do
  - `set -euo pipefail` immediately after header
  - `[forge-bridge]` prefix on every operator-facing echo line
  - ERROR lines go to >&2 (not stdout); remediation in the same line
  - exit 1 on hard fail (NOT exit 2 — reserved for forge-bridge console doctor "server unreachable")

FROM RESEARCH.md Pattern 3 (lines 501-585) — Postgres bootstrap functions (drop-in):
  probe_pg_auth() / resolve_pg_hba() / align_pg_hba() / bootstrap_pg()

FROM RESEARCH.md Pitfall 5 (lines 843-861) — single substitution helper:
  substitute_placeholders() — sed -i.bak with __SUDO_USER__, __CONDA_BASE__, __REPO_ROOT__ keys

FROM RESEARCH.md Example 1 (lines 902-970) — script skeleton (drop-in):
  Flag parsing case statement, EUID/SUDO_USER asserts, OS detection switch, conda env probe.

FROM RESEARCH.md Pattern 4 (lines 633-657) — install_env_file() function (drop-in):
  mkdir /etc/forge-bridge → cp template if absent → chown root:$SUDO_USER → chmod 0640 → prompt_for_llm_url

FROM RESEARCH.md Pattern 4 lines 635-647 — interactive prompt:
  read -rp "Ollama base URL [default]: " URL  (NOT read -s; URL is non-secret)

FROM RESEARCH.md TL;DR item 9 + Pitfall 2 — macOS log-dir creation:
  mkdir -p /var/log/forge-bridge && chown root:wheel /var/log/forge-bridge && chmod 755 /var/log/forge-bridge
  BEFORE launchctl bootstrap (otherwise daemon silent-aborts with `last exit code = 78`)

FROM RESEARCH.md TL;DR item 10 — modern launchctl syntax:
  sudo launchctl bootstrap system /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist
  (NOT `launchctl load` — deprecated)

FROM RESEARCH.md SECURITY DOMAIN line 1245 — $SUDO_USER validation:
  $SUDO_USER must match ^[a-z][a-z0-9_-]*$ before substitution (systemd unit injection prevention)

FLAG MATRIX (D-07; ALL five must work):
  --track-b              skip Flame hook install
  --no-postgres          skip bootstrap_pg() entirely (use existing remote DB)
  --mcp-only             implies --track-b + --no-postgres + skip Console daemon (BUS+MCP only deploy)
  --with-flame-mac       macOS only — opt INTO Flame hook (rare; default is auto-applied --track-b)
  --non-interactive      skip the FORGE_LOCAL_LLM_URL prompt; use defaults

LOCKED ENTRY POINTS (DO NOT confuse with orphan):
  python -m forge_bridge          → __main__.py → MCP+Console
  python -m forge_bridge.server   → server/__main__.py → WS bus
  ❌ forge_bridge/server.py (top-level FILE) is the pre-Phase-5 orphan; never reference it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/install-bootstrap.sh — the imperative bootstrap</name>
  <files>scripts/install-bootstrap.sh</files>
  <read_first>
    - scripts/install-flame-hook.sh (the structural precedent; mirror header / log-prefix / error patterns)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-RESEARCH.md (Example 1 lines 902-970 — drop-in skeleton; Pattern 3 lines 501-585 — bootstrap_pg() functions; Pattern 4 lines 633-657 — install_env_file(); Pitfalls 1-8 lines 791-897 — every documented landmine)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md (§ "scripts/install-bootstrap.sh" lines 27-157 — the analog patterns from install-flame-hook.sh; Cross-cutting Patterns A/C/D/E)
    - .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md (gaps #1-#12 — every gap with disposition "scriptable" or "doc-only-hard" is a thing this script must close)
    - packaging/forge-bridge.env.example (the template to install)
    - packaging/systemd/forge-bridge*.service (the units to copy + substitute)
    - packaging/launchd/com.cnoellert.forge-bridge*.plist (the plists to copy + substitute)
    - packaging/launchd/forge-bridge*-daemon (the wrappers to copy + substitute + chmod +x)
  </read_first>
  <action>
Create `scripts/install-bootstrap.sh` and `chmod +x` it. The script must implement the full install path. Below is the mandatory structure — copy verbatim where verbatim is called out; fill in the body using the cited research/patterns line-ranges.

──────────────────────────────────────────────
HEADER (lines 1-30) — mirror scripts/install-flame-hook.sh structure
──────────────────────────────────────────────

  #!/usr/bin/env bash
  #
  # install-bootstrap.sh — bootstrap a forge-bridge install on a fresh machine.
  #
  # Usage:
  #   sudo ./scripts/install-bootstrap.sh [flags]
  #
  # Flags:
  #   --track-b            Skip Flame hook install (Track B / MCP-only deploy)
  #   --no-postgres        Skip Postgres bootstrap (use existing remote DB)
  #   --mcp-only           --track-b + --no-postgres + skip Console daemon
  #   --with-flame-mac     macOS only — opt INTO Flame hook install (rare)
  #   --non-interactive    Skip the FORGE_LOCAL_LLM_URL prompt
  #   -h, --help           Show this help
  #
  # What it does:
  #   1. Detects OS (Rocky/RHEL Linux or Darwin macOS) and validates pre-reqs
  #   2. (Linux) dnf install + initdb + auth alignment + role+db creation + alembic upgrade
  #   3. (macOS) Homebrew postgresql@16 detect/install (rare branch)
  #   4. Installs /etc/forge-bridge/forge-bridge.env from the template (mode 0640)
  #   5. Installs systemd units (Linux) or launchd plists + wrappers (macOS)
  #   6. Substitutes __SUDO_USER__ placeholders in every copied artifact
  #   7. Starts daemons (systemctl enable --now / launchctl bootstrap)
  #   8. Auto-runs `forge-bridge console doctor` for verification
  #
  # What it does NOT do:
  #   - No conda env install (operator does this in Step 1 of INSTALL.md)
  #   - No pip install (operator does this in Step 2 of INSTALL.md)
  #   - No Flame hook deploy (use scripts/install-flame-hook.sh for that)
  #   - No Ollama install (Ollama runs on a separate LLM service host)
  #   - No Homebrew auto-install on macOS (D-06 — operator-trust)

  set -euo pipefail

──────────────────────────────────────────────
FLAG PARSING + OS DETECTION (after header)
──────────────────────────────────────────────

Use RESEARCH.md Example 1 lines 911-957 verbatim — TRACK_B / NO_POSTGRES / MCP_ONLY / WITH_FLAME_MAC / NON_INTERACTIVE defaults; while-shift case parser; EUID + SUDO_USER asserts; OS detection switch.

Add ONE thing to the OS detection block per RESEARCH.md SECURITY DOMAIN line 1245 (BEFORE any sed substitution):

  # Validate $SUDO_USER for safe sed substitution (systemd unit injection prevention).
  if ! [[ "$SUDO_USER" =~ ^[a-z][a-z0-9_-]*$ ]]; then
      echo "[forge-bridge] ERROR: \$SUDO_USER='$SUDO_USER' contains unsafe characters" >&2
      exit 1
  fi

──────────────────────────────────────────────
PATH RESOLUTION
──────────────────────────────────────────────

  SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
  REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
  CONDA_BASE="$(sudo -u "$SUDO_USER" bash -lc 'conda info --base' 2>/dev/null || echo "/home/${SUDO_USER}/miniconda3")"
  CONDA_PYTHON="${CONDA_BASE}/envs/forge/bin/python"
  if [ ! -x "$CONDA_PYTHON" ]; then
      echo "[forge-bridge] ERROR: conda env 'forge' not found at $CONDA_PYTHON" >&2
      echo "[forge-bridge] HINT: complete Step 1 + Step 2 of docs/INSTALL.md before running this script" >&2
      exit 1
  fi

──────────────────────────────────────────────
substitute_placeholders() helper (RESEARCH.md Pitfall 5 lines 850-859)
──────────────────────────────────────────────

  substitute_placeholders() {
      local file="$1"
      sudo sed -i.bak \
          -e "s|__SUDO_USER__|${SUDO_USER}|g" \
          -e "s|__CONDA_BASE__|${CONDA_BASE}|g" \
          -e "s|__REPO_ROOT__|${REPO_ROOT}|g" \
          "$file"
      sudo rm -f "${file}.bak"
  }

──────────────────────────────────────────────
bootstrap_pg() — Linux Postgres bootstrap (RESEARCH.md Pattern 3 lines 549-585, verbatim)
──────────────────────────────────────────────

Copy probe_pg_auth(), resolve_pg_hba(), align_pg_hba(), bootstrap_pg() verbatim from RESEARCH.md Pattern 3 lines 513-585. The 5-step body of bootstrap_pg() is:
  1. command -v psql || dnf install -y postgresql-server postgresql-contrib
  2. [ -d /var/lib/pgsql/data/base ] || sudo postgresql-setup --initdb
  3. sudo systemctl enable --now postgresql
  4. for i in $(seq 1 30); do pg_isready -h localhost -p 5432 && break; sleep 1; done
  5. probe_pg_auth + align_pg_hba + idempotent role+db psql + alembic upgrade head

Closes Phase 20 gaps #1, #3, #4, #5, #6 in one function.

──────────────────────────────────────────────
bootstrap_pg_macos() — macOS Postgres branch (D-04 rare path)
──────────────────────────────────────────────

  bootstrap_pg_macos() {
      if command -v psql >/dev/null 2>&1; then
          echo "[forge-bridge] psql already on PATH — skipping macOS Postgres install"
      elif brew list postgresql@16 >/dev/null 2>&1; then
          echo "[forge-bridge] postgresql@16 brew package present — starting via brew services"
          brew services start postgresql@16
      else
          echo "[forge-bridge] installing postgresql@16 via Homebrew (D-04 rare branch)"
          brew install postgresql@16
          brew services start postgresql@16
      fi
      # readiness gate
      for i in $(seq 1 30); do
          pg_isready -h localhost -p 5432 >/dev/null 2>&1 && break
          sleep 1
      done
      # macOS Homebrew Postgres ships with `trust` for localhost — skip pg_hba alignment
      # Idempotent role+db creation (psql defaults to current user as superuser on Homebrew)
      psql -tAc "SELECT 1 FROM pg_roles WHERE rolname='forge'" postgres 2>/dev/null | grep -q 1 || \
          psql -c "CREATE USER forge WITH PASSWORD 'forge';" postgres
      psql -tAc "SELECT 1 FROM pg_database WHERE datname='forge_bridge'" postgres 2>/dev/null | grep -q 1 || \
          psql -c "CREATE DATABASE forge_bridge OWNER forge;" postgres
      cd "$REPO_ROOT" && sudo -u "$SUDO_USER" "$CONDA_PYTHON" -m alembic upgrade head
  }

──────────────────────────────────────────────
install_env_file() — RESEARCH.md Pattern 4 lines 649-657 verbatim
──────────────────────────────────────────────

  install_env_file() {
      sudo mkdir -p /etc/forge-bridge
      if [ ! -f /etc/forge-bridge/forge-bridge.env ]; then
          sudo cp "${REPO_ROOT}/packaging/forge-bridge.env.example" /etc/forge-bridge/forge-bridge.env
          echo "[forge-bridge] installed /etc/forge-bridge/forge-bridge.env from template"
      else
          echo "[forge-bridge] /etc/forge-bridge/forge-bridge.env already exists — preserving operator edits"
      fi
      sudo chown "root:${SUDO_USER}" /etc/forge-bridge/forge-bridge.env
      sudo chmod 0640 /etc/forge-bridge/forge-bridge.env
      prompt_for_llm_url
  }

  prompt_for_llm_url() {
      if [ "$NON_INTERACTIVE" = "1" ]; then
          echo "[forge-bridge] non-interactive mode — keeping existing FORGE_LOCAL_LLM_URL"
          return 0
      fi
      local default="http://localhost:11434/v1"
      read -rp "Ollama base URL [${default}]: " url
      url="${url:-$default}"
      sudo sed -i.bak -E "s|^FORGE_LOCAL_LLM_URL=.*|FORGE_LOCAL_LLM_URL=${url}|" /etc/forge-bridge/forge-bridge.env
      sudo rm -f /etc/forge-bridge/forge-bridge.env.bak
  }

──────────────────────────────────────────────
install_linux_units() — copy systemd units, substitute, daemon-reload, enable+start
──────────────────────────────────────────────

  install_linux_units() {
      echo "[forge-bridge] installing systemd units"
      sudo cp "${REPO_ROOT}/packaging/systemd/forge-bridge-server.service" /etc/systemd/system/
      sudo cp "${REPO_ROOT}/packaging/systemd/forge-bridge.service" /etc/systemd/system/
      substitute_placeholders /etc/systemd/system/forge-bridge-server.service
      substitute_placeholders /etc/systemd/system/forge-bridge.service
      sudo systemctl daemon-reload
      sudo systemctl enable --now forge-bridge-server.service
      if [ "$MCP_ONLY" = "1" ]; then
          echo "[forge-bridge] --mcp-only — bus only; skipping forge-bridge.service start"
      else
          sudo systemctl enable --now forge-bridge.service
      fi
  }

──────────────────────────────────────────────
install_macos_units() — copy plists + wrappers, substitute, mkdir log dir, launchctl bootstrap
──────────────────────────────────────────────

  install_macos_units() {
      echo "[forge-bridge] installing launchd plists + wrappers"
      # Pitfall 2 fix: log dir MUST exist BEFORE launchctl bootstrap.
      sudo mkdir -p /var/log/forge-bridge
      sudo chown root:wheel /var/log/forge-bridge
      sudo chmod 755 /var/log/forge-bridge

      sudo cp "${REPO_ROOT}/packaging/launchd/com.cnoellert.forge-bridge-server.plist" /Library/LaunchDaemons/
      sudo cp "${REPO_ROOT}/packaging/launchd/com.cnoellert.forge-bridge.plist" /Library/LaunchDaemons/
      sudo cp "${REPO_ROOT}/packaging/launchd/forge-bridge-server-daemon" /usr/local/bin/
      sudo cp "${REPO_ROOT}/packaging/launchd/forge-bridge-daemon" /usr/local/bin/
      sudo chmod +x /usr/local/bin/forge-bridge-server-daemon /usr/local/bin/forge-bridge-daemon

      substitute_placeholders /Library/LaunchDaemons/com.cnoellert.forge-bridge-server.plist
      substitute_placeholders /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist
      substitute_placeholders /usr/local/bin/forge-bridge-server-daemon
      substitute_placeholders /usr/local/bin/forge-bridge-daemon

      # Idempotent: bootout-then-bootstrap is the modern reload path.
      sudo launchctl bootout system /Library/LaunchDaemons/com.cnoellert.forge-bridge-server.plist 2>/dev/null || true
      sudo launchctl bootstrap system /Library/LaunchDaemons/com.cnoellert.forge-bridge-server.plist
      if [ "$MCP_ONLY" = "1" ]; then
          echo "[forge-bridge] --mcp-only — bus only; skipping forge-bridge.plist bootstrap"
      else
          sudo launchctl bootout system /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist 2>/dev/null || true
          sudo launchctl bootstrap system /Library/LaunchDaemons/com.cnoellert.forge-bridge.plist
      fi
  }

──────────────────────────────────────────────
verify_doctor() — D-12 auto-run + exit code mapping
──────────────────────────────────────────────

  verify_doctor() {
      echo "[forge-bridge] running post-install verification (forge-bridge console doctor)"
      # Run as the operator user — doctor is operator-account state, not root.
      if sudo -u "$SUDO_USER" "$CONDA_PYTHON" -m forge_bridge.cli console doctor; then
          echo "[forge-bridge] doctor PASS or PARTIAL — install OK"
          return 0
      else
          echo "[forge-bridge] doctor FAIL — see output above for the failing row"
          return 1
      fi
  }

──────────────────────────────────────────────
MAIN — run the steps in order
──────────────────────────────────────────────

  case "$OS" in
      Linux)
          [ "$NO_POSTGRES" = "1" ] || bootstrap_pg
          install_env_file
          install_linux_units
          ;;
      Darwin)
          [ "$NO_POSTGRES" = "1" ] || bootstrap_pg_macos
          install_env_file
          install_macos_units
          ;;
  esac

  verify_doctor
  cat <<'NEXT'

  [forge-bridge] Install complete. Next steps:

    1. Visit the Artist Console at http://localhost:9996/ui/
    2. Inspect daemon logs (Linux):
         sudo journalctl -u forge-bridge -f
         sudo journalctl -u forge-bridge-server -f
    3. Inspect daemon logs (macOS):
         tail -f /var/log/forge-bridge/console.log
         tail -f /var/log/forge-bridge/server.log
    4. Re-run the doctor any time:
         forge-bridge console doctor

  NEXT

──────────────────────────────────────────────
FINAL STEP: chmod +x
──────────────────────────────────────────────

  chmod +x scripts/install-bootstrap.sh

──────────────────────────────────────────────
KEY DISCIPLINES (apply throughout):
──────────────────────────────────────────────
  - Every operator-facing echo line uses `[forge-bridge]` prefix (Cross-cutting Pattern C from PATTERNS.md)
  - ERROR lines go to >&2 with remediation in the same line
  - Idempotency throughout: probe-then-act for every mutation (Cross-cutting Pattern E)
  - Use `read -rp` for the LLM URL prompt (NOT `read -s`; URL is non-secret per memory feedback_secret_paste_pattern.md)
  - Do NOT auto-install Homebrew on macOS (D-06)
  - Do NOT touch /etc/sudoers (out of scope)
  - Do NOT bundle Flame hook install (use scripts/install-flame-hook.sh; INSTALL.md Step 4 stays)

DO NOT reference forge_bridge/server.py (orphan file). All Python invocations use submodule notation.
  </action>
  <verify>
    <automated>test -x scripts/install-bootstrap.sh && bash -n scripts/install-bootstrap.sh && head -1 scripts/install-bootstrap.sh | grep -F '#!/usr/bin/env bash' && grep -E '^set -euo pipefail$' scripts/install-bootstrap.sh && grep -E '^substitute_placeholders\(\) \{' scripts/install-bootstrap.sh && grep -E '^bootstrap_pg\(\) \{' scripts/install-bootstrap.sh && grep -E '^install_env_file\(\) \{' scripts/install-bootstrap.sh && grep -E '^verify_doctor\(\) \{' scripts/install-bootstrap.sh && grep -F -- '--track-b' scripts/install-bootstrap.sh && grep -F -- '--no-postgres' scripts/install-bootstrap.sh && grep -F -- '--mcp-only' scripts/install-bootstrap.sh && grep -F -- '--with-flame-mac' scripts/install-bootstrap.sh && grep -F -- '--non-interactive' scripts/install-bootstrap.sh && grep -F 'forge-bridge console doctor' scripts/install-bootstrap.sh && ! grep -F 'forge_bridge/server.py' scripts/install-bootstrap.sh && ! grep -F 'launchctl load' scripts/install-bootstrap.sh && ! grep -F 'launchctl unload' scripts/install-bootstrap.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/install-bootstrap.sh` exists (test: `test -f scripts/install-bootstrap.sh`)
    - File is executable (test: `test -x scripts/install-bootstrap.sh`)
    - First line is exactly `#!/usr/bin/env bash` (test: `head -1 scripts/install-bootstrap.sh == '#!/usr/bin/env bash'`)
    - `bash -n scripts/install-bootstrap.sh` exits 0 (syntax-clean)
    - Contains `set -euo pipefail` (test: `grep -E '^set -euo pipefail$'`)
    - Contains all 5 flags by name (literal grep): `--track-b`, `--no-postgres`, `--mcp-only`, `--with-flame-mac`, `--non-interactive`
    - Contains `--help` AND `-h` flag handling
    - Contains all required functions by name: `substitute_placeholders()`, `bootstrap_pg()`, `bootstrap_pg_macos()`, `install_env_file()`, `prompt_for_llm_url()`, `install_linux_units()`, `install_macos_units()`, `verify_doctor()`
    - Contains EUID assertion: `grep -F '[ "$EUID" -eq 0 ]'` matches
    - Contains $SUDO_USER regex validation: `grep -E '^[a-z]\[a-z0-9_-\]\*\$' || grep -F 'a-z][a-z0-9_-'` (the validation pattern)
    - Contains exact string `forge-bridge console doctor` (D-12 auto-run guard)
    - Contains exact path `/etc/forge-bridge/forge-bridge.env`
    - Contains exact path `/Library/LaunchDaemons/` (macOS install target)
    - Contains exact path `/etc/systemd/system/` (Linux install target)
    - Contains exact path `/usr/local/bin/forge-bridge-daemon` (macOS wrapper install target)
    - Contains `mkdir -p /var/log/forge-bridge` (Pitfall 2 fix; macOS log dir creation)
    - Contains `chmod 0640 /etc/forge-bridge/forge-bridge.env` (Pitfall 3 fix)
    - Contains `chown "root:${SUDO_USER}" /etc/forge-bridge/forge-bridge.env`
    - Contains `read -rp` (NOT `read -s`) for the LLM URL prompt
    - Contains `pg_isready -h localhost -p 5432` readiness gate
    - Contains `password_encryption` probe (Pattern 3)
    - Contains exact string `pg_reload_conf()` (auth alignment reload)
    - Contains `CREATE USER forge WITH PASSWORD 'forge'` (idempotent role creation)
    - Contains `CREATE DATABASE forge_bridge OWNER forge` (idempotent db creation)
    - Contains `alembic upgrade head`
    - Contains `launchctl bootstrap system` (modern syntax)
    - Does NOT contain `launchctl load` or `launchctl unload` (deprecated; test: `! grep -E 'launchctl (load|unload)'`)
    - Does NOT contain `forge_bridge/server.py` (CLAUDE.md anti-pattern)
    - Does NOT contain `KeepAlive=true` reference (P3 plists already lock this)
    - Does NOT modify `/etc/sudoers` (test: `! grep -F '/etc/sudoers'`)
    - Does NOT auto-install Homebrew (D-06; test: `! grep -E 'curl.*Homebrew/install'`)
    - Line count >= 250 (substantive coverage check; test: `[ $(wc -l < scripts/install-bootstrap.sh) -ge 250 ]`)
  </acceptance_criteria>
  <done>
    scripts/install-bootstrap.sh exists, is executable, syntax-clean, and implements the full install path: OS detection → flag matrix → Postgres bootstrap (Linux + macOS branches) → env-file install (mode 0640 root:$SUDO_USER) → unit/plist registration with `__SUDO_USER__` substitution → daemon start → forge doctor verification. Closes Phase 20 gaps #1-#9 (the install-time gaps; gaps #10-#11 closed by P2/P3 unit/plist directives). All 5 D-07 flags work. Idempotent on re-run per D-21 + RESEARCH.md OQ #3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append install-bootstrap.sh regression tests to tests/test_packaging.py</name>
  <files>tests/test_packaging.py</files>
  <read_first>
    - tests/test_packaging.py (current state — 24 existing tests after P1+P2+P3)
    - scripts/install-bootstrap.sh (just created in Task 1)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-VALIDATION.md (lines 53 — `bash -n scripts/install-bootstrap.sh` Wave 0 acceptance)
  </read_first>
  <behavior>
    Test 1: test_install_bootstrap_exists_and_executable — file exists with x mode bit set.
    Test 2: test_install_bootstrap_bash_syntax_clean — `bash -n scripts/install-bootstrap.sh` exits 0.
    Test 3: test_install_bootstrap_has_all_flags — every D-07 flag literal appears in the script (--track-b, --no-postgres, --mcp-only, --with-flame-mac, --non-interactive).
    Test 4: test_install_bootstrap_no_orphan_server_reference — script does not contain forge_bridge/server.py.
    Test 5: test_install_bootstrap_uses_modern_launchctl — script does not contain `launchctl load` or `launchctl unload`.
    Test 6: test_install_bootstrap_chains_doctor — script invokes `forge-bridge console doctor` (D-12 auto-run).
    Test 7: test_install_bootstrap_log_prefix_discipline — every echo line that mentions `[forge-bridge]` indicates the prefix discipline is in place. Heuristic: count `[forge-bridge]` occurrences in echo'd strings, expect >=10.
  </behavior>
  <action>
Append these tests to tests/test_packaging.py. They go AFTER the existing tests (P1 env, P2 systemd, P3 launchd).

  _SCRIPTS = _REPO_ROOT / "scripts"

  def test_install_bootstrap_exists_and_executable():
      """scripts/install-bootstrap.sh must exist and be executable."""
      path = _SCRIPTS / "install-bootstrap.sh"
      assert path.is_file(), "scripts/install-bootstrap.sh missing — Phase 20.1 P4 deliverable"
      assert os.access(path, os.X_OK), \
          f"install-bootstrap.sh not executable (mode {oct(path.stat().st_mode)[-3:]})"

  def test_install_bootstrap_bash_syntax_clean():
      """`bash -n scripts/install-bootstrap.sh` must exit 0 — catches syntax errors at test time."""
      bash = shutil.which("bash")
      if not bash:
          import pytest
          pytest.skip("bash not on PATH")
      r = subprocess.run([bash, "-n", str(_SCRIPTS / "install-bootstrap.sh")],
                         capture_output=True, text=True)
      assert r.returncode == 0, f"install-bootstrap.sh syntax error: {r.stderr!r}"

  def test_install_bootstrap_has_all_flags():
      """D-07: ALL 5 flags must be present (no planted-but-unimplemented lazy paths)."""
      content = (_SCRIPTS / "install-bootstrap.sh").read_text()
      for flag in ("--track-b", "--no-postgres", "--mcp-only", "--with-flame-mac", "--non-interactive"):
          assert flag in content, f"install-bootstrap.sh missing flag: {flag}"

  def test_install_bootstrap_no_orphan_server_reference():
      """CLAUDE.md anti-pattern guard — orphan forge_bridge/server.py never referenced."""
      content = (_SCRIPTS / "install-bootstrap.sh").read_text()
      assert "forge_bridge/server.py" not in content, \
          "install-bootstrap.sh references the pre-Phase-5 orphan top-level file"

  def test_install_bootstrap_uses_modern_launchctl():
      """RESEARCH.md TL;DR item 10: bootstrap/bootout, NOT load/unload (deprecated since macOS 11)."""
      content = (_SCRIPTS / "install-bootstrap.sh").read_text()
      # Allow `launchctl bootout` but not `launchctl unload`. Check word-boundary forms.
      import re
      assert not re.search(r"\blaunchctl\s+load\b", content), \
          "install-bootstrap.sh uses deprecated `launchctl load`; use `launchctl bootstrap system ...`"
      assert not re.search(r"\blaunchctl\s+unload\b", content), \
          "install-bootstrap.sh uses deprecated `launchctl unload`; use `launchctl bootout system ...`"

  def test_install_bootstrap_chains_doctor():
      """D-12: script auto-runs `forge-bridge console doctor` at end as PASS/FAIL gate."""
      content = (_SCRIPTS / "install-bootstrap.sh").read_text()
      assert "forge-bridge console doctor" in content or "forge_bridge.cli console doctor" in content, \
          "install-bootstrap.sh must auto-run the doctor (D-12 verification gate)"

  def test_install_bootstrap_log_prefix_discipline():
      """Cross-cutting Pattern C: every operator-facing log line uses [forge-bridge] prefix."""
      content = (_SCRIPTS / "install-bootstrap.sh").read_text()
      # Heuristic: at least 10 [forge-bridge] mentions across echo lines (script has ~250 lines).
      occurrences = content.count("[forge-bridge]")
      assert occurrences >= 10, \
          f"install-bootstrap.sh log-prefix discipline weak — found {occurrences} '[forge-bridge]' " \
          f"mentions, expected >=10 (Cross-cutting Pattern C)"

Run the tests:

  pytest tests/test_packaging.py -x -v

All P1+P2+P3 tests (24) plus P4's 7 — total 31 — must pass.
  </action>
  <verify>
    <automated>pytest tests/test_packaging.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_packaging.py contains 7 new test functions whose names start with `test_install_bootstrap_`
    - File defines `_SCRIPTS = _REPO_ROOT / "scripts"` constant
    - `pytest tests/test_packaging.py -x -v` exits 0 with at least 31 tests collected (7 P1 + 7 P2 + 10 P3 + 7 P4)
    - `pytest tests/test_packaging.py::test_install_bootstrap_bash_syntax_clean -x` exits 0 (or skips cleanly if bash absent)
    - `pytest tests/test_packaging.py::test_install_bootstrap_has_all_flags -x` exits 0 (D-07 all-flags-implemented guard)
    - `pytest tests/test_packaging.py::test_install_bootstrap_uses_modern_launchctl -x` exits 0 (deprecated-syntax guard)
    - `pytest tests/test_packaging.py::test_install_bootstrap_chains_doctor -x` exits 0 (D-12 auto-run guard)
  </acceptance_criteria>
  <done>
    7 install-bootstrap.sh regression tests appended. Bash syntax + flag matrix + doctor-chaining + log-prefix discipline + deprecated-syntax + orphan-file-guard all assert via pytest. Total tests in tests/test_packaging.py = 31, all passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (sudo) → script execution | Script asserts `[ "$EUID" -eq 0 ]` and `[ -n "$SUDO_USER" ]` at entry. $SUDO_USER validated against `^[a-z][a-z0-9_-]*$` BEFORE any sed substitution (T-20.1-09 systemd unit injection prevention). |
| script → /etc, /Library/LaunchDaemons, /usr/local/bin | All writes use `sudo cp` + `substitute_placeholders` + `chown`/`chmod`. No symlink-following operations. cp default refuses to overwrite a non-file destination. |
| operator → interactive prompt for FORGE_LOCAL_LLM_URL | `read -rp` (visible — URL is non-secret per memory feedback_secret_paste_pattern.md). `-r` correct (no backslash interpretation needed). Single prompt, single `sed -i.bak` write. |
| script → forge-bridge console doctor | Doctor invoked as `sudo -u "$SUDO_USER" "$CONDA_PYTHON" -m forge_bridge.cli console doctor` — operator account, not root. Exit code maps directly to script exit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-15 | T (Tampering) | sed substitution of $SUDO_USER with shell-meta chars | mitigate | Validate `$SUDO_USER` matches `^[a-z][a-z0-9_-]*$` at script entry. test_install_bootstrap_has_all_flags + test_install_bootstrap_no_orphan_server_reference + manual UAT walks (P7) close. |
| T-20.1-16 | I (Information Disclosure) | env file world-readable after install | mitigate | Script chmod 0640 + chown root:$SUDO_USER on /etc/forge-bridge/forge-bridge.env. acceptance_criteria asserts these literals present. Pitfall 3 explicit. |
| T-20.1-17 | T (Tampering) | pg_hba.conf rewrite without backup → cluster lockout | mitigate | align_pg_hba() copies pg_hba to .bak.$(date +%s) BEFORE sed -i.tmp; idempotent re-run safe. RESEARCH.md Pattern 3 line 540. |
| T-20.1-18 | D (Denial of Service) | postgres-setup --initdb run twice corrupts cluster | mitigate | Idempotency guard: `[ -d /var/lib/pgsql/data/base ] || sudo postgresql-setup --initdb` — only runs if cluster directory absent. RESEARCH.md Pattern 3 line 555. |
| T-20.1-19 | T (Tampering) | wrapper-script env-file source injection | mitigate (transitive) | P3 wrappers source the env file with `set -a; . FILE; set +a`. P4 sets file mode 0640 root:$SUDO_USER, so non-operator-non-root cannot modify. KEY=VALUE format from P1 prevents shell-command injection. |
| T-20.1-20 | I (Information Disclosure) | Postgres role created with default password 'forge' | accept (with documentation) | Locked from FB-A v1.4 design — local-only single-operator credential. SEED-AUTH-V1.5 work (v1.6+) introduces real auth. INSTALL.md Step 5 (P6) documents the credential rotation surface for operators who later harden their cluster. |
| T-20.1-21 | E (Elevation of Privilege) | sudo not asserted; script run as bare user | mitigate | `[ "$EUID" -eq 0 ]` asserted at script entry; exits 1 with clear message otherwise. acceptance_criteria asserts the literal. |
| T-20.1-22 | T (Tampering) | macOS launchd plist log-file open with mode-elevation race | mitigate | Pitfall 2 fix: `mkdir -p /var/log/forge-bridge && chown root:wheel && chmod 755` BEFORE `launchctl bootstrap`. Plist's StandardOutPath then opens an existing dir, not race-creating it. |
</threat_model>

<verification>
Quick suite:
  pytest tests/test_packaging.py -x -v
  # → 31 tests pass

Full suite:
  pytest tests/ -x

Bash syntax sanity:
  bash -n scripts/install-bootstrap.sh

Help-text sanity (script must accept --help without sudo):
  ./scripts/install-bootstrap.sh --help    # exits 0; prints usage + flag matrix

Real install testing happens in P7 HUMAN-UAT walks on flame-01 and portofino — NOT in CI (D-17 fresh-VM contract).
</verification>

<success_criteria>
1. scripts/install-bootstrap.sh exists, is executable, ≥250 lines, syntax-clean.
2. All 5 D-07 flags implemented (no planted-but-unimplemented paths).
3. 9 of 13 Phase 20 gaps closed in this script (the install-time gaps; #10/#11 by P2 directives, #12 by P6 doc reshape, #13 by v1.4.x).
4. tests/test_packaging.py grows from 24 → 31 tests; all pass.
5. Idempotency contract: re-running the script is a no-op (RESEARCH.md OQ #3 — env-file edits preserved).
6. forge-bridge console doctor auto-runs at end (D-12).
7. $SUDO_USER injection prevention (validated regex; substitute_placeholders helper).
8. Modern launchctl bootstrap/bootout (no deprecated load/unload).
9. Single git commit per D-23 (imperative wiring; no INSTALL.md reshape; no doctor sub-check).
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-04-SUMMARY.md` documenting:
- scripts/install-bootstrap.sh exists; line count; flag matrix verified.
- Postgres bootstrap auth-alignment via probe (RESEARCH.md Pattern 3) — handles md5 (Rocky 9 PG13) and scram-sha-256 (PG14.3+) honestly.
- Idempotency contract: which steps probe-then-act; which preserve operator state across re-runs.
- $SUDO_USER injection-prevention regex validation in place.
- D-12 doctor auto-run gates the install success.
- 4 of 13 Phase 20 gaps (#10, #11, #12, #13) closed elsewhere — the rest closed here.
- Foundation laid for P5 (doctor sub-check) and P6 (INSTALL.md reshape).
- Foundation laid for P7 (HUMAN-UAT walks on flame-01 + portofino).
</output>
