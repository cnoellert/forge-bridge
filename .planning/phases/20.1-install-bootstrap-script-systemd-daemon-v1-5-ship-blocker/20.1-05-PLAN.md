---
phase: 20.1
plan: 05
type: execute
wave: 4
depends_on: [20.1-02, 20.1-03]
files_modified:
  - forge_bridge/cli/doctor.py
  - tests/test_cli_doctor.py
autonomous: true
requirements: [INSTALL-01]
tags: [cli, doctor, daemon-state, code-fix]

must_haves:
  truths:
    - "forge_bridge.cli.doctor exposes a new private function `_check_daemon_state()` that probes systemd (Linux) or launchd (macOS) for daemon liveness"
    - "On Linux, `_check_daemon_state` runs `systemctl is-active forge-bridge-server` AND `systemctl is-active forge-bridge` and aggregates results"
    - "On macOS, `_check_daemon_state` runs `launchctl print system/com.cnoellert.forge-bridge-server` AND `launchctl print system/com.cnoellert.forge-bridge` and parses `state = X` lines"
    - "The check returns ONE row in the doctor table (single sub-check name `daemon_state`, granular detail in the fact cell — D-13 single-row choice)"
    - "Status mapping: ok if both daemons up; warn if one up; fail if neither up; warn for non-linux/non-darwin platforms"
    - "T-11-01 / LRN-05 discipline: exception class names ONLY in fact cell, never str(exc)"
    - "Sub-check is wired into `doctor_cmd()` so `forge-bridge console doctor` includes the daemon_state row alongside existing rows"
    - "Three new pytest cases cover: Linux ok path, macOS partial path, probe-fail surfaces exception class name"
  artifacts:
    - path: "forge_bridge/cli/doctor.py"
      provides: "Updated module: imports subprocess at top; defines `_check_daemon_state() -> dict` per RESEARCH.md Pattern 5; appends call to checks list inside doctor_cmd()"
      contains: "def _check_daemon_state, sys.platform == 'linux', sys.platform == 'darwin', systemctl is-active, launchctl print system/, type(exc).__name__"
    - path: "tests/test_cli_doctor.py"
      provides: "3 new pytest cases covering daemon_state sub-check"
      contains: "test_daemon_state_linux_both_active, test_daemon_state_darwin_partial, test_daemon_state_probe_fail_surfaces_class_name"
  key_links:
    - from: "forge_bridge/cli/doctor.py:_check_daemon_state"
      to: "packaging/systemd/forge-bridge*.service (P2) and packaging/launchd/com.cnoellert.forge-bridge*.plist (P3)"
      via: "unit names + plist labels match what _check_daemon_state probes"
      pattern: "forge-bridge-server|com\\.cnoellert\\.forge-bridge-server"
    - from: "forge_bridge/cli/doctor.py:doctor_cmd"
      to: "_check_daemon_state"
      via: "checks.append(_check_daemon_state())"
      pattern: "_check_daemon_state\\(\\)"
---

<objective>
Add the `daemon_state` sub-check to `forge_bridge.cli.doctor` so `forge-bridge console doctor` reports systemd unit liveness on Linux and launchd job liveness on macOS. Closes D-13 from CONTEXT.md and gives Phase 23 DIAG-05 its first concrete daemon-state coverage row.

Purpose: P4's install-bootstrap.sh auto-runs `forge-bridge console doctor` as the install-success gate (D-12). Without the daemon_state sub-check, the doctor would report ALL existing checks green (port reachable, JSONL parseable, etc.) even if one of the two daemons crashed during start. The new sub-check makes "are the daemons actually running?" a first-class doctor row, OS-branched internally so the operator sees one consistent table on either OS.

Output:
- New `_check_daemon_state()` function in `forge_bridge/cli/doctor.py` (drop-in from RESEARCH.md Pattern 5 lines 668-737)
- Wire-in line in `doctor_cmd()` after `_check_disk_space()`
- 3 new pytest cases in `tests/test_cli_doctor.py` (Linux active, macOS partial, probe-fail class-name surface)

Per D-23: this is a code change. Single atomic commit (1 module + 1 test file). No INSTALL.md reshape (P6).

Per CLAUDE.md "v1.5 constraints": no new external libs, no public `__all__` symbols added (function is module-private, leading-underscore). Public surface stays at 19.

Per D-13 single-row vs two-row debate: ONE row keeps the doctor table readable; the fact cell carries `forge-bridge-server=active, forge-bridge=active` for granular detail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-CONTEXT.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-RESEARCH.md
@.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md
@forge_bridge/cli/doctor.py
@tests/test_cli_doctor.py

<interfaces>
<!-- Doctor table dict shape — every check returns this exact dict. Source: forge_bridge/cli/doctor.py:283-310 -->
{
    "name": str,            # short check identifier (e.g. "console_port_reprobe", "daemon_state")
    "status": "ok" | "warn" | "fail",
    "fact": str,            # observation (e.g. "http 200 on :9996", "forge-bridge-server=active, forge-bridge=active")
    "try": str,             # remediation hint (empty when status=ok; non-empty actionable command otherwise)
}

<!-- Existing analog: _check_console_port_reconfirm (forge_bridge/cli/doctor.py:283-310) -->
<!-- Same return shape, same T-11-01 / LRN-05 exception-class-only redaction discipline. -->

<!-- doctor_cmd() wire-in location: AFTER existing _check_disk_space() block, BEFORE "Compute exit code" -->
<!-- Per PATTERNS.md line 698-705: -->
checks.append(_check_console_port_reconfirm())
disk_check = _check_disk_space()
if disk_check is not None:
    checks.append(disk_check)
checks.append(_check_daemon_state())   # ← NEW: appended here

<!-- subprocess module is NOT currently imported at module top in doctor.py (verified). -->
<!-- sys IS already imported. -->

<!-- T-11-01 / LRN-05 discipline (existing in _check_console_port_reconfirm:303-310): -->
except (httpx.ConnectError, httpx.TimeoutException, httpx.RemoteProtocolError) as exc:
    # T-11-01: surface the exception class name only — never str(exc).
    return {
        "name": "console_port_reprobe",
        "status": "fail",
        "fact": f"reprobe failed: {type(exc).__name__}",   # ← f"...: {type(exc).__name__}"
        "try": "the initial /api/v1/health call succeeded but the reprobe failed — ...",
    }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `_check_daemon_state()` function and wire into doctor_cmd()</name>
  <files>forge_bridge/cli/doctor.py, tests/test_cli_doctor.py</files>
  <read_first>
    - forge_bridge/cli/doctor.py (FULL FILE — confirm current import block, locate _check_console_port_reconfirm at lines 283-310 as the analog, locate the doctor_cmd checks-list assembly to find the wire-in point)
    - tests/test_cli_doctor.py (FULL FILE — confirm existing test patterns; the existing CliRunner+monkeypatch style is NOT used for these 3 new tests because they unit-test the helper directly via subprocess.run mock)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-RESEARCH.md (Pattern 5 lines 668-737 — VERIFIED full body of _check_daemon_state; Example 3 lines 1052-1083 — VERIFIED test bodies)
    - .planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-PATTERNS.md (§ "forge_bridge/cli/doctor.py" lines 584-710 — wire-in location explicit; § "tests/test_cli_doctor.py" lines 501-580)
    - packaging/systemd/forge-bridge-server.service (P2 — confirm unit name `forge-bridge-server` matches what _check_daemon_state probes via `systemctl is-active forge-bridge-server`)
    - packaging/launchd/com.cnoellert.forge-bridge-server.plist (P3 — confirm Label `com.cnoellert.forge-bridge-server` matches what _check_daemon_state probes via `launchctl print system/com.cnoellert.forge-bridge-server`)
  </read_first>
  <behavior>
    Test 1 (RED → GREEN): test_daemon_state_linux_both_active — mock subprocess.run to return MagicMock(stdout="active\n", returncode=0); patch sys.platform="linux"; assert result["status"]=="ok" and "forge-bridge-server=active" + "forge-bridge=active" in result["fact"].
    Test 2 (RED → GREEN): test_daemon_state_darwin_partial — mock subprocess.run side_effect to return "state = running" for the bus and "state = not running" for the console; patch sys.platform="darwin"; assert result["status"]=="warn".
    Test 3 (RED → GREEN): test_daemon_state_probe_fail_surfaces_class_name — mock subprocess.run side_effect = FileNotFoundError("systemctl: command not found"); patch sys.platform="linux"; assert "FileNotFoundError" in result["fact"] AND "command not found" NOT in result["fact"] (T-11-01 — class name only, never str(exc)) AND result["status"]=="fail".
  </behavior>
  <action>
This task has THREE sub-steps performed in sequence. RED tests fail; GREEN function makes them pass; wire-in adds the row to the live doctor.

──────────────────────────────────────────────
Step A (RED): Append the 3 tests to tests/test_cli_doctor.py
──────────────────────────────────────────────

Append to the END of tests/test_cli_doctor.py (file is 252 lines; existing import block at lines 9-20). Add `import sys` and `from unittest.mock import MagicMock` to the existing import block if not already present (verified by reading the file first).

  import sys
  from unittest.mock import patch, MagicMock


  @patch("forge_bridge.cli.doctor.subprocess.run")
  def test_daemon_state_linux_both_active(mock_run):
      """Linux: both units active → ok."""
      mock_run.return_value = MagicMock(stdout="active\n", returncode=0)
      with patch.object(sys, "platform", "linux"):
          from forge_bridge.cli.doctor import _check_daemon_state
          result = _check_daemon_state()
      assert result["status"] == "ok"
      assert "forge-bridge-server=active" in result["fact"]
      assert "forge-bridge=active" in result["fact"]
      assert result["name"] == "daemon_state"


  @patch("forge_bridge.cli.doctor.subprocess.run")
  def test_daemon_state_darwin_partial(mock_run):
      """macOS: bus running, console not → warn (single-row partial-state legibility)."""
      def runner_(args, *a, **kw):
          if "forge-bridge-server" in args[2]:
              return MagicMock(stdout="state = running\n", returncode=0)
          return MagicMock(stdout="state = not running\n", returncode=0)
      mock_run.side_effect = runner_
      with patch.object(sys, "platform", "darwin"):
          from forge_bridge.cli.doctor import _check_daemon_state
          result = _check_daemon_state()
      assert result["status"] == "warn"
      assert result["name"] == "daemon_state"


  @patch("forge_bridge.cli.doctor.subprocess.run")
  def test_daemon_state_probe_fail_surfaces_class_name(mock_run):
      """T-11-01 / LRN-05: subprocess failure surfaces exception CLASS, never str(exc)."""
      mock_run.side_effect = FileNotFoundError("systemctl: command not found")
      with patch.object(sys, "platform", "linux"):
          from forge_bridge.cli.doctor import _check_daemon_state
          result = _check_daemon_state()
      assert "FileNotFoundError" in result["fact"]
      # NEVER str(exc) — the literal "command not found" must NOT appear in the fact cell.
      assert "command not found" not in result["fact"], \
          f"T-11-01 violation: str(exc) leaked into doctor fact cell: {result['fact']!r}"
      assert result["status"] == "fail"

Run the tests — they MUST fail (function does not yet exist):

  pytest tests/test_cli_doctor.py::test_daemon_state_linux_both_active tests/test_cli_doctor.py::test_daemon_state_darwin_partial tests/test_cli_doctor.py::test_daemon_state_probe_fail_surfaces_class_name -x

Expect: ImportError or AttributeError on `from forge_bridge.cli.doctor import _check_daemon_state`. This is the RED state.

──────────────────────────────────────────────
Step B (GREEN): Add `_check_daemon_state()` to forge_bridge/cli/doctor.py
──────────────────────────────────────────────

In forge_bridge/cli/doctor.py:

1. Add `import subprocess` to the top-of-module import block (if not already present — verify by reading lines 1-20 first). `import sys` is ALREADY present per CLAUDE.md guidance.

2. After the existing `_check_console_port_reconfirm()` function (currently at lines 283-310), append the function below verbatim from RESEARCH.md Pattern 5 lines 668-737:

  def _check_daemon_state() -> dict:
      """D-13: Probe systemd (Linux) or launchd (macOS) for daemon liveness.

      Returns ONE row in the doctor table — the fact cell carries granular detail
      so we don't double the table footprint with one row per unit.
      """
      name = "daemon_state"
      if sys.platform == "linux":
          results: dict[str, str] = {}
          for unit in ("forge-bridge-server", "forge-bridge"):
              try:
                  r = subprocess.run(
                      ["systemctl", "is-active", unit],
                      capture_output=True, text=True, timeout=2,
                  )
                  results[unit] = (r.stdout or "").strip() or "unknown"
              except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
                  # T-11-01: surface exception class name only, never str(exc)
                  results[unit] = f"probe-failed:{type(exc).__name__}"

          all_active = all(v == "active" for v in results.values())
          any_active = any(v == "active" for v in results.values())
          status = "ok" if all_active else ("warn" if any_active else "fail")
          fact = ", ".join(f"{u}={s}" for u, s in results.items())
          try_msg = "" if all_active else (
              "sudo systemctl status forge-bridge && journalctl -u forge-bridge -n 50"
          )
          return {"name": name, "status": status, "fact": fact, "try": try_msg}

      if sys.platform == "darwin":
          results: dict[str, str] = {}
          for label in ("com.cnoellert.forge-bridge-server", "com.cnoellert.forge-bridge"):
              try:
                  r = subprocess.run(
                      ["launchctl", "print", f"system/{label}"],
                      capture_output=True, text=True, timeout=2,
                  )
                  if r.returncode != 0:
                      results[label.split(".")[-1]] = "not-loaded"
                      continue
                  # Parse `state = running` line from `launchctl print` output.
                  state = "unknown"
                  for line in (r.stdout or "").splitlines():
                      if "state =" in line:
                          state = line.split("=", 1)[1].strip()
                          break
                  results[label.split(".")[-1]] = state
              except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
                  results[label.split(".")[-1]] = f"probe-failed:{type(exc).__name__}"

          all_running = all(v == "running" for v in results.values())
          any_running = any(v == "running" for v in results.values())
          status = "ok" if all_running else ("warn" if any_running else "fail")
          fact = ", ".join(f"{k}={v}" for k, v in results.items())
          try_msg = "" if all_running else (
              "sudo launchctl print system/com.cnoellert.forge-bridge && tail -50 /var/log/forge-bridge/console.log"
          )
          return {"name": name, "status": status, "fact": fact, "try": try_msg}

      # Other platforms (Windows, BSD) — skip with informative warn.
      return {
          "name": name,
          "status": "warn",
          "fact": f"daemon_state probe not implemented for sys.platform={sys.platform!r}",
          "try": "no-op — supported only on linux + darwin in v1.5",
      }

Re-run the tests — they MUST pass now:

  pytest tests/test_cli_doctor.py::test_daemon_state_linux_both_active tests/test_cli_doctor.py::test_daemon_state_darwin_partial tests/test_cli_doctor.py::test_daemon_state_probe_fail_surfaces_class_name -x -v

Expect: 3 passed.

──────────────────────────────────────────────
Step C (WIRE-IN): Append `_check_daemon_state()` to the doctor checks list
──────────────────────────────────────────────

Locate the `doctor_cmd()` function in forge_bridge/cli/doctor.py. Find the existing block (per PATTERNS.md lines 700-705 — confirm via `grep -n "_check_console_port_reconfirm" forge_bridge/cli/doctor.py`):

  checks.append(_check_console_port_reconfirm())
  disk_check = _check_disk_space()
  if disk_check is not None:
      checks.append(disk_check)

Append this single line after that block (before the "Compute exit code" comment / logic):

  checks.append(_check_daemon_state())

──────────────────────────────────────────────
Step D: Run the full test suite
──────────────────────────────────────────────

  pytest tests/test_cli_doctor.py -x -v

All existing test_cli_doctor.py tests must still pass (regression-free). Plus the 3 new tests pass.

  pytest tests/ -x

Full suite must remain green.

CRITICAL DISCIPLINE:
  - Single atomic commit (one diff) per D-23 — module + tests together (NOT split across two commits)
  - subprocess imported at top of module (NOT lazy-imported inside the function)
  - The function is module-private (leading underscore) — public `__all__` stays at 19 symbols
  - T-11-01 / LRN-05: f"...: {type(exc).__name__}" pattern — never str(exc) in the fact cell
  - DO NOT modify _check_console_port_reconfirm or any other existing check function (out of scope)
  - DO NOT add new doctor_cmd() flags / arguments (out of scope; D-09 surface deferred)
  - DO NOT reference forge_bridge/server.py orphan
  </action>
  <verify>
    <automated>pytest tests/test_cli_doctor.py -x -v && grep -E '^def _check_daemon_state' forge_bridge/cli/doctor.py && grep -F 'checks.append(_check_daemon_state())' forge_bridge/cli/doctor.py && grep -E '^import subprocess' forge_bridge/cli/doctor.py && grep -F 'systemctl' forge_bridge/cli/doctor.py && grep -F 'launchctl' forge_bridge/cli/doctor.py && grep -F 'type(exc).__name__' forge_bridge/cli/doctor.py</automated>
  </verify>
  <acceptance_criteria>
    - File `forge_bridge/cli/doctor.py` contains `import subprocess` at module top (test: `grep -E '^import subprocess$' forge_bridge/cli/doctor.py`)
    - File `forge_bridge/cli/doctor.py` defines `def _check_daemon_state(` (test: `grep -E '^def _check_daemon_state\(' forge_bridge/cli/doctor.py`)
    - Function body contains `sys.platform == "linux"` AND `sys.platform == "darwin"` branches (test: `grep -F 'sys.platform == "linux"' && grep -F 'sys.platform == "darwin"'`)
    - Function body contains `systemctl` (Linux probe) AND `launchctl` (macOS probe) literals (test: grep -F 'systemctl' AND grep -F 'launchctl print')
    - Function body contains `type(exc).__name__` (T-11-01 discipline; test: `grep -F 'type(exc).__name__' forge_bridge/cli/doctor.py`)
    - Function body does NOT contain `str(exc)` in the daemon_state code path (test: read function block, confirm no `str(exc)` literal)
    - `doctor_cmd()` wires in the new check via `checks.append(_check_daemon_state())` (test: `grep -F 'checks.append(_check_daemon_state())' forge_bridge/cli/doctor.py`)
    - `pytest tests/test_cli_doctor.py::test_daemon_state_linux_both_active -x` exits 0
    - `pytest tests/test_cli_doctor.py::test_daemon_state_darwin_partial -x` exits 0
    - `pytest tests/test_cli_doctor.py::test_daemon_state_probe_fail_surfaces_class_name -x` exits 0
    - `pytest tests/test_cli_doctor.py -x` exits 0 (full file regression-free; existing tests still pass)
    - `pytest tests/ -x` exits 0 (full suite regression-free)
    - `forge_bridge.__all__` length unchanged at 19 (test: `python3 -c "import forge_bridge; assert len(forge_bridge.__all__) == 19"`)
    - Function does NOT reference `forge_bridge/server.py` orphan file (test: read function body, confirm no such literal)
  </acceptance_criteria>
  <done>
    `_check_daemon_state()` lives in forge_bridge/cli/doctor.py per RESEARCH.md Pattern 5; subprocess imported at module top; doctor_cmd() includes the daemon_state row; 3 new pytest cases prove Linux ok / macOS partial / probe-fail-class-name discipline. Public `__all__` unchanged. Single atomic commit per D-23.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor (operator account) → systemctl / launchctl subprocess | Read-only probes via `systemctl is-active` and `launchctl print` — both are safe-to-call-as-non-root. doctor runs as $SUDO_USER (the daemon-account); subprocess inherits this identity. No state mutation. |
| subprocess output → doctor table fact cell | Output passed through `(r.stdout or "").strip()` AND further filtered (only the `state =` line for launchctl; only the `is-active` token for systemctl). Bounded text; cannot leak filesystem paths or env values into screenshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20.1-23 | I (Information Disclosure) | str(exc) leaking filesystem paths into doctor table | mitigate | f"...:{type(exc).__name__}" pattern enforced. test_daemon_state_probe_fail_surfaces_class_name asserts "command not found" string NOT in fact cell. |
| T-20.1-24 | I (Information Disclosure) | launchctl print output verbatim into fact cell | mitigate | Function only matches the `state = X` line; ignores program path / env / arguments. RESEARCH.md SECURITY DOMAIN line 1246 explicit. |
| T-20.1-25 | D (Denial of Service) | subprocess.run with no timeout hangs doctor | mitigate | `timeout=2` on every subprocess.run call. TimeoutExpired caught + reported as `probe-failed:TimeoutExpired`. |
</threat_model>

<verification>
Quick suite:
  pytest tests/test_cli_doctor.py -x -v
  # → 3 new tests pass; existing tests still pass

Full suite:
  pytest tests/ -x

Public API regression check:
  python3 -c "import forge_bridge; assert len(forge_bridge.__all__) == 19, f'__all__ grew: {len(forge_bridge.__all__)}'"

Manual smoke (on a host with one or both daemons running):
  forge-bridge console doctor
  # → table now includes a `daemon_state` row with status ok/warn/fail
</verification>

<success_criteria>
1. `_check_daemon_state()` exists in forge_bridge/cli/doctor.py (single function, OS-branched).
2. `subprocess` imported at module top (NOT lazy-imported inside the function).
3. `doctor_cmd()` includes the new check via `checks.append(_check_daemon_state())`.
4. 3 new pytest cases in tests/test_cli_doctor.py all pass; existing tests regression-free.
5. T-11-01 / LRN-05 discipline: exception class names only in fact cell.
6. Public `__all__` unchanged at 19 symbols.
7. D-13 single-row choice honored — one `daemon_state` row, granular detail in fact cell.
8. Single atomic commit per D-23 (module + tests together).
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-install-bootstrap-script-systemd-daemon-v1-5-ship-blocker/20.1-05-SUMMARY.md` documenting:
- Function added to forge_bridge/cli/doctor.py with line ranges.
- Wire-in location in doctor_cmd() (after _check_disk_space).
- 3 pytest cases added with case names + assertions.
- T-11-01 discipline confirmed by test_daemon_state_probe_fail_surfaces_class_name.
- Public `__all__` length still 19 (no public API changes).
- Foundation laid for INSTALL.md (P6) Step 6 to reference `forge-bridge console doctor` as the verification path.
</output>
