---
phase: "20.1"
plan: "06"
subsystem: "docs"
tags: [docs, install, reshape, daemon-model]

dependency_graph:
  requires:
    - "20.1-01: packaging/forge-bridge.env.example — env file template documented in Step 5"
    - "20.1-02: packaging/systemd/*.service — unit names referenced in Step 6 Linux commands"
    - "20.1-03: packaging/launchd/*.plist — label names referenced in Step 6 macOS commands"
    - "20.1-04: scripts/install-bootstrap.sh — the script Step 3 points operators at"
    - "20.1-05: forge doctor daemon_state row — referenced as verification output in Step 6"
  provides:
    - "docs/INSTALL.md — reshaped to bootstrap-script + daemon model (Steps 3, 5, 6)"
  affects:
    - "20.1-P7 (HUMAN-UAT walks): INSTALL.md is the document operators walk; reshape is the test subject"
    - "Phase 21 (GETTING-STARTED.md): can reference INSTALL.md Step 3 as the canonical first install path"

tech_stack:
  added: []
  patterns:
    - "Heading style: ## Step N: <Verb> <object> — Phase 20 voice preserved"
    - "Imperative voice; code blocks with explicit language hints (bash, ini)"
    - "Verification commands with EXPECTED OUTPUT after → symbol"
    - "Bold-emphasis operative keys (**mandatory**, **Skip this step if Track B**)"
    - "Three-dash separator (---) between steps — preserved on both sides of each reshaped section"

key_files:
  created: []
  modified:
    - "docs/INSTALL.md"

decisions:
  - "Steps 1, 2, 4, 7, 8 preserved verbatim — Phase 20 close / UAT-validated; reshape scoped to Steps 3, 5, 6 only per CONTEXT.md line 113"
  - "Step 6c prose references the stdin-keepalive workaround by description rather than by literal command string — satisfies both the plan's deletion criterion and the contextual explanation"
  - "Reference appendix sections (env vars, ports, LLM service host) left untouched — pre-existing export FORGE_LOCAL_LLM_URL in appendix is out of scope per plan boundaries"

metrics:
  duration: "~15 minutes"
  completed: "2026-05-01"
  tasks_completed: 3
  tasks_total: 3
  files_created: 0
  files_modified: 1
---

# Phase 20.1 Plan 06: INSTALL.md Reshape Summary

**One-liner:** docs/INSTALL.md Steps 3, 5, 6 reshaped to bootstrap-script + daemon model — deletes the Phase 20 gap #11 single-process lie, collapses manual psql ceremony to `sudo ./scripts/install-bootstrap.sh`, and documents systemctl/launchctl daemon verification with stop/restart/logs coverage.

## What Was Built

Reshaped three sections of `docs/INSTALL.md` to match the daemon model shipped in P1-P5. Steps 1, 2, 4, 7, 8 are untouched.

### Step 3 (was: "Set up Postgres" → now: "Run the bootstrap script")

Collapsed 39 lines of manual Postgres + pg_hba + alembic ceremony to a single `sudo ./scripts/install-bootstrap.sh` invocation. Added:
- All 5 D-07 flags documented in a table (`--track-b`, `--no-postgres`, `--mcp-only`, `--with-flame-mac`, `--non-interactive`)
- `--help` reference for live flag matrix
- What-the-script-does narrative for both Linux (Rocky/RHEL) and macOS paths
- Custom-credentials addendum (`alembic -x url=...`) for non-default Postgres setups
- `forge-bridge console doctor` auto-run as install-success gate

Closes Phase 20 gaps #1-#6 in the doc surface (the script closes them in behavior; the doc points the operator at the script).

### Step 5 (was: "Configure environment variables" → now: "Configure environment")

Replaced `export FOO=bar` ephemeral shell-state guidance with the env-file model:
- Points operator at `/etc/forge-bridge/forge-bridge.env` (the P1/P4 artifact)
- Documents Linux restart (`sudo systemctl restart forge-bridge`) and macOS reload (`sudo launchctl kickstart -k system/com.cnoellert.forge-bridge`)
- 5c addendum documents the `forge:forge` local-only credential + `SEED-AUTH-V1.5` v1.6+ hardening path

Closes Phase 20 gaps #7 (env persistence) and #8 (conda re-init bypassed by daemon model).

### Step 6 (was: "Start the server" → now: "Verify the daemons are running")

Deleted the Phase 20 gap #11 doc lie: ~~"The single `python -m forge_bridge` process boots **all four hosted surfaces in one shot**"~~. Replaced with:
- Two-process system explanation (`forge-bridge-server` WS bus on `:9998` + `forge-bridge` MCP+Console on `:9996`)
- Linux: `systemctl status / journalctl -f / restart / stop / disable` for both units
- macOS: `launchctl print / tail -f / kickstart / bootout` for both daemons
- 6c explains the OS-init-system lifespan model; stdin-keepalive workaround no longer needed

Closes Phase 20 gap #11 (doc lie deleted) and gap #12 (stop/restart/logs — operators now have documented clean stop semantics for both platforms).

## Files Modified

| File | Change | Description |
|------|--------|-------------|
| `docs/INSTALL.md` | MODIFIED | Steps 3, 5, 6 reshaped; Steps 1, 2, 4, 7, 8 unchanged; 407 lines total (was 375) |

## Phase 20 Gaps Closed (doc surface)

| Gap | Description | Closed by |
|-----|-------------|-----------|
| #1-#6 | Manual Postgres + pg_hba + alembic ceremony | Step 3 → bootstrap script |
| #7 | Env persistence — `export FOO=bar` was ephemeral | Step 5 → env file |
| #8 | Conda re-init pitfall — shell sourcing bypass | Step 5 → daemon model note |
| #11 | Doc lie — single process boots all surfaces | Step 6 → lie deleted |
| #12 | No clean stop semantics | Step 6 → full systemctl/launchctl coverage |

## Commits

| Commit | Type | Description |
|--------|------|-------------|
| `95a4476` | docs | reshape INSTALL.md Step 3 — collapse to bootstrap script invocation |
| `ae16869` | docs | reshape INSTALL.md Step 5 — collapse to env-file edit + restart command |
| `e039220` | docs | reshape INSTALL.md Step 6 — remove doc lie; document daemon verification + logs |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Step 6c backtick-quoted keepalive command matched `! grep -F` deletion criterion**
- **Found during:** Task 3 verification
- **Issue:** The plan's Step 6c prose included the literal string `` `tail -f /dev/null | python -m forge_bridge` `` in backticks as a "there is no X anymore" reference. The acceptance criterion `! grep -F 'tail -f /dev/null | python -m forge_bridge' docs/INSTALL.md` would fail because `grep -F` matches literal strings regardless of markdown backtick context.
- **Fix:** Rewrote Step 6c to say "The stdin-keepalive workaround from earlier versions is no longer needed" — preserves the architectural explanation without including the literal deprecated command string.
- **Files modified:** `docs/INSTALL.md` (Step 6c only)
- **Commit:** `e039220` (included in Task 3 commit)

## Known Stubs

None — this is a doc-only plan. All content is fully wired: Step 3 points at `scripts/install-bootstrap.sh` (P4, shipped), Step 5 points at `/etc/forge-bridge/forge-bridge.env` (P1/P4, shipped), Step 6 references unit names from P2 (systemd) and P3 (launchd) that are shipped.

## Threat Surface Scan

No new trust boundaries introduced. This is a doc-only plan.

T-20.1-26 mitigated: Phase 20 gap #11 doc lie deleted. Verified with `! grep -F 'all four hosted surfaces in one shot' docs/INSTALL.md`.

T-20.1-27 (forge:forge credential): Step 5c documents the local-only credential reality + SEED-AUTH-V1.5 rotation path. Accept disposition with documentation — as designed.

T-20.1-28 (INSTALL.md ↔ README drift): Steps 1, 2, 4, 7, 8 preserved verbatim. README still points at `docs/INSTALL.md` for the canonical install path. No version drift introduced.

## Self-Check: PASSED

- `docs/INSTALL.md` exists: FOUND
- `grep -c '^## Step' docs/INSTALL.md` → 8: PASSED
- `! grep -F 'all four hosted surfaces in one shot' docs/INSTALL.md`: PASSED
- `grep -F 'sudo ./scripts/install-bootstrap.sh' docs/INSTALL.md`: FOUND
- `grep -F '## Step 3: Run the bootstrap script' docs/INSTALL.md`: FOUND
- `grep -F '## Step 5: Configure environment' docs/INSTALL.md`: FOUND
- `grep -F '## Step 6: Verify the daemons are running' docs/INSTALL.md`: FOUND
- `grep -F 'sudo systemctl restart forge-bridge' docs/INSTALL.md`: FOUND
- `grep -F 'sudo launchctl kickstart -k system/com.cnoellert.forge-bridge' docs/INSTALL.md`: FOUND
- `grep -F 'sudo journalctl -u forge-bridge -f' docs/INSTALL.md`: FOUND
- `grep -F 'tail -f /var/log/forge-bridge/console.log' docs/INSTALL.md`: FOUND
- `! grep -F 'tail -f /dev/null | python -m forge_bridge' docs/INSTALL.md`: PASSED
- Commit `95a4476` (Task 1): FOUND
- Commit `ae16869` (Task 2): FOUND
- Commit `e039220` (Task 3): FOUND
- INSTALL.md line count (407, >= 280): PASSED
