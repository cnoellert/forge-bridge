---
status: partial
phase: 10.1-artist-ux-gap-closure
source: [10.1-VERIFICATION.md, 10.1-UAT.md]
started: 2026-04-24T12:30:00-07:00
updated: 2026-04-24T12:30:00-07:00
---

## Current Test

[awaiting human testing — see items below]

## Tests

### 1. Truly-fresh-operator re-UAT (requalify SC#6 without deviation)
expected: A non-developer artist who has NOT previously seen the Phase 10 or Phase 10.1 Web UI (not `CN/dev`, not `ET/tester`) opens `http://localhost:9996/ui/` cold and identifies the three most-recently-synthesized tools AND their status (active / loaded) within 30 seconds. The operator arrives at the answer by reading the column headers and chips, not by inferring from list position. This requalifies SC#6 against the D-44 fresh-operator rule without the deviation that Phase 10.1 UAT recorded.
result: [pending]

### 2. Default-sort affordance legibility
expected: A fresh operator looks at the `Created` column header and understands (without asking) that the table is sorted descending by that column. Candidate remediations (any or a combination): rename the header to `Synthesized` or `Last synth`, add a visible `▼ newest first` caret, show relative time ("2h ago") alongside or instead of the ISO timestamp. This test is satisfied when a fresh operator can explain the sort order without being told.
result: [pending]

### 3. Non-Chromium browser parity smoke
expected: Open `http://localhost:9996/ui/tools` in Firefox AND Safari. Confirm in each: (a) no duplicate nav / health strip on click, (b) Status chips render in the same position and with visible color contrast, (c) clicking a preset chip narrows the list the same way it does in Chromium. Playwright only drives Chromium; Firefox/Safari are not automated. A single manual smoke per browser is sufficient — does not need to be repeated unless the CSS or htmx swap contract changes.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
