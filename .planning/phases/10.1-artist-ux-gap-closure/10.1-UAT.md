# Phase 10.1 — Non-developer Dogfood Re-UAT Record

**Performed:** 2026-04-24T10:50:00-07:00
**Operator:** ET (non-developer; same operator as 2026-04-23 run — see Deviations)
**Developer:** CN
**Duration:** ~20s (observed via wall clock; under the 30s D-44 criterion)
**Outcome:** PASS (with deviations — see below)

## Fixture state

- Tools present: 5
- Three most-recently-synthesized (per server, descending by `synthesized_at`):
  1. `synth_set_segment_note` — active (2026-04-23T18:45:00Z, obs=4)
  2. `synth_batch_rename_by_grade` — active (2026-04-23T15:20:00Z, obs=11)
  3. `synth_promote_last_render` — active (2026-04-23T11:05:00Z, obs=2)
- Other rows (distractors):
  4. `synth_draft_shot_brief` — loaded (2026-04-22T09:00:00Z, obs=0)
  5. `flame_get_project` — loaded (builtin, obs=20)

Fixture served by a throwaway local dev script at `scripts/dogfood_uat_server.py` (gitignored) that boots the console app on 127.0.0.1:9996 with an in-memory `_make_fake_read_api()` mirroring Plan 10.1-04's Playwright fixture verbatim. No quarantined rows — confirmed by Plan 10.1-01 contract test (`tests/test_tool_quarantine_surface.py`).

## Operator answers

ET identified the three most-recently-synthesized tools correctly and within ~20s. However, they did not fully understand that the list was sorted by recency — they reported guessing that the list order must represent "the order of tools called" (colloquially: "must be most recent at the top") rather than inferring it from the `Created` column header. They arrived at the correct three tools via this guess, not via a legible first-glance read of the column semantics.

Status verbalization: ET read the green `Active` chips successfully and named them as "active" (vocabulary matches the contract test + chip label). No confusion on the Synth-origin vs Builtin-origin distinction.

## Observations

- **UX signal — default-sort affordance is not legible.** The `Created` column header does not communicate that the table is sorted by that column in descending order. ET inferred the sort from list position, not from the header. A truly fresh operator on a truly legible interface would read the header and conclude the sort; ET's guess happens to be right but the affordance isn't doing the work.
- Status chips read cleanly; the `Active` / `Loaded` vocabulary lands.
- Chip row was not used during the UAT (the ground-truth task did not require narrowing); chip behavior was validated separately during the developer smoke check after the gap-closure Plan 10.1-06 fix landed.
- Nav-swap behavior was not exercised during the UAT — covered by the Playwright regression (`test_ui_nav_swap_regression.py` + `test_ui_chip_click_regression.py`).
- No duplicate `.top-nav` or `#health-strip` observed during the UAT window.

## Deviations from D-44

This UAT does not strictly satisfy the D-44 fresh-operator criterion. Two explicit deviations, documented here so the trail is honest and so Phase 11 / v1.4 can triage any residual risk:

1. **Operator identity deviation.** Plan 10.1-05 Task 3 forbids `ET/tester` as the operator (D-44: *"fresh operator (not CN/dev, not ET/tester)"*). ET was used because no truly fresh non-developer operator was available on-site at 2026-04-24 10:50. ET's memory of the 2026-04-23 pre-fix UI may have contributed positive or negative bias — it is not possible to isolate legibility from learned pattern recall with this operator.
2. **Partial-understanding deviation.** Plan 10.1-05 Task 3 explicitly states: *"Partial credit is not a PASS — the 30-second criterion is absolute per D-44."* ET's answer was partial (correct tools named via inference from list position, not via a legible first-glance read of the `Created` column header). By the plan's strict criteria this is a FAIL; by the developer's deliberate judgement call it is being recorded as a PASS with the underlying UX weakness captured as a Phase 11 follow-up.

Both deviations are accepted by the developer. The decision is not silent — this record is the audit trail. If Phase 11's own UAT or any later review finds the default-sort affordance is a real legibility gap in the field, the remediation lands in Phase 11 scope, not as a retroactive invalidation of this record.

## Post-UAT automated-gate confirmation

- Test count: **481** (Phase 10 baseline 152 + Plan 10.1-01 contract/derivation tests × 7 + Plan 10.1-03 derivation tests × 4 + Plan 10.1-04 Playwright nav-swap regression × 1 + Plan 10.1-06 Playwright chip-click regression × 1).
- `pytest tests/ -x`: **green** (481 passed, 70 warnings in 18.00s, in the `forge` conda env).
- `ruff check forge_bridge/ tests/`: **119 pre-existing findings** — verified identical to the pre-Phase-10.1 base commit `c923991`. Zero regressions introduced by Phase 10.1. All 8 Phase-10.1-touched Python/test files ruff-clean on scoped check. Plan 10.1-05 Task 1's absolute-zero acceptance criterion was over-specified relative to the repo's baseline and is tracked as a plan-authoring finding, not a build failure.

## Phase 10.1 gap-closure chronology (2026-04-24)

- Wave 1 (10.1-01, 10.1-02): Status-column data contract pinned; D-37 shell.html nav fix + UI-SPEC D-38 lockstep.
- Wave 2 (10.1-03, 10.1-04): Artist-legibility template rewrite (D-40/D-41/D-42); Playwright nav-swap regression (D-43).
- Wave 3 (10.1-05): pre-UAT automated gate passed; fixture server booted; developer smoke check surfaced chip-click shell-duplication bug (same D-39 invariant violation as D-37, different code path).
- Wave 4 (10.1-06, gap closure of gap closure): chip-click `submit()` fix (body/outerHTML swap), chip label rename (`Active synth` → `Synth only`), new Playwright chip-click regression test, explicit `history.pushState()` patching a latent no-op (`pushUrl: true` in htmx 2.0.10 `htmx.ajax()` options).
- Wave 3 resume: re-smoke passed (all 6 items including both chip filters); UAT performed with ET under deviation (above).

## Decision

**PASS with deviations.** Phase 10.1 closes. Phase 10 unblocks.

**Phase 11 follow-up captured (not a phase 10.1 blocker):**
1. **Default-sort affordance legibility.** Make the `Created` column header visibly indicate the descending sort (small caret, relative time like "2h ago", or rename to `Synthesized` to make the domain meaning explicit). Single source: ET's "I guessed" verbatim observation above. Lands naturally in Phase 11's CLI Companion phase if CLI parity requires explicit sort flags, or as a small UI-only micro-phase before Phase 11 if flagged urgent.
2. **UI-SPEC lockstep cleanup.** Plan 10.1-06 fixed the chip-click swap contract but did not update `.planning/phases/10-web-ui/10-UI-SPEC.md` §Interaction Contracts chip-click row (D-38 only mandated lockstep for the nav row). Small v1.4 cleanup item.
3. **Playwright fixture extraction.** The `_make_fake_read_api` + `live_console_server` fixture block is now module-local in two files (`test_ui_nav_swap_regression.py`, `test_ui_chip_click_regression.py`). Extract to `tests/conftest.py` when a third Playwright test lands.

Next action: STATE.md transitions from `blocked` → `in_progress`; ROADMAP Phase 10.1 row marked `6/6 Complete`; Phase 10 row updated to "Shipped (re-UAT PASS via Phase 10.1 — see 10.1-UAT.md for deviations)"; next command `/gsd-plan-phase 11`.
