---
phase: 06-learning-pipeline-integration
verified: 2026-04-17T00:00:00Z
status: human_needed
score: 15/15 must-haves verified (automated)
overrides_applied: 0
human_verification:
  - test: "SC #1 — live cross-process log isolation"
    expected: "Running two forge-bridge processes simultaneously (one standalone via `python -m forge_bridge`, one via projekt-forge with FORGE_PROJECT_ROOT set) writes to two distinct JSONL paths with no cross-contamination of promotion counts."
    why_human: "Automated coverage validates the invariant in-process via test_two_execution_logs_at_different_paths_are_isolated (projekt-forge tests/test_learning_wiring.py). A true two-process smoke test requires launching both servers, triggering executions in each, and visually confirming path separation + promotion-counter independence — that cannot be scripted without a live Flame/MCP session."
  - test: "SC #2 — synthesis request hits forge's Ollama instance"
    expected: "With forge_config.yaml pointing llm.local_url at forge's real Ollama server, trigger a synthesis (execute the same Flame code pattern 3 times through projekt-forge). The synthesized tool is generated by forge's configured model, not forge-bridge's standalone default."
    why_human: "Automated tests verify LLMRouter receives cfg values from forge_config.yaml (test_init_learning_pipeline_with_config asserts `router.local_url == cfg[\"llm\"][\"local_url\"]`). Confirming the resulting HTTP call actually reaches forge's Ollama instance requires a running Ollama, a valid forge_config.yaml, and a synthesis-triggering execution — no stubs."
  - test: "SC #4 — enriched prompt visible in live synthesis logs"
    expected: "With projekt-forge running, trigger synthesis with FORGE_PROJECT_CODE set and observe that the emitted synthesis log line / Ollama request body carries the hook-injected constraint (`do not import flame`) and the `project:<code>` tag."
    why_human: "test_enriched_prompt_reaches_acomplete_end_to_end (projekt-forge) mocks acomplete and asserts the constraint propagates in the `system=` kwarg. A live-log check gives independent evidence that nothing between acomplete and Ollama strips it."
---

# Phase 6: Learning Pipeline Integration — Verification Report

**Phase Goal:** projekt-forge's startup wires forge-bridge's learning pipeline with forge's own LLM config, log path, and storage callback — so synthesis uses forge's Ollama instance and logs persist separately from the standalone bridge process

**Verified:** 2026-04-17
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (merged from ROADMAP success criteria + plan frontmatter must-haves)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | Two forge-bridge processes running simultaneously write to distinct execution log files with no cross-contamination of promotion counts (ROADMAP SC #1) | VERIFIED (automated invariant) + NEEDS HUMAN (live two-process test) | `projekt-forge/tests/test_learning_wiring.py::test_two_execution_logs_at_different_paths_are_isolated` asserts two `ExecutionLog` instances at different paths keep independent `_counters` dicts and independent JSONL files. Passes (8/8 in test_learning_wiring.py). See `human_verification` for live smoke. |
| 2   | projekt-forge startup constructs `LLMRouter` with values from `forge_config.yaml` (not env vars) and synthesis requests route to forge's configured Ollama instance (ROADMAP SC #2) | VERIFIED (wiring) + NEEDS HUMAN (live Ollama) | `projekt-forge/projekt_forge/learning/wiring.py::_build_llm_router` reads `cfg["llm"].get("local_url"/"local_model"/"system_prompt")` and passes them to `LLMRouter(...)`. Test `test_init_learning_pipeline_with_config` asserts `router.local_url == "http://ollama.test/v1"`. Live Ollama routing is unverifiable without a running server. |
| 3   | An execution routed through projekt-forge fires the registered storage callback and the event appears in projekt-forge's storage, not only in the JSONL file (ROADMAP SC #3) | VERIFIED (scope-reduced per documented deferral) | `projekt_forge.learning.wiring._persist_execution` is registered via `execution_log.set_storage_callback(_persist_execution)` in `init_learning_pipeline`. Test `test_init_registers_storage_callback` asserts the callback fires (emits `"execution mirrored"` INFO log). Per CONTEXT.md §Out of scope + REQUIREMENTS.md §Future, EXT-03 (SQL backend) is explicitly deferred to v1.1.x; Phase 6 scope is "projekt-forge's log stream" (logger-only stub). Migration path documented in-line in `_persist_execution` docstring. |
| 4   | A synthesis triggered via projekt-forge passes through the `pre_synthesis_hook` and the enriched prompt is visible in synthesis logs (ROADMAP SC #4) | VERIFIED (end-to-end via test) + NEEDS HUMAN (live log inspection) | `test_enriched_prompt_reaches_acomplete_end_to_end` builds the real pipeline via `init_learning_pipeline()`, mocks only `router.acomplete`, runs `await synth.synthesize(...)`, and asserts `"do not import flame" in kwargs["system"]`. Passes. Live log verification deferred to human. |
| 5   | Consumers can register a single sync or async callback on an ExecutionLog instance that fires after every `.record()` write (LRN-02 truth from 06-01 must_haves) | VERIFIED | `ExecutionLog.set_storage_callback()` at `forge_bridge/learning/execution_log.py:117-141` — accepts `fn: Optional[StorageCallback]`; detects sync vs async via `inspect.iscoroutinefunction(fn)`; caches `_storage_callback_is_async`. Tested by `test_storage_callback_fires_on_record` (sync), `test_async_storage_callback_fires_on_record` (async), and 3 more tests. |
| 6   | A failing storage callback never prevents the JSONL line from being written (LRN-02) | VERIFIED | `record()` at `execution_log.py:193-227` writes JSONL under `fcntl.LOCK_EX`, releases `LOCK_UN`, then dispatches callback. Sync path wraps in `try/except Exception → logger.warning(exc_info=True)`. Async path uses `asyncio.ensure_future` + `add_done_callback(_log_callback_exception)` with explicit `RuntimeError` catch for no-loop case. Covered by `test_storage_callback_error_does_not_break_jsonl_write`. |
| 7   | The callback receives the full ExecutionRecord frozen dataclass, not a dict (LRN-02) | VERIFIED | `ExecutionRecord` at `execution_log.py:33-46` is `@dataclass(frozen=True)` with 5 fields (code_hash, raw_code, intent, timestamp, promoted). `record()` constructs the dataclass FIRST and then writes `json.dumps(asdict(record))` so JSONL schema = dataclass shape. `test_callback_receives_full_execution_record` asserts all 5 fields populated correctly. |
| 8   | SkillSynthesizer constructor accepts `pre_synthesis_hook=None` by default (no behavior change) and an async callable otherwise (LRN-04) | VERIFIED | Constructor at `synthesizer.py:241-253` — `pre_synthesis_hook: PreSynthesisHook \| None = None` kwarg stored as `self._pre_synthesis_hook` with no fallback. Tested by `test_pre_synthesis_hook_none_is_noop` (constructor defaults). |
| 9   | When a hook is registered, it is invoked with `(intent, params)` before the LLM `acomplete()` call and its returned `PreSynthesisContext` modifies system_prompt and user_prompt additively (LRN-04) | VERIFIED | `synthesize()` at `synthesizer.py:271-310` — invokes `await self._pre_synthesis_hook(intent or "", {"raw_code": raw_code, "count": count})` BEFORE `acomplete`. Constraints append as "Constraints:" block; extra_context appends; examples prepend. Base `SYNTH_SYSTEM` / `SYNTH_PROMPT` never replaced (grep confirms `SYNTH_PROMPT.format(` appears exactly once). Tests: `test_pre_synthesis_hook_invoked_with_intent_and_params`, `test_pre_synthesis_context_extra_context_appended_to_system`, `test_pre_synthesis_context_constraints_injected`. |
| 10  | A raising hook falls back to a default `PreSynthesisContext()` and synthesis continues (LRN-04) | VERIFIED | `synthesizer.py:274-285` — `try/except Exception` around the hook await, `logger.warning("pre_synthesis_hook raised — falling back to empty context", exc_info=True)`, then `ctx = PreSynthesisContext()`. Covered by `test_pre_synthesis_hook_exception_falls_back_to_empty_context`. |
| 11  | `PreSynthesisContext` is a frozen dataclass with exactly 4 fields (extra_context, tags, examples, constraints), all defaulting to empty (LRN-04) | VERIFIED | `synthesizer.py:64-86` — `@dataclass(frozen=True)` with 4 fields. Smoke test: `FrozenInstanceError` raised on attribute assignment; all fields default to empty string/list. `tests/test_public_api.py::test_phase6_symbols_importable_from_root` asserts `PreSynthesisContext.__dataclass_params__.frozen is True`. |
| 12  | Consumers can import `PreSynthesisContext`, `PreSynthesisHook`, `ExecutionRecord`, and `StorageCallback` from the forge_bridge package root (06-03 public API) | VERIFIED | `forge_bridge/__init__.py:28-37` re-exports all four symbols. `__all__` at lines 47-68 contains 15 entries including the four new ones. Smoke: `from forge_bridge import ExecutionRecord, StorageCallback, PreSynthesisContext, PreSynthesisHook` succeeds; `len(forge_bridge.__all__) == 15`. |
| 13  | The forge-bridge package version is bumped so projekt-forge can depend on the new symbols via a pinned version (06-03) | VERIFIED | `pyproject.toml:7` → `version = "1.1.0"`. `grep '^version = "1.0.1"$' pyproject.toml` → zero matches. `pip show forge-bridge` in forge env → `Version: 1.1.0`. |
| 14  | An annotated git tag `v1.1.0` exists on origin so downstream consumers can resolve the forge-bridge git-URL pin at install time (06-03) | VERIFIED | `git ls-remote --tags origin v1.1.0` → `df972506... refs/tags/v1.1.0`. Local `git cat-file -t v1.1.0` → `tag` (annotated). Message contains `LRN-02` and `LRN-04`. Target commit `76b50f135a3e94ff1d8e8f76702298a61000fa62` = Task 2 test commit. |
| 15  | projekt-forge startup reads forge_config.yaml and constructs LLMRouter + ExecutionLog + SkillSynthesizer with hooks registered; invoked from both `_run_mcp_only` and `_run_mcp_server` (06-04 consumer wiring) | VERIFIED | `projekt_forge/__main__.py` lines 31-32 (`_run_mcp_only`) and 54-55 (`_run_mcp_server`) both call `init_learning_pipeline(args)` inside a try/except that falls through on failure (T-06-16 mitigation). `projekt_forge/learning/wiring.py::init_learning_pipeline` tolerates `load_forge_config` RuntimeError (falls back to env/defaults), builds router + log + synthesizer, registers `_persist_execution` via `set_storage_callback`, and passes `_build_pre_synthesis_context` as `pre_synthesis_hook`. Tests 1-6 in `test_learning_wiring.py` cover the contract. |

**Score:** 15/15 truths verified automatically. Three truths (SC #1, SC #2, SC #4) also carry live-smoke items routed to human verification.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `forge_bridge/learning/execution_log.py` | ExecutionRecord frozen dataclass + StorageCallback alias + `set_storage_callback()` + post-flush dispatch | VERIFIED | 261 lines; `ExecutionRecord` at lines 33-46 (frozen); `StorageCallback = Callable[[ExecutionRecord], Union[None, Awaitable[None]]]` at line 49; `set_storage_callback` at lines 117-141; dispatch at lines 211-227 (after `fcntl.LOCK_UN`). |
| `forge_bridge/learning/synthesizer.py` | PreSynthesisContext frozen dataclass + PreSynthesisHook alias + constructor param + hook invocation + additive prompt composition + tags sidecar | VERIFIED | Class at 64-86 (frozen, 4 fields); alias at 89; constructor at 241-253 with `pre_synthesis_hook` kwarg; hook invoked at 271-310; `.tags.json` sidecar at 367-371; `SYNTH_PROMPT.format(` appears exactly once (D-11 invariant). |
| `forge_bridge/__init__.py` | 4 new top-level re-exports + 15-entry `__all__` | VERIFIED | Multi-line imports at 28-37; `__all__` at 47-68 with 15 entries; `PreSynthesisContext`, `PreSynthesisHook`, `ExecutionRecord`, `StorageCallback` all present. |
| `pyproject.toml` | Version bumped to 1.1.0 | VERIFIED | Line 7: `version = "1.1.0"`. |
| `tests/test_execution_log.py` | 5 new storage-callback tests | VERIFIED | All 5 tests present at lines 226, 245, 266, 290, 305. 21 tests total in file, all pass. |
| `tests/test_synthesizer.py` | 5 new pre_synthesis_hook tests | VERIFIED | `class TestPreSynthesisHook` at line 268 with 5 methods. 25 tests total, all pass. |
| `tests/test_public_api.py` | 2 new Phase 6 tests + updated `test_all_contract` + updated `test_package_version` | VERIFIED | `test_phase6_symbols_importable_from_root` (line 234), `test_public_surface_has_15_symbols` (line 266). `test_all_contract` updated to 15-symbol set; `test_package_version` asserts 1.1.0. |
| `/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/learning/__init__.py` | Package marker | VERIFIED | Docstring-only package marker; file exists. |
| `/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/learning/wiring.py` | init_learning_pipeline + builders + hooks + accessors | VERIFIED | 224 lines; all 9 symbols importable (`init_learning_pipeline`, `get_execution_log`, `get_synthesizer`, `get_router`, `_persist_execution`, `_build_pre_synthesis_context`, `_build_llm_router`, `_build_execution_log`, `_reset_for_testing`). |
| `/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py` | init_learning_pipeline invoked in both MCP lifecycles | VERIFIED | `_run_mcp_only` lines 31-34; `_run_mcp_server` lines 54-57. Both invoke inside try/except that logs-and-continues on failure. |
| `/Users/cnoellert/Documents/GitHub/projekt-forge/tests/test_learning_wiring.py` | 8 tests (6 wiring + SC #1 + SC #4) | VERIFIED | 8 tests present. All pass (`pytest tests/test_learning_wiring.py -x` → `8 passed`). |
| `/Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml` | forge-bridge pin bumped to @v1.1.0 + allow-direct-references=true | VERIFIED | Line 25: `"forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.1.0"`. `[tool.hatch.metadata] allow-direct-references = true` at lines 39-44. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `forge_bridge/learning/execution_log.py::record` | `self._storage_callback` | sync call OR asyncio.ensure_future + add_done_callback, AFTER fcntl.LOCK_UN | WIRED | Dispatch at 211-227; fires after `LOCK_UN` at 208; both modes isolate exceptions. |
| `forge_bridge/learning/synthesizer.py::SkillSynthesizer.synthesize` | `self._pre_synthesis_hook` | `await` call before `self._router.acomplete()` | WIRED | `await self._pre_synthesis_hook(intent or "", {...})` at 276-279; base prompt composition at 287-310; `acomplete` at 314-319. |
| `forge_bridge/__init__.py` | `forge_bridge.learning.synthesizer` | `from ... import PreSynthesisContext, PreSynthesisHook, SkillSynthesizer` | WIRED | Lines 33-37 (multi-line import). |
| `forge_bridge/__init__.py` | `forge_bridge.learning.execution_log` | `from ... import ExecutionLog, ExecutionRecord, StorageCallback` | WIRED | Lines 28-32. |
| forge-bridge repo HEAD after version bump | `origin refs/tags/v1.1.0` | `git tag -a v1.1.0 -m '...' && git push origin v1.1.0` | WIRED | Tag `df972506...` on origin; annotated; message carries LRN-02/LRN-04 provenance; targets commit `76b50f1` which has `version = "1.1.0"` in pyproject.toml. |
| `projekt_forge/__main__.py` | `projekt_forge.learning.wiring` | `from projekt_forge.learning.wiring import init_learning_pipeline; init_learning_pipeline(args)` | WIRED | Two call sites (lines 31-32, 54-55) wrapped in try/except. |
| `projekt_forge/learning/wiring.py` | `forge_bridge` (site-packages v1.1.0) | `from forge_bridge import LLMRouter, ExecutionLog, ExecutionRecord, PreSynthesisContext, SkillSynthesizer` | WIRED | Lines 20-26; `pip show forge-bridge → Version: 1.1.0` confirms installed. |
| `projekt_forge/learning/wiring.py` | `projekt_forge.config.forge_config.load_forge_config` | deferred import + try/except RuntimeError | WIRED | Line 170 (local import inside `init_learning_pipeline`); RuntimeError handled at 172-176 (falls through to `cfg = {}`). |
| `projekt_forge/pyproject.toml` | forge-bridge git ref v1.1.0 on origin | `forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.1.0` | WIRED | Line 25. Preserves PEP 508 direct-reference form per REQUIREMENTS.md §Out of Scope. Hatchling accepts it via `allow-direct-references = true` at 44. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `ExecutionLog.record()` | `record` (ExecutionRecord) | Built inline from `(code, intent, now, False, hash)` | Real, non-empty on every call | FLOWING |
| `ExecutionLog.record()` → callback dispatch | `self._storage_callback` | `set_storage_callback()` registration | Flows when registered (tested); no-op when None | FLOWING |
| `SkillSynthesizer.synthesize()` | `ctx` (PreSynthesisContext) | `await self._pre_synthesis_hook(intent, params)` or default `PreSynthesisContext()` | Real when hook registered; default empty otherwise — both paths flow additively into `system_prompt`/`user_prompt` | FLOWING |
| `synthesize()` → `acomplete` | `system_prompt`, `user_prompt` | Composed from `SYNTH_SYSTEM + ctx.constraints + ctx.extra_context` and `SYNTH_PROMPT.format(...) + ctx.examples` | End-to-end asserted by SC #4 test — `do not import flame` present in `kwargs["system"]` | FLOWING |
| `projekt_forge/learning/wiring.py::init_learning_pipeline` | `cfg` | `load_forge_config()` (tolerates RuntimeError) | Real when config exists; empty dict otherwise (falls back to env/defaults inside LLMRouter and `_resolve_log_path`) | FLOWING |
| `projekt_forge/learning/wiring.py::_persist_execution` | `record` (ExecutionRecord) | forge-bridge `ExecutionLog.record()` dispatch | Real — `logger.info("execution mirrored: ...")` emitted (scope-reduced per CONTEXT.md §Out of scope; EXT-03 DB write deferred) | FLOWING (scope-reduced) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| `forge_bridge.__all__` has 15 entries, all 4 new symbols importable, both frozen dataclasses verified | `python -c "import forge_bridge; ..."` | `len(__all__) == 15`, FrozenInstanceError raised on mutation, version == 1.1.0 | PASS |
| forge-bridge Phase 6 test subset | `pytest tests/test_execution_log.py tests/test_synthesizer.py tests/test_public_api.py -x` | `61 passed, 1 warning` | PASS |
| forge-bridge full suite regression | `pytest tests/` | `219 passed, 2 warnings` | PASS |
| projekt-forge wiring tests | `pytest tests/test_learning_wiring.py -x` (cwd=projekt-forge) | `8 passed, 1 warning` | PASS |
| projekt-forge full suite regression | `pytest tests/` (cwd=projekt-forge) | `422 passed, 3 xfailed, 1 warning` | PASS |
| Async storage callback fires with ExecutionRecord | In-process asyncio script | Callback called once, payload is ExecutionRecord with correct fields | PASS |
| `PreSynthesisContext` frozen invariant | In-process mutation attempt | `FrozenInstanceError` | PASS |
| `projekt_forge.learning.wiring` importable (site-packages-installed forge-bridge) | `python -c "from projekt_forge.learning.wiring import ..."` | All 9 symbols import | PASS |
| Annotated v1.1.0 tag on origin | `git ls-remote --tags origin v1.1.0` | `df972506... refs/tags/v1.1.0` | PASS |
| forge-bridge installed as 1.1.0 in forge env | `pip show forge-bridge` | `Version: 1.1.0` | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| LRN-01 | 06-04 (consumer-side; bridge side was D-15 already satisfied) | ExecutionLog path is configurable at construction (no hardcoded ~/.forge-bridge/executions.jsonl) | SATISFIED | `projekt_forge.learning.wiring._resolve_log_path` priority: cfg > FORGE_PROJECT_ROOT > default. Tests `test_init_learning_pipeline_with_config` and `test_init_learning_pipeline_tolerates_missing_config` cover both paths. Bridge-side API was already constructor-injectable (D-15). |
| LRN-02 | 06-01 (bridge API) + 06-03 (public re-export) + 06-04 (consumer wiring) | projekt-forge startup calls set_execution_callback() with its own storage callback | SATISFIED | `ExecutionLog.set_storage_callback()` added; `ExecutionRecord` frozen dataclass delivered; re-exported at package root; projekt-forge registers `_persist_execution` via `init_learning_pipeline`. Test coverage: 5 bridge-side tests + 1 consumer-side test. Name deviation from REQUIREMENTS.md ("set_execution_callback" → "set_storage_callback") is intentional: the existing `set_execution_callback` on `bridge.py` is a Flame-bridge hook for every `execute()` call; the new learning-pipeline hook lives on `ExecutionLog` and uses the clearer name. Documented in 06-CONTEXT.md §decisions D-01. |
| LRN-03 | 06-04 (consumer-side) | projekt-forge constructs LLMRouter with forge config values and injects into Synthesizer | SATISFIED | `_build_llm_router(cfg)` reads `cfg["llm"]` keys and passes to `LLMRouter(...)`. `init_learning_pipeline` constructs `SkillSynthesizer(router=router, pre_synthesis_hook=...)`. Test `test_init_learning_pipeline_with_config` asserts config values flow through. |
| LRN-04 | 06-02 (bridge API) + 06-03 (public re-export) + 06-04 (consumer wiring) | SkillSynthesizer supports a pre_synthesis_hook for prompt enrichment from projekt-forge's DB context | SATISFIED | `pre_synthesis_hook` kwarg added to `SkillSynthesizer.__init__`; `PreSynthesisContext` frozen dataclass delivered; re-exported; projekt-forge registers `_build_pre_synthesis_context`. SC #4 test confirms end-to-end flow. DB-context enrichment is stubbed (logger-only mirror + `do not import flame` constraint + optional project tag from env); full DB read deferred per CONTEXT.md §Out of scope. |

All 4 phase requirements SATISFIED. No orphaned REQUIREMENTS.md IDs for Phase 6.

### Anti-Patterns Found

From 06-REVIEW.md (3 warnings, 6 info, 0 critical) — surfaced here for transparency; none block goal achievement:

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `tests/test_execution_log.py` | (new section) | Async failure paths (ensure_future RuntimeError, done_callback exception) lack dedicated tests | Info | Sync failure path covered; async invariants validated indirectly via the async-dispatch test, but explicit failure coverage is absent. Does not block phase goal — implementation is correct, test coverage is a polish item. |
| `forge_bridge/learning/execution_log.py` | 34-46, 236-248 | `ExecutionRecord` docstring claims it "mirrors the JSONL on-disk schema exactly" but `mark_promoted()` writes a 3-field shape that is not dataclass-built | Info | Functionally correct — `_replay()` handles both shapes. Doc drift risk only. |
| `tests/conftest.py` | 14-18 | `sys.path.insert(0, repo_root)` as Rule 3 workaround for site-packages shadow | Info | Test-only; does not affect production behavior. Makes the test suite non-portable against an installed wheel. Flagged in REVIEW.md for follow-up. |
| `forge_bridge/learning/synthesizer.py` | 14 | `import json as _json` alias with no actual shadow to avoid | Info | Cosmetic. |
| `forge_bridge/learning/synthesizer.py` | 21, 145, 243-245 | Mixed `Optional[X]` and `X \| None` typing styles | Info | Cosmetic. |
| `tests/test_public_api.py` | 247-248 | `PreSynthesisHook is not None` is a trivial assertion for a type alias | Info | Test-only nit. |
| `tests/test_synthesizer.py` | 311-356 | Prompt composition ORDER (constraints before extra_context; examples before base) not explicitly tested | Info | Existing tests cover presence, not ordering. D-11 invariant holds by code structure. |
| `forge_bridge/learning/synthesizer.py` | 367-371 | `.tags.json` sidecar write path has no test coverage | Info | Write path exists and is deterministic; no regression test. |
| `tests/test_execution_log.py` | 96 | `log.mark_promoted(log._counters.copy().popitem()[0])` reaches into private state | Info | Pre-existing test; not a Phase 6 regression. |

None of these rise to Blocker or Warning severity that would affect goal achievement.

### Human Verification Required

See `human_verification` frontmatter for structured items. Summary:

1. **SC #1 — Live cross-process log isolation.** Run `python -m forge_bridge` (standalone) and `python -m projekt_forge --no-db` (with `FORGE_PROJECT_ROOT=/tmp/forge-a` set) simultaneously. Confirm each process writes to its own JSONL path and promotion counters do not cross-contaminate. Automated in-process invariant passes; live two-process test requires human.

2. **SC #2 — Synthesis hits forge's Ollama instance.** With a real `forge_config.yaml` pointing `llm.local_url` at forge's Ollama, trigger synthesis (execute the same Flame code pattern 3 times through projekt-forge). Confirm the synthesized tool is generated by forge's configured model. Unit tests assert config-flow; live routing requires a running Ollama.

3. **SC #4 — Enriched prompt visible in synthesis logs.** With projekt-forge running and `FORGE_PROJECT_CODE=ACM_12345` set, trigger synthesis. Observe the emitted synthesis log / Ollama request body and confirm the hook-injected constraint (`do not import flame`) and the `project:ACM_12345` tag propagate. End-to-end test mocks acomplete; live log check adds independent evidence.

### Gaps Summary

No automated gaps. All 15 must-haves verified in-process; all 4 requirement IDs (LRN-01, LRN-02, LRN-03, LRN-04) SATISFIED; all key links WIRED; all behavioral spot-checks PASS; `v1.1.0` tag published on origin; both repos' full test suites green (forge-bridge 219/219, projekt-forge 422/422 + 3 xfailed).

Phase 6 is functionally complete. The `human_needed` status reflects live-smoke verification of 3 ROADMAP success criteria that require running servers / Ollama / MCP client to observe end-to-end behavior beyond what unit tests can assert. The Task 4 checkpoint inside 06-04-SUMMARY.md records that cnoellert ran the live startup smoke on 2026-04-18 and observed all 3 expected INFO lines; this VERIFICATION.md surfaces the full structured list so the human sign-off is auditable.

---

_Verified: 2026-04-17_
_Verifier: Claude (gsd-verifier)_
