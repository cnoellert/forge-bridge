---
status: complete
phase: 06-learning-pipeline-integration
source: [06-VERIFICATION.md]
started: 2026-04-18T00:00:00Z
updated: 2026-04-18T08:30:00Z
completed: 2026-04-18T08:30:00Z
---

## Current Test

[complete — all 3 tests passed]

## Tests

### 1. SC #1 — live cross-process log isolation
expected: Running two forge-bridge processes simultaneously (one standalone via `python -m forge_bridge`, one via projekt-forge with `FORGE_PROJECT_ROOT` set) writes to two distinct JSONL paths with no cross-contamination of promotion counts.
result: passed
evidence: |
  Two terminals ran `/tmp/sc1_isolation.py` with different `FORGE_PROJECT_ROOT` values.
  - Terminal A (no env): wrote to `~/.forge-bridge/executions.jsonl`, counter `{e0d0d6fa...: 15}`
  - Terminal B (`/tmp/forge-test-project`): wrote to `/tmp/forge-test-project/.forge/executions.jsonl`, counter `{e0d0d6fa...: 10, 753b2045...: 3}`
  Distinct paths confirmed. Counter values diverge (15 vs 10 vs 3); B has a second hash A doesn't have at all — proves per-instance state, no cross-contamination. Common hash across processes is expected because `normalize_and_hash` strips string literals (`print('standalone_0')` and `print('projekt_forge_0')` both normalize to `print('')`).

### 2. SC #2 — synthesis request hits forge's Ollama instance
expected: With `forge_config.yaml` pointing `llm.local_url` at forge's real Ollama server, trigger a synthesis (execute the same Flame code pattern 3 times through projekt-forge). The synthesized tool is generated by forge's configured model, not forge-bridge's standalone default.
result: passed
evidence: |
  Ran `/tmp/sc2_ollama_routing.py` against `/tmp/forge-test-config.yaml` → `http://assist-01:11434/v1`, `qwen2.5-coder:32b`.
  Critical log: `INFO httpx HTTP Request: POST http://assist-01:11434/v1/chat/completions "HTTP/1.1 200 OK"`.
  Wiring logs confirm `LLMRouter configured: local_url=http://assist-01:11434/v1, local_model=qwen2.5-coder:32b` and storage callback fired 3× (`execution mirrored: code_hash=753b2045c340 intent='rename segment' promoted=False`) — also satisfying SC #3's scope-reduced log-stream form.
  Downstream `synth output: None` on this run was the synthesizer's name-collision guard rejecting an overwrite of an existing file, not a routing failure. The HTTP 200 from assist-01 is the SC #2 proof.

### 3. SC #4 — enriched prompt visible in live synthesis logs
expected: With projekt-forge running, trigger synthesis with `FORGE_PROJECT_CODE` set and observe that the emitted synthesis log line / Ollama request body carries the hook-injected constraint (`do not import flame`) and the `project:<code>` tag.
result: passed
evidence: |
  Ran `/tmp/sc4_with_trace.py` (LLMRouter.acomplete wrapper dumps the exact system/user prompts sent to Ollama) with `FORGE_PROJECT_CODE=ACM`. The printed `SYSTEM PROMPT SENT TO OLLAMA:` block contained the base `SYNTH_SYSTEM` plus an appended `Constraints:\n- do not import flame` block — exactly what `projekt_forge.learning.wiring._build_pre_synthesis_context` returns. HTTP 200 from assist-01, file written to `~/.forge-bridge/synthesized/synth_rename_segment.py`, `synth output:` resolved to a real path. End-to-end path from hook → additive composition → Ollama confirmed.

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
