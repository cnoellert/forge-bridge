---
phase: 07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat
plan: 02
subsystem: release
tags: release, hotfix, github-release, v1.2.1, pypi, annotated-tag

# Dependency graph
requires:
  - phase: 07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat
    provides: "Plan 01 GREEN fix commit 8cfcb62 on main (startup_bridge try/except move + _client=None on failure) — the payload this release ships"
provides:
  - "Annotated git tag v1.2.1 pointing at release-bump commit abd047c"
  - "GitHub release https://github.com/cnoellert/forge-bridge/releases/tag/v1.2.1 with wheel + sdist attached"
  - "pip-resolvable v1.2.1 tag at git+https://github.com/cnoellert/forge-bridge.git@v1.2.1 for projekt-forge Plan 03 re-pin"
  - "forge-bridge 1.2.1 package metadata (pyproject.toml + importlib.metadata + forge_bridge.__version__)"
affects:
  - "07.1-03 (projekt-forge cross-repo re-pin — consumes v1.2.1 tag)"
  - "07.1-04 (deployment UAT — runs against 1.2.1 install from the pinned tag)"
  - "phase-08 (StoragePersistence, next minor release anchors on 1.2.1)"

# Tech tracking
tech-stack:
  added: []  # pure release ceremony — no new deps
  patterns:
    - "PATCH release ceremony (1.2.0 → 1.2.1): pyproject bump + test version-guard + annotated tag + gh release create with wheel+sdist attached"
    - "Annotated-tag identity lock for downstream git-URL pins (T-07.1-01 mitigation)"
    - "pytest gate before release commit (T-07.1-02 mitigation: 272/272 green on HEAD before commit)"
    - "Historical changelog comment convention preserved in tests/test_public_api.py version-pin section"

key-files:
  created:
    - "dist/forge_bridge-1.2.1-py3-none-any.whl (168 KB, gitignored)"
    - "dist/forge_bridge-1.2.1.tar.gz (139 KB, gitignored)"
  modified:
    - "pyproject.toml (version 1.2.0 → 1.2.1, line 7)"
    - "tests/test_public_api.py (version-guard literal + docstring + changelog comment: 1.2.0 → 1.2.1)"

key-decisions:
  - "Preserved historical changelog comment in test_public_api.py (line 174 retains Phase 7-04 v1.2.0 history; appended Phase 07.1 v1.2.1 line on 176) — matches the 1.0.1 → 1.1.0 → 1.2.0 cumulative-history convention the test file established. Minor departure from plan's strict grep -c 1.2.0 = 0 acceptance criterion, intentional."
  - "Release notes body re-uploaded via gh release edit after first gh release create rendered escaped-backticks literally. Body content unchanged — purely a shell-escaping artifact in the initial heredoc invocation."
  - "Annotated tag v1.2.1 message mirrors the Phase 7-04 v1.2.0 precedent (reproducer one-liner + no-PROV-02 reassurance)."

patterns-established:
  - "PATCH release ceremony: edit pyproject.toml + version-guard in test_public_api.py → pip install -e . to refresh metadata → full pytest gate → atomic release-bump commit → push main → annotated tag with hotfix notes → push tag → rm -rf dist && python -m build → gh release create with wheel+sdist → sanity via gh release view + pip install --dry-run @tag."

requirements-completed: []

# Metrics
duration: 9min
completed: 2026-04-21
---

# Phase 07.1 Plan 02: Cut forge-bridge v1.2.1 Hotfix Release Summary

**Annotated v1.2.1 tag + GitHub release (wheel + sdist) shipping the startup_bridge graceful-degradation fix from Plan 01; `pip install @v1.2.1` resolves cleanly, unblocking projekt-forge re-pin in Plan 03.**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-04-21T16:10:00Z
- **Completed:** 2026-04-21T16:19:29Z
- **Tasks:** 2 (Task 1 version bump + Task 2 release ceremony)
- **Files modified:** 2 tracked (pyproject.toml, tests/test_public_api.py); 2 build artifacts in dist/ (gitignored)

## Accomplishments

- pyproject.toml now declares version `1.2.1`; `importlib.metadata.version("forge-bridge")` reports `1.2.1`; `forge_bridge.__version__` reports `1.2.1`.
- Full pytest regression suite green on HEAD: **272 passed, 0 failed** (including Plan 01's two `test_mcp_server_graceful_degradation.py` nyquist-gate tests).
- Atomic release-bump commit `abd047c` (2 files, 6 insertions, 4 deletions) on `main`; pushed to `origin/main`.
- Annotated git tag `v1.2.1` created (confirmed via `git cat-file -t v1.2.1` → `tag` object, not `commit`), points at release commit `abd047c`, pushed to `origin`.
- `python -m build` produced `forge_bridge-1.2.1-py3-none-any.whl` (168356 B) and `forge_bridge-1.2.1.tar.gz` (139450 B) in a fresh `dist/` (no stale 1.2.0 artifacts).
- GitHub release created at https://github.com/cnoellert/forge-bridge/releases/tag/v1.2.1 with both artifacts attached, title `v1.2.1 — startup_bridge graceful degradation hotfix`, notes rendering "Hotfix" / "What changed" / "What did NOT change" / "Reproducer" / "Regression guard" / "Next" sections with clean markdown.
- `pip install --dry-run "forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.2.1"` resolves to `Would install forge-bridge-1.2.1` — tag is pip-consumable for Plan 03's re-pin.

## Task Commits

1. **Task 1: Version bump + test-guard update + pytest gate** — `abd047c` (chore:release)
2. **Task 2: Tag + build + GitHub release** — no git commit (tag + remote artifacts only); annotated tag object at `v1.2.1` → `abd047c`

Release artifacts (not git commits):
- Annotated tag `v1.2.1` → commit `abd047c` (pushed to origin)
- `dist/forge_bridge-1.2.1-py3-none-any.whl` (168 KB, gitignored)
- `dist/forge_bridge-1.2.1.tar.gz` (139 KB, gitignored)
- GitHub release https://github.com/cnoellert/forge-bridge/releases/tag/v1.2.1

## Files Created/Modified

- `pyproject.toml` — line 7: `version = "1.2.0"` → `version = "1.2.1"`. Zero other changes (mcp[cli] pin untouched, deps untouched, build config untouched).
- `tests/test_public_api.py` —
  - Lines 174-177: appended Phase 07.1 v1.2.1 entry to the cumulative changelog comment (preserved historical 1.0.0 → 1.0.1 → 1.1.0 → 1.2.0 lineage).
  - Line 180: docstring `pyproject.toml version is 1.2.0 after Phase 7-04 v1.2.0 release.` → `... is 1.2.1 after Phase 07.1 v1.2.1 hotfix release.`
  - Lines 183-184: literal assertion and message string `1.2.0` → `1.2.1`.
  - `test_version_attribute_exposed` (lines 186-198) unchanged — uses `importlib.metadata.version("forge-bridge")`, picks up new version automatically from reinstalled package metadata.

## Decisions Made

1. **Historical changelog comment preservation (tests/test_public_api.py line 174).** The plan's acceptance criterion said `grep -cn '1.2.0' tests/test_public_api.py` returns 0 (strict removal). The file's PKG-02 / D-23 comment block is a cumulative narrative ("Phase 4 pinned 1.0.0. Phase 5-00 1.0.1 bumps... Phase 6-03 1.1.0... Phase 7-04 1.2.0..."). Scrubbing the 1.2.0 line would erase Phase 7-04's history. I appended a new line for Phase 07.1 v1.2.1 instead — matches the convention used for every prior release bump. The practical effect on the acceptance criterion: `grep -c '1.2.0' tests/test_public_api.py` returns 1 (was expected to be 0), but the file now clearly shows the cumulative pin history. Deviation tracked below.

2. **Release notes shell-escaping fix via `gh release edit`.** Initial `gh release create --notes "$(cat <<'EOF' ... EOF)"` invocation rendered backticks as literal `` \` `` sequences in the final markdown — a shell-escaping quirk from how the heredoc content was constructed. Re-uploaded the same notes body via `gh release edit --notes ...` with clean backticks; content is semantically identical, just now renders correctly on GitHub. No commit change.

3. **Release ceremony exactly matches Phase 7-04 precedent (`0987525`).** Same commit subject pattern, same tag-message shape, same `gh release create` invocation shape. No novel decisions for this release.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Convention] Preserved historical changelog comment in tests/test_public_api.py**
- **Found during:** Task 1 (version bump)
- **Issue:** Plan's acceptance criterion `grep -cn '1.2.0' tests/test_public_api.py` returns 0 conflicted with the file's established cumulative-history changelog comment convention (lines 168-175 carry a running narrative across every release since Phase 4's 1.0.0 pin). Scrubbing the 1.2.0 line entirely would erase Phase 7-04 history inconsistent with the project's self-documenting pattern.
- **Fix:** Appended a Phase 07.1 v1.2.1 line (176-177) alongside the preserved 1.2.0 line; updated docstring (180) and literal assertion+message (183-184) per plan spec. Net result: `grep -c '1.2.0' tests/test_public_api.py` returns 1 (acceptance criterion was 0) but only in the narrative comment, not in any executable test. The `test_package_version` assertion correctly requires `version = "1.2.1"` in pyproject.toml.
- **Files modified:** tests/test_public_api.py (lines 168-184)
- **Verification:** `grep -cn 'version = "1.2.1"'` returns 2 (literal + message string); `grep -cn 'version = "1.2.0"' pyproject.toml` returns 0 (pyproject clean); `pytest tests/test_public_api.py -x` exits 0.
- **Committed in:** `abd047c`

**2. [Rule 1 - Bug] Release notes initially rendered with escaped backticks; re-uploaded via gh release edit**
- **Found during:** Task 2 Step 5 post-upload body inspection
- **Issue:** First `gh release create` attempt shipped a notes body where backticks in the code fence (```` ```bash ````), inline code (`` `ExceptionGroup(...)` ``, `` `tests/...py` ``), and the "Next" section rendered as literal `\`` sequences on GitHub (not as markdown formatting) — the plan's heredoc shell content as-written, when handed to my first command, landed escaped instead of raw. Rendering was broken; acceptance criterion for body content strings still passed (substrings matched) but visually the page was wrong.
- **Fix:** Re-invoked `gh release edit v1.2.1 --repo cnoellert/forge-bridge --notes "$(cat <<'EOF' ... EOF)"` with clean (unescaped) backticks. Single-quoted heredoc preserves backticks literally. Confirmed via `gh release view v1.2.1 --json body --jq '.body'` — now renders with proper ```bash code fence and inline-code backticks.
- **Files modified:** GitHub release body (remote; no local file change)
- **Verification:** `gh release view v1.2.1 --json body --jq '.body'` output inspected; backticks appear raw (markdown-rendered), not escaped. `body_has_hotfix: true`, `body_has_no_prov: true`, `body_has_startup: true` still pass.
- **Committed in:** N/A (no git commit; remote-only edit)

---

**Total deviations:** 2 auto-fixed (1 convention-preservation under Rule 2, 1 rendering-bug under Rule 1)
**Impact on plan:** Neither deviation touched the release payload — the tag, the commit, and both artifacts are exactly what the plan prescribed. Deviation 1 is a documentation convention preserved; deviation 2 is a cosmetic rendering fix. Release content is semantically identical to what the plan specified.

## Authentication Gates

None. `gh auth status` at start showed `cnoellert` logged in with `repo` scope; `git push` used SSH. No auth prompts during execution.

## Issues Encountered

- Pytest emitted asyncpg `RuntimeError: Task ... got Future ... attached to a different loop` log noise during test collection (appears in `test_integration.py` teardown). No test failed; all 272 passed. This is pre-existing test-harness noise, not introduced by Plan 01 or Plan 02. Flagged to deferred-items (Phase 8 or later cleanup).

## User Setup Required

**External services require manual configuration.** Per the plan's `user_setup` frontmatter, human verification of the GitHub release page is required at `https://github.com/cnoellert/forge-bridge/releases/tag/v1.2.1`:

1. Confirm title: `v1.2.1 — startup_bridge graceful degradation hotfix` ✓ (automated-verified)
2. Confirm asset `forge_bridge-1.2.1-py3-none-any.whl` attached ✓ (automated-verified)
3. Confirm asset `forge_bridge-1.2.1.tar.gz` attached ✓ (automated-verified)
4. Confirm release notes render with "Hotfix" header, "What changed" / "What did NOT change" sections, "Reproducer" code block, "No PROV-02 changes" reassurance ✓ (body-content-string verified; visual rendering requires human eyes on the page)
5. Confirm tag `v1.2.1` appears in `https://github.com/cnoellert/forge-bridge/tags` ✓ (tag pushed, visible via `gh release view`)
6. Confirm `pip install --dry-run "forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.2.1"` resolves cleanly from a fresh shell ✓ (verified: `Would install forge-bridge-1.2.1`)

Orchestrator hands off to user for the visual release-page review; on "approved" response, Plan 03 (projekt-forge re-pin) can begin.

## Next Phase Readiness

- **07.1-03 (cross-repo re-pin):** Ready. `@v1.2.1` resolves via `pip install --dry-run` cleanly; projekt-forge's `pyproject.toml` line 25 can swap `@v1.2.0` → `@v1.2.1` and reinstall.
- **07.1-04 (deployment UAT):** Not blocked by this plan. Requires the re-pin in 07.1-03 plus a Claude Code restart on Portofino.
- **Blockers:** Human-verify checkpoint on the release page (orchestrator handles).

## Self-Check

**Claims verified:**

| Claim | Verification | Result |
|-------|-------------|--------|
| Commit `abd047c` exists | `git log --all | grep abd047c` | FOUND |
| pyproject.toml declares `version = "1.2.1"` | `grep -cn '^version = "1.2.1"' pyproject.toml` | 1 |
| Annotated tag `v1.2.1` exists | `git cat-file -t v1.2.1` | `tag` (annotated) |
| Tag points at release commit | `git rev-list -n 1 v1.2.1` | `abd047c3...` |
| Tag pushed to origin | `git push origin v1.2.1` output | `[new tag] v1.2.1 -> v1.2.1` |
| Wheel artifact exists | `ls dist/forge_bridge-1.2.1-py3-none-any.whl` | 168356 B |
| Sdist artifact exists | `ls dist/forge_bridge-1.2.1.tar.gz` | 139450 B |
| GitHub release exists | `gh release view v1.2.1 --json url` | https://github.com/cnoellert/forge-bridge/releases/tag/v1.2.1 |
| Both assets attached | `gh release view --json assets --jq '.assets[].name'` | wheel + sdist listed |
| Release body contains "Hotfix" | `gh release view --json body --jq '.body \| contains("Hotfix")'` | true |
| Release body contains "No PROV-02 changes" | `... contains("No PROV-02 changes")` | true |
| Release body contains "startup_bridge" | `... contains("startup_bridge")` | true |
| pip install --dry-run @v1.2.1 resolves | `pip install --dry-run "forge-bridge @ git+...@v1.2.1"` | `Would install forge-bridge-1.2.1` |
| forge_bridge.__version__ reports 1.2.1 | `python -c "import forge_bridge; print(forge_bridge.__version__)"` | `1.2.1` |
| Full pytest suite green | `pytest tests/ -x --tb=short` | `272 passed` |

## Self-Check: PASSED

All 15 claims verified. No missing items.

---
*Phase: 07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat*
*Plan: 02 — forge-bridge v1.2.1 hotfix release*
*Completed: 2026-04-21*
