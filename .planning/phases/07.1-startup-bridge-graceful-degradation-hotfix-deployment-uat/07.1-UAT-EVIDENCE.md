# Phase 07.1 — Deployment UAT Evidence

**Session:** Fresh Claude Code session, post-restart
**Date:** 2026-04-21T17:35:00Z (UAT start) / 2026-04-21T18:15:30Z (completion)
**forge-bridge version under test:** 1.2.1
**projekt-forge pin:** @v1.2.1 (Plan 03 commit `2f64b9e`)
**MCP registration:** `claude mcp add -s user projekt-forge -e FORGE_BRIDGE_HOST=127.0.0.1 -e FORGE_BRIDGE_PORT=9999 -- /Users/cnoellert/miniconda3/envs/forge/bin/python -m projekt_forge --no-db`
**No monkey-patches or harness scripts used.** MCP server started by Claude Code directly via `claude mcp add` registration. Two supplementary probe scripts (`/tmp/mcp_probe_synth.py`, `/tmp/mcp_invoke_synth.py`) were written solely to read the MCP wire directly because Claude Code's session-start tool catalog does not refresh after watcher-added tools arrive; see Step 1 for the detailed rationale. Those probes spawn their own stdio projekt-forge instance via the official MCP Python SDK — no patching of Plan 01's code.

---

## Runtime state verification (pre-UAT)

| Check | Command | Result |
|---|---|---|
| Flame HTTP bridge reachable | `curl -s http://127.0.0.1:9999/ -o /dev/null -w "HTTP %{http_code}\n"` | `HTTP 200` (after user relaunched Flame mid-session) |
| Port 9999 listener | `lsof -iTCP:9999` | `flame 35438 cnoellert 70u IPv4 0xb8420ff8e0c0e433 0t0 TCP localhost:distinct (LISTEN)` |
| Port 9998 listener (MUST be empty — SC1 target) | `lsof -i :9998` | empty (exit 1) |
| Ollama qwen2.5-coder:32b reachable | `curl -s http://192.168.86.15:11434/api/tags` | `qwen2.5-coder:32b` listed |
| forge-bridge version in forge env | `pip show forge-bridge \| grep Version` | `Version: 1.2.1` |
| `~/.forge-bridge/synthesized/` pre-UAT baseline | `ls -la` | matches handoff snapshot: `synth_rename_segment.py` + `.tags.json` + `.manifest.json` + `__pycache__/` |
| MCP registration still ✓ Connected | `claude mcp get projekt-forge` | `Status: ✓ Connected` |

Initial session blocked on Flame being down at :9999; user relaunched Flame on Portofino, then UAT proceeded.

---

### UAT step 1 — Tool enumeration

**Timestamp:** 2026-04-21T17:36:00Z
**Method:** Inspection of the Claude Code session-start deferred-tool list (the canonical list of MCP tools visible to the agent at session initialization). Cross-checked against `ListMcpResourcesTool(server="projekt-forge")` and a direct MCP SDK probe (`/tmp/mcp_probe_synth.py`) that spawns its own projekt-forge subprocess and calls `tools/list` over stdio.

**Verbatim — `mcp__projekt-forge__*` tools in Claude Code's session-start deferred catalog:**

```
flame_* (29):
  flame_assemble_published_sequence
  flame_assign_roles
  flame_clone_version
  flame_context
  flame_create_version
  flame_disconnect_segments
  flame_find_media
  flame_get_project
  flame_get_sequence_segments
  flame_inspect_batch_xml
  flame_inspect_sequence_versions
  flame_list_desktop
  flame_list_libraries
  flame_ping
  flame_preview_rename
  flame_preview_start_frames
  flame_prune_batch_xml
  flame_publish_sequence
  flame_query_alternatives
  flame_reconform_sequence
  flame_reconstruct_track
  flame_rename_segments
  flame_rename_shots
  flame_replace_segment_media
  flame_scan_roles
  flame_set_segment_attribute
  flame_set_start_frames
  flame_snapshot_timeline
  flame_switch_grade

forge_* (27):
  forge_blast_radius
  forge_check_shots
  forge_create_project
  forge_create_shot
  forge_get_dependents
  forge_get_events
  forge_get_project
  forge_get_shot
  forge_get_shot_deps
  forge_get_shot_lineage
  forge_get_shot_stack
  forge_get_shot_versions
  forge_list_desktop
  forge_list_media
  forge_list_projects
  forge_list_published_plates
  forge_list_roles
  forge_list_shots
  forge_list_versions
  forge_media_scan
  forge_ping
  forge_publish_pipeline
  forge_register_publish
  forge_seed_catalog
  forge_setup_denoise
  forge_trace_lineage
  forge_update_shot_status

synth_* (0 — in Claude Code's static enumeration)
```

**`ListMcpResourcesTool(server="projekt-forge")` result:**
```json
[{"name":"llm_health","uri":"forge://llm/health","description":"Report available LLM backends for forge-bridge.","mimeType":"text/plain","server":"projekt-forge"}]
```

**Finding: runtime-added synth_* tools are NOT visible in Claude Code's session-start tool catalog, but the MCP server DOES register and expose them over the wire.**

Source inspection (`forge_bridge/mcp/server.py:_lifespan`, `forge_bridge/learning/watcher.py`):
- `register_builtins(mcp)` registers all 29 flame_* + 27 forge_* tools synchronously during server startup, before the FastMCP server completes its MCP handshake.
- `watch_synthesized_tools(mcp)` launches as a background asyncio task in `_lifespan`, polling `~/.forge-bridge/synthesized/` every **5 seconds**. First registration of pre-existing synth tools therefore happens after the server's initial `tools/list` roundtrip with the client.
- Claude Code's deferred-tool catalog is captured at client-init time. It does not appear to act on `ToolListChangedNotification` from the server (neither for pre-existing synth_rename_segment, which has been on disk since Apr 18, nor for the UAT-created synth_set_segment_gain).

To confirm this is a client-visibility limitation rather than a server-registration bug, `/tmp/mcp_probe_synth.py` spawns its own stdio projekt-forge subprocess via the Python MCP SDK, waits 12s (two watcher cycles), then calls `tools/list`. Result (abridged to the decisive fields — full payload reproduced under Step 4):

```
2026-04-21 11:14:03,452 forge_bridge.mcp.server WARNING Could not connect to forge-bridge at ws://127.0.0.1:9998: [Errno 61] Connect call failed ('127.0.0.1', 9998)
forge_* tools will fail. flame_* tools still work if Flame is running.
2026-04-21 11:14:08,457 forge_bridge.learning.watcher INFO Registered synthesized tool: synth_rename_segment
2026-04-21 11:14:08,459 forge_bridge.learning.watcher INFO Registered synthesized tool: synth_set_segment_gain
2026-04-21 11:14:15,455 mcp.server.lowlevel.server INFO Processing request of type ListToolsRequest
...
  "total_tools": 58,
  "synth_tools": [ {synth_rename_segment...}, {synth_set_segment_gain...} ],
```

The server reports 58 tools over the MCP wire (29 flame_ + 27 forge_ + 2 synth_), including both synth tools with complete `_meta` envelopes. Claude Code sees only 56 (no synth_* tools) in its static enumeration — **a client-side tool-list-refresh behaviour, not a server-side registration failure**.

**Pass/fail:** PASS (with client-visibility caveat). Both prefix presence requirements (≥1 flame_*, ≥1 forge_*) satisfied in Claude Code's direct enumeration. The `synth_rename_segment` presence requirement is satisfied via MCP wire-protocol evidence (ListToolsRequest result on the MCP subprocess), not via Claude Code's static tool catalog. The caveat is a Claude Code UX limitation that does not invalidate SC1 or SC5 — the data is correctly served by the MCP server end-to-end.

---

### UAT step 2 — flame_ping

**Timestamp:** 2026-04-21T17:36:45Z
**Method:** Invoked `mcp__projekt-forge__flame_ping` with no arguments from this Claude Code session.

**Verbatim result:**
```json
{"result":"{\n  \"connected\": true,\n  \"version\": \"2026.2.1\",\n  \"project\": \"local\",\n  \"current_tab\": \"Timeline\",\n  \"bridge_url\": \"http://127.0.0.1:9999\"\n}"}
```

Parsed inner payload:
- `connected`: `true`
- `version`: `"2026.2.1"`
- `project`: `"local"`
- `current_tab`: `"Timeline"`
- `bridge_url`: `"http://127.0.0.1:9999"`

**Pass/fail:** PASS. `connected == true`, `version` matches live Flame (2026.2.1), `project` non-empty, `bridge_url == "http://127.0.0.1:9999"`. This is the decisive SC1 proof: a tool call served by the projekt-forge MCP server — which booted with :9998 unreachable — routed through the Flame HTTP bridge and returned a real response. The degraded-mode fix from Plan 01 holds under live MCP traffic from Claude Code.

---

### UAT step 3 — Real Ollama synthesis

**Timestamp:** 2026-04-21T17:35:42Z (first HTTP call) → 2026-04-21T17:35:57Z (synth write)
**Method:** `/Users/cnoellert/miniconda3/envs/forge/bin/python /tmp/ollama_synth_uat.py`

**Verbatim driver stdout:**
```
[04/21/26 10:35:42] INFO     HTTP Request: GET                   _client.py:1740
                             http://192.168.86.15:11434/v1/model                
                             s "HTTP/1.1 200 OK"                                
[04/21/26 10:35:57] INFO     HTTP Request: POST                  _client.py:1740
                             http://192.168.86.15:11434/v1/chat/                
                             completions "HTTP/1.1 200 OK"                      
                    INFO     Synthesized tool written:        synthesizer.py:389
                             /Users/cnoellert/.forge-bridge/s                   
                             ynthesized/synth_set_segment_gai                   
                             n.py                                               
LLMRouter: local_url=http://192.168.86.15:11434/v1 local_model=qwen2.5-coder:32b
Health: {'local': True, 'cloud': False, 'local_model': 'qwen2.5-coder:32b', 'cloud_model': 'claude-opus-4-6', 'cloud_error': 'anthropic not installed'}
PASS — Ollama reachable + model available ({'local': True, 'cloud': False, 'local_model': 'qwen2.5-coder:32b', 'cloud_model': 'claude-opus-4-6', 'cloud_error': 'anthropic not installed'})
Existing synth_* files: ['synth_rename_segment.py']

Invoking synthesize(intent='set the gain of a segment', count=3) …
synthesize() returned in 14.8s: /Users/cnoellert/.forge-bridge/synthesized/synth_set_segment_gain.py
PASS — synthesize() returned a Path (not None)
PASS — synth .py file exists at /Users/cnoellert/.forge-bridge/synthesized/synth_set_segment_gain.py
PASS — tool name has synth_ prefix (synth_set_segment_gain.py)
PASS — v1.2 .sidecar.json exists at /Users/cnoellert/.forge-bridge/synthesized/synth_set_segment_gain.sidecar.json
PASS — sidecar schema_version == 1 (got 1)
PASS — sidecar has tags list
PASS — sidecar has meta dict
PASS — meta['forge-bridge/origin'] == 'synthesizer' (got 'synthesizer')
PASS — meta has forge-bridge/code_hash
PASS — meta has forge-bridge/synthesized_at
PASS — meta has forge-bridge/version
PASS — meta['forge-bridge/observation_count'] == 3 (got 3)

--- Generated synth_set_segment_gain.py ---
async def synth_set_segment_gain(segment_name: str, gain_value: float) -> dict:
    """
    Sets the gain of a specified segment in Flame.

    Parameters:
    - segment_name (str): The name of the segment.
    - gain_value (float): The gain value to set for the segment.

    Returns:
    - dict: The result of the execution.
    """
    from forge_bridge.bridge import execute_json
    code = f"{segment_name}.gain = {gain_value}"
    return await execute_json(code)

--- Sidecar synth_set_segment_gain.sidecar.json ---
{
  "tags": [],
  "meta": {
    "forge-bridge/origin": "synthesizer",
    "forge-bridge/code_hash": "16ad4226dea7da72403d82f979de16a0afc36dd121ad832225981d4715072a59",
    "forge-bridge/synthesized_at": "2026-04-21T17:35:57.072122+00:00",
    "forge-bridge/version": "1.2.1",
    "forge-bridge/observation_count": 3
  },
  "schema_version": 1
}

New files this run: ['synth_set_segment_gain.py']
PASS — at least one new synth_*.py created (new=['synth_set_segment_gain.py'])

UAT PASSED — real Ollama synthesis wrote PROV-02-compliant v1.2 envelope
NEW_TOOL_STEM: synth_set_segment_gain
```

**`NEW_TOOL_STEM`:** `synth_set_segment_gain` — recorded as the cleanup target.

**Verbatim `ls -la ~/.forge-bridge/synthesized/` after the run:**
```
total 40
drwxr-xr-x  4 cnoellert  staff  128 Apr 21 10:35 __pycache__
drwxr-xr-x  8 cnoellert  staff  256 Apr 21 10:35 .
drwxr-xr-x  4 cnoellert  staff  128 Apr 18 08:18 ..
-rw-r--r--  1 cnoellert  staff  480 Apr 21 10:35 .manifest.json
-rw-r--r--  1 cnoellert  staff  407 Apr 18 08:31 synth_rename_segment.py
-rw-r--r--  1 cnoellert  staff   25 Apr 18 08:31 synth_rename_segment.tags.json
-rw-r--r--  1 cnoellert  staff  468 Apr 21 10:35 synth_set_segment_gain.py
-rw-r--r--  1 cnoellert  staff  312 Apr 21 10:35 synth_set_segment_gain.sidecar.json
```

**Verbatim sidecar JSON (`synth_set_segment_gain.sidecar.json`):**
```json
{"tags": [], "meta": {"forge-bridge/origin": "synthesizer", "forge-bridge/code_hash": "16ad4226dea7da72403d82f979de16a0afc36dd121ad832225981d4715072a59", "forge-bridge/synthesized_at": "2026-04-21T17:35:57.072122+00:00", "forge-bridge/version": "1.2.1", "forge-bridge/observation_count": 3}, "schema_version": 1}
```

**Verbatim manifest delta (`.manifest.json` — full post-UAT contents):**
```json
{
  "synth_get_shot_name.py": "28fcbb18ec2e56b118f2fc206401a13feae1fb945210df23925e53d8d5d14886",
  "synth_get_project_info.py": "b73385935218f8b050bb4ec259998884d90de01dfc3f91b5405557502e1be1e3",
  "synth_rename_segment.py": "ce0c31a109d738d44a4629e83ab00da919076db00354463861c938cf399ea30a",
  "synth_noop.py": "24300775573d42067ff6f3e5304fcd9910408f2a93f4ab231d7ef166343718ef",
  "synth_set_segment_gain.py": "16ad4226dea7da72403d82f979de16a0afc36dd121ad832225981d4715072a59"
}
```
New key: `"synth_set_segment_gain.py": "16ad4226..."` — will be removed at cleanup.

**Pass/fail:** PASS. Driver exit 0 with `UAT PASSED` banner. Sidecar meta contains all five PROV-02 keys (`forge-bridge/origin` = `synthesizer`, `code_hash`, `synthesized_at`, `version` = `1.2.1`, `observation_count` = `3`). Synth `.py` + `.sidecar.json` both written.

---

### UAT step 4 — Watcher hot-load + PROV-02 visibility on the MCP wire

**Timestamp:** 2026-04-21T18:14:03Z (probe start) → 2026-04-21T18:14:15Z (`ListToolsRequest` response)
**Method:** Claude Code's session-start tool catalog does not refresh (see Step 1 finding). Fallback method from handoff: confirm watcher registered the tool by reading the MCP server's stderr for the watcher's `INFO Registered synthesized tool:` log line, and fetch the `_meta` envelope that the server publishes via `tools/list` by spawning a direct MCP SDK client (`/tmp/mcp_probe_synth.py`). The probe connects to a fresh projekt-forge stdio subprocess, waits 12s (two watcher cycles), then calls `session.list_tools()` and dumps each synth tool's full `_meta` dict.

**Verbatim probe stderr (watcher registration confirmation):**
```
2026-04-21 11:14:03,436 projekt_forge.learning.wiring INFO LLMRouter configured: local_url=http://localhost:11434/v1, local_model=qwen2.5-coder:32b
2026-04-21 11:14:03,437 projekt_forge.learning.wiring INFO ExecutionLog configured: log_path=/Users/cnoellert/.forge-bridge/executions.jsonl, threshold=3
2026-04-21 11:14:03,437 projekt_forge.learning.wiring INFO learning pipeline initialized (model=qwen2.5-coder:32b, log_path=/Users/cnoellert/.forge-bridge/executions.jsonl)
2026-04-21 11:14:03,452 forge_bridge.mcp.server WARNING Could not connect to forge-bridge at ws://127.0.0.1:9998: [Errno 61] Connect call failed ('127.0.0.1', 9998)
forge_* tools will fail. flame_* tools still work if Flame is running.
2026-04-21 11:14:03,452 forge_bridge.client.async_client INFO Client 'mcp_claude' disconnected.
2026-04-21 11:14:08,457 forge_bridge.learning.watcher INFO Registered synthesized tool: synth_rename_segment
2026-04-21 11:14:08,459 forge_bridge.learning.watcher INFO Registered synthesized tool: synth_set_segment_gain
2026-04-21 11:14:15,455 mcp.server.lowlevel.server INFO Processing request of type ListToolsRequest
```

Key observations:
- :9998 unreachable → `WARNING` + `forge_* tools will fail. flame_* tools still work if Flame is running.` — the graceful-degradation message from Plan 01's `startup_bridge`.
- 5.005 seconds after server start (`11:14:08,457`), watcher completed its first scan and registered both synth tools.
- `ListToolsRequest` served successfully at 11:14:15, confirming the MCP protocol roundtrip works in degraded mode.

**Verbatim `tools/list` payload (synth_set_segment_gain entry — the UAT-produced tool):**
```json
{
  "name": "synth_set_segment_gain",
  "description": "\n    Sets the gain of a specified segment in Flame.\n\n    Parameters:\n    - segment_name (str): The name of the segment.\n    - gain_value (float): The gain value to set for the segment.\n\n    Returns:\n    - dict: The result of the execution.\n    ",
  "annotations": {
    "readOnlyHint": false
  },
  "_meta": {
    "_source": "synthesized",
    "forge-bridge/origin": "synthesizer",
    "forge-bridge/code_hash": "16ad4226dea7da72403d82f979de16a0afc36dd121ad832225981d4715072a59",
    "forge-bridge/synthesized_at": "2026-04-21T17:35:57.072122+00:00",
    "forge-bridge/version": "1.2.1",
    "forge-bridge/observation_count": 3,
    "forge-bridge/tags": [
      "synthesized"
    ]
  }
}
```

**Verbatim `tools/list` payload (pre-existing synth_rename_segment entry, included for completeness):**
```json
{
  "name": "synth_rename_segment",
  "description": "\n    Renames a segment in Flame to the specified name.\n\n    Parameters:\n    - segment_name (str): The new name for the segment.\n\n    Returns:\n    - dict: A dictionary containing the result of the operation.\n    ",
  "annotations": {
    "readOnlyHint": false
  },
  "_meta": {
    "_source": "synthesized",
    "forge-bridge/tags": [
      "synthesized",
      "project:ACM"
    ]
  }
}
```

Note: `synth_rename_segment` predates v1.2 (its legacy `.tags.json` has no `meta` block), which is why its `_meta` lacks the five PROV-02 keys. That's correct v1.1→v1.2 compatibility behaviour from `watcher._read_sidecar` — PROV-02 keys only appear for sidecars that carry them. The UAT target is the FRESH tool (`synth_set_segment_gain`) — it has every required key.

**Code hash match check:**
- Sidecar on disk: `"forge-bridge/code_hash": "16ad4226dea7da72403d82f979de16a0afc36dd121ad832225981d4715072a59"`
- MCP wire `_meta`: `"forge-bridge/code_hash": "16ad4226dea7da72403d82f979de16a0afc36dd121ad832225981d4715072a59"`
- Identical ✓

**Pass/fail:** PASS. Watcher hot-loaded the new synth tool within ~5s. The MCP server's `tools/list` wire payload exposes the full PROV-02 `_meta` envelope to any connected MCP client:
- `forge-bridge/origin`: `"synthesizer"` ✓
- `forge-bridge/code_hash`: 64-hex-char digest, matches sidecar ✓
- `forge-bridge/synthesized_at`: ISO-8601 `"2026-04-21T17:35:57.072122+00:00"` ✓
- `forge-bridge/version`: `"1.2.1"` — **exact match of the re-pinned release** ✓
- `forge-bridge/observation_count`: `3` — matches `count=3` driver arg ✓

Caveat carried from Step 1: direct `_meta` inspection in Claude Code's static tool catalog is UI-blocked (this Claude Code session only exposes tools captured at init time). The wire-level evidence from the MCP SDK probe is authoritative.

---

### UAT step 5 — Invoke the new synth_* tool

**Timestamp:** 2026-04-21T18:15:10Z
**Method:** Claude Code's tool catalog does not include `synth_set_segment_gain`, so invoking it via `mcp__projekt-forge__synth_set_segment_gain` from this session is not possible (no name to dispatch against). Fallback: `/tmp/mcp_invoke_synth.py` — a direct Python MCP SDK client that spawns its own projekt-forge stdio subprocess, waits for watcher registration, then calls `session.call_tool("synth_set_segment_gain", {"segment_name": "test_segment", "gain_value": 1.5})`.

**Verbatim result:**
```
2026-04-21 11:14:58,767 projekt_forge.learning.wiring INFO LLMRouter configured: local_url=http://localhost:11434/v1, local_model=qwen2.5-coder:32b
2026-04-21 11:14:58,767 projekt_forge.learning.wiring INFO ExecutionLog configured: log_path=/Users/cnoellert/.forge-bridge/executions.jsonl, threshold=3
2026-04-21 11:14:58,767 projekt_forge.learning.wiring INFO learning pipeline initialized (model=qwen2.5-coder:32b, log_path=/Users/cnoellert/.forge-bridge/executions.jsonl)
2026-04-21 11:14:58,781 forge_bridge.mcp.server WARNING Could not connect to forge-bridge at ws://127.0.0.1:9998: [Errno 61] Connect call failed ('127.0.0.1', 9998)
forge_* tools will fail. flame_* tools still work if Flame is running.
2026-04-21 11:14:58,781 forge_bridge.client.async_client INFO Client 'mcp_claude' disconnected.
2026-04-21 11:15:03,783 forge_bridge.learning.watcher INFO Registered synthesized tool: synth_rename_segment
2026-04-21 11:15:03,783 forge_bridge.learning.watcher INFO Registered synthesized tool: synth_set_segment_gain
2026-04-21 11:15:10,785 mcp.server.lowlevel.server INFO Processing request of type CallToolRequest
2026-04-21 11:15:10,809 httpx INFO HTTP Request: POST http://127.0.0.1:9999/exec "HTTP/1.0 200 OK"
{
  "isError": true,
  "content": [
    {
      "type": "text",
      "text": "Error executing tool synth_set_segment_gain: Traceback (most recent call last):\n  File \"/opt/Autodesk/shared/python/forge_bridge/scripts/forge_bridge.py\", line 149, in _execute_code\n    exec(compile(tree, \"<bridge>\", \"exec\"), _namespace)\n  File \"<bridge>\", line 1, in <module>\nNameError: name 'test_segment' is not defined\n",
      "annotations": null,
      "meta": null
    }
  ],
  "structuredContent": null
}
```

**Transport analysis:**
- MCP client → MCP server: `CallToolRequest` processed at 11:15:10,785 ✓
- MCP server → Flame HTTP bridge: `httpx INFO HTTP Request: POST http://127.0.0.1:9999/exec "HTTP/1.0 200 OK"` at 11:15:10,809 ✓
- Flame → synth tool body → error path: `NameError: name 'test_segment' is not defined` (raised inside Flame's `_execute_code` at `/opt/Autodesk/shared/python/forge_bridge/scripts/forge_bridge.py:149` because the f-string interpolated `test_segment.gain = 1.5` and `test_segment` isn't bound in Flame's namespace) ✓
- Error surfaced back through MCP as `isError: true` with full traceback ✓

**Pass/fail:** PASS. The full MCP client → MCP server → forge-bridge HTTP → Flame → error-return roundtrip completed. None of the forbidden failure modes occurred:
- ❌ MCP-level transport error: **NOT present** — `CallToolRequest` processed normally, `isError: true` is a well-formed MCP error response, not a transport failure.
- ❌ ConnectionError to forge-bridge internals (:9998): **NOT present** — the server's :9998 warning happened at startup and did not prevent synth tool registration or invocation.
- ❌ Python import error inside the synthesized body: **NOT present** — `from forge_bridge.bridge import execute_json` resolved, the f-string built the code string, and the HTTP POST reached Flame.

The `NameError` for the unbound `test_segment` is the expected Flame-level error the handoff calls out explicitly as a PASS signal.

---

### UAT step 6 — No PROV-02 key contamination on flame_* / forge_* tools

**Timestamp:** 2026-04-21T18:14:15Z (from the probe's single `tools/list` response)
**Method:** Inspect the `_meta` envelopes of the four sampled tools (two flame_*, two forge_*) from the same `tools/list` wire payload used in Step 4.

**Verbatim `_meta` envelopes from the MCP wire:**

`flame_ping`:
```json
{
  "name": "flame_ping",
  "annotations": { "readOnlyHint": true },
  "_meta": { "_source": "builtin" }
}
```

`flame_context`:
```json
{
  "name": "flame_context",
  "annotations": {
    "title": "Get current Flame context — project, workspace, desktop, all reel contents",
    "readOnlyHint": true,
    "idempotentHint": true
  },
  "_meta": { "_source": "builtin" }
}
```

`forge_ping`:
```json
{
  "name": "forge_ping",
  "annotations": {
    "title": "Check forge-bridge connection",
    "readOnlyHint": true,
    "idempotentHint": true
  },
  "_meta": { "_source": "builtin" }
}
```

`forge_list_projects`:
```json
{
  "name": "forge_list_projects",
  "annotations": {
    "title": "List all pipeline projects",
    "readOnlyHint": true,
    "idempotentHint": true
  },
  "_meta": { "_source": "builtin" }
}
```

**Contamination check:** Each of the four `_meta` dicts contains **only** `{"_source": "builtin"}`. None contain any of `forge-bridge/origin`, `forge-bridge/code_hash`, `forge-bridge/synthesized_at`, `forge-bridge/version`, `forge-bridge/observation_count`, or `forge-bridge/tags`.

**Pass/fail:** PASS. No PROV-02 key contamination on hand-written flame_* / forge_* tools. The v1.0 `_source` annotation remains (permitted per handoff) and lives in a different namespace than the v1.2 PROV-02 `forge-bridge/*` keys.

---

## Summary

- **SC1** (startup_bridge graceful degradation): **PASS**
  - Rationale: the fresh Claude Code session registered projekt-forge as an MCP server on Portofino with nothing on :9998. The MCP server booted cleanly — `claude mcp list` shows `projekt-forge: ... - ✓ Connected`. UAT step 2 (flame_ping from Claude Code) returned a valid payload proving the MCP server routes tool calls in degraded mode. Independently, the probe subprocesses both logged the exact graceful-degradation message from Plan 01 (`WARNING Could not connect to forge-bridge at ws://127.0.0.1:9998: [Errno 61] Connect call failed` followed by `forge_* tools will fail. flame_* tools still work if Flame is running.`) and proceeded to register tools and serve requests.

- **SC5** (PROV-02 _meta end-to-end via real MCP client): **PASS**
  - Rationale: UAT step 3 produced a fresh `synth_set_segment_gain` tool whose sidecar carries all five PROV-02 keys. UAT step 4's MCP-wire capture shows the server's `tools/list` response surfaces the full `_meta` envelope — `forge-bridge/origin = synthesizer`, `code_hash` matching the sidecar's 16ad4226... hex digest, ISO-8601 `synthesized_at`, **`version = "1.2.1"` (exact match of the re-pinned release — this is the end-to-end proof the Plan 03 re-pin landed)**, and `observation_count = 3`. UAT step 5 then confirmed the synth tool is invocable through the MCP protocol via the full roundtrip to Flame. UAT step 6 confirmed PROV-02 keys do not contaminate the `_meta` of hand-written flame_* / forge_* tools.
  - Caveat: direct inspection of `_meta` via Claude Code's static tool enumeration is UI-blocked — Claude Code's session-start tool catalog does not refresh when the projekt-forge watcher registers synth tools at runtime, so neither the pre-existing `synth_rename_segment` nor the UAT-created `synth_set_segment_gain` appear in this Claude Code session's tool list. This is a Claude Code client-side refresh behaviour; the server correctly implements the MCP protocol and exposes the tools and their `_meta` to any client that re-fetches `tools/list` after the watcher's 5-second poll cycle. Two direct MCP SDK probes (`/tmp/mcp_probe_synth.py`, `/tmp/mcp_invoke_synth.py`) captured the wire-level evidence for the UAT record.

---

## Cleanup

Performed post-UAT to restore `~/.forge-bridge/synthesized/` to pre-UAT baseline. Only the UAT-produced `synth_set_segment_gain.*` stem removed; pre-existing `synth_rename_segment.*` + `.manifest.json` (for pre-existing entries) + `__pycache__/` untouched.

See `07.1-04-SUMMARY.md` for the cleanup command log once the orchestrator writes it.
