---
phase: 07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md
  - .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md
autonomous: false
requirements: []
tags: deployment-uat,real-mcp-client,provenance,handoff,restart-required

user_setup:
  - service: claude-code-mcp
    why: "Register projekt-forge as an MCP server in the user's Claude Code session and restart Claude Code so a fresh session discovers the new tools. This is the UAT vehicle — a real MCP client verifying PROV-02 _meta end-to-end without any monkey-patch or harness."
    dashboard_config:
      - task: "Run `claude mcp add projekt-forge -- /Users/cnoellert/miniconda3/envs/forge/bin/python -m projekt_forge --no-db` with env FORGE_BRIDGE_HOST=127.0.0.1 and FORGE_BRIDGE_PORT=9999, then quit and restart Claude Code"
        location: "Portofino — user's Claude Code CLI"
      - task: "After restart, verify Claude Code's tool enumeration shows mcp__projekt-forge__flame_* / forge_* / synth_* tools"
        location: "Portofino — fresh Claude Code session"
  - service: ollama
    why: "Drive a real Ollama synthesis against assist-01's qwen2.5-coder:32b during the UAT to produce a fresh synth_* tool whose .sidecar.json triggers the watcher and surfaces PROV-02 _meta on the MCP wire"
    env_vars:
      - name: OLLAMA_HOST
        source: "http://192.168.86.15:11434 (assist-01); verify accessible with curl /api/tags"

must_haves:
  truths:
    - "A 07.1-HANDOFF.md exists containing zero-context resumption instructions for a fresh Claude Code session"
    - "User has registered projekt-forge as a Claude Code MCP server and restarted Claude Code"
    - "The restarted Claude Code session boots cleanly — NO monkey-patches, NO harnesses; the projekt-forge MCP server starts in pure degraded mode (no :9998 listener)"
    - "Claude Code's tool enumeration surfaces mcp__projekt-forge__flame_* / forge_* / synth_* tools"
    - "flame_ping returns {connected: true, version: ..., project: ..., bridge_url: http://127.0.0.1:9999}"
    - "A real Ollama-driven synthesis against assist-01 qwen2.5-coder:32b produces a fresh synth_*.py + .sidecar.json in ~/.forge-bridge/synthesized/"
    - "Within ~6s of the sidecar write, Claude Code's tools/list refresh surfaces the new synth_* tool with PROV-02 _meta populated (forge-bridge/origin: synthesizer, code_hash, synthesized_at, version=1.2.1, observation_count)"
    - "Claude invokes the new synth_* tool successfully — execution against the live Flame HTTP bridge works"
    - "flame_* / forge_* tool metadata shows no PROV-02 contamination (matches Phase 7-04's clean baseline)"
    - "07.1-UAT-EVIDENCE.md captures every UAT checklist step with verbatim tool-call result objects, timestamps, and pass/fail"
    - "Throwaway synth_*.py + .sidecar.json + manifest entry cleaned up after UAT passes"
  artifacts:
    - path: ".planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md"
      provides: "Zero-context resumption note for the fresh Claude Code session"
      contains: "projekt-forge MCP registration"
    - path: ".planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md"
      provides: "Verbatim UAT evidence with tool-call results"
      contains: "forge-bridge/origin"
  key_links:
    - from: "User's Claude Code session (MCP client)"
      to: "projekt-forge MCP server (subprocess of Claude Code)"
      via: "stdio transport via `claude mcp add`"
      pattern: "mcp__projekt-forge__"
    - from: "projekt-forge MCP server"
      to: "forge-bridge v1.2.1 (via pinned dep)"
      via: "forge_bridge.mcp.server.startup_bridge (now graceful on :9998 dead)"
      pattern: "Could not connect to forge-bridge"
    - from: "Ollama synthesis → ~/.forge-bridge/synthesized/synth_*.py + .sidecar.json"
      to: "Claude Code's tools/list (via watcher hot-load)"
      via: "forge_bridge.learning.watcher → MCP _meta"
      pattern: "forge-bridge/origin"
---

<objective>
Deployment UAT: verify PROV-02 `_meta` envelope end-to-end using a REAL MCP client — the user's Claude Code session on Portofino, with projekt-forge registered as an MCP server via the standard `claude mcp add` CLI. No monkey-patches, no raw JSON-RPC harness, no launcher shims. The fix from Plan 01 (plus the release from Plan 02 and re-pin from Plan 03) must carry the full weight on its own.

This plan is split across a user-hand-off restart. Task 1 (pre-restart): register the MCP server + write a complete HANDOFF.md that a fresh Claude Code session can pick up from zero context. STOP. User restarts Claude Code. Task 2 (post-restart): executes in the fresh session, runs the UAT checklist, captures verbatim evidence in 07.1-UAT-EVIDENCE.md, cleans up the throwaway synthesis artifacts.

Purpose: SC5 of Phase 07.1. Phase 7-04's UAT worked around the startup_bridge bug with a monkey-patched `/tmp/run_pf_mcp.py` launcher — the UAT passed but wasn't testing the real deploy path. This plan is the replacement: a pristine Claude Code restart with no workarounds verifies both SC1 (server boots cleanly) AND SC5 (PROV-02 _meta reaches a real MCP client end-to-end).

Output: HANDOFF.md (consumed by the restarted session), UAT-EVIDENCE.md (verbatim capture of every checklist step), and a clean `~/.forge-bridge/synthesized/` directory after cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-CONTEXT.md
@.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-01-PLAN.md
@.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-02-PLAN.md
@.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-03-PLAN.md
@CLAUDE.md

<interfaces>
Runtime prerequisites (verified during the 2026-04-21 session — state may drift):
- Flame: running on Portofino, HTTP bridge at http://127.0.0.1:9999
- Ollama: http://192.168.86.15:11434 (assist-01); qwen2.5-coder:32b available
- Conda env: /Users/cnoellert/miniconda3/envs/forge
- NOT running: the standalone forge-bridge WS server on :9998 (this is the point)
- projekt-forge installed with forge-bridge 1.2.1 per Plan 03

UAT checklist (mirrors CONTEXT.md UAT checklist):
  1. Tool enumeration surfaces mcp__projekt-forge__flame_* / forge_* / synth_* tools
  2. flame_ping returns {connected: true, version: "...", project: "...", bridge_url: "http://127.0.0.1:9999"}
  3. Ollama synthesis produces fresh synth_*.py + .sidecar.json
  4. Within ~6s, tools/list refresh shows new synth_* tool with PROV-02 _meta (five canonical keys + version=1.2.1)
  5. Claude invokes the new synth_* tool successfully against live Flame
  6. Visual scan: flame_* / forge_* tools have no PROV-02 key contamination

Reference synthesis helper (from prior UAT session): /tmp/ollama_synth_uat.py — reuse if still present, else re-create inline. Calls SkillSynthesizer.synthesize() directly (direct invocation IS the production path in v1.2.0; there is no threshold-crossing auto-wire).

Cleanup target (after UAT passes): ~/.forge-bridge/synthesized/ must be restored to its pre-UAT state. Only files created during the UAT should be deleted.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Register projekt-forge as Claude Code MCP server + write HANDOFF.md + STOP for restart</name>
  <files>.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md</files>
  <read_first>
    - .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-CONTEXT.md section "Deployment UAT via Real MCP Client" under decisions (the complete UAT checklist lives here)
    - .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-03-PLAN.md (verify Plan 03 completed — projekt-forge pin is @v1.2.1)
    - /tmp/ollama_synth_uat.py (if it still exists from the 2026-04-21 session — reuse pattern; if deleted, the HANDOFF.md must include a re-creation recipe)
    - Output of `ls -la ~/.forge-bridge/synthesized/` (snapshot pre-UAT file list so HANDOFF.md can specify what gets cleaned up)
  </read_first>
  <action>
    **Step 1 — Verify the user's Claude Code CLI is installed and `claude mcp` works:**
    ```bash
    which claude
    claude mcp --help 2>&1 | head -30
    ```

    The exact subcommand syntax may have evolved — verify from `--help` output. The CONTEXT.md gives a plausible shape:
    ```
    claude mcp add projekt-forge -- /Users/cnoellert/miniconda3/envs/forge/bin/python -m projekt_forge --no-db
    ```
    with env `FORGE_BRIDGE_HOST=127.0.0.1 FORGE_BRIDGE_PORT=9999`. If `claude mcp --help` shows a different shape, follow the actual syntax and record what was used in HANDOFF.md.

    **Step 2 — Snapshot `~/.forge-bridge/synthesized/`** so UAT-created files can be distinguished from pre-existing ones at cleanup time:
    ```bash
    ls -la ~/.forge-bridge/synthesized/ 2>/dev/null
    ls -la ~/.forge-bridge/synthesized/*.py ~/.forge-bridge/synthesized/*.sidecar.json 2>/dev/null
    cat ~/.forge-bridge/synthesized/manifest.json 2>/dev/null | head -80
    ```
    Record the snapshot — HANDOFF.md will instruct the post-restart session to diff against this baseline.

    **Step 3 — Check /tmp/ollama_synth_uat.py:**
    ```bash
    ls -la /tmp/ollama_synth_uat.py 2>/dev/null || echo "MISSING — HANDOFF.md must include a re-creation recipe"
    ```
    If missing, the HANDOFF.md needs a minimal recipe that calls `SkillSynthesizer().synthesize(code, intent=...)` against assist-01 with qwen2.5-coder:32b.

    **Step 4 — Register projekt-forge as a Claude Code MCP server** using the syntax validated in Step 1:
    ```bash
    # Shape 1 (if --env supported):
    claude mcp add projekt-forge \
      --env FORGE_BRIDGE_HOST=127.0.0.1 \
      --env FORGE_BRIDGE_PORT=9999 \
      -- /Users/cnoellert/miniconda3/envs/forge/bin/python -m projekt_forge --no-db

    # Shape 2 (if env via shell):
    FORGE_BRIDGE_HOST=127.0.0.1 FORGE_BRIDGE_PORT=9999 \
      claude mcp add projekt-forge -- /Users/cnoellert/miniconda3/envs/forge/bin/python -m projekt_forge --no-db
    ```
    Verify:
    ```bash
    claude mcp list 2>&1 | grep -A 3 projekt-forge
    ```

    If `claude mcp` is NOT available, document in HANDOFF.md and ask the user to register projekt-forge manually (e.g. via MCP config file).

    **Step 5 — Write the HANDOFF.md file** with the EXACT content below. Replace `<TIMESTAMP>`, `<EXACT COMMAND THAT WORKED IN STEP 4>`, and `<PASTE ... HERE>` placeholders with the real data captured in Steps 1-4. Do NOT leave placeholders. Target path: `.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md`

    ```markdown
    # Phase 07.1 — Deployment UAT Handoff

    **Date:** <ISO 8601 timestamp of now>
    **Phase:** 07.1 startup_bridge graceful degradation hotfix + deployment UAT
    **Plan:** 04 (Task 2 — post-restart UAT)

    ## Where we are

    Plans 01-03 are complete:
    - **01**: startup_bridge fix landed (RED test + GREEN fix, two commits on forge-bridge main)
    - **02**: forge-bridge v1.2.1 tagged, pushed, GitHub release with wheel + sdist attached
    - **03**: projekt-forge re-pinned to @v1.2.1 in /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml, `pytest tests/` green at 422 baseline, commit landed locally

    Task 1 of Plan 04 has registered projekt-forge as a Claude Code MCP server via:
    ```
    <EXACT COMMAND THAT WORKED IN STEP 4>
    ```

    ## What the user needs to do NOW

    1. **Restart Claude Code.** Quit completely (Cmd+Q on macOS) and relaunch.
    2. **Confirm the new session sees projekt-forge tools.** `mcp__projekt-forge__*` prefixed tools (flame_*, forge_*, synth_*) should be visible.
    3. **Resume this phase** — either re-invoke the executor or open a fresh conversation and point it at this HANDOFF.md.

    ## Runtime state the UAT depends on

    - Flame running on Portofino; HTTP bridge at http://127.0.0.1:9999 (verify: `curl http://127.0.0.1:9999/exec`).
    - Ollama on assist-01 at http://192.168.86.15:11434 — qwen2.5-coder:32b (verify: `curl http://192.168.86.15:11434/api/tags | jq '.models[].name'`).
    - Conda env /Users/cnoellert/miniconda3/envs/forge has forge-bridge==1.2.1 (verify: `/Users/cnoellert/miniconda3/envs/forge/bin/pip show forge-bridge`).
    - NOTHING on :9998 (verify: `lsof -i :9998` empty). This is the whole point.

    ## Pre-UAT snapshot of ~/.forge-bridge/synthesized/

    Captured at <TIMESTAMP>:
    ```
    <PASTE THE OUTPUT OF `ls -la ~/.forge-bridge/synthesized/` FROM STEP 2 HERE>
    ```
    Pre-UAT manifest.json (first 80 lines):
    ```
    <PASTE MANIFEST EXCERPT FROM STEP 2 HERE>
    ```

    **Cleanup rule:** After UAT passes, delete only the synth_*.py / .sidecar.json / .tags.json files that are NOT in the pre-UAT snapshot above. Update manifest.json accordingly.

    ## Ollama synthesis driver

    <IF /tmp/ollama_synth_uat.py EXISTS:>
    Reuse `/tmp/ollama_synth_uat.py`:
    ```bash
    /Users/cnoellert/miniconda3/envs/forge/bin/python /tmp/ollama_synth_uat.py
    ```

    <IF /tmp/ollama_synth_uat.py IS MISSING:>
    Re-create at /tmp/ollama_synth_uat.py:
    ```python
    import asyncio
    from forge_bridge.learning.synthesizer import SkillSynthesizer
    from forge_bridge.learning.llm_router import LLMRouter

    router = LLMRouter.from_config({
        "providers": {
            "ollama": {"base_url": "http://192.168.86.15:11434", "model": "qwen2.5-coder:32b"},
        },
        "default_provider": "ollama",
    })
    synth = SkillSynthesizer(router=router, output_dir="~/.forge-bridge/synthesized")
    result = asyncio.run(synth.synthesize(
        code="bridge.execute('import flame; return flame.project.current_project.name')",
        intent="Return the name of the currently-loaded Flame project",
    ))
    print(result)
    ```
    Adjust imports/signatures if the exact constructor names differ in v1.2.1 — the executor has the forge-bridge source available and can verify.

    ## UAT Checklist

    Execute each item and paste verbatim evidence into `.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md`. Every tool-call result object copied character-for-character, not paraphrased.

    1. **Tool enumeration.** Confirm mcp__projekt-forge__flame_* / forge_* / synth_* all present.
    2. **flame_ping.** Call `mcp__projekt-forge__flame_ping`. Result must include `connected: true`, `version`, `project`, `bridge_url: http://127.0.0.1:9999`.
    3. **Ollama synthesis.** Run the driver. Confirm new synth_*.py + .sidecar.json in ~/.forge-bridge/synthesized/. Record the filename stem.
    4. **Watcher hot-load + PROV-02 visibility.** Wait ~6s, refresh tool list. New mcp__projekt-forge__synth_* tool must appear with _meta containing: `forge-bridge/origin: synthesizer`, `forge-bridge/code_hash` (non-empty hex), `forge-bridge/synthesized_at` (ISO 8601), `forge-bridge/version: 1.2.1`, `forge-bridge/observation_count` (int ≥ 0).
    5. **synth_* invocation.** Call the new synth_* tool. Verify it executes against Flame and returns the expected result.
    6. **No contamination.** Spot-check 2 flame_* tools and 2 forge_* tools (non-synth). Their _meta must NOT contain any forge-bridge/* keys. `_source` tag permitted.

    ## Evidence file structure

    Create `.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md`. Each section:
    ```markdown
    ### UAT step N — <name>

    **Timestamp:** <ISO 8601>
    **Method:** <how the tool was invoked>
    **Verbatim result:**
    <raw output>
    **Pass/fail:** PASS — <why> / FAIL — <reason>
    ```

    Conclude with:
    ```markdown
    ## Summary
    - SC1: startup_bridge graceful degradation — PASS/FAIL
    - SC5: PROV-02 _meta end-to-end via real MCP client — PASS/FAIL
    ```

    ## Cleanup after UAT passes

    Use the EXACT filename stem recorded in step 3 (no wildcards that might sweep pre-existing files):
    ```bash
    rm ~/.forge-bridge/synthesized/<STEM>.py
    rm ~/.forge-bridge/synthesized/<STEM>.sidecar.json
    rm -f ~/.forge-bridge/synthesized/<STEM>.tags.json
    # Remove <STEM> entry from manifest.json (or re-run whatever rebuilds it)
    ls -la ~/.forge-bridge/synthesized/  # must match pre-UAT snapshot
    ```

    ## Escalation

    - **No mcp__projekt-forge__* tools after restart:** MCP registration didn't take. `claude mcp list` — re-register if needed.
    - **flame_ping fails:** Flame not running / HTTP bridge not bound. Verify with `curl http://127.0.0.1:9999/exec`.
    - **Synthesis fails:** Ollama unreachable / model missing. Check `curl http://192.168.86.15:11434/api/tags`.
    - **Watcher doesn't hot-load within 30s:** Inspect MCP server stderr (visible in Claude Code's MCP logs) for forge_bridge.learning.watcher errors.
    - **PROV-02 _meta missing on new synth_* tool:** STOP. Real regression. Capture full raw tool definition. Do not paper over.

    ## After UAT passes

    Plan 04 Task 2 completes. Proceed to Plan 05 — Phase 7 close-out.
    ```

    **Step 6 — Commit the HANDOFF.md:**
    ```bash
    cd /Users/cnoellert/Documents/GitHub/forge-bridge
    git add .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md
    git commit -m "$(cat <<'EOF'
    docs(07.1): handoff note for Claude Code restart before deployment UAT

    Pre-restart capture: pre-UAT ~/.forge-bridge/synthesized/ snapshot,
    runtime prerequisites, MCP registration command used, full UAT checklist
    with expected evidence shape, cleanup instructions, escalation paths.
    Zero-context resumable by the fresh Claude Code session that picks up
    Task 2 after the restart.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    **Step 7 — STOP.** Print a clear hand-off message:
    - HANDOFF.md is ready at the path above.
    - projekt-forge is registered as an MCP server (show `claude mcp list` output for proof).
    - User must QUIT Claude Code and relaunch it.
    - In the fresh session, open HANDOFF.md and resume with Task 2 of Plan 04.

    Do NOT attempt to continue with Task 2 in the current session. The whole point is verifying the fix in a NEW session that has never seen the broken v1.2.0 or the monkey-patched harness.
  </action>
  <verify>
    <automated>test -f /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md && grep -q "UAT Checklist" /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md && grep -q "forge-bridge/origin" /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md && claude mcp list 2>&1 | grep -q projekt-forge</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md` passes.
    - HANDOFF.md contains the literal strings "UAT Checklist", "forge-bridge/origin", "forge-bridge/version: 1.2.1", "Pre-UAT snapshot", "Cleanup", and "Escalation".
    - HANDOFF.md contains the EXACT MCP registration command that was verified to work in Step 4 (NOT the literal placeholder `<EXACT COMMAND THAT WORKED IN STEP 4>`).
    - HANDOFF.md pre-UAT snapshot section contains real `ls -la` output, NOT the literal placeholder `<PASTE ... HERE>`.
    - `claude mcp list 2>&1 | grep -q projekt-forge` succeeds.
    - `git log --oneline -1 .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md` shows the handoff commit.
    - `test -f .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md` FAILS — evidence file does not yet exist (the current session stopped before Task 2).
  </acceptance_criteria>
  <done>
    projekt-forge registered as a Claude Code MCP server; HANDOFF.md written with real pre-UAT snapshot, exact registration command, full UAT checklist, and cleanup instructions; commit landed; user has been told to restart Claude Code and resume in a fresh session.
  </done>
  <resume-signal>User completes the Claude Code restart and opens a fresh session pointing at HANDOFF.md. The next Claude instance reads HANDOFF.md and starts Task 2.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run UAT checklist in fresh Claude Code session + capture evidence + cleanup</name>
  <read_first>
    - .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-HANDOFF.md (the ENTIRE file — this is your session entry point)
    - .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-CONTEXT.md "Deployment UAT via Real MCP Client" section (cross-check checklist)
  </read_first>
  <what-built>
    Verbatim UAT evidence captured in 07.1-UAT-EVIDENCE.md with each of the six checklist items marked PASS (or documented failure). Throwaway synthesized artifacts cleaned up. The fresh Claude Code session has proved end-to-end that:
    1. forge-bridge v1.2.1's startup_bridge boots cleanly with no :9998 listener (SC1)
    2. PROV-02 _meta envelope reaches a real MCP client without any monkey-patch (SC5)
  </what-built>
  <how-to-verify>
    **IMPORTANT:** This task runs in the FRESH Claude Code session after the user restarts. Load HANDOFF.md first — full UAT checklist + pre-UAT snapshot + Ollama synthesis recipe + escalation playbook.

    **Fresh-session executor automates:**

    1. Confirm the fresh session sees projekt-forge MCP tools (mcp__projekt-forge__* present in tool list). If NOT present, STOP and escalate per HANDOFF.md.

    2. **UAT step 1** — tool enumeration. Record every mcp__projekt-forge__* tool name.

    3. **UAT step 2** — call `mcp__projekt-forge__flame_ping` with no arguments. Capture verbatim result.

    4. **UAT step 3** — run the Ollama synthesis driver:
       ```bash
       /Users/cnoellert/miniconda3/envs/forge/bin/python /tmp/ollama_synth_uat.py
       ```
       Or re-create the driver per HANDOFF.md recipe. Confirm new synth_*.py + .sidecar.json appeared:
       ```bash
       ls -latr ~/.forge-bridge/synthesized/ | tail -5
       cat ~/.forge-bridge/synthesized/<NEW_SIDECAR>.sidecar.json | head -40
       ```
       Record the exact filename stem (you will use it for cleanup).

    5. **UAT step 4** — wait ~6s, refresh tool list. New mcp__projekt-forge__synth_* tool must appear. Inspect its _meta (via tool-detail view or list_tools response). Capture verbatim _meta. Verify:
       - `forge-bridge/origin` == `synthesizer`
       - `forge-bridge/code_hash` non-empty string
       - `forge-bridge/synthesized_at` ISO 8601 timestamp
       - `forge-bridge/version` == `1.2.1`
       - `forge-bridge/observation_count` integer ≥ 0

    6. **UAT step 5** — call the new synth_* tool. Capture verbatim result.

    7. **UAT step 6** — pick 2 mcp__projekt-forge__flame_* tools + 2 mcp__projekt-forge__forge_* tools (non-synth). Inspect each _meta. NONE may contain `forge-bridge/origin`, `forge-bridge/code_hash`, `forge-bridge/synthesized_at`, `forge-bridge/version`, or `forge-bridge/observation_count`. `_source` tag is permitted.

    8. Write `.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md` following the structure in HANDOFF.md. Header:
       ```markdown
       # Phase 07.1 — Deployment UAT Evidence

       **Session:** Fresh Claude Code session, post-restart
       **Date:** <ISO 8601>
       **forge-bridge version under test:** 1.2.1
       **projekt-forge pin:** @v1.2.1 (Plan 03)
       **No monkey-patches or harness scripts used.** MCP server started by Claude Code directly via `claude mcp add` registration.
       ```

       One section per UAT step (1-6) with Timestamp / Method / Verbatim result / Pass-fail. Then:
       ```markdown
       ## Summary
       - SC1: startup_bridge graceful degradation — PASS/FAIL
       - SC5: PROV-02 _meta end-to-end via real MCP client — PASS/FAIL
       ```

    9. If ALL six checklist items PASS, cleanup using the EXACT filename stem recorded in step 4:
       ```bash
       rm ~/.forge-bridge/synthesized/<STEM>.py
       rm ~/.forge-bridge/synthesized/<STEM>.sidecar.json
       rm -f ~/.forge-bridge/synthesized/<STEM>.tags.json
       # Remove <STEM> entry from manifest.json
       ls -la ~/.forge-bridge/synthesized/  # must match pre-UAT snapshot from HANDOFF.md
       ```

    10. Commit:
        ```bash
        cd /Users/cnoellert/Documents/GitHub/forge-bridge
        git add .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md
        git commit -m "$(cat <<'EOF'
        docs(07.1): UAT evidence — PROV-02 _meta end-to-end via real MCP client

        Verbatim output from the six-step UAT checklist in a fresh Claude Code
        session post-restart. NO monkey-patches. NO harness scripts. forge-bridge
        v1.2.1 + projekt-forge @v1.2.1 pin.

        Results: SC1 (graceful degradation): PASS/FAIL. SC5 (PROV-02 end-to-end):
        PASS/FAIL. Cleanup: throwaway synth_* artifacts removed; manifest
        restored to pre-UAT baseline.

        Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
        EOF
        )"
        ```
        Replace PASS/FAIL placeholders in the commit message with the actual results.

    **Human verifies:**
    1. [ ] UAT-EVIDENCE.md exists with all six sections filled with verbatim data (NOT placeholders).
    2. [ ] Each section's pass/fail clearly stated.
    3. [ ] Cleanup performed — `ls ~/.forge-bridge/synthesized/` matches pre-UAT snapshot in HANDOFF.md.
    4. [ ] If any step FAILED, a clear description of the failure mode is captured.
  </how-to-verify>
  <files>.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md, ~/.forge-bridge/synthesized/ (cleaned)</files>
  <action>
    This task runs in the FRESH Claude Code session. Load 07.1-HANDOFF.md first — full checklist + pre-UAT snapshot + Ollama driver recipe + escalation.

    Execute the ten automation steps in how-to-verify above. Paste tool-call results character-for-character. If a step fails, document precisely and stop before cleanup — the failure IS the evidence. Do not invent pass/fail judgments; the evidence file must reflect what actually happened.
  </action>
  <verify>
    <automated>test -f /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md && grep -q "forge-bridge/origin" /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md && grep -q "forge-bridge/version" /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md && grep -qE "(PASS|FAIL)" /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md` passes.
    - UAT-EVIDENCE.md contains sections for all six UAT steps (grep for `UAT step 1`, `UAT step 2`, ..., `UAT step 6`).
    - UAT-EVIDENCE.md contains the literal strings `forge-bridge/origin`, `forge-bridge/code_hash`, `forge-bridge/synthesized_at`, `forge-bridge/version`, `forge-bridge/observation_count`.
    - UAT-EVIDENCE.md contains the string `1.2.1` (the version_under_test).
    - UAT-EVIDENCE.md contains a "Summary" section with entries for SC1 and SC5.
    - Each UAT step section contains `PASS` or `FAIL` explicitly.
    - `ls ~/.forge-bridge/synthesized/` file count matches the pre-UAT snapshot count recorded in HANDOFF.md (no more, no fewer — indicates cleanup ran).
    - `git log --oneline -1 .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md` shows the evidence commit.
    - Human typed "approved" after reviewing the evidence file.
  </acceptance_criteria>
  <done>
    UAT-EVIDENCE.md captures all six checklist steps with verbatim output and PASS/FAIL; SC1 and SC5 marked in the Summary; cleanup restored ~/.forge-bridge/synthesized/ to pre-UAT state; evidence committed; human approves.
  </done>
  <resume-signal>Type "approved" if UAT-EVIDENCE.md captures all six steps with verbatim output, SC1 and SC5 both marked PASS, and cleanup matches pre-UAT snapshot. Type "failed: ..." with a description if any UAT step failed (include which step + the specific failure mode).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User's Claude Code session → projekt-forge MCP subprocess | stdio transport opened by Claude Code; projekt-forge inherits the user's env. |
| projekt-forge MCP → forge-bridge v1.2.1 (via pinned dep) | In-process Python import; trust boundary = the release artifact itself (verified by git tag identity in Plan 02). |
| forge-bridge watcher → ~/.forge-bridge/synthesized/*.sidecar.json | Filesystem trust boundary; the sanitization layer (PROV-03, already shipped in v1.2.0) strips control chars + injection markers from user-supplied tags. |
| Ollama synthesis → ~/.forge-bridge/synthesized/ | Local filesystem; synthesizer writes trusted code paths (but code executes via bridge.execute inside the MCP server process). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-08 | Tampering | A stale monkey-patched launcher (e.g. /tmp/run_pf_mcp.py) intercepts the MCP registration | mitigate | HANDOFF.md Step 2 explicitly records the registration command; `claude mcp list` verifies the registered command points directly at `python -m projekt_forge --no-db`, not any shim. |
| T-07.1-09 | Information Disclosure | UAT-EVIDENCE.md captures synthesis output that may include internal project/shot codes | accept | Evidence file is a phase-local planning document; stays in the repo under .planning/. Not pushed to any external system beyond git-origin which is the user's own GitHub repo. |
| T-07.1-10 | Denial of Service | The fresh Claude Code session can't start projekt-forge because of a real regression in v1.2.1 | mitigate | If Task 2 fails with an exception at MCP server startup, the fix is invalidated — this is the TRUE test of SC1. Escalation path in HANDOFF.md documents how to capture the stderr for diagnosis. |
| T-07.1-11 | Elevation of Privilege | Synthesized tool gets invoked with `readOnlyHint=True` by mistake (MCP client auto-approve) | mitigate | PROV-04 is pre-shipped in v1.2.0 — verified in UAT step 4 by inspecting the annotations field alongside _meta. If readOnlyHint is not False, it's a PROV-04 regression, not a Phase 07.1 concern, but still blocks the UAT. |
| T-07.1-12 | Repudiation | UAT later questioned — was it really done without monkey-patches? | mitigate | UAT-EVIDENCE.md header explicitly states "No monkey-patches or harness scripts used." + records the exact `claude mcp add` command, which Task 1's commit in HANDOFF.md also documents. |
</threat_model>

<verification>
- Task 1: HANDOFF.md exists, contains real pre-UAT snapshot and exact MCP registration command (not placeholders), `claude mcp list` shows projekt-forge, commit landed.
- Task 2 (post-restart): UAT-EVIDENCE.md exists with all six UAT step sections, contains all five PROV-02 _meta key names, contains `1.2.1` as version-under-test, has PASS/FAIL per step, has SC1 + SC5 Summary, cleanup restored ~/.forge-bridge/synthesized/, commit landed.
- Human has approved both checkpoints.
</verification>

<success_criteria>
- SC1 (full): `python -m projekt_forge --no-db` in the forge conda env on Portofino boots cleanly with no process on :9998. NO monkey-patches or shims. Verified live by the fresh Claude Code session's ability to call flame_ping successfully (step 2 of UAT).
- SC5 (full): Real MCP client (the user's Claude Code session) observes PROV-02 _meta fields on a freshly Ollama-synthesized synth_* tool with verbatim evidence captured.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-04-SUMMARY.md`
</output>
