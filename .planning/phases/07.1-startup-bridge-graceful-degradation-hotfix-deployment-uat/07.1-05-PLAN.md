---
phase: 07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat
plan: 05
type: execute
wave: 5
depends_on:
  - 04
files_modified:
  - .planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
tags: phase-closeout,backfill,roadmap-update

must_haves:
  truths:
    - "07-04-SUMMARY.md exists (backfilled if absent) with an explicit pointer to 07.1 as the true UAT vehicle"
    - "07-04-SUMMARY.md cites 07.1-UAT-EVIDENCE.md as the canonical deployment-UAT artifact"
    - "07-04-SUMMARY.md explicitly notes the 2026-04-20 harness-based attempt and why it was superseded"
    - "ROADMAP.md Phase 7 Plans list marks 07-04 as complete (- [x])"
    - "ROADMAP.md gsd-tools plan-progress update runs without error, or if it fails the SUMMARY + ROADMAP edit still lands"
    - "A single commit in forge-bridge lands the backfill + tracking update"
  artifacts:
    - path: ".planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md"
      provides: "Backfilled 07-04 summary with 07.1 cross-reference"
      contains: "07.1-UAT-EVIDENCE.md"
    - path: ".planning/ROADMAP.md"
      provides: "07-04 marked complete"
      contains: "- [x] 07-04-PLAN.md"
  key_links:
    - from: ".planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md"
      to: ".planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md"
      via: "explicit relative path reference"
      pattern: "07.1-UAT-EVIDENCE.md"
    - from: ".planning/ROADMAP.md"
      to: "07-04 completion"
      via: "checkbox state in Phase 7 Plans list"
      pattern: "- [x] 07-04-PLAN.md"
---

<objective>
Close out Phase 7 now that Phase 07.1 has delivered the real deployment UAT. This plan backfills `07-04-SUMMARY.md` with an explicit pointer to `07.1-UAT-EVIDENCE.md` as the canonical UAT artifact (the original 07-04 UAT attempt on 2026-04-20 was superseded by the hotfix + re-UAT in 07.1), marks 07-04 complete in ROADMAP.md via gsd-tools, and unblocks the Phase 7 verification pipeline (`gsd-code-review`, regression gate, schema-drift, gsd-verifier) which runs AFTER this plan completes — not as part of 07.1.

Purpose: SC6 of Phase 07.1. Phase 7's 07-04 plan did not produce a SUMMARY.md before the UAT session surfaced the deployment-blocking bug. Without backfilling 07-04 and marking it complete, Phase 7 close-out (and by extension Phase 8 which is gated on v1.2 milestone progress) stays stuck.

Output: 07-04-SUMMARY.md created or updated with the pointer to 07.1 as the UAT vehicle; ROADMAP.md updated to mark 07-04 complete; single atomic commit landing both changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-CONTEXT.md
@.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-04-PLAN.md
@.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-PLAN.md
@CLAUDE.md

<interfaces>
ROADMAP.md Phase 7 Plans section (current — line 59-63):
```
Plans:
- [x] 07-01-PLAN.md — Sidecar schema evolution: ...
- [x] 07-02-PLAN.md — Watcher read-path + _sanitize_tag() ...
- [x] 07-03-PLAN.md — register_tool(..., provenance=) kwarg + _meta merge ...
- [ ] 07-04-PLAN.md — Release ceremony: mcp[cli]>=1.19,<2 pin, v1.2.0 annotated tag on main, GitHub release (wheel + sdist), projekt-forge pin bump + cross-repo UAT tools/list diff
```

Target state — only the checkbox changes:
```
- [x] 07-04-PLAN.md — Release ceremony: ... (UAT superseded by Phase 07.1 — see 07.1-UAT-EVIDENCE.md)
```

gsd-tools command shape (per CONTEXT.md "Phase 7 Close-Out" under <decisions>):
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap update-plan-progress "07" "07-04" "complete"
```

This command edits ROADMAP.md's checkbox for 07-04 idempotently. If it fails (e.g. unexpected roadmap-format drift), the fallback is a manual edit.

Path to check for existing 07-04-SUMMARY.md:
`.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md`

The most recent Phase 7 handoff commit (`4b64232 docs(07-04): handoff note for session restart`) suggests there's already a HANDOFF.md in the 07 directory — the backfilled SUMMARY should cite both the release commit (0987525) AND the 07.1 UAT evidence as the joint completion artifact.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backfill 07-04-SUMMARY.md with pointer to 07.1 as the UAT vehicle</name>
  <files>.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/07-tool-provenance-in-mcp-annotations/07-04-PLAN.md (understand what 07-04 promised — specifically must_haves, three tasks, success criteria)
    - Check if `.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md` already exists (`ls` that path); if yes, read its current contents to append rather than overwrite
    - Check for any other handoff/summary files in 07-directory (`ls .planning/phases/07-tool-provenance-in-mcp-annotations/*.md`)
    - .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-04-PLAN.md (confirm the UAT evidence file path for the cross-reference)
    - $HOME/.claude/get-shit-done/templates/summary.md (summary format template)
  </read_first>
  <action>
    **Step 1 — Check for an existing SUMMARY:**
    ```bash
    ls -la /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07-tool-provenance-in-mcp-annotations/*.md
    test -f /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md && echo "EXISTS" || echo "MISSING — create fresh"
    ```

    If EXISTS, read the current content — you will APPEND a new section, not overwrite. If MISSING, create fresh per Step 2.

    **Step 2 — Write the backfilled SUMMARY.md** — use the EXACT content below. Replace only `<TIMESTAMP>` with current ISO 8601 and (if applicable) append-onto-existing rather than overwriting. Target path: `.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md`

    ```markdown
    # Phase 7 Plan 04 Summary — Release Ceremony + Deployment UAT

    **Plan:** 07-04 — Release Ceremony: v1.2.0 tag, GitHub release, projekt-forge pin bump, cross-repo UAT
    **Status:** COMPLETE (UAT completed via Phase 07.1)
    **Completed:** <TIMESTAMP>

    ## What shipped

    - forge-bridge `v1.2.0` annotated tag on main (commit `0987525`, 2026-04-20)
    - GitHub release at https://github.com/cnoellert/forge-bridge/releases/tag/v1.2.0 with wheel + sdist attached
    - `mcp[cli]>=1.19,<2` dependency pin
    - `__version__ == "1.2.0"` exposed via `importlib.metadata`
    - projekt-forge pinned to `@v1.2.0` (initial pin landed 2026-04-20; subsequently bumped to `@v1.2.1` per Phase 07.1 Plan 03)

    ## Deployment UAT — deferred to Phase 07.1

    The original UAT attempted on 2026-04-20 surfaced a deployment-blocking bug in
    `forge_bridge.mcp.server.startup_bridge`: the try/except guarded only
    `wait_until_connected()` and let `_client.start()`'s ConnectionRefusedError
    escape, crashing the MCP server lifespan on any machine without a listener on
    :9998. That session worked around the bug with a runtime monkey-patch
    (`/tmp/run_pf_mcp.py`) — which made the UAT "pass" but was not actually
    testing the deployment path.

    **The true deployment UAT for Phase 7's PROV-02 work is captured in
    Phase 07.1:**

    - **Hotfix:** `forge_bridge/mcp/server.py startup_bridge()` wraps BOTH
      `_client.start()` AND `wait_until_connected()` in a single try/except;
      `_client.stop()` called in a nested try/except on failure; `_client = None`
      so `shutdown_bridge()` cleanly skips. Released as `v1.2.1`.
    - **Regression test:** `tests/test_mcp_server_graceful_degradation.py` — FAILED
      against v1.2.0, PASSES against v1.2.1.
    - **UAT vehicle:** the user's Claude Code session on Portofino with
      projekt-forge registered as an MCP server via `claude mcp add` — a REAL
      MCP client, no monkey-patches, no harness.
    - **UAT evidence:** `.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md`
      captures verbatim tool-call results proving all five PROV-02 `_meta` keys
      (`forge-bridge/origin`, `code_hash`, `synthesized_at`, `version`,
      `observation_count`) reach a real MCP client on a freshly Ollama-synthesized
      synth_* tool, AND confirms `flame_*` / `forge_*` (non-synth) tools have no
      PROV-02 contamination.

    ## Commits carrying this plan

    - `0987525` — chore(release): bump version 1.1.1 → 1.2.0; pin mcp[cli]>=1.19,<2
    - `4b64232` — docs(07-04): handoff note for session restart (original UAT)
    - (Phase 07.1 commits — RED test, GREEN fix, release bump, pin bump, handoff, evidence)

    ## Requirements closed

    - PROV-01 through PROV-06 — verified via Phase 07.1 UAT (see UAT-EVIDENCE.md)

    ## What did not ship in 07-04 (intentionally deferred)

    - None — all 07-04 deliverables shipped. The UAT was deferred to 07.1 because the
      latent bug made the original UAT environment an unreliable deployment reproducer.

    ## Next

    Phase 7 close-out pipeline (runs AFTER this plan):
    1. `/gsd-code-review 7` (advisory)
    2. Regression gate (prior-phase tests)
    3. `gsd-tools.cjs verify schema-drift 07`
    4. Spawn `gsd-verifier` for `07-VERIFICATION.md`
    5. `gsd-tools.cjs phase complete 07`

    Phase 8 (StoragePersistence Protocol) is gated on Phase 7 close-out completing.
    ```

    **Step 3 — Verify the SUMMARY has no placeholders left:**
    ```bash
    grep -n '<TIMESTAMP>' /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md && echo "PLACEHOLDER STILL PRESENT — FIX" || echo "OK"
    ```
    All `<...>` placeholders must be replaced with real values before committing.
  </action>
  <verify>
    <automated>test -f /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md && grep -q "07.1-UAT-EVIDENCE.md" /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md && grep -q "startup_bridge" /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md && ! grep -q '<TIMESTAMP>' /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md` passes.
    - SUMMARY.md contains the literal strings `07.1-UAT-EVIDENCE.md`, `startup_bridge`, `v1.2.0`, `v1.2.1`, `monkey-patch` (or `monkey-patched`), `Claude Code`, and `qwen2.5-coder` is NOT required but `Ollama` or `synthesis` should appear.
    - SUMMARY.md contains ALL six `PROV-0` IDs (`PROV-01` through `PROV-06`).
    - SUMMARY.md contains NO literal placeholders (no `<TIMESTAMP>`, no `<...>`).
    - SUMMARY.md references commit SHA `0987525` (the v1.2.0 release commit).
  </acceptance_criteria>
  <done>
    07-04-SUMMARY.md exists with the backfill content; explicit pointer to 07.1-UAT-EVIDENCE.md as the canonical UAT vehicle; no placeholders remaining; all PROV-0X IDs mentioned.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mark 07-04 complete in ROADMAP.md + commit both files atomically</name>
  <files>.planning/ROADMAP.md, .planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 57-63 (verify current state of Phase 7 Plans list — 07-04 should be `- [ ]` unchecked)
    - $HOME/.claude/get-shit-done/bin/gsd-tools.cjs help output for `roadmap update-plan-progress`:
      ```bash
      node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap --help 2>&1 || true
      ```
  </read_first>
  <action>
    **Step 1 — Update the ROADMAP.md 07-04 checkbox via gsd-tools:**
    ```bash
    cd /Users/cnoellert/Documents/GitHub/forge-bridge
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap update-plan-progress "07" "07-04" "complete"
    ```

    Expected: the command edits ROADMAP.md line ~63 from `- [ ] 07-04-PLAN.md` to `- [x] 07-04-PLAN.md` (and may update any summary/progress counters).

    If the command fails (e.g. "plan not found in phase 07", "roadmap format drift"):
    - Fall back to a manual edit: open `.planning/ROADMAP.md`, find the line starting with `- [ ] 07-04-PLAN.md`, change `- [ ]` to `- [x]`. Do NOT touch any other line.
    - Document in the task summary why the gsd-tools call failed so the pattern can be fixed upstream.

    **Step 2 — Verify the edit landed:**
    ```bash
    grep -n '07-04-PLAN.md' /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/ROADMAP.md
    ```
    Expected: the matched line starts with `- [x] 07-04-PLAN.md`.

    **Step 3 — (Optional but recommended) Append a "UAT superseded" annotation to the ROADMAP line** — this helps future readers understand the 07.1 relationship without having to dig. Append to the 07-04 line:
    ```
    - [x] 07-04-PLAN.md — Release ceremony: mcp[cli]>=1.19,<2 pin, v1.2.0 annotated tag on main, GitHub release (wheel + sdist), projekt-forge pin bump + cross-repo UAT tools/list diff (UAT vehicle deferred to Phase 07.1 — see .planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-UAT-EVIDENCE.md)
    ```
    Use a string replacement or direct file edit. Only modify THIS one line.

    **Step 4 — Commit both files atomically:**
    ```bash
    cd /Users/cnoellert/Documents/GitHub/forge-bridge
    git add .planning/ROADMAP.md .planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md
    git commit -m "$(cat <<'EOF'
    docs(07): close out 07-04 — backfill SUMMARY + mark complete in ROADMAP

    Phase 07.1 delivered the real deployment UAT that 07-04 promised. Closes
    out Phase 7 so the verification pipeline can run (gsd-code-review,
    regression gate, schema-drift, gsd-verifier) before Phase 8 starts.

    07-04-SUMMARY.md backfilled:
      - v1.2.0 shipped on 2026-04-20 (commit 0987525)
      - Original UAT attempt on 2026-04-20 surfaced startup_bridge deployment bug
      - True UAT deferred to Phase 07.1 (see 07.1-UAT-EVIDENCE.md)
      - All PROV-01..06 requirements confirmed via 07.1 UAT

    ROADMAP.md:
      - 07-04 checkbox flipped from [ ] to [x]
      - Inline annotation points to 07.1-UAT-EVIDENCE.md as the canonical
        UAT artifact

    Does NOT run the Phase 7 close-out pipeline (gsd-code-review, schema-drift,
    gsd-verifier, phase-complete) — that runs separately, after this commit,
    as the orchestrator decides.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>grep -qE '^\- \[x\] 07-04-PLAN\.md' /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/ROADMAP.md && ! grep -qE '^\- \[ \] 07-04-PLAN\.md' /Users/cnoellert/Documents/GitHub/forge-bridge/.planning/ROADMAP.md && git -C /Users/cnoellert/Documents/GitHub/forge-bridge log --oneline -1 | grep -q "docs(07):"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\- \[x\] 07-04-PLAN\.md' .planning/ROADMAP.md` returns 1.
    - `grep -cE '^\- \[ \] 07-04-PLAN\.md' .planning/ROADMAP.md` returns 0 (old unchecked state gone).
    - The 07-04 line in ROADMAP.md contains a reference to `07.1` somewhere in its inline text (the annotation from Step 3).
    - `git diff HEAD~1 HEAD --stat` shows exactly 2 files changed: `.planning/ROADMAP.md` and `.planning/phases/07-tool-provenance-in-mcp-annotations/07-04-SUMMARY.md`.
    - `git log --oneline -1` subject starts with `docs(07): close out 07-04`.
  </acceptance_criteria>
  <done>
    ROADMAP.md marks 07-04 complete with a cross-reference annotation to 07.1; SUMMARY.md backfilled; single atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gsd-tools.cjs → ROADMAP.md | Programmatic roadmap edit; trust = the gsd-tools CLI itself. Manual fallback edit is available if the tool can't parse the roadmap. |
| Git working tree → committed state | Atomic commit ensures ROADMAP + SUMMARY land together; no mid-edit state exposed to verification pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-13 | Tampering | gsd-tools silently corrupts ROADMAP.md | mitigate | Step 2 verification greps for the expected checkbox state; fallback is manual edit with precise target specified. |
| T-07.1-14 | Repudiation | Future session can't tell whether 07-04 was really UAT'd | mitigate | SUMMARY.md explicitly narrates the 07-04→07.1 deferral with commit SHAs; ROADMAP.md inline annotation points to the evidence file. |
| T-07.1-15 | Denial of Service | Phase 7 verification pipeline can't run because ROADMAP is in an inconsistent state | mitigate | Atomic commit ensures pipeline sees either "07-04 incomplete" or "07-04 complete with SUMMARY" — never a half-state. |
</threat_model>

<verification>
- Task 1: 07-04-SUMMARY.md exists, cross-references 07.1-UAT-EVIDENCE.md, no placeholders, mentions all six PROV-0X IDs.
- Task 2: ROADMAP.md Phase 7 07-04 line is `- [x] 07-04-PLAN.md` with inline 07.1 annotation; atomic commit landed.
- `git status` clean after the commit (no stray edits in other files).
</verification>

<success_criteria>
- SC6 (full): Phase 7 close-out unblocked. 07-04-SUMMARY.md backfilled with the pointer to 07.1 as the UAT vehicle. 07-04 plan marked complete via gsd-tools (with manual fallback documented if the tool errors). Phase 7 verification pipeline can now proceed (runs AFTER this plan, not part of 07.1).
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat/07.1-05-SUMMARY.md`
</output>
