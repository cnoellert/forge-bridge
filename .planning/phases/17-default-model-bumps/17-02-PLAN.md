---
phase: 17-default-model-bumps
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - forge_bridge/llm/router.py
  - tests/test_llm.py
  - .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md
autonomous: false
requirements: [MODEL-01]

must_haves:
  truths:
    - "`_DEFAULT_CLOUD_MODEL` value is `claude-sonnet-4-6` (was `claude-opus-4-6` after P-01)."
    - "`LLMRouter().cloud_model == 'claude-sonnet-4-6'` (verified by a default-suite unit test that runs without API keys or `FB_INTEGRATION_TESTS=1`)."
    - "Live LLMTOOL-02 (`test_anthropic_tool_call_loop_live`) PASSES against the new default with NO `FORGE_CLOUD_MODEL` env override."
    - "`SEED-OPUS-4-7-TEMPERATURE-V1.5.md` exists in `.planning/seeds/` and captures (a) `claude-opus-4-7` rejects `temperature` per v1.4 audit line 273, and (b) AnthropicAdapter currently always sends `temperature`."
    - "The bump commit on `forge_bridge/llm/router.py` is a one-line value flip — no other source lines in the file change in that commit."
  artifacts:
    - path: "forge_bridge/llm/router.py"
      provides: "Updated `_DEFAULT_CLOUD_MODEL = \"claude-sonnet-4-6\"` constant value."
      contains: "_DEFAULT_CLOUD_MODEL = \"claude-sonnet-4-6\""
    - path: "tests/test_llm.py"
      provides: "Default-value regression assertion: `LLMRouter().cloud_model == 'claude-sonnet-4-6'`."
      contains: "claude-sonnet-4-6"
    - path: ".planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md"
      provides: "v1.5 forward-looking seed for AnthropicAdapter `temperature` elision before any opus-4-7 bump becomes viable."
      contains: "temperature is deprecated"
  key_links:
    - from: "tests/test_llm.py::test_default_fallback"
      to: "_DEFAULT_CLOUD_MODEL constant in forge_bridge/llm/router.py"
      via: "default-instantiation regression assertion"
      pattern: 'router\.cloud_model == "claude-sonnet-4-6"'
    - from: "tests/integration/test_complete_with_tools_live.py::test_anthropic_tool_call_loop_live"
      to: "LLMRouter() default cloud_model"
      via: "live Anthropic API tool-call loop with NO FORGE_CLOUD_MODEL override"
      pattern: "FORGE-INTEGRATION-SENTINEL"
---

<objective>
MODEL-01 cloud-default bump: flip `_DEFAULT_CLOUD_MODEL` from `"claude-opus-4-6"` to `"claude-sonnet-4-6"` in `forge_bridge/llm/router.py`. Single one-line value change in the source file. Add a default-suite regression assertion in `tests/test_llm.py`. Plant `SEED-OPUS-4-7-TEMPERATURE-V1.5.md` to capture the AnthropicAdapter `temperature`-elision work needed before any future opus-4-7 bump becomes viable.

Purpose: The current default `claude-opus-4-6` returns 500 from the live Anthropic API (deprecated alias per v1.4 LLMTOOL-02 audit). `claude-sonnet-4-6` was already verified passing in v1.4 LLMTOOL-02 UAT after the `tool_choice` + `additionalProperties: false` adapter fixes. Per Phase 15 D-30, the bump is shipped as an isolated commit touching ONLY `_DEFAULT_*` constants — `git blame` on this line will read "MODEL-01 cloud bump", not coupled with any other change.

NOT targeting `claude-opus-4-7` despite the original SEED-CLOUD-MODEL-BUMP-V1.4.x language: opus-4-7 rejects `temperature` (v1.4 audit line 273) and AnthropicAdapter currently always sends it. Switching to opus-4-7 would require an adapter change, violating the Phase 17 "isolated default-only" boundary. SEED-OPUS-4-7-TEMPERATURE-V1.5 captures the v1.5 follow-up.

Output: Two atomic commits — (1) the source bump + regression test, (2) the seed plant. (Optionally combinable, but separating them keeps the bump commit pure per D-30.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-default-model-bumps/17-CONTEXT.md
@.planning/phases/17-default-model-bumps/17-01-SUMMARY.md
@.planning/seeds/SEED-CLOUD-MODEL-BUMP-V1.4.x.md
@forge_bridge/llm/router.py
@tests/test_llm.py
@./CLAUDE.md

<interfaces>
<!-- After P-01, the constant lives at module scope. P-02 flips ONLY the value string. -->

From forge_bridge/llm/router.py (post-P-01):
```python
# Default Anthropic model used for non-sensitive (cloud) completions.
# claude-opus-4-6 is the v1.4 ship value (Phase 15 D-30); a Phase 17 bump to
# claude-sonnet-4-6 is the next planned change (MODEL-01).
# Override via the FORGE_CLOUD_MODEL env var or the cloud_model= kwarg on LLMRouter().
_DEFAULT_CLOUD_MODEL = "claude-opus-4-6"   # ← THIS LINE flips to "claude-sonnet-4-6" in P-02
```

From tests/test_llm.py (lines 86-96) — existing default-value regression test that this plan UPDATES:
```python
def test_default_fallback(monkeypatch):
    """LLMRouter() with no args and no env uses hardcoded defaults."""
    for key in ("FORGE_LOCAL_LLM_URL", "FORGE_LOCAL_MODEL",
                "FORGE_CLOUD_MODEL", "FORGE_SYSTEM_PROMPT"):
        monkeypatch.delenv(key, raising=False)
    from forge_bridge.llm.router import LLMRouter
    router = LLMRouter()
    assert router.local_url == "http://localhost:11434/v1"
    assert router.local_model == "qwen2.5-coder:32b"
    assert router.cloud_model == "claude-opus-4-6"        # ← THIS LINE flips to "claude-sonnet-4-6"
    assert "Flame" in router.system_prompt
```

From tests/integration/test_complete_with_tools_live.py — read-only verification gate:
```
test_anthropic_tool_call_loop_live   # LLMTOOL-02 — runs with FB_INTEGRATION_TESTS=1 + ANTHROPIC_API_KEY
                                     # MUST pass with NO FORGE_CLOUD_MODEL env override (acceptance criterion)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip `_DEFAULT_CLOUD_MODEL` value and update default-suite regression assertion</name>
  <files>forge_bridge/llm/router.py, tests/test_llm.py</files>
  <read_first>
    - forge_bridge/llm/router.py (post-P-01 state — locate the `_DEFAULT_CLOUD_MODEL = "claude-opus-4-6"` line by content)
    - tests/test_llm.py lines 86-96 (the `test_default_fallback` test that asserts default values; the `cloud_model` line is the one to update)
    - .planning/REQUIREMENTS.md §MODEL-01 (acceptance text — `LLMRouter().cloud_model == "claude-sonnet-4-6"`; LLMTOOL-02 still passes against the new default with no env override; no other source files touched in the same commit)
    - .planning/phases/17-default-model-bumps/17-CONTEXT.md §"D-03 P-02" (plan boundary)
    - .planning/phases/17-default-model-bumps/17-01-SUMMARY.md (verify P-01 landed cleanly)
  </read_first>
  <action>
    Two edits in one atomic commit:

    **Edit 1 — `forge_bridge/llm/router.py`**:
    Change the line:
    ```python
    _DEFAULT_CLOUD_MODEL = "claude-opus-4-6"
    ```
    to:
    ```python
    _DEFAULT_CLOUD_MODEL = "claude-sonnet-4-6"
    ```

    Also update the comment block immediately above the constant. After P-01 the comment reads (verbatim):
    ```python
    # Default Anthropic model used for non-sensitive (cloud) completions.
    # claude-opus-4-6 is the v1.4 ship value (Phase 15 D-30); a Phase 17 bump to
    # claude-sonnet-4-6 is the next planned change (MODEL-01).
    # Override via the FORGE_CLOUD_MODEL env var or the cloud_model= kwarg on LLMRouter().
    ```
    Update it to (verbatim):
    ```python
    # Default Anthropic model used for non-sensitive (cloud) completions.
    # claude-sonnet-4-6 is the Phase 17 (v1.4.x) bump from claude-opus-4-6 (deprecated;
    # returned 500 from the live Anthropic API per v1.4 LLMTOOL-02 UAT). Verified
    # passing in v1.4 LLMTOOL-02 UAT after the tool_choice + additionalProperties:
    # false adapter fixes. See SEED-OPUS-4-7-TEMPERATURE-V1.5 for the next bump path.
    # Override via the FORGE_CLOUD_MODEL env var or the cloud_model= kwarg on LLMRouter().
    ```

    Also update the `LLMRouter` class docstring at lines ~160-169 — the line that reads `-> Model: claude-opus-4-6` becomes `-> Model: claude-sonnet-4-6`, and the env-override block line `FORGE_CLOUD_MODEL      default: claude-opus-4-6` becomes `FORGE_CLOUD_MODEL      default: claude-sonnet-4-6`. These are docstring-only changes that match the new constant value; they keep `git diff` honest.

    Do NOT touch `_DEFAULT_LOCAL_MODEL`, `_DEFAULT_SYSTEM_PROMPT`, the `__init__` body, the FB-C exception classes, the `_in_tool_loop` ContextVar, or any method bodies. The only literal value that changes is `"claude-opus-4-6"` → `"claude-sonnet-4-6"` (one occurrence in the constant + the docstring/comment mirrors).

    **Edit 2 — `tests/test_llm.py`**:
    In `test_default_fallback` (around line 95), change:
    ```python
    assert router.cloud_model == "claude-opus-4-6"
    ```
    to:
    ```python
    assert router.cloud_model == "claude-sonnet-4-6"
    ```

    The surrounding test body (env clearing, `LLMRouter()` instantiation, the local_model + system_prompt assertions) does NOT change. The test continues to run in the default suite with NO `FB_INTEGRATION_TESTS=1` and NO API keys — exactly what D-04 calls for.

    Do NOT add a new test function — the existing `test_default_fallback` is the right home (already gates all four defaults in one place; adding a parallel test would duplicate the env-clearing fixture). This is the choice D-04 leaves to "Claude's discretion" (CONTEXT.md §Discretion); updating in place is cleaner than splitting.

    Do NOT touch any other test file. Do NOT touch `tests/integration/`. Do NOT add `FB_INTEGRATION_TESTS=1` gating to this assertion.

    Commit message:
    ```
    feat(llm/router): bump _DEFAULT_CLOUD_MODEL to claude-sonnet-4-6 (MODEL-01)

    Closes MODEL-01 / SEED-CLOUD-MODEL-BUMP-V1.4.x. claude-opus-4-6 was deprecated
    and returned 500 from the live Anthropic API (v1.4 LLMTOOL-02 audit).
    claude-sonnet-4-6 was verified passing in v1.4 LLMTOOL-02 UAT after the
    tool_choice + additionalProperties:false adapter fixes.

    Single-line constant flip per Phase 15 D-30 decoupled-commit mandate.
    Regression test updated in tests/test_llm.py::test_default_fallback.

    Phase 17 P-02 of 3.
    ```
  </action>
  <acceptance_criteria>
    - `grep -c '^_DEFAULT_CLOUD_MODEL = "claude-sonnet-4-6"$' forge_bridge/llm/router.py` returns `1`
    - `grep -c '"claude-opus-4-6"' forge_bridge/llm/router.py` returns `0` (the deprecated value is purged from the source file)
    - `grep -c 'claude-opus-4-6' forge_bridge/llm/router.py` returns `0` (also purged from comments and docstring)
    - `grep -c 'claude-sonnet-4-6' forge_bridge/llm/router.py` is `>= 1` (constant) and ideally `>= 3` if comment + docstring are updated as instructed
    - `grep -n 'router.cloud_model == "claude-sonnet-4-6"' tests/test_llm.py` returns exactly one match (the updated `test_default_fallback` line)
    - `grep -c 'router.cloud_model == "claude-opus-4-6"' tests/test_llm.py` returns `0`
    - `pytest tests/test_llm.py::test_default_fallback -v` exits 0
    - `pytest tests/ -q` exits 0 (no other test broken by the bump)
    - `python -c "from forge_bridge.llm.router import LLMRouter; r = LLMRouter(); assert r.cloud_model == 'claude-sonnet-4-6', r.cloud_model; print('ok')"` exits 0 and prints `ok`
    - `git diff --name-only HEAD~1 HEAD` shows exactly two files: `forge_bridge/llm/router.py` and `tests/test_llm.py` (commit isolation)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_llm.py::test_default_fallback -v && pytest tests/ -q</automated>
  </verify>
  <done>
    - `_DEFAULT_CLOUD_MODEL` now `"claude-sonnet-4-6"` in `forge_bridge/llm/router.py`
    - `tests/test_llm.py::test_default_fallback` asserts the new value and passes in the default suite
    - Full default suite passes
    - One atomic commit covering both files (the bump + its regression guard belong together)
  </done>
</task>

<task type="auto">
  <name>Task 2: Plant SEED-OPUS-4-7-TEMPERATURE-V1.5.md</name>
  <files>.planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md</files>
  <read_first>
    - .planning/seeds/SEED-CLOUD-MODEL-BUMP-V1.4.x.md (canonical seed format — frontmatter shape, section headings, cross-reference style)
    - .planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md (second seed example to triangulate the format)
    - .planning/phases/17-default-model-bumps/17-CONTEXT.md §"D-05" (seed content directive — the two facts to capture)
  </read_first>
  <action>
    Create a new file at `.planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` mirroring the format of the two existing v1.4.x seeds. Use this content verbatim:

    ```markdown
    ---
    name: SEED-OPUS-4-7-TEMPERATURE-V1.5
    description: AnthropicAdapter must elide `temperature` for claude-opus-4-7 before any opus-4-7 default bump becomes viable
    type: forward-looking-idea
    planted_during: v1.4.x Phase 17 P-02 (MODEL-01 cloud bump, 2026-04-28)
    trigger_when: A v1.5 cloud-model bump consideration (claude-opus-4-7 or successor) AND/OR Anthropic deprecation of claude-sonnet-4-6
    ---

    # SEED-OPUS-4-7-TEMPERATURE-V1.5: Per-model `temperature` elision in AnthropicAdapter

    ## Idea

    `claude-opus-4-7` rejects the `temperature` parameter — the live API returns
    "temperature is deprecated for this model" (captured in the v1.4 milestone
    audit, line 273). `forge_bridge/llm/_adapters.py::AnthropicAdapter` currently
    sends `temperature` on every request unconditionally. Phase 17 MODEL-01
    therefore targets `claude-sonnet-4-6` (which still accepts temperature), not
    `claude-opus-4-7`, even though the original SEED-CLOUD-MODEL-BUMP-V1.4.x seed
    called for opus-4-7. Before any future bump to opus-4-7 (or any successor
    that rejects temperature), AnthropicAdapter must elide the parameter for the
    affected model(s).

    ## Why This Matters

    - **Phase 17 deliberately stops short of opus-4-7** — the "isolated default-
      only change" boundary in Phase 15 D-30 forbids coupling an adapter change
      to a default-bump commit.
    - **Anthropic deprecation cadence** — sonnet-4-6 will eventually be replaced
      by an opus-4-7-style model that also rejects `temperature`. The adapter
      change has to land before the bump, not alongside it.
    - **Determinism contract preserved** — current call sites pass
      `temperature=0.1` for deterministic pipeline-code generation. The fix is
      per-model elision, not unconditional removal.

    ## Two Facts to Carry Forward

    1. **`claude-opus-4-7` rejects `temperature`** — verified during v1.4 close-
       out (audit line 273). Live API response: `temperature is deprecated for
       this model`.
    2. **`AnthropicAdapter` always sends `temperature`** — see
       `forge_bridge/llm/_adapters.py` (the adapter's request-payload assembly
       path). No model-aware conditional today.

    ## When to Surface

    - A v1.5 milestone planning session that proposes bumping the cloud default
      to claude-opus-4-7 (or any model published after sonnet-4-6 that rejects
      `temperature`).
    - Anthropic announces deprecation of claude-sonnet-4-6 (forces the bump and
      therefore forces this seed's resolution).
    - Any v1.5 phase that touches `AnthropicAdapter` request assembly — fold this
      into the same change while you're already in the file.

    ## How to Apply

    1. Identify the set of Anthropic models that reject `temperature` (likely
       `claude-opus-4-7` and successors; confirm against current Anthropic API
       docs at apply time).
    2. Add a model-aware conditional in `forge_bridge/llm/_adapters.py`
       AnthropicAdapter request assembly — elide `temperature` when the target
       model is in the rejection set; pass-through otherwise. Default behavior
       for sonnet-4-6 and earlier MUST NOT change.
    3. Add a unit test that constructs the adapter for an opus-4-7-style model
       and asserts the request payload contains no `temperature` key. Add a
       parallel positive test for sonnet-4-6 confirming `temperature` is still
       sent.
    4. Re-run LLMTOOL-02 (`test_anthropic_tool_call_loop_live`) against the
       newer model to confirm tool-call loops still pass after the elision.
    5. Now — and only now — bump `_DEFAULT_CLOUD_MODEL` in
       `forge_bridge/llm/router.py` in an isolated commit (mirror the Phase 17
       P-02 commit shape).

    ## Cross-References

    - `.planning/milestones/v1.4-MILESTONE-AUDIT.md` line 273 — empirical capture
      of the opus-4-7 temperature-rejection error.
    - `.planning/seeds/SEED-CLOUD-MODEL-BUMP-V1.4.x.md` — the seed Phase 17
      MODEL-01 closes; this seed is its v1.5 successor for the opus-4-7 path.
    - `forge_bridge/llm/_adapters.py` — `AnthropicAdapter` (the file to edit).
    - `forge_bridge/llm/router.py` `_DEFAULT_CLOUD_MODEL` — the constant the
      eventual v1.5 bump touches.
    - Phase 15 D-30 — decoupled-commit mandate (still binding for the v1.5
      bump; the adapter change and the default flip are separate commits).
    ```

    Do NOT modify any source code in this task. Seed files live in `.planning/seeds/` and are documentation-only.

    Optional second commit (or amend into Task 1's commit if the repo's tooling prefers): the seed is a separate `docs(seeds): plant SEED-OPUS-4-7-TEMPERATURE-V1.5` commit. Either commit shape preserves the D-30 single-source-flip purity of Task 1's `feat(llm/router)` commit.

    Suggested commit message for the seed (if shipped separately):
    ```
    docs(seeds): plant SEED-OPUS-4-7-TEMPERATURE-V1.5

    Captures the v1.5 follow-up surfaced by Phase 17 MODEL-01: AnthropicAdapter
    must per-model elide `temperature` before any opus-4-7 (or successor) bump
    becomes viable. Two facts captured: opus-4-7 rejects temperature (v1.4 audit
    line 273); AnthropicAdapter currently always sends temperature.

    Phase 17 P-02 of 3 (companion to the MODEL-01 cloud bump).
    ```
  </action>
  <acceptance_criteria>
    - File exists: `test -f .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` exits 0
    - `grep -c '^name: SEED-OPUS-4-7-TEMPERATURE-V1.5$' .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` returns `1` (frontmatter present)
    - `grep -c 'temperature is deprecated' .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` returns `>= 1` (fact 1 captured verbatim)
    - `grep -ci 'AnthropicAdapter currently' .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` returns `>= 1` (fact 2 captured)
    - `grep -c 'How to Apply' .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` returns `1` (mirrors the seed-file structure of the other v1.4.x seeds)
    - `grep -c 'audit line 273\|line 273' .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` returns `>= 1` (cross-ref to milestone audit captured)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md && grep -q 'name: SEED-OPUS-4-7-TEMPERATURE-V1.5' .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md && grep -q 'temperature is deprecated' .planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md && echo OK</automated>
  </verify>
  <done>
    - `.planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` exists with frontmatter + the two facts + How-to-Apply section + cross-references
    - File mirrors the canonical SEED-* format (frontmatter shape, section headings)
    - Either committed as a standalone `docs(seeds):` commit or folded into Task 1's commit per the executor's repo-hygiene preference
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live LLMTOOL-02 verification (human-in-the-loop)</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - REQUIREMENTS.md §MODEL-01 acceptance text (the "no env override" criterion)
    - .planning/phases/17-default-model-bumps/17-CONTEXT.md §"specifics" (the exact verification command)
  </read_first>
  <what-built>
    `_DEFAULT_CLOUD_MODEL` was flipped to `"claude-sonnet-4-6"` in `forge_bridge/llm/router.py`. The default-suite regression assertion in `tests/test_llm.py::test_default_fallback` now asserts the new value and passes. SEED-OPUS-4-7-TEMPERATURE-V1.5 is planted. The acceptance criterion that requires live API access — "LLMTOOL-02 still passes against the new default with no env override" — needs an `ANTHROPIC_API_KEY` and is therefore the human-in-the-loop gate.
  </what-built>
  <how-to-verify>
    1. Confirm `ANTHROPIC_API_KEY` is set in the dev shell environment (`echo "${ANTHROPIC_API_KEY:0:7}…"` should print `sk-ant-…` or similar).

    2. Confirm NO `FORGE_CLOUD_MODEL` override is set: `unset FORGE_CLOUD_MODEL && env | grep -i forge_cloud_model` should print nothing.

    3. Run the live LLMTOOL-02 gate from the project root on dev:
       ```
       FB_INTEGRATION_TESTS=1 pytest tests/integration/test_complete_with_tools_live.py::test_anthropic_tool_call_loop_live -v
       ```

    4. Expected: test PASSES. The terminal LLM response contains the sentinel substring `FORGE-INTEGRATION-SENTINEL-XJK29Q` (or whatever sentinel the test asserts). Total elapsed time well under the test's `max_seconds` budget.

    5. Expected NOT-OK: any 4xx/5xx from the Anthropic API, sentinel missing, `LLMLoopBudgetExceeded` raised, OR the test reports it ran against a model OTHER than `claude-sonnet-4-6` (would mean the env override isn't actually unset).

    6. If PASSED: capture the test output (timing + sentinel echo) and paste into the resume signal. If FAILED: capture the failure (stdout + stderr + which assertion fired) and paste it back so the planner / executor can decide whether to revert P-02 or open a follow-up plan.

    Acceptance criterion (REQUIREMENTS.md MODEL-01 verbatim): "LLMTOOL-02 still passes against the new default with no env override; no other source files touched in the same commit." The "no env override" part is enforced by step 2; the "no other source files" part is enforced by Task 1's `git diff --name-only` acceptance criterion.
  </how-to-verify>
  <action>
    Pause for human verification per the `<how-to-verify>` block above. The executor sets up the environment (confirms `ANTHROPIC_API_KEY` set; confirms `FORGE_CLOUD_MODEL` unset), runs the live integration test, captures the output, and presents PASS / FAIL to the user. The user types the resume signal once they confirm the result.

    No files are modified by this task. All file changes were committed in Task 1 (source bump + regression test) and Task 2 (seed plant). Task 3 is purely a verification gate.
  </action>
  <acceptance_criteria>
    - `ANTHROPIC_API_KEY` confirmed set in the shell environment (verification step 1)
    - `FORGE_CLOUD_MODEL` confirmed unset in the shell environment (verification step 2)
    - `FB_INTEGRATION_TESTS=1 pytest tests/integration/test_complete_with_tools_live.py::test_anthropic_tool_call_loop_live -v` exits 0
    - Test output contains `FORGE-INTEGRATION-SENTINEL` substring (sentinel echoed in terminal LLM response)
    - User confirms PASS via the resume signal `LLMTOOL-02 PASS`
  </acceptance_criteria>
  <verify>
    <automated>FB_INTEGRATION_TESTS=1 pytest tests/integration/test_complete_with_tools_live.py::test_anthropic_tool_call_loop_live -v</automated>
  </verify>
  <done>
    - LLMTOOL-02 live test passes against the new default with no env override
    - User has typed `LLMTOOL-02 PASS` (or `LLMTOOL-02 FAIL` with captured output, in which case follow-up planning is needed)
    - MODEL-01 acceptance fully closed (default-value assertion + live UAT both green)
  </done>
  <resume-signal>
    Type `LLMTOOL-02 PASS` (with the test duration and sentinel confirmation) OR `LLMTOOL-02 FAIL` (with the captured failure output) to resume.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forge-bridge → Anthropic API | Cloud egress for non-sensitive completions. Boundary already exists pre-Phase-17; no surface change here — only the model identifier in the request body changes. No new auth, no new data classification, no new payload shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.02-01 | (n/a) | forge_bridge/llm/router.py `_DEFAULT_CLOUD_MODEL` | accept | No new security surface — model-identifier string change only. The sensitive=True (local) path is untouched; only the non-sensitive cloud default flips, and the sensitivity gate that decides which model to use is unchanged. ASVS L1 sufficient. |
</threat_model>

<verification>
- `pytest tests/test_llm.py::test_default_fallback -v` exits 0 with the new `claude-sonnet-4-6` assertion
- `pytest tests/ -q` exits 0 (no regression)
- Live `FB_INTEGRATION_TESTS=1 pytest tests/integration/test_complete_with_tools_live.py::test_anthropic_tool_call_loop_live -v` passes on dev with `ANTHROPIC_API_KEY` set and `FORGE_CLOUD_MODEL` unset
- `grep` checks confirm `claude-opus-4-6` is purged from `forge_bridge/llm/router.py`
- `.planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` exists with the prescribed content
- Source bump commit is isolated to `forge_bridge/llm/router.py` + `tests/test_llm.py` (regression guard)
</verification>

<success_criteria>
- `LLMRouter().cloud_model == "claude-sonnet-4-6"` (default-suite unit test passes)
- LLMTOOL-02 live UAT passes against the new default with NO `FORGE_CLOUD_MODEL` env override
- `SEED-OPUS-4-7-TEMPERATURE-V1.5.md` planted with two facts + How-to-Apply + cross-references
- Decoupled-commit purity preserved: the bump commit touches `forge_bridge/llm/router.py` (one-line value flip + comment/docstring mirrors) and `tests/test_llm.py` (the regression guard) only
- MODEL-01 acceptance fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/17-default-model-bumps/17-02-SUMMARY.md` documenting:
- Files modified: `forge_bridge/llm/router.py`, `tests/test_llm.py`, `.planning/seeds/SEED-OPUS-4-7-TEMPERATURE-V1.5.md` (new)
- Acceptance criteria evidence (grep output, default-suite pytest output, live LLMTOOL-02 output captured at the human-verify checkpoint)
- The exact LLMTOOL-02 command run + result (PASS, duration, sentinel echo)
- Confirmation that no other source files were touched in the bump commit
- Note that MODEL-01 is closed and SEED-CLOUD-MODEL-BUMP-V1.4.x can be retired (or the executor moves it to `.planning/seeds/closed/` per repo convention if such a path exists)
</output>
