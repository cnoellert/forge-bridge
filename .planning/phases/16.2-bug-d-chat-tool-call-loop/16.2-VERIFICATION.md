---
phase: 16.2-bug-d-chat-tool-call-loop
verified: 2026-04-27T22:00:00Z
status: passed
score: 5/5 ROADMAP success criteria + 23/23 plan-level must-have truths verified
overrides_applied: 1
overrides:
  - must_have: "tests/integration/test_chat_endpoint_live.py::test_chat_canonical_uat_prompt_under_60s gains two new D-06 assertions (D-06 #2 loop-iteration assertion via messages[].role)"
    reason: "Plan 03 D-06 #2 was structurally wrong — it asserted on body['messages'] (which carries only [user_input, final_assistant_answer]) while iteration evidence lives inside LLMRouter.complete_with_tools() and is NOT forwarded into the response body. The assertion as written could not succeed against any healthy response. Discovered, fixed, and re-verified within Plan 04's UAT loop (commit 5988de8 on dev / f657b01 on assist-01). D-06 #1 (the load-bearing Bug D regex guard) is intact. Server-side iteration evidence captured in 16.2-HUMAN-UAT.md (router log markers iter=1 → iter=2). Per CONTEXT D-09 'the floor must hold' — what the floor IS now holds; what it was supposed to be was structurally impossible. The HUMAN-UAT.md documents this in 'Live correction during UAT (Plan 03 D-06 #2 defect)'."
    accepted_by: "CN/dev"
    accepted_at: "2026-04-28T08:51:00Z"
gates_observed:
  - "D-08 #1 (Strategy B E2E live, FB_INTEGRATION_TESTS=1, qwen2.5-coder:32b on assist-01): PASS in 21.38s — operator-recorded in 16.2-HUMAN-UAT.md"
  - "D-08 #2a (adapter unit tests on assist-01): PASS 16/16 — operator-recorded"
  - "D-08 #2b (adapter unit tests on dev): PASS 16/16 — re-verified by this verifier on dev (16/16 in 0.02s)"
  - "D-08 #3 (CN/dev manual walkthrough at /api/v1/chat — surface-equivalent to /ui/chat): PASS — synthesized natural-language answer, 21–22s, regex-reject D-06 #1 classified terminal content correctly"
---

# Phase 16.2: Bug D — Chat Tool-Call Loop + Fresh-Operator UAT Verification Report

**Phase Goal:** Close Bug D — the chat tool-call loop terminating after one Ollama turn rendering raw tool-call JSON as the assistant's terminal text — and re-run the fresh-operator UAT to satisfy CHAT-04 and unblock v1.4 milestone close.

**Success Bar (CONTEXT D-08):** PASS-with-deviations IF AND ONLY IF all three technical-floor conditions hold:
1. Strategy B chat E2E passes live on assist-01 with `FB_INTEGRATION_TESTS=1` + live qwen2.5-coder:32b
2. Adapter unit tests pass on dev AND assist-01
3. CN/dev manual walkthrough produces a useful natural-language answer to the canonical CHAT-04 prompt

**Verified:** 2026-04-27T22:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria + plan must_haves)

#### ROADMAP-Level Success Criteria (5 / 5)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| SC1 | Captured Ollama HTTP response committed at `16.2-CAPTURED-OLLAMA-RESPONSE.json` AND as verbatim `_OLLAMA_BUG_D_RESPONSE_CONTENT` in `tests/llm/test_ollama_adapter.py`; shape exhibits empty/null `tool_calls` AND parseable `{"name": ..., "arguments": ...}` content | VERIFIED | Artifact JSON valid, `bug_d_shape_confirmed: True`, `tool_calls: None`, content parses to `{"name": "forge_tools_read", "arguments": {"name": "synthesis-tools"}}`. Test constant byte-identical to artifact (verified by Python re-extraction). |
| SC2 | RED adapter test in `TestOllamaToolAdapterBugDFallback::test_text_content_tool_call_salvaged` failed on `main` with explicit Bug D regression message; negative companion `test_plain_text_terminal_response_not_misclassified` PASSED on `main` | VERIFIED | RED state captured verbatim in `16.2-01-SUMMARY.md` (commit `15928d2`); transition trail RED→GREEN documented. Both tests now PASS post-Plan-02 (verifier re-ran live: 16/16 in `tests/llm/test_ollama_adapter.py`). |
| SC3 | GREEN fix in `_adapters.py` only: `_try_parse_text_tool_call` helper + salvage hook; helper NEVER raises; RED test flips to PASSED; negative test still PASSES; `AnthropicToolAdapter` / `router.py` / `console/handlers.py` byte-identical to main | VERIFIED | Helper at line 122 (49 lines, defensive try/except, type-guards on `name`/`arguments`, never raises — verifier re-ran 5 malformed-input invariants live, all return None). Salvage hook at line 485 with `text = ""` reset at line 488. AnthropicToolAdapter range contains 0 references to helper (verified by `awk` range scan). Last commit touching `router.py` is `9a9a56b` (Phase 16-01, pre-16.2); last touching `handlers.py` is `ae81af3` (Phase 16.1-01, pre-16.2). |
| SC4 | `test_chat_canonical_uat_prompt_under_60s` gains D-06 #1 (regex-reject) AND D-06 #2 (loop-iteration) assertions; no "natural prose detection" heuristic; default pytest skips cleanly without `FB_INTEGRATION_TESTS=1` | PASSED (override) | D-06 #1 verified at line 245; module-top regex at line 95 with 7-case invariant documented. D-06 #2 was dropped during Plan 04 UAT (commit `5988de8`) because it asserted on `body["messages"]` while iteration evidence is router-internal and NOT forwarded. Override accepted by CN/dev — see frontmatter. Default pytest skips cleanly: re-verified live (`1 skipped, 0 failed`). |
| SC5 | Fresh-operator UAT records `Outcome: PASS with deviations` in `16.2-HUMAN-UAT.md` IF AND ONLY IF all three D-08 conditions hold; D-10 ratification path documented as post-close annotation hook (does NOT gate close) | VERIFIED | HUMAN-UAT.md exists, `status: passed`, `Outcome: PASS with deviations` line present, all three D-08 conditions explicitly enumerated with PASS evidence (D-08 #1: 21.38s; D-08 #2a/b: 16/16; D-08 #3: synthesized natural-language answer verbatim). D-10 section explicitly documents "does not re-open Phase 16.2" + "stays open as post-close annotation hook". CHAT-04 referenced 10× in HUMAN-UAT. CN/dev sign-off present. |

#### Plan-Level Must-Have Truths

**Plan 01 (RED diagnosis lock) — 4 / 4 verified**

| #     | Truth | Status | Evidence |
| ----- | ----- | ------ | -------- |
| P1.1  | Operator captured verbatim Ollama HTTP response reproducing Bug D on assist-01 against qwen2.5-coder:32b | VERIFIED | Captured artifact at `.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-CAPTURED-OLLAMA-RESPONSE.json` valid JSON, `bug_d_shape_confirmed: True`, host: assist-01, model: qwen2.5-coder:32b, ollama 0.21.0. Capture script paper-trail at `capture-bug-d.sh`. |
| P1.2  | Adapter unit test feeds captured shape to `OllamaToolAdapter.send_turn()` and asserts non-empty tool_calls list | VERIFIED | `TestOllamaToolAdapterBugDFallback::test_text_content_tool_call_salvaged` at line 284. Asserts `len(resp.tool_calls) >= 1`, `tool_calls[0].tool_name == "forge_tools_read"`, `arguments == {"name": "synthesis-tools"}`, `text == ""`. |
| P1.3  | Test FAILS on main (RED commit) — locks diagnosis as regression guard before fix | VERIFIED | Commit `15928d2` shipped failing on main. Verbatim RED output preserved in `16.2-01-SUMMARY.md` lines 99-126 (assert 0 >= 1, Bug D regression diagnostic). |
| P1.4  | Negative-case companion test asserts plain-text terminal responses still return tool_calls=[] | VERIFIED | `test_plain_text_terminal_response_not_misclassified` at line 290+. Verifier re-ran live: PASSED. |

**Plan 02 (GREEN fix) — 6 / 6 verified**

| #     | Truth | Status | Evidence |
| ----- | ----- | ------ | -------- |
| P2.1  | `OllamaToolAdapter.send_turn()` salvages text-shaped tool call from `message.content` when `message.tool_calls` is empty | VERIFIED | Salvage hook at lines 484-488 of `_adapters.py`: `if not tool_calls and text: salvaged = _try_parse_text_tool_call(text); if salvaged is not None: tool_calls.append(salvaged); text = ""`. |
| P2.2  | After fix, RED test flips to GREEN — proves diagnosis-fix loop closed | VERIFIED | `16.2-02-SUMMARY.md` lines 99-134 capture GREEN output verbatim. Verifier re-ran live: `test_text_content_tool_call_salvaged PASSED`. |
| P2.3  | Negative test STILL passes — no false positives on legitimate prose | VERIFIED | Verifier re-ran live: `test_plain_text_terminal_response_not_misclassified PASSED`. |
| P2.4  | AnthropicToolAdapter is untouched — fix lives entirely inside OllamaToolAdapter | VERIFIED | `awk` range scan: `class AnthropicToolAdapter` to `class OllamaToolAdapter` contains 0 references to `_try_parse_text_tool_call`. Confirmed by manual inspection of helper placement (line 122, before any adapter class). |
| P2.5  | Salvage helper never raises — returns None on parse failure | VERIFIED | Verifier behavioral spot-check: `_try_parse_text_tool_call('not json')`, `'{'`, `''`, `'{"foo": "bar"}'` all return None; valid input returns `_ToolCall(ref="0:forge_tools_read", ...)`. Try/except `(json.JSONDecodeError, ValueError)` at line 150. Type guards at lines 152, 155, 164. |
| P2.6  | Full adapter test suite passes (no regression in Compile/AllowList/SendTurn/AppendResults/Errors) | VERIFIED | Verifier re-ran live: 16/16 PASSED in `tests/llm/test_ollama_adapter.py`. |

**Plan 03 (Strategy B E2E hardening) — 6 / 7 verified, 1 PASSED (override)**

| #     | Truth | Status | Evidence |
| ----- | ----- | ------ | -------- |
| P3.1  | `test_chat_canonical_uat_prompt_under_60s` gains two new D-06 assertions | PASSED (override) | D-06 #1 (regex-reject) shipped at line 245. D-06 #2 (loop-iteration) was shipped in commit `c7a8eb9` but discovered structurally wrong during Plan 04 UAT and dropped in commit `5988de8`. See override entry in frontmatter. |
| P3.2  | Assertion #1 (D-06 #1): rejects raw tool-call JSON via `^\s*\{\s*"name"\s*:` regex with explicit Bug D citation in failure message | VERIFIED | Regex at line 95: `_BUG_D_TERMINAL_JSON_RE = re.compile(r'^\s*\{\s*"name"\s*:', re.IGNORECASE)`. Assertion at line 245 with "Bug D regression — terminal content is raw tool-call JSON..." failure message citing Phase 16.2 D-03 + Phase 16.1 HUMAN-UAT. 7-case regex invariant documented in 16.2-03-SUMMARY.md (3 positive Bug-D, 4 negative legitimate-response). |
| P3.3  | Assertion #2 (D-06 #2): asserts `>=1 role=tool turn` AND `>=2 role=assistant turns` | PASSED (override) | Override applied — see SC4 evidence. The replacement is an in-source comment at lines 252-262 explaining why iteration evidence belongs in HUMAN-UAT.md (server-side router logs) rather than in the test (response body doesn't expose intermediate turns). Iteration evidence captured in HUMAN-UAT.md: router.py log markers `iter=1 → iter=2` for the live D-08 #1 PASS run. |
| P3.4  | Pre-Plan-02 the strengthened E2E test FAILS on assist-01 with FB_INTEGRATION_TESTS=1 — proves assertions surface Bug D | VERIFIED | Plan 03 was shipped before Plan 02 reached assist-01 via the cherry-pick path; the FAIL path was operator-observed and recorded in `16.2-HUMAN-UAT.md` (Pre-flight automated gates table — pre-correction run on assist-01 had Bug D semantics evident). |
| P3.5  | After Plan 02 GREEN, the strengthened E2E test PASSES on assist-01 — proves end-to-end closure | VERIFIED | Operator-recorded in HUMAN-UAT.md: `1 passed, 1 warning in 21.38s` post-correction. |
| P3.6  | No "natural prose detection" heuristic introduced (D-07 reject) | VERIFIED | grep on `tests/integration/test_chat_endpoint_live.py` — no `len(content.split())`, no word-counting heuristics, no prose-shape probes. Only the surgical regex + (now-dropped) message-role check. D-07 rejection rationale documented inline at line 95 (regex comment) and in 16.2-03-SUMMARY.md "Why D-07 Was Rejected" section. |
| P3.7  | On dev machines without Ollama or without FB_INTEGRATION_TESTS=1, the test still skips cleanly | VERIFIED | Verifier re-ran live: `1 skipped, 0 failed, 1 warning in 0.02s`. Skip gates `requires_integration` + `requires_ollama` unchanged. |

**Plan 04 (UAT closure) — 6 / 6 verified**

| #     | Truth | Status | Evidence |
| ----- | ----- | ------ | -------- |
| P4.1  | D-08 #1: Strategy B E2E PASSES on assist-01 with FB_INTEGRATION_TESTS=1 + live qwen2.5-coder:32b | VERIFIED | Operator-recorded: `1 passed, 1 warning in 21.38s` (`/tmp/16.2-d08-1-rerun.log` on assist-01). Pre-flight gates table line 1 in HUMAN-UAT.md. |
| P4.2  | D-08 #2: Adapter unit tests PASS on dev AND on assist-01 | VERIFIED | Operator-recorded: 16/16 on both machines. Verifier re-ran on dev live: 16/16 in 0.02s. |
| P4.3  | D-08 #3: CN/dev manual walkthrough produces useful natural-language answer (NOT raw JSON, NOT refusal, NOT loop_budget_exceeded) | VERIFIED | HUMAN-UAT.md "Test (D-08 #3 walkthrough)" + "Observations" sections. Verbatim response captured: "It seems there are no synthesis tools registered this week..." — synthesized natural language, 21-22s, no Bug D shape, no error banner. Surface deviation (curl POST vs. browser session) documented as transparent equivalence (same `/api/v1/chat` endpoint that `/ui/chat` posts to). |
| P4.4  | 16.2-HUMAN-UAT.md records technical-floor evidence + walkthrough capture in Phase 10.1 / 16.1 D-14 format | VERIFIED | Format precedent followed: front-matter, header (Date / Operator / Host / Commit / Branch), Fixture State, Pre-flight automated gates table, Live correction subsection, Test, Observations, Cross-Phase 16 must-have regression check, Bug D closure assessment, Deviations, Outcome, Action, D-10 ratification annotation hook, Operator sign-off. |
| P4.5  | Outcome: PASS with deviations IF AND ONLY IF all three D-08 conditions hold; otherwise FAIL and Phase 16.3 required | VERIFIED | "Outcome: PASS with deviations" line at line 142. All three D-08 conditions PASS in pre-flight table. Reasoning section confirms floor holds. Action section: "Phase 16.2 closes-with-deviation. CHAT-04 satisfied. v1.4 milestone close unblocked." |
| P4.6  | D-10 ratification path documented as post-close annotation hook (NOT a gating criterion) | VERIFIED | "D-10 ratification annotation hook" section: "ratification does not re-open Phase 16.2" + "stays open as post-close annotation hook" + "would convert the close from 'PASS with deviations' to 'PASS with deviations, ratified by fresh artist on <date>'". |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `tests/llm/test_ollama_adapter.py` | TestOllamaToolAdapterBugDFallback class with 2 tests + `_OLLAMA_BUG_D_RESPONSE_CONTENT` constant | VERIFIED | Class at line 284, constant at line 40, both tests collected and PASSED post-fix. |
| `.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-CAPTURED-OLLAMA-RESPONSE.json` | Operator-readable capture artifact with full metadata | VERIFIED | Valid JSON, `bug_d_shape_confirmed: True`, full capture metadata (date, host, model, ollama version, prompt). |
| `forge_bridge/llm/_adapters.py` | `_try_parse_text_tool_call` helper + salvage hook inside `OllamaToolAdapter.send_turn()` | VERIFIED | Helper at line 122 (49 lines additive); salvage hook at lines 484-488. Existence + substantive (defensive logic + type guards) + wired (called from send_turn). |
| `tests/integration/test_chat_endpoint_live.py` | Two D-06 assertions in `test_chat_canonical_uat_prompt_under_60s` | PARTIAL (override) | D-06 #1 present at line 245; D-06 #2 dropped per accepted override. Module-top `import re` (line 43) + `_BUG_D_TERMINAL_JSON_RE` regex (line 95) intact. |
| `.planning/phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` | Operator-recorded UAT outcome closing CHAT-04 | VERIFIED | All required sections present, Outcome: PASS with deviations, all three D-08 conditions PASS, CHAT-04 referenced 10×, D-10 ratification annotation hook present, sign-off line present. |

### Key Link Verification

The gsd-tools key-link verifier returned ambiguous results because the `from` fields encode symbol/class names rather than file paths. Manual grep verification was performed:

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `tests/llm/test_ollama_adapter.py::TestOllamaToolAdapterBugDFallback` | `OllamaToolAdapter.send_turn` | `MagicMock client.chat returning _fake_response_dict with content=JSON-shaped string and tool_calls=None` | WIRED | Test class at line 284 instantiates adapter, mocks `client.chat` via `AsyncMock(return_value=_fake_response_dict(...))`, calls `await adapter.send_turn(state)`. 9 occurrences of `send_turn` in test file. |
| `_OLLAMA_BUG_D_RESPONSE_CONTENT` (test constant) | `16.2-CAPTURED-OLLAMA-RESPONSE.json::raw_response.message.content` | verbatim string equality | WIRED | Verifier re-ran the equality check live: `match: YES, value={"name": "forge_tools_read", "arguments": {"name": "synthesis-tools"}}`. Mitigates T-16.2.01-01 (drift). |
| `OllamaToolAdapter.send_turn` | `_try_parse_text_tool_call` | salvage hook with `text = ""` reset to prevent double-emit | WIRED | Hook at line 485: `salvaged = _try_parse_text_tool_call(text)`; reset at line 488: `text = ""`. Confirmed by direct file inspection. |
| `_try_parse_text_tool_call` | `_ToolCall composite-ref convention` | `ref=f"0:{name}"` mirrors existing `f"{idx}:{name}"` at line ~472 | WIRED | Line 167: `ref=f"0:{name}"`. Mirrors structured path's `ref=f"{idx}:{name}"` at line 472 verbatim. |
| `test_chat_canonical_uat_prompt_under_60s` | `_BUG_D_TERMINAL_JSON_RE` | module-top compiled regex applied to assistant terminal content | WIRED | Regex declaration at line 95; assertion site at line 245. Pattern: `assert not _BUG_D_TERMINAL_JSON_RE.match(content)`. |
| `test_chat_canonical_uat_prompt_under_60s` | `messages[].role agentic-loop shape assertion` | `len([m for m in messages if m.get('role') == 'tool']) >= 1 and ...` | NOT WIRED (override) | Dropped per accepted override. Replacement: in-source comment at lines 252-262 explaining iteration evidence belongs in HUMAN-UAT.md (router log markers). |
| `16.2-HUMAN-UAT.md` | `.planning/REQUIREMENTS.md::CHAT-04` | D-08 PASS-with-deviations satisfies CHAT-04 per D-14 precedent | WIRED | CHAT-04 referenced 10 times in HUMAN-UAT.md, including in Cross-Phase 16 must-have regression check ("CHAT-04 (artist-meaningful response): SATISFIED") and Bug D closure assessment. |
| `16.2-HUMAN-UAT.md` | `16.2-01..03-SUMMARY.md` | Pre-flight gates table cites the SUMMARY commits | WIRED | Pre-flight gates table cites Plan 01 RED→GREEN flip evidence (`16.2-01-SUMMARY.md`); commit chain at top of HUMAN-UAT cites `b61c3e2` (Plan 01), `4aa38b2` (Plan 02), `ed71a2c` (Plan 03), `f657b01` (Plan 04 fix). |

### Data-Flow Trace (Level 4)

The phase produces no UI components or dashboards rendering dynamic data. The artifacts are: a Python helper (defensive parser), a salvage hook (data-flow integrator), test code, a captured fixture, and an operator-authored UAT document. Data flow is implicit in the code path:

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `_try_parse_text_tool_call(text)` | `text` (LLM response content) | Live Ollama HTTP response (verified via captured fixture + live D-08 #1) | YES — verifier behavioral spot-check returned `_ToolCall(ref="0:forge_tools_read", tool_name="forge_tools_read", arguments={"name": "synthesis-tools"})` from real captured input | FLOWING |
| Salvage hook `tool_calls.append(salvaged)` | `tool_calls` list | Helper output, fed to `_TurnResponse` | YES — operator-recorded D-08 #1 PASS confirms downstream `router.py:435` iterated (router log markers iter=1 → iter=2) | FLOWING |
| `_BUG_D_TERMINAL_JSON_RE.match(content)` | `content` (assistant terminal text) | Real chat API response on live D-08 #1 run | YES — assertion fires correctly on Bug D shape, accepts healthy natural-language responses (verified by 7-case regex invariant + live D-08 #1 PASS) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Adapter test suite (D-08 #2b dev half) | `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/llm/test_ollama_adapter.py -p pytest_asyncio.plugin -v` | 16 passed, 1 warning in 0.02s | PASS |
| Default integration test skip behavior | `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/integration/test_chat_endpoint_live.py -p pytest_asyncio.plugin -q` | 1 skipped, 0 failed | PASS |
| Captured fixture validity | `python3 -m json.tool 16.2-CAPTURED-OLLAMA-RESPONSE.json` | Valid JSON; `bug_d_shape_confirmed: True`; `tool_calls: None`; content parses with `name`+`arguments` | PASS |
| Salvage helper invariants (5 cases) | Python one-liner: 4 malformed inputs return None, 1 valid input returns `_ToolCall(ref="0:forge_tools_read", ...)` | All 5 invariants hold | PASS |
| AnthropicToolAdapter untouched | `awk '/^class AnthropicToolAdapter/,/^class OllamaToolAdapter/' _adapters.py \| grep -c '_try_parse_text_tool_call'` | 0 | PASS |
| router.py byte-identical to pre-Phase-16.2 | `git log --oneline -5 -- forge_bridge/llm/router.py` | Last touch is `9a9a56b` (Phase 16-01, predates 16.2) | PASS |
| handlers.py byte-identical to pre-Phase-16.2 | `git log --oneline -5 -- forge_bridge/console/handlers.py` | Last touch is `ae81af3` (Phase 16.1-01, predates 16.2) | PASS |
| D-08 #1 (live, requires assist-01 + Ollama) | `FB_INTEGRATION_TESTS=1 pytest test_chat_canonical_uat_prompt_under_60s` | Operator-recorded `1 passed, 1 warning in 21.38s` on assist-01 | PASS (operator-recorded; cannot re-run from dev) |
| D-08 #2a (live, requires assist-01) | `pytest tests/llm/test_ollama_adapter.py` on assist-01 | Operator-recorded 16/16 PASS | PASS (operator-recorded; cannot re-run from dev) |
| D-08 #3 (live walkthrough) | curl POST `/api/v1/chat` with canonical CHAT-04 prompt | Operator-recorded synthesized natural-language answer in 21-22s | PASS (operator-recorded; cannot re-run from dev) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| CHAT-04 | Plans 01, 02, 03, 04 (all four declare it; Plan 04 closes it) | Non-developer dogfood UAT — artist asks "what synthesis tools were created this week?" in Web UI chat panel and receives a useful, plain-English answer within <60s on assist-01 hardware | SATISFIED | All three D-08 floor conditions PASS per HUMAN-UAT.md. Bug D closed at adapter layer (`_try_parse_text_tool_call` salvages text-shaped tool calls); strengthened E2E test passes live (21.38s); operator walkthrough produced synthesized natural-language answer ("It seems there are no synthesis tools registered this week..."). Per HUMAN-UAT Cross-Phase 16 must-have regression check: "CHAT-04 (artist-meaningful response): SATISFIED". REQUIREMENTS.md still shows "Open" — that is the natural pre-close-phase state; SUMMARY 04 confirms close-phase will mark it Complete. |
| FB-D | Plan 02 (closure delivered by GREEN fix); Plans 01, 03, 04 are regression guards + UAT | Bug D — chat surface returning raw tool-call JSON as terminal text instead of executing tool and synthesizing answer | SATISFIED | Plan 02's `_try_parse_text_tool_call` helper salvages text-shaped tool calls in `OllamaToolAdapter`. Three independent layers of evidence: (1) captured-fixture regression guard (`TestOllamaToolAdapterBugDFallback` flips RED→GREEN), (2) strengthened E2E (D-06 #1 regex-reject), (3) live walkthrough (synthesized answer, no Bug D shape). |

**Orphan check:** No additional requirement IDs map to Phase 16.2 in REQUIREMENTS.md beyond CHAT-04 (the per-phase requirement) and FB-D (the parent phase identifier). Both are accounted for. No orphans.

### Anti-Patterns Found

Scanned files: `forge_bridge/llm/_adapters.py`, `tests/llm/test_ollama_adapter.py`, `tests/integration/test_chat_endpoint_live.py`, `.planning/phases/16.2-bug-d-chat-tool-call-loop/*`.

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `tests/integration/test_chat_endpoint_live.py` | 252-262 | Long in-source comment block where Plan 03's D-06 #2 assertion was dropped | INFO | Documents a known live-corrected planning defect with full reasoning + pointer to HUMAN-UAT.md for iteration evidence. Intentional and load-bearing for future readers diagnosing why iteration is checked outside the test. Not a stub. |
| `forge_bridge/llm/_adapters.py` | 142-170 | Defensive `return None` on every parse-failure branch | INFO | This is the explicit T-16.2.02-01 mitigation per the Plan 02 threat model (raise → HTTP 500 worse failure mode). Not a stub — each return-None branch corresponds to a documented type/shape guard. |
| `tests/llm/test_ollama_adapter.py` | 40 | Hardcoded captured fixture string `_OLLAMA_BUG_D_RESPONSE_CONTENT` | INFO | This IS the captured fixture — verbatim mirror of `16.2-CAPTURED-OLLAMA-RESPONSE.json::raw_response.message.content`. Equality asserted at verify time per T-16.2.01-01 mitigation. Not a stub or hardcoded test data; it's reproduce-from-disk regression evidence. |

No blockers. No warnings. The "anti-patterns" detected by static scan are all intentional, documented, and load-bearing for the diagnosis-fix-regression-guard chain.

### Human Verification Required

**None remaining.** All three D-08 technical-floor conditions were operator-verified live on assist-01 and recorded in `16.2-HUMAN-UAT.md` with verbatim test output, captured response text, and CN/dev sign-off. The verifier re-ran the dev-side adapter test suite (D-08 #2b half) live to confirm portability of the captured fixture; D-08 #1 and D-08 #2a require live assist-01 + Ollama and cannot be re-run from dev — operator records stand as authoritative evidence.

The D-10 ratification annotation hook (post-close fresh-artist re-run) is documented as **not gating** for Phase 16.2 close per CONTEXT D-10. If a fresh artist becomes available before v1.4 milestone close, their observation appends to HUMAN-UAT.md as ratification — it does not re-open this phase.

### Gaps Summary

**No gaps.**

**Notable accepted deviations** (all documented with full evidence trail):

1. **D-06 #2 assertion dropped (override applied)** — Plan 03's loop-iteration assertion checked `body["messages"]` (which carries only `[user_input, final_assistant_answer]`) when iteration evidence is router-internal and NOT forwarded into the chat API response body. The assertion as written could not succeed against any healthy response. Discovered, fixed, and re-verified within Plan 04's UAT loop (commit `5988de8` on dev / `f657b01` on assist-01). D-06 #1 (the load-bearing Bug D regex guard) is intact. Iteration evidence captured in HUMAN-UAT.md (router log markers `iter=1 → iter=2`). Per CONTEXT D-09 "the floor must hold" — what the floor IS now holds; what it was supposed to be was structurally impossible.

2. **D-08 #3 walkthrough surface deviation** (already in scope) — operator used `curl POST /api/v1/chat` instead of browser `/ui/chat` session. Documented as transparent surface equivalence (chat panel UI is a thin client over the same endpoint). Per CONTEXT D-09: "the deviation acceptable here is 'no fresh artist; CN/dev re-test against the strengthened automated floor' — and the floor must hold." The floor holds.

3. **Operator deviation** (already in scope) — CN/dev re-tested against the strengthened technical floor (D-08) instead of a fresh-artist UAT. This is the deviation D-14 explicitly authorizes when (a) the technical floor holds AND (b) the deviation is not itself the must-have failure. Phase 16.1 closed-with-known-gap because Bug D itself was the must-have failure (the deviation didn't compensate). Phase 16.2 PASSES with this deviation because the floor holds AND Bug D itself is closed.

4. **Branch divergence carried as post-close debt** (out of phase scope) — dev `main` / `origin/gsd/v1.4-staged-ops-platform` / assist-01 local v1.4 are three-way divergent. Tracked in `project_branch_divergence_post_16_2.md` memory. Not gating for Phase 16.2 close because all three D-08 floor conditions hold against the cherry-picked code that runs on assist-01.

5. **Model-quality follow-up surfaced** (out of phase scope) — qwen2.5-coder occasionally appends `<|im_start|>` chat-template tokens + a speculative second tool-call JSON at the tail of synthesized prose. This is a model-quality artifact, NOT Bug D — the terminal content is unambiguously natural language and the regex-reject (D-06 #1) classifies it correctly. Worth a v1.5 polish plan; not blocking v1.4 close.

---

## Conclusion

Phase 16.2 achieved its goal: **Bug D is closed end-to-end with three independent layers of evidence** (captured-fixture adapter test, strengthened Strategy B E2E, live operator walkthrough), and the fresh-operator UAT records `Outcome: PASS with deviations` per the Phase 10.1 / 16.1 D-14 precedent. All three CONTEXT D-08 technical-floor conditions hold:

- **D-08 #1** (Strategy B E2E live on assist-01): PASS in 21.38s
- **D-08 #2** (adapter unit tests on dev AND assist-01): PASS 16/16 on both machines
- **D-08 #3** (CN/dev manual walkthrough): PASS — synthesized natural-language answer

CHAT-04 is satisfied. v1.4 milestone close is unblocked. Phase 16.3 is NOT required. The D-10 ratification annotation hook stays open as a post-close strengthening mechanism, not a gating criterion.

The single accepted override (D-06 #2 assertion dropped) is well-documented as a live-corrected planning defect within the same UAT loop — consistent with the Phase 10.1 / 16.1 working pattern for live-correctable assertion defects. The load-bearing Bug D guard (D-06 #1 regex) is intact and was operator-verified to fire correctly against a Bug D shape and accept healthy natural-language responses.

---

_Verified: 2026-04-27T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
