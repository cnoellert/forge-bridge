---
phase: 05-import-rewiring
plan: 04
type: execute
wave: 4
depends_on: [00, 01, 02, 03]
files_modified:
  - /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py
autonomous: true
requirements: [RWR-04]
tags: [verification, conftest, pytest, cross-repo]

must_haves:
  truths:
    - "projekt-forge tests/conftest.py contains an autouse session-scoped fixture that asserts forge_bridge.__file__ resolves to a site-packages path"
    - "The same fixture asserts no top-level forge_bridge/ directory exists at the projekt-forge repo root (defensive namespace-collision guard)"
    - "The fixture runs exactly once per pytest session and fails fast if either assertion breaks"
    - "Full projekt-forge test suite passes including the new assertion"
    - "Running pytest from the projekt-forge repo root exits 0 and includes the fixture invocation evidence (-v output shows the fixture)"
    - "Single atomic commit in projekt-forge repo (Wave D)"
  artifacts:
    - path: "/Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py"
      provides: "autouse session fixture asserting forge_bridge resolves to site-packages + defensive collision guard"
      contains: "assert_forge_bridge_from_site_packages"
  key_links:
    - from: "tests/conftest.py"
      to: "forge_bridge pip install"
      via: "pathlib.Path(forge_bridge.__file__).resolve()"
      pattern: "site-packages"
    - from: "tests/conftest.py"
      to: "projekt-forge repo root"
      via: "pathlib.Path(__file__).parent.parent / 'forge_bridge'"
      pattern: "local_pkg\\.exists"
---

<objective>
Append a pytest conftest fixture that asserts `forge_bridge` resolves to the pip-installed package in site-packages (not a local directory), satisfying RWR-04 "verified in CI". This is Wave D of the D-11 four-wave plan — the phase-wide safety net that catches any future reintroduction of the namespace collision.

Purpose: Wave A renamed the local package and Wave B deleted the duplicates, but without an automated guard a future developer could recreate `forge_bridge/` at the projekt-forge repo root and re-shadow the pip install silently. The conftest fixture makes the collision loud: any pytest run fails immediately with a message pointing at the exact cause. Per D-16, "verified in CI" means "runs automatically on every pytest invocation" — no new CI infrastructure is needed; the assertion is the contract.

Output: `tests/conftest.py` appended with the RWR-04 guard fixture; full pytest suite green; one atomic commit in the projekt-forge repo closing Phase 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/05-import-rewiring/05-CONTEXT.md
@.planning/phases/05-import-rewiring/05-RESEARCH.md
@.planning/phases/05-import-rewiring/05-PATTERNS.md
@.planning/phases/05-import-rewiring/05-VALIDATION.md

@.planning/phases/05-import-rewiring/05-00-SUMMARY.md
@.planning/phases/05-import-rewiring/05-01-SUMMARY.md
@.planning/phases/05-import-rewiring/05-02-SUMMARY.md
@.planning/phases/05-import-rewiring/05-03-SUMMARY.md

<!-- Cross-repo target -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py -->

<interfaces>
Append pattern (verbatim from PATTERNS.md §"Wave D — tests/conftest.py" + CONTEXT D-15 + VALIDATION §"RWR-04 Conftest Assertion"):

```python
# ---------------------------------------------------------------------------
# RWR-04 — forge_bridge namespace collision guard (Phase 5)
# ---------------------------------------------------------------------------

import pathlib
import forge_bridge
import pytest


@pytest.fixture(autouse=True, scope="session")
def assert_forge_bridge_from_site_packages():
    """RWR-04: Verify forge_bridge resolves to pip install, not local directory.

    Runs once per test session. Fails immediately if the namespace collision
    recurs (e.g., someone recreated a local forge_bridge/ directory).
    """
    p = pathlib.Path(forge_bridge.__file__).resolve()
    assert "site-packages" in p.parts, (
        f"forge_bridge resolved to {p} — expected site-packages. "
        "Check: no local forge_bridge/ directory should exist at the "
        "projekt-forge repo root. Re-run: pip install -e /path/to/forge-bridge"
    )

    # Defensive: confirm no local forge_bridge/ at repo root
    repo_root = pathlib.Path(__file__).parent.parent
    local_pkg = repo_root / "forge_bridge"
    assert not local_pkg.exists(), (
        f"Local forge_bridge/ directory found at {local_pkg}. "
        "This causes a namespace collision — remove it."
    )
```

Placement: append to the existing /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py (current file ends at line ~225 per PATTERNS.md). Add after the final existing fixture, not before, to avoid disrupting import ordering.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Append RWR-04 autouse fixture to tests/conftest.py, run full suite, commit</name>
  <files>/Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py</files>
  <read_first>
    - /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py (entire current file — confirm the last-existing-fixture line number; the new fixture appends after it)
    - .planning/phases/05-import-rewiring/05-PATTERNS.md §"Wave D — tests/conftest.py (test, transform)" (verbatim append pattern)
    - .planning/phases/05-import-rewiring/05-VALIDATION.md §"RWR-04 Conftest Assertion (D-15)" (exact fixture body)
    - .planning/phases/05-import-rewiring/05-CONTEXT.md §D-15 (conftest assertion requirement) and §D-16 ("verified in CI" = any pytest run)
  </read_first>
  <action>
    STEP 1 — Confirm preconditions from prior waves:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        test ! -d forge_bridge          # Wave A: no local collision directory
        test -d projekt_forge           # Wave A: rename complete
        pip show forge-bridge | grep -q 'Version: 1.0.1'    # Wave B: pip dep installed
        python -c "import forge_bridge, pathlib; p = pathlib.Path(forge_bridge.__file__).resolve(); assert 'site-packages' in p.parts, f'FAIL: {p}'"

    If any precondition fails, STOP. Phase 5 is not in a state to land the final assertion.

    STEP 2 — Read the current conftest.py to locate the append point (end of file):
        wc -l /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py
        tail -30 /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py

    Identify the last existing fixture definition and confirm the file ends with at least one blank line. If not, you will add one before the appended block.

    STEP 3 — Append the RWR-04 fixture block to /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py.

    Use the Edit tool (or the executor's equivalent write-append mechanism) to append this block verbatim at the end of the file:

        # ---------------------------------------------------------------------------
        # RWR-04 — forge_bridge namespace collision guard (Phase 5)
        # ---------------------------------------------------------------------------

        import pathlib
        import forge_bridge
        import pytest


        @pytest.fixture(autouse=True, scope="session")
        def assert_forge_bridge_from_site_packages():
            """RWR-04: Verify forge_bridge resolves to pip install, not local directory.

            Runs once per test session. Fails immediately if the namespace collision
            recurs (e.g., someone recreated a local forge_bridge/ directory).
            """
            p = pathlib.Path(forge_bridge.__file__).resolve()
            assert "site-packages" in p.parts, (
                f"forge_bridge resolved to {p} — expected site-packages. "
                "Check: no local forge_bridge/ directory should exist at the "
                "projekt-forge repo root. Re-run: pip install -e /path/to/forge-bridge"
            )

            # Defensive: confirm no local forge_bridge/ at repo root
            repo_root = pathlib.Path(__file__).parent.parent
            local_pkg = repo_root / "forge_bridge"
            assert not local_pkg.exists(), (
                f"Local forge_bridge/ directory found at {local_pkg}. "
                "This causes a namespace collision — remove it."
            )

    Preserve any existing imports at the top of conftest.py. The new `import pathlib`, `import forge_bridge`, and `import pytest` lines inside the appended block are intentionally placed alongside the fixture for section cohesion — if any of these names are already imported at the top of the file, the duplicate imports are harmless in Python and preserve the block's self-contained look. (If the ruff config in this repo flags duplicate imports, reconcile by moving those three imports to the existing import section at the top and keeping only the fixture itself in the appended block.)

    STEP 4 — Run the full test suite:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        pytest -v -q --no-header 2>&1 | tail -40

    Expected:
    - The `assert_forge_bridge_from_site_packages` fixture appears exactly once in the test session (scope="session")
    - Every test passes (the fixture assertions pass because Waves A-C completed successfully)
    - Exit code 0

    If the site-packages assertion fails:
    - Check `python -c "import forge_bridge, pathlib; print(pathlib.Path(forge_bridge.__file__).resolve())"` — where does it resolve?
    - If it resolves to `/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/` (NOT forge_bridge) → somehow there is still a `forge_bridge/` entry on sys.path; `find /Users/cnoellert/Documents/GitHub/projekt-forge -name "forge_bridge" -type d` will locate it.
    - If it resolves to `/Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/` (dev shadow) → that is actually acceptable for RWR-04 IF the user has the editable install of forge-bridge active per D-06. But the assertion explicitly requires site-packages. Decision: run with only the pip git-dep install for the assertion (uninstall the editable shadow, verify, reinstall):
        pip uninstall -y forge-bridge
        pip install -e /Users/cnoellert/Documents/GitHub/projekt-forge    # re-resolves the git dep
        pytest -v -q --no-header

    If the defensive directory assertion fails:
    - `find /Users/cnoellert/Documents/GitHub/projekt-forge -maxdepth 1 -type d -name forge_bridge` will show the stray directory. Wave A's rename was incomplete. Do NOT paper over — investigate and remove the stray. Phase 5 is not done until both assertions pass.

    STEP 5 — Commit atomically:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        git add tests/conftest.py
        git commit -m "test(projekt_forge): add RWR-04 conftest guard asserting forge_bridge resolves to site-packages -- forge-bridge phase 5 wave D"

    STEP 6 — Final Phase 5 verification sweep across all four waves:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        git log --oneline "origin/main..HEAD" | head -20   # all projekt-forge phase 5 commits
        pytest -q --no-header                                # full suite (not -x; let it run through)

    Expected: 4 wave commits (A, B, C, D); full suite green with the RWR-04 fixture running once per session.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/projekt-forge &amp;&amp; grep -c 'assert_forge_bridge_from_site_packages' tests/conftest.py &amp;&amp; grep -c 'site-packages' tests/conftest.py &amp;&amp; grep -c 'autouse=True' tests/conftest.py &amp;&amp; pytest -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'def assert_forge_bridge_from_site_packages' /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py returns 1
    - grep -c '@pytest.fixture(autouse=True, scope="session")' /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py returns >= 1
    - grep -c '"site-packages" in p.parts' /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py returns 1
    - grep -c 'local_pkg.exists()' /Users/cnoellert/Documents/GitHub/projekt-forge/tests/conftest.py returns 1
    - test ! -d /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge passes (defensive guard precondition)
    - python -c "import forge_bridge, pathlib; p = pathlib.Path(forge_bridge.__file__).resolve(); assert 'site-packages' in p.parts, f'FAIL: {p}'; print('OK')" exits 0
    - cd /Users/cnoellert/Documents/GitHub/projekt-forge && pytest -q --no-header exits 0
    - cd /Users/cnoellert/Documents/GitHub/projekt-forge && pytest -v --no-header 2>&1 | grep -c 'assert_forge_bridge_from_site_packages' returns >= 1 (fixture visible in verbose output)
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge log -1 --pretty=%s matches `test\(projekt_forge\).*forge-bridge phase 5 wave D`
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge status --porcelain returns empty (clean tree)
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge log --oneline "origin/main..HEAD" | grep -c 'forge-bridge phase 5 wave' returns >= 4 (A, B, C, D commits)
  </acceptance_criteria>
  <done>
    tests/conftest.py has the RWR-04 autouse session fixture asserting both site-packages resolution and absence of local forge_bridge/ at repo root; full pytest suite green; Wave D commit landed in projekt-forge; all four wave commits visible in git log. Phase 5 is complete pending /gsd-verify-work.
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. Fixture present:
   - tests/conftest.py has assert_forge_bridge_from_site_packages autouse session fixture
   - Both assertions (site-packages + no local forge_bridge/) are in place

2. Pytest green:
   - Full suite passes with the fixture running once per session
   - Verbose output shows the fixture invoked

3. Defensive check:
   - No forge_bridge/ directory at projekt-forge repo root
   - forge_bridge imports from a site-packages path

4. Wave D commit landed; 4 total wave commits visible in projekt-forge git log
</verification>

<success_criteria>
- RWR-04 satisfied: forge_bridge.__file__ resolves to site-packages, verified automatically on every pytest invocation
- D-15 conftest assertion in place with both the site-packages check and the defensive directory check
- D-16 "verified in CI" contract satisfied (any pytest run enforces the guard)
- Full projekt-forge test suite green
- Single atomic Wave D commit following D-18 convention
- All four Phase 5 wave commits visible in projekt-forge main branch
- Phase 5 ready for /gsd-verify-work review
</success_criteria>

<output>
After completion, create .planning/phases/05-import-rewiring/05-04-SUMMARY.md with:
- Commit hash + subject in projekt-forge
- Number of lines appended to conftest.py
- Full pytest pass/fail count + duration (final green run)
- forge_bridge.__file__ actual resolved path (from STEP 1 precondition check)
- Confirmation that git log shows 4 wave commits (A/B/C/D) since last origin/main
- Any deviations (especially around STEP 4 editable-install shadow handling)
</output>
</content>
