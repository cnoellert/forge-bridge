---
phase: 05-import-rewiring
plan: 03
type: execute
wave: 3
depends_on: [00, 01, 02]
files_modified:
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py
autonomous: true
requirements: [RWR-03]
tags: [mcp, registry, lifespan, cross-repo]

must_haves:
  truths:
    - "projekt_forge/server/mcp.py contains zero direct mcp.tool(...) calls"
    - "projekt_forge/server/mcp.py calls get_mcp() exactly once to obtain the FastMCP singleton from the pip package"
    - "projekt_forge/server/mcp.py calls register_tools(mcp, [...], prefix='forge_', source='builtin') at module top-level (before any mcp.run())"
    - "All forge-specific tools (catalog.trace_lineage, catalog.get_shot_deps, orchestrate.publish_pipeline, scan.media_scan, seed.seed_catalog, batch.setup_denoise, project.list_desktop) appear in the register_tools() argument list"
    - "projekt_forge/__main__.py no longer calls _startup/_shutdown or startup_bridge/shutdown_bridge directly — the canonical lifespan handles these when mcp.run() starts"
    - "projekt_forge/__main__.py imports configure from forge_bridge.bridge (pip) — NOT from projekt_forge.bridge (deleted in Wave B)"
    - "Running `python -m projekt_forge --help` exits 0 (server is importable and main() runs)"
    - "Full projekt-forge test suite passes including any MCP tool-registration smoke tests"
  artifacts:
    - path: "/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py"
      provides: "FastMCP singleton obtained from pip + forge-specific tools registered via register_tools()"
      contains: "from forge_bridge import get_mcp, register_tools"
    - path: "/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py"
      provides: "TaskGroup(mcp + db_server) orchestration wired to canonical lifespan"
      contains: "from forge_bridge.bridge import configure"
  key_links:
    - from: "projekt_forge/server/mcp.py"
      to: "forge_bridge.mcp (pip)"
      via: "get_mcp() + register_tools()"
      pattern: "from forge_bridge import get_mcp, register_tools"
    - from: "projekt_forge/__main__.py"
      to: "canonical lifespan in forge_bridge/mcp/server.py"
      via: "mcp.run() triggers the pip package's _lifespan which calls startup_bridge/shutdown_bridge"
      pattern: "mcp\\.run\\(\\)"
---

<objective>
Rewrite projekt-forge's MCP server around the canonical `get_mcp()` + `register_tools()` surface provided by forge-bridge v1.0.1. This is Wave C of the D-11 four-wave plan — it satisfies RWR-03 by registering projekt-forge's forge-specific tools (catalog, orchestrate, scan, seed, batch, project) through the pip package's registry with `source="builtin"`.

Purpose: Pre-rewire, `projekt_forge/server/mcp.py` manually called `mcp.tool(...)` ~30+ times for every tool (canonical flame_* tools and forge-specific tools). Wave B deleted the canonical tool files (publish, switch_grade, timeline) so those registrations break. Wave C replaces the manual registration loop with a single `register_tools()` call that targets only the forge-specific tools projekt-forge still owns — the canonical flame_* tools come pre-registered by the pip package's `register_builtins()` called inside `_lifespan`. This also wires `__main__.py` to rely on the canonical lifespan instead of hand-calling `_startup`/`_shutdown`.

Output: `projekt_forge/server/mcp.py` collapsed from ~500 lines to ~30 lines; `projekt_forge/__main__.py` updated to use canonical lifespan; server starts cleanly via `python -m projekt_forge`; full test suite green. One atomic commit in the projekt-forge repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/05-import-rewiring/05-CONTEXT.md
@.planning/phases/05-import-rewiring/05-RESEARCH.md
@.planning/phases/05-import-rewiring/05-PATTERNS.md

@.planning/phases/05-import-rewiring/05-00-SUMMARY.md
@.planning/phases/05-import-rewiring/05-01-SUMMARY.md
@.planning/phases/05-import-rewiring/05-02-SUMMARY.md

<!-- Canonical pip source — read to confirm the public API shape post-Phase-4 -->
@forge_bridge/mcp/__init__.py
@forge_bridge/mcp/server.py
@forge_bridge/mcp/registry.py

<!-- Cross-repo targets -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py -->

<interfaces>
Canonical API consumed by Wave C (Phase 4 D-01/D-02/D-04 — confirmed via 05-CONTEXT.md §canonical_refs):

```python
# From forge_bridge/mcp/__init__.py:
from forge_bridge.mcp.server import mcp, startup_bridge, shutdown_bridge
from forge_bridge.mcp.registry import register_tools

def get_mcp():
    """Return the FastMCP singleton (already has flame_* tools registered)."""
    return mcp

__all__ = ["get_mcp", "register_tools", "startup_bridge", "shutdown_bridge"]

# From forge_bridge/mcp/registry.py:
def register_tools(
    mcp,
    fns: list[Callable],
    prefix: str = "",
    source: str = "user-taught",
) -> None:
    """Register a list of tool functions with the MCP server.
    Raises RuntimeError if called after mcp.run() (post-run guard, Phase 4 API-05).
    """
    ...

# From forge_bridge/mcp/server.py (FastMCP singleton):
@asynccontextmanager
async def _lifespan(server):
    await startup_bridge()          # lazy-connects bridge HTTP client
    _server_started = True          # guard flag for register_tools()
    try:
        yield
    finally:
        await shutdown_bridge()     # closes bridge client
```

Post-Wave-C projekt_forge/server/mcp.py target shape (from CONTEXT D-13 + PATTERNS §"Wave C MCP Rewire"):

```python
from __future__ import annotations

from forge_bridge import get_mcp, register_tools

from projekt_forge.tools import catalog, orchestrate, scan, seed
from projekt_forge.tools import batch as forge_batch
from projekt_forge.tools import project as forge_project

mcp = get_mcp()  # pip singleton; flame_* tools already registered by register_builtins()

register_tools(
    mcp,
    [
        catalog.trace_lineage,
        catalog.get_shot_deps,
        orchestrate.publish_pipeline,
        scan.media_scan,
        seed.seed_catalog,
        forge_batch.setup_denoise,
        forge_project.list_desktop,
    ],
    prefix="forge_",
    source="builtin",
)
```

Post-Wave-C projekt_forge/__main__.py changes (from PATTERNS §"Wave C __main__.py"):
- KEEP the TaskGroup orchestration (forge-specific — runs mcp + db_server concurrently)
- KEEP the argparse block (--no-db, --db-only, --bridge-host, etc.)
- KEEP the `dispose_all_engines()` finally block
- FLIP `from projekt_forge.bridge import configure` → `from forge_bridge.bridge import configure` (done in Wave B Task 2 STEP 7 — verify it's still there)
- REMOVE any direct calls to `startup_bridge()` / `shutdown_bridge()` / `_startup()` / `_shutdown()` from __main__.py body — lifespan handles these
- VERIFY `from projekt_forge.server.mcp import mcp` still works after the mcp.py rewrite
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite projekt_forge/server/mcp.py around get_mcp() + register_tools()</name>
  <files>/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py</files>
  <read_first>
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py (entire current file — post-Wave-B state; confirm which forge-specific tools are currently registered via mcp.tool and extract the exact function references to pass to register_tools)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/catalog.py (confirm exported names: trace_lineage, get_shot_deps or similar)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/orchestrate.py (exported tool function name — likely publish_pipeline)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/scan.py (exported tool function name — likely media_scan)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/seed.py (exported tool function name — likely seed_catalog)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/batch.py (exported forge-specific tool — setup_denoise)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/project.py (exported forge-specific tool — list_desktop with enhanced scope/filter)
    - forge_bridge/mcp/__init__.py (confirm get_mcp, register_tools exports)
    - forge_bridge/mcp/registry.py lines 75-115 (register_tools signature + post-run guard)
    - .planning/phases/05-import-rewiring/05-PATTERNS.md §"Wave C MCP Rewire" (verbatim target shape)
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §"Pitfall 4: register_tools() called after mcp.run()" (contract: all register_tools at module top-level, not inside lifespan)
  </read_first>
  <action>
    STEP 1 — Read current mcp.py and build the actual tool-function inventory:
        grep -nE "mcp\.tool\s*\(" /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py
    Extract every callable currently registered. Cross-reference against the PRESERVE list from Plan 02:
    - catalog.* — forge-specific, keep in register_tools
    - orchestrate.* — forge-specific, keep
    - scan.* — forge-specific, keep
    - seed.* — forge-specific, keep
    - batch.setup_denoise — forge-specific (setup_denoise function), keep
    - project.list_desktop — forge-specific (enhanced version), keep
    - publish.* / switch_grade.* / timeline.* / flame_* CANONICAL tools — REMOVE (they come from the pip package's register_builtins())

    If the inventory turns up any tool that does not fit either bucket, document it as a DEVIATION and decide per MOVE/DELETE criteria in D-08.

    STEP 2 — Write the new projekt_forge/server/mcp.py verbatim per PATTERNS.md §"Wave C MCP Rewire" (adjust tool references to match actual function names found in STEP 1):

        """
        projekt-forge MCP server — forge-specific tool registration.

        Obtains the FastMCP singleton from the forge-bridge pip package (which
        already has all flame_* tools registered via register_builtins), then
        registers projekt-forge's forge-specific tools under the forge_ prefix.

        Usage:
            python -m projekt_forge           # via __main__.py
            python -m projekt_forge --no-db   # MCP only

        Config (env vars — same as forge-bridge canonical):
            FORGE_BRIDGE_URL      ws://host:9998
            FORGE_BRIDGE_HOST     127.0.0.1
            FORGE_BRIDGE_PORT     9999
        """
        from __future__ import annotations

        from forge_bridge import get_mcp, register_tools

        from projekt_forge.tools import catalog, orchestrate, scan, seed
        from projekt_forge.tools import batch as forge_batch
        from projekt_forge.tools import project as forge_project

        # All tool registrations must happen at module import time (before mcp.run()).
        # register_tools() raises RuntimeError if the FastMCP server is already running
        # (post-run guard added by forge-bridge Phase 4 API-05 in registry.py).
        mcp = get_mcp()  # FastMCP singleton; flame_* tools already registered

        register_tools(
            mcp,
            [
                catalog.trace_lineage,
                catalog.get_shot_deps,
                orchestrate.publish_pipeline,
                scan.media_scan,
                seed.seed_catalog,
                forge_batch.setup_denoise,
                forge_project.list_desktop,
            ],
            prefix="forge_",
            source="builtin",
        )

    If the actual exported names in the tool modules differ from the list above (e.g., catalog.py exports `lineage_trace` not `trace_lineage`), adjust the list to match the actual functions. The pattern is: `<module>.<tool_function_name>` where `<tool_function_name>` is the top-level async function in the tool module that the old mcp.tool(...) call wrapped.

    If a tool module has MULTIPLE callable tools (e.g., catalog.py has both trace_lineage and get_shot_deps), list each as a separate entry.

    STEP 3 — DO NOT include a `main()` function, argparse, or `mcp.run()` call in mcp.py. Those responsibilities move entirely to __main__.py (Task 2).

    STEP 4 — Verify the rewrite imports cleanly:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        python -c "from projekt_forge.server.mcp import mcp; print(f'OK: {type(mcp).__name__}')"

    Expected output: `OK: FastMCP` (or whatever the singleton's class name is).

    If ImportError:
    - Attribute missing on a tool module → the STEP 1 inventory referenced a name that does not exist; correct the register_tools list.
    - RuntimeError from register_tools → something already imported projekt_forge.server.mcp and also called mcp.run(); scan for lingering `mcp.run()` calls in projekt_forge and fix.

    STEP 5 — DO NOT commit yet. Task 2 updates __main__.py and the commit is atomic per wave.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/projekt-forge &amp;&amp; python -c "from projekt_forge.server.mcp import mcp; print(f'OK: {type(mcp).__name__}')" &amp;&amp; ! grep -qE 'mcp\.tool\s*\(' projekt_forge/server/mcp.py &amp;&amp; grep -q 'from forge_bridge import get_mcp, register_tools' projekt_forge/server/mcp.py &amp;&amp; grep -q 'register_tools(' projekt_forge/server/mcp.py &amp;&amp; grep -q 'source="builtin"' projekt_forge/server/mcp.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE 'mcp\.tool\s*\(' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py returns 0 (all manual tool() calls removed)
    - grep -c 'from forge_bridge import get_mcp, register_tools' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py returns 1
    - grep -c 'get_mcp()' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py returns 1 (single singleton acquisition)
    - grep -c 'register_tools(' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py returns >= 1
    - grep -c 'prefix="forge_"' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py returns 1
    - grep -c 'source="builtin"' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py returns 1
    - grep -c 'def main\|argparse\|mcp.run(' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py returns 0 (responsibilities moved to __main__.py)
    - wc -l /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/mcp.py < 80 (collapsed from ~500 lines; target ~30-60)
    - python -c "from projekt_forge.server.mcp import mcp; assert type(mcp).__name__ == 'FastMCP'" exits 0
    - working tree dirty — do NOT commit until Task 2 finishes
  </acceptance_criteria>
  <done>
    projekt_forge/server/mcp.py contains a single get_mcp() call + one register_tools() call with prefix="forge_" and source="builtin"; no manual mcp.tool() calls; no main()/argparse/run(); imports cleanly; file < 80 lines. Working tree dirty with mcp.py change; no commit yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update __main__.py to use canonical lifespan (remove direct _startup/_shutdown calls)</name>
  <files>/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py</files>
  <read_first>
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py (entire current file — post-Wave-B; confirm the `from forge_bridge.bridge import configure` flip from Plan 02 Task 2 STEP 7 is present)
    - forge_bridge/mcp/server.py lines 44-100 (canonical _lifespan that calls startup_bridge and shutdown_bridge — confirm what mcp.run() triggers automatically)
    - .planning/phases/05-import-rewiring/05-PATTERNS.md §"Wave C __main__.py"
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §"Pitfall 5: startup_bridge() called twice"
    - .planning/phases/05-import-rewiring/05-CONTEXT.md §D-14 (projekt_forge/__main__.py stays but is updated to use startup_bridge()/shutdown_bridge() via the FastMCP lifespan -- no direct _startup/_shutdown calls anywhere)
  </read_first>
  <action>
    STEP 1 — Grep for any direct startup/shutdown calls in projekt_forge:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        grep -rnE "_startup\(|_shutdown\(|startup_bridge\(|shutdown_bridge\(" projekt_forge/

    - 0 matches → Task 2 is light; proceed to STEP 2 for verification.
    - >=1 match in projekt_forge/__main__.py → those are the lines to remove. Reading forge_bridge/mcp/server.py _lifespan confirms that `startup_bridge()` is called automatically when `mcp.run()` enters its lifespan. Calling it from __main__.py AS WELL causes a duplicate bridge session (RESEARCH Pitfall 5). Remove the direct calls.
    - Matches in other projekt_forge files (not __main__.py) → surface as a DEVIATION: the canonical lifespan should cover all startup/shutdown; any non-__main__ caller is likely doing something non-standard. Remove or wrap based on the specific site; err on the side of removal.

    STEP 2 — Read __main__.py and confirm the Wave B flip (Plan 02 Task 2 STEP 7) landed:
        grep -n 'from forge_bridge.bridge import configure' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py
    - Expected: 1 match.
    - 0 matches → Wave B flip missing (deviation); apply it now as part of Wave C:
        Replace any `from projekt_forge.bridge import configure` with `from forge_bridge.bridge import configure`.

    STEP 3 — Confirm mcp singleton acquisition in __main__.py still works post-Task-1:
        grep -n 'from projekt_forge.server.mcp import mcp' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py
    - Expected: 1 match (this import path is preserved — Task 1 kept the `mcp` module attribute so __main__.py's existing import line keeps working).

    STEP 4 — Remove any direct startup/shutdown calls found in STEP 1 from __main__.py body. Example transformations:

        # BEFORE (Phase 4 pre-API-04 shape — may or may not be present):
        from projekt_forge.server.mcp import mcp
        await _startup()
        await mcp.run()
        await _shutdown()

        # AFTER (Wave C — canonical lifespan handles bridge start/stop):
        from projekt_forge.server.mcp import mcp
        await mcp.run()   # lifespan handles startup_bridge/shutdown_bridge

    Do NOT touch the TaskGroup orchestration — the TaskGroup(mcp + db_server) concurrency pattern is forge-specific and stays.

    Do NOT touch `dispose_all_engines()` in the finally block — that is forge-specific DB cleanup, independent of the bridge lifespan.

    Do NOT touch the argparse block (--no-db, --db-only, --bridge-host, --bridge-port, --bridge-timeout).

    STEP 5 — Verify __main__.py is importable and --help works:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        python -m projekt_forge --help

    Expected: Usage output with the argparse flags. Exit code 0.

    If failures:
    - ImportError: No module named 'projekt_forge.bridge' → missing Wave B flip; apply STEP 2 fallback.
    - RuntimeError: register_tools() called after mcp.run() → Task 1 must have put register_tools inside a runtime path; revisit Task 1 and ensure it is at module top-level.
    - RuntimeError from the canonical _lifespan (bridge connection) → STEP 4 may have left a direct startup_bridge() in __main__.py that is now double-calling; remove.

    STEP 6 — Run the full test suite:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        pytest -x -q --no-header

    Expected: green.

    STEP 7 — Atomic Wave C commit:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        git add projekt_forge/server/mcp.py projekt_forge/__main__.py
        git commit -m "refactor(projekt_forge): rebuild MCP server around get_mcp() + register_tools; rely on canonical lifespan -- forge-bridge phase 5 wave C"
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/projekt-forge &amp;&amp; grep -q 'from forge_bridge.bridge import configure' projekt_forge/__main__.py &amp;&amp; ! grep -qE '_startup\(|_shutdown\(' projekt_forge/__main__.py &amp;&amp; python -m projekt_forge --help &amp;&amp; pytest -x -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'from forge_bridge.bridge import configure' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py returns 1
    - grep -cE '_startup\(|_shutdown\(' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py returns 0 (no direct lifecycle calls)
    - grep -cE 'await startup_bridge\(\)|await shutdown_bridge\(\)' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py returns 0 (lifespan handles these)
    - grep -c 'from projekt_forge.server.mcp import mcp' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py returns 1 (import path preserved)
    - grep -c 'TaskGroup' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py returns >= 1 (forge-specific concurrency preserved)
    - grep -c 'dispose_all_engines' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py returns >= 1 (finally block preserved)
    - python -m projekt_forge --help exits 0
    - cd /Users/cnoellert/Documents/GitHub/projekt-forge && pytest -x -q --no-header exits 0
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge log -1 --pretty=%s matches `refactor\(projekt_forge\).*forge-bridge phase 5 wave C`
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge log -1 --name-only shows projekt_forge/server/mcp.py AND projekt_forge/__main__.py in the same commit
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge status --porcelain returns empty
  </acceptance_criteria>
  <done>
    projekt_forge/__main__.py has no direct _startup/_shutdown or startup_bridge/shutdown_bridge calls; configure is imported from forge_bridge.bridge (pip); TaskGroup orchestration preserved; python -m projekt_forge --help works; full test suite green; atomic Wave C commit landed covering both mcp.py and __main__.py.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. MCP rewire:
   - projekt_forge/server/mcp.py has zero mcp.tool() calls
   - register_tools() called once at module top-level with prefix="forge_", source="builtin"
   - File collapsed from ~500 lines to < 80

2. __main__.py:
   - configure imported from forge_bridge.bridge (pip)
   - No direct _startup/_shutdown/startup_bridge/shutdown_bridge calls
   - TaskGroup + dispose_all_engines preserved

3. Runtime:
   - python -m projekt_forge --help exits 0
   - FastMCP singleton acquired via get_mcp() has all forge-specific tools registered under forge_ prefix

4. Tests: full suite green

5. Commit: one atomic Wave C commit in projekt-forge
</verification>

<success_criteria>
- RWR-03 satisfied: forge-specific tools (catalog, orchestrate, scan, seed, batch, project) registered via register_tools() with source="builtin"
- Phase 4 API-04/API-05 contract respected: startup_bridge/shutdown_bridge are not manually called by projekt-forge (canonical lifespan owns the lifecycle)
- Phase 4 post-run guard respected: all register_tools calls are at module top-level (no RuntimeError)
- MCP server starts cleanly via python -m projekt_forge
- Full test suite green
- Single atomic Wave C commit
</success_criteria>

<output>
After completion, create .planning/phases/05-import-rewiring/05-03-SUMMARY.md with:
- Commit hash + subject in projekt-forge
- Line count of old vs new projekt_forge/server/mcp.py
- List of forge-specific tools actually registered (from STEP 1 inventory)
- Whether any direct _startup/_shutdown calls were removed (STEP 1 count + STEP 4 action)
- python -m projekt_forge --help output snippet (first 10 lines)
- Full pytest pass/fail count + duration
- Any deviations
</output>
</content>
