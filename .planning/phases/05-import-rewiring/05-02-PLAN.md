---
phase: 05-import-rewiring
plan: 02
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/bridge.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/publish.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/switch_grade.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/timeline.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/catalog.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/scan.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/orchestrate.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/tests/uat_test_async_client.py
  - /Users/cnoellert/Documents/GitHub/projekt-forge/tests/uat_test_sync_client.py
autonomous: false
requirements: [RWR-01, RWR-02]
tags: [pip, delete, pip-install, cross-repo, atomic]

must_haves:
  truths:
    - "projekt-forge pyproject.toml declares 'forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.0.1' in [project] dependencies"
    - "Per-D-08 DELETE list executed: projekt_forge/bridge.py, projekt_forge/tools/publish.py, projekt_forge/tools/switch_grade.py, projekt_forge/tools/timeline.py, projekt_forge/client/async_client.py, projekt_forge/client/sync_client.py are all removed"
    - "projekt_forge/client/__init__.py still exists (kept — AsyncClient/SyncClient re-exports retargeted to pip for the generic client, but project_name extension stays per D-09b — see action step for retention rule)"
    - "projekt_forge/tools/batch.py and projekt_forge/tools/project.py are PRESERVED (D-10 MOVE disposition — forge-specific extensions)"
    - "projekt_forge/server/protocol/ directory is PRESERVED (D-09a proper-extension disposition — used by catalog/scan tools)"
    - "Canonical imports restored via pip: orchestrate.py imports forge_bridge.bridge; tool files use forge_bridge.server.protocol for query_lineage/query_shot_deps/media_scan (v1.0.1 canonical)"
    - "Full projekt-forge test suite passes with the pip-installed forge-bridge v1.0.1"
    - "Single atomic commit in projekt-forge (per RWR-02 language: pip dep add + duplicate delete in the SAME commit)"
  artifacts:
    - path: "/Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml"
      provides: "forge-bridge git dep + preserved existing layout"
      contains: "forge-bridge @ git+"
    - path: "/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/orchestrate.py"
      provides: "canonical bridge.BRIDGE_URL/BRIDGE_TIMEOUT imports via pip"
      contains: "forge_bridge.bridge"
  key_links:
    - from: "projekt_forge/tools/orchestrate.py"
      to: "forge_bridge.bridge (pip)"
      via: "import forge_bridge.bridge as bridge"
      pattern: "import forge_bridge\\.bridge"
    - from: "projekt_forge/tools/catalog.py"
      to: "forge_bridge.server.protocol.query_lineage (v1.0.1 canonical)"
      via: "lazy import inside async function body"
      pattern: "from forge_bridge\\.server\\.protocol import"
---

<objective>
Consume forge-bridge v1.0.1 as a pip dependency and atomically delete the duplicated modules that the pip package now provides. This is Wave B of the D-11 four-wave execution plan — the literal RWR-02 "in same commit" switch.

Purpose: Wave A renamed the local package but kept every file. Wave B adds the pip dep and deletes the subset of files that are pure duplicates of canonical forge-bridge v1.0.1. The D-09/D-10 audits determined exactly which files to delete (canonical duplicates) vs preserve (forge-specific extensions). After this wave, projekt-forge's runtime uses the pip-installed forge-bridge for all canonical functionality (bridge.execute, AsyncClient correlation, timeline.rename_shots, publish output_directory, switch_grade) while retaining forge-specific extensions (client/__init__.py with project_name AsyncClient wrapper if present, server/protocol/ with query_lineage/query_shot_deps/media_scan wire types, tools/batch.py with setup_denoise, tools/project.py with enhanced list_desktop).

Output: projekt-forge repo with forge-bridge v1.0.1 pip-installed, duplicated modules deleted, canonical imports restored, and the full test suite green. One atomic commit in the projekt-forge repo (Claude's Discretion — the pip install step is manual/network; the commit itself is atomic per RWR-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/05-import-rewiring/05-CONTEXT.md
@.planning/phases/05-import-rewiring/05-RESEARCH.md
@.planning/phases/05-import-rewiring/05-PATTERNS.md
@.planning/phases/05-import-rewiring/05-VALIDATION.md

@.planning/phases/05-import-rewiring/05-00-SUMMARY.md
@.planning/phases/05-import-rewiring/05-01-SUMMARY.md

<!-- Cross-repo targets (this plan executes against the projekt-forge repo) -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml (after Wave A) -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/ -->

<interfaces>
D-08 DELETE list (from CONTEXT + RESEARCH §D-10 + §D-09):
- projekt_forge/bridge.py                   — canonical execute/execute_json/execute_and_read
- projekt_forge/tools/publish.py            — canonical + contains /mnt/portofino (Phase 4 PKG-03 regression)
- projekt_forge/tools/switch_grade.py       — canonical evolved in v1.0; projekt-forge copy is stale
- projekt_forge/tools/timeline.py           — canonical v1.0.1 has gap-fill + strip fix
- projekt_forge/client/async_client.py      — canonical v1.0.1 has ref_msg_id fix (project_name extension was forge-specific; deleted here, re-wrapped by downstream if needed)
- projekt_forge/client/sync_client.py       — canonical v1.0.1 has entity_list narrowing

D-08 PRESERVE list (forge-specific):
- projekt_forge/tools/batch.py              — adds setup_denoise (Neat Video chain)
- projekt_forge/tools/project.py            — enhanced list_desktop with scope/filter
- projekt_forge/tools/catalog.py            — forge-specific lineage queries
- projekt_forge/tools/orchestrate.py        — forge-specific publish pipeline
- projekt_forge/tools/scan.py               — forge-specific scanner MCP wrapper
- projekt_forge/tools/seed.py               — forge-specific DB seeder MCP wrapper
- projekt_forge/server/protocol/            — extended wire types (QUERY_LINEAGE etc.)
- projekt_forge/server/handlers.py, db_server.py, registry.py — forge-specific
- projekt_forge/scanner/, conform/, db/, config/, cli/, seed/ — all forge-specific

Wave B canonical flip targets (files whose imports must point at pip after delete):
- projekt_forge/tools/orchestrate.py        line 17: `from projekt_forge import bridge` → `import forge_bridge.bridge as bridge`
- projekt_forge/tools/catalog.py            lazy imports of query_lineage, query_shot_deps → `from forge_bridge.server.protocol import ...` (v1.0.1 canonical)
- projekt_forge/tools/scan.py               lazy import of project_list builder → canonical `from forge_bridge.server.protocol import project_list`
- projekt_forge/__main__.py                 `from projekt_forge.bridge import configure` → `from forge_bridge.bridge import configure`
- projekt_forge/tests/uat_test_*.py         AsyncClient/SyncClient imports → `from forge_bridge.client import ...` (pip)

Per D-09b, the `project_name` kwarg on AsyncClient is forge-specific. Research recommends deletion of projekt_forge/client/async_client.py (since v1.0.1 has the ref_msg_id fix) AND NO re-creation — downstream uses the canonical AsyncClient directly. If any projekt-forge db_server code passes `project_name=...` to AsyncClient, that code must be updated to wrap canonical AsyncClient with the forge-specific kwarg handling. See action step for the audit path.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add forge-bridge v1.0.1 pip dep, install in projekt-forge venv, verify resolution</name>
  <files>
    /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml
  </files>
  <read_first>
    - /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml (post-Wave-A state — confirm packages = ["projekt_forge"], forge script re-pointed, forge-bridge script removed)
    - .planning/phases/05-import-rewiring/05-PATTERNS.md §"Git dep syntax for pyproject.toml"
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §"Pip Dependency Syntax"
    - .planning/phases/05-import-rewiring/05-00-SUMMARY.md (confirm v1.0.1 tag was pushed in Wave 0 — URL `@v1.0.1` must resolve before this task)
  </read_first>
  <action>
    STEP 1 — Edit /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml. Add to the [project] `dependencies` list (NOT optional-dependencies — this is a core runtime dep):

        "forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.0.1",

    Place this entry alphabetically or at the end of the existing deps array. Preserve all other deps untouched.

    STEP 2 — Install in the projekt-forge virtualenv. This step requires network access to GitHub:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        pip install -e .

    Expected behavior:
    - pip fetches forge-bridge @ v1.0.1 tag from GitHub
    - Installs it into site-packages (not as an editable)
    - The editable install of projekt_forge also completes

    If the install fails:
    - `Could not find a version` or `fatal: reference is not a tree: v1.0.1` → Wave 0 tag push did not complete. Abort and re-run Wave 0 Task 4 checkpoint.
    - GitHub auth failure → confirm the user is authenticated via gh CLI or SSH keys; surface as a checkpoint.

    STEP 3 — Verify forge-bridge resolves to site-packages (this is also enforced by conftest in Wave D, but check early):
        python -c "import forge_bridge, pathlib; p = pathlib.Path(forge_bridge.__file__).resolve(); assert 'site-packages' in p.parts, f'unexpected path: {p}'; print(f'OK: {p}')"

    STEP 4 — (Optional local dev loop — only run if the user asks for it; otherwise skip. D-06):
        pip install -e /Users/cnoellert/Documents/GitHub/forge-bridge   # shadows the git-pinned install

    DO NOT commit yet. Task 2 adds the deletions and the commit is atomic per RWR-02.
  </action>
  <verify>
    <automated>grep -c 'forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.0.1' /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml &amp;&amp; python -c "import forge_bridge, pathlib; p = pathlib.Path(forge_bridge.__file__).resolve(); assert 'site-packages' in p.parts, f'FAIL: {p}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.0.1' /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml returns 1
    - `pip show forge-bridge` (in projekt-forge venv) emits `Version: 1.0.1`
    - `python -c "import forge_bridge, pathlib; print(pathlib.Path(forge_bridge.__file__).resolve())"` prints a site-packages path
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge status --porcelain shows pyproject.toml modified (NOT yet committed — atomic with Task 2)
  </acceptance_criteria>
  <done>
    pyproject.toml has the v1.0.1 git dependency; pip install -e . completed successfully; forge_bridge resolves to site-packages; working tree is dirty with the pyproject.toml change only (no commit yet — that happens after Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete duplicated modules (D-08 DELETE list), flip call sites to canonical pip imports, commit atomically</name>
  <files>
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/bridge.py (delete),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/publish.py (delete),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/switch_grade.py (delete),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/timeline.py (delete),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/client/async_client.py (delete),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/client/sync_client.py (delete),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/orchestrate.py (import flip),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/catalog.py (lazy-import flip),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/scan.py (lazy-import flip),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py (bridge.configure flip),
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/client/__init__.py (re-export flip),
    /Users/cnoellert/Documents/GitHub/projekt-forge/tests/uat_test_async_client.py (import flip),
    /Users/cnoellert/Documents/GitHub/projekt-forge/tests/uat_test_sync_client.py (import flip)
  </files>
  <read_first>
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/orchestrate.py line 17 (current: `from projekt_forge import bridge` after Wave A — confirm)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/catalog.py lines 22-23, 55-56, 77-78 (the lazy import sites for query_lineage and query_shot_deps)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/scan.py lines 23-27 (lazy imports — determine which are canonical vs forge-specific)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py (look for `configure` import — expected around top imports)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/client/__init__.py (current exports — likely re-exports AsyncClient/SyncClient from the now-to-be-deleted files; flip to pip re-exports)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/tests/uat_test_async_client.py and uat_test_sync_client.py (import lines — flip to forge_bridge.client.*)
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §"Pattern 2: Wave B Canonical Flip"
    - .planning/phases/05-import-rewiring/05-PATTERNS.md §Wave B (catalog.py, scan.py, orchestrate.py flip shapes)
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §"D-09b disposition" (project_name kwarg disposition — if projekt-forge uses `AsyncClient(project_name=...)` anywhere outside the deleted files, it must be audited and wrapped here)
  </read_first>
  <action>
    Perform all of Task 2 as a single unit; the commit at the end is atomic per RWR-02.

    STEP 1 — D-09b audit for project_name kwarg usage:
        grep -rn "AsyncClient(.*project_name" /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/ /Users/cnoellert/Documents/GitHub/projekt-forge/tests/ /Users/cnoellert/Documents/GitHub/projekt-forge/forge_gui/
    - 0 matches → proceed to STEP 2 (no wrapping needed; canonical AsyncClient is a drop-in).
    - >=1 match → DEVIATION: document call site in the commit message and decide:
      (a) If db_server.py is the only consumer and it does not actually need project_name after the server-side protocol is verified: remove the kwarg from the call site.
      (b) If it is genuinely required: create a thin projekt_forge/client/async_client.py wrapper that subclasses canonical AsyncClient and accepts/stores project_name, and retain that file (changing its content to the wrapper, not deleting it). Update this plan's DELETE list accordingly in the commit message.

    STEP 2 — DELETE the duplicate files (use git rm so the deletions are tracked):
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        git rm projekt_forge/bridge.py
        git rm projekt_forge/tools/publish.py
        git rm projekt_forge/tools/switch_grade.py
        git rm projekt_forge/tools/timeline.py
        # Per STEP 1 audit result:
        git rm projekt_forge/client/async_client.py    # if audit found 0 matches
        git rm projekt_forge/client/sync_client.py

    Then CONFIRM each deletion took effect:
        test ! -f projekt_forge/bridge.py
        test ! -f projekt_forge/tools/publish.py
        test ! -f projekt_forge/tools/switch_grade.py
        test ! -f projekt_forge/tools/timeline.py
        test ! -f projekt_forge/client/sync_client.py
        # async_client.py — verify per audit result

    STEP 3 — Flip projekt_forge/client/__init__.py to re-export from pip:
        Read current content, then replace the body so it re-exports AsyncClient and SyncClient from canonical pip:

            # projekt_forge/client/__init__.py
            from __future__ import annotations
            from forge_bridge.client import AsyncClient, SyncClient

            __all__ = ["AsyncClient", "SyncClient"]

    (If STEP 1 audit kept a forge-specific async_client.py wrapper, change the AsyncClient line to `from projekt_forge.client.async_client import AsyncClient` instead.)

    STEP 4 — Flip projekt_forge/tools/orchestrate.py imports (approx line 17):

        # BEFORE (after Wave A):
        from projekt_forge import bridge

        # AFTER (Wave B canonical flip):
        import forge_bridge.bridge as bridge

    Verify no other code in orchestrate.py references a local bridge symbol. The `bridge.BRIDGE_URL` and `bridge.BRIDGE_TIMEOUT` attribute reads continue to work because canonical forge_bridge.bridge exposes these names (confirmed in canonical bridge.py).

    STEP 5 — Flip projekt_forge/tools/catalog.py lazy imports (inside the async function bodies that use query_lineage and query_shot_deps):

        # BEFORE (after Wave A):
        from projekt_forge.server.protocol import query_lineage as build_query_lineage  # lazy
        from projekt_forge.server.protocol import query_shot_deps as build_query_shot_deps  # lazy

        # AFTER (Wave B canonical flip — v1.0.1 added these):
        from forge_bridge.server.protocol import query_lineage as build_query_lineage  # lazy (pip v1.0.1)
        from forge_bridge.server.protocol import query_shot_deps as build_query_shot_deps  # lazy (pip v1.0.1)

    Keep `from projekt_forge.client import AsyncClient` (or `from projekt_forge.client.async_client import AsyncClient` if the STEP 1 wrapper path applies). The projekt_forge.client re-export now points to pip AsyncClient transparently (STEP 3).

    STEP 6 — Flip projekt_forge/tools/scan.py lazy imports:

        # AFTER:
        from projekt_forge.scanner import MediaScanner, config_role_to_db_role       # forge-specific, keep
        from projekt_forge.scanner.role_resolver import resolve_directory              # forge-specific, keep
        from projekt_forge.client import AsyncClient                                  # projekt_forge re-export -> pip
        from forge_bridge.server.protocol import project_list                          # canonical (pip)

    STEP 7 — Flip projekt_forge/__main__.py `configure` import:

        # BEFORE (after Wave A):
        from projekt_forge.bridge import configure

        # AFTER (Wave B — local bridge.py deleted):
        from forge_bridge.bridge import configure

    Do NOT change any other imports in __main__.py — Wave C (Plan 03) handles the MCP rewire.

    STEP 8 — Flip test imports in /Users/cnoellert/Documents/GitHub/projekt-forge/tests/uat_test_async_client.py and uat_test_sync_client.py:

        # AFTER:
        from forge_bridge.client import AsyncClient   # or SyncClient, per file

    STEP 9 — Clear bytecode caches (post-delete):
        find /Users/cnoellert/Documents/GitHub/projekt-forge -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null; true

    STEP 10 — Run full test suite:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        pytest -x -q --no-header

    If failures:
    - ModuleNotFoundError: No module named 'projekt_forge.bridge' / .tools.publish / etc. → a call site still imports the deleted local module. Grep: `grep -rn "projekt_forge.bridge\|projekt_forge.tools.publish\|projekt_forge.tools.switch_grade\|projekt_forge.tools.timeline\|projekt_forge.client.async_client\|projekt_forge.client.sync_client" projekt_forge/ tests/ forge_gui/` and flip each to the canonical pip import.
    - AttributeError on the pip canonical API (e.g., `SyncClient.entity_list` missing kwarg) → v1.0.1 patch may be incomplete. Abort and revisit Wave 0.
    - Any behavior regression → investigate; deletion may have removed a forge-specific customization that was overlooked in D-08.

    STEP 11 — Commit atomically (per RWR-02 language: "in same commit as pip dep addition"):
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        git add -A
        git commit -m "refactor(projekt_forge): consume forge-bridge v1.0.1 via pip; delete duplicated modules; flip canonical imports -- forge-bridge phase 5 wave B"

    This is the one-commit-per-wave execution per D-11 + D-18 commit convention.

    STEP 12 — Run phase 4 regression check (PKG-03: no portofino leak inside the projekt-forge tree):
        grep -r "portofino\|assist-01\|ACM_" /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/ || echo "CLEAN"
    Expected output: `CLEAN` (or zero matches). If the PKG-03 regex matches anything inside projekt_forge/, that content was carried in a preserved file (batch.py, project.py, catalog.py, etc.) and must be scrubbed as a follow-up. Surface as DEVIATION.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/projekt-forge &amp;&amp; test ! -f projekt_forge/bridge.py &amp;&amp; test ! -f projekt_forge/tools/publish.py &amp;&amp; test ! -f projekt_forge/tools/switch_grade.py &amp;&amp; test ! -f projekt_forge/tools/timeline.py &amp;&amp; test ! -f projekt_forge/client/sync_client.py &amp;&amp; grep -r "import forge_bridge.bridge" projekt_forge/tools/orchestrate.py &amp;&amp; pytest -x -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - test ! -f /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/bridge.py passes
    - test ! -f /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/publish.py passes
    - test ! -f /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/switch_grade.py passes
    - test ! -f /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/timeline.py passes
    - test ! -f /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/client/sync_client.py passes
    - test -f /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/batch.py passes (PRESERVED)
    - test -f /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/project.py passes (PRESERVED)
    - test -d /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/server/protocol passes (PRESERVED)
    - grep -c 'import forge_bridge.bridge' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/orchestrate.py returns >= 1
    - grep -c 'from forge_bridge.server.protocol import' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/tools/catalog.py returns >= 1 (v1.0.1 canonical lineage builders)
    - grep -c 'from forge_bridge.bridge import configure' /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__main__.py returns 1
    - grep -r "portofino\|assist-01\|ACM_" /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/ returns 0 matches (PKG-03 regression clean)
    - cd /Users/cnoellert/Documents/GitHub/projekt-forge && pytest -x -q --no-header exits 0
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge log -1 --pretty=%s matches `refactor\(projekt_forge\).*forge-bridge phase 5 wave B`
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge log -1 --name-status shows both pyproject.toml modification AND deletion of bridge.py/tools/publish.py/tools/switch_grade.py/tools/timeline.py/client/sync_client.py in the SAME commit (RWR-02 atomicity)
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge status --porcelain returns empty (clean tree post-commit)
  </acceptance_criteria>
  <done>
    projekt-forge v1.0.1 pip dep added + duplicates deleted + canonical imports flipped + tests green, all in a single atomic commit. PKG-03 regression check passes (no portofino leaks). forge_bridge.__file__ resolves to site-packages. Ready for Wave C MCP rewire.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Pip dependency: `pip show forge-bridge` emits `Version: 1.0.1` in the projekt-forge venv; pyproject.toml has the git+...@v1.0.1 URL

2. Deletions (D-08 DELETE list): bridge.py, tools/{publish,switch_grade,timeline}.py, client/{async_client,sync_client}.py no longer exist in projekt_forge/

3. Preservations (D-08 PRESERVE + D-10 MOVE list): tools/batch.py, tools/project.py, tools/catalog.py, tools/orchestrate.py, tools/scan.py, tools/seed.py, server/protocol/, server/handlers.py, server/db_server.py, server/registry.py still exist

4. Canonical flips: orchestrate.py uses `forge_bridge.bridge`; catalog.py lazy-imports from `forge_bridge.server.protocol`; __main__.py imports `configure` from `forge_bridge.bridge`; client/__init__.py re-exports from `forge_bridge.client`

5. PKG-03 regression: grep -r "portofino|assist-01|ACM_" projekt_forge/ returns 0 matches

6. Full pytest suite: green

7. Atomic commit: single projekt-forge commit combining pip-dep add + duplicate-delete + canonical-flip (per RWR-02 "in same commit")
</verification>

<success_criteria>
- RWR-01 satisfied: projekt-forge pyproject.toml has forge-bridge>=1.0,<2.0 equivalent (git+...@v1.0.1 pins exactly; the >=1.0,<2.0 range semantics are satisfied by the pin)
- RWR-02 satisfied: duplicated tool modules deleted in same commit as pip dep addition
- Canonical imports restored via pip for all former local duplicates
- Full test suite green
- Forge-specific extensions (batch.py, project.py, protocol/, etc.) preserved
- Single atomic commit following D-18 convention
- PKG-03 regression clean (no portofino residue leaked through preservation)
</success_criteria>

<output>
After completion, create .planning/phases/05-import-rewiring/05-02-SUMMARY.md with:
- Commit hash + subject in projekt-forge
- Final state of D-08 DELETE list (confirm each file removed)
- Final state of D-08 PRESERVE list (confirm each file retained)
- D-09b audit result (project_name kwarg matches + disposition)
- PKG-03 grep result
- Full pytest pass/fail count + duration
- Any deviations (especially around STEP 1 audit or hidden call sites that needed extra flips)
</output>
</content>
