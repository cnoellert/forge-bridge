---
phase: 05-import-rewiring
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/
  - /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml
  - /Users/cnoellert/Documents/GitHub/projekt-forge/CLAUDE.md
  - /Users/cnoellert/Documents/GitHub/projekt-forge/tests/
  - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_gui/
autonomous: true
requirements: [RWR-01]
tags: [rename, namespace, projekt-forge, cross-repo]

must_haves:
  truths:
    - "projekt-forge/forge_bridge/ directory no longer exists; projekt-forge/projekt_forge/ directory exists with identical content"
    - "All 178 internal 'from forge_bridge.*' imports inside projekt-forge (scoped to projekt_forge/, tests/, forge_gui/) rewritten to 'from projekt_forge.*'"
    - "flame_hooks/ tree is untouched — still contains 'from forge_bridge.*' strings (those resolve against the pip package, not the renamed local package)"
    - "pyproject.toml packages list contains 'projekt_forge' (not 'forge_bridge') and the forge script entry points at projekt_forge.cli.main:cli"
    - "Pre-rewrite forge-bridge console script entry ('forge-bridge = forge_bridge.server:main') is removed from projekt-forge pyproject.toml — pip package will provide it after Wave 2"
    - "CLAUDE.md documents the dev-loop (pip install -e /path/to/forge-bridge after pip install -e .)"
    - "Full projekt-forge pytest suite passes after rename"
    - "Single atomic commit (or at most two — rename + import-sweep) in projekt-forge repo"
  artifacts:
    - path: "/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__init__.py"
      provides: "renamed local package root"
      contains: ""
    - path: "/Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml"
      provides: "updated packages list and scripts block"
      contains: "projekt_forge"
    - path: "/Users/cnoellert/Documents/GitHub/projekt-forge/CLAUDE.md"
      provides: "dev-loop documentation for shadowing pip dep with editable install"
      contains: "pip install -e"
  key_links:
    - from: "/Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/**/*.py"
      to: "projekt_forge.* internal imports"
      via: "relative/absolute Python imports after sed rewrite"
      pattern: "from projekt_forge\\."
    - from: "/Users/cnoellert/Documents/GitHub/projekt-forge/flame_hooks/**/*.py"
      to: "forge_bridge.* imports (untouched — pip package resolution)"
      via: "subprocess script strings executed by conda Python env"
      pattern: "from forge_bridge\\."
---

<objective>
Rename projekt-forge's embedded `forge_bridge/` package to `projekt_forge/` and rewrite all 178 internal imports without changing behavior. This is Wave A of the D-11 four-wave execution plan: a behavior-preserving rename that eliminates the namespace collision between projekt-forge's local copy and the pip package that will be added in Wave B.

Purpose: Without the rename, `import forge_bridge` inside projekt-forge resolves to the embedded directory (higher sys.path priority than site-packages), silently masking any canonical pip imports added in Wave B. Renaming first turns Wave B's potential collisions into clean ModuleNotFoundErrors that immediately point to the fix.

Output: projekt-forge repo where the local source tree lives under `projekt_forge/`, the full test suite passes, and no pip dependency has been added yet. One (or at most two) atomic commit(s) in the projekt-forge repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-import-rewiring/05-CONTEXT.md
@.planning/phases/05-import-rewiring/05-RESEARCH.md
@.planning/phases/05-import-rewiring/05-PATTERNS.md
@.planning/phases/05-import-rewiring/05-VALIDATION.md

<!-- Plan 00 summary will exist after Wave 0 completes -->
@.planning/phases/05-import-rewiring/05-00-SUMMARY.md

<!-- Cross-repo source to modify (this plan executes against the projekt-forge repo) -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/ (to be renamed) -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/tests/ -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/forge_gui/ -->
<!-- /Users/cnoellert/Documents/GitHub/projekt-forge/CLAUDE.md -->

<interfaces>
From /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml (current — confirmed via PATTERNS.md §Wave A):

```toml
[project.scripts]
forge-bridge = "forge_bridge.server:main"     # REMOVED in this wave (pip ships it)
forge = "forge_bridge.cli.main:cli"            # becomes forge = "projekt_forge.cli.main:cli"

[tool.hatch.build.targets.wheel]
packages = ["forge_bridge"]                    # becomes ["projekt_forge"]
```

Existing git-dep pattern to match (lines 29-33):
```toml
[project.optional-dependencies]
cv = [
    "forge-align @ git+https://github.com/cnoellert/forge-align.git",
]
```

178-import distribution (research-verified):
- forge_bridge.server.*   36
- forge_bridge.conform.*  28
- forge_bridge.cli.*      27
- forge_bridge.db.*       26
- forge_bridge.config.*   17
- forge_bridge.client.*   15
- forge_bridge.scanner.*  11
- forge_bridge.tools.*    10
- forge_bridge.seed.*      5
- forge_bridge.bridge      3

Total 178 imports across ~40 Python files inside projekt_forge/, tests/, forge_gui/. flame_hooks/ is EXCLUDED from the rewrite — those imports stay as forge_bridge.* and resolve against the pip package.
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directory, rewrite all internal imports via scoped sed, update pyproject.toml, add dev-loop to CLAUDE.md</name>
  <files>
    /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/ (renamed from forge_bridge/),
    /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml,
    /Users/cnoellert/Documents/GitHub/projekt-forge/CLAUDE.md,
    /Users/cnoellert/Documents/GitHub/projekt-forge/tests/**/*.py,
    /Users/cnoellert/Documents/GitHub/projekt-forge/forge_gui/**/*.py
  </files>
  <read_first>
    - /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml (current — confirm `packages = ["forge_bridge"]` at line 37 and the two [project.scripts] entries around lines 34-35)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/__init__.py (confirm the directory still exists before rename)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/CLAUDE.md (current content — the dev-loop note is APPENDED, not replacing existing sections)
    - .planning/phases/05-import-rewiring/05-PATTERNS.md §"Wave A — Bulk import rename" (macOS-correct sed command with empty string after -i)
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §"flame_hooks Audit" (explicit do-not-touch boundary for flame_hooks)
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §"Common Pitfalls" (Pitfall 2: __pycache__ cleanup; Pitfall 3: sed scope exclusion)
  </read_first>
  <action>
    Execute these steps in order inside /Users/cnoellert/Documents/GitHub/projekt-forge. All commands use absolute paths since this plan lives in the forge-bridge repo.

    STEP 1 — git mv the directory (preserves history):
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        git mv forge_bridge projekt_forge

    STEP 2 — Verify no forge_bridge/ dir remains and projekt_forge/ has all expected content:
        test ! -d /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge || { echo "FAIL: forge_bridge/ still exists"; exit 1; }
        test -d /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge || { echo "FAIL: projekt_forge/ missing"; exit 1; }
        test -f /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/__init__.py

    STEP 3 — Bulk rewrite `from forge_bridge.` to `from projekt_forge.` — SCOPED to projekt_forge/, tests/, forge_gui/. Do NOT apply to flame_hooks/:
        find /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge \
             /Users/cnoellert/Documents/GitHub/projekt-forge/tests \
             /Users/cnoellert/Documents/GitHub/projekt-forge/forge_gui \
             -name "*.py" \
             -exec sed -i '' 's/from forge_bridge\./from projekt_forge\./g' {} +

    STEP 4 — Catch bare `import forge_bridge` lines (rare but possible):
        find /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge \
             /Users/cnoellert/Documents/GitHub/projekt-forge/tests \
             /Users/cnoellert/Documents/GitHub/projekt-forge/forge_gui \
             -name "*.py" \
             -exec sed -i '' 's/^import forge_bridge$/import projekt_forge/g' {} +

    STEP 5 — Verify flame_hooks untouched (imports there MUST still reference forge_bridge — those resolve against the pip package):
        grep -rn "from projekt_forge" /Users/cnoellert/Documents/GitHub/projekt-forge/flame_hooks/ && \
            { echo "FAIL: flame_hooks was touched"; exit 1; } || echo "CLEAN"

    STEP 6 — Clear __pycache__ (stale bytecode caches can mask import errors):
        find /Users/cnoellert/Documents/GitHub/projekt-forge -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null; true

    STEP 7 — Update /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml. The concrete edits:

    (a) Change `packages = ["forge_bridge"]` to `packages = ["projekt_forge"]` in the [tool.hatch.build.targets.wheel] section.

    (b) Remove the line `forge-bridge = "forge_bridge.server:main"` from [project.scripts] entirely (pip package will provide this console_scripts entry after Wave B).

    (c) Change `forge = "forge_bridge.cli.main:cli"` to `forge = "projekt_forge.cli.main:cli"` in [project.scripts].

    (d) If a [tool.hatch.build] include line exists mentioning `forge_bridge/**`, update it to `projekt_forge/**`.

    Do NOT add the forge-bridge git dependency in this wave — that is Wave B's first task. Do NOT touch existing optional-dependencies blocks (cv, dev, etc.).

    STEP 8 — Append dev-loop doc to /Users/cnoellert/Documents/GitHub/projekt-forge/CLAUDE.md (append-only; read current content first to find a natural section boundary). Add this section after existing setup instructions:

        ## Local dev loop with forge-bridge

        projekt-forge consumes forge-bridge as a pip git dependency pinned to a tag (see pyproject.toml).
        For local development against a working copy of forge-bridge:

            # First-time setup
            pip install -e /Users/cnoellert/Documents/GitHub/projekt-forge
            pip install -e /Users/cnoellert/Documents/GitHub/forge-bridge   # shadows the git-pinned install

        The editable install of forge-bridge takes precedence over the git-pinned dependency,
        so changes made in the forge-bridge working copy are picked up immediately without
        needing to re-tag.

        To return to the tagged release (no local shadow):

            pip uninstall forge-bridge
            pip install -e /Users/cnoellert/Documents/GitHub/projekt-forge   # re-resolves git dep

    STEP 9 — Run the full test suite (still using the RENAMED local package — no pip dep yet, so `import forge_bridge` in tests will fail; but tests themselves no longer import forge_bridge.*, they import projekt_forge.*, so the suite should be green):
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        pip install -e .      # reinstall with new package layout
        pytest -x -q --no-header

    If pytest fails:
    - ModuleNotFoundError: No module named 'projekt_forge.X' → check that the sed rewrite covered all call sites (run `grep -rn "from forge_bridge\." projekt_forge/ tests/ forge_gui/` — should return 0 matches)
    - ModuleNotFoundError: No module named 'forge_bridge' → a test or forge_gui file still imports the old name; locate with the grep above and sed-rewrite the stragglers
    - Any other failure → investigate individually (do not mass-fix; tests should be behavior-identical)

    STEP 10 — Commit strategy (per CONTEXT D-11 and D-18, Claude's Discretion: single commit if diff < ~500 lines changed, else split):

    Preferred single commit:
        cd /Users/cnoellert/Documents/GitHub/projekt-forge
        git add -A
        git commit -m "refactor(projekt_forge): rename forge_bridge namespace to projekt_forge + internal import sweep -- forge-bridge phase 5 wave A"

    If the diff exceeds 500 changed lines and review would suffer, split into two commits:
        1. git commit -m "refactor(projekt_forge): rename forge_bridge/ directory to projekt_forge/ + pyproject.toml package list -- forge-bridge phase 5 wave A (1/2)"
        2. git commit -m "refactor(projekt_forge): rewrite internal imports from forge_bridge.* to projekt_forge.* -- forge-bridge phase 5 wave A (2/2)"

    Use exact commit-message convention from D-18. The `{type}(projekt_forge): {subject} -- forge-bridge phase 5 wave A` format is mandatory.
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/projekt-forge &amp;&amp; test -d projekt_forge &amp;&amp; test ! -d forge_bridge &amp;&amp; grep -c 'projekt_forge' pyproject.toml &amp;&amp; ! grep -q 'forge_bridge = ' pyproject.toml &amp;&amp; ! grep -rq 'from forge_bridge\.' projekt_forge/ tests/ forge_gui/ &amp;&amp; grep -rq 'from forge_bridge\.' flame_hooks/ &amp;&amp; pytest -x -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - test -d /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge returns 0 (directory exists)
    - test ! -d /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge returns 0 (old directory gone)
    - find /Users/cnoellert/Documents/GitHub/projekt-forge -maxdepth 1 -type d -name forge_bridge prints nothing
    - grep -rn "from forge_bridge\." /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/ /Users/cnoellert/Documents/GitHub/projekt-forge/tests/ /Users/cnoellert/Documents/GitHub/projekt-forge/forge_gui/ returns 0 matches (no stragglers)
    - grep -rn "from forge_bridge\." /Users/cnoellert/Documents/GitHub/projekt-forge/flame_hooks/ returns >= 1 match (flame_hooks preserved)
    - grep -c 'packages = \["projekt_forge"\]' /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml returns 1
    - grep -c 'forge = "projekt_forge.cli.main:cli"' /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml returns 1
    - grep -c 'forge-bridge = "forge_bridge.server:main"' /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml returns 0 (removed)
    - grep -c 'forge-bridge @ git+' /Users/cnoellert/Documents/GitHub/projekt-forge/pyproject.toml returns 0 (NOT added yet — that is Wave B)
    - grep -c 'pip install -e' /Users/cnoellert/Documents/GitHub/projekt-forge/CLAUDE.md returns >= 1 (dev-loop doc appended)
    - cd /Users/cnoellert/Documents/GitHub/projekt-forge && pytest -x -q --no-header exits 0
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge log -1 --pretty=%s matches `refactor\(projekt_forge\).*forge-bridge phase 5 wave A`
    - git -C /Users/cnoellert/Documents/GitHub/projekt-forge status --porcelain returns empty (clean tree post-commit)
  </acceptance_criteria>
  <done>
    projekt-forge repo: forge_bridge/ directory renamed to projekt_forge/; all 178 internal imports rewritten; flame_hooks/ untouched; pyproject.toml package list and scripts updated (old forge-bridge script removed, forge script re-pointed); CLAUDE.md has dev-loop section; full pytest suite green; one or two atomic commits landed with the exact D-18 commit-message convention; working tree clean.
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. Directory rename:
   - /Users/cnoellert/Documents/GitHub/projekt-forge/projekt_forge/ exists
   - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/ does NOT exist

2. Import sweep:
   - grep -rn "from forge_bridge\." projekt_forge/ tests/ forge_gui/ → 0 matches
   - grep -rn "from forge_bridge\." flame_hooks/ → >= 1 match (preserved intentionally)

3. Configuration:
   - pyproject.toml packages list = ["projekt_forge"]
   - forge-bridge console script entry removed
   - forge script re-pointed to projekt_forge.cli.main:cli

4. Docs: CLAUDE.md contains dev-loop section with `pip install -e` instruction

5. Tests: Full pytest suite green after pip install -e .

6. Commit: One (or two) commits in projekt-forge main branch following D-18 convention
</verification>

<success_criteria>
- Wave A (RWR-01 part 1) complete: projekt-forge's local source tree is renamed and behavior-preserving
- Full test suite green
- No pip dependency added yet (deferred to Wave B)
- flame_hooks/ intentionally untouched (verified by grep)
- Single atomic commit (or two if diff large) landed in projekt-forge repo following D-18 convention
- Ready for Wave B (Plan 02) to add the pip dep and delete duplicates
</success_criteria>

<output>
After completion, create .planning/phases/05-import-rewiring/05-01-SUMMARY.md with:
- Commit hash(es) + subject(s) in projekt-forge repo
- Count of files touched by sed (find . -name *.py -newer [ref] | wc -l)
- pytest pass/fail count + duration
- Whether commit was split into 1 or 2 commits + rationale
- Any import stragglers found outside the expected 178 count
</output>
</content>
