---
phase: 05-import-rewiring
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - forge_bridge/server/protocol.py
  - forge_bridge/client/async_client.py
  - forge_bridge/client/sync_client.py
  - forge_bridge/tools/timeline.py
  - pyproject.toml
autonomous: false
requirements: [RWR-01]
tags: [protocol, websocket, release, pip]

must_haves:
  truths:
    - "forge-bridge canonical protocol exposes query_lineage and query_shot_deps builders"
    - "entity_list builder accepts optional shot_id, role, and source_name narrowing kwargs"
    - "AsyncClient correlates responses via ref_msg_id fallback (not just msg.msg_id)"
    - "sync_client.entity_list() accepts shot_id and role narrowing kwargs"
    - "timeline.rename_shots fills T0 gaps by scanning upward tracks"
    - "forge-bridge v1.0.1 git tag exists on origin/main"
  artifacts:
    - path: "forge_bridge/server/protocol.py"
      provides: "query_lineage, query_shot_deps, extended entity_list builders"
      contains: "def query_lineage"
    - path: "forge_bridge/client/async_client.py"
      provides: "ref_msg_id correlation fallback in _handle_message"
      contains: "ref_msg_id"
    - path: "forge_bridge/client/sync_client.py"
      provides: "entity_list narrowing kwargs"
      contains: "shot_id"
    - path: "forge_bridge/tools/timeline.py"
      provides: "gap_fills set + upward track scan in rename_shots"
      contains: "gap_fills"
    - path: "pyproject.toml"
      provides: "version 1.0.1"
      contains: 'version = "1.0.1"'
  key_links:
    - from: "git tag v1.0.1"
      to: "projekt-forge pyproject.toml @v1.0.1 URL"
      via: "GitHub origin push"
      pattern: "v1\\.0\\.1"
---

<objective>
Ship a forge-bridge v1.0.1 patch release that unblocks Phase 5 Wave B in projekt-forge.

Purpose: RESEARCH §v1.0.1 BLOCKER VERDICT identified four hard blockers that prevent deleting projekt-forge's local `client/`, `server/protocol/`, and `tools/timeline.py` files: (a) canonical protocol lacks `query_lineage`/`query_shot_deps`/`media_scan` builders and types; (b) canonical `entity_list` builder + `sync_client.entity_list()` lack `shot_id`/`role`/`source_name` narrowing kwargs; (c) canonical `async_client._handle_message` drops responses correlated by `ref_msg_id` field; (d) canonical `timeline.rename_shots` silently skips T0 gaps during shot renaming. This plan ports the projekt-forge fixes upstream so Wave B can delete local copies without regression, then tags and pushes v1.0.1 so `pyproject.toml @v1.0.1` resolves.

Output: forge-bridge v1.0.1 git tag pushed to origin with the four patches committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/05-import-rewiring/05-CONTEXT.md
@.planning/phases/05-import-rewiring/05-RESEARCH.md
@.planning/phases/05-import-rewiring/05-PATTERNS.md
@.planning/phases/05-import-rewiring/05-VALIDATION.md

@forge_bridge/server/protocol.py
@forge_bridge/client/async_client.py
@forge_bridge/client/sync_client.py
@forge_bridge/tools/timeline.py
@pyproject.toml

<interfaces>
From forge_bridge/server/protocol.py (current structure — flat file, class-attr MsgType, Message is a dict subclass, msg.msg_id reads self["id"]):

```python
class MsgType:
    HELLO = "hello"
    # ...
    QUERY_DEPENDENTS = "query.dependents"
    QUERY_SHOT_STACK = "query.shot_stack"
    QUERY_EVENTS = "query.events"
    # add: QUERY_LINEAGE, QUERY_SHOT_DEPS, MEDIA_SCAN

class Message(dict):
    @property
    def msg_id(self) -> str | None: return self.get("id")

def _new_id() -> str: ...
def entity_list(entity_type: str, project_id: str) -> Message: ...
# extend with optional shot_id, role, source_name
```

From forge_bridge/client/async_client.py (~line 417):
```python
async def _handle_message(self, msg: Message) -> None:
    msg_type = msg.type
    if msg_type == "ok":
        msg_id = msg.msg_id  # BUG: only reads "id", not "ref_msg_id"
```

Fix pattern (from projekt-forge version):
```python
msg_id = msg.get("ref_msg_id") or msg.msg_id
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add query_lineage, query_shot_deps, media_scan builders + entity_list narrowing to canonical protocol.py</name>
  <files>forge_bridge/server/protocol.py, tests/test_protocol_builders.py</files>
  <read_first>
    - forge_bridge/server/protocol.py (current — see exact _new_id(), Message(dict), and existing builder call signatures; NEW code MUST follow the flat-file pattern, NOT the projekt-forge directory/enum pattern)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/server/protocol/builders.py (read ONLY to see field shapes for QUERY_LINEAGE, QUERY_SHOT_DEPS, MEDIA_SCAN payloads — do NOT copy the enum+dataclass style)
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §D-09a "Symbols in projekt-forge protocol not in canonical" table
    - .planning/phases/05-import-rewiring/05-PATTERNS.md §"v1.0.1 Patch Patterns — forge_bridge/server/protocol.py"
  </read_first>
  <behavior>
    - Test: query_lineage(entity_id="e-123") returns a Message with msg["type"] == "query.lineage" and msg["entity_id"] == "e-123" and a non-empty msg.msg_id
    - Test: query_shot_deps(shot_id="s-456") returns a Message with msg["type"] == "query.shot_deps" and msg["shot_id"] == "s-456"
    - Test: media_scan(project_name="proj", role="plate", shot_name="s001", project_id="p-1") returns a Message with msg["type"] == "media.scan" and all four fields preserved
    - Test: entity_list("shot", "p-1") still works (backward compat — no narrowing kwargs produced)
    - Test: entity_list("version", "p-1", shot_id="s-1", role="plate", source_name="cam_a") returns a Message whose payload contains all three narrowing fields
    - Test: MsgType.QUERY_LINEAGE == "query.lineage", MsgType.QUERY_SHOT_DEPS == "query.shot_deps", MsgType.MEDIA_SCAN == "media.scan"
  </behavior>
  <action>
    Follow TDD: write tests first in tests/test_protocol_builders.py covering the six behaviors above, confirm RED, then implement.

    Edit forge_bridge/server/protocol.py:

    1. Add three class attributes to MsgType (same indent/style as existing attributes):
       QUERY_LINEAGE = "query.lineage"
       QUERY_SHOT_DEPS = "query.shot_deps"
       MEDIA_SCAN = "media.scan"

    2. Add three builder functions near the existing query builders (look for query_dependents, query_shot_stack, query_events around lines 338-361; follow their Message-construction shape exactly):

       def query_lineage(entity_id: str) -> Message:
           return Message({
               "type": MsgType.QUERY_LINEAGE,
               "id": _new_id(),
               "entity_id": entity_id,
           })

       def query_shot_deps(shot_id: str) -> Message:
           return Message({
               "type": MsgType.QUERY_SHOT_DEPS,
               "id": _new_id(),
               "shot_id": shot_id,
           })

       def media_scan(
               project_name: str,
               role: str,
               shot_name: str,
               project_id: str,
       ) -> Message:
           return Message({
               "type": MsgType.MEDIA_SCAN,
               "id": _new_id(),
               "project_name": project_name,
               "role": role,
               "shot_name": shot_name,
               "project_id": project_id,
           })

    3. Extend entity_list(entity_type, project_id) — make narrowing args keyword-only so existing 2-arg positional call sites in flame_hooks/forge_publish/ continue to work:

       def entity_list(
               entity_type: str,
               project_id: str,
               *,
               shot_id: str | None = None,
               role: str | None = None,
               source_name: str | None = None,
       ) -> Message:
           payload = {
               "type": MsgType.ENTITY_LIST,
               "id": _new_id(),
               "entity_type": entity_type,
               "project_id": project_id,
           }
           if shot_id is not None:
               payload["shot_id"] = shot_id
           if role is not None:
               payload["role"] = role
           if source_name is not None:
               payload["source_name"] = source_name
           return Message(payload)

    Run pytest tests/test_protocol_builders.py -x — GREEN. Run full suite pytest — still green.

    Commit in forge-bridge repo: feat(protocol): add query_lineage, query_shot_deps, media_scan builders + entity_list narrowing -- forge-bridge v1.0.1 patch (1/4)
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-bridge &amp;&amp; pytest tests/test_protocol_builders.py -x -q --no-header &amp;&amp; python -c "from forge_bridge.server.protocol import query_lineage, query_shot_deps, media_scan, entity_list, MsgType; assert MsgType.QUERY_LINEAGE == 'query.lineage'; assert MsgType.QUERY_SHOT_DEPS == 'query.shot_deps'; assert MsgType.MEDIA_SCAN == 'media.scan'; m = entity_list('version', 'p-1', shot_id='s-1', role='plate'); assert m['shot_id'] == 's-1' and m['role'] == 'plate'; m2 = entity_list('shot', 'p-1'); assert 'shot_id' not in m2; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE '^def (query_lineage|query_shot_deps|media_scan)\(' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/server/protocol.py returns 3
    - grep -c 'QUERY_LINEAGE = "query.lineage"' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/server/protocol.py returns 1
    - grep -c 'QUERY_SHOT_DEPS = "query.shot_deps"' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/server/protocol.py returns 1
    - grep -c 'MEDIA_SCAN = "media.scan"' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/server/protocol.py returns 1
    - grep -cE 'shot_id: str \| None|role: str \| None|source_name: str \| None' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/server/protocol.py returns >= 3
    - python -c "from forge_bridge.server.protocol import entity_list; m = entity_list('shot', 'p-1'); assert 'shot_id' not in m" exits 0 (backward compat)
    - cd /Users/cnoellert/Documents/GitHub/forge-bridge && pytest tests/test_protocol_builders.py -x exits 0
    - cd /Users/cnoellert/Documents/GitHub/forge-bridge && pytest exits 0
    - git -C /Users/cnoellert/Documents/GitHub/forge-bridge log -1 --pretty=%s matches feat\(protocol\).*v1\.0\.1 patch \(1/4\)
  </acceptance_criteria>
  <done>
    Protocol exports three new builders + three new MsgType constants; entity_list accepts optional shot_id/role/source_name kwargs; backward-compat 2-arg entity_list still works; test_protocol_builders.py covers all six behaviors; full suite green; atomic commit (1/4) landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix ref_msg_id correlation in async_client + entity_list narrowing in sync_client</name>
  <files>forge_bridge/client/async_client.py, forge_bridge/client/sync_client.py, tests/test_async_client_correlation.py</files>
  <read_first>
    - forge_bridge/client/async_client.py lines 417-460 (current _handle_message — see msg_id = msg.msg_id on both the "ok" and "error" branches)
    - forge_bridge/client/sync_client.py (current entity_list method — see whether it wraps protocol.entity_list directly or does something bespoke)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/client/async_client.py lines 417-460 (fix pattern reference: msg.get("ref_msg_id") or msg.msg_id)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/client/sync_client.py (extended entity_list with narrowing kwargs)
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §D-09b "Client Hybrid Audit" disposition table (ref_msg_id fallback + entity_list narrowing are pushed upstream; project_name kwarg is NOT — stays in projekt-forge)
    - .planning/phases/05-import-rewiring/05-PATTERNS.md §"forge_bridge/client/async_client.py -- ref_msg_id correlation fix"
  </read_first>
  <behavior>
    - Test: AsyncClient correlates a response {"type":"ok", "id":"resp-7", "ref_msg_id":"req-42"} to a pending request whose request msg_id == "req-42" (currently fails — canonical reads response.msg_id which is "resp-7")
    - Test: AsyncClient still correlates responses whose correlation field is "id" echo (server echoes request id as the response's id field — backward compat)
    - Test: AsyncClient does the same fallback for "error"-type responses as for "ok"-type
    - Test: sync_client.entity_list(entity_type, project_id) still works with 2 positional args (backward compat)
    - Test: sync_client.entity_list(entity_type, project_id, shot_id="s-1", role="plate") passes both narrowing fields through to the built protocol message
    - NOT changed: AsyncClient.__init__ signature (project_name kwarg stays in projekt-forge's fork — do NOT add it here)
  </behavior>
  <action>
    Follow TDD: write tests first in tests/test_async_client_correlation.py, confirm RED, then implement.

    Test skeleton (using asyncio.Future-based PendingRequest; adapt to the actual PendingRequest class in async_client.py):

        # tests/test_async_client_correlation.py
        import asyncio
        import pytest
        from forge_bridge.server.protocol import Message
        from forge_bridge.client.async_client import AsyncClient, PendingRequest

        @pytest.mark.asyncio
        async def test_ref_msg_id_correlation():
            loop = asyncio.get_event_loop()
            client = AsyncClient("ws://example/ws")
            pending = PendingRequest(loop)
            client._pending["req-42"] = pending
            response = Message({"type": "ok", "id": "resp-7", "ref_msg_id": "req-42", "result": None})
            await client._handle_message(response)
            assert pending._future.done()

        @pytest.mark.asyncio
        async def test_id_echo_correlation():
            loop = asyncio.get_event_loop()
            client = AsyncClient("ws://example/ws")
            pending = PendingRequest(loop)
            client._pending["req-77"] = pending
            response = Message({"type": "ok", "id": "req-77", "result": None})  # no ref_msg_id
            await client._handle_message(response)
            assert pending._future.done()

        @pytest.mark.asyncio
        async def test_error_uses_ref_msg_id():
            loop = asyncio.get_event_loop()
            client = AsyncClient("ws://example/ws")
            pending = PendingRequest(loop)
            client._pending["req-99"] = pending
            response = Message({"type": "error", "id": "resp-99", "ref_msg_id": "req-99", "code": "X", "message": "nope"})
            await client._handle_message(response)
            assert pending._future.done()

        def test_sync_entity_list_narrowing_passthrough():
            from forge_bridge.client.sync_client import SyncClient
            import inspect
            sig = inspect.signature(SyncClient.entity_list)
            assert "shot_id" in sig.parameters
            assert "role" in sig.parameters
            assert "source_name" in sig.parameters

    Edit forge_bridge/client/async_client.py — in _handle_message (around line 417):

    Current "ok" branch:
        if msg_type == "ok":
            msg_id = msg.msg_id
            if msg_id and msg_id in self._pending:
                self._pending[msg_id].resolve(msg)

    Change to:
        if msg_type == "ok":
            # Prefer ref_msg_id (server explicitly echoes request id) over msg.msg_id
            # (which is the response's own id) -- fixes correlation for servers that
            # use a distinct ref_msg_id field in responses.
            msg_id = msg.get("ref_msg_id") or msg.msg_id
            if msg_id and msg_id in self._pending:
                self._pending[msg_id].resolve(msg)

    Apply the identical msg.get("ref_msg_id") or msg.msg_id pattern to the "error" branch in the same method (the current code uses msg_id = msg.msg_id there too — grep msg_id = msg.msg_id to find all occurrences in _handle_message and update each one).

    Do NOT add a project_name kwarg to AsyncClient.__init__. Do NOT touch _connect(), request(), or any other method.

    Edit forge_bridge/client/sync_client.py — update entity_list to accept keyword-only narrowing kwargs and pass them through to protocol.entity_list:

        def entity_list(
                self,
                entity_type: str,
                project_id: str,
                *,
                shot_id: str | None = None,
                role: str | None = None,
                source_name: str | None = None,
        ) -> list[dict]:
            msg = protocol.entity_list(
                entity_type,
                project_id,
                shot_id=shot_id,
                role=role,
                source_name=source_name,
            )
            response = self._send_and_wait(msg)  # use whatever the existing send method is
            return response.get("entities", [])

    (If the existing implementation uses a different send primitive, preserve it — only the signature + the pass-through of the new kwargs into protocol.entity_list changes.)

    Run pytest tests/test_async_client_correlation.py -x — GREEN. Full suite pytest — still green.

    Commit in forge-bridge repo: fix(client): correlate responses by ref_msg_id fallback + entity_list narrowing kwargs -- forge-bridge v1.0.1 patch (2/4)
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-bridge &amp;&amp; pytest tests/test_async_client_correlation.py -x -q --no-header &amp;&amp; python -c "import inspect; from forge_bridge.client.sync_client import SyncClient; sig = inspect.signature(SyncClient.entity_list); assert 'shot_id' in sig.parameters and 'role' in sig.parameters and 'source_name' in sig.parameters; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'ref_msg_id' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/client/async_client.py returns >= 2 (once for "ok" branch, once for "error" branch)
    - grep -c 'msg.get("ref_msg_id") or msg.msg_id' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/client/async_client.py returns >= 2
    - grep -c 'project_name' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/client/async_client.py returns 0 (forge-specific; stays in projekt-forge)
    - grep -cE 'shot_id|role|source_name' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/client/sync_client.py returns >= 3
    - cd /Users/cnoellert/Documents/GitHub/forge-bridge && pytest tests/test_async_client_correlation.py -x exits 0
    - cd /Users/cnoellert/Documents/GitHub/forge-bridge && pytest exits 0
    - git -C /Users/cnoellert/Documents/GitHub/forge-bridge log -1 --pretty=%s matches fix\(client\).*v1\.0\.1 patch \(2/4\)
  </acceptance_criteria>
  <done>
    AsyncClient._handle_message correlates via ref_msg_id-first fallback for both "ok" and "error" branches; backward compat with id-echo servers preserved; sync_client.entity_list has shot_id/role/source_name keyword-only kwargs that pass through to protocol.entity_list; no project_name kwarg leaked into canonical; test file covers all four behaviors; full suite green; atomic commit (2/4) landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port timeline.rename_shots gap-fill fix + bump version to 1.0.1</name>
  <files>forge_bridge/tools/timeline.py, pyproject.toml, tests/test_timeline_gap_fill.py</files>
  <read_first>
    - forge_bridge/tools/timeline.py (current — see rename_shots around line 249; confirm gap-handling logic; note .strip("'\\"") already present on lines 1447, 1452 per grep)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/timeline.py lines 249-400 (the rename_shots with gap_fills set + upward track scan — port this logic into canonical)
    - /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/timeline.py lines 280-310 (the gap_fills set construction at the top of the rename loop)
    - forge_bridge/pyproject.toml (current version = "1.0.0" — bump to "1.0.1")
    - .planning/phases/05-import-rewiring/05-RESEARCH.md §D-10 "timeline.py" disposition row (gap-fill fix is pushed to canonical so projekt-forge can DELETE its local copy)
  </read_first>
  <behavior>
    - Test: rename_shots on a timeline with a real segment on T1 and a gap on T0 at the same record range produces a rename entry that treats the T1 segment as the shot, not None
    - Test: rename_shots on a pure-gap column (all tracks empty at a record range) does NOT produce a rename entry for that gap
    - Test: rename_shots preserves existing behavior when T0 has a real segment (no regression — upward scan only kicks in when T0 is gap)
    - Test: pyproject.toml version string becomes "1.0.1"
  </behavior>
  <action>
    Part A — TDD port the gap-fill fix:

    Write tests/test_timeline_gap_fill.py with stubs for the three gap-fill behaviors above. Mock flame.PySegment-shaped objects with .name, .record_in, .record_out, .source_name, .file_path attributes. Use a SimpleNamespace or dataclass to build test tracks. Look at existing tests/test_timeline_*.py if present for the mocking pattern.

    Run RED. Then edit forge_bridge/tools/timeline.py::rename_shots (around line 249):

    Port the projekt-forge version's gap-fill algorithm (reference: /Users/cnoellert/Documents/GitHub/projekt-forge/forge_bridge/tools/timeline.py lines ~283-310):
    1. Inside the rename_shots function, before iterating T0 segments, build an empty gap_fills = set() (stores id(seg) of segments used as gap fills).
    2. For each T0 gap (segment with empty/None source_name), scan upward through T1, T2, etc. to find a real segment whose (record_in, record_out) covers the gap range.
    3. If found: record id(higher_track_segment) in gap_fills and use that segment's shot/source_name for the rename entry.
    4. If no real segment found on any track: skip — do not produce a rename entry (pure-gap column).

    The projekt-forge timeline.py around lines 283-310 has the reference implementation — port it verbatim into the canonical function, adjusting only for any signature/type-annotation differences.

    Run pytest tests/test_timeline_gap_fill.py -x — GREEN. Full suite green.

    Commit: fix(timeline): fill T0 gaps by scanning upward tracks in rename_shots -- forge-bridge v1.0.1 patch (3/4)

    Part B — Version bump:

    Edit pyproject.toml line 7:
        version = "1.0.1"

    Commit: chore(release): bump version to 1.0.1 -- forge-bridge v1.0.1 patch (4/4)

    Tag creation and push is deferred to the checkpoint task below (requires user confirmation for network push).
  </action>
  <verify>
    <automated>cd /Users/cnoellert/Documents/GitHub/forge-bridge &amp;&amp; pytest tests/test_timeline_gap_fill.py -x -q --no-header &amp;&amp; grep -c 'gap_fills' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/tools/timeline.py &amp;&amp; grep -c 'version = "1.0.1"' /Users/cnoellert/Documents/GitHub/forge-bridge/pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'gap_fills' /Users/cnoellert/Documents/GitHub/forge-bridge/forge_bridge/tools/timeline.py returns >= 1
    - grep -c 'version = "1.0.1"' /Users/cnoellert/Documents/GitHub/forge-bridge/pyproject.toml returns 1
    - grep -c 'version = "1.0.0"' /Users/cnoellert/Documents/GitHub/forge-bridge/pyproject.toml returns 0
    - cd /Users/cnoellert/Documents/GitHub/forge-bridge && pytest tests/test_timeline_gap_fill.py -x exits 0
    - cd /Users/cnoellert/Documents/GitHub/forge-bridge && pytest exits 0
    - git -C /Users/cnoellert/Documents/GitHub/forge-bridge log --oneline -2 | grep -c 'v1.0.1 patch' returns 2 (gap-fill + version-bump commits)
  </acceptance_criteria>
  <done>
    timeline.py has gap_fills set + upward track scan in rename_shots; pyproject.toml version is "1.0.1"; test_timeline_gap_fill.py passes; full suite green; two atomic commits landed (3/4 and 4/4). Tag creation + push is handled by the next checkpoint task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Create and push v1.0.1 git tag (network push confirmation)</name>
  <files>/Users/cnoellert/Documents/GitHub/forge-bridge (no file edits — tag + push only)</files>
  <action>CHECKPOINT — see &lt;what-built&gt;, &lt;how-to-verify&gt;, and &lt;resume-signal&gt; below. Upon user approval, executor runs: git tag -a v1.0.1 -m "v1.0.1: protocol builders + ref_msg_id fix + timeline gap-fill" && git push origin main && git push origin v1.0.1 (all from /Users/cnoellert/Documents/GitHub/forge-bridge).</action>
  <verify>See &lt;acceptance_criteria&gt; for the four post-push checks.</verify>
  <done>v1.0.1 annotated tag exists on origin, origin/main is at HEAD, working tree clean.</done>
  <what-built>
    Four v1.0.1 patch commits on /Users/cnoellert/Documents/GitHub/forge-bridge main branch:
    1. feat(protocol): query_lineage, query_shot_deps, media_scan builders + entity_list narrowing (1/4)
    2. fix(client): ref_msg_id correlation fallback + sync_client.entity_list narrowing (2/4)
    3. fix(timeline): T0 gap-fill via upward track scan (3/4)
    4. chore(release): bump version to 1.0.1 (4/4)

    Tag + push NOT yet executed. This checkpoint gates the network push to GitHub.
  </what-built>
  <how-to-verify>
    Before confirming, the executor runs:
        cd /Users/cnoellert/Documents/GitHub/forge-bridge
        git log --oneline origin/main..HEAD   # show the 4 patch commits
        git status                             # must be clean
        pytest                                 # must be green

    Verify the 4 commits match the commit messages above and the working tree is clean.

    Then on user approval, executor runs:
        git tag -a v1.0.1 -m "v1.0.1: protocol builders + ref_msg_id fix + timeline gap-fill"
        git push origin main
        git push origin v1.0.1

    Post-push verification:
    - git ls-remote --tags origin | grep -c 'refs/tags/v1.0.1$' returns 1
    - pip install 'forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.0.1' --dry-run in a clean env resolves without error
  </how-to-verify>
  <resume-signal>Type "approved" to execute git push origin main && git push origin v1.0.1, or "hold" if Wave 0 patches need review first</resume-signal>
  <acceptance_criteria>
    - git -C /Users/cnoellert/Documents/GitHub/forge-bridge ls-remote --tags origin output contains refs/tags/v1.0.1
    - git -C /Users/cnoellert/Documents/GitHub/forge-bridge tag --list 'v1.0.*' emits v1.0.1
    - git -C /Users/cnoellert/Documents/GitHub/forge-bridge status --porcelain returns empty (clean tree)
    - git -C /Users/cnoellert/Documents/GitHub/forge-bridge rev-parse origin/main equals git -C /Users/cnoellert/Documents/GitHub/forge-bridge rev-parse HEAD
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:

1. Patch completeness:
   - grep -c 'def query_lineage\|def query_shot_deps\|def media_scan' forge_bridge/server/protocol.py >= 3
   - grep -c 'ref_msg_id' forge_bridge/client/async_client.py >= 2
   - grep -c 'shot_id' forge_bridge/client/sync_client.py >= 1
   - grep -c 'gap_fills' forge_bridge/tools/timeline.py >= 1
   - pyproject.toml shows version = "1.0.1"

2. Test suite: cd /Users/cnoellert/Documents/GitHub/forge-bridge && pytest exits 0

3. Tag + remote:
   - git -C /Users/cnoellert/Documents/GitHub/forge-bridge tag --list includes v1.0.1
   - git -C /Users/cnoellert/Documents/GitHub/forge-bridge ls-remote --tags origin includes refs/tags/v1.0.1

4. Dry-run consumer resolution:
   - pip install 'forge-bridge @ git+https://github.com/cnoellert/forge-bridge.git@v1.0.1' --dry-run exits 0
</verification>

<success_criteria>
- forge_bridge/server/protocol.py exports query_lineage, query_shot_deps, media_scan, and narrowed entity_list
- forge_bridge/client/async_client.py _handle_message correlates responses via ref_msg_id-first fallback
- forge_bridge/client/sync_client.py entity_list accepts shot_id/role/source_name keyword-only kwargs
- forge_bridge/tools/timeline.py rename_shots fills T0 gaps by scanning upward tracks
- pyproject.toml version = "1.0.1"
- Four atomic v1.0.1 patch commits on main
- v1.0.1 annotated tag created + pushed to origin
- Full pytest suite green post-patch
- pip install forge-bridge@git+...@v1.0.1 dry-run resolves
</success_criteria>

<output>
After completion, create .planning/phases/05-import-rewiring/05-00-SUMMARY.md with:
- Commits landed (hashes + subjects)
- Tag sha + remote push confirmation
- Test suite duration + pass count
- Any deviations from the patch plan
</output>
</content>
