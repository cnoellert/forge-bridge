---
phase: 19-code-quality-polish
verified: 2026-04-29T00:00:00Z
status: passed
score: 14/14 must-haves verified
overrides_applied: 0
requirements_satisfied: [POLISH-01, POLISH-02, POLISH-03, POLISH-04]
---

# Phase 19: Code-quality polish — Verification Report

**Phase Goal:** Code-quality polish for v1.4.x — close POLISH-01..04 carry-forward items: WR-02 ref-collision guard in salvage helper; Phase 13 type-contract + atomicity sub-test fixes; qwen2.5-coder `<|im_start|>` tail-token strip in OllamaToolAdapter.

**Verified:** 2026-04-29
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Aggregated from PLAN frontmatter `must_haves.truths` across 19-01..19-04 (14 truths total). Roadmap inline-list format for v1.4.x carry-forward debt has no separate `### Success Criteria` block; phase goal text + four POLISH-XX requirement IDs constitute the contract, fully realized through the 14 plan-level truths below.

| #   | Truth (source plan)                                                                                                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | (19-01) Salvaged tool-call ref composed at call site from current `tool_calls` length, not hardcoded literal                                                                                                                                          | VERIFIED   | `forge_bridge/llm/_adapters.py:591` — `salvaged = replace(salvaged, ref=f"{len(tool_calls)}:{salvaged.tool_name}")`                                                                                     |
| 2   | (19-01) String literal `"0:"` no longer appears in salvage helper or its call site (excluding comments)                                                                                                                                              | VERIFIED   | `grep -nE 'ref=f"0:' forge_bridge/llm/_adapters.py \| grep -v '^\s*#'` returns 0 matches                                                                                                              |
| 3   | (19-01) Salvage helper returns placeholder ref (`salvage:{name}`) that call site overwrites with index-derived ref                                                                                                                                   | VERIFIED   | `forge_bridge/llm/_adapters.py:207` — `ref=f"salvage:{name}"`; call site at line 591 overrides via `replace`                                                                                            |
| 4   | (19-01) Unit test asserts `resp.tool_calls[0].ref == "0:forge_tools_read"`                                                                                                                                                                            | VERIFIED   | `tests/llm/test_ollama_adapter.py:436` — `test_salvaged_tool_call_ref_uses_index_zero_when_first_call` PASSED                                                                                            |
| 5   | (19-02) Literal `"(missing)"` no longer appears anywhere in `forge_bridge/` or `tests/`                                                                                                                                                              | VERIFIED   | `grep -rn '"(missing)"' forge_bridge/ tests/` returns 0 matches; test source uses runtime-reconstruction `"(" + "missing" + ")"`                                                                         |
| 6   | (19-02) Comment at `staged_operations.py:325-330` reads as past-tense closure                                                                                                                                                                          | VERIFIED   | `forge_bridge/store/staged_operations.py:326` — `# WR-01 (Phase 13 review) was closed by passing`                                                                                                       |
| 7   | (19-02) Regression test asserts `from_status` is `None` for unknown UUID, non-None status string for illegal transition, never `"(missing)"`                                                                                                       | VERIFIED   | `tests/test_staged_operations.py:631` — `test_lifecycle_error_from_status_is_optional_str_never_missing_sentinel` PASSED against live Postgres                                                          |
| 8   | (19-03) `test_transition_atomicity` no longer contains `assert True  # placeholder` and no longer contains contradictory `assert row is None` block                                                                                                  | VERIFIED   | `grep -nE 'assert True  # placeholder' tests/test_staged_operations.py` and `grep -nE 'assert row is None'` both return 0 matches                                                                       |
| 9   | (19-03) Rewritten body uses single `async with session_factory()` block exercising propose+commit, approve+flush+observe, rollback, observe-again — all in same session                                                                              | VERIFIED   | `tests/test_staged_operations.py:323-350` — single block confirmed via Read; `events_after = await EventRepo(session).get_recent(entity_id=op.id, ...)` at line 345; `assert fetched.status == "proposed"` at line 350 |
| 10  | (19-03) Test passes against live dev Postgres at `:7533/forge_bridge`                                                                                                                                                                                | VERIFIED   | `FORGE_DB_URL=postgresql+asyncpg://forge:forge@127.0.0.1:7533/forge_bridge pytest test_transition_atomicity` PASSED in re-run                                                                            |
| 11  | (19-03) RED→GREEN evidence recorded in 19-03-SUMMARY.md only — not landed as separate RED commit                                                                                                                                                     | VERIFIED   | 19-03-SUMMARY.md "RED → GREEN evidence" section captures both runs verbatim; `git log --oneline` shows only the GREEN `5f7ad7f` commit (no separate RED commit)                                          |
| 12  | (19-04) New private helper `_strip_terminal_chat_template_tokens(text: str) -> str` lives in `forge_bridge/llm/_adapters.py` (NOT in `_sanitize_patterns.py`)                                                                                       | VERIFIED   | `forge_bridge/llm/_adapters.py:238` — helper present; colocation guard `grep _strip_terminal_chat_template_tokens forge_bridge/_sanitize_patterns.py` returns 0 matches (FB-C D-09 honored)             |
| 13  | (19-04) Helper invoked in `OllamaToolAdapter.send_turn()` AFTER salvage block and BEFORE constructing `_TurnResponse`                                                                                                                                | VERIFIED   | `forge_bridge/llm/_adapters.py:598` — `text = _strip_terminal_chat_template_tokens(text)` immediately after salvage block at line 591, before `_TurnResponse` construction                              |
| 14  | (19-04) `INJECTION_MARKERS` grows from 8 to 10 entries (adds `<|im_end|>` and `<|endoftext|>`); count-lock test bumped 8→10 in same atomic commit; new `TestOllamaToolAdapterChatTemplateTailStrip` class with two methods                            | VERIFIED   | `len(INJECTION_MARKERS) == 10`; `<|im_end|>` and `<|endoftext|>` present, `<|im_sep|>` absent; `tests/test_sanitize.py:177` — `assert len(INJECTION_MARKERS) == 10`; tail-strip class at line 477 with 2 methods PASSED; all four edits in single commit `1b4e525` |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact                                       | Expected                                                                                       | Status                | Details                                                                                                                                                  |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `forge_bridge/llm/_adapters.py`                | replace import; salvage placeholder; call-site override; tail-strip helper + integration       | VERIFIED              | Line 29 `from dataclasses import dataclass, replace`; line 207 placeholder; line 591 override; lines 224-251 tail-strip helper; line 598 integration call |
| `forge_bridge/_sanitize_patterns.py`           | `INJECTION_MARKERS` extended with `<|im_end|>` and `<|endoftext|>` (10 entries total)         | VERIFIED              | `len(INJECTION_MARKERS) == 10`, both new tokens present, `<|im_sep|>` correctly absent                                                                  |
| `forge_bridge/store/staged_operations.py`      | Past-tense closure comment; no `(missing)` literal; production raise unchanged                  | VERIFIED              | Line 326 `WR-01 (Phase 13 review) was closed by passing`; line 330 production raise unchanged                                                            |
| `tests/llm/test_ollama_adapter.py`             | POLISH-01 regression test method + new `TestOllamaToolAdapterChatTemplateTailStrip` class       | VERIFIED              | Line 436 `test_salvaged_tool_call_ref_uses_index_zero_when_first_call`; line 477 `TestOllamaToolAdapterChatTemplateTailStrip` with 2 methods at 504/535 |
| `tests/test_staged_operations.py`              | POLISH-02 regression test + rewritten `test_transition_atomicity` body                          | VERIFIED              | Line 631 `test_lifecycle_error_from_status_is_optional_str_never_missing_sentinel`; lines 323-350 rewritten atomicity body                              |
| `tests/test_sanitize.py`                       | Count-lock assertion bumped 8 → 10 atomically with tuple extension                              | VERIFIED              | Line 177 `assert len(INJECTION_MARKERS) == 10`; old `== 8` form absent; landed in atomic commit `1b4e525` together with `_sanitize_patterns.py` change   |

### Key Link Verification

| From                                                          | To                                                                          | Via                                                                              | Status | Details                                                                                                                                                  |
| ------------------------------------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 19-01 salvage block at `_adapters.py:543`                     | `_try_parse_text_tool_call` helper at `_adapters.py:122`                    | `salvaged = _try_parse_text_tool_call(text); replace(salvaged, ref=...)`         | WIRED  | Line 591 contains exact `replace(salvaged, ref=f"{len(tool_calls)}:{salvaged.tool_name}")` invocation pattern                                            |
| 19-02 `StagedOpRepo._transition` `staged_op_not_found` branch | FB-B handlers (404 vs 409 routing)                                          | `from_status is None` discriminator                                              | WIRED  | Line 330 `from_status=None, to_status=new_status, op_id=op_id`; type signature at line 74 `from_status: str \| None`; regression test pins both branches |
| 19-03 `test_transition_atomicity` body                        | `StagedOpRepo.approve / EventRepo.get_recent / StagedOpRepo.get / session.rollback()` | single-session observation (CONTEXT D-08)                                       | WIRED  | All four calls present in single block; rollback test against live Postgres PASSED                                                                       |
| 19-04 `OllamaToolAdapter.send_turn`                           | `_strip_terminal_chat_template_tokens` helper                                | `text = _strip_terminal_chat_template_tokens(text)` (after salvage, before return) | WIRED  | Line 598 invokes helper post-salvage at line 591, before `_TurnResponse` construction                                                                   |

### Data-Flow Trace (Level 4)

Phase 19 is a code-quality polish phase. Modules touched are not UI components rendering dynamic data; they are LLM adapters, stores, and tests. Data flow trace is replaced by behavioral spot-checks below.

### Behavioral Spot-Checks

| Behavior                                                                       | Command                                                                                                                                                              | Result                                                | Status |
| ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------ |
| `_strip_terminal_chat_template_tokens` strips token tail                        | `_strip_terminal_chat_template_tokens('hello <\|im_start\|>') == 'hello'`                                                                                          | True                                                  | PASS   |
| `_strip_terminal_chat_template_tokens` passthrough on clean prose               | `_strip_terminal_chat_template_tokens('hello') == 'hello'`                                                                                                          | True                                                  | PASS   |
| `_strip_terminal_chat_template_tokens` empty short-circuit                      | `_strip_terminal_chat_template_tokens('') == ''`                                                                                                                    | True                                                  | PASS   |
| `_strip_terminal_chat_template_tokens` greedy tail with newlines                | `_strip_terminal_chat_template_tokens('hello\n<\|im_start\|><\|im_start\|>\n<\|im_start\|><\|im_start\|>') == 'hello'`                                              | True                                                  | PASS   |
| `_strip_terminal_chat_template_tokens` mid-content untouched                    | `_strip_terminal_chat_template_tokens('<\|im_start\|>mid prose') == '<\|im_start\|>mid prose'`                                                                      | True                                                  | PASS   |
| `INJECTION_MARKERS` length and membership                                       | `len == 10`, contains `<\|im_end\|>` + `<\|endoftext\|>`, excludes `<\|im_sep\|>`                                                                                  | True                                                  | PASS   |
| All POLISH-targeted regression tests pass against live Postgres                 | 6 specific tests run with `FORGE_DB_URL=postgresql+asyncpg://forge:forge@127.0.0.1:7533/forge_bridge`                                                                | 6 passed in 0.29s                                     | PASS   |
| Full unit suite regression                                                     | `pytest tests/` with `FORGE_DB_URL` set                                                                                                                              | 865 passed, 19 skipped (env-gated), 0 failed in 35.4s | PASS   |
| ReDoS worst-case check on tail-strip regex (IN-01 informational)                | Realistic 8KB input timing                                                                                                                                           | 0.12 ms (well under 20ms budget cited in code review)  | PASS   |
| All four atomic commits exist on the worktree branch                            | `git show --no-patch 47133b0 55168e3 5f7ad7f 1b4e525`                                                                                                                | All four commit messages match plan-mandated form    | PASS   |

Note: SUMMARY claimed "860 passed, 4 by-design skips". Re-run on this verifier observed 865 passed / 19 skipped — extra skips are environment-gated (Ollama daemon and `FB_INTEGRATION_TESTS` not present in this run); zero failures and zero new skips of the targeted regression tests.

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                       |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| POLISH-01   | 19-01       | WR-02 closure — `_try_parse_text_tool_call` no longer hard-codes `ref="0:{name}"`; new unit test verifies derivation when salvage runs alongside zero structured `tool_calls`                                | SATISFIED  | Helper placeholder + call-site override + unit test all landed in commit `47133b0`; static greps + behavioral test PASSED                                                                                                                                       |
| POLISH-02   | 19-02       | Phase 13 WR-01 closure — type-contract issue with `from_status="(missing)"`; replace sentinel with proper `Optional[str]`; no `"(missing)"` literal                                                          | SATISFIED  | Production code already correct (`from_status=None` at line 330, signature `str \| None` at line 74); past-tense comment + regression test landed in commit `55168e3`; grep guard returns 0 matches across `forge_bridge/` and `tests/`                        |
| POLISH-03   | 19-03       | Phase 13 WR-02 closure — placeholder cross-session atomicity sub-test becomes real assertion; RED on broken atomicity, GREEN on current code, both behaviors recorded in phase summary                       | SATISFIED  | Test body rewritten to single-session observation in commit `5f7ad7f`; RED→GREEN evidence captured in 19-03-SUMMARY.md (no RED commit landed); test PASSED against live Postgres at `:7533`                                                                    |
| POLISH-04   | 19-04       | qwen2.5-coder `<|im_start|>` tail-token strip; fresh fixture from qwen2.5-coder noise-tail variant fails on unstripped current behavior, passes after strip; no impact on legitimate prose responses        | SATISFIED  | Helper + integration + new test class + count-lock bump all in atomic commit `1b4e525`; both test methods PASSED; clean-prose passthrough confirmed; mid-content untouched per FB-C D-09 layering. **Note**: helper landed in `OllamaToolAdapter` (adapter layer) not `console/handlers.py`; this is per CONTEXT D-11 (provider-scoped) — strictly stronger than the requirement wording. |

All 4 declared phase requirements satisfied; no orphaned requirements found in REQUIREMENTS.md mapping table for Phase 19.

### Anti-Patterns Found

Run on the six modified source files. None of the matches below are stubs, placeholders, or unwired code.

| File                                          | Line | Pattern                                            | Severity | Impact                                                                                                                                                                                                                                                                  |
| --------------------------------------------- | ---- | -------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `forge_bridge/_sanitize_patterns.py`          | n/a  | (no anti-patterns found)                            | n/a      | Clean tuple extension                                                                                                                                                                                                                                                   |
| `forge_bridge/llm/_adapters.py`               | n/a  | (no anti-patterns found)                            | n/a      | New helper has substantive body; salvage placeholder ref `f"salvage:{name}"` is intentional and overridden at call site (regression test asserts placeholder cannot leak)                                                                                              |
| `forge_bridge/store/staged_operations.py`     | n/a  | (no anti-patterns found)                            | n/a      | Comment-only change; production raise byte-identical                                                                                                                                                                                                                    |
| `tests/llm/test_ollama_adapter.py`            | n/a  | (no anti-patterns found)                            | n/a      | Test bodies have real assertions and call live mocks                                                                                                                                                                                                                    |
| `tests/test_sanitize.py`                       | n/a  | (no anti-patterns found)                            | n/a      | Single-line assertion bump; intent preserved                                                                                                                                                                                                                            |
| `tests/test_staged_operations.py`             | n/a  | (no anti-patterns found)                            | n/a      | Both new test bodies exercise repo APIs against live Postgres; placeholder + contradiction-assertion blocks confirmed removed                                                                                                                                            |

Code review (19-REVIEW.md) reported 0 critical, 0 warning, 3 info findings (IN-01 ReDoS theoretical worst case bounded under realistic 8KB inputs at <0.2ms; IN-02 token-list duplication intentional per CONTEXT D-13; IN-03 `dataclasses.replace` style choice). All info-level; none blocking.

### Human Verification Required

None. Phase 19 is a pure code-quality polish phase: all four POLISH items are deterministically verifiable via static greps + automated regression tests + live-Postgres unit tests. There is no UI surface, no real-time behavior, no external service contract change, and no fresh-operator UAT requirement carried forward.

The Phase 16.2 fresh-operator UAT artifact (HUMAN-UAT.md:106-107) provided the canonical noise-tail fixture for POLISH-04; that fixture is already encoded as the synthetic `NOISE_TAIL_PROSE` in `TestOllamaToolAdapterChatTemplateTailStrip`. No re-UAT needed.

### Gaps Summary

No gaps. All 14 plan-level must-have truths verified, all four POLISH-XX requirements satisfied, all four atomic commits present on the worktree branch (`47133b0`, `55168e3`, `5f7ad7f`, `1b4e525`), full unit test suite (865 passed, 0 failed), POLISH-targeted regression tests (6/6 passed) against live dev Postgres at `:7533/forge_bridge`. Code review reported 0 blocking, 3 informational findings (all bounded / by-design / style).

Phase 19 closes the v1.4.x carry-forward POLISH track. Combined with Phase 17 (MODEL-01..02) and Phase 18 (HARNESS-01..03), all 9 v1.4.x requirements are now satisfied. Ready for the post-milestone audit and `v1.4.1` tag per ROADMAP.md target.

---

_Verified: 2026-04-29_
_Verifier: Claude (gsd-verifier)_
