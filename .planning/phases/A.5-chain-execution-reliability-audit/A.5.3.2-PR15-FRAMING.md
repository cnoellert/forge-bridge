# A.5.3.2 PR 15 — Framing (multi-survivor cardinality divergence as Gate 4 calibration exercise + PR-12 trigger surface)

**Status:** PR 15 opens at `bbbeead` (local main + origin/main
parity, PR 14 closed). This framing locks the architectural
posture for the multi-survivor cardinality divergence exercise
— Gate 4's third + final primary PR + the third calibration
substrate for Placement A + Placement B operational
corroboration + the dedicated PR-12 trigger surface evaluation
vehicle — and the binding decisions reached during the PR 14
close → PR 15 framing convergence pass.

PR 15 is the third + final of three primary PRs sequenced
within Gate 4 per Gate 4 framing §5.5 + §5.6. The three-PR
slot structure (PR 13 ordering → PR 14 partial-narrow → PR 15
multi-survivor mismatch) closes here; PR 15 framing operates
against the locked sequence + ordering.

The convergence pass produced six binding decisions: PR 15
ships exactly one new fixture (filename
`fix_multi_survivor_mismatch.py`; `fixture_id` field
`fix-pr15-multi-survivor-mismatch`; module export `FIXTURE`
per PR 9 canonical convention) + exactly one new test
(`test_recomposition_arc_multi_survivor_mismatch`) — single-
vector, single-direction (authored-single-vs-observed-multi),
single-instance of the multi-survivor cardinality divergence
calibration exercise (§5.1 + §5.2 binding); 0 production
source modifications is the architectural sufficiency signal
target inherited at gate level from Gate 4 framing §2.2 (§5.3
binding) extending the five-PR cumulative escalation evidence
PR 14 contributed; three predicted cleanup-pressure forms
enumerated with named suppression mechanisms (§5.4 binding,
Placement A + B substrate) per Gate 4 framing §10 PR 15
binding; one PR-15-LOCAL binding statement (§0 + §5.5) —
multi-survivor cardinality isolation discipline rejecting
multi-vector fixture pressure (references PR-13-LOCAL as
PR-of-origin for the pure-isolation pattern + PR-14-LOCAL as
parallel-not-regenerative scope-local discipline per PR 13
close §2.2 + PR 14 close §1.4 + §2.2); **the authored-single-
vs-observed-multi direction is an affirmative architectural
decision** (§5.10) — the inverse of PR 14's authored-superset
direction, exercising the comparator's compare-as-persisted
discipline under direction-symmetric cardinality pressure
across the PR 14 + PR 15 two-PR pair; **the PR-12 trigger
surface evaluation responsibility is encoded** (§5.12) — PR 15
close §1 inventories actual join call-site count + qualitative
second-clause pressure per Gate 4 framing §10 PR 15 binding +
§5.10 conditional reopen.

**Active carrier count at PR 15 open: 17.** Composition
unchanged from Gate 4 framing §3.1 + PR 13 inheritance + PR 14
inheritance: 15 inherited carriers (#1–#15) + carrier #16
(active, promoted at Gate 3 close) + carrier #17 (active from
Gate 3 framing). PR 15 introduces NO new carriers; the
no-new-carriers discipline preserves from Gate 4 framing §3.1
+ §6.1 + PR 13 framing §3.1 + PR 14 framing §3.1.

**Reference discipline (binding):** *"17 active carriers"* is
the canonical reference. The candidate-substrate marking
discipline retired at Gate 3 close; PR 15 surfaces travel
carriers in natural numeric ordering without substrate
marking, citing by reference to canonical sources per PR 13
framing §3.1 + PR 14 framing §3.1 citation-by-reference
precedent.

---

## 0. PR-15-LOCAL binding statement — pure-isolation discipline

One sentence governs PR 15 verbatim. It appears at the top of
the PR 15 test module docstring + PR 15 fixture module
docstring + PR 15 commit message bodies under "preserved
invariants."

**PR-15-LOCAL binding statement — multi-survivor cardinality isolation:**

> **PR 15 isolates multi-survivor cardinality divergence as
> the sole pressure vector. Multi-vector fixture pressure
> within PR 15 scope — combining cardinality with ordering,
> semantic-normalization, duplicate-handling, partial-set
> (within shared cardinality), or any other divergence form —
> is rejected at the spec layer. The pure-isolation property
> is what gives PR 15 its laboratory-grade methodology
> corroboration value for Placement A + Placement B
> substrate.**

This is the operational discipline statement governing PR 15
implementation. PR 15's evidentiary value depends entirely on
single-vector isolation: if multi-vector pressure surfaces
within the fixture or the test, attribution from observed
outcomes to the single shaping constraint becomes impossible,
and the absence-evidence loses its methodology-corroboration
weight.

**Why "laboratory-grade":**

The vocabulary is deliberate and inherits the framing PR 13
established + PR 14 corroborated. PR 15 IS a pure-isolation
case — its multi-survivor cardinality vector operates at the
comparator's interpretive layer without ordering, semantic-
normalization, duplicate, or partial-set-within-shared-
cardinality confound. The authored expectation contains
exactly ONE element matching observation position 0 verbatim
(no ordering confound; no semantic-normalization confound —
the element is an exact-match identifier); the cardinality-
class asymmetry IS the entire divergence vector (authored
length 1 vs. observed length 2; cardinality classes singleton
vs. multi-survivor); each list contains distinct elements (no
duplicate confound); the shared element is identical at both
sides (no partial-set-within-shared-cardinality confound).

PR 15's multi-survivor-cardinality-only-vector property is
the third calibration point in the three-PR series; the
cumulative calibration arc (PR 13 ordering / PR 14 partial-
set / PR 15 multi-survivor) gives Placement A + Placement B
operational corroboration substrate at three independent-
conditions substrates BEFORE Gate 4 close evaluates promotion
candidacy. PR 15 is the FINAL calibration point; the three-
point series closes at PR 15.

**Why PR-15-LOCAL is non-regenerating:**

Per the PR-N-LOCAL non-regeneration rule (Gate 2 framing
§3.1) + PR 13 framing §0 inheritance + PR 14 framing §0
inheritance, PR-15-LOCAL is scope-local. It does NOT travel
to PR 15 successors — future Gate-X work authors its own
PR-N-LOCAL binding appropriate to its target divergence
dimension. The non-regeneration is the discipline; future
work inheriting "multi-survivor cardinality isolation"
verbatim would be category error since future-Gate-X
substrates may target entirely different divergence
dimensions (e.g., semantic-normalization, duplicate-handling).

**PR-13-LOCAL + PR-14-LOCAL as predecessor archaeology:**

PR-13-LOCAL is the PR-of-origin for the pure-isolation
discipline pattern (per PR 13 close §2.2). PR-14-LOCAL is
parallel scope-local — it authored its own discipline
statement appropriate to partial-set divergence rather than
regenerating PR-13-LOCAL's ordering-isolation form (per PR 14
close §1.4 + §2.2; introducing the parallel-not-regenerative
discipline pattern). PR-15-LOCAL is the **second corroboration
of the parallel-not-regenerative pattern**: it authors its
own discipline statement appropriate to multi-survivor
cardinality divergence rather than regenerating either
PR-13-LOCAL or PR-14-LOCAL. The two-PR corroboration of the
parallel-not-regenerative pattern (PR 14 introduced + PR 15
second corroboration) registers as cumulative architectural
archaeology at Gate 4 close.

The pattern (single-vector fixture pressure as laboratory-
grade calibration substrate) inherits as architectural-
substrate evidence; the specific PR-N-LOCAL statements are
non-regenerating scope-local bindings.

**Travel discipline at PR 15:**

PR-15-LOCAL appears at:

- `tests/corpus/test_pr15_multi_survivor_mismatch.py` module
  docstring (Step 1 surface).
- `tests/corpus/fixtures/fix_multi_survivor_mismatch.py`
  module docstring (Step 2 surface, when authored).
- All PR 15 commit message bodies under "preserved
  invariants" / "PR-15-LOCAL" sections.
- This framing artifact §0 (verbatim form above).

**No Gate-4-LOCAL governing sentence accompanies PR-15-
LOCAL.** Gate 4 framing §3.1 + §6.1 + §7 item 22 explicitly
rejected speculative Gate-4-LOCAL authoring; PR 13 framing §0
preserved the rejection; PR 14 framing §0 preserved the
rejection; PR 15 framing inherits the rejection unchanged.
The §0 carries PR-15-LOCAL alone.

---

## 1. Predecessors (binding, in order)

- `A.5.3.2-FRAMING.md` — phase shape, objective lock.
- `A.5.3.2-INSTRUMENT-CONTRACT.md` — instrument shape, six
  interlocking structural-invariant pairs.
- `A.5.3.2-GATE-1-SPEC.md` — Gate 1 sequencing, three
  architecturally-prohibited patterns, helper signature,
  visual-asymmetry pattern.
- `A.5.3.2-GATE-2-FRAMING.md` (`ceac9b5`) — three-authority-
  surface partition; carrier #14; binding clarification on
  call-site-owned arbitration inputs.
- `A.5.3.2-PR7-CLOSE.md` (`b035c87`) — observation +
  dispatch-provenance surfaces; carrier #14;
  `_KNOWN_RECORD_KINDS` 2-element lock; cleanup-pressure-
  resistance class members #1–#6.
- `A.5.3.2-PR8-CLOSE.md` (`b102010`) — authored expectation
  surface; PR-INTERNAL three-way authority partition (write-
  side); carrier #15; members #7 + #8.
- `A.5.3.2-PR9-CLOSE.md` (`a6e42f0`) — three-fixture corpus;
  grounding-time amendment variant; member #9; fixture
  corpus locked as archaeology; **fixture naming convention**
  PR 15 inherits; PR 9 integration test infrastructure
  (`_apply_pr9_patches`, `_read_records`) PR 15 reuses as
  test-internal archaeology surfaces; **multi-match
  arbitration trace** at `fix_multi_match.py:105-140` PR 15
  inherits as observation-emission grounding.
- `A.5.3.2-GATE-2-CLOSE.md` (`a6e42f0`) — gate-arc synthesis;
  four §7.3 ontological questions unbinding (preserved at
  PR 15 inheritance via Gate 4 framing §5.2).
- `A.5.3.2-GATE-3-FRAMING.md` (`2f70cbf`) — Path B locked
  precedent; binding framing clarification on cross-surface
  unbinding (preserved at Gate 4 inheritance + PR 15
  inheritance).
- `A.5.3.2-PR10-CLOSE.md` (`cf2b7ee`) — comparator surface
  operational; `compare_records` + `DivergenceReport` +
  `ComparatorInputError` (PR 10 spec §4.1.6 reference
  implementation) PR 15 consumes unchanged; **§4.2 binding
  behavioral commitment ("compare as persisted")** is the
  central architectural commitment PR 15 exercises under
  multi-survivor cardinality divergence pressure; class
  member #10.
- `A.5.3.2-PR11-CLOSE.md` (`ee2225b`) — recomposition arc
  operational end-to-end; **PR-11-LOCAL traverses-not-erases-
  seams discipline** applied at gate level (Gate 3 close §3
  item 10) — PR 15 inherits the discipline as gate-level
  constraint, NOT PR-11-LOCAL regeneration; zero-incarnation-
  amendments cleanest arc; §5.3 candidate methodology
  second-instance ABSENCE outcome.
- `A.5.3.2-GATE-3-CLOSE.md` (`ee2225b`) — gate-arc synthesis;
  carrier #16 promotion; cleanup-pressure-resistance class
  promotion to named methodology (10 members); 17 active
  carriers; four-walker Layer 2 partition operational;
  conditional PR 12 DEFER; four §7.3 ontological questions
  hand forward to Gate 4 (preserved at PR 15 inheritance).
- `A.5.3.2-GATE-4-FRAMING.md` (`fbf2285`) — gate-level
  inheritance contract PR 15 operates against. §2.4
  architectural commitment ("Gate 4 is the deliberate
  continuation of empirically bounded topology proof through
  divergence-shape robustness exercise"); three-PR primary
  slot structure locked (§5.5); PR ordering locked (§5.6);
  **§10 PR 15 sub-section provides the architectural
  commitment + predicted cleanup-pressure forms + suppression
  mechanisms + PR-12 trigger surface evaluation responsibility
  PR 15 framing builds on**; §5.10 conditional PR 12 reopened-
  not-prebound (PR 15 evidence informs Gate 4 close's final
  PR 12 disposition); Placement A operational corroboration
  target (§5.8 + §6.2); Placement B methodology-stack
  maturation claim with three causality preconditions (§4.4 +
  §4.5 + §6.3); §7 22 non-acquisition commitments PR 15
  honors at gate level.
- `A.5.3.2-PR13-FRAMING.md` (`8f429b2`) — **first-of-three
  primary PR framing precedent.** PR 13's 11-section framing
  shape (§0–§10 inclusive) + 9-surface PR-LOCAL travel
  inventory (per PR 13 close §1.4 close-time inventory) +
  8-surface §2.4 architectural commitment travel inventory
  (per PR 13 close §1.5; framing surfaces deliberately
  excluded from the close inventory) + three-predicted-
  cleanup-pressure-forms-with-named-suppression-mechanisms
  pattern + evidence inflation rejection discipline + both-
  skeletons-at-Step-1 lifecycle invariant all inherit as
  framing-shape precedent at PR 15. Citation-by-reference
  discipline (carriers + bindings travel via canonical-source
  references rather than verbatim repetition) inherits as
  framing-artifact-content discipline.
- `A.5.3.2-PR13-SPEC.md` (`2e05149`) — fixture + test
  implementation contract precedent. PR 15 spec drafting will
  inherit the 11-section spec shape + locked-mergeability-
  anchors table + citation-by-reference interpretation +
  travel-discipline-asymmetry table + both-skeletons-at-
  Step-1 lifecycle lock as spec-shape precedent.
- `A.5.3.2-PR13-CLOSE.md` (`f53a469`) — first calibration
  point predecessor; PR-13-LOCAL pure-isolation discipline
  operational verification; 3-form-ABSENCE Placement A
  contribution; PR-of-origin for pure-isolation pattern (§2.2);
  PR-of-origin for both-skeletons-at-Step-1 lifecycle
  invariant (§2.4); catch-point migration instance #5 single-
  corroboration with recursive-self-governance discipline
  introduction (§1.8); five-PR populating evidence + three-PR
  ABSENCE-corroboration evidence cumulative archaeology.
- `A.5.3.2-PR14-FRAMING.md` (`30412a3`) — second-of-three
  primary PR framing precedent. PR 14's 11-section framing
  shape + Direction A authored-superset rationale (§5.10
  three-reason argumentation pattern) + `"list"`-as-
  calibration-prompt archaeology elevation (§4.6) + four
  framing-convergence-pass grounding catches as catch-shape
  continuation of instance #4 all inherit as framing-shape
  precedent at PR 15.
- `A.5.3.2-PR14-SPEC.md` (`23c358a`) — second-of-three primary
  PR spec precedent. PR 14 spec's 11-section shape +
  ~30-subsection depth + five binding decisions encoded at
  spec-level file precision + spec-drafting-time grounding
  catch as catch-shape continuation of instance #5 (line-
  number-citation correction at fix_multi_match.py:127→:126)
  all inherit as spec-shape precedent at PR 15.
- `A.5.3.2-PR14-CLOSE.md` (`bbbeead`) — **immediate
  predecessor; the second calibration substrate PR 15 builds
  on at the third calibration point.** PR 14 close's
  0-prod-mod outcome + 3-form-ABSENCE Placement A
  contribution (SECOND Gate 4 PR instance; FOURTH §5.3
  instance overall) + Placement B preconditions 1+2
  manifestation strengthening to two-PR cumulative + catch-
  point migration three-fold catch-shape continuation at PR 14
  (recursive-self-governance operating symmetrically across
  three methodology-stack levels) + cleanup-pressure-resistance
  class 10 members unchanged + PR-14-LOCAL pure-isolation
  discipline operational verification + parallel-not-
  regenerative pattern introduction at PR 14 (PR-14-LOCAL
  parallel scope-local to PR-13-LOCAL PR-of-origin) + Direction
  A authored-superset rationale archaeology all inherit as
  predecessor archaeology. **PR-13-LOCAL remains PR-of-origin**
  for pure-isolation pattern (per PR 13 close §2.2 + PR 14
  close §2.2); **PR-14-LOCAL is parallel-not-regenerative
  scope-local discipline at second calibration point** (per
  PR 14 close §1.4 + §2.2); **PR-15-LOCAL is parallel-not-
  regenerative scope-local discipline at third calibration
  point — second corroboration of the parallel-not-
  regenerative pattern**. **The both-skeletons-at-Step-1
  lifecycle invariant has two-PR operational corroboration**
  (PR 13 PR-of-origin + PR 14 second instance per PR 14 close
  §2.4) — PR 15 inherits the invariant as TWO-PR-CORROBORATED
  archaeological pattern.
- **PR 14 close → PR 15 framing convergence pass (this
  session):** six binding decisions locked (PR-15-LOCAL
  binding statement authored at framing-level decision; fixture
  + test name + count locked at one of each; 0-prod-mod target
  inherited from Gate 4 with explicit deviation protocol;
  three predicted cleanup-pressure forms enumerated per Gate
  4 framing §10 PR 15 binding with named suppression
  mechanisms; authored-single-vs-observed-multi direction
  as affirmative architectural decision INVERSE of PR 14's
  authored-superset direction; PR-12 trigger surface
  evaluation responsibility encoded at PR 15 close §1
  inventory). The convergence pass surfaced **the four-PR
  cumulative single-variable discipline (PR 9 / PR 13 / PR 14
  / PR 15)** as load-bearing architectural-substrate evidence
  (the comparator surface remains the only moving interpretive
  layer across the four-PR series — prompt, reachable-tool
  set, arbitration trace, and observation emission path all
  hold constant; only the authored expectation varies across
  four directions: no-divergence baseline / ordering swap /
  authored-superset partial-set / authored-subset multi-
  survivor); **the inverse-direction-from-PR-14** as the
  stronger architectural move (Direction A authored ⊂ observed
  at PR 15 vs. PR 14's authored ⊃ observed; two-PR pair
  exercises the comparator's compare-as-persisted discipline
  under direction-symmetric cardinality pressure); **the
  `"list"`-as-calibration-prompt archaeology** spanning four
  independent reliability PRs (PR 9 no-divergence baseline /
  PR 13 ordering / PR 14 partial-set / PR 15 multi-survivor)
  as becoming mature known-good substrate within Gate 4;
  **the parallel-not-regenerative pattern second corroboration**
  (PR-15-LOCAL parallel scope-local to PR-13-LOCAL PR-of-
  origin + PR-14-LOCAL second instance; PR-15-LOCAL third
  instance) as the operational evidence Gate 4 close evaluates
  for prescriptive lineage-discipline promotion candidacy.

---

## 2. PR 15 objective

### 2.1 The narrow architectural question

PR 15 asks one architectural question:

> Under multi-survivor cardinality divergence (authored
> singleton expectation vs. observed multi-survivor
> arbitration; OR inverse cardinality-class shape) traversing
> the validated comparator + PR 11 recomposition arc, does the
> end-to-end recomposition arc detect the cardinality-class
> divergence with `narrow_diverged=True` AND preserve the
> structural cardinality-class shape (singleton vs. multi-
> survivor) faithfully through emission → persistence →
> readback → interpretive comparison, all WITHOUT modifying
> any production source?

The question's load-bearing pieces:

1. **Multi-survivor cardinality divergence as sole pressure
   vector** (per PR-15-LOCAL §0). The fixture's authored
   expectation differs from observation at exactly one
   dimension: cardinality-class (singleton vs. multi-
   survivor). No ordering / semantic-normalization /
   duplicate-handling / partial-set-within-shared-cardinality
   confound.
2. **Detection via direct list-equality at `_compare.py:503`**
   — `obs_decision != exp_narrow` triggers per length
   asymmetry (1 vs. 2 OR 2 vs. 1) AND element-membership
   asymmetry at the non-shared position. No sort, no
   canonicalization, no semantic coercion, no cardinality-
   aware computation at any traversal seam.
3. **Cardinality-class structural-shape preservation** —
   `report["expectation"]["expected_narrow"]` carries the
   authored cardinality-class shape (length 1 OR length 2
   depending on direction); `report["observation"]["observed_narrow"]`
   carries the observed cardinality-class shape; both
   surface at distinct dict paths with distinct list lengths.
4. **0-prod-mod outcome** — the cardinality-class divergence
   is detectable end-to-end without modifying any production
   surface (comparator + recomposition arc + fixture corpus
   + test infrastructure all unchanged). PR 15 contributes
   the SIXTH PR (PR 9 + 10 + 11 + 13 + 14 + 15) to the
   architectural-sufficiency-signal escalation if 0-prod-mod
   holds.
5. **PR-12 trigger surface evaluation** — PR 15's join call-
   site contribution + qualitative second-clause pressure
   observation contribute to Gate 4 close's final PR 12
   disposition (per Gate 4 framing §10 PR 15 binding + §5.10).

The question's answer is operationally "yes" if PR 15
implementation produces a single passing test with the four-
key DivergenceReport assertions intact + 0 production source
modifications across the full PR 15 chain. The PR-12 trigger
surface evaluation produces structured archaeology at PR 15
close §1 regardless of disposition outcome.

### 2.2 What PR 15 ships

PR 15 ships exactly two new files containing exactly one
named test:

| File | Role |
|---|---|
| `tests/corpus/fixtures/fix_multi_survivor_mismatch.py` | Seed fixture — `prompt "list"` + `expected_narrow ["forge_list_projects"]` (authored singleton; subset of observation's two-element multi-survivor arbitration) |
| `tests/corpus/test_pr15_multi_survivor_mismatch.py` | Single integration test (`test_recomposition_arc_multi_survivor_mismatch`) driving the fixture through the full decomposition seam path under multi-survivor cardinality divergence pressure |

The two-file shape mirrors PR 13's + PR 14's exactly. Both
files travel the establishment → activation lifecycle
symmetrically per the both-skeletons-at-Step-1 lifecycle
invariant (PR 13 PR-of-origin + PR 14 second instance + PR 15
third instance THREE-PR-CORROBORATED archaeological pattern).

The single test exercises:

- The PR 11 consumption pattern (`_apply_pr9_patches` +
  `drive_seed_fixture` + `_read_records` + partition-by-
  fixture-id-and-record-kind + `compare_records` + four-key
  assertions) inherited unchanged at PR 13 + PR 14 + now
  PR 15.
- The PR 14 9-step traversal annotation pattern inherited at
  PR 15's test body.
- The four-key DivergenceReport assertion contract inherited
  at PR 15.
- Multi-survivor cardinality divergence as the sole
  divergence vector at the authored expectation.

### 2.3 Gate 4 calibration role

PR 15 is the **third + final calibration point** in Gate 4's
three-PR calibration arc:

| Calibration point | PR | Divergence vector | Direction relative to PR 14 |
|---|---|---|---|
| First | PR 13 | Ordering (same set, different order) | n/a (no superset/subset relation; set-equal) |
| Second | PR 14 | Partial-set (authored 3 ⊃ observed 2) | authored ⊃ observed |
| **Third (final)** | **PR 15** | **Multi-survivor cardinality (authored 1 ⊂ observed 2)** | **authored ⊂ observed — INVERSE of PR 14** |

The three-PR calibration arc closes at PR 15. Gate 4 close
performs the three-PR cumulative Placement A + Placement B
evaluation against the three calibration points.

**The two-PR direction-symmetric pair (PR 14 + PR 15)** is
load-bearing methodology evidence: PR 14 + PR 15 exercise the
comparator's compare-as-persisted discipline under direction-
symmetric cardinality pressure (PR 14: authored ⊃ observed
with length asymmetry 3 vs. 2; PR 15: authored ⊂ observed
with length asymmetry 1 vs. 2). The comparator's direct
list-equality at `_compare.py:503` is direction-symmetric;
PR 14 + PR 15 operationally corroborate this empirically
through the two-PR pair.

### 2.4 PR 15 in the three-PR sequence

PR 15 closes the three-PR sequence per Gate 4 framing §5.5 +
§5.6 + §10. The sequence:

| # | PR | Slot | Status at PR 15 framing open |
|---|---|---|---|
| 1 | PR 13 | First (ordering) | CLOSED at `f53a469` per PR 13 close artifact |
| 2 | PR 14 | Second (partial-set) | CLOSED at `bbbeead` per PR 14 close artifact |
| **3** | **PR 15** | **Third + final (multi-survivor cardinality)** | **OPEN at this framing** |

**PR 15 close pairs at same-commit with Gate 4 close** per
Gate 4 framing §11.8 + §10 Gate 4 close section + PR 14
framing §11 inheritance + PR 14 close §7 deferral. This is
the **DIFFERENT close cadence from PR 13 + PR 14 standalone
closes**. The same-commit-pairing handles the gate-arc
synthesis responsibility cleanly: PR 15 close owns PR-15-
scoped archaeology; Gate 4 close owns gate-arc synthesis;
the two artifacts ship at one commit.

The same-commit-pairing pattern was established at Gate 2
close (`a6e42f0`) + Gate 3 close (`ee2225b`); PR 15 close +
Gate 4 close pairing is the third operational instance of the
pattern.

**Final disposition responsibilities at PR 15 close + Gate 4
close pairing:**

- PR 15 close §1: PR-15-scoped archaeology (implementation
  arc + cleanup-pressure-form outcomes + 0-prod-mod outcome
  + Placement A 3-form-ABSENCE-or-SURFACE contribution +
  Placement B precondition 1+2 manifestation + PR-12 trigger
  surface evaluation results — actual join call-site count +
  qualitative second-clause pressure observation).
- Gate 4 close: gate-arc synthesis (three-PR Placement A
  cumulative evaluation; Placement B precondition 3 final
  cumulative manifestation; §5.3 candidate methodology
  promotion evaluation against five-instance ABSENCE
  evidence if PR 15 contributes ABSENCE; six-PR architectural-
  sufficiency-signal escalation evaluation; catch-point
  migration candidate methodology prescriptive promotion
  evaluation; PR-12 final disposition; cleanup-pressure-
  resistance class promotion evaluation; gate-level
  inheritance contract toward Gate 5).

---

## 3. Architectural inheritance from Gate 4 + PR 13 + PR 14

### 3.1 17 active carriers + no new carriers at PR 15

PR 15 inherits the 17 active carriers unchanged from Gate 4
framing §3.1 + PR 13 framing §3.1 + PR 14 framing §3.1. No
candidate carrier introduced at PR 15. Citation-by-reference
discipline applies: PR 15 module docstrings cite canonical
sources (`_capture.py:6-135` + `_seed.py:19-135` + `_compare.py`
module docstring + `A.5.3.2-GATE-3-CLOSE.md §1.6`) rather
than regenerate the 17-carrier inventory verbatim.

The 17 active carriers in canonical reference form:

| # | Carrier | Canonical source |
|---|---|---|
| #1–#14 | Substrate carriers + Gate 2 binding framing clarification on call-site-owned arbitration inputs | `forge_bridge/corpus/_capture.py:6-135` |
| #15 | Authored expectation discipline + PR-8-LOCAL bindings (truth-partitioning + semantics-not-topology) | `forge_bridge/corpus/_seed.py:19-135` |
| #16 | Reliability work proves topology, not infrastructure | `A.5.3.2-GATE-3-CLOSE.md §1.6` |
| #17 | Recomposition discipline + PR-10-LOCAL read-only mutability invariant + PR 10 §4.2 binding behavioral commitment + cross-surface unbinding + proactive scope guardrail | `forge_bridge/corpus/_compare.py` module docstring + `compare_records` function docstring |

**No-new-carriers discipline:** PR 15 introduces no new
candidate carriers. The 17-carrier composition is stable
across the full Gate 4 substrate (PR 13 + PR 14 + PR 15);
Gate 4 close §x evaluates whether any Gate-4-PR's close-time
pressure encounter surfaced a new carrier candidate — at
PR 15 framing open, no such candidate is anticipated.

### 3.2 The validated comparator + recomposition arc

PR 15 consumes the comparator surface unchanged (per Gate 4
framing §5.3 + PR 13 framing §3.2 + PR 14 framing §3.2 + §5.7
binding below):

- `compare_records(observation_record, expectation_record) →
  DivergenceReport` — the interpretive-read seam PR 15
  invokes at the test body's Step 8 of traversal.
- `DivergenceReport` — the structural dict the test asserts
  against at Step 9 of traversal (four-key assertion
  contract).
- `ComparatorInputError` — the structural error class
  preserved unchanged (NOT raised by PR 15's well-formed
  inputs; preserved as PR 10 reference implementation).
- `_compare.py:503` direct list-equality — the divergence
  detection point exercised under multi-survivor cardinality
  pressure at PR 15. `obs_decision != exp_narrow` evaluates
  to True via length asymmetry (1 vs. 2) + element-
  membership asymmetry at the non-shared position.

PR 15 consumes the recomposition arc unchanged (per Gate 3
close §3 item 10 PR-11-LOCAL discipline at gate level + PR 13
framing §3.2 + PR 14 framing §3.2):

- `drive_seed_fixture(*, fixture_id, prompt, expected_narrow)`
  — the orchestration seam entry point PR 15 invokes at the
  test body's Steps 2-5 of traversal.
- `emit_seed_expectation` — internal expectation persistence
  seam exercised via `drive_seed_fixture`.
- `chat_handler` arbitration — internal observation production
  seam exercised via `drive_seed_fixture`.
- `emit_divergence_capture` — internal observation persistence
  seam exercised via `drive_seed_fixture`.
- JSONL persistence + reader (via `_read_records`) — readback
  seam exercised at the test body's Step 6.

### 3.3 The PR 9 three-fixture corpus + PR 13 + PR 14 calibration-substrate inheritance

PR 15 inherits the PR 9 three-fixture corpus + PR 13
ordering-divergence fixture + PR 14 partial-set-divergence
fixture as **predecessor archaeology**:

| Fixture | PR | Role | PR 15 inheritance |
|---|---|---|---|
| `fix_single_match.py` | PR 9 | Single-match clean-narrowing case | Architectural-substrate evidence (PR 9 substrate locked) |
| `fix_no_match.py` | PR 9 | Empty-narrowing case | Architectural-substrate evidence |
| `fix_multi_match.py` | PR 9 | Multi-match ambiguity-rejection case | **Arbitration trace inherited at `fix_multi_match.py:105-140`; `:126` observation list-literal source line** |
| `fix_ordering_divergence.py` | PR 13 | Ordering-divergence pure-isolation case | First calibration point; fixture structural shape PR 15 mirrors |
| `fix_partial_narrow_divergence.py` | PR 14 | Partial-set divergence pure-isolation case | Second calibration point; fixture structural shape PR 15 mirrors; authored-superset Direction A precedent (PR 15 inverts to authored-subset Direction A INVERSE) |

PR 15 fixture mirrors PR 13 + PR 14 fixture structural shape
at:

- Single `FIXTURE` dict symbol export per PR 9 canonical
  convention.
- Three-key FIXTURE schema (`fixture_id`, `prompt`,
  `expected_narrow`) per cleanup-pressure-resistance class
  member #4.
- `from __future__ import annotations` as the only import
  per cleanup-pressure-resistance class member #9 fixture-
  data-discipline + member #10 imports-land-when-used.
- Module docstring carrying PR-15-LOCAL binding statement +
  17 carriers cited by reference + grounded arbitration
  trace + Direction A INVERSE rationale + references.

### 3.4 Placement A operational corroboration substrate role

PR 15 contributes to Placement A operational corroboration
substrate per Gate 4 framing §6.2 + PR 13 framing §3.4 + PR 14
framing §3.4:

- PR 15 framing names three predicted cleanup-pressure forms
  per Gate 4 framing §10 PR 15 binding (§5.4 below).
- PR 15 framing names suppression mechanism per predicted
  form (Placement B precondition 2 operational manifestation
  at PR 15).
- PR 15 implementation produces ABSENCE-or-SURFACE outcome
  per predicted form (recorded at PR 15 close §1).
- Gate 4 close evaluates three-PR cumulative Placement A
  contribution (PR 13 + PR 14 contributed 3-form-ABSENCE
  each; PR 15 contributes its own predicted-form outcomes).

**Three-PR cumulative Placement A target:** if PR 13 + PR 14
+ PR 15 all contribute 3-form-ABSENCE outcomes (cumulative
nine-form-ABSENCE evidence), the §5.3 candidate methodology
observation reaches five-instance cumulative ABSENCE evidence
(PR 10 + 11 + 13 + 14 + 15). Gate 4 close performs the gate-
level promotion evaluation against five-instance evidence per
asymmetric weighting (PR 11 framing §6.4 + Gate 4 framing §6.4
+ PR 13 close §1.6 + PR 14 close §1.6).

### 3.5 Placement B methodology-stack maturation substrate role

PR 15 contributes to Placement B methodology-stack maturation
substrate per Gate 4 framing §6.3 + PR 13 framing §3.5 + PR 14
framing §3.5:

- Precondition 1 (prior pressure prediction at framing time):
  manifests at PR 15 framing §5.4 (three predicted forms
  enumerated pre-implementation).
- Precondition 2 (named suppression mechanism per predicted
  form): manifests at PR 15 framing §5.4 (each form pairs
  with named suppression mechanism).
- Precondition 3 (corroborated recurrence across multiple
  PR scopes): completes to three-PR cumulative manifestation
  at PR 15 close IF PR 15 contributes ABSENCE outcomes
  matching PR 13 + PR 14 pattern. Gate 4 close evaluates
  final-cumulative manifestation per Gate 4 framing §6.2.

### 3.6 §2.4 architectural commitment travels at PR 15

Gate 4 framing §2.4 architectural commitment travels verbatim
at PR 15 surfaces per Gate 4 framing §2.4 binding form + PR 13
framing §5.6 precedent + PR 14 framing §5.6 precedent
(deliberately stops short of fixture/test docstrings):

> **Gate 4 is the deliberate continuation of empirically
> bounded topology proof through divergence-shape robustness
> exercise.**

Operational placement (binding, parallel to PR 13 + PR 14):

- This framing artifact §2.4 + §3.6 + §5.6 (this binding
  decision).
- PR 15 spec §2 architectural commitment section.
- PR 15 Step 1, 2, 3 commit message body under "architectural
  commitment" section.
- PR 15 close artifact §1 + §6.5 (or equivalent architectural-
  commitment + architectural-sufficiency sections).

NOT in PR 15 test module docstring. NOT in PR 15 fixture
module docstring. The asymmetry vs. carrier travel (active
carriers DO travel through fixture/test docstrings per §3.1)
preserves the carrier / governing sentence / methodology-stack
category integrity Gate 4 framing established + PR 13 + PR 14
operationally verified (per PR 13 close §1.5 inventory + PR 14
close §1.5 inventory).

### 3.7 10-member promoted cleanup-pressure-resistance class

PR 15 inherits the 10-member cleanup-pressure-resistance
class unchanged from Gate 3 close §1.6 + PR 13 framing §3.7 +
PR 14 framing §3.7. The class is promoted-to-named-methodology
(per Gate 3 close §1.6). The 10 members are operative at PR 15
implementation as named protections against predictable
cleanup-pressure forms:

| Member | Discipline | Relevance at PR 15 |
|---|---|---|
| #1 | `_KNOWN_RECORD_KINDS` 2-element lock | Inherited; PR 15 fixture's `record_kind` field constrained to observation/expectation |
| #2 | `forge_bridge.__all__` 19-symbol lock | Inherited; PR 15 §5.4 form 2 suppression cites this |
| #3 | Three-authority-surface partition | Inherited; PR 15 test asserts the three surfaces (fixture_id + expectation + observation + divergence) at distinct dict paths |
| #4 | Expectation record schema three-required-keys non-revisitable | Inherited; PR 15 §5.4 form 3 suppression cites this |
| #5 | PR-INTERNAL three-way authority partition | Inherited at gate level |
| #6 | Observation record schema five-required-keys non-revisitable | Inherited |
| #7 | Truth-partitioning (PR-8-LOCAL) | Inherited at gate level |
| #8 | Semantics-not-topology (PR-8-LOCAL) | Inherited at gate level |
| #9 | Fixture-surface-data-discipline | Inherited; PR 15 fixture observes the discipline (data + `from __future__ import annotations` only) |
| #10 | Imports-land-when-used | Inherited; PR 15 test imports land at Step 2 per discipline |

**No new class members at PR 15 framing.** Gate 4 close
evaluates whether PR 15 implementation surfaces any new
candidate class member (current expectation: no new members).

### 3.8 Four-walker Layer 2 partition operational

PR 15 inherits the four-walker Layer 2 partition unchanged
from Gate 3 close §1.7 + PR 13 framing §3.8 + PR 14 framing
§3.8:

| Walker | Discipline | PR 15 disjointness |
|---|---|---|
| PR 4 walker | Production-import-topology | Target-disjoint from PR 15's two new files (corpus-subtree) |
| PR 8 walker | Orchestration-participation (5-symbol bounded toolbox) | Target-disjoint |
| PR 9 walker | Declarative-fixture-data (single-symbol-gate) | Target-disjoint; PR 15 fixture's single `FIXTURE` symbol is the partition's signature member |
| PR 10 walker | Read-only-interpretive-authority (zero-symbol-gate) | Target-disjoint |

The four-walker partition operates at PR 15 close §6.4 +
Step 3 verification per spec §6.3 inheritance from PR 13 + 
PR 14.

### 3.9 Five candidate methodologies + PR 15's contribution

PR 15 inherits the five candidate methodologies from Gate 3
close §3 + Gate 4 framing §3.6 + PR 13 framing §3.9 + PR 14
framing §3.9. PR 15's potential contributions:

| Candidate methodology | PR 15 potential contribution |
|---|---|
| §5.3 framing-time-pressure-prediction-through-absence | FIFTH instance ABSENCE if PR 15 implementation matches PR 13 + PR 14 pattern |
| Architectural-sufficiency-signal | SIXTH PR extension (PR 9 + 10 + 11 + 13 + 14 + 15) if PR 15 0-prod-mod outcome holds |
| Cleanup-pressure-resistance class | No expected new members at PR 15 framing open |
| Catch-point migration | Continuations of existing instances anticipated (catch-shape continuations per PR 14 close §1.8 three-fold continuation precedent) |
| Both-skeletons-at-Step-1 lifecycle invariant | THIRD operational corroboration if PR 15 ships multi-file (it does: fixture + test) |

PR 15 also potentially contributes to two additional patterns
introduced at PR 14:

- **PR-N-LOCAL parallel-not-regenerative pattern** (PR 14
  close §2.2 introduced; PR 15 second corroboration).
- **Direction selection rationale at framing-level direction-
  symmetric pressure** (PR 14 §5.10 Direction A precedent;
  PR 15 §5.10 Direction A INVERSE precedent — two-PR
  corroboration of the framing-level direction-decision
  pattern).

Gate 4 close evaluates all five candidate methodologies +
two PR-14-introduced patterns + the recursive-self-governance
discipline (PR 13 close §1.8 introduced; PR 14 close §1.8
three-fold operational instance) for prescriptive promotion
candidacy.

### 3.10 PR-13-LOCAL + PR-14-LOCAL as predecessor archaeology — second-corroboration inheritance

PR 15 inherits TWO predecessor PR-N-LOCAL bindings as
archaeology:

1. **PR-13-LOCAL** — PR-of-origin for pure-isolation
   discipline pattern (per PR 13 close §2.2). The PR-N-LOCAL
   non-regeneration rule applies at PR 15: PR-15-LOCAL does
   NOT regenerate PR-13-LOCAL's ordering-isolation form.
2. **PR-14-LOCAL** — parallel-not-regenerative scope-local
   discipline at second calibration point (per PR 14 close
   §1.4 + §2.2). The parallel-not-regenerative pattern PR 14
   introduced applies at PR 15: PR-15-LOCAL is parallel
   scope-local discipline appropriate to multi-survivor
   cardinality divergence, distinct from BOTH PR-13-LOCAL
   AND PR-14-LOCAL.

**PR 15 is the second corroboration of the parallel-not-
regenerative pattern.** PR 14 introduced the pattern;
PR 15 corroborates the pattern operates at the third
calibration point. The two-PR corroboration (PR 14 introduce
+ PR 15 second instance) is the cumulative architectural
archaeology Gate 4 close evaluates for prescriptive lineage-
discipline promotion candidacy.

**Inheritance summary:**

| Pattern | PR-of-origin | Second instance | Third instance |
|---|---|---|---|
| Pure-isolation discipline | PR 13 | PR 14 (parallel scope-local) | PR 15 (parallel scope-local; second corroboration of parallel-not-regenerative pattern) |
| Both-skeletons-at-Step-1 lifecycle invariant | PR 13 | PR 14 (second operational instance) | PR 15 (third operational instance; expected) |
| Parallel-not-regenerative pattern | PR 14 (introduce) | PR 15 (second corroboration) | (future-Gate-X) |
| Direction selection rationale | PR 14 §5.10 (Direction A authored ⊃ observed) | PR 15 §5.10 (Direction A INVERSE authored ⊂ observed; second corroboration) | (future-Gate-X) |
| Three-fold catch-shape continuation precedent | PR 14 §1.8 | (future-Gate-X) | (future-Gate-X) |
| Recursive-self-governance discipline | PR 13 (introduce) | PR 14 (three-fold operational instance) | PR 15 (potential second three-fold OR alternative shape) |

---

## 4. Architectural delta — what PR 15 introduces

### 4.1 One new fixture file

PR 15 ships one new fixture file at
`tests/corpus/fixtures/fix_multi_survivor_mismatch.py`.

**Fixture name discipline (per PR 9 + PR 13 + PR 14
convention):**

| Surface | PR 15 value | Convention |
|---|---|---|
| Filename | `fix_multi_survivor_mismatch.py` | snake_case; NO PR anchor in filename (consistent with PR 9 + PR 13 + PR 14) |
| `fixture_id` field | `"fix-pr15-multi-survivor-mismatch"` | kebab-case; WITH PR anchor in field (consistent with PR 9 + PR 13 + PR 14) |
| Module symbol export | `FIXTURE` | canonical name across all fixture modules |

**Fixture content (binding at framing; spec encodes at file-
level precision):**

```python
FIXTURE: dict = {
    "fixture_id": "fix-pr15-multi-survivor-mismatch",
    "prompt": "list",
    "expected_narrow": ["forge_list_projects"],
}
```

**Locked values (binding rationale):**

| Key | Value | Rationale |
|---|---|---|
| `fixture_id` | `"fix-pr15-multi-survivor-mismatch"` | Two-surface form; kebab-case with PR anchor |
| `prompt` | `"list"` | §4.6 single-variable discipline preservation across PR 9 / PR 13 / PR 14 / PR 15 |
| `expected_narrow[0]` | `"forge_list_projects"` | PR 9 multi-match observation position 0 (verbatim from `fix_multi_match.py:126`); single-element authored expectation; semantically legible authorial claim "I expected only this one tool to survive narrowing" |

**Observation record at PR 15 (deterministic per PR 9 multi-
match arbitration trace; identical to PR 13 + PR 14
observation):**

```
narrower.decision = ["forge_list_projects", "flame_list_libraries"]
```

(verbatim PR14 input order through PR21; `_PR9_REACHABLE_TOOLS`
declared ordering at `test_pr9_fixture_integration.py:208-213`.)

**Cardinality-class asymmetry IS the divergence vector:**

- Authored length: 1 (singleton).
- Observed length: 2 (multi-survivor).
- Cardinality classes: singleton vs. multi-survivor.
- Shared element: `forge_list_projects` at both authored
  position 0 + observed position 0.
- Authored element is a SUBSET of observed (authored ⊂
  observed; INVERSE of PR 14's authored ⊃ observed).

### 4.2 One new test module + one named test

PR 15 ships one new test module at
`tests/corpus/test_pr15_multi_survivor_mismatch.py` containing
exactly one named test
(`test_recomposition_arc_multi_survivor_mismatch`).

**Test signature (binding):**

```python
def test_recomposition_arc_multi_survivor_mismatch(
    clean_rate_limit_state: None,
    monkeypatch: pytest.MonkeyPatch,
    tmp_path: pathlib.Path,
) -> None:
```

- Fixtures consumed: `clean_rate_limit_state` (from
  conftest.py — chat-handler rate-limit state cleanup) +
  `monkeypatch` (pytest stdlib — for `_apply_pr9_patches`
  invocation) + `tmp_path` (pytest stdlib — for per-test
  corpus dir isolation).
- Return type: `None`.
- No decorators (no parametrize per §5.2 single-test lock).

**Test body 9-step traversal pattern** (six header comments
covering nine logical traversal steps; mirroring PR 13 +
PR 14 test body pattern verbatim except for the multi-survivor-
cardinality-specific content at Step 8 + Step 9 assertions).

**Four-key DivergenceReport assertion contract (binding per
§5.4 three predicted form suppression evidence):**

1. `assert report["fixture_id"] == FIX_MULTI_SURVIVOR_MISMATCH["fixture_id"]`
2. `assert report["expectation"]["expected_narrow"] == ["forge_list_projects"]` (length 1)
3. `assert report["observation"]["observed_narrow"] == ["forge_list_projects", "flame_list_libraries"]` (length 2)
4. `assert report["divergence"]["narrow_diverged"] is True`

**Assertion ordering rationale** (per PR 13 spec §4.2.4 + PR 14
spec §4.2.4 precedent): dependency order — fixture_id first
(joinability foundation); then authored surface (write-side
authority); then arbitration surface (write-side authority);
then interpretive surface (read-side authority).

### 4.3 Multi-survivor cardinality divergence as pure single-vector pressure

PR 15's architectural delta is the multi-survivor cardinality
divergence vector as the **sole pressure vector** under test.
The divergence vector enumerated against every other
divergence dimension:

| Dimension | PR 15 outcome | Confound status |
|---|---|---|
| Cardinality-class asymmetry | IS the divergence vector (singleton 1 vs. multi-survivor 2) | The target pressure surface |
| Ordering at shared positions | NO divergence (shared element at observation position 0) | NOT a confound |
| Partial-set within shared cardinality | NO divergence (shared cardinality is 1 at intersection; one shared element verbatim) | NOT a confound |
| Semantic-normalization | NO divergence (exact-match identifiers) | NOT a confound |
| Duplicate-handling | NO divergence (distinct elements at both lists) | NOT a confound |
| Multi-vector pressure | REJECTED at spec layer per PR-15-LOCAL §0 | Operationally suppressed |

The pure-isolation property holds at every dimension except
the target cardinality-class vector. This is the third
calibration point's evidentiary value: the comparator surface
remains the only moving interpretive layer at PR 15 (per §4.6
four-PR cumulative single-variable discipline).

### 4.4 The single-test traversal as architectural test posture

PR 15 ships exactly ONE named test (no parametrize; no
multiple-direction-coverage variants). The single-test
posture per §5.2 binding is the architectural commitment:

- ONE fixture exercises ONE divergence vector at ONE
  direction (authored-single-vs-observed-multi; authored ⊂
  observed; INVERSE of PR 14).
- The direction-symmetric pair PR 14 + PR 15 (each ONE test
  at OPPOSITE direction) is the methodology evidence; PR 15
  is NOT required to author two tests for both directions.
- The single-test traversal annotation pattern (9-step
  traversal explicitly annotated with 6-header comments)
  mirrors PR 13 + PR 14 test body verbatim.

**Why single-test:** the recomposition arc traversal is
PR 11's architectural commitment (per PR-11-LOCAL discipline
at gate level + Gate 3 close §3 item 10). Each PR's
architectural delta is the SPECIFIC divergence vector + the
SPECIFIC direction; the traversal is reused unchanged. One
test per direction per PR is the canonical form.

### 4.5 Evidence inflation rejection

PR 15 framing applies the evidence-inflation rejection
discipline introduced at PR 13 close §1.8 + operationalized
recursively at PR 14 close §1.8 (three-fold catch-shape
continuation as continuations, NOT new instances). PR 15
inherits the discipline at framing level:

- Multi-PR-direction-symmetric pair (PR 14 + PR 15) is ONE
  corroboration of the direction-symmetric pressure-vector
  pattern, NOT two independent corroborations.
- Catch-shape continuations at PR 15 (if any surface) are
  continuations of existing instances, NOT new methodology
  instances, per the recursive-self-governance discipline
  PR 13 close §1.8 introduced + PR 14 close §1.8
  operationalized.
- The cumulative architectural-sufficiency-signal escalation
  to six-PR (PR 9 + 10 + 11 + 13 + 14 + 15) is ONE
  cumulative-evidence pattern, not six independent
  corroborations.

### 4.6 The `"list"`-as-calibration-prompt archaeology — four-PR cumulative

The `"list"`-as-calibration-prompt archaeology PR 9
established + PR 13 first inherited + PR 14 second
corroborated extends to FOUR-PR cumulative at PR 15:

| PR | Authored expectation | Direction vs observation | Divergence vector |
|---|---|---|---|
| PR 9 multi-match | `["forge_list_projects", "flame_list_libraries"]` | Exact match (verbatim observation) | No divergence (baseline) |
| PR 13 ordering | `["flame_list_libraries", "forge_list_projects"]` | Same set, different order | Ordering |
| PR 14 partial-set | `["forge_list_projects", "flame_list_libraries", "forge_ping"]` | Authored ⊃ observed (+1 element at position 2) | Partial-set |
| **PR 15 multi-survivor** | **`["forge_list_projects"]`** | **Authored ⊂ observed (-1 element; singleton subset)** | **Multi-survivor cardinality** |

**Single-variable discipline across PR 9 / PR 13 / PR 14 /
PR 15** — only the authored expectation varies; the prompt,
reachable-tool set, arbitration trace, observation, and the
PR 11 consumption pattern all hold constant. The comparator
surface remains the only moving interpretive layer across the
four-PR series.

**`"list"` prompt is mature known-good substrate** — four
independent reliability PRs have inherited the prompt; the
prompt is no longer a per-PR choice but a substrate primitive
within Gate 4 reliability work. Gate 4 close §x evaluates
whether the `"list"`-as-calibration-prompt archaeology
warrants prescriptive elevation (the prompt becomes "the
canonical calibration prompt for Gate 4-style reliability
work").

The authored expectation's structural shape across the four
PRs maps directly to the four cardinality-class + element-
membership permutations of the observation:

- PR 9: same elements, same order, same cardinality (no
  divergence).
- PR 13: same elements, different order, same cardinality
  (ordering divergence).
- PR 14: superset elements (+1), same order at shared
  positions, +1 cardinality (partial-set divergence).
- PR 15: subset elements (-1), same order at shared
  positions, -1 cardinality (multi-survivor cardinality
  divergence).

The four-PR series exhaustively exercises the comparator's
direct list-equality at `_compare.py:503` under the four
canonical divergence shapes. Gate 4 close evaluates the
exhaustiveness as gate-arc archaeology.

---

## 5. Binding decisions

### 5.1 PR 15 scope locked: one fixture + one test, authored-single-vs-observed-multi multi-survivor cardinality divergence

PR 15 ships exactly ONE new fixture file (per §4.1) + ONE
new test module containing exactly ONE named test (per §4.2).
The scope is locked at framing per the single-vector +
single-direction + single-instance discipline.

**Scope rejections (binding):**

- Multi-fixture scope (e.g., authoring both directions in two
  separate fixtures) — rejected per evidence-inflation
  discipline + §5.10 binding. The direction-symmetric pair
  PR 14 + PR 15 is the methodology evidence, NOT two
  fixtures at PR 15.
- Multi-test scope (e.g., parametrize at PR 15 over both
  directions) — rejected per single-test lock + §5.2
  binding.
- Multi-vector scope (e.g., combining cardinality with
  ordering at PR 15) — rejected per PR-15-LOCAL §0 binding.

### 5.2 Test count locked at exactly 1

PR 15 ships exactly ONE named test
(`test_recomposition_arc_multi_survivor_mismatch`). No
parametrize, no decorators that multiply the test count, no
helper-generated tests. The single-test posture is the
architectural commitment per §4.4.

**Test count anchors:**

- Forge env at PR 15 close: **220 collected** (219 baseline
  at PR 14 close + 1 PR 15 new). Exact target.
- Forge-bridge env: 214 projected (213 baseline + 1 PR 15
  new); 6-test gap inherited from PR 7 (do not conflate the
  two env counts).

### 5.3 0 production source modifications

PR 15 targets **0 production source modifications** across
the full PR 15 chain (framing → spec → Steps 1, 2, 3 →
close). The architectural-sufficiency-signal target inherits
from Gate 4 framing §2.2 + PR 13 framing §5.3 + PR 14 framing
§5.3.

**Verification at PR 15 Step 3 + close:** `git diff --stat
<PR-15-framing-commit>..HEAD -- forge_bridge/` returns
EMPTY.

**Architectural-sufficiency-signal six-PR escalation target:**

| PR | Production diff |
|---|---|
| PR 9 | 0 prod mods |
| PR 10 | 1 new file (`_compare.py`), 0 mods elsewhere |
| PR 11 | 0 prod mods |
| PR 13 | 0 prod mods |
| PR 14 | 0 prod mods |
| **PR 15 (target)** | **0 prod mods** |

If PR 15 implementation surfaces a real production-source
need, the §5.11 justified-deviation protocol applies (per
PR 11 spec §5.2 + PR 13 framing §5.11 + PR 14 framing §5.11
inheritance).

### 5.4 Three predicted cleanup-pressure forms + named suppression mechanisms

PR 15 framing predicts three cleanup-pressure forms PR 15
implementation may encounter. Each prediction pairs with a
named suppression mechanism (per Placement B precondition 2
operational manifestation at PR 15). The predictions inherit
the three forms Gate 4 framing §10 PR 15 named pre-PR-15-
framing; PR 15 framing grounds each in PR-15-specific
implementation surfaces.

**Predicted form 1: Multi-survivor cardinality smoothing pressure.**

The pressure form: pressure to interpret authored-singleton-
vs-observed-multi-survivor (or inverse) as full-divergence
collapsing cardinality information. The temptation surfaces
as: "the test body could just assert `narrow_diverged=True`
and skip asserting the structural list values, since
divergence is divergence." This temptation would mask the
load-bearing cardinality-class structural shape claim — the
discipline being tested is that the comparator preserves the
cardinality-class structural shape (singleton vs. multi-
survivor) faithfully, not that divergence is reported at all.

A second variant of the pressure: pressure to add a
`cardinality_class` field to the DivergenceReport signaling
"singleton" vs. "multi-survivor" classification. This would
convert the cardinality-class divergence from a structural
property (length asymmetry at the structural list shapes) to
a declared-classification property (overlap-aware field on
the report).

**Named suppression mechanism:** PR 10 §4.2 binding behavioral
commitment ("compare as persisted") + comparator structural-
shape preservation discipline (cleanup-pressure-resistance
class member #10 extended to structural-shape surface) +
PR 10 spec §4.1.6 reference implementation lock (Gate 3 close
§3 item 2). The comparator returns sequence-and-length-
preserving `DivergenceReport`; PR 15 assertions read the
four-key shape at full structural fidelity (not via
narrow_diverged-only shortcuts). The test body's four-key
assertion contract is identical to PR 13's + PR 14's (assert
`fixture_id` + `expectation.expected_narrow` + `observation.observed_narrow`
+ `divergence.narrow_diverged` explicitly).

**Predicted form 2: Join-helper proliferation pressure (PR-12 trigger surface).**

The pressure form: pressure to extract a join helper that
absorbs the authored-vs-observed cardinality-aware filter+
partition logic. The temptation surfaces as: "the test body
filters records by `fixture_id`, then partitions by
`record_kind`, then selects observation + expectation — this
is repeated across PR 11 + PR 13 + PR 14 + now PR 15. We
should extract a helper `join_records_by_fixture_id` that
returns (observation, expectation) tuples." This would
collapse the four-PR call-site count from 4 to 1 + (4 helper
invocations); the helper would absorb the explicit
decomposition seam Gate 2 close §2.1 established.

A second variant: pressure to factor the helper at a lower
abstraction level (e.g., a `filter_records_by_fixture_id`
helper + a `partition_by_record_kind` helper). This would
preserve the seam topology but add a layer of indirection.

**This is the PR-12 trigger surface evaluation pressure.**
PR 15's actual join call-site count contribution + qualitative
second-clause pressure observation are inventoried at PR 15
close §1 per Gate 4 framing §10 PR 15 binding + §5.10
conditional reopen + §5.12 below.

**Named suppression mechanism:** PR-11-LOCAL traverses-not-
erases-seams discipline applied at gate level (Gate 3 close
§3 item 10) + PR 12 conditional disposition per Gate 4 framing
§5.10. The PR-11-LOCAL discipline keeps the filter +
partition explicit at the test body; the PR 12 conditional
disposition keeps the helper-abstraction decision deferred to
Gate 4 close evaluation. PR 15 does NOT introduce the helper
within PR 15 scope; if Gate 4 close determines PR 12
materialization is warranted, PR 12 framing authors the helper
at Gate 4 close (or via in-flight Gate 4 framing amendment
per Gate 4 framing §5.10).

**Predicted form 3: Recomposition-smoothing-through-helper pressure.**

The pressure form: pressure to factor the multi-survivor
recomposition traversal into a reusable test helper. The
temptation surfaces as: "the 9-step traversal is verbose;
extract `assert_recomposition_arc_divergence(fixture,
expected_observed, expected_authored, expected_diverged)` and
all four PRs (PR 11 + PR 13 + PR 14 + PR 15) become one-
liners." This would erase PR-11-LOCAL traverses-not-erases-
seams discipline at the most fundamental level — the
traversal IS the test's architectural commitment.

**Named suppression mechanism:** PR-11-LOCAL discipline at
gate level (Gate 3 close §3 item 10) + four-walker Layer 2
partition target-disjointness preservation (PR 4 walker
discipline at production-import-topology). The traversal is
the test's architectural-commitment-bearing structure; a
helper absorbing the traversal would invert the discipline
(seams erased instead of traversed). PR 15 does NOT introduce
the helper.

**Three "while we're here" pressure forms PR-15-LOCAL explicitly rejects:**

In addition to the three predicted forms above, PR-15-LOCAL
guards against three implementation-pressure forms that would
multi-vector PR 15's fixture/test:

- *"While we're here, also test ordering divergence at the
  multi-survivor cardinality"* — adding an ordering-vector to
  the multi-survivor fixture would convert PR 15 from pure-
  vector to multi-vector. PR 13 owns ordering pressure.
- *"While we're here, also test duplicate handling at the
  multi-survivor cardinality"* — adding a duplicate element
  to the multi-survivor fixture would introduce duplicate-
  handling-class divergence as a confound. Duplicate handling
  is not in Gate 4 scope at any of the three primary PRs.
- *"While we're here, also test semantic normalization at the
  multi-survivor cardinality"* — adding casing variants or
  substring matches to the multi-survivor fixture would
  introduce semantic-normalization pressure as a confound.
  Semantic normalization is not in Gate 4 scope.

All three "while we're here" pressures are rejected at the
spec layer per PR-15-LOCAL + §7 non-acquisition commitments
items below.

### 5.5 PR-15-LOCAL binding statement — pure-isolation discipline

Repeated verbatim from §0 (single-form discipline; one
authoritative wording):

> **PR 15 isolates multi-survivor cardinality divergence as
> the sole pressure vector. Multi-vector fixture pressure
> within PR 15 scope — combining cardinality with ordering,
> semantic-normalization, duplicate-handling, partial-set
> (within shared cardinality), or any other divergence form
> — is rejected at the spec layer. The pure-isolation
> property is what gives PR 15 its laboratory-grade
> methodology corroboration value for Placement A + Placement
> B substrate.**

Operational placement (binding):

- `test_pr15_multi_survivor_mismatch.py` module docstring.
- `fix_multi_survivor_mismatch.py` module docstring.
- All PR 15 commit message bodies under "preserved
  invariants" / "PR-15-LOCAL" sections.

**PR-13-LOCAL + PR-14-LOCAL as predecessor references:**
PR-15-LOCAL references BOTH PR-13-LOCAL (PR-of-origin for
pure-isolation pattern per PR 13 close §2.2) AND PR-14-LOCAL
(parallel-not-regenerative scope-local at second calibration
point per PR 14 close §2.2). PR-15-LOCAL is the **third
parallel scope-local discipline** + the **second corroboration
of the parallel-not-regenerative pattern PR 14 introduced**.

Cleanup pressure to soften the binding (e.g., "the inverse-
direction case isn't really multi-vector since it's the same
divergence vector with sides swapped") is rejected at the
spec layer: even direction-swap variants test the same
discipline through symmetric assertion paths and constitute
evidence inflation per §4.5 + §5.10 direction-symmetric
pressure-vector observation.

### 5.6 §2.4 architectural commitment travels verbatim at PR 15 framing/spec/close + commit bodies

Gate 4 framing §2.4's architectural commitment sentence
travels verbatim at PR 15 surfaces per Gate 4 framing §2.4
binding form + PR 13 framing §5.6 + PR 14 framing §5.6
precedent (relaxed travel discipline; deliberately stops
short of fixture/test docstrings):

> **Gate 4 is the deliberate continuation of empirically
> bounded topology proof through divergence-shape robustness
> exercise.**

Operational placement (binding, parallel to PR 13 + PR 14):

- This framing artifact §2.4 + §3.6 + §5.6 (this binding
  decision).
- PR 15 spec §2 architectural commitment section.
- PR 15 Step 1, 2, 3 commit message body under "architectural
  commitment" section.
- PR 15 close artifact §1 + §6.5 (or equivalent architectural-
  commitment + architectural-sufficiency sections).
- **GATE 4 CLOSE artifact** (same-commit-paired with PR 15
  close) — Gate 4 close also carries §2.4 architectural
  commitment at its own architectural-commitment section
  (gate-arc-synthesis surface).

NOT in PR 15 test module docstring. NOT in PR 15 fixture
module docstring. The asymmetry vs. carrier travel (active
carriers DO travel through fixture/test docstrings per §3.1)
preserves the carrier / governing sentence / methodology-stack
category integrity Gate 4 framing established + PR 13 + PR 14
operationally verified.

### 5.7 Comparator surface preserved unchanged

PR 15 does NOT modify the comparator surface (`compare_records`
+ `DivergenceReport` + `ComparatorInputError`) per Gate 4
framing §5.3 binding + §7 item 3 non-acquisition + PR 13
framing §5.7 + PR 14 framing §5.7 + Gate 3 close §3 item 2:

- No new `DivergenceReport` field (no `cardinality_class`,
  no `divergence_kind`, no `survivor_count`, no
  `singleton_side`, no `multi_survivor_side`).
- No new `compare_records` keyword argument (no
  `cardinality_aware`, no `compare_mode`, no caller-side
  configuration).
- No pre-processing helper on the comparator side.
- No change to the §4.2 binding behavioral commitment.

If PR 15 implementation surfaces a real gap in comparator
behavior that requires modification, the gap registers as
Gate-X inheritance archaeology per §5.11 deviation protocol
— NOT as in-flight production modification within PR 15.

### 5.8 `forge_bridge.__all__` stays at 19 symbols

PR 15 does NOT modify `forge_bridge.__all__` per Gate 4
framing §5.4 + §7 item 12 non-acquisition + PR 13 framing
§5.8 + PR 14 framing §5.8 + Gate 2 close §2.4 item 2. The
19-symbol public API preserves.

PR 15 fixture + test consume the comparator + recomposition
arc through corpus-internal import paths (`from
forge_bridge.corpus._compare import compare_records` + `from
forge_bridge.corpus._seed import drive_seed_fixture` +
underscored-private PR 9 imports as test-internal archaeology
surfaces).

### 5.9 PR 9 fixture naming convention preserved (two-surface form)

PR 15 fixture preserves the PR 9 + PR 13 + PR 14 fixture
naming convention at both naming surfaces:

| Surface | PR 15 value | PR 9 + PR 13 + PR 14 convention preserved |
|---|---|---|
| Filename | `fix_multi_survivor_mismatch.py` | snake_case; NO PR anchor in filename |
| `fixture_id` field | `"fix-pr15-multi-survivor-mismatch"` | kebab-case; WITH PR anchor in field |
| Module symbol export | `FIXTURE` | canonical name across all fixture modules |

Alternative naming patterns at any surface are rejected at the
spec layer per §3.3 archaeology preservation discipline.

### 5.10 Authored-single-vs-observed-multi direction (INVERSE of PR 14) as affirmative architectural decision

PR 15 selects the **authored-single-vs-observed-multi
direction** (authored singleton ⊂ observed multi-survivor) as
an **affirmative architectural decision**, not as a tolerated
inversion of Gate 4 framing §10's primary phrasing.

**The pressure vector under test is cardinality-class
preservation, not directional ownership of the cardinality-
class asymmetry.** The temptation toward cardinality-aware
helper fields on the DivergenceReport (predicted form 1 at
§5.4 above) operates symmetrically regardless of which side
contains the multi-survivor cardinality:

| Direction | Authored | Observed | Cleanup pressure |
|---|---|---|---|
| **A (this PR; INVERSE of PR 14)** | `["forge_list_projects"]` (length 1) | `["forge_list_projects", "flame_list_libraries"]` (length 2) | "Maybe we should add `cardinality_class: 'singleton'` to expectation surface to signal authored ⊂ observed." |
| B (rejected; SAME direction as PR 14) | `["forge_list_projects", "flame_list_libraries"]` (length 2) | `["forge_list_projects"]` (length 1) | "Maybe we should add `cardinality_class: 'multi-survivor'` to expectation surface to signal authored ⊃ observed." |

Both directions test the same architectural commitment (the
comparator preserves the cardinality-class structural shape
faithfully without normalization) and produce the same
cleanup-pressure vector (cardinality-aware field on the
DivergenceReport). Selecting Direction A is the **stronger
architectural move** for three reasons:

1. **Direction-symmetric corroboration with PR 14** — PR 14
   exercised authored ⊃ observed (superset relation; partial-
   set divergence). Direction A at PR 15 inverts to authored
   ⊂ observed (subset relation; multi-survivor cardinality
   divergence). The two-PR pair (PR 14 + PR 15) operationally
   corroborates the comparator's compare-as-persisted
   discipline operates direction-symmetrically. Direction B
   at PR 15 would repeat PR 14's direction (authored ⊃
   observed) and miss the direction-symmetric corroboration
   evidence.

2. **Single-variable discipline preservation across PR 9 /
   PR 13 / PR 14 / PR 15** (per §4.6). Direction A reuses
   PR 9 multi-match's deterministic arbitration output
   verbatim — same prompt, same reachable-tool set, same
   observation. The comparator surface becomes the only
   moving interpretive layer across the four-PR series.
   Direction B would require either a different observation
   (need an arbitration trace that produces only one
   survivor) OR a different prompt + reachable-tool set;
   either choice introduces new arbitration topology + new
   prompt semantics as additional moving axes —
   simultaneously testing multi-survivor cardinality
   divergence and second-order arbitration variation.

3. **Semantically legible authorial claim.** Direction A's
   authored expectation is `["forge_list_projects"]` (single
   element matching observation position 0 verbatim). The
   author is asserting "I expected only this one tool to
   survive narrowing; arbitration's ambiguity is unexpected."
   This is a visibly interpretive cardinality-class
   prediction — the author predicted clean narrowing but got
   multi-survivor ambiguity. Direction B's authored
   expectation would assert "I expected multi-survivor
   ambiguity, but arbitration collapsed to clean narrowing"
   — also legible, but inverts the more typical authorial
   prediction (the author usually predicts clean outcomes,
   not ambiguity outcomes).

The architectural-pressure-vector is direction-symmetric per
the table above. Selecting Direction A is the affirmative
move that maximally exploits the direction-symmetric-
corroboration-with-PR-14 + single-variable discipline +
semantic-legibility advantages. The framing-time selection
rationale is preserved at PR 15 spec §0 + close §1 for
archaeology.

**This is the SECOND corroboration of the framing-level
direction-selection rationale pattern** PR 14 §5.10
introduced. PR 14 introduced the three-reason argumentation
pattern (substrate reuse + single-variable discipline +
semantic legibility); PR 15 §5.10 corroborates the pattern
operates at the third calibration point with adapted reasons
(direction-symmetric corroboration WITH PR 14 + single-
variable discipline + semantic legibility). Gate 4 close
evaluates the two-PR-corroborated pattern for prescriptive
direction-symmetric-pressure-decision pattern promotion
candidacy.

**Cleanup pressure to revisit the direction selection at spec
or implementation phase is rejected.** The direction is bound
at framing per the affirmative architectural decision; any
direction-revisit pressure registers as evidence-inflation-
through-direction-symmetry-testing (per §5.5 binding above).

### 5.11 Justified-deviation protocol if 0-prod-mod target falsifies

If PR 15 implementation surfaces a real production-source
need, the justified-deviation protocol applies per PR 11
spec §5.2 + PR 13 framing §5.11 + PR 14 framing §5.11
inheritance:

1. Pause implementation at the moment of falsification.
2. Surface the deviation at framing-level evaluation (this
   framing artifact's evaluation surface OR Gate 4 framing
   amendment surface).
3. If real architectural gap: ship the production-source
   modification as a Step N.5 surgical commit BEFORE Step 3
   verification.
4. PR 15 close §5 records the deviation as Gate-X inheritance
   archaeology.

The Step N.5 surgical cadence is goal-oriented availability,
not constraint. PR 15's expected outcome is **zero Step N.5
commits** (per the five-PR cumulative escalation evidence at
PR 14 close §1.3 — five-PR zero-Step-N.5 corroboration).

### 5.12 PR-12 trigger surface evaluation responsibility encoded

PR 15 framing encodes the responsibility to evaluate the
PR-12 trigger surface per Gate 4 framing §10 PR 15 binding +
§5.10 conditional reopen. The responsibility:

- **PR 15 close §1 inventories actual join call-site count
  contribution** — counts the number of explicit join sites
  (filter-by-fixture_id + partition-by-record_kind +
  observation/expectation selection) at the PR 15 test body.
  Expected count: 1 join site (the standard pattern PR 11 +
  PR 13 + PR 14 inherited unchanged).
- **PR 15 close §1 inventories qualitative second-clause
  pressure observation** — records whether "preserving
  decomposition becomes harder than abstracting" surfaced as
  qualitative pressure during PR 15 implementation. Expected
  outcome: NO qualitative pressure (per PR 11 + PR 13 + PR 14
  cumulative ABSENCE evidence).
- **Cumulative call-site count at Gate 4 close** — Gate 4
  close aggregates the PR 13 + PR 14 + PR 15 call-site
  contributions (each ~1 site per PR) + PR 11's 3 sites for
  cumulative count. Projected: 3 (PR 11) + 1 (PR 13) + 1
  (PR 14) + 1 (PR 15) = 6 sites. Threshold ≥4 satisfied
  numerically.
- **Final PR-12 disposition decision** — Gate 4 close
  evaluates first clause (numerical threshold) AND second
  clause (qualitative pressure observation) per Gate 4
  framing §5.10 three disposition options (promotion /
  deferral preserved / rejection).

**PR 15 close does NOT make the final PR-12 disposition
decision.** PR 15 close §1 ships the evaluation inputs (count
+ qualitative observation); Gate 4 close ships the final
disposition. This responsibility split mirrors PR 11 close
§1.9 + PR 13 close §5.2 + PR 14 close §5.2 deferral pattern
applied to the specific PR-12 trigger surface evaluation.

**If PR 15 close §1 records SURFACE of the qualitative
second-clause pressure** (preserving decomposition becomes
harder than abstracting surfaces during implementation), this
materially shifts the Gate 4 close PR-12 disposition toward
promotion. The framing-time expectation is ABSENCE (matching
PR 11 + PR 13 + PR 14 cumulative pattern); the evaluation
remains open through PR 15 implementation.

---

## 6. Placement A + Placement B substrate contribution

### 6.1 Placement A operational corroboration substrate

PR 15 contributes to Placement A operational corroboration
substrate per Gate 4 framing §6.2 + PR 13 framing §6.1 +
PR 14 framing §6.1:

**Predicted-form-outcome contribution:** PR 15 implementation
produces three predicted cleanup-pressure form outcomes per
§5.4 (ABSENCE or SURFACE per form). Recorded at PR 15 close
§1 + structurally inventoried at Gate 4 close §3 + §x.

**Three-PR cumulative Placement A evaluation:**

| PR | Predicted forms (count) | Expected outcome | Actual outcome at close |
|---|---|---|---|
| PR 13 | 3 forms | 3-form-ABSENCE | 3-form-ABSENCE (verified at PR 13 close §1.6) |
| PR 14 | 3 forms | 3-form-ABSENCE | 3-form-ABSENCE (verified at PR 14 close §1.6) |
| **PR 15** | **3 forms** | **3-form-ABSENCE (expected)** | **TBD at PR 15 close §1** |

If PR 15 contributes 3-form-ABSENCE, the cumulative nine-
form-ABSENCE evidence across Gate 4 substrate corroborates
§5.3 candidate methodology observation at five-instance
cumulative ABSENCE evidence (PR 10 + 11 + 13 + 14 + 15). Gate
4 close performs the gate-level promotion evaluation against
five-instance evidence.

### 6.2 Placement B methodology-stack maturation substrate

PR 15 contributes to Placement B methodology-stack maturation
substrate per Gate 4 framing §6.3 + PR 13 framing §6.2 +
PR 14 framing §6.2:

**Precondition manifestation at PR 15:**

| Precondition | Manifest at PR 15? | Evidence |
|---|---|---|
| 1 — Prior pressure prediction at framing time | YES (this framing §5.4) | Three predicted forms enumerated pre-implementation per Gate 4 framing §10 PR 15 binding inheritance |
| 2 — Named suppression mechanism per predicted form | YES (this framing §5.4) | Each form pairs with named suppression mechanism grounded in PR 10 §4.2 binding + PR-11-LOCAL discipline at gate level + cleanup-pressure-resistance class member references |
| 3 — Corroborated recurrence across multiple PR scopes | **Completes to THREE-PR cumulative manifestation at PR 15 close** | PR 13 + PR 14 manifest preconditions 1+2; PR 15 manifests 1+2; three-PR cumulative manifestation evaluation lives at Gate 4 close §3 |

PR 15 is the **third + final PR contributing to Placement B
substrate**; the three-PR cumulative manifestation evaluation
closes at Gate 4 close.

### 6.3 Two-placement substrate independence

The Placement A + Placement B substrates remain operationally
independent at PR 15 per Gate 4 framing §6.4 + PR 13 framing
§6.3 + PR 14 framing §6.3:

- Placement A: operational corroboration of §5.3 candidate
  methodology observation (framing-time-pressure-prediction-
  through-absence) at the methodology-observation layer.
- Placement B: methodology-stack maturation toward seed
  reliability phase methodology V1.6+ at the methodology-
  stack layer.

The two layers, two jobs, two-Placement structure preserves
at PR 15. Gate 4 close performs cumulative evaluation of both
Placements at gate scope.

### 6.4 Evidence inflation rejection as PR 15 framing discipline

PR 15 framing inherits the evidence-inflation rejection
discipline introduced at PR 13 close §1.8 + operationalized
recursively at PR 14 close §1.8. PR 15 framing applies the
discipline at framing level:

- The two-PR direction-symmetric pair (PR 14 + PR 15) is ONE
  corroboration of the direction-symmetric pressure-vector
  pattern, NOT two independent corroborations.
- Catch-shape continuations at PR 15 (if any surface) are
  continuations of existing instances, NOT new methodology
  instances.
- The cumulative architectural-sufficiency-signal escalation
  to six-PR is ONE cumulative-evidence pattern, not six
  independent corroborations.

Gate 4 close inherits the evidence-inflation rejection
discipline at gate scope for the final methodology-promotion
evaluation.

---

## 7. Non-acquisition commitments

PR 15 framing honors Gate 4 framing's 22 non-acquisition
commitments at gate level + PR 13 framing's PR-13-specific
commitments + PR 14 framing's PR-14-specific commitments +
authors PR-15-specific commitments:

1. **No new carriers at PR 15** (17 active carriers preserve).
2. **No new `record_kind` values** (PR 7 + PR 8 + PR 13 +
   PR 14 inheritance; 2-element observation/expectation lock).
3. **No comparator surface modification** (per §5.7).
4. **No `forge_bridge.__all__` modification** (per §5.8).
5. **No new fixture corpus convention** (PR 9 two-surface
   form preserved per §5.9).
6. **No multi-vector fixture pressure** (per PR-15-LOCAL §0
   + §5.5).
7. **No multi-fixture-per-PR scope** (per §5.1).
8. **No multi-test-per-PR scope** (per §5.2).
9. **No parametrize at the single test** (per §5.2).
10. **No production source modification** (per §5.3 +
    §5.11 deviation protocol if needed).
11. **No declared-intent FIXTURE schema extension** (per §5.4
    form 3 suppression; three-key schema lock).
12. **No declared-classification DivergenceReport field
    extension** (per §5.4 form 1 suppression + §5.7).
13. **No join-helper extraction at PR 15** (per §5.4 form 2
    suppression; PR 12 disposition deferred to Gate 4 close).
14. **No recomposition-arc helper extraction at PR 15** (per
    §5.4 form 3 suppression).
15. **No PR-N-LOCAL regeneration** (PR-15-LOCAL is non-
    regenerating per §0 + §5.5).
16. **No Gate-4-LOCAL governing sentence authoring** (per
    §0).
17. **No Direction B (authored ⊃ observed) authoring** (per
    §5.10; Direction A INVERSE is affirmative architectural
    decision).
18. **No fixture authored expectation regeneration of PR 14's
    direction** (PR 15 inverts the direction; preserves the
    direction-symmetric corroboration evidence).
19. **No premature PR-12 disposition finalization** (per
    §5.12; PR 15 close ships evaluation inputs; Gate 4 close
    ships final disposition).
20. **No same-commit-pairing of PR 15 close with PR 13 close
    or PR 14 close** (PR 13 + PR 14 closes shipped STANDALONE;
    PR 15 close pairs at same-commit with Gate 4 close per
    §2.4 + Gate 4 framing §11.8).
21. **No `clean_rate_limit_state` fixture modification** (PR
    15 test consumes the conftest.py fixture unchanged).
22. **No `_apply_pr9_patches` or `_read_records` public-API
    promotion** (per §1.13 test-internal archaeology surfaces
    inheritance from PR 13 + PR 14).
23. **No silent re-ordering of three-PR sequence at PR 15**
    (PR 13 → PR 14 → PR 15 ordering locked at Gate 4 framing
    §5.6).
24. **No silent scope expansion to combined-divergence
    fixtures** (per PR-15-LOCAL §0 + §5.5).

---

## 8. Layer 1 / Layer 2 / Layer 3 implications

### 8.1 Layer 1 — `_ALLOWLIST` unchanged

PR 15 does NOT modify `_ALLOWLIST` per Gate 4 framing §8.1 +
PR 13 framing §8.1 + PR 14 framing §8.1. PR 15's two new
files are corpus-subtree placements (auto-excluded by PR 3
discipline) + are target-disjoint from `_ALLOWLIST`'s
production-import-topology partition.

### 8.2 Layer 2 — four-walker partition unchanged

PR 15 inherits the four-walker Layer 2 partition unchanged
from Gate 3 close §1.7 + PR 13 framing §8.2 + PR 14 framing
§8.2:

| Walker | Discipline | PR 15 disjointness |
|---|---|---|
| PR 4 walker | Production-import-topology | Target-disjoint (no production imports at PR 15) |
| PR 8 walker | Orchestration-participation (5-symbol bounded toolbox) | Target-disjoint |
| PR 9 walker | Declarative-fixture-data (single-symbol-gate) | Target-disjoint (PR 15 fixture's single `FIXTURE` symbol is the partition's signature member) |
| PR 10 walker | Read-only-interpretive-authority (zero-symbol-gate) | Target-disjoint |

### 8.3 Layer 3 — unchanged

PR 15 does NOT modify Layer 3 (PR 6 lint regression discipline)
per Gate 4 framing §8.3 + PR 13 framing §8.3 + PR 14 framing
§8.3. PR 15 introduces ZERO new `emit_divergence_capture`
call sites; the lint's discovery walk input set is unchanged.

---

## 9. Phase-end conditions for PR 15

### 9.1 Test count anchor — 220 forge env collected

PR 15 close test count arithmetic:

```
219 baseline (PR 14 close §1.9 forge env collected)
+ 1 PR 15 multi-survivor cardinality recomposition arc test (Step 2)
= 220 forge env collected at PR 15 close
```

Verification at Step 3 + close:

```
$ python -m pytest tests/corpus/ --collect-only -q | tail -1
220 tests collected in 0.05s
```

PR 15 ships **1 named test**; named == collected.

### 9.2 PR 15 suite regression — 1/1 passed

```
$ python -m pytest tests/corpus/test_pr15_multi_survivor_mismatch.py
1 passed in 0.0Xs
```

PR 15 suite: 1/1.

### 9.3 0-prod-mod outcome verified

```
$ git diff --stat <PR-15-framing-commit>..HEAD -- forge_bridge/
(empty)
```

Zero production source modifications across all PR 15 commits
(framing + spec + Step 1 + Step 2 + Step 3 + close-with-Gate-
4-close pairing).

### 9.4 Predicted cleanup-pressure form outcomes recorded

PR 15 close §1 records ABSENCE or SURFACE outcome per the
three predicted forms enumerated at §5.4:

- Multi-survivor cardinality smoothing pressure: ABSENCE / SURFACE.
- Join-helper proliferation pressure (PR-12 trigger surface): ABSENCE / SURFACE + actual join call-site count + qualitative second-clause pressure observation.
- Recomposition-smoothing-through-helper pressure: ABSENCE / SURFACE.

Expected outcome: 3-form-ABSENCE (mirroring PR 13 + PR 14
pattern).

### 9.5 Placement B precondition operational manifestation recorded

PR 15 close §1 records Placement B precondition manifestation:

- Precondition 1 (prior pressure prediction at framing time):
  MANIFEST.
- Precondition 2 (named suppression mechanism per predicted
  form): MANIFEST.
- Precondition 3 (corroborated recurrence across multiple PR
  scopes): completes to THREE-PR cumulative manifestation.

### 9.6 Module docstring carrier travel verified

- PR-15-LOCAL at `fix_multi_survivor_mismatch.py` module
  docstring + `test_pr15_multi_survivor_mismatch.py` module
  docstring (verified at Step 1; preserved through Steps 2-3).
- 17 inherited carriers cited by reference at both module
  docstrings.
- Traversal trace verbatim from §2.1 at PR 15 test module
  docstring.
- Test-internal-archaeology-surfaces framing at PR 15 test
  module docstring.

### 9.7 §2.4 architectural commitment travel verified

§2.4 Gate 4 architectural commitment verbatim at PR 15 spec
§0 + §1 + §2 + Step 1, 2, 3 commit message bodies + PR 15
close §1.5 + §6.5 + Gate 4 close architectural-commitment
section = 9 cumulative travel surfaces (extended by 1 vs.
PR 13 + PR 14 close-time 8-surface inventory; the +1 surface
is Gate 4 close's architectural-commitment section, accessible
via same-commit-pairing).

### 9.8 Public API anchor — `forge_bridge.__all__` at 19 symbols

```
$ python -c "import forge_bridge; print(len(forge_bridge.__all__))"
19
```

### 9.9 Imports-land-when-used discipline verified (member #10)

Both PR 15 files verify the imports-land-when-used discipline
symmetrically:

| File | Step 1 imports | Step 2 imports landed (when first used) | Step 3 imports |
|---|---|---|---|
| `fix_multi_survivor_mismatch.py` | `from __future__ import annotations` only | NO additional imports (FIXTURE dict only requires literal syntax) | No changes |
| `test_pr15_multi_survivor_mismatch.py` | `from __future__ import annotations` only | `pathlib`, `pytest`, `compare_records`, `drive_seed_fixture`, `FIX_MULTI_SURVIVOR_MISMATCH`, `_apply_pr9_patches`, `_read_records` (each landed when first used at test body) | No changes |

### 9.10 Cleanup-pressure-resistance class additions registered

PR 15 close §1 records cleanup-pressure-resistance class
member additions surfaced at PR 15 implementation. Expected
outcome: NO new members (10-member inventory unchanged across
the full PR 15 chain).

### 9.11 Test-internal archaeology surfaces inheritance verified

`_apply_pr9_patches` and `_read_records` imported from
`tests.corpus.test_pr9_fixture_integration` as test-internal
archaeology surfaces (NOT public APIs) per spec §4.2.1 site 9
+ PR 13 spec §4.2.1 site 9 + PR 14 spec §4.2.1 site 9
inheritance. Third operational corroboration of the
underscored-private-status discipline.

### 9.12 Step archaeology summary

PR 15 ships exactly 4 commits in the post-spec implementation
arc (matching PR 11 + PR 13 + PR 14 cleanest-arc pattern;
spec is predecessor context):

1. **Spec** — file-level precision derived from framing's
   binding decisions. Spec drafting expected outcome: ZERO
   amendments at spec drafting (matching PR 11 + PR 13 + PR 14
   clean-arc pattern).
2. **Step 1 — both skeletons bundled** — both files at
   structurally-symmetric skeleton state per the both-
   skeletons-at-Step-1 lifecycle invariant (PR 13 PR-of-origin
   + PR 14 second instance + PR 15 third instance; THREE-PR-
   CORROBORATED archaeological pattern).
3. **Step 2 — architectural-center bundled** — test body +
   FIXTURE dict landed in one commit. Three-round review per
   Gate 2 framing §5.7 integration-work elevation.
4. **Step 3 — final verification (empty commit; archaeology
   in body)** — 10-item verification checklist + predicted-
   form outcomes + Placement A + B contributions + catch-
   point migration outcomes + commit chain summary +
   **PR-12 trigger surface evaluation inputs (join call-site
   count + qualitative second-clause observation)**.

**PR 15 close pairs at same-commit with Gate 4 close** per
§2.4 + Gate 4 framing §11.8. The close-cadence asymmetry
(PR 13 + PR 14 standalone vs. PR 15 same-commit-paired) is
the structural seam Gate 4 framing §11.8 introduced. PR 15
close artifact + Gate 4 close artifact ship at one commit.

**Step N.5 surgical cadence available if needed** per §5.11
deviation protocol. Expected outcome: zero Step N.5 commits
(matching PR 11 + PR 13 + PR 14 clean-arc pattern).

---

## 10. Cross-references

- **`A.5.3.2-FRAMING.md`** — phase shape; objective lock at
  Gate 4 substrate.
- **`A.5.3.2-INSTRUMENT-CONTRACT.md`** — instrument shape;
  six interlocking structural-invariant pairs.
- **`A.5.3.2-GATE-1-SPEC.md`** — Gate 1 sequencing;
  architecturally-prohibited patterns.
- **`A.5.3.2-GATE-2-FRAMING.md`** (`ceac9b5`) — Gate 2
  three-authority-surface partition + carrier #14 + binding
  framing clarification on call-site-owned arbitration
  inputs.
- **`A.5.3.2-PR7-CLOSE.md`** (`b035c87`) — observation +
  dispatch-provenance surfaces; carrier #14;
  `_KNOWN_RECORD_KINDS` 2-element lock.
- **`A.5.3.2-PR8-CLOSE.md`** (`b102010`) — authored
  expectation surface; PR-INTERNAL three-way authority
  partition; carrier #15.
- **`A.5.3.2-PR9-CLOSE.md`** (`a6e42f0`) — three-fixture
  corpus; PR 9 fixture naming convention PR 15 inherits;
  arbitration trace at `fix_multi_match.py:105-140`; `:126`
  observation list-literal source; PR 9 test infrastructure
  PR 15 imports as test-internal archaeology surfaces.
- **`A.5.3.2-GATE-2-CLOSE.md`** (`a6e42f0`) — Gate 2 gate-
  arc synthesis; four §7.3 ontological questions PR 15
  inherits via Gate 4 framing §5.2.
- **`A.5.3.2-GATE-3-FRAMING.md`** (`2f70cbf`) — Path B locked
  precedent; binding framing clarification on cross-surface
  unbinding.
- **`A.5.3.2-PR10-CLOSE.md`** (`cf2b7ee`) — comparator
  surface operational; PR 10 §4.2 binding behavioral
  commitment ("compare as persisted") PR 15 exercises under
  multi-survivor cardinality divergence pressure.
- **`A.5.3.2-PR11-CLOSE.md`** (`ee2225b`) — recomposition
  arc operational; PR-11-LOCAL discipline at gate level;
  PR 15 inherits the consumption pattern unchanged; §1.9
  PR-12 trigger surface PR 11 contribution (3 call sites).
- **`A.5.3.2-GATE-3-CLOSE.md`** (`ee2225b`) — gate-arc
  synthesis; carrier #16 promotion; cleanup-pressure-
  resistance class promotion to named methodology (10
  members); 17 active carriers; four-walker Layer 2
  partition operational; conditional PR 12 DEFER (PR 15
  reopens evaluation per Gate 4 framing §5.10 + §10 PR 15
  binding).
- **`A.5.3.2-GATE-4-FRAMING.md`** (`fbf2285`) — gate-level
  inheritance contract; §2.4 architectural commitment; §5
  binding decisions (including §5.10 conditional PR 12
  reopened-not-prebound); §10 PR 15 sub-section (this
  framing's primary source); §11.8 same-commit Gate 4 close
  pattern (PR 15 close + Gate 4 close pairing).
- **`A.5.3.2-PR13-FRAMING.md`** (`8f429b2`) — first-of-three
  primary PR framing precedent; 11-section framing shape
  inherited at PR 15.
- **`A.5.3.2-PR13-SPEC.md`** (`2e05149`) — first-of-three
  primary PR spec precedent; 11-section spec shape inherited
  at PR 15.
- **`A.5.3.2-PR13-CLOSE.md`** (`f53a469`) — first calibration
  point predecessor; PR-of-origin for pure-isolation pattern
  (§2.2); PR-of-origin for both-skeletons-at-Step-1
  lifecycle invariant (§2.4); 3-form-ABSENCE Placement A
  contribution precedent.
- **`A.5.3.2-PR14-FRAMING.md`** (`30412a3`) — second-of-three
  primary PR framing precedent; Direction A authored-superset
  rationale precedent (§5.10 three-reason argumentation
  pattern; PR 15 §5.10 inverts to authored-subset Direction
  A INVERSE as SECOND corroboration); `"list"`-as-
  calibration-prompt archaeology (§4.6); both-skeletons-at-
  Step-1 second operational instance.
- **`A.5.3.2-PR14-SPEC.md`** (`23c358a`) — second-of-three
  primary PR spec precedent; 11-section / ~30-subsection
  spec shape precedent at PR 15.
- **`A.5.3.2-PR14-CLOSE.md`** (`bbbeead`) — immediate
  predecessor; second calibration substrate; parallel-not-
  regenerative pattern introduction (§2.2; PR 15 second
  corroboration); 3-form-ABSENCE SECOND Gate 4 PR instance
  (PR 15 expected to contribute THIRD instance); three-fold
  catch-shape continuation precedent (§1.8; PR 15 may
  contribute additional continuations or alternative shape);
  five-PR architectural-sufficiency escalation evidence
  (PR 15 may extend to six-PR).
- **`forge_bridge/corpus/_compare.py::compare_records`** —
  PR 10 interpretive-read surface PR 15 consumes;
  `_compare.py:503` direct list-equality detects multi-
  survivor cardinality divergence as `narrow_diverged=True`
  via length asymmetry (1 vs. 2).
- **`forge_bridge/corpus/_seed.py::drive_seed_fixture`** —
  PR 8 orchestration entry point PR 15 invokes per fixture.
- **`forge_bridge/console/handlers.py::chat_handler`** —
  production arbitration surface PR 15 drives via the PR 9
  patched `_invoke_chat_handler_in_process`.
- **`tests/corpus/fixtures/fix_multi_match.py:105-140`** —
  PR 9 multi-match arbitration trace PR 15's fixture
  inherits.
- **`tests/corpus/fixtures/fix_multi_match.py:126`** — PR 9
  observation list-literal source line PR 15's
  `expected_narrow[0]` (`forge_list_projects`) grounds
  against.
- **`tests/corpus/test_pr9_fixture_integration.py:208-213`**
  — `_PR9_REACHABLE_TOOLS` declared order PR 15's fixture
  arbitration trace grounds against.
- **`tests/corpus/fixtures/fix_ordering_divergence.py`** (PR
  13, 149 lines) — first calibration point fixture; PR 15
  mirrors structural shape.
- **`tests/corpus/test_pr13_ordering_divergence.py`** (PR 13,
  224 lines) — first calibration point test; PR 15 mirrors
  9-step traversal annotation + four-key assertion contract.
- **`tests/corpus/fixtures/fix_partial_narrow_divergence.py`**
  (PR 14, 208 lines) — second calibration point fixture;
  PR 15 mirrors structural shape (authored expectation
  varies: PR 14 superset vs. PR 15 subset).
- **`tests/corpus/test_pr14_partial_narrow_divergence.py`**
  (PR 14, 252 lines) — second calibration point test;
  PR 15 mirrors 9-step traversal annotation + four-key
  assertion contract.
- **Memory cursors operative at PR 15 framing time:**
  - [[project-state-2026-05-12-pr-14-closed]] — immediate
    predecessor cursor; PR 14 durably closed at `bbbeead`;
    PR-14-introduced patterns (parallel-not-regenerative +
    Direction A rationale) PR 15 inherits as predecessor
    archaeology.
  - [[feedback_cursor_before_retrospective_synthesis]] — PR
    15 framing-landed cursor write at framing landing
    boundary per pattern.
  - [[feedback_ground_specs_in_actual_files]] — operate at
    PR 15 spec drafting + implementation time.
  - [[feedback_counts_are_archaeology_grade]] — recount
    before stating at every PR 15 count claim.
  - [[feedback_writers_room_lead_with_views]] — applies at
    PR 15 framing structural seams (direction selection +
    PR-12 trigger surface evaluation responsibility +
    same-commit-pairing cadence).
- **`SEED-RELIABILITY-PHASE-METHODOLOGY-V1.6+.md`** —
  promotion-candidate methodology seed; PR 15 contributes
  (Gate 4 close evaluates):
  - Potential five-instance ABSENCE corroboration of §5.3
    candidate methodology observation (PR 10 + 11 + 13 + 14
    + 15).
  - Potential six-PR architectural-sufficiency-signal
    escalation (PR 9 + 10 + 11 + 13 + 14 + 15).
  - Both-skeletons-at-Step-1 lifecycle invariant THIRD
    operational corroboration (PR 13 PR-of-origin + PR 14
    second + PR 15 third instance; THREE-PR-CORROBORATED
    archaeological pattern).
  - PR-N-LOCAL parallel-not-regenerative pattern SECOND
    corroboration (PR 14 introduce + PR 15 second instance).
  - Direction selection rationale at framing-level
    direction-symmetric pressure SECOND corroboration (PR 14
    Direction A introduce + PR 15 Direction A INVERSE second
    instance; two-PR-corroborated framing-decision pattern).
  - PR-12 trigger surface evaluation inputs (final
    disposition at Gate 4 close).
