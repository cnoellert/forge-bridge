---
phase: 11-cli-companion
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .planning/phases/11-cli-companion/11-UAT.md
autonomous: false
requirements:
  - CLI-04
requirements_addressed:
  - CLI-04
threat_model: []

must_haves:
  truths:
    - "Developer (CN/dev) acting as their own test subject runs forge-bridge console tools, execs --since 24h, manifest, health, and doctor against a live :9996 server"
    - "Developer records a single PASS/FAIL + note line in 11-UAT.md per D-08"
    - "Rich rendering on a real TTY is visually verified — amber header, SQUARE box, 8-char hash, Created ▼ glyph legible (manual verification per VALIDATION.md `Manual-Only Verifications` table)"
    - "If FAIL: a follow-up plan is queued inside Phase 11 OR a Phase 11.1 micro-pass is recommended — phase does NOT re-open"
  artifacts:
    - path: ".planning/phases/11-cli-companion/11-UAT.md"
      provides: "Single-line soft self-UAT record"
      contains: "PASS"
  key_links:
    - from: ".planning/phases/11-cli-companion/11-UAT.md"
      to: "D-08 soft self-administered gate"
      via: "Subjective 'can I decipher the output without re-reading the source?' criterion"
      pattern: "D-08"
---

<objective>
Close Phase 11 with the soft self-administered dogfood UAT specified by CONTEXT.md D-08. The developer (CN/dev) runs the five `forge-bridge console <subcommand>` invocations against a live `:9996` server, applies the criterion "can I decipher the output without re-reading the source?", and records a single `PASS / FAIL + note` line in `11-UAT.md`.

Per D-08 (verbatim): no fresh-operator requirement (Phase 10.1 D-44 does NOT apply); no 30-second timer; no "ships back to planning" clause — failure produces a follow-up plan inside Phase 11 or a Phase 11.1 micro-pass.

This plan has ONE checkpoint task. Execution pauses for the developer to run the commands and write the verdict. Plan 03 is non-autonomous by design.

Purpose: Apply Phase 10's hard-won "dogfood-or-die" discipline at a softer gate that respects the actual CLI user population (SSH operators, CI scripts, scripted pipelines — assumed more technical than artist Web UI users). This closes the dogfood gap that Phase 10 retrospectively identified as essential for catching the artist-UX failure class that unit tests miss.

Output: 1 file — `.planning/phases/11-cli-companion/11-UAT.md` containing the verdict line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-companion/11-CONTEXT.md
@.planning/phases/11-cli-companion/11-VALIDATION.md
@.planning/phases/11-cli-companion/11-01-PLAN.md
@.planning/phases/11-cli-companion/11-02-PLAN.md

<interfaces>
<!-- Locked verdict format from CONTEXT.md D-08 + VALIDATION.md Manual-Only Verifications -->

CONTEXT.md D-08 (verbatim, locked):
- Acceptable operators: developer (CN/dev) acting as their own test subject — no fresh-operator requirement
- Pass criterion: "can I decipher the output without re-reading the source?"
- Recorded as a single subjective `PASS / FAIL + note` line at `.planning/phases/11-cli-companion/11-UAT.md`
- No 30-second timer
- NO "ships back to planning" clause — failure produces a follow-up plan inside Phase 11 or a Phase 11.1 micro-pass, not a re-open

VALIDATION.md "Manual-Only Verifications" (verbatim):
| Soft dogfood UAT | SC#4 | Subjective per D-08 | Developer runs `forge-bridge console tools` and `forge-bridge console execs --since 24h` against a live server, records PASS/FAIL+note line at `.planning/phases/11-cli-companion/11-UAT.md` |
| Rich rendering on real TTY | CLI-04 | CliRunner can't confirm visual fidelity | Developer runs `forge-bridge console tools` and `forge-bridge console health` on an interactive terminal; confirms Rich tables/panels render as intended (amber header, SQUARE box, truncated 8-char hash, `Created ▼` sort glyph legible). Recorded in `11-UAT.md` |

ROADMAP.md Phase 11 SC#4 (the goal this UAT verifies):
"Non-developer dogfood: an operator can answer 'what synthesized tools are active?' and 'when did the last execution run?' from the terminal without opening a browser, within 30 seconds." — interpreted per D-08 as developer-as-operator with the "can I decipher" criterion (no 30-second timer).

Suggested commands to run during the soft UAT (developer adapts as needed):
1. `python -m forge_bridge` (in one terminal — boots MCP + console on :9996)
2. In another terminal (with the same forge env active):
   - `forge-bridge console health` — confirm 4 panels render with amber headers and SQUARE box
   - `forge-bridge console tools` — confirm Created ▼ glyph appears in header; status chips render with color
   - `forge-bridge console tools <some_synth_tool_name>` — confirm Rich Panel drilldown
   - `forge-bridge console execs --since 24h` — confirm execution history table
   - `forge-bridge console execs --tool <some_tool>` — confirm stderr note appears: "note: --tool is filtered client-side until v1.4 API support…"
   - `forge-bridge console execs --tool <some_tool> --json` — confirm note is suppressed (P-01); pipe to `| jq .` to confirm clean JSON
   - `forge-bridge console manifest` — confirm same column shape as tools
   - `forge-bridge console doctor` — confirm 6+ check rows with ok/warn/fail chips; confirm exit code via `echo $?`
   - `forge-bridge console doctor --json | jq .` — confirm structured JSON output
3. Stop the server. Re-run any one of the above without --json — confirm stderr message: "forge-bridge console: server is not running on :9996. / Start it with: python -m forge_bridge"; confirm exit 2 via `echo $?`.

Verdict file format (single line, plus optional context):
```
[YYYY-MM-DD] PASS — <one-line note about what worked or surprised in a good way>
```
OR
```
[YYYY-MM-DD] FAIL — <one-line note about what was hard to decipher>
- Follow-up: <queue a follow-up plan inside Phase 11 OR recommend a Phase 11.1 micro-pass>
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Soft self-administered dogfood UAT (D-08)</name>
  <files>.planning/phases/11-cli-companion/11-UAT.md</files>
  <read_first>
    - .planning/phases/11-cli-companion/11-CONTEXT.md (`<decisions>` D-08 — soft UAT criterion, no-ship-back-to-planning clause)
    - .planning/phases/11-cli-companion/11-VALIDATION.md ("Manual-Only Verifications" table — soft UAT row + Rich-rendering-on-real-TTY row)
    - .planning/phases/11-cli-companion/11-01-SUMMARY.md (Plan 01 closure — coverage actuals, primitives shipped)
    - .planning/phases/11-cli-companion/11-02-SUMMARY.md (Plan 02 closure — full command list shipped, exit code matrix verified)
  </read_first>
  <action>
    This task is a checkpoint:human-verify — execution PAUSES here for the developer to perform the soft UAT manually. The complete verification procedure is in the <how-to-verify> block below: boot the server in one terminal (`python -m forge_bridge`), exercise all five subcommands in a second terminal, verify Rich rendering on a real TTY (amber header, SQUARE box, `Created ▼` glyph, status-chip colors), exercise the server-down UX (Ctrl-C the server, confirm locked stderr message + exit 2 in non-json mode AND JSON envelope on stdout + exit 2 in --json mode), apply the D-08 criterion ("can I decipher the output without re-reading the source?"), then write and commit `.planning/phases/11-cli-companion/11-UAT.md` per the locked PASS/FAIL+note verdict format. Reply `approved` (PASS) or `failed: <summary>` (FAIL) to the orchestrator.
  </action>
  <what-built>
    Phases 11-01 and 11-02 shipped: a Typer-based CLI with five subcommands (`tools`, `execs`, `manifest`, `health`, `doctor`) consuming the `:9996` console HTTP API via sync `httpx`. Output is Rich-formatted in TTY mode (SQUARE box, bold-yellow headers, amber-feel status chips, 8-char hash truncation, `Created ▼` default-sort glyph) and raw JSON in `--json` mode (P-01 stdout-pure). Connection-error UX is uniform across commands (locked stderr message + exit 2). The `doctor` subcommand runs an expanded check matrix with CI-gating exit codes. ≥ 80% coverage on `forge_bridge/cli/`. Full automated test suite is green.

    Coverage report from Plan 02:
    - forge_bridge/cli/client.py: ≥ 80% (per --cov-fail-under=80 gate)
    - forge_bridge/cli/render.py: ≥ 80%
    - forge_bridge/cli/since.py: ≥ 90%
    - forge_bridge/cli/tools.py: ≥ 80%
    - forge_bridge/cli/execs.py: ≥ 80%
    - forge_bridge/cli/manifest.py: ≥ 80%
    - forge_bridge/cli/health.py: ≥ 80%
    - forge_bridge/cli/doctor.py: ≥ 80%

    Threat-model mitigations (T-11-01/02/03/04) all implemented and tested.

    The remaining gap is what unit tests cannot verify: Rich rendering on a real terminal emulator (CliRunner sees plain output) AND the subjective "can I decipher this without re-reading the source?" criterion that Phase 10 retrospectively taught us catches the UX failure class unit tests miss.
  </what-built>
  <how-to-verify>
    **Step 1 — Boot the server in one terminal:**

    ```bash
    cd /Users/cnoellert/Documents/GitHub/forge-bridge
    # Use whatever shell activates the forge conda env on this system
    python -m forge_bridge
    ```

    Wait for the MCP startup logs to settle. The console HTTP API listens on `:9996` once `_lifespan` finishes its setup (Phase 9 D-31).

    **Step 2 — In a second terminal (forge env active), run the dogfood sequence:**

    ```bash
    # Health — confirms /api/v1/health is reachable and 4 Rich Panels render
    forge-bridge console health
    echo "exit: $?"   # expect 0

    # Tools list — confirm 4 columns render (Name | Status | Type | Created ▼) and chips have color
    forge-bridge console tools

    # Tools drilldown — pick a tool name from the list above
    forge-bridge console tools <NAME>

    # Tools filter
    forge-bridge console tools --origin synthesized
    forge-bridge console tools -q synth

    # Execs history
    forge-bridge console execs --since 24h
    forge-bridge console execs --since 7d --promoted

    # Execs --tool — confirm STDERR note is emitted, then suppressed in --json
    forge-bridge console execs --tool <SOME_TOOL_NAME>            # expect stderr note line
    forge-bridge console execs --tool <SOME_TOOL_NAME> --json | jq .   # expect NO stderr note + clean JSON

    # Manifest
    forge-bridge console manifest
    forge-bridge console manifest --json | jq .

    # Doctor — confirm check table with ok/warn/fail chips
    forge-bridge console doctor
    echo "exit: $?"   # 0 if all ok/warn, 1 if any fail

    forge-bridge console doctor --json | jq .   # confirm structured JSON
    ```

    **Step 3 — Server-down UX check (the connection-error contract):**

    Stop the `python -m forge_bridge` process (Ctrl-C in terminal 1). Then:

    ```bash
    forge-bridge console health
    # Expect on STDERR:
    # forge-bridge console: server is not running on :9996.
    # Start it with: python -m forge_bridge
    echo "exit: $?"   # expect 2

    forge-bridge console health --json
    # Expect on STDOUT:
    # {"error": {"code": "server_unreachable", "message": "..."}}
    echo "exit: $?"   # expect 2
    ```

    **Step 4 — Apply the D-08 criterion:**

    "Can I decipher the output without re-reading the source?"

    Specifically check:
    - [ ] Rich tables render with amber-feel header (bold yellow) on this terminal
    - [ ] SQUARE box characters render (┌─┬┐│ etc.) without corruption
    - [ ] Status chips have color (active=green, fail=red, warn=yellow, etc.)
    - [ ] `Created ▼` header glyph is legible — sort order is obvious without reading docs
    - [ ] 8-char hash truncation is readable on the execs list
    - [ ] Drilldown panels are readable
    - [ ] Doctor output makes the next action obvious (the "try:" column actionable)
    - [ ] Server-unreachable message is clear and non-scary (no traceback, no `str(httpx_exc)` leaking implementation)
    - [ ] `--json` output is machine-parseable across all commands (`| jq .` succeeds; no Rich bleed)

    **Step 5 — Write `11-UAT.md`:**

    Create `.planning/phases/11-cli-companion/11-UAT.md` with content:

    PASS template:
    ```markdown
    # Phase 11 — CLI Companion — Soft UAT (D-08)

    **Operator:** CN/dev (developer-as-operator per D-08)
    **Date:** YYYY-MM-DD
    **Server:** python -m forge_bridge on :9996
    **Criterion:** "Can I decipher the output without re-reading the source?"

    ---

    [YYYY-MM-DD] PASS — <one-line note about what worked / what surprised in a good way>

    ## Notes

    <optional context: anything noteworthy, anything close-but-fine, anything to watch>
    ```

    FAIL template:
    ```markdown
    # Phase 11 — CLI Companion — Soft UAT (D-08)

    **Operator:** CN/dev (developer-as-operator per D-08)
    **Date:** YYYY-MM-DD
    **Server:** python -m forge_bridge on :9996
    **Criterion:** "Can I decipher the output without re-reading the source?"

    ---

    [YYYY-MM-DD] FAIL — <one-line note about what was hard to decipher>

    ## Follow-up

    Per D-08 ("no ships back to planning" clause), failure produces:
    - [ ] Option A — follow-up plan inside Phase 11 (run `/gsd-plan-phase 11 --gaps`)
    - [ ] Option B — Phase 11.1 micro-pass (run `/gsd-plan-phase 11.1`)

    Recommendation: <which option, with one-line rationale>

    ## Notes

    <specific friction observed, ranked roughly by severity>
    ```

    Commit the file:
    ```bash
    git add .planning/phases/11-cli-companion/11-UAT.md
    git commit -m "docs(11): record soft self-UAT verdict (D-08)"
    ```

    **Step 6 — Resume the orchestrator:**

    Reply with `approved` (PASS verdict) OR `failed: <one-line summary + chosen follow-up option>` (FAIL verdict).
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/11-cli-companion/11-UAT.md</automated>
    <automated>grep -E "^\[[0-9]{4}-[0-9]{2}-[0-9]{2}\] (PASS|FAIL) — " .planning/phases/11-cli-companion/11-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/11-cli-companion/11-UAT.md` exists
    - File contains exactly one verdict line matching the regex `^\[[0-9]{4}-[0-9]{2}-[0-9]{2}\] (PASS|FAIL) — .+` (verified by grep -E)
    - File names CN/dev as operator (per D-08 acceptable-operators rule)
    - File references the D-08 criterion "Can I decipher the output without re-reading the source?"
    - If verdict is FAIL: file includes a Follow-up section naming Option A (`/gsd-plan-phase 11 --gaps`) OR Option B (`/gsd-plan-phase 11.1`) per D-08 no-ship-back-to-planning clause
    - File is committed to git (verified via `git log -1 -- .planning/phases/11-cli-companion/11-UAT.md` showing the commit)
  </acceptance_criteria>
  <resume-signal>Type `approved` (if PASS) or `failed: <summary>` (if FAIL). On approved: orchestrator marks Phase 11 complete and Plan 03 done. On failed: orchestrator queues the chosen follow-up per D-08.</resume-signal>
  <done>11-UAT.md exists with a single verdict line per the locked format, committed to git. Phase 11 closure path is clear: PASS → orchestrator marks complete; FAIL → orchestrator queues follow-up per D-08 (no phase re-open).</done>
</task>

</tasks>

<threat_model>
This plan modifies only `.planning/phases/11-cli-companion/11-UAT.md` — a documentation artifact. No code changes, no new trust boundaries, no STRIDE-applicable surface. Threats T-11-01..04 were addressed in Plans 01 and 02 and require no further mitigation here.
</threat_model>

<verification>
After the checkpoint task lands:

1. `.planning/phases/11-cli-companion/11-UAT.md` exists
2. The verdict line matches the locked single-line format
3. The file is committed to git
4. The orchestrator has the verdict signal (`approved` or `failed: ...`) to drive the next step
</verification>

<success_criteria>
- The soft UAT was actually performed against a live `:9996` server (not a desk exercise)
- The Rich rendering on a real terminal was visually confirmed (amber header, SQUARE box, `Created ▼` glyph, status-chip colors) — closing the CLI-04 gap that CliRunner cannot verify
- The server-unreachable UX was confirmed end-to-end (Ctrl-C the server, run a command, see the locked stderr message + exit 2; run with `--json`, see the structured-JSON envelope on stdout + exit 2)
- The verdict is recorded in `11-UAT.md` per the locked format
- If FAIL, a follow-up path is chosen (Option A or B) — no silent re-open, no abandoned defect
- Phase 11 is ready to close
</success_criteria>

<output>
After completion, create `.planning/phases/11-cli-companion/11-03-SUMMARY.md` documenting:
- The verdict (PASS or FAIL + reason)
- Which subcommands were exercised
- Any UX observations worth carrying into v1.4 (deferred-but-noticed items)
- The follow-up plan chosen (if FAIL)
- A note on whether the Phase 12 velocity gate (LLM Chat) is being honored (per ROADMAP — Phase 12 was already superseded by FB-D 2026-04-23, so this is a confirmation that no work is leaking back into Phase 12)
</output>
