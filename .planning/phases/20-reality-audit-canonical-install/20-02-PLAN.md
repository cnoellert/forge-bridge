---
phase: 20
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [DOCS-02]
nyquist_compliant: true

must_haves:
  truths:
    - "An AI assistant reading CLAUDE.md sees the v1.4.1 ground truth (5 surfaces, observability, learning pipeline, staged ops, chat) — not the v1.0 'extracted from projekt-forge' snapshot"
    - "The 'What exists and works' section enumerates the 5 user-facing surfaces by file path + port"
    - "The 'What is designed but not yet implemented' section correctly states that the canonical vocabulary layer IS shipped, while dep-graph traversal and Maya endpoint and auth are NOT"
    - "The 'Active Development Context' section references milestone v1.5 Legibility (not '2026-02-24 just extracted')"
    - "The 'Repository Layout' section reflects the actual top-level directories shipped at v1.4.1"
    - "The 'How to Get Running' section shows install commands that match v1.4.1 reality (no stale --http or --bridge-host flags)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Refreshed ground-truth reference for AI assistants resuming context on this project"
      contains: "Milestone: v1.5 Legibility"
      min_lines: 150
  key_links:
    - from: "CLAUDE.md 'What exists and works'"
      to: "forge_bridge/console/app.py + forge_bridge/cli/__main__.py + forge_bridge/console/handlers.py + forge_bridge/learning/ + forge_bridge/store/"
      via: "explicit file-path enumeration of all 5 surfaces"
      pattern: "forge_bridge/console/app\\.py|:9996|:9999|/api/v1/chat"
---

<objective>
Refresh `CLAUDE.md` to reflect v1.4.1 reality (DOCS-02). The current file is anchored to a 2026-02-24 snapshot that says "Just extracted from projekt-forge — this is the initial standalone repo," which has been false for 19 phases and 6 milestones. An AI assistant reading this file today is being actively misled about which surfaces exist, where they live, and what is shipped vs. designed.

Purpose: AI assistants (including Claude Code itself) re-read this file at the top of every fresh session per the file's own first sentence. If the file lies about what exists, every plan, every code change, every recommendation that follows is built on the wrong premise. The Phase 20 forcing function — non-author UAT will fail if the AI assistant guiding the install can't trust its own context-recovery doc.

Output: `CLAUDE.md` rewritten in five surgical edits (per CONTEXT.md D-12 + 20-PATTERNS.md): (1) "What exists and works" — full rewrite enumerating all 5 surfaces; (2) "What is designed but not yet implemented" — corrected (vocab IS shipped, dep-graph + Maya + auth still pending); (3) "Repository Layout" — actual top-level directories; (4) "Active Development Context" — v1.5 milestone reality; (5) "How to Get Running" — install commands that match the rest of Phase 20's deliverables.

**Out of scope for this plan** (per D-10 / D-12): "What is this project?" intro (still directionally correct), "Key Design Decisions" table (directionally correct, vocab "automatic" claim gets a footnote at most), "Vocabulary Summary" (correct at concept level), "Relationship to projekt-forge" (accurate), "Questions To Come Back To" (still valid). Do not restructure or add new H2 sections — those are Phase 21 territory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md
@.planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md
@.planning/phases/20-reality-audit-canonical-install/20-PATTERNS.md

<interfaces>
<!-- Source files that establish the v1.4.1 ground truth the rewrite must match. -->
<!-- Do NOT modify these; READ them so the rewrite reflects what actually ships. -->

Surface entry points (verified by RESEARCH §B1):
- Web UI: `forge_bridge/console/app.py:build_console_app()` mounted on `:9996/ui/`, launched via MCP server lifespan in `forge_bridge/mcp/server.py:_start_console_task()`
- CLI `forge-bridge`: `forge_bridge/__main__.py:app` (Typer entry registered in `pyproject.toml [project.scripts]`)
- HTTP `/api/v1/chat`: `forge_bridge/console/handlers.py:chat_handler` registered at `Route("/api/v1/chat", chat_handler, methods=["POST"])` in `forge_bridge/console/app.py`
- MCP server: `forge_bridge/mcp/server.py:main()` invoked by `forge_bridge/__main__.py`, runs FastMCP in stdio mode by default
- Flame hook: `flame_hooks/forge_bridge/scripts/forge_bridge.py` installed by `scripts/install-flame-hook.sh` to `/opt/Autodesk/shared/python/forge_bridge/scripts/`, listens on `:9999`

Actual top-level directories under `forge_bridge/` (verified by ls at HEAD):
`cli/`, `client/`, `console/`, `core/`, `flame/`, `learning/`, `llm/`, `mcp/`, `server/`, `store/`, `tools/`

Canonical vocabulary layer (currently said to be "not yet implemented" — RESEARCH §A1 confirms it IS shipped):
- `forge_bridge/core/vocabulary.py`
- `forge_bridge/core/entities.py`
- `forge_bridge/core/traits.py`
- `forge_bridge/core/registry.py`

Still NOT shipped (legitimately on the "designed but not implemented" list):
- Dependency graph traversal engine (relationships exist in DB schema via FK + DBEvent rows, but no graph-traversal module)
- Bridge core service as a separate router process
- Event-driven canonical pub/sub layer (WebSocket server in `forge_bridge/server/` ships, but the canonical pub/sub abstraction does not)
- Maya endpoint
- Editorial / shot tracking adapters
- Authentication (deferred — SEED-AUTH-V1.5)

Subcommand registry (verified by `forge_bridge/__main__.py` lines 36–40):
- `forge-bridge console tools`
- `forge-bridge console execs`
- `forge-bridge console manifest`
- `forge-bridge console health`
- `forge-bridge console doctor`

Operator-facing artifact paths (auto-created by the running server, per RESEARCH §B3):
- `~/.forge-bridge/executions.jsonl`
- `~/.forge-bridge/synthesized/`
- `~/.forge-bridge/probation/`
- `~/.forge-bridge/quarantined/`

Stale install commands currently in CLAUDE.md "How to Get Running" (lines 128–143) that must be corrected:
- `pip install -e .` → `pip install -e ".[dev,llm]"` (the `[llm]` extra is mandatory for chat + synthesis)
- `python -m forge_bridge --http --port 8080` → REMOVE (this flag pattern does not launch the Web UI; the Web UI ships on `:9996` via lifespan automatically)
- `cp flame_hooks/.../forge_bridge.py /opt/Autodesk/...` → replace with reference to `./scripts/install-flame-hook.sh`
- Add stdin-keepalive note for headless server launch: `tail -f /dev/null | python -m forge_bridge` (per 16.2-HUMAN-UAT.md)
- Add Web UI smoke test: `curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"` → expect 200
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md "Current State" + "Repository Layout" + "How to Get Running" + "Active Development Context"</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file — confirm current line numbers + content match the drift map below)
    - .planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md (§A1 CLAUDE.md drift map, §B v1.4.1 ground truth)
    - .planning/phases/20-reality-audit-canonical-install/20-PATTERNS.md (§ CLAUDE.md — the section-by-section guidance, sections that survive verbatim, sections that need rewrite)
    - .planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md (D-10, D-12 — the Phase 20 vs Phase 21 boundary)
    - .planning/STATE.md (top section — milestone v1.5 status + v1.4.1 close metrics)
    - forge_bridge/__main__.py (confirm subcommand list)
    - forge_bridge/console/app.py (confirm Web UI mount path + chat route registration)
  </read_first>
  <action>
    Make four surgical edits to `CLAUDE.md`. Preserve the file's existing top-level structure (H1 title + the seven H2 sections in current order). Do NOT add new H2 sections, do NOT renumber, do NOT touch the sections marked "preserve verbatim."

    **EDIT 1 — Replace the "## Current State" section (currently lines 21–46) ENTIRELY with:**

    ```markdown
    ## Current State

    Shipped at v1.4.1 (2026-04-30). 19 phases across 6 milestones (v1.0 → v1.4.x). ~40,594 LOC. 19 symbols in `forge_bridge.__all__`. The v1.5 Legibility milestone is open (Phases 20-23) — see `.planning/STATE.md` for the live cursor.

    ### What exists and works

    **Five user-facing surfaces, all live:**

    1. **Flame HTTP bridge** (`flame_hooks/forge_bridge/scripts/forge_bridge.py`)
       - Runs inside Flame as an HTTP server on port 9999
       - Accepts Python code via `POST /exec`, executes on Flame's main thread via `schedule_idle_event`
       - Returns `{result, stdout, stderr, error, traceback}` as JSON
       - Has an interactive web UI at `http://localhost:9999/`
       - Persistent namespace across requests
       - Installed via `./scripts/install-flame-hook.sh`

    2. **MCP server** (`forge_bridge/mcp/server.py`)
       - Model Context Protocol server (FastMCP, stdio default)
       - Entry point: `python -m forge_bridge` (wired through `forge_bridge/__main__.py`)
       - Co-hosts the Artist Console + chat endpoint on `:9996` via lifespan
       - Tools: project, timeline, batch, publish, utility — plus staged-ops tools (`forge_list_staged`, `forge_get_staged`, `forge_approve_staged`, `forge_reject_staged`), manifest tools (`forge_manifest_read`, `forge_tools_read`), and any LLM-synthesized tools registered by the learning pipeline
       - Resources: `forge://manifest/synthesis`, `forge://staged/pending`, `forge://llm/health`

    3. **Artist Console / Web UI** (`forge_bridge/console/app.py`) — Phase 10/10.1
       - Mount: `http://localhost:9996/ui/`
       - Five views: tools, execs, manifest, health, chat
       - Jinja2 + vendored htmx + Alpine.js with SRI; zero JS build step
       - LOGIK-PROJEKT amber-on-dark palette (UI-SPEC.md design contract)
       - Co-hosted with the MCP server on `:9996` via lifespan (NOT FastMCP `custom_route`)

    4. **CLI `forge-bridge`** (`forge_bridge/__main__.py` + `forge_bridge/cli/`) — Phase 11
       - Typer + Rich, sync `httpx` consumer of the `:9996` Read API
       - Subcommands: `forge-bridge console tools | execs | manifest | health | doctor`
       - `--json` short-circuits Rich (P-01 stdout purity)
       - Locked exit codes: 0=ok, 1=fail, 2=unreachable

    5. **Chat endpoint** (`forge_bridge/console/handlers.py:chat_handler`) — Phase 16/16.1/16.2
       - `POST http://localhost:9996/api/v1/chat`
       - Agentic tool-call loop via `LLMRouter.complete_with_tools()` (Phase 15 / FB-C)
       - `sensitive=True` hardcoded → routes through local Ollama (`qwen2.5-coder:32b`); `ANTHROPIC_API_KEY` not required for chat
       - Rate limit: 10 req/60s (IP-keyed; SEED-AUTH-V1.5 plants caller-identity migration)
       - Outer wall-clock 125s (FB-C 120s inner + 5s framing buffer)

    **Subsystems behind the surfaces:**

    - **Canonical vocabulary layer** (`forge_bridge/core/`) — `vocabulary.py`, `entities.py`, `traits.py`, `registry.py`. Project → Sequence → Shot → Version → Media + Stack/Layer/Asset, plus Versionable/Locatable/Relational traits. **Shipped** (was on the "not yet implemented" list in v1.0; corrected here).
    - **WebSocket server** (`forge_bridge/server/`) — wire protocol, connection management, event-driven hooks. Standalone WS server on `:9998` (graceful degradation if unreachable per Phase 07.1).
    - **Postgres persistence layer** (`forge_bridge/store/` + Alembic migrations in `forge_bridge/store/migrations/`) — entities, relationships, events, registry, and the `staged_operation` table from FB-A.
    - **Async/sync client pair** (`forge_bridge/client/`) — `AsyncClient` + `SyncClient` for connecting to the WS server.
    - **Learning pipeline** (`forge_bridge/learning/`) — execution log (JSONL source of truth at `~/.forge-bridge/executions.jsonl`), threshold-driven promotion, LLM synthesizer (Ollama backend), registry watcher, probation system. SQL mirror via `StoragePersistence` Protocol (Phase 8) — JSONL-authoritative, SQL eventual.
    - **LLM router** (`forge_bridge/llm/router.py`) — `LLMRouter` with `acomplete()` and `complete_with_tools()`; sensitive routing to local Ollama vs. cloud Anthropic; hard caps (8 iterations, 120s wall-clock, 30s per-tool); `LLMLoopBudgetExceeded` / `RecursiveToolLoopError` / `LLMToolError` exported from the public API.
    - **Staged operations platform** (`forge_bridge/store/staged_operation.py` + console + MCP wiring) — `proposed → approved → executed/rejected/failed` state machine. Approval is bookkeeping; the proposer subscribes to events and executes against its own domain.
    - **Tool provenance** — every synthesized tool carries `_meta.forge-bridge/*` fields (`origin`, `code_hash`, `synthesized_at`, `version`, `observation_count`); `annotations.readOnlyHint=False` always; sanitization at the read boundary (single-source `_sanitize_patterns.py` shared across Phase 7 + FB-C).

    ### What is designed but not yet implemented

    - Dependency graph traversal engine (relationships exist in the schema via FK + `DBEvent` rows, but no graph-traversal module)
    - Bridge core service as a separate router process
    - Canonical event-driven pub/sub abstraction (the WebSocket server ships; the canonical pub/sub layer on top of it does not)
    - Maya endpoint
    - Editorial / shot-tracking adapters
    - Authentication (deferred — SEED-AUTH-V1.5; v1.6+ scope)
    ```

    **EDIT 2 — Replace the "## Repository Layout" section (currently lines 50–79) ENTIRELY with:**

    ```markdown
    ## Repository Layout

    ```
    forge-bridge/
    ├── CLAUDE.md               ← YOU ARE HERE
    ├── README.md               ← Project overview, install quick-start
    ├── pyproject.toml          ← Package config (version, deps, extras [dev], [llm], [test-e2e])
    ├── alembic.ini             ← DB migration config (sync URL; reads FORGE_DB_URL or hardcoded forge:forge@localhost)
    │
    ├── forge_bridge/                ← Python package
    │   ├── __init__.py             ← Public __all__ (19 symbols)
    │   ├── __main__.py             ← Typer app: `forge-bridge` CLI + `python -m forge_bridge` MCP entry
    │   ├── bridge.py               ← HTTP client to Flame bridge (`bridge.execute()` etc.)
    │   ├── cli/                    ← Typer + Rich subcommands (tools, execs, manifest, health, doctor)
    │   ├── client/                 ← Async/sync WS clients
    │   ├── console/                ← Artist Console (Web UI on :9996, /api/v1/chat handler, app.py mount)
    │   ├── core/                   ← Canonical vocabulary (entities, traits, registry, vocabulary)
    │   ├── flame/                  ← Flame-specific helpers shared across tools
    │   ├── learning/               ← Execution log, synthesizer, watcher, probation
    │   ├── llm/                    ← LLMRouter, tool adapters (Ollama, Anthropic), sanitization
    │   ├── mcp/                    ← FastMCP server + tool registry (the canonical MCP server lives HERE, not in server.py)
    │   ├── server/                 ← WebSocket server + protocol
    │   ├── store/                  ← Postgres entities, sessions, Alembic migrations, staged_operation
    │   └── tools/                  ← MCP tool implementations: project, timeline, batch, publish, utility
    │
    ├── flame_hooks/             ← Installs into Flame's Python hooks dir
    │   └── forge_bridge/
    │       └── scripts/
    │           └── forge_bridge.py   ← HTTP server running inside Flame on :9999
    │
    ├── scripts/
    │   └── install-flame-hook.sh    ← Deploys the Flame hook (defaults pinned to v1.4.1)
    │
    ├── tests/                   ← pytest suite (test_*.py); see tests/llm/, tests/integration/, tests/console/
    │
    └── docs/
        ├── INSTALL.md          ← Canonical install guide (Phase 20 ships this)
        ├── VOCABULARY.md       ← The canonical language
        ├── ARCHITECTURE.md     ← Design decisions
        ├── API.md              ← HTTP API for the Flame bridge endpoint
        ├── ENDPOINTS.md        ← How to write new endpoint adapters
        ├── DATA_MODEL.md       ← Domain reference
        └── FLAME_API.md        ← Domain reference
    ```

    **Note:** `forge_bridge/server.py` (top-level file, NOT the `server/` directory) is a pre-Phase-5 orphan — broken imports, not re-exported, not runtime-imported. The canonical MCP server lives at `forge_bridge/mcp/server.py`. Tracked as a follow-up dead-code cleanup.
    ```

    **EDIT 3 — Replace the "## How to Get Running" section (currently lines 128–143) ENTIRELY with:**

    ```markdown
    ## How to Get Running

    See `docs/INSTALL.md` for the canonical operator-workstation install path. Quick reference:

    ```bash
    # 1. conda env (matches the reference deployment)
    conda create -n forge python=3.11 -y
    conda activate forge

    # 2. Install with the LLM extras (mandatory for chat + synthesis)
    pip install -e ".[dev,llm]"

    # 3. Install the Flame hook
    ./scripts/install-flame-hook.sh
    # or standalone: curl -fsSL https://raw.githubusercontent.com/cnoellert/forge-bridge/v1.4.1/scripts/install-flame-hook.sh | bash

    # 4. Run migrations (defaults to forge:forge@localhost:5432/forge_bridge — set FORGE_DB_URL to override)
    alembic upgrade head

    # 5. Run the MCP server + co-hosted Web UI + chat endpoint on :9996
    python -m forge_bridge
    # On a headless host where stdin closes immediately:
    tail -f /dev/null | python -m forge_bridge

    # 6. Smoke-test the surfaces
    curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"   # Web UI → 200
    curl -s   http://localhost:9999/status                                  # Flame hook → JSON with flame_available
    forge-bridge console doctor                                             # CLI + post-install diagnostic
    ```

    `ANTHROPIC_API_KEY` is **optional** — the chat endpoint hardcodes `sensitive=True`, which routes through local Ollama (`qwen2.5-coder:32b`). The key is only needed for `sensitive=False` cloud routing (not the daily operator workflow).

    **Do not use `qwen3:32b` as the default model.** It exceeds the 60s wall-clock budget due to thinking-mode token verbosity (SEED-DEFAULT-MODEL-BUMP-V1.4.x).
    ```

    **EDIT 4 — Replace the "## Active Development Context" section (currently lines 147–154) ENTIRELY with:**

    ```markdown
    ## Active Development Context

    Milestone: **v1.5 Legibility** (opened 2026-04-30; see `.planning/STATE.md` for the live cursor).

    Theme: make forge-bridge usable by its first daily user — close the gap between what's shipped (19 phases, 5 surfaces, ~40k LOC) and what a person can sit down and actually use without re-deriving the deployment topology each time. Four phases:

    - **Phase 20** (current) — Reality Audit + Canonical Install: ships `docs/INSTALL.md`, refreshes README install section + this `CLAUDE.md` ground-truth, pins `install-flame-hook.sh` default to `v1.4.1`. Forcing function: a non-author UAT must pass on a fresh conda env.
    - **Phase 21** — Surface Map + Concept Docs: ships `docs/GETTING-STARTED.md`, rewrites README "What This Is", documents the projekt-forge consumer relationship.
    - **Phase 22** — Daily Workflow Recipes: 6 step-by-step recipes (first-time setup, Claude Desktop wiring, tool synthesis, Flame chat automation, staged-ops approval, manifest inspection).
    - **Phase 23** — Diagnostics + Recovery: ships `docs/TROUBLESHOOTING.md`; in-flight `forge doctor` polish if recipe authoring surfaces gaps.

    **v1.5 constraints:** Legibility, not features. No new external libraries. Public `forge_bridge.__all__` stays at 19 unless an install audit surfaces a genuine need. All v1.5+ planted seeds (`SEED-OPUS-4-7-TEMPERATURE-V1.5`, `SEED-AUTH-V1.5`, `SEED-DEFAULT-MODEL-BUMP-V1.4.x`, `SEED-CHAT-STREAMING-V1.4.x`, etc.) defer to v1.6+.

    **Don't break:** the Flame hook + MCP server + Artist Console + CLI + chat endpoint are all in production use on assist-01. Refactoring during a docs milestone is out of scope unless the install audit explicitly surfaces a code gap that blocks the operator path (CONTEXT.md D-04).
    ```

    Touch nothing else in CLAUDE.md. The following sections survive VERBATIM:
    - `# CLAUDE.md — forge-bridge Context Recovery` (header)
    - `## What is this project?` (lines 7–18)
    - `## Key Design Decisions (brief version)` (lines 83–94)
    - `## Vocabulary Summary` (lines 98–113)
    - `## Relationship to projekt-forge` (lines 117–124)
    - `## Questions To Come Back To` (lines 158–163)

    Commit message: `docs(20): refresh CLAUDE.md to v1.4.1 ground truth (DOCS-02)`
  </action>
  <verify>
    <automated>grep -q '## Current State' CLAUDE.md && grep -q ':9996/ui/' CLAUDE.md && grep -q '/api/v1/chat' CLAUDE.md && grep -q 'forge-bridge console doctor' CLAUDE.md && grep -q 'v1.5 Legibility' CLAUDE.md && [ "$(grep -c 'As of 2026-02-24' CLAUDE.md)" = "0" ] && [ "$(grep -c 'Just extracted from projekt-forge' CLAUDE.md)" = "0" ] && [ "$(grep -c 'forge_bridge/' CLAUDE.md)" -ge "10" ]</automated>
  </verify>
  <done>
    - `CLAUDE.md` contains `## Current State` heading
    - `grep -q ':9996/ui/' CLAUDE.md` exits 0 (Web UI surface enumerated)
    - `grep -q '/api/v1/chat' CLAUDE.md` exits 0 (chat surface enumerated)
    - `grep -q 'forge-bridge console doctor' CLAUDE.md` exits 0 (CLI surface enumerated)
    - `grep -q 'flame_hooks/forge_bridge/scripts/forge_bridge.py' CLAUDE.md` exits 0 (Flame hook surface enumerated)
    - `grep -q 'python -m forge_bridge' CLAUDE.md` exits 0 (MCP server surface enumerated)
    - `grep -q 'v1.5 Legibility' CLAUDE.md` exits 0 (Active Development Context refreshed)
    - `grep -q 'Phase 20' CLAUDE.md` exits 0
    - `grep -q 'forge_bridge/core/' CLAUDE.md` exits 0 (vocabulary correctly described as shipped)
    - `grep -c 'As of 2026-02-24' CLAUDE.md` returns 0 (stale date purged)
    - `grep -c 'Just extracted from projekt-forge' CLAUDE.md` returns 0 (stale framing purged)
    - `grep -c 'python -m forge_bridge --http --port 8080' CLAUDE.md` returns 0 (stale CLI flag purged)
    - `grep -c 'pip install -e \\.$' CLAUDE.md` returns 0 (the bare `pip install -e .` without extras is gone)
    - `grep -q '\\[dev,llm\\]' CLAUDE.md` exits 0 (the [dev,llm] extras are documented)
    - `grep -q 'tail -f /dev/null | python -m forge_bridge' CLAUDE.md` exits 0 (stdin-keepalive note present)
    - All six "preserve verbatim" sections still present (`# CLAUDE.md`, `## What is this project?`, `## Key Design Decisions`, `## Vocabulary Summary`, `## Relationship to projekt-forge`, `## Questions To Come Back To`)
    - One commit on branch with message `docs(20): refresh CLAUDE.md to v1.4.1 ground truth (DOCS-02)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI assistant → CLAUDE.md | The file is read at the top of every session. It is the canonical context-recovery doc. A misleading file biases every downstream decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Information Disclosure | CLAUDE.md content | accept (LOW) | The file is already in the repo at HEAD; the rewrite changes accuracy, not what's published. No new secrets, no internal paths leaked. The `~/.forge-bridge/*` paths are operator-machine paths already documented in shipped CLI source. |
| T-20-05 | Spoofing | CLAUDE.md "What exists" claims | mitigate | The rewrite enumerates surfaces by file path. The verify-step grep gates ensure the file actually mentions each of the 5 surfaces by their canonical path/port. If a future edit silently drops a surface, the grep gate fails on next CI/local-test run. |
</threat_model>

<verification>
```bash
# Section presence
grep -q '## Current State' CLAUDE.md
grep -q '## Repository Layout' CLAUDE.md
grep -q '## How to Get Running' CLAUDE.md
grep -q '## Active Development Context' CLAUDE.md

# All five surfaces mentioned
grep -q 'flame_hooks/forge_bridge/scripts/forge_bridge.py' CLAUDE.md   # Flame hook
grep -q 'python -m forge_bridge' CLAUDE.md                              # MCP server
grep -q ':9996/ui/' CLAUDE.md                                            # Web UI
grep -q 'forge-bridge console' CLAUDE.md                                 # CLI
grep -q '/api/v1/chat' CLAUDE.md                                         # Chat endpoint

# v1.4.1 reality reflected; v1.0 snapshot purged
grep -q 'v1.5 Legibility' CLAUDE.md
grep -q 'Phase 20' CLAUDE.md
grep -q 'forge_bridge/core/' CLAUDE.md
[ "$(grep -c 'As of 2026-02-24' CLAUDE.md)" = "0" ]
[ "$(grep -c 'Just extracted from projekt-forge' CLAUDE.md)" = "0" ]
[ "$(grep -c 'python -m forge_bridge --http --port 8080' CLAUDE.md)" = "0" ]

# Preserved sections still present
grep -q '## What is this project?' CLAUDE.md
grep -q '## Key Design Decisions' CLAUDE.md
grep -q '## Vocabulary Summary' CLAUDE.md
grep -q '## Relationship to projekt-forge' CLAUDE.md
grep -q '## Questions To Come Back To' CLAUDE.md
```
</verification>

<success_criteria>
- [ ] `## Current State` enumerates all 5 surfaces by file path + port
- [ ] `## Current State` correctly states canonical vocabulary IS shipped (was on the wrong list)
- [ ] `## Current State` correctly lists what is still NOT implemented (dep-graph, Maya, auth, etc.)
- [ ] `## Repository Layout` shows actual top-level dirs (cli/, console/, core/, learning/, llm/, mcp/, store/, etc.)
- [ ] `## Repository Layout` flags `forge_bridge/server.py` as a pre-Phase-5 orphan vs. `forge_bridge/mcp/server.py` (the real one)
- [ ] `## How to Get Running` uses `pip install -e ".[dev,llm]"` (not bare `pip install -e .`)
- [ ] `## How to Get Running` removes the stale `--http --port 8080` and `--bridge-host` flags
- [ ] `## How to Get Running` includes the stdin-keepalive note + `:9996/ui/` + `:9999/status` smoke tests
- [ ] `## How to Get Running` cross-links `docs/INSTALL.md`
- [ ] `## Active Development Context` references milestone v1.5 + Phase 20 + v1.4.1 (no 2026-02-24 anchor)
- [ ] All six preserve-verbatim sections still present
- [ ] Single commit on branch
- [ ] Full pytest suite still green: `pytest tests/ -x` (no test depends on CLAUDE.md content but the consistency tests from Plan 20-01 must still pass)
</success_criteria>

<output>
After completion, create `.planning/phases/20-reality-audit-canonical-install/20-02-SUMMARY.md` covering:
- Diff summary (what was rewritten, what was preserved)
- Confirmation that all 5 surfaces are enumerated by canonical path + port
- Verification grep results
- Any subsystems / surfaces discovered during the rewrite that didn't make it into the original surface list
- Any drift surfaced in the preserved sections (logged as candidates for Phase 21 vs accepted as directionally-correct)
</output>
