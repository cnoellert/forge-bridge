---
phase: 20
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - README.md
autonomous: true
requirements: [INSTALL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "README 'Quick Start' install commands match v1.4.1 reality (no stale --http or --bridge-host flags; [llm] extras documented)"
    - "README 'Test the connection' step covers BOTH the Flame hook (:9999) AND the Web UI (:9996/ui/) — currently it only mentions :9999"
    - "README 'Current Status' table reflects v1.4.1: canonical vocabulary marked Shipped (not 'In design'); rows added for Artist Console, CLI, /api/v1/chat, staged ops, LLMRouter agentic loop"
    - "README install section and docs/INSTALL.md (Plan 20-04) agree — install-flame-hook.sh defaults, conda env name, pip extras, env vars all match"
    - "README curl URL and pyproject.toml version remain in lockstep (the 20-01 consistency test continues to pass)"
  artifacts:
    - path: "README.md"
      provides: "Refreshed install section + Current Status table aligned to v1.4.1 reality"
      contains: "http://localhost:9996/ui/"
      min_lines: 200
  key_links:
    - from: "README.md 'Run the MCP server'"
      to: "forge_bridge/__main__.py:app"
      via: "command examples that match real Typer subcommand surface"
      pattern: "python -m forge_bridge|forge-bridge console"
    - from: "README.md 'Current Status' table"
      to: "forge_bridge/core/ + forge_bridge/console/ + forge_bridge/cli/ + forge_bridge/llm/ + forge_bridge/learning/ + forge_bridge/store/"
      via: "table rows enumerating shipped subsystems"
      pattern: "Artist Console|CLI|/api/v1/chat|Staged ops|LLMRouter"
---

<objective>
Refresh `README.md`'s "Quick Start" install section and "Current Status" table to reflect v1.4.1 reality. The table currently says canonical vocabulary is "🔧 In design" — it shipped with v1.0. The install section currently shows `python -m forge_bridge --http --port 8080` and `--bridge-host 192.168.1.100`, neither of which is a real CLI option. Bare `pip install -e .` omits the `[llm]` extra that's mandatory for chat + synthesis. The "Test the connection" step only covers `:9999` — it never mentions the primary Web UI surface on `:9996/ui/`.

Purpose: Plan 20-01 already locked the curl URL at `v1.4.1` and added the consistency-guard test. This plan finishes the README half of the install consistency picture (INSTALL-03) so a reader following the README reaches the same state as a reader following `docs/INSTALL.md` (Plan 20-04). The "Current Status" table is the second visible-on-arrival summary an outside reader sees — leaving "Canonical vocabulary spec: In design" published next to a 19-phase shipped codebase actively misleads.

Output: Two surgical edits to README.md per CONTEXT.md D-11 + 20-PATTERNS.md: (1) the "Quick Start" install command block (lines ~95–142, EXCLUDING the curl URL line which Plan 20-01 already owns); (2) the "Current Status" table (lines ~52–63). Touch nothing else — "What This Is", "Vision", "Architecture", "Repository Structure", "Design Principles" all belong to Phase 21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md
@.planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md
@.planning/phases/20-reality-audit-canonical-install/20-PATTERNS.md
@.planning/phases/20-reality-audit-canonical-install/20-01-PLAN.md

<interfaces>
<!-- Plan 20-01 already touched README.md lines 106 + 109 (the curl URL + "default v1.4.1" prose). -->
<!-- This plan must NOT re-touch those lines. -->

Lines Plan 20-01 has ALREADY corrected (do not re-edit):
- Line 106: `curl -fsSL https://raw.githubusercontent.com/cnoellert/forge-bridge/v1.4.1/scripts/install-flame-hook.sh | bash`
- Line 109: `Override `FORGE_BRIDGE_VERSION` (default `v1.4.1`) ...`

Lines THIS plan must correct (currently still stale):

Current Status table (lines 52–63):
```
| Component | Status |
|-----------|--------|
| Flame HTTP bridge | ✅ Working |
| MCP server (LLM tools) | ✅ Working |
| Canonical vocabulary spec | 🔧 In design |
| Dependency graph engine | 📋 Planned |
| Maya endpoint | 📋 Planned |
| Editorial/shot tracking adapters | 📋 Planned |
| Event-driven channel system | 📋 Planned |
```

"Install the MCP server" (lines 120–124):
```
### Install the MCP server

```bash
pip install -e .
```
```

"Run the MCP server" (lines 126–137):
```
### Run the MCP server

```bash
# Local (stdio — for use with Claude Desktop or similar)
python -m forge_bridge

# Remote Flame (bridge on another machine)
python -m forge_bridge --bridge-host 192.168.1.100

# HTTP transport (multi-client)
python -m forge_bridge --http --port 8080
```
```

"Test the connection" (lines 139–141):
```
### Test the connection

Open `http://localhost:9999/` in a browser for the interactive Python console. Type `flame` to confirm the Flame API is available.
```

Sections that MUST NOT be touched in this plan (per CONTEXT.md D-10/D-11 — these are Phase 21):
- "Vision" (lines 9–13)
- "Architecture" diagram (lines 18–36)
- "Current Implementation (Phase 1)" prose (lines 38–48)
- "Conda environment" section (lines 65–91) — directionally correct, leave as-is
- "Install the Flame hook" (lines 97–118) — Plan 20-01 already bumped the curl URL + prose; the rest is accurate
- "Repository Structure" block (lines 145–176) — stale but Phase 21 territory unless it directly blocks install
- "Documentation" links (lines 180–185) — accurate
- "Relationship to projekt-forge" (lines 189–193) — accurate
- "Design Principles" (lines 199–208) — accurate
- "License" (lines 211–213) — accurate

Real subcommand surface (verified by `forge_bridge/__main__.py` and CONTEXT.md):
- `forge-bridge console tools | execs | manifest | health | doctor` (NO `forge events`, NO `forge errors` — those don't exist)

Stale flags to remove (verified by RESEARCH §A2):
- `--http --port 8080` — does not launch the Web UI; the Web UI ships on `:9996` via lifespan automatically
- `--bridge-host 192.168.1.100` — not a Typer option; the MCP server reads `FORGE_BRIDGE_HOST` env var instead

stdin-keepalive note (per RESEARCH §B1, captured in 16.2-HUMAN-UAT.md Fixture State):
- `tail -f /dev/null | python -m forge_bridge` — for headless deploy hosts where stdin closes immediately

Five-surface smoke commands (per RESEARCH §D1):
- Web UI: `curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"` → 200
- CLI: `forge-bridge --help`
- Chat: `curl -s -X POST http://localhost:9996/api/v1/chat -H "content-type: application/json" -d '{"messages":[{"role":"user","content":"hello"}]}'`
- MCP server: covered by `forge-bridge --help` plus a Claude Desktop connection
- Flame hook: `curl -s http://localhost:9999/status` → JSON with `flame_available`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace README "Current Status" table + "Install/Run/Test" install-section blocks</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — confirm line numbers + current content match the drift map below; Plan 20-01 has already updated lines 106 + 109)
    - .planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md (§A2 README install section drift map, §A3 Current Status table drift map, §B1 five-surface entry points, §D1 per-surface smoke commands)
    - .planning/phases/20-reality-audit-canonical-install/20-PATTERNS.md (§ README.md — install section + Current Status table refresh; sections that must NOT be touched)
    - .planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md (D-10, D-11 — Phase 20 vs Phase 21 boundary)
    - forge_bridge/__main__.py (confirm subcommand list — `console tools | execs | manifest | health | doctor`)
    - tests/test_install_hook_version_consistency.py (created by Plan 20-01 — must remain green after this plan)
  </read_first>
  <action>
    Three surgical edits to `README.md`. Touch nothing else.

    **EDIT 1 — Replace the entire "## Current Status" table block (currently lines 52–63) with:**

    ```markdown
    ## Current Status

    Shipped at **v1.4.1** (2026-04-30). 19 phases across 6 milestones. Active milestone: **v1.5 Legibility** (Phases 20-23 — install + concept docs + recipes + diagnostics).

    | Component | Status |
    |-----------|--------|
    | Flame HTTP bridge (`:9999`) | ✅ Shipped (v1.0) |
    | MCP server (`python -m forge_bridge`, stdio) | ✅ Shipped (v1.0; expanded through v1.4) |
    | Canonical vocabulary layer (`forge_bridge/core/`) | ✅ Shipped (v1.0) |
    | Postgres persistence + migrations | ✅ Shipped (v1.0) |
    | Async/sync WebSocket clients | ✅ Shipped (v1.0) |
    | Tool provenance in MCP `_meta` (PROV-01..06) | ✅ Shipped (v1.2.0) |
    | StoragePersistence Protocol + SQL mirror | ✅ Shipped (v1.3.0) |
    | Artist Console / Web UI (`:9996/ui/`) | ✅ Shipped (v1.3.1, Phases 10/10.1) |
    | CLI `forge-bridge console tools \| execs \| manifest \| health \| doctor` | ✅ Shipped (v1.3.1, Phase 11) |
    | Staged-operations platform (`/api/v1/staged`, MCP tools, lifecycle) | ✅ Shipped (v1.4, FB-A + FB-B) |
    | LLMRouter agentic tool-call loop (`complete_with_tools()`) | ✅ Shipped (v1.4, FB-C) |
    | Chat endpoint (`POST /api/v1/chat`) | ✅ Shipped (v1.4, FB-D + 16.1 + 16.2) |
    | WebSocket event server (`:9998`) | ✅ Shipped (graceful degradation per Phase 07.1) |
    | Learning pipeline (synthesis + probation + manifest) | ✅ Shipped (v1.0; refined through v1.4) |
    | Dependency graph traversal engine | 📋 Planned (relationships persist; no traversal module yet) |
    | Canonical event-driven pub/sub abstraction | 📋 Planned (WS server ships; canonical layer does not) |
    | Maya endpoint | 📋 Planned |
    | Editorial / shot-tracking adapters | 📋 Planned |
    | Authentication (multi-user, caller identity) | 📋 Planned (SEED-AUTH-V1.5; v1.6+) |
    ```

    **EDIT 2 — Replace the "### Install the MCP server" + "### Run the MCP server" + "### Test the connection" blocks (currently lines 120–141) with:**

    ```markdown
    ### Install forge-bridge

    Install with the LLM extras (mandatory for chat + learning-pipeline synthesis):

    ```bash
    pip install -e ".[dev,llm]"
    ```

    `[dev]` adds pytest + ruff. `[llm]` adds `openai`, `anthropic`, `ollama`. Bare `pip install -e .` skips both extras and silently breaks the chat endpoint and tool synthesis.

    ### Run the MCP server

    The same process boots the MCP server (stdio for Claude Desktop / Claude Code), the Artist Console Web UI on `:9996`, and the `/api/v1/chat` endpoint:

    ```bash
    # Daily local launch — uses FORGE_DB_URL, FORGE_LOCAL_LLM_URL, FORGE_BRIDGE_HOST defaults
    python -m forge_bridge

    # Headless host where stdin closes immediately (deploy hosts, ssh-detached sessions):
    tail -f /dev/null | python -m forge_bridge
    ```

    Flame target host override (the bridge defaults to `127.0.0.1:9999`):

    ```bash
    FORGE_BRIDGE_HOST=192.168.1.100 python -m forge_bridge
    ```

    See `docs/INSTALL.md` for the full env-var reference and the canonical operator-workstation install path.

    ### Test the connection

    Five surfaces should be reachable after install:

    ```bash
    # 1. Flame hook (requires Flame running with the hook installed)
    curl -s http://localhost:9999/status        # JSON with "flame_available": true

    # 2. MCP server CLI
    forge-bridge --help

    # 3. Artist Console Web UI
    curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"   # 200

    # 4. Chat endpoint (requires Ollama + qwen2.5-coder:32b running)
    curl -s -X POST http://localhost:9996/api/v1/chat \
      -H "content-type: application/json" \
      -d '{"messages":[{"role":"user","content":"hello"}]}'

    # 5. Post-install diagnostic (covers JSONL log, sidecar dirs, port reachability, disk)
    forge-bridge console doctor
    ```

    `ANTHROPIC_API_KEY` is **optional** — chat hardcodes `sensitive=True`, which routes through local Ollama. The key is only needed for `sensitive=False` cloud routing.

    The Flame bridge's interactive Python console is still available at `http://localhost:9999/` if you want to drive Flame's Python API directly (handy for ad-hoc debugging).
    ```

    Touch no other section in README.md. The following sections survive VERBATIM:
    - "Vision" (lines 9–13)
    - "Architecture" diagram block (lines 17–36)
    - "Current Implementation (Phase 1)" prose (lines 38–48) — out of date but Phase 21 territory
    - "Conda environment" (lines 65–91) — directionally correct
    - "### Install the Flame hook" + the curl URL + the FORGE_BRIDGE_VERSION default prose (lines 97–118) — Plan 20-01 already bumped the curl URL + prose to v1.4.1; the surrounding text is correct
    - "Repository Structure" (lines 145–176) — Phase 21 will rewrite
    - "Documentation" links (lines 180–185)
    - "Relationship to projekt-forge" (lines 189–193)
    - "Design Principles" (lines 199–208)
    - "License" (lines 211–213)

    Commit message: `docs(20): refresh README install commands + Current Status table to v1.4.1 (INSTALL-03)`
  </action>
  <verify>
    <automated>grep -q 'http://localhost:9996/ui/' README.md && grep -q '/api/v1/chat' README.md && grep -q 'forge-bridge console doctor' README.md && grep -q 'pip install -e ".\[dev,llm\]"' README.md && grep -q 'tail -f /dev/null | python -m forge_bridge' README.md && grep -q 'Artist Console' README.md && [ "$(grep -c -- '--http --port 8080' README.md)" = "0" ] && [ "$(grep -c -- '--bridge-host 192.168.1.100' README.md)" = "0" ] && [ "$(grep -c '🔧 In design' README.md)" = "0" ] && pytest tests/test_install_hook_version_consistency.py -x</automated>
  </verify>
  <done>
    - `grep -q 'http://localhost:9996/ui/' README.md` exits 0 (primary Web UI surface present)
    - `grep -q '/api/v1/chat' README.md` exits 0 (chat surface present)
    - `grep -q 'forge-bridge console doctor' README.md` exits 0 (CLI surface present in smoke tests)
    - `grep -q 'pip install -e ".\[dev,llm\]"' README.md` exits 0 ([llm] extra documented)
    - `grep -q 'tail -f /dev/null | python -m forge_bridge' README.md` exits 0 (stdin-keepalive note present)
    - `grep -q 'Artist Console' README.md` exits 0 (Current Status table includes new row)
    - `grep -q 'LLMRouter agentic' README.md` exits 0 (Current Status table includes FB-C row)
    - `grep -q 'Staged-operations platform' README.md` exits 0 (Current Status table includes FB-A/B row)
    - `grep -q 'Canonical vocabulary layer' README.md` exits 0 (corrected as Shipped, not "In design")
    - `grep -c -- '--http --port 8080' README.md` returns 0 (stale flag purged)
    - `grep -c -- '--bridge-host 192.168.1.100' README.md` returns 0 (stale flag purged)
    - `grep -c '🔧 In design' README.md` returns 0 (the misleading vocab status is gone)
    - `pytest tests/test_install_hook_version_consistency.py -x` exits 0 (Plan 20-01 consistency guard remains green)
    - `pytest tests/ -x` exits 0 (full suite still passes; no test depends on README content but the version-consistency test reads README)
    - One commit on branch with message `docs(20): refresh README install commands + Current Status table to v1.4.1 (INSTALL-03)`
    - All preserve-verbatim sections (Vision, Architecture diagram, Conda environment, Install the Flame hook, Repository Structure, Documentation, Relationship to projekt-forge, Design Principles, License) byte-identical to pre-edit (verified via diff)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → README.md | The README is the project's front-door doc. A reader who runs the documented commands trusts they will work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Spoofing | README "Current Status" table | mitigate | The current table actively misleads about what's shipped (claims canonical vocab is "In design"). The rewrite enumerates every shipped subsystem with its phase + version anchor; verify-step grep gates ensure the new rows are actually present. |
| T-20-07 | Tampering | README install command examples | mitigate | The current examples include flags that don't exist (`--http`, `--bridge-host`). A user copy-pasting them gets a broken install with no clear error. The rewrite removes the non-existent flags and shows the env-var override pattern that actually works. The 5-surface smoke test block gives the operator immediate ground truth. |
| T-20-08 | Information Disclosure | curl URL in README | accept (LOW) | The curl URL is to a public GitHub raw path at a pinned tag. No new exposure. The Plan 20-01 consistency test prevents the URL from drifting independently from the script. |
</threat_model>

<verification>
```bash
# New content present
grep -q 'http://localhost:9996/ui/' README.md
grep -q '/api/v1/chat' README.md
grep -q 'forge-bridge console doctor' README.md
grep -q 'pip install -e ".\[dev,llm\]"' README.md
grep -q 'tail -f /dev/null' README.md
grep -q 'Artist Console' README.md
grep -q 'Staged-operations platform' README.md
grep -q 'LLMRouter agentic' README.md

# Stale content purged
[ "$(grep -c -- '--http --port 8080' README.md)" = "0" ]
[ "$(grep -c -- '--bridge-host 192.168.1.100' README.md)" = "0" ]
[ "$(grep -c '🔧 In design' README.md)" = "0" ]
[ "$(grep -c 'pip install -e \\.$' README.md)" = "0" ]   # bare pip install -e . without extras

# Plan 20-01 invariant preserved
grep -c 'cnoellert/forge-bridge/v1.4.1/scripts/install-flame-hook.sh' README.md  # → 1
pytest tests/test_install_hook_version_consistency.py -x

# Preserve-verbatim sections still present
grep -q '## Vision' README.md
grep -q '## Architecture' README.md
grep -q '## Conda environment' README.md
grep -q '### Install the Flame hook' README.md
grep -q '## Repository Structure' README.md
grep -q '## Documentation' README.md
grep -q '## Relationship to projekt-forge' README.md
grep -q '## Design Principles' README.md
grep -q '## License' README.md

# Full suite green
pytest tests/ -x
```
</verification>

<success_criteria>
- [ ] "Current Status" table replaced with v1.4.1 reality (canonical vocab marked Shipped, all 5 surfaces enumerated)
- [ ] "Install the MCP server" block uses `pip install -e ".[dev,llm]"` and explains the [dev] / [llm] split
- [ ] "Run the MCP server" block removes `--http --port 8080` and `--bridge-host 192.168.1.100` and adds the stdin-keepalive note
- [ ] "Test the connection" block covers all 5 surfaces (Flame hook, MCP CLI, Web UI, chat, doctor)
- [ ] `forge-bridge console doctor` is referenced as the post-install diagnostic
- [ ] All preserve-verbatim sections are byte-identical to pre-edit
- [ ] Plan 20-01's `tests/test_install_hook_version_consistency.py` continues to pass (curl URL still v1.4.1)
- [ ] One commit on branch
- [ ] Full pytest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/20-reality-audit-canonical-install/20-03-SUMMARY.md` covering:
- Diff summary (which sections were rewritten, which were preserved verbatim)
- Confirmation that Plan 20-01's consistency test remains green
- Verification grep results
- Any drift surfaced in preserve-verbatim sections (logged as Phase 21 candidates, NOT fixed here)
- Confirmation that the README install path now matches what `docs/INSTALL.md` (Plan 20-04) will document
</output>
