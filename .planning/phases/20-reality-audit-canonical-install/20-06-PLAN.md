---
phase: 20
plan: 06
type: execute
wave: 4
depends_on: [20-05]
files_modified:
  - .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md
autonomous: false
requirements: [INSTALL-01, INSTALL-04]
nyquist_compliant: true
user_setup:
  - service: Track B host (Flame-less workstation or laptop)
    why: "Track B exists per CONTEXT.md D-01 to surface dep-presence assumptions Track A masks (e.g., 'I assumed Postgres was already running')"
    env_vars:
      - name: FORGE_DB_URL
        source: "Operator's Postgres credentials on the Track B box (or skip if Postgres not installed there — that's the gap to surface)"
    dashboard_config:
      - task: "Track B host selection"
        location: "Author's secondary machine — explicitly NOT assist-01. Must be a host without Flame installed. Postgres + Ollama presence is OPTIONAL (the absence of those is exactly what Track B surfaces)"

must_haves:
  truths:
    - "Author dry-runs docs/INSTALL.md on a Flame-less host per CONTEXT.md D-03 — surfaces dep-presence assumptions Track A masked"
    - "The 'If you don't have Flame' carveout is exercised: operator skips Step 4 and confirms Surfaces 1-4 still reach"
    - "Any 'I assumed X was already running' gap surfaced is logged in 20-HUMAN-UAT.md Track B section with disposition"
    - "Track B is NOT a milestone gate per D-03 — it exists to catch assumptions, not to gate the milestone"
  artifacts:
    - path: ".planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md"
      provides: "Track B section appended to the existing Track A UAT file"
      contains: "## Track B annotation (author-driven)"
  key_links:
    - from: "20-HUMAN-UAT.md Track B section"
      to: "docs/INSTALL.md 'If you don't have Flame' carveout"
      via: "verifying the carveout actually enables Track B success on a Flame-less host"
      pattern: "If you don't have Flame|Track B"
---

<objective>
Run the **Track B / integrator dry-run** of `docs/INSTALL.md` on a Flame-less host and append the result to the same `20-HUMAN-UAT.md` file Track A populated. Per CONTEXT.md D-03, Track B is **author-driven** (no non-author UAT required) and is **NOT a milestone gate** — it exists specifically to catch dep-presence assumptions Track A would mask (Track A runs on assist-01 where Postgres + Ollama + Flame are already in place).

Purpose: An integrator who wants to use forge-bridge as a programmatic surface (MCP server + Web UI + chat) without Flame is a real consumer pattern. The "If you don't have Flame" carveout (D-08) needs to be exercised against a real Flame-less host. If Postgres or Ollama isn't pre-installed on the target machine, the doc should either: (a) call that out clearly in "Before you start", OR (b) cover the install of those deps within Step 3. Either pattern is acceptable per D-08; what's not acceptable is the doc silently assuming both are present.

Output: A "Track B annotation" section appended to the bottom of `20-HUMAN-UAT.md` with: host details, fixture state (which deps were/weren't pre-installed), per-step observations focused on assumption gaps, the per-surface reachability outcome (Surface 5 will be SKIP per the carveout; Surfaces 1-4 should PASS), gap log specifically for Track-B-surfaced issues, and the author's PASS/FAIL signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md
@.planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md
@.planning/phases/20-reality-audit-canonical-install/20-VALIDATION.md
@docs/INSTALL.md
@.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md

<interfaces>
<!-- Track B section structure mirrors Track A but is appended after Track A sign-off. -->
<!-- The author owns this section entirely (no non-author handoff per D-03). -->

Track B host requirements:
- NOT assist-01 (assist-01 is Track A's host)
- NOT a host with Flame installed (the whole point of Track B is no-Flame)
- Postgres presence: OPTIONAL — the absence is one of the assumption gaps Track B surfaces
- Ollama presence: OPTIONAL — same reasoning
- Python 3.10+ + conda: REQUIRED (the doc cannot install conda; if conda isn't there, the operator fails before Step 1)

Surfaces expected on Track B (per docs/INSTALL.md "If you don't have Flame" carveout):
- Surface 1 (Flame hook on :9999): SKIP (no Flame)
- Surface 2 (MCP server CLI): PASS
- Surface 3 (Web UI on :9996/ui/): PASS
- Surface 4 (HTTP /api/v1/chat): PASS (requires Ollama — gap if not pre-installed)
- Surface 5 (Browser visual check): PASS

Staged-ops degradation expected on Track B:
- If Postgres is not running, `forge_*_staged` MCP tools and `/api/v1/staged` HTTP routes will fail
- The carveout in docs/INSTALL.md should mention this; if it doesn't, that's a gap

Track B section template (to be appended to the existing 20-HUMAN-UAT.md):
```markdown
---

## Track B annotation (author-driven)

**Date:** [author fills]
**Operator:** [author name + role] (Track B is author-driven per CONTEXT.md D-03)
**Host:** [hostname / description — NOT assist-01, NOT Flame-equipped]
**Branch / HEAD commit:** [HEAD SHA at Track B walk start]

### Fixture state (recorded at walk start)

- **Postgres:** [running on :5432 / not installed / installed but not running]
- **Ollama:** [running on :11434 / not installed / installed but not running]
- **qwen2.5-coder:32b:** [pulled / not pulled — only relevant if Ollama is running]
- **Flame:** [not installed — confirmed]
- **conda:** [version installed / not installed (would block at Step 1)]

### Per-step observations (focus on assumption gaps)

For each of the 8 steps in docs/INSTALL.md, record:
- Did the doc tell the operator what they needed BEFORE starting the step? (INSTALL-04)
- Did the carveout fire when expected (Step 4 — Flame hook)?
- Were there any "I assumed X was already running" surprises?

### Step 1: Prepare the conda environment
[author fills]

### Step 2: Install forge-bridge
[author fills]

### Step 3: Set up Postgres
[author fills — if Postgres wasn't pre-installed, surface that here]

### Step 4: Install the Flame hook (SKIP per carveout)
[author confirms the carveout fired and they correctly skipped]

### Step 5: Configure environment variables
[author fills]

### Step 6: Start the server
[author fills]

### Step 7: Verify all five surfaces
[author fills the table below]

### Step 8: Post-install diagnostic
[author fills — including how `forge doctor` reports Flame-bridge as `degraded` per Phase 07.1]

### Per-surface reachability outcome (Track B)

| # | Surface | Smoke command | Result | Notes |
|---|---------|--------------|--------|-------|
| 1 | Flame hook on :9999 | `curl -s http://localhost:9999/status` | SKIP | Track B — no Flame |
| 2 | MCP server CLI | `forge-bridge --help` | [PASS / FAIL] | |
| 3 | Web UI on :9996/ui/ | `curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"` | [PASS / FAIL] | |
| 4 | HTTP /api/v1/chat | `curl -s -X POST http://localhost:9996/api/v1/chat ...` | [PASS / FAIL / SKIP if no Ollama] | |
| 5 | Browser visual check | open http://localhost:9996/ui/ — confirm 4-of-5 views render (chat may fail without Ollama) | [PASS / FAIL] | |

### forge doctor output (Track B)

```
[author pastes verbatim — expect flame_bridge to show as `degraded`]
```

### Track B gap log

Track B-specific gaps (assumption gaps Track A masked). Disposition rules from Track A apply (CONTEXT.md D-04 / D-05 / D-06).

| # | Step | Gap description (assumption-flavored) | Disposition | Plan / seed that addressed it |
|---|------|---------------------------------------|-------------|-------------------------------|
| [author fills as gaps surface] | | | | |

### Track B outcome

**Track B result:** [PASS / PASS with deviations / FAIL]

**Reasoning** (author writes 2-3 sentences answering: would an integrator without Flame on the box reach Surfaces 1-4 by following this doc, OR did they need to derive deps?):

[author fills]

### Track B sign-off

[author name] — [date]
```

Track B is NOT a milestone gate per D-03. A FAIL here logs gaps for follow-up but does NOT block Phase 20 close — the milestone gate is Track A. However, gaps surfaced here that are doc-only fixes should still be patched inline before Phase 20 close (D-04 in-flight gating still applies to assumption gaps).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Track B fixture-state checklist + scaffold to 20-HUMAN-UAT.md</name>
  <files>.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md (post Plan 20-05 — Track A section is complete; append Track B after the existing Operator sign-off line)
    - .planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md (D-01, D-03, D-08)
    - .planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md (§G2 Track B annotation pattern)
    - docs/INSTALL.md (the doc the author will dry-run; particularly the "If you don't have Flame" carveout)
  </read_first>
  <action>
    Append the Track B section scaffold (from `<interfaces>` above) to the END of `.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md`, after the existing Track A "Operator sign-off" line. Do NOT modify any Track A content — Track A is already signed and committed.

    The exact append-block:

    ```markdown


    ---

    ## Track B annotation (author-driven)

    **Date:** [author fills at Track B walk start]
    **Operator:** [author name + role] (Track B is author-driven per CONTEXT.md D-03 — no non-author UAT required)
    **Host:** [hostname / description — NOT assist-01, NOT Flame-equipped]
    **Branch / HEAD commit:** [HEAD SHA at Track B walk start]

    ### Fixture state (recorded at walk start)

    - **Postgres:** [running on :5432 / not installed / installed but not running]
    - **Ollama:** [running on :11434 / not installed / installed but not running]
    - **qwen2.5-coder:32b:** [pulled / not pulled — only relevant if Ollama is running]
    - **Flame:** [not installed — confirmed]
    - **conda:** [version installed / not installed (would block at Step 1)]

    ### Per-step observations (focus on assumption gaps)

    For each step, the question is: *did the doc tell the operator what they needed BEFORE starting, or did they hit it mid-install?* (INSTALL-04)

    #### Step 1: Prepare the conda environment
    [author fills]

    #### Step 2: Install forge-bridge
    [author fills]

    #### Step 3: Set up Postgres
    [author fills — if Postgres wasn't pre-installed on the Track B host, surface that as the central assumption gap of this track]

    #### Step 4: Install the Flame hook (SKIP per carveout)
    [author confirms the carveout fired correctly: doc told them to skip; they skipped; nothing downstream broke]

    #### Step 5: Configure environment variables
    [author fills]

    #### Step 6: Start the server
    [author fills]

    #### Step 7: Verify all five surfaces
    [author fills the per-surface table below]

    #### Step 8: Post-install diagnostic
    [author fills — confirm that `forge doctor` reports `flame_bridge` as `degraded` (not `failed`) per Phase 07.1 graceful-degradation contract]

    ### Per-surface reachability outcome (Track B)

    | # | Surface | Smoke command | Result | Notes |
    |---|---------|--------------|--------|-------|
    | 1 | Flame hook on :9999 | `curl -s http://localhost:9999/status` | SKIP | Track B — no Flame, expected per carveout |
    | 2 | MCP server CLI | `forge-bridge --help` | [PASS / FAIL] | |
    | 3 | Web UI on :9996/ui/ | `curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"` | [PASS / FAIL] | |
    | 4 | HTTP /api/v1/chat | `curl -s -X POST http://localhost:9996/api/v1/chat -H "content-type: application/json" -d '{"messages":[{"role":"user","content":"hello"}]}'` | [PASS / FAIL / SKIP if no Ollama] | |
    | 5 | Browser visual check | open http://localhost:9996/ui/ — confirm 4-of-5 views render (chat may fail without Ollama) | [PASS / FAIL] | |

    ### forge doctor output (Track B)

    ```
    [author pastes verbatim — expect flame_bridge to show as `degraded`, not `failed`]
    ```

    ### Track B gap log

    Track B-specific gaps (the assumption gaps Track A masked because assist-01 already had everything pre-installed). Disposition rules from Track A apply (CONTEXT.md D-04 / D-05 / D-06):
    - **doc-only** → patch INSTALL.md inline; re-walk affected step
    - **code-fix (small)** → spin a Phase 20 follow-up plan
    - **code-fix (large)** → spin a 20.x decimal phase per D-05
    - **deferred (v1.6+)** → log + plant a seed

    | # | Step | Gap description (assumption-flavored) | Disposition | Plan / seed that addressed it |
    |---|------|---------------------------------------|-------------|-------------------------------|
    | [author fills as gaps surface] | | | | |

    ### Track B outcome

    **Track B result:** [PASS / PASS with deviations / FAIL]

    **Reasoning** (author writes 2-3 sentences answering: would an integrator without Flame on the box reach Surfaces 1-4 by following this doc, OR did they need to derive dependencies the doc didn't surface?):

    [author fills]

    **Note on milestone-gate impact:** Track B is NOT a milestone gate per CONTEXT.md D-03. Track B FAIL does NOT block Phase 20 close. However, doc-only fixes triggered by Track B gaps should still land before close (D-04 in-flight gating still applies to assumption gaps the integrator path surfaces).

    ### Track B sign-off

    [author name] — [date]
    ```

    Then commit the appended scaffold. Do NOT change the file's YAML frontmatter `status:` line — that reflects Track A's outcome. Update `updated:` timestamp to the current ISO8601 to reflect the file was modified.

    Commit message: `docs(20): append Track B integrator dry-run scaffold to UAT`
  </action>
  <verify>
    <automated>grep -q '## Track B annotation (author-driven)' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q 'NOT a milestone gate per CONTEXT.md D-03' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q "If you don't have Flame\\|carveout" .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q 'Track B sign-off' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q '## Operator sign-off' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md</automated>
  </verify>
  <done>
    - Track B annotation section appended to existing 20-HUMAN-UAT.md
    - Track A "Operator sign-off" section still present (Track A content untouched)
    - Track B section includes: fixture state checklist, per-step observation scaffold, per-surface reachability table (with SKIP pre-filled for Surface 1), forge doctor output placeholder, gap log table, outcome line, sign-off line
    - One commit on branch
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Author dry-runs docs/INSTALL.md on a Flame-less host and completes Track B section</name>
  <files>.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md, docs/INSTALL.md (any inline doc-only fixes patched mid-walk)</files>
  <action>
    This is a CHECKPOINT — author runs the dry-run on a Flame-less host (NOT assist-01) per D-03 author-driven Track. Full step-by-step instructions for the author are in the `<how-to-verify>` block; do not duplicate here. Track B is NOT a milestone gate per D-03 — a FAIL does not block Phase 20 close, but doc-only assumption-gap fixes still land inline before close.
  </action>
  <verify>
    <automated>grep -q '^### Track B sign-off$' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q 'Track B result:' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && [ "$(grep -c '\[author fills\]' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md)" -lt "5" ]</automated>
  </verify>
  <done>
    - Track B section in `20-HUMAN-UAT.md` populated (most `[author fills]` placeholders replaced)
    - Per-surface reachability table populated for Track B (Surface 1 = SKIP, others recorded)
    - `Track B result:` line filled with PASS / PASS-with-deviations / FAIL + reasoning
    - Author signed and dated Track B sign-off
    - Track A section unmodified
    - File committed to git
    - Plan 20-01 consistency test still green
  </done>
  <what-built>
    - Track A non-author UAT is signed off (PASS or PASS-with-deviations) per Plan 20-05
    - All Phase 20 doc + script + version-bump deliverables are landed
    - Track B scaffold is appended to 20-HUMAN-UAT.md
  </what-built>
  <how-to-verify>
    **Run the Track B dry-run on a Flame-less host.** This is author-driven per D-03 — no non-author handoff required. The author IS the operator for Track B.

    **Author instructions:**

    1. Pick a host that is NOT assist-01 and does NOT have Flame installed. Examples: a personal laptop, a development VM, a colleague's workstation borrowed for the dry-run.
    2. Open `docs/INSTALL.md` at HEAD on the Phase 20 branch.
    3. Open `.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md` — fill the Track B section as you go.
    4. Record fixture state at walk start: which of Postgres / Ollama / qwen2.5-coder:32b / conda are pre-installed, which are not. Confirm Flame is NOT installed.
    5. Walk every step in `docs/INSTALL.md` IN ORDER. At Step 4, confirm the "If you don't have Flame" carveout fires correctly — you read the carveout, you skip the step, the doc told you which surfaces won't be reachable.
    6. For EACH step, record any **assumption gap**: anything the doc assumed was pre-installed that wasn't on this host, OR anything that worked on assist-01 (Track A) but didn't here.
    7. After Step 8, fill the **Per-surface reachability outcome (Track B)** table. Surface 1 will be SKIP (Flame not installed). Surfaces 2-3-5 should PASS. Surface 4 (chat) PASSES if Ollama + qwen2.5-coder are present, otherwise SKIP.
    8. Paste the verbatim `forge-bridge console doctor` output. Confirm `flame_bridge` is reported as `degraded` (not `failed`) per Phase 07.1.
    9. For each gap surfaced, log it with disposition (doc-only / code-fix / deferred) per the Track A rules. Doc-only fixes still get patched inline.
    10. Fill the **Track B outcome** with PASS / PASS-with-deviations / FAIL + 2-3 sentences answering: *would an integrator without Flame reach Surfaces 1-4 by following this doc?*
    11. Sign and date the **Track B sign-off** line.
    12. Commit the populated Track B section.

    **Important:** Track B FAIL does NOT block Phase 20 close. The milestone gate is Track A (already passed in Plan 20-05). Track B exists to surface assumption gaps for future-doc-iteration purposes. Doc-only fixes should still land inline; large code-fix gaps that weren't milestone-gate blockers can be deferred to v1.6+ with a seed.

    **Resume signal:** type `approved` once the author has completed the Track B walk, signed off, and committed the populated Track B section. Type `failed: <reason>` if the dry-run produced a FAIL — Phase 20 close is NOT blocked, but the failure reason should be captured for follow-up planning.
  </how-to-verify>
  <resume-signal>Type `approved` once the author has completed the Track B dry-run, signed off, and committed the populated Track B section to `20-HUMAN-UAT.md`. Type `failed: <one-line reason>` if the dry-run surfaced gaps that warrant follow-up — Phase 20 close is NOT blocked by Track B per D-03, but the reason should be captured.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → Track B host | Author runs commands on a different machine than the reference setup. Misconfigured environment could mask gaps OR introduce false negatives — but that's exactly what Track B exists to catch. |
| author → INSTALL.md | Author reads their own doc on a fresh host. Comprehension blind spots may persist (the structural reason Track A is non-author), but assumption gaps DO surface here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-13 | Repudiation | Track B outcome | mitigate | Track B section is committed to git with author sign-off — provides an audit trail. Pattern matches the Track A artifact discipline. |
| T-20-14 | Information Disclosure | Track B host details | accept (LOW) | Track B host description in the file may include hostname / OS — these are operational details the author can choose to obscure (e.g., "personal laptop, macOS 14") if desired. No secrets are recorded. |
</threat_model>

<verification>
After the checkpoint resolves:

```bash
# Track B section is populated, not just scaffold
test -f .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md
grep -q '^### Track B sign-off$' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md
[ "$(grep -c '\[author fills\]' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md)" -lt "5" ]   # most placeholders filled

# Track A still intact
grep -q '^## Operator sign-off$' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md

# Per-surface reachability table for Track B has results
grep -q 'Track B — no Flame' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md   # Surface 1 SKIP recorded

# All version-consistency invariants from prior plans still hold
pytest tests/test_install_hook_version_consistency.py -x
pytest tests/ -x
```
</verification>

<success_criteria>
- [ ] Track B section appended to `20-HUMAN-UAT.md` with all author-fillable placeholders populated
- [ ] Track A section unmodified (still signed by Track A non-author)
- [ ] Track B fixture state recorded (which deps were/weren't pre-installed; Flame absence confirmed)
- [ ] All 8 steps observed with focus on assumption gaps
- [ ] "If you don't have Flame" carveout exercised at Step 4 (carveout fired correctly)
- [ ] Per-surface reachability table: Surface 1 = SKIP, Surfaces 2-3-5 PASS expected, Surface 4 PASS-or-SKIP-on-Ollama-absence
- [ ] `forge doctor` output captured; `flame_bridge` shows as `degraded`
- [ ] Gap log filled for any assumption gaps surfaced
- [ ] Track B outcome line filled with PASS / PASS-with-deviations / FAIL + reasoning
- [ ] Author signed and dated Track B sign-off
- [ ] Any doc-only fixes triggered by Track B gaps patched inline before close
- [ ] File committed to git
- [ ] Plan 20-01 consistency test still green
- [ ] Full pytest suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/20-reality-audit-canonical-install/20-06-SUMMARY.md` covering:
- Track B host description (general — laptop, VM, etc.)
- Track B fixture state (which deps were pre-installed, which weren't)
- Per-surface reachability summary (expected: Surface 1 SKIP; 2-3-5 PASS; 4 conditional)
- Assumption gaps surfaced (specifically the ones Track A masked)
- Doc-only fixes landed inline as part of Track B
- Track B outcome (PASS / PASS-with-deviations / FAIL) + author reasoning
- Confirmation that the milestone gate (Track A) is still cleared regardless of Track B outcome
- Reflection: what assumption patterns the doc should better surface for v1.6+ iteration (carry-forward to Phase 21 / SEED)
</output>
