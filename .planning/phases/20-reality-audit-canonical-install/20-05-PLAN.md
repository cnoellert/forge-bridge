---
phase: 20
plan: 05
type: execute
wave: 4
depends_on: [20-01, 20-02, 20-03, 20-04]
files_modified:
  - .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md
autonomous: false
requirements: [INSTALL-01, INSTALL-02, INSTALL-03, INSTALL-04, DOCS-02]
nyquist_compliant: true
user_setup:
  - service: assist-01 (operator workstation)
    why: "Track A non-author UAT requires the reference operator-workstation environment with Flame + Postgres + Ollama already running"
    env_vars:
      - name: FORGE_DB_URL
        source: "Operator's Postgres credentials (default forge:forge@localhost:5432/forge_bridge if matching the reference setup)"
      - name: ANTHROPIC_API_KEY
        source: "OPTIONAL — only if operator wants to test sensitive=False cloud routing during walk; daily workflow does not require it"
    dashboard_config:
      - task: "Pre-walk fixture setup (author runs before handing to non-author)"
        location: "assist-01 — `conda env remove -n forge` (if exists) → `conda create -n forge python=3.11 -y`; verify Postgres + Ollama daemons running; verify qwen2.5-coder:32b pulled in Ollama; checkout the Phase 20 branch HEAD"
      - task: "Non-author identification + scheduling"
        location: "Coordinate with the non-author UAT operator (see CONTEXT.md D-02 — must be a non-author per Phase 10.1 D-36 / Phase 16.1 / Phase 16.2 precedent)"

must_haves:
  truths:
    - "A non-author follows docs/INSTALL.md verbatim on a fresh conda env on assist-01 and reaches all 5 surfaces"
    - "Every gap encountered during the walk is logged in 20-HUMAN-UAT.md with disposition (doc-only fix vs code-fix vs deferred)"
    - "Doc-only and small code-fix gaps surface inline as Phase 20 follow-up plans (D-04 in-flight gating)"
    - "Gaps exceeding ~1 plan worth of code spin a 20.x decimal phase (D-05 soft cap)"
    - "20-HUMAN-UAT.md captures the canonical PASS / PASS-with-deviations / FAIL outcome with non-author sign-off"
  artifacts:
    - path: ".planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md"
      provides: "Track A non-author UAT record + gap log (the milestone gate per CONTEXT.md D-02)"
      contains: "Outcome: PASS"
      min_lines: 80
  key_links:
    - from: "20-HUMAN-UAT.md outcome"
      to: "Phase 20 milestone gate"
      via: "non-author sign-off + 5/5 surfaces reachable"
      pattern: "Outcome:|Operator sign-off"
---

<objective>
Run the **Track A non-author UAT** of `docs/INSTALL.md` on assist-01 and record the result in `20-HUMAN-UAT.md`. This is the milestone-gate moment for Phase 20 — per CONTEXT.md D-02, the non-author UAT pass is the locked acceptance criterion. "Passes for the author" does not count.

Purpose: The forcing function of v1.5 ships through this UAT. If a fresh-eyes operator on a clean conda env on assist-01 cannot follow the doc verbatim and reach all 5 surfaces, the doc isn't done. Per D-04, gaps surfaced during the walk are fixed in-flight as Phase 20 follow-up plans (NOT deferred to v1.6+). Per D-05, gaps that balloon beyond ~1 plan's worth of code spin a decimal phase (20.1, 20.2, ...).

Output: `.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md` populated with the Track A walkthrough, per-step observations, gap log, per-surface reachability table, `forge doctor` output, deviations, outcome, and operator sign-off. Pattern source: `.planning/milestones/v1.4-phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md` (per RESEARCH §G2).

This plan has TWO tasks: (1) the **author** writes the UAT scaffold (frontmatter, pre-walk setup checklist, blank walkthrough table) and prepares the assist-01 fixture, (2) the **checkpoint:human-verify** task hands the doc to the non-author and pauses for the non-author to walk through it and complete the file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md
@.planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md
@.planning/phases/20-reality-audit-canonical-install/20-VALIDATION.md
@.planning/phases/20-reality-audit-canonical-install/20-04-PLAN.md
@docs/INSTALL.md

<interfaces>
<!-- The UAT artifact pattern is sourced from prior HUMAN-UAT.md files. -->
<!-- 16.2-HUMAN-UAT.md is the most complete example (see RESEARCH §G1). -->

YAML frontmatter pattern (per RESEARCH §G2):
```yaml
---
status: pending
phase: 20-reality-audit-canonical-install
source: [20-01-SUMMARY.md, 20-02-SUMMARY.md, 20-03-SUMMARY.md, 20-04-SUMMARY.md, 20-CONTEXT.md, 20-RESEARCH.md]
started: <ISO8601 — non-author fills at walk start>
updated: <ISO8601 — non-author fills at walk completion>
track: A
---
```

Section order (mirror 16.2-HUMAN-UAT.md exactly):
1. Header (Date, Operator, Host, Branch, INSTALL.md commit, Reference versions discovered)
2. Pre-walk setup checklist (author-prepared)
3. Fixture state (services running at walk start)
4. Walk-through (numbered steps 1-8 from INSTALL.md, what the operator typed, observed verbatim)
5. Per-surface reachability outcome table
6. Gap log (gaps surfaced; each gets a disposition: doc-only / code-fix / deferred)
7. `forge doctor` output (paste verbatim)
8. Deviations (numbered, justified)
9. Outcome (PASS / PASS with deviations / FAIL + reasoning)
10. Action (next steps based on outcome)
11. Operator sign-off

Pre-walk setup that the author must complete on assist-01 BEFORE handing to the non-author:
- Remove any pre-existing `forge` conda env: `conda env remove -n forge` (if it exists)
- Verify Postgres daemon is running on `localhost:5432`
- Verify Ollama daemon is running on `localhost:11434`
- Verify `qwen2.5-coder:32b` is pulled: `ollama list | grep qwen2.5-coder`
- Verify Flame is installed and the v1.4.1 hook can be loaded (delete any prior Flame hook file at `/opt/Autodesk/shared/python/forge_bridge/scripts/forge_bridge.py` so the install step actually does work)
- Check out the branch with all Phase 20 plans 20-01 through 20-04 landed
- Capture the HEAD commit SHA for the UAT header

Per-surface reachability table template (the non-author fills the Result + Notes columns during the walk):
| # | Surface | Smoke command | Result | Notes |
|---|---------|--------------|--------|-------|
| 1 | Flame hook on :9999 | `curl -s http://localhost:9999/status` | PASS / FAIL / SKIP | |
| 2 | MCP server CLI | `forge-bridge --help` | PASS / FAIL | |
| 3 | Web UI on :9996/ui/ | `curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"` | PASS / FAIL | |
| 4 | HTTP /api/v1/chat | `curl -s -X POST http://localhost:9996/api/v1/chat ...` | PASS / FAIL | |
| 5 | Browser visual check | open http://localhost:9996/ui/ in browser; confirm 5 views render | PASS / FAIL | |

Gap log table template (filled during walk; each row triggers a Phase 20 follow-up decision):
| # | Step | Gap description | Disposition (doc-only / code-fix / deferred) | Plan that addressed it |
|---|------|----------------|---------------------------------------------|-----------------------|
| 1 | | | | |

D-04 / D-05 gap-fix decision tree (state explicitly in the file so the non-author understands the rules):
- **Doc-only gap** (typo, missing step, ambiguous wording) → patch INSTALL.md inline as part of this plan; re-walk affected step
- **Small code-fix gap** (≤1 plan worth of code, ≤5 files modified, no architectural change) → spin Plan 20-06 (or 20-07, 20-08...) inside Phase 20 per D-04
- **Large code-fix gap** (substantive feature, new healthcheck, config-file contract redesign, etc.) → spin a 20.1 / 20.2 decimal phase per D-05
- **Genuinely v1.6+ gap** (auth, multi-machine, streaming) → log here, defer with seed file per D-06

The non-author UAT is the milestone gate. Track A FAIL means Phase 20 does not close. Track A PASS / PASS with deviations means the doc is good enough to ship.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 20-HUMAN-UAT.md scaffold + prep assist-01 fixture</name>
  <files>.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v1.4-phases/16.2-bug-d-chat-tool-call-loop/16.2-HUMAN-UAT.md (the analog — RESEARCH §G1 + 20-PATTERNS.md)
    - .planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md (§G2 recommended template)
    - .planning/phases/20-reality-audit-canonical-install/20-PATTERNS.md (§ 20-HUMAN-UAT.md)
    - .planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md (D-02, D-04, D-05, D-06)
    - docs/INSTALL.md (the doc the non-author will walk; needed to populate the per-step walkthrough scaffold)
  </read_first>
  <action>
    Create `.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md` with the scaffold below VERBATIM. The non-author fills the bracketed `[…]` placeholders during the walk. The author leaves the file in `status: pending` until the non-author signs off.

    File content (verbatim — write this entire block to disk):

    ```markdown
    ---
    status: pending
    phase: 20-reality-audit-canonical-install
    source: [20-01-SUMMARY.md, 20-02-SUMMARY.md, 20-03-SUMMARY.md, 20-04-SUMMARY.md, 20-CONTEXT.md, 20-RESEARCH.md, docs/INSTALL.md]
    started: [non-author fills ISO8601 at walk start]
    updated: [non-author fills ISO8601 at walk completion]
    track: A
    ---

    # Phase 20 — Track A INSTALL.md Walk-through (Non-Author UAT)

    **Date:** [non-author fills]
    **Operator:** [non-author name + role / relation to project]
    **Host:** assist-01 (Flame + Postgres + Ollama pre-installed; fresh conda env `forge` created for this walk)
    **Branch / HEAD commit:** [author fills HEAD SHA when handing to non-author]
    **INSTALL.md commit:** [same as branch HEAD unless explicitly different]

    ## Reference versions discovered during walk

    | Dependency | Version found |
    |------------|--------------|
    | Python (after `conda activate forge`) | [non-author fills] |
    | conda | [non-author fills] |
    | PostgreSQL | [non-author fills — `psql --version`] |
    | Ollama | [non-author fills — `ollama --version`] |
    | qwen2.5-coder:32b pulled? | [non-author fills — yes/no, from `ollama list`] |
    | Flame | [non-author fills if Track A; SKIP if Track B] |
    | forge-bridge `__version__` (after Step 2) | [non-author fills — should be 1.4.1] |

    ## Pre-walk setup (author-prepared, ✓ when complete)

    - [ ] Pre-existing `forge` conda env removed (`conda env remove -n forge` if it existed)
    - [ ] Postgres daemon verified running on `localhost:5432`
    - [ ] Ollama daemon verified running on `localhost:11434`
    - [ ] `qwen2.5-coder:32b` verified pulled (`ollama list | grep qwen2.5-coder`)
    - [ ] Any prior Flame hook removed (`rm -f /opt/Autodesk/shared/python/forge_bridge/scripts/forge_bridge.py` so the install step does real work)
    - [ ] Phase 20 branch checked out at HEAD with Plans 20-01 through 20-04 landed
    - [ ] `docs/INSTALL.md` open in a separate window for the non-author to follow

    ## Fixture state (recorded by non-author at walk start)

    What was running on the box when the walk started, and what wasn't:

    - **Postgres:** [running on :5432 / not running / unsure]
    - **Ollama:** [running on :11434 / not running / unsure]
    - **Flame:** [running with v1.4.1 hook installed / running with prior hook / not running]
    - **forge conda env:** [exists from author prep / missing — non-author creates in Step 1]
    - **forge-bridge:** [installed in this env? — if yes, NOT a fresh walk; surface this as a deviation]

    ## Walk-through (non-author runs verbatim)

    Walk `docs/INSTALL.md` top to bottom. For EACH numbered step, record:
    - The exact command pasted (verbatim)
    - The observable output (verbatim, OR "as expected" if it matched the doc's prediction)
    - Any error, warning, or surprise (verbatim)
    - Time elapsed if longer than 10s

    ### Step 1: Prepare the conda environment
    [non-author fills — commands run, output observed, any deviation from doc]

    ### Step 2: Install forge-bridge
    [non-author fills]

    ### Step 3: Set up Postgres
    [non-author fills]

    ### Step 4: Install the Flame hook (Track A only)
    [non-author fills; SKIP if Track B]

    ### Step 5: Configure environment variables
    [non-author fills]

    ### Step 6: Start the server
    [non-author fills — including time-to-ready]

    ### Step 7: Verify all five surfaces
    [non-author fills the per-surface table below]

    ### Step 8: Post-install diagnostic
    [non-author fills — paste `forge-bridge console doctor` output verbatim in the section below]

    ## Per-surface reachability outcome

    | # | Surface | Smoke command | Result | Notes |
    |---|---------|--------------|--------|-------|
    | 1 | Flame hook on :9999 | `curl -s http://localhost:9999/status` | [PASS / FAIL / SKIP] | |
    | 2 | MCP server CLI | `forge-bridge --help` | [PASS / FAIL] | |
    | 3 | Web UI on :9996/ui/ | `curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"` | [PASS / FAIL] | |
    | 4 | HTTP /api/v1/chat | `curl -s -X POST http://localhost:9996/api/v1/chat -H "content-type: application/json" -d '{"messages":[{"role":"user","content":"hello"}]}'` | [PASS / FAIL] | |
    | 5 | Browser visual check | open http://localhost:9996/ui/ — confirm 5 views render (tools, execs, manifest, health, chat) | [PASS / FAIL] | |

    ## forge doctor output

    ```
    [non-author pastes `forge-bridge console doctor` output verbatim here]
    ```

    ## Gap log

    Each gap surfaced during the walk gets a disposition per CONTEXT.md D-04 / D-05 / D-06:
    - **doc-only** → patch INSTALL.md inline; re-walk the affected step. Do NOT close the UAT until the patched doc walks clean.
    - **code-fix (≤1 plan)** → spin a Phase 20 follow-up plan (20-06, 20-07, ...). Block the UAT until the code-fix plan lands.
    - **code-fix (>1 plan)** → spin a 20.1 decimal phase per D-05. UAT outcome is FAIL until the decimal phase closes.
    - **v1.6+ deferred** → log here, plant a seed in `.planning/seeds/`. Do not block the UAT.

    | # | Step | Gap description | Disposition | Plan / seed that addressed it |
    |---|------|----------------|-------------|-------------------------------|
    | [non-author + author fill collaboratively as gaps surface] | | | | |

    ## Deviations

    Anything the non-author did that wasn't strictly verbatim from INSTALL.md, with justification:

    1. [non-author + author fill collaboratively]

    ## Outcome

    **Track A result:** [PASS / PASS with deviations / FAIL]

    **Reasoning** (non-author writes 2-3 sentences answering: could a first-time user follow this doc without deriving the topology themselves?):

    [non-author fills]

    ## Action

    Based on the outcome:
    - **PASS** → Phase 20 milestone gate cleared. Proceed to Plan 20-06 (Track B dry-run).
    - **PASS with deviations** → review deviations with author; if all are acceptable per the Phase 10.1 / 16.1 / 16.2 PASS-with-deviations precedent, proceed.
    - **FAIL** → fix gaps via the disposition rules above; re-walk; do not close Phase 20 until a clean PASS or PASS-with-deviations is recorded.

    ## Operator sign-off

    [non-author name] — [date]
    ```

    Then run the assist-01 fixture prep:

    ```bash
    # On assist-01 (or in a documented prep ticket the non-author runs at walk start):
    conda env remove -n forge 2>/dev/null   # idempotent — succeeds if env doesn't exist
    rm -f /opt/Autodesk/shared/python/forge_bridge/scripts/forge_bridge.py   # idempotent
    pg_isready -h localhost -p 5432         # confirms Postgres is up
    ollama list | grep -q qwen2.5-coder:32b  # confirms model is pulled
    git rev-parse HEAD                       # captures HEAD SHA for the UAT header
    ```

    Update the `20-HUMAN-UAT.md` "Branch / HEAD commit" line with the SHA from the last command BEFORE handing to the non-author.

    Commit message: `docs(20): scaffold Track A non-author UAT for INSTALL.md`
  </action>
  <verify>
    <automated>test -f .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q '^status: pending$' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q '^track: A$' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q 'Per-surface reachability outcome' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q 'Gap log' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q 'Operator sign-off' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && [ "$(wc -l < .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md)" -ge "80" ]</automated>
  </verify>
  <done>
    - `20-HUMAN-UAT.md` exists with status `pending`, track `A`, and the full scaffold structure
    - All 11 sections present (header, fixture state, pre-walk setup, walk-through Steps 1-8, reachability table, doctor output, gap log, deviations, outcome, sign-off)
    - HEAD SHA recorded in the file's "Branch / HEAD commit" line
    - assist-01 fixture prep complete (conda env removed, prior Flame hook removed, Postgres + Ollama + qwen2.5-coder verified)
    - One commit on branch
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Non-author walks docs/INSTALL.md verbatim and completes 20-HUMAN-UAT.md</name>
  <files>.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md, docs/INSTALL.md (any inline doc-only fixes patched mid-walk)</files>
  <action>
    This is a CHECKPOINT — handoff to the non-author UAT operator. Author preparation tasks before the handoff are listed in `<how-to-verify>` below; the non-author then walks `docs/INSTALL.md` end-to-end on assist-01 and populates the scaffold from Task 1. Author triages gaps in real time per CONTEXT.md D-04 / D-05 / D-06 dispositions. The full step-by-step instructions for the non-author are in the `<how-to-verify>` block — do not duplicate them here.
  </action>
  <verify>
    <automated>grep -q '^status: \(passed\|partial\|failed\)$' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && grep -q '^Outcome: \?\(PASS\|FAIL\)' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md && [ "$(grep -c 'PASS / FAIL' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md)" = "0" ]</automated>
  </verify>
  <done>
    - `20-HUMAN-UAT.md` YAML frontmatter `status:` flipped from `pending` to `passed` / `partial` / `failed`
    - Per-surface reachability table populated (no `[PASS / FAIL]` placeholders)
    - `Outcome:` line filled with PASS / PASS-with-deviations / FAIL + reasoning
    - Non-author signed and dated
    - File committed to git
    - Plan 20-01 consistency test still green
  </done>
  <what-built>
    - Plans 20-01 through 20-04 have landed: pyproject.toml + tests + script + README + CLAUDE.md + docs/INSTALL.md all reflect v1.4.1 reality
    - `docs/INSTALL.md` is the canonical operator-workstation install path
    - `20-HUMAN-UAT.md` scaffold is in place with assist-01 fixture prepped
  </what-built>
  <how-to-verify>
    **Hand the doc to the non-author.** They run the entire walk on assist-01 in a fresh conda env (the author's prep guaranteed a clean starting state). The non-author owns the file from this point until sign-off.

    **Non-author instructions** (give these to the non-author at handoff):

    1. Open `docs/INSTALL.md` at HEAD on the Phase 20 branch.
    2. Open `.planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md` in a separate editor — you will fill it in as you walk.
    3. Set the `started:` ISO8601 timestamp in the YAML frontmatter at walk start.
    4. Walk every step in `docs/INSTALL.md` IN ORDER, top to bottom. Do not skip ahead. Do not improvise.
    5. For each step, record what you typed and what you observed in the corresponding `### Step N:` section of the UAT file.
    6. When you hit a gap (something the doc didn't tell you, something that didn't work, something ambiguous), STOP and log it in the **Gap log** table with a proposed disposition (doc-only / code-fix / deferred).
       - **doc-only**: pause the walk, tell the author, let them patch INSTALL.md, then re-walk the affected step.
       - **code-fix (small)**: pause the walk, tell the author, let them spin a follow-up Phase 20 plan; resume after it lands.
       - **code-fix (large)**: pause the walk, tell the author, let them spin a 20.x decimal phase. The UAT outcome is FAIL until that decimal phase closes.
       - **deferred (v1.6+)**: log it and continue — do not block on it.
    7. After Step 8, fill the **Per-surface reachability outcome** table with PASS/FAIL/SKIP for each of the 5 surfaces.
    8. Paste the verbatim `forge-bridge console doctor` output into the **forge doctor output** section.
    9. Fill the **Outcome** section with PASS / PASS-with-deviations / FAIL plus 2-3 sentences answering: *could a first-time user follow this doc without deriving the topology themselves?*
    10. Set the `updated:` ISO8601 timestamp in the YAML frontmatter at walk completion.
    11. Update the YAML frontmatter `status:` to match outcome (`passed` / `partial` / `failed`).
    12. Sign and date the **Operator sign-off** line.
    13. Commit the completed `20-HUMAN-UAT.md`.

    **Author instructions during the walk:**
    - Be available to triage gaps as they surface.
    - When a gap warrants a doc-only or small-code-fix plan, spin it inline (Plan 20-06+ if needed). The non-author waits while it lands, then re-walks the affected step.
    - Do NOT prompt the non-author or guide them through any step. The whole point of a non-author UAT is to surface comprehension gaps the author can't see.

    **Resume signal:** type `approved` after the non-author has signed off on `20-HUMAN-UAT.md` with PASS or PASS-with-deviations and the file is committed. Type `failed: <reason>` if the walk produced a FAIL — Phase 20 stays open until follow-up plans land and the UAT is re-run to PASS.
  </how-to-verify>
  <resume-signal>Type `approved` once the non-author has completed the walk, signed off, and committed the populated `20-HUMAN-UAT.md` with a PASS or PASS-with-deviations outcome. Type `failed: <one-line reason>` if the walk produced a FAIL — Phase 20 will stay open until follow-up plans land.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → non-author | The author hands a doc to a non-author and trusts they will surface gaps honestly. The non-author trusts the author to fix what they surface. The whole point of the boundary is to catch what the author can't see. |
| non-author → assist-01 | The non-author runs commands from the doc against a real workstation with Flame + Postgres + Ollama. Misconfigured environment could mask gaps OR introduce false negatives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Repudiation | UAT outcome | mitigate | The HUMAN-UAT.md file is committed to git with non-author sign-off — provides an audit trail of who walked the doc, when, with what outcome. Pattern matches Phase 10.1 D-36 / Phase 16.1 / Phase 16.2 precedent. |
| T-20-11 | Tampering | Pre-walk fixture state | mitigate | The author records the pre-walk fixture-prep checklist at the top of the UAT file (Postgres up, Ollama up, qwen2.5-coder pulled, prior Flame hook removed, prior conda env removed). The non-author confirms the fixture state at walk start. If the fixture diverges, the deviation is logged. |
| T-20-12 | Spoofing | Doc-only fixes mid-walk | mitigate | Doc-only patches happen between non-author steps. The non-author re-walks the affected step after each patch — they don't accept the author's word that "it's fixed now." |
</threat_model>

<verification>
After the checkpoint resolves:

```bash
# UAT file exists and is in a terminal state
test -f .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md
grep -q '^status: \(passed\|partial\)$' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md   # not still 'pending'
grep -q '^Outcome:' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md  # outcome line filled
grep -q 'Operator sign-off' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md

# Per-surface reachability table has results (no [PASS / FAIL] placeholders left)
[ "$(grep -c '\[PASS / FAIL\]' .planning/phases/20-reality-audit-canonical-install/20-HUMAN-UAT.md)" = "0" ]

# All version-consistency invariants from prior plans still hold
pytest tests/test_install_hook_version_consistency.py -x
pytest tests/ -x

# Any in-flight gap-fix plans landed (check by greping the gap log for 'addressed by')
# (Soft check — only meaningful if gaps were surfaced)
```
</verification>

<success_criteria>
- [ ] `20-HUMAN-UAT.md` exists with full scaffold + all sections populated by the non-author
- [ ] Track A walk completed end-to-end (or PARTIAL with documented reasons)
- [ ] Per-surface reachability table populated for all 5 surfaces (PASS / FAIL / SKIP, no placeholder text)
- [ ] `forge doctor` output captured verbatim in the file
- [ ] Gap log populated for every gap surfaced; each has a disposition + the plan/seed that addressed it
- [ ] Any doc-only fixes patched inline in `docs/INSTALL.md` with re-walk confirmation
- [ ] Any small code-fix gaps spun as Phase 20 follow-up plans (20-07, 20-08...) landed and re-verified
- [ ] Any large code-fix gaps spun as 20.x decimal phases per D-05
- [ ] `Outcome:` line filled with PASS / PASS-with-deviations / FAIL + reasoning
- [ ] Non-author signed and dated
- [ ] YAML frontmatter `status:` matches outcome (`passed` / `partial` / `failed`)
- [ ] File committed to git
- [ ] Plan 20-01 consistency test still green
- [ ] Full pytest suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/20-reality-audit-canonical-install/20-05-SUMMARY.md` covering:
- UAT outcome (PASS / PASS-with-deviations / FAIL)
- Non-author identity + role
- Reference versions discovered during walk
- Per-surface reachability summary (all 5 PASS, or breakdown)
- Gap log summary: count of gaps by disposition (doc-only / code-fix / deferred), and the Phase 20 follow-up plans they triggered
- Any 20.x decimal phases spun (per D-05)
- Confirmation that the milestone gate is cleared (PASS or PASS-with-deviations) OR the explicit blockers preventing close (FAIL)
- Reflection: which parts of the doc worked best vs. which surfaced the most friction
</output>
