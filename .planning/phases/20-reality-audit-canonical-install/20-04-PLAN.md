---
phase: 20
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - docs/INSTALL.md
autonomous: true
requirements: [INSTALL-01, INSTALL-02, INSTALL-03, INSTALL-04]
nyquist_compliant: true

must_haves:
  truths:
    - "An operator with Flame + Postgres + Ollama + a fresh conda env can follow docs/INSTALL.md verbatim and reach a working forge-bridge install (INSTALL-01)"
    - "All five surfaces (Web UI :9996/ui/, CLI forge-bridge, /api/v1/chat, MCP server stdio, Flame hook :9999) are reachable after the doc is followed end-to-end (INSTALL-01)"
    - "The doc lists every external dependency (Postgres, Ollama, conda, Python, optional Anthropic key) BEFORE the install steps begin so the operator can prep the machine (INSTALL-04)"
    - "Every version number in the doc agrees with README.md and pyproject.toml (the consistency test from Plan 20-01 continues to pass) (INSTALL-03)"
    - "The doc points operators at the install-flame-hook.sh defaults that ship at v1.4.1 — never at a stale tag (INSTALL-02)"
    - "Track B / MCP-only operators (no Flame on the box) get a compact carveout telling them which steps to skip and which surfaces will not be reachable (D-08)"
  artifacts:
    - path: "docs/INSTALL.md"
      provides: "Single linear opinionated operator-workstation install path (D-07) with Track B carveout (D-08)"
      contains: "## Step 1"
      min_lines: 200
  key_links:
    - from: "docs/INSTALL.md"
      to: "scripts/install-flame-hook.sh"
      via: "documented invocation matches the script's pinned default"
      pattern: "install-flame-hook\\.sh"
    - from: "docs/INSTALL.md"
      to: "alembic.ini + forge_bridge/store/migrations/"
      via: "alembic upgrade head with FORGE_DB_URL guidance"
      pattern: "alembic upgrade head"
    - from: "docs/INSTALL.md"
      to: "forge_bridge/cli/doctor.py"
      via: "post-install diagnostic invocation"
      pattern: "forge-bridge console doctor"
---

<objective>
Create `docs/INSTALL.md` — the canonical operator-workstation install path that the v1.5 milestone hangs on. This is the largest deliverable of Phase 20 and the artifact that the non-author UAT in Plan 20-05 will walk verbatim. Per CONTEXT.md D-07, it is **opinionated** — one path through. Per D-08, it includes a compact Track B / MCP-only carveout for integrators without Flame.

Purpose: Until this file exists, an outside operator has to reverse-engineer the deployment topology from README + CLAUDE.md + the source tree. The forcing function of the v1.5 milestone is that this doc must work end-to-end on a clean machine for a non-author. If the doc lies, the milestone doesn't ship.

Output: `docs/INSTALL.md` — a new file (~250 lines) with 8 sequential steps, a "Before you start" external-dep table (INSTALL-04), an "If you don't have Flame" carveout (D-08), an env-var reference appendix, and a closing post-install verification block. Every version number, env-var name, and command is sourced from RESEARCH §B–§D — no assumptions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md
@.planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md
@.planning/phases/20-reality-audit-canonical-install/20-PATTERNS.md
@.planning/phases/20-reality-audit-canonical-install/20-VALIDATION.md
@.planning/phases/20-reality-audit-canonical-install/20-01-PLAN.md
@.planning/phases/20-reality-audit-canonical-install/20-02-PLAN.md
@.planning/phases/20-reality-audit-canonical-install/20-03-PLAN.md
@docs/API.md
@docs/ARCHITECTURE.md

<interfaces>
<!-- All facts below are verified by RESEARCH.md §B–§D against the codebase at HEAD. -->
<!-- Use these literally; do not paraphrase or guess. -->

External dependencies (RESEARCH §C2):
- conda (latest stable; reference ~24.x on assist-01)
- Python ≥3.10 (reference 3.11 — matches the conda env)
- PostgreSQL ≥14 (reference 16.x on assist-01) — required for staged ops + execution-log SQL mirror
- Ollama (latest stable; reference 0.21.0 from 16.2-HUMAN-UAT.md) — required for chat + LLM synthesis
- qwen2.5-coder:32b — locked default LLM model; do NOT use qwen3:32b (SEED-DEFAULT-MODEL-BUMP-V1.4.x)
- Flame 2026.x (reference 2026.2.1) — required only for the Flame hook surface; Track B operators skip
- Anthropic API key — OPTIONAL; only needed for `sensitive=False` cloud routing; chat hardcodes `sensitive=True`

Default credentials (hardcoded in `forge_bridge/store/session.py` and `alembic.ini`):
- Postgres: `forge:forge@localhost:5432/forge_bridge` (override via `FORGE_DB_URL`)
- async URL: `postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge`
- sync URL (Alembic): `postgresql+psycopg2://forge:forge@localhost:5432/forge_bridge`
- Note: `alembic.ini` line 6 hardcodes the sync URL; it does NOT auto-read `FORGE_DB_URL`. Operators with non-default credentials must either edit `alembic.ini` or pass `--url` to `alembic upgrade head`.

Three Alembic migrations (RESEARCH §C3):
- `0001_initial_schema.py` — base entity/event schema
- `0002_role_class_media_roles_process_graph.py` — role/relationship registry
- `0003_staged_operation.py` — staged_operation entity (FB-A)

Operator env vars (RESEARCH §C5–§C6):
- `FORGE_DB_URL` — Postgres async URL (default `postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge`)
- `ANTHROPIC_API_KEY` — optional, cloud routing only
- `FORGE_LOCAL_LLM_URL` — Ollama base URL (default `http://localhost:11434/v1`)
- `FORGE_LOCAL_MODEL` — Ollama model (default `qwen2.5-coder:32b`)
- `FORGE_CLOUD_MODEL` — Anthropic model (default `claude-sonnet-4-6` — verified Phase 17)
- `FORGE_CONSOLE_PORT` — Web UI / chat port (default `9996`)
- `FORGE_BRIDGE_HOST` — Flame bridge target host (default `127.0.0.1`)
- `FORGE_BRIDGE_PORT` — Flame bridge port (default `9999`)
- `FORGE_BRIDGE_ENABLED` — set to `0` to disable the Flame hook without uninstalling

NO `forge_config.yaml` exists (RESEARCH §C6). All config is env-var driven. No YAML loading code in the codebase.

Five surfaces + smoke tests (RESEARCH §B1, §D1):
| Surface | Smoke Command | Expected |
| Web UI | `curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"` | `200` |
| CLI | `forge-bridge --help` | Typer help output |
| HTTP chat | `curl -s -X POST http://localhost:9996/api/v1/chat -H "content-type: application/json" -d '{"messages":[{"role":"user","content":"hello"}]}'` | JSON with `messages`, `stop_reason`, `request_id` |
| MCP server | `forge-bridge --help` (full MCP smoke needs a Claude Desktop connection) | Typer help output |
| Flame hook | `curl -s http://localhost:9999/status` | JSON: `{"status":"running","flame_available":true,...}` |

stdin-keepalive for headless launch (RESEARCH §B1, 16.2-HUMAN-UAT.md):
- `tail -f /dev/null | python -m forge_bridge` keeps stdin open on bare deploy hosts where stdin closes immediately

Operator-facing artifact paths (auto-created — operator does NOT need to mkdir these):
- `~/.forge-bridge/executions.jsonl`
- `~/.forge-bridge/synthesized/`
- `~/.forge-bridge/probation/`
- `~/.forge-bridge/quarantined/`

`forge doctor` output (RESEARCH §D2) — what it covers and what it does NOT cover:
- COVERS: MCP liveness, watcher liveness, console port, Flame bridge (degraded-tolerant), Ollama backend, JSONL parseability, sidecar/probation dirs, disk space
- DOES NOT COVER: Postgres reachability / migration version, FORGE_DB_URL validity, Ollama model availability (just daemon up), version-drift between install-flame-hook.sh and pyproject.toml (Plan 20-01's pytest covers this instead)

Documentation conventions in this repo (`docs/API.md`, `docs/ARCHITECTURE.md`, `docs/VOCABULARY.md`):
- H1 for the doc title, H2 for major sections, H3 for subsections
- `---` horizontal rule between every major section
- Code blocks always use triple backtick with language tag (`bash`, `json`, `python`, `toml`)
- Tables use full pipe-aligned markdown
- Relative cross-links use just the filename when both files live in `docs/` (e.g., `[API Reference](API.md)`)
- No emoji in headings; no Obsidian-style `> [!NOTE]` callouts (this repo uses plain markdown)
- One-sentence scope line directly under the H1; prose preamble of 1-3 sentences max before the first `---`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/INSTALL.md from the verified RESEARCH ground truth</name>
  <files>docs/INSTALL.md</files>
  <read_first>
    - .planning/phases/20-reality-audit-canonical-install/20-RESEARCH.md (§B v1.4.1 ground truth, §C install dependencies, §D reachability smoke tests)
    - .planning/phases/20-reality-audit-canonical-install/20-PATTERNS.md (§ docs/INSTALL.md — the explicit structural recommendation)
    - .planning/phases/20-reality-audit-canonical-install/20-CONTEXT.md (D-07, D-08, D-09, D-13, D-14)
    - docs/API.md (markdown convention reference — heading levels, horizontal rules, code-fence language tags)
    - docs/ARCHITECTURE.md (markdown convention reference)
    - README.md (post Plans 20-01 + 20-03 — confirm install commands match what we will write into INSTALL.md)
    - CLAUDE.md (post Plan 20-02 — confirm "How to Get Running" reference matches what we will write here)
    - scripts/install-flame-hook.sh (post Plan 20-01 — confirm v1.4.1 default)
    - alembic.ini (confirm hardcoded sync URL line 6)
    - forge_bridge/store/session.py (confirm default credential string + FORGE_DB_URL behavior)
    - forge_bridge/llm/router.py (confirm _DEFAULT_LOCAL_MODEL = "qwen2.5-coder:32b" + _DEFAULT_CLOUD_MODEL = "claude-sonnet-4-6")
    - forge_bridge/__main__.py (confirm subcommand list)
  </read_first>
  <action>
    Create `docs/INSTALL.md`. Follow the structure below EXACTLY — do not reorder steps, do not add sections, do not omit sections. Every literal command, env var, and version number is sourced from the `<interfaces>` block; copy them verbatim.

    The complete file content (write this VERBATIM as `docs/INSTALL.md` — substitute nothing):

    ```markdown
    # forge-bridge Install Guide

    Single-machine operator install for forge-bridge v1.4.1. Walks a fresh conda env on a workstation that already has Flame, Postgres, and Ollama running, all the way to a five-surface check (Web UI, CLI, chat endpoint, MCP server, Flame hook).

    This is the **opinionated** path — one route through. Multi-machine deployment, multi-user / authenticated setups, and the projekt-forge consumer walkthrough are out of scope (deferred to v1.6+ and Phase 21 respectively). For design rationale, see [ARCHITECTURE.md](ARCHITECTURE.md).

    ---

    ## Before you start

    ### External dependencies

    Set these up BEFORE following the steps below. Install will fail mid-flight if any are missing.

    | Dependency | Required for | Minimum | Reference (tested at v1.4.1) |
    |------------|-------------|---------|------------------------------|
    | conda | env isolation | latest stable | conda ~24.x |
    | Python | runtime | 3.10 | 3.11 |
    | PostgreSQL | staged ops + execution-log SQL mirror | 14 | 16.x |
    | Ollama | chat endpoint + LLM tool synthesis | latest stable | 0.21.0 |
    | `qwen2.5-coder:32b` model | chat + synthesis (locked default) | n/a | pulled via `ollama pull qwen2.5-coder:32b` |
    | Flame | Flame hook surface (skip for Track B) | 2026.x | 2026.2.1 |
    | Anthropic API key | OPTIONAL — only for `sensitive=False` cloud routing | n/a | n/a (chat hardcodes `sensitive=True`) |

    **Do not use `qwen3:32b`** as the default model. Cold-start LLM thinking-mode token verbosity (400-525 tok/turn) exceeds the 60s wall-clock budget. Stay on `qwen2.5-coder:32b` (locked default). Context: `.planning/seeds/SEED-DEFAULT-MODEL-BUMP-V1.4.x.md`.

    ### If you don't have Flame (Track B / MCP-only operators)

    Skip Step 4 (Flame hook install) entirely. Surfaces 1-4 (Web UI, CLI, chat, MCP server) work without Flame. Surface 5 (Flame hook on `:9999`) will not be reachable, and the `flame_*` MCP tools will return errors when invoked. The `forge-bridge console doctor` output will mark `flame_bridge` as `degraded` rather than failing — this is expected behavior (see Phase 07.1 graceful-degradation contract).

    Without Postgres, the staged-ops endpoints (`/api/v1/staged`, `forge_*_staged` MCP tools) will fail with DB connection errors. Either run Step 3 (Postgres setup) or expect those features to be unavailable.

    ---

    ## Step 1: Prepare the conda environment

    forge-bridge expects a conda env named `forge` running Python 3.11 — this matches the reference deployment.

    ```bash
    conda create -n forge python=3.11 -y
    conda activate forge
    ```

    Verify Python:

    ```bash
    python --version    # Python 3.11.x
    ```

    ---

    ## Step 2: Install forge-bridge

    Clone the repo (or `cd` into your existing checkout) and install with the LLM extras:

    ```bash
    git clone https://github.com/cnoellert/forge-bridge.git
    cd forge-bridge
    pip install -e ".[dev,llm]"
    ```

    The `[dev]` extra adds pytest + ruff for development. The `[llm]` extra adds `openai`, `anthropic`, and `ollama` — **mandatory** for the chat endpoint and the learning-pipeline tool synthesizer. Bare `pip install -e .` will silently break both.

    Verify the package version self-reports correctly:

    ```bash
    python -c "import forge_bridge; print(forge_bridge.__version__)"   # 1.4.1
    ```

    Verify the CLI entry point installed:

    ```bash
    forge-bridge --help
    ```

    ---

    ## Step 3: Set up Postgres

    forge-bridge defaults to `forge:forge@localhost:5432/forge_bridge`. Either match those defaults or set `FORGE_DB_URL` to your own.

    Default-credential setup:

    ```bash
    # As a Postgres superuser:
    createuser -P forge          # set password 'forge' when prompted
    createdb -O forge forge_bridge
    ```

    Run the three Alembic migrations:

    ```bash
    # From the repo root:
    alembic upgrade head
    ```

    **Note on Alembic + custom credentials:** `alembic.ini` hardcodes the sync URL `postgresql+psycopg2://forge:forge@localhost:5432/forge_bridge` and does NOT auto-read `FORGE_DB_URL`. If you set `FORGE_DB_URL` to non-default credentials in Step 5, you must also either:
    - edit `sqlalchemy.url` in `alembic.ini`, OR
    - pass the URL explicitly: `alembic -x url=postgresql+psycopg2://USER:PASS@HOST:PORT/DB upgrade head`

    Verify migrations applied:

    ```bash
    alembic current   # shows 0003_staged_operation as the head revision
    ```

    ---

    ## Step 4: Install the Flame hook

    **Skip this step if you are running Track B / MCP-only.**

    From the repo root:

    ```bash
    ./scripts/install-flame-hook.sh
    ```

    Or standalone on any Flame workstation (no clone required):

    ```bash
    curl -fsSL https://raw.githubusercontent.com/cnoellert/forge-bridge/v1.4.1/scripts/install-flame-hook.sh | bash
    ```

    The installer copies `forge_bridge.py` into `/opt/Autodesk/shared/python/forge_bridge/scripts/`, sanity-checks it parses as Python, and prints next-step instructions. Override defaults via:

    | Env var | Default | Purpose |
    |---------|---------|---------|
    | `FORGE_BRIDGE_VERSION` | `v1.4.1` | git tag to pull the hook from |
    | `FORGE_BRIDGE_HOOK_DIR` | `/opt/Autodesk/shared/python/forge_bridge/scripts` | install target |

    Launch (or relaunch) Flame. The hook auto-starts a Python HTTP server on `http://127.0.0.1:9999/`.

    ---

    ## Step 5: Configure environment variables

    All forge-bridge configuration is via environment variables — there is no config file. Set these in your shell or `.env` profile (do **not** commit secrets to a version-controlled file).

    Required for the daily operator workflow:

    ```bash
    # Postgres connection (override the default if your credentials differ)
    export FORGE_DB_URL="postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge"
    ```

    Optional (defaults are usually fine):

    ```bash
    # Ollama backend (default http://localhost:11434/v1)
    export FORGE_LOCAL_LLM_URL="http://localhost:11434/v1"

    # Local model (default qwen2.5-coder:32b — DO NOT use qwen3:32b, see warning above)
    export FORGE_LOCAL_MODEL="qwen2.5-coder:32b"

    # Flame target host (default 127.0.0.1)
    export FORGE_BRIDGE_HOST="127.0.0.1"

    # Web UI / chat endpoint port (default 9996)
    export FORGE_CONSOLE_PORT="9996"
    ```

    Optional — cloud LLM routing (NOT required for the daily operator workflow; chat hardcodes `sensitive=True` which routes through local Ollama):

    ```bash
    export ANTHROPIC_API_KEY="sk-ant-..."
    export FORGE_CLOUD_MODEL="claude-sonnet-4-6"
    ```

    **Anthropic key handling:** put it in your shell profile (`~/.zshrc` / `~/.bashrc`) or a sourced `.env` file outside version control. Never paste it on the command line directly (shell history will capture it). Never commit it to git.

    ---

    ## Step 6: Start the server

    The single `python -m forge_bridge` process boots **all four hosted surfaces in one shot** — MCP server (stdio), Artist Console Web UI (`:9996`), chat endpoint (`:9996/api/v1/chat`), and the WebSocket event server (`:9998`).

    Daily local launch:

    ```bash
    python -m forge_bridge
    ```

    On a headless host where stdin closes immediately (deploy hosts, ssh-detached sessions), keep stdin alive so FastMCP doesn't exit:

    ```bash
    tail -f /dev/null | python -m forge_bridge
    ```

    The first run auto-creates the operator-facing artifact directory `~/.forge-bridge/`:

    ```
    ~/.forge-bridge/executions.jsonl    # source-of-truth execution log
    ~/.forge-bridge/synthesized/        # auto-promoted MCP tool Python files
    ~/.forge-bridge/probation/          # in-probation tool state
    ~/.forge-bridge/quarantined/        # quarantined tool state
    ```

    Leave the server running in another terminal / tmux pane and proceed to Step 7.

    ---

    ## Step 7: Verify all five surfaces

    Run each smoke test. All five should pass on a Track A workstation; Surface 5 (Flame hook) will skip for Track B.

    ```bash
    # 1. Flame hook (Track A only — needs Flame running with the hook installed)
    curl -s http://localhost:9999/status
    # → {"status":"running","flame_available":true,"namespace_keys":[...]}

    # 2. MCP server (CLI entry point)
    forge-bridge --help
    # → Typer help text (subcommands: console)

    # 3. Artist Console Web UI
    curl -fsS http://localhost:9996/ui/ -o /dev/null -w "%{http_code}\n"
    # → 200

    # 4. Chat endpoint (needs Ollama + qwen2.5-coder:32b loaded)
    curl -s -X POST http://localhost:9996/api/v1/chat \
      -H "content-type: application/json" \
      -d '{"messages":[{"role":"user","content":"hello"}]}'
    # → JSON with messages, stop_reason, request_id

    # 5. Browser check — open http://localhost:9996/ui/ and confirm the five views
    #    (tools, execs, manifest, health, chat) all render
    ```

    First chat call may take 30-60s on a cold Ollama (model load). Subsequent calls are sub-10s.

    ---

    ## Step 8: Post-install diagnostic

    Run the `forge doctor` check. It probes JSONL log parseability, sidecar/probation dir writability, port reachability, disk space, and the Ollama backend:

    ```bash
    forge-bridge console doctor
    ```

    Expected exit codes: `0` = ok, `1` = check failure, `2` = server unreachable.

    `forge doctor` does **not** currently cover Postgres reachability or Ollama model presence (just daemon up). If you need those, hit them directly:

    ```bash
    psql -h localhost -U forge -d forge_bridge -c '\dt'   # lists tables — confirms migrations + connection
    ollama list | grep qwen2.5-coder                       # confirms model is pulled
    ```

    ---

    ## Reference: Environment variables

    | Var | Default | Purpose |
    |-----|---------|---------|
    | `FORGE_DB_URL` | `postgresql+asyncpg://forge:forge@localhost:5432/forge_bridge` | Postgres async URL |
    | `FORGE_LOCAL_LLM_URL` | `http://localhost:11434/v1` | Ollama base URL |
    | `FORGE_LOCAL_MODEL` | `qwen2.5-coder:32b` | Local model (do NOT use qwen3:32b) |
    | `FORGE_CLOUD_MODEL` | `claude-sonnet-4-6` | Anthropic model (only used when `sensitive=False`) |
    | `ANTHROPIC_API_KEY` | unset | Optional — cloud routing only |
    | `FORGE_CONSOLE_PORT` | `9996` | Web UI / chat endpoint port |
    | `FORGE_BRIDGE_HOST` | `127.0.0.1` | Flame bridge target host |
    | `FORGE_BRIDGE_PORT` | `9999` | Flame bridge port |
    | `FORGE_BRIDGE_ENABLED` | `1` | Set `0` to disable the Flame hook without uninstalling |
    | `FORGE_BRIDGE_VERSION` | `v1.4.1` | (`install-flame-hook.sh` only) git tag to pull the hook from |
    | `FORGE_BRIDGE_HOOK_DIR` | `/opt/Autodesk/shared/python/forge_bridge/scripts` | (`install-flame-hook.sh` only) install target |

    ---

    ## Reference: Ports

    | Port | Surface | Process |
    |------|---------|---------|
    | `9996` | Web UI + `/api/v1/chat` + `/api/v1/staged` + Read API | `python -m forge_bridge` |
    | `9998` | WebSocket event server | `python -m forge_bridge` (graceful degradation if unreachable per Phase 07.1) |
    | `9999` | Flame hook HTTP server | Flame process (loads hook on launch) |

    ---

    ## Reference: Cross-links

    - [API.md](API.md) — Flame bridge HTTP API (`POST /exec` semantics, namespace persistence)
    - [ARCHITECTURE.md](ARCHITECTURE.md) — design rationale + decision log
    - [VOCABULARY.md](VOCABULARY.md) — canonical entity model (Project → Sequence → Shot → Version → Media + Stack/Layer/Asset + traits)
    - [ENDPOINTS.md](ENDPOINTS.md) — guide to writing new endpoint adapters
    - `../README.md` — project overview + Quick Start
    - `../CLAUDE.md` — context-recovery doc for AI assistants
    ```

    Touch nothing else in the repo. Do not modify the existing `docs/*.md` files. Do not create `docs/GETTING-STARTED.md` (Phase 21).

    Commit message: `docs(20): create canonical operator install guide (INSTALL-01..04)`
  </action>
  <verify>
    <automated>test -f docs/INSTALL.md && grep -q '^# forge-bridge Install Guide' docs/INSTALL.md && grep -q '^## Before you start' docs/INSTALL.md && grep -q '^## Step 1: Prepare the conda environment' docs/INSTALL.md && grep -q '^## Step 2: Install forge-bridge' docs/INSTALL.md && grep -q '^## Step 3: Set up Postgres' docs/INSTALL.md && grep -q '^## Step 4: Install the Flame hook' docs/INSTALL.md && grep -q '^## Step 5: Configure environment variables' docs/INSTALL.md && grep -q '^## Step 6: Start the server' docs/INSTALL.md && grep -q '^## Step 7: Verify all five surfaces' docs/INSTALL.md && grep -q '^## Step 8: Post-install diagnostic' docs/INSTALL.md && grep -q "If you don't have Flame" docs/INSTALL.md && grep -q 'qwen2.5-coder:32b' docs/INSTALL.md && grep -q 'qwen3:32b' docs/INSTALL.md && grep -q 'tail -f /dev/null | python -m forge_bridge' docs/INSTALL.md && grep -q 'forge-bridge console doctor' docs/INSTALL.md && grep -q 'curl -fsS http://localhost:9996/ui/' docs/INSTALL.md && grep -q 'curl -s http://localhost:9999/status' docs/INSTALL.md && grep -q 'POST http://localhost:9996/api/v1/chat\\|POST /api/v1/chat\\|api/v1/chat' docs/INSTALL.md && grep -q 'alembic upgrade head' docs/INSTALL.md && grep -q 'pip install -e ".\[dev,llm\]"' docs/INSTALL.md && grep -q 'cnoellert/forge-bridge/v1.4.1/scripts/install-flame-hook.sh' docs/INSTALL.md && [ "$(wc -l < docs/INSTALL.md)" -ge "200" ]</automated>
  </verify>
  <done>
    - `docs/INSTALL.md` exists and is non-empty
    - File starts with `# forge-bridge Install Guide` (H1)
    - "Before you start" section present with external-dep table covering ALL of: conda, Python, PostgreSQL, Ollama, qwen2.5-coder:32b, Flame, Anthropic API key (INSTALL-04)
    - All 8 H2 step headings present in correct order: Prepare conda, Install forge-bridge, Set up Postgres, Install Flame hook, Configure env vars, Start server, Verify five surfaces, Post-install diagnostic
    - "If you don't have Flame" carveout present (D-08)
    - qwen3:32b warning present with reference to SEED-DEFAULT-MODEL-BUMP-V1.4.x
    - stdin-keepalive note present (`tail -f /dev/null | python -m forge_bridge`)
    - 5-surface smoke test block in Step 7 covers all five (Flame hook :9999, MCP/CLI, Web UI :9996/ui/, chat /api/v1/chat, browser visual check)
    - `forge-bridge console doctor` referenced in Step 8
    - `alembic upgrade head` documented with note about `alembic.ini` hardcoded URL
    - `pip install -e ".[dev,llm]"` documented (NOT bare `pip install -e .`)
    - install-flame-hook.sh curl URL pinned at v1.4.1
    - Env-var reference appendix present with all 11 env vars
    - Port reference appendix present with all 3 ports (9996, 9998, 9999)
    - Cross-links to API.md, ARCHITECTURE.md, VOCABULARY.md, ENDPOINTS.md, README.md, CLAUDE.md
    - File length ≥ 200 lines
    - Plan 20-01 consistency test still passes: `pytest tests/test_install_hook_version_consistency.py -x` exits 0 (the v1.4.1 curl URL in INSTALL.md is consistent with the same URL in README.md and the script default)
    - Full pytest suite green: `pytest tests/ -x`
    - One commit on branch with message `docs(20): create canonical operator install guide (INSTALL-01..04)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → docs/INSTALL.md | The operator runs commands the doc tells them to. The doc is the trust anchor for the entire install. |
| operator → curl raw.githubusercontent.com | Step 4's standalone curl chain pulls a script from GitHub. Pinned to a specific tag, not `main`. |
| operator → alembic upgrade head | Migrations run against whatever DB `FORGE_DB_URL` (or alembic.ini hardcoded URL) points at. Misconfigured env could target the wrong DB. |
| operator → ANTHROPIC_API_KEY | Operator pastes a secret somewhere. Doc must specify the canonical safe location. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | install-flame-hook.sh `curl | bash` chain | accept (LOW) | Risk envelope already exists at HEAD; this doc only documents the existing chain. The doc pins to a specific git tag (`v1.4.1`), not `main`, preserving the existing trust model. The `<security_context>` reminded operators to review the script before piping to bash if upstream isn't trusted; this is reflected in the Step 4 prose by linking to the script in the repo so a reader can inspect it before invoking the curl form. |
| T-20-02 | Tampering | `alembic upgrade head` invocation | mitigate | Step 3 explicitly calls out that `FORGE_DB_URL` controls the migration target and that `alembic.ini` hardcodes a separate URL that does NOT auto-read the env var. The operator is told to either match the default credentials or explicitly pass `--url`. The verify-`alembic current` step gives immediate feedback if the migration ran against the wrong DB (no head revision shown). |
| T-20-03 | Information Disclosure | `ANTHROPIC_API_KEY` handling | mitigate | Step 5 specifies the canonical safe locations (shell profile or sourced `.env` file outside version control) and warns explicitly against pasting on the command line (shell history) and committing to git. |
| T-20-09 | Spoofing | Doc claims about which surfaces are reachable | mitigate | The 5-surface smoke-test block in Step 7 gives the operator immediate ground-truth verification. If the doc lies about a surface, the curl exits non-zero or returns the wrong content. The non-author UAT in Plan 20-05 is the structural mitigation — a fresh-eyes operator running the doc verbatim will surface any falsehood. |
</threat_model>

<verification>
```bash
# File created at the canonical path
test -f docs/INSTALL.md
[ "$(wc -l < docs/INSTALL.md)" -ge "200" ]

# Required H1 + 8 step headings present in order
grep -q '^# forge-bridge Install Guide' docs/INSTALL.md
grep -q '^## Before you start' docs/INSTALL.md
grep -q '^## Step 1: Prepare the conda environment' docs/INSTALL.md
grep -q '^## Step 2: Install forge-bridge' docs/INSTALL.md
grep -q '^## Step 3: Set up Postgres' docs/INSTALL.md
grep -q '^## Step 4: Install the Flame hook' docs/INSTALL.md
grep -q '^## Step 5: Configure environment variables' docs/INSTALL.md
grep -q '^## Step 6: Start the server' docs/INSTALL.md
grep -q '^## Step 7: Verify all five surfaces' docs/INSTALL.md
grep -q '^## Step 8: Post-install diagnostic' docs/INSTALL.md

# CONTEXT.md decisions reflected
grep -q "If you don't have Flame" docs/INSTALL.md   # D-08
grep -q 'qwen2.5-coder:32b' docs/INSTALL.md          # locked default
grep -q 'qwen3:32b' docs/INSTALL.md                  # warning present
grep -q 'tested at v1.4.1' docs/INSTALL.md           # D-13 reference-version model

# All five surfaces' smoke commands present
grep -q 'curl -s http://localhost:9999/status' docs/INSTALL.md
grep -q 'forge-bridge --help' docs/INSTALL.md
grep -q 'curl -fsS http://localhost:9996/ui/' docs/INSTALL.md
grep -q 'POST http://localhost:9996/api/v1/chat' docs/INSTALL.md
grep -q 'forge-bridge console doctor' docs/INSTALL.md

# Version consistency with README + pyproject (Plan 20-01 test guards this)
grep -q 'cnoellert/forge-bridge/v1.4.1/scripts/install-flame-hook.sh' docs/INSTALL.md
grep -q 'forge-bridge v1.4.1' docs/INSTALL.md
pytest tests/test_install_hook_version_consistency.py -x

# Full suite green
pytest tests/ -x
```
</verification>

<success_criteria>
- [ ] `docs/INSTALL.md` exists, ≥200 lines
- [ ] All 8 step headings present in canonical order
- [ ] "Before you start" external-dep table covers all dependencies (INSTALL-04)
- [ ] D-08 "If you don't have Flame" carveout present
- [ ] D-13 minimum-version + reference-version model used in dep table
- [ ] qwen3:32b warning present with seed reference
- [ ] stdin-keepalive note present
- [ ] All 5 surfaces have smoke commands in Step 7 (INSTALL-01)
- [ ] `forge-bridge console doctor` referenced in Step 8
- [ ] install-flame-hook.sh curl URL pinned at v1.4.1 (INSTALL-02)
- [ ] Plan 20-01 consistency test continues to pass (the v1.4.1 anchor in INSTALL.md is consistent with README + pyproject; INSTALL-03)
- [ ] Env-var + port reference appendices present
- [ ] Cross-links to API.md, ARCHITECTURE.md, VOCABULARY.md, ENDPOINTS.md, README.md, CLAUDE.md
- [ ] No `forge_config.yaml` mentioned (the file does not exist)
- [ ] One commit on branch
- [ ] Full pytest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/20-reality-audit-canonical-install/20-04-SUMMARY.md` covering:
- Word count + line count of the created doc
- Confirmation that all 8 steps + the carveout are present
- Verification grep results (all gates passed)
- Confirmation that consistency with README + pyproject + script holds (Plan 20-01 test still green)
- Any gaps surfaced during authoring (e.g., env vars discovered in source that weren't in RESEARCH.md, undocumented behaviors)
- Note on whether the `alembic.ini` hardcoded-URL note triggered any code-fix candidates for follow-up phases (could become a Phase 23 `forge doctor` enhancement)
</output>
