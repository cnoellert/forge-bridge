---
gsd_state_version: 1.0
milestone: v1.5
milestone_name: Legibility
status: milestone_closed
stopped_at: v1.5 Legibility CLOSED 2026-05-14 — milestone-close synthesis landed at `.planning/milestones/v1.5-MILESTONE-CLOSE.md` (666 lines / 11 sections / 54 subsections); writer's-room milestone-arc archaeology synthesis (NOT standard milestone-audit substrate per §9 substrate-mismatch reasoning); 6 phases CLOSED (20 + 20.1 full GSD substrate; 21 + 22 + 23 + 23.1 writer's-room cadence); 4 reader-facing docs shipped (INSTALL.md 440 + GETTING-STARTED.md 119 + RECIPES.md 627 + TROUBLESHOOTING.md 575 = 1761 lines total); 36 seeds at close (5 Phase 23.1 + 3 non-author UAT + 1 Phase 20.1 archaeology + 12 earlier v1.5-tagged + 7 v1.5+ forward-pointing + 5 v1.6+-only + 3 v1.4.x legacy); public `__all__` = 19 (byte-equivalent to v1.4.x close); forcing-function disposition OPTION (a) per operator framing 2026-05-14 (the milestone changed what needed proving — Phase 21 author cold-read + 3 non-author UAT seeds + Phase 23.1 yes-with-known-residuals softening = in-flight governance, not retroactive excuse-making); non-author UAT carries forward to v1.6 as opening forcing function; v1.6 framing artifact already landed at `.planning/milestones/v1.6-FRAMING.md`; same-commit-paired close cadence at milestone-arc scope (5th operational instance overall, 1st at milestone-arc scope; flagged not promoted per §5.3)
last_updated: "2026-05-15T23:30:00.000Z"
last_activity: 2026-05-15 -- **Phase 24 operational substrate loop completed on portofino; remaining obligations explicitly bounded.** 4-commit operational arc Exists → See → Trust → Drive shipped under writer's-room cadence: (1) `033b093` feat(24) `fbridge graph list/show` — read-only debug CLI over the JSONL graph store; closes v1.6-PHASE-24-CONVERGENCE.md §2.4 (records structurally useful) + Q20 (minimal debug CLI); 14 tests + live smoke against operator's `~/.forge-bridge/graphs/` records. (2) `c9c07c4` feat(24) `fbridge doctor` `graph_store` row — Q18 first operator-visible surface of the graph-native runtime; tri-state via optional `chip` field (ok/loaded/fail) preserves "substrate installed != substrate exercised" operational distinction at the surface operators reach first; 5 tests + live smoke of all three states (loaded for missing/empty dir, ok for populated parseable, fail for corrupt newest with structural-error suggested-next-action). (3) `163f07f` test(24) Q8 POSIX append atomicity — empirically verified on portofino (macOS, PIPE_BUF=512); 8 workers × 100 records (= 800 concurrent appends to single shared file) with payloads biased 600/1500/3000 bytes (all above macOS PIPE_BUF); 5 invariants held (no torn writes, no merged lines, no truncated JSON, no dropped records exact count preservation, content integrity via per-worker filler-character interleave detector); convergence-anticipated macOS failure mode did NOT manifest; fcntl fallback stays a deferred-not-needed seed; **subsequently verified on flame-01 (Linux, PIPE_BUF=4096) 2026-05-15 — 1 passed in 2.81s, dual-platform acceptance criterion now met. The append-only JSONL substrate has been operationally verified under concurrent multiprocessing pressure on both production platforms, rather than relying on inherited POSIX assumptions. fcntl fallback remains intentionally deferred-not-rejected — operational trigger condition never manifested on either platform. Operationally-verified truth, not inherited POSIX folklore.** Resolved-operational-assumption archaeology recorded at convergence §2 Q8 entry. (4) `c091e5e` feat(24) `fbridge flame-exec` — operator-side surface onto the SAME execution substrate the LLM tool-call path uses, NOT a second execution substrate; substrate-discipline refactor extracted `_execute_python_core(code, main_thread, graph_id)` as shared body; public `execute_python` (MCP entry point) becomes 2-line wrapper; node_kind stays `"python"` because operator surface is metadata NOT ontology; graph_id surfaced in BOTH human + JSON modes so operators can immediately `fbridge graph show <graph_id>`; exit codes locked 0=success/1=Flame-error/2=transport; no retry/no HTTP/no graph-render hints/no stdin at v1.6.0; 14 tests including single-substrate discipline assertions + 1 sibling test_pydantic_coverage regression aligned (private-helper underscore-skip captured structurally). **Operator loop closed end-to-end on portofino against real Flame:** `fbridge flame-exec "import flame; print(flame.project.current_project.name)"` → `graph_id 7ab74e47e2a4456e8ec58c3e24b870fd` + `status ok` + project-name returned → `fbridge graph show 7ab74e47e2a4456e8ec58c3e24b870fd` replays full event stream (2 records: started + ok, identical code_hash, node_kind="python") → `fbridge doctor | grep graph_store` row ticks 5→6 graphs with newest timestamp matching. This is the first moment the entire substrate became visibly self-consistent from the operator side. **Architectural discipline preserved as load-bearing project philosophy:** *operator surfaces intentionally remained projections onto the same execution substrate rather than parallel execution paths* — future contributors will absolutely try to bypass this; the substrate-discipline refactor encoded it structurally (shared `_execute_python_core`) so divergence requires deliberate intent. **Convergence-vs-operability honest asymmetry preserved:** what is PROVED at this cursor — observability (graph_store row teaches existence + health), execution (CLI exec via shared substrate), replay (graph_id round-trip), operator access (CLI surfaces composable). What is NOT YET PROVED (after Q8 dual-platform closure, remaining obligations are primarily interaction-layer) — reliable conversational convergence under non-author operator load (§2.1 + §2.2 four-proofs), non-author operability UAT against Phase-20/20.1/21 seeds, session-graph propagation across multi-turn chat (currently each `flame_execute_python` is a one-graph; chat-session graph propagation lands later per utility.py:174). **Posture shift:** substrate side is increasingly evidenced and operationally hardened; remaining uncertainty is now primarily human / LLM interaction-layer behavior. **Phase 24 substrate sweep: 124/124 passed across regression bands.** Predecessor cursor `project_state_2026_05_15_v1_6_convergence_closed_phase_24_writers_room.md` SUPERSEDED. Artifact `.planning/milestones/v1.6-WRITERS-ROOM-CONVERGENCE.md` (299 lines / 11 sections) records writer's-room positions on **20 open questions** (recount catch — framing's §12 contains Q1–Q20 across 5 schema / 3 storage / 3 surface integration / 4 subgraph+promotion / 2 foundry / 3 scope-adjacent clusters; an earlier prose reference to "16 questions" was the miscount). Operator walk surfaced 4 revisions: Q1 reframed (storage permits in-place / operational layer preserves append-oriented lifecycle semantics; full append-only storage is v1.7+ trigger) + Q1 callout names the load-bearing architectural observation explicitly — **operational semantics are becoming event-oriented *before* execution becomes graph-native** (ties together graph events, provenance, replayability, transport_error archaeology, protocol normalization, doctor surfaces, graph_store diagnostics — explains why current substrate decisions look the way they do). Q4 (UUIDv7) confirmed against drift toward provider-interop / OpenTelemetry-shaped semantics / distributed-runtime compatibility. Q9 (reference-only `{graph_id, root_node_id}` chat response) confirmed; importantly prevents `/api/v1/chat` from accidentally becoming the canonical graph transport surface. Q18 strengthened: **graph_store doctor row is the first operator-visible surface of the graph-native runtime** — doctrinal continuation of Phase 23's doctor-as-architectural-teaching pattern into v1.6 (graph emission, normalization boundaries, provenance, dispatch substrate, transport_error traces are core runtime concepts now, not optional observability extras). Three intentionally-unbound dispositions preserved (Q3 metrics-stream promotion pending size-cap breach; Q7 retention pending operator pressure; Q17 foundry-bias re-read at v1.6.1 phase open). **Path C of the four directional paths CLOSED at this convergence** — §7 of the artifact promotes domain-boundary discipline to forward-governance norm: each layer has its own success metric, optimizations evaluated against the layer they target not cross-layer, cross-layer effects surfaced explicitly, investigative phases honor the decomposition. Anti-scope binding from THREE-DOMAIN-DECOMPOSITION carried forward + strengthened with §7.3 trigger conditions for re-opening (cache tuning / instrumentation / narrowing / prompt surgery each have specific evidence-trigger conditions — intentionally unbound pending evidence per [[feedback-deferral-first-class-governance]], not implicitly rejected). **Paths A / B / D remain v1.6+ architectural scope** — close inside their respective phases: A via Phase 25/26 protocol-normalization substrate, B implicitly at Phase 26 schema universalization (the canonical type IS the node record's `kind`+`name`+`inputs` shape), D via Phase 24 + `SEED-FAST-FAIL-LLM-CONNECT-TIMEOUT-V1.5+` activation. **Phase 24 (Operability Minimum) unblocked** — framing §11.5 said writer's-room converges before phases open; this artifact converges. Phase 24 may now open against framing + this convergence document's positions on Q1–Q5 / Q6–Q8 / Q9–Q11 / Q18 / Q20. **Predecessor cursor:** 2026-05-14 evening 4-commit investigative-arc closure (`1e58c56` → `5aef0ec` → `60e9f85` → `4cd53da`); same architectural baseline carries forward (forge-bridge is a low-latency conversational runtime + a graph-native operational runtime sharing a common dispatch substrate; three engineering domains held separately). 4 commits this session, all on origin/main: `1e58c56` docs(24.1) methodology-debt fix — resolve protocol-investigation appendix log-path mismatch with the audited 3-mode matrix (managed-daemon vs macOS launchd vs Linux systemd) → `5aef0ec` feat(24.1) protocol-path observability Pass 1 instrumentation at `OllamaToolAdapter.send_turn` — structured `ollama-turn` log line capturing raw_tool_calls + parsed_tool_calls + salvage_fired + content_hash + content_prefix, plus 3 tests covering structured-call / Bug-D-salvage / protocol-failure-shape signatures → `60e9f85` docs synthesize THREE-DOMAIN-DECOMPOSITION.md (157 lines) as the architectural conclusion of both investigations → `4cd53da` docs(v1.6) fold three-domain decomposition into v1.6-FRAMING.md as §2.5 (two runtime lanes: conversational + operational) + §9.5 (protocol normalization — provider-native as ingestion format, canonical internal object as normalized operation intent; Bug-D salvage promoted to first-class normalization layer). Live portofino measurement against the instrumented build (HEAD `5aef0ec` deployed via main checkout + launchd kickstart) produced clean Pass 1 data on canonical-warm call (canonical-cold timed out at iter=0 in 120s — tools_offered_count grew 7→58 since artifact-time measurement, pushing prompt-eval past budget): 8/8 turns showed raw_tool_calls=0 / parsed_tool_calls=1 / salvage_fired=true. **Inverts the artifact's prediction** — Bug-D salvage IS load-bearing under qwen2.5-coder + 58-tool prefix (model emits JSON-shape calls embedded in conversational prose, not Python-method-call fabrications). Graph events also emitted (5 jsonl files; status=transport_error at Flame hook — stacked failure, out of protocol scope). Architectural baseline updated: bridge is no longer "a chat surface with optional tool execution" — it is "a low-latency conversational runtime + a graph-native operational runtime sharing a common dispatch substrate." Three engineering domains (runtime economics / protocol serialization / dispatch substrate) now held separately. Runtime philosophy frozen in §2.5: deterministic runtime identity > aggressive token minimization. Anti-scope binding STRENGTHENED: no more cache tuning, no more instrumentation, no more narrowing experiments, no more prompt surgery. Implementation specifics preserved in archaeology artifacts, not framing. Four directional paths (A salvage formalization / B canonical operation-intent type / C domain-boundary discipline / D operator-facing perceived-responsiveness optimization) opened at THREE-DOMAIN-DECOMPOSITION §"What this artifact opens" — path ordering intentionally unbound at this cursor (operator-direction surface, not synthesis-scope). Terminal cursor `project_state_2026_05_14_v1_6_framing_three_domain_integrated.md` carries the full handoff state for cold-resume next session; predecessor cursor `project_state_2026_05_14_phase_24_1_closed_protocol_investigation_opened.md` SUPERSEDED.
progress:
  total_phases: 1
  completed_phases: 1
  total_plans: 0
  completed_plans: 0
  percent: 100
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-30 at v1.5 milestone open)

**Project core value:** forge-bridge is the single canonical pip-installable middleware (`pip install forge-bridge`) — protocol-agnostic communication bus with a canonical vocabulary that any endpoint (Flame, Maya, editorial, LLM agents) connects to.
**Current focus (terminal handoff state 2026-05-15 late evening — UPDATED for Q8 dual-platform closure):** **Phase 24 operational substrate loop completed on portofino; Q8 atomicity subsequently verified on flame-01 (Linux) — substrate side is increasingly evidenced and operationally hardened; remaining uncertainty has shifted to the human / LLM interaction layer.** Four-commit operational arc Exists → See → Trust → Drive shipped under writer's-room cadence (`033b093` graph list/show → `c9c07c4` doctor graph_store row → `163f07f` Q8 atomicity verified on portofino → `c091e5e` flame-exec CLI). Q8 acceptance criterion subsequently met dual-platform 2026-05-15 — `pytest -m atomicity` passed cleanly on flame-01 (Linux PIPE_BUF=4096, 2.81s) following the portofino macOS verification (PIPE_BUF=512 with payloads intentionally above it). **The append-only JSONL substrate has now been operationally verified under concurrent multiprocessing pressure on both production platforms, rather than relying on inherited POSIX assumptions.** Operator loop closed end-to-end on portofino against real Flame: `fbridge flame-exec "..."` → graph_id surfaced → `fbridge graph show <id>` replays the event stream → `fbridge doctor` graph_store row ticks count up. This is the first moment the entire substrate became visibly self-consistent from the operator side. **Architectural discipline preserved as load-bearing project philosophy:** *operator surfaces intentionally remained projections onto the same execution substrate rather than parallel execution paths* — encoded structurally via the shared `_execute_python_core` body in `forge_bridge/tools/utility.py` (both the CLI and the MCP tool delegate into it; both emit `node_kind="python"` because the substrate truth is the same: a Python execution against Flame occurred). **Honest asymmetry preserved with posture shift after Q8 dual-platform closure:** PROVED — observability (graph_store row teaches existence + health), execution (CLI exec via shared substrate), replay (graph_id round-trip), operator access, **substrate atomicity dual-platform** (operationally-verified truth, not inherited POSIX folklore; fcntl fallback stays deferred-not-rejected — operational trigger never manifested). NOT YET PROVED (now primarily interaction-layer) — reliable conversational convergence under non-author operator load (Phase 24 §2.1 + §2.2 four-proofs), non-author operability UAT against Phase-20/20.1/21 seeds, session-graph propagation across multi-turn chat. Predecessor architectural baseline (carries forward unchanged): forge-bridge is "a low-latency conversational runtime + a graph-native operational runtime sharing a common dispatch substrate" (v1.6-FRAMING §2.5); three engineering domains held separately. Terminal cursor for cold-resume: `project_state_2026_05_15_phase_24_operational_substrate_loop_completed.md` (will be augmented with Q8 flame-01 closure in next memory pass).

## Current Position

Phase: 24 (Operability Minimum) — **operational substrate loop completed on portofino**; not closed as a phase artifact (Phase 24 §2.1/§2.2 conversational-convergence proofs + non-author UAT remain pending obligations).
Plan: 0 of 0 (writer's-room cadence; Phase 24 shipped 4 atomic commits, no formal GSD substrate per operator direction).
Status: **Phase 24 operational substrate loop completed on portofino; Q8 atomicity now dual-platform verified (portofino + flame-01).** 4-commit arc: `033b093` graph list/show (§2.4 + Q20) → `c9c07c4` doctor graph_store row (Q18) → `163f07f` Q8 atomicity verified on portofino (subsequently extended to flame-01 2026-05-15) → `c091e5e` flame-exec CLI. Operator loop visible end-to-end: `fbridge flame-exec "..."` → graph_id returned → `fbridge graph show <id>` replays records → `fbridge doctor` graph_store row reflects new count. Substrate side increasingly evidenced and operationally hardened; remaining uncertainty has shifted to the human / LLM interaction layer (conversational convergence, non-author operability, session-graph propagation).
Milestone: **v1.5 Legibility CLOSED 2026-05-14**; **v1.6 Operability OPEN** — framing at `.planning/milestones/v1.6-FRAMING.md`; §12 convergence at `.planning/milestones/v1.6-WRITERS-ROOM-CONVERGENCE.md` (Q8 entry updated 2026-05-15 to record dual-platform verification — operationally-verified truth across both production platforms, not inherited POSIX folklore); Phase 24 operational substrate now landed.
Next milestone: v1.6 continues. Phase 24 §2.1 + §2.2 conversational-convergence proofs + non-author UAT are the natural next forcing functions; Phase 25 / 26 (Paths A + B from convergence §8) remain v1.6+ architectural scope.
Last activity: 2026-05-15 -- Phase 24 operational substrate loop completed on portofino. See frontmatter `last_activity` for full commit-by-commit detail. Architectural discipline preserved as load-bearing project philosophy: *operator surfaces remain projections onto the same execution substrate, not parallel execution paths* — encoded structurally via `_execute_python_core` shared body.

**v1.4.x closed 2026-04-30** — patch milestone shipped at tag `v1.4.1`. 9/9 requirements (MODEL-01..02, HARNESS-01..03, POLISH-01..04) closed across 3 phases (17, 18, 19); audit `passed`; 7/7 cross-phase integration wires verified; public `__all__` byte-identical to v1.4 close (19 symbols).

**v1.5 scope** — Legibility milestone, no LOC target, no API surface change expected:

- **Phase 20 + 20.1** (CLOSED 2026-05-12) — Reality audit + canonical install. Walk a fresh install end-to-end on a clean machine; fix gaps as they surface. Output: `docs/INSTALL.md`, refreshed `README.md` install section, refreshed `CLAUDE.md` ground-truth section, `install-flame-hook.sh` default pinned to `v1.4.1`. Maps: INSTALL-01..04 + DOCS-02.
- **Phase 21** (CLOSED 2026-05-13) — Surface map + concept docs. Documented the five user-facing surfaces (Web UI on `:9996/ui/`, CLI `fbridge`, `/api/v1/chat` HTTP, MCP server via `fbridge mcp stdio` / `fbridge mcp http`, Flame hook on `:9999`) plus projekt-forge relationship. Output: `docs/GETTING-STARTED.md` (new; 115 lines) + rewritten README "What This Is" section + new H2 "Relationship to projekt-forge" subsection. Maps: DOCS-01, DOCS-03, DOCS-04. Phase shipped under writer's-room cadence (no formal GSD plans). DOCS-02 (CLAUDE.md ground-truth refresh) — assigned to Phase 20 in original v1.5 roadmap, but stale sections not fully caught at Phase 20 close; ground-truth refresh actually landed in Phase 21 under `fb01681` per D-04 fix-as-discovered cadence.
- **Phase 22** (CLOSED 2026-05-13) — Daily workflow recipes. Six recipes authored under writer's-room cadence; substrate/consumer architectural truth surfaced + propagated across two subsystems; demo-driver canonicalized as documentation pattern; recipes form progressive-trust operational arc. Output: `docs/RECIPES.md` (627 lines / 6 recipes). Maps: RECIPES-01..06.
- **Phase 23** (CLOSED 2026-05-14) — Diagnostics + recovery + observability-evolution arc. Four failure-mode sections authored in `docs/TROUBLESHOOTING.md` (575 lines) under the symptom→doctor→diagnosis→recovery→verification grammar; doctor polished in-flight via 2 code-touching commits (Commit A: llm_backends.detail reachability clarification; Commit B: new `postgres` row orthogonal to `storage_callback` with 4-state truth table teaching substrate/consumer split at doctor surface). All four DIAG modes covered in doctor output at degraded-tolerant rows; operational-survivability invariant ("doctor severity reflects what's usable, not what's impaired") encoded across all five degraded-tolerant rows. Output: `docs/TROUBLESHOOTING.md` + new postgres probe + llm_backend reachability detail. Maps: DIAG-01..05.
- **Phase 23.1** (CLOSED 2026-05-14) — Chat-convergence ship-blocker decimal patch following the Phase 20.1 precedent. Phase 23 closed with TROUBLESHOOTING.md + doctor-as-architectural-teaching landing cleanly, but post-close dogfood of the canonical operator query "What are the clips on Reel 1" exposed that the chat surface itself was operationally broken — model burned the 120s server-side LLM budget without ever producing a usable answer. Root-cause partition surfaced three architectural fixes shipped in-flight per D-20: (1) `flame_execute_python` MCP tool registered + repositioned via docstring rewrite as the canonical Flame introspection surface (function existed in `forge_bridge/tools/utility.py` pre-v1.5 but was never registered — the LLM was blind to the very escape hatch the substrate provides); (2) `forge_bridge/llm/call_wrapper.py` exhaustion-path message rewritten so a budget-exhausted-after-server-5xx event no longer renders as "LLM request timed out after Ns" (pre-23.1 misread by operators as a hard threshold); (3) function signature flattened from `execute_python(params: ExecutePythonInput)` to `execute_python(code: str, main_thread: bool = False)` — the BaseModel-wrapped signature generated a nested JSON schema FastMCP exposed to the LLM, which the chat model consistently failed to call (silent dispatch failure with zero wrapper logs despite `tool=flame_execute_python` appearing in the router log). Minimum observability shipped at the tool wrapper (`code_hash`/`main_thread`/`elapsed_ms`/`status`/`code_len` per invocation). Author-walk on portofino confirmed end-to-end pipeline correctness via the new utility.py:195 structured log line emitting `status=ok elapsed_ms=12 code_len=581`. Residual failure shape is purely local-32b inference speed (~71s/turn at 15K-token prompt context); diagnostic swap to `sensitive=False` cloud routing surfaced a separately-bounded bug (cloud agentic loop aborts pre-LLM in 0.2s with `tool_loop_error` — dark since FB-D because production hardcodes `sensitive=True`). Both residuals route to v1.6+ via seeds (`SEED-DEFAULT-MODEL-PERFORMANCE-V1.6+`, `SEED-CLOUD-CHAT-AGENTIC-ROUTER-V1.6+`). Output: `forge_bridge/tools/utility.py` docstring rewrite + signature flatten + observability instrumentation, `forge_bridge/mcp/registry.py` registration, `forge_bridge/llm/call_wrapper.py` message rewrite, 17 new tests across `test_flame_execute_python.py` + `test_llm_call_wrapper.py` + `test_tools.py` carve-outs, 5 v1.6+ seeds, v1.6 milestone framing artifact, Phase 23.1 CONTEXT.md. Maps: chat-path-operational-correctness (no formal v1.5 requirement — surfaced post-Phase-23-close as a Phase-23-dogfood gap).

**Phase 24 operational substrate loop — what landed (4 commits, all on origin/main pending push):**

| Goal | Status | Commit | Scope |
|---|---|---|---|
| Exists | pre-existing | `56ea87b` / `b66ceef` / `3c253bf` / `942413b` + Phase 23.1 | `_execute_python_core` body emits graph events at `~/.forge-bridge/graphs/<graph_id>.jsonl` |
| See — browser | ✅ | `033b093` | `fbridge graph list/show` closes §2.4 + Q20; 14 tests |
| See — doctor | ✅ | `c9c07c4` | `fbridge doctor` graph_store row closes Q18; tri-state ok/loaded/fail; 5 tests |
| Trust | ✅ portofino + ✅ flame-01 (dual-platform) | `163f07f` | Q8 POSIX append atomicity verified on **both** production platforms. macOS portofino was the harder case (PIPE_BUF=512, payloads 600/1500/3000 bytes intentionally above PIPE_BUF, 5 invariants held under 800 concurrent appends). Linux flame-01 (PIPE_BUF=4096) verified 2026-05-15: 1 passed in 2.81s. fcntl fallback stays **deferred-not-rejected** — operational trigger never manifested. |
| Drive | ✅ | `c091e5e` | `fbridge flame-exec` CLI delegates into shared `_execute_python_core`; graph_id surfaced in both render modes; 14 tests + 1 sibling-test alignment |

**Operator loop closed end-to-end on portofino against real Flame:**

```
$ fbridge flame-exec "import flame; print(flame.project.current_project.name)"
graph_id 7ab74e47e2a4456e8ec58c3e24b870fd
status   ok
1248-genesis-gv70-thrill-tv-mnt

$ fbridge graph show 7ab74e47e2a4456e8ec58c3e24b870fd
records: 2
... python loaded event=21e5485e  payload: {code_hash, main_thread, code_len}
... python ok     event=c2ade62d  payload: {code_hash, main_thread, elapsed_ms: 20, code_len}

$ fbridge doctor | grep graph_store
graph_store  ok    6 graphs, last 2026-05-15T05:59:53.087Z
```

This is the first moment the entire substrate became visibly self-consistent from the operator side.

**Architectural discipline preserved as load-bearing project philosophy:**

*Operator surfaces intentionally remained projections onto the same execution substrate rather than parallel execution paths.* The CLI delegates into `_execute_python_core` directly; both the MCP tool entry point (`execute_python`) and the CLI (`fbridge flame-exec`) emit `node_kind="python"` because the substrate truth is the same — *a Python execution against Flame occurred*. The operator surface is metadata, not ontology. Future contributors will absolutely try to bypass this; the shared-body refactor encoded it structurally so divergence requires deliberate intent.

**Honest asymmetry preserved (convergence-vs-operability) — posture shift after Q8 dual-platform closure:**

The append-only JSONL substrate has now been operationally verified under concurrent multiprocessing pressure on **both** production platforms, rather than relying on inherited POSIX assumptions. Combined with the Phase 24 operator-loop surfaces, the **substrate side of the bridge is increasingly evidenced and operationally hardened**. The remaining uncertainty has shifted: it is now primarily **human / LLM interaction-layer behavior**, not substrate correctness.

PROVED at this cursor:
- Observability — `fbridge doctor` graph_store row teaches substrate existence + health
- Execution — CLI surface drives the same execution path as the chat surface
- Replay — graph_id round-trips operator → graph show → doctor count
- Operator access — three Phase 24 surfaces compose without coupling
- **Substrate atomicity under concurrent multiprocessing pressure — dual-platform** (portofino macOS PIPE_BUF=512 with above-PIPE_BUF payloads; flame-01 Linux PIPE_BUF=4096). Operationally-verified truth, not inherited POSIX folklore.

NOT YET PROVED (remaining architectural obligations — now primarily interaction-layer):
- Reliable conversational convergence under non-author operator load (Phase 24 §2.1 discoverability + §2.2 chat converges reliably on Flame introspection)
- Non-author operability — UAT against the three Phase-20/20.1/21 seeds (`SEED-PHASE-20-NON-AUTHOR-UAT-V1.6+`, `SEED-PHASE-20.1-ARTIST-UAT-V1.6+`, `SEED-PHASE-21-NON-AUTHOR-UAT-V1.6+`)
- Session-graph propagation across multi-turn chat (currently each `flame_execute_python` invocation is a one-graph; chat-session graph propagation lands later per `forge_bridge/tools/utility.py:174` note)

**Phase 24 substrate sweep:** 124/124 passed across regression bands. Public `__all__` unchanged; no new external libraries; substrate refactor preserves byte-equivalence of existing tool-call observability.

**Next action:** intentionally unbound at this cursor. With Q8 dual-platform closure, the remaining natural candidates concentrate on the interaction-layer side: (a) non-author UAT against the operational surfaces now landed (would satisfy three of the v1.5-deferred seeds simultaneously); (b) chat-path convergence work toward §2.1 + §2.2 four-proofs; (c) session-graph propagation across multi-turn chat; (d) Phase 25/26 Paths A/B substrate work. Path selection is operator-direction surface, not synthesis-scope.

**Disposition of the four directional paths (per convergence §8):**

1. **Path A — Salvage formalization.** v1.6+ architectural; activates at Phase 25 / 26 protocol-normalization substrate work.
2. **Path B — Canonical operation-intent type.** v1.6+ architectural; closes implicitly at Phase 26 schema universalization (the canonical type IS the node record's `kind`+`name`+`inputs` shape per framing §5).
3. **Path C — Domain-boundary discipline.** **CLOSED at this convergence** — §7 of the convergence artifact promotes the discipline to forward-governance norm with §7.3 trigger conditions for re-opening anti-scope-bound work (intentionally unbound pending evidence, not implicitly rejected).
4. **Path D — Operator-facing perceived-responsiveness.** v1.6+; activates at Phase 24 (CLI surface + Flame escape hatch shortens conversational tool-free path); `SEED-FAST-FAIL-LLM-CONNECT-TIMEOUT-V1.5+` independent activation possible alongside Phase 24's misleading-timeout-message work.

**Carry-forward forward-routes (still live):** v1.6+ Layer 2 consumer integration per A.5.3.2 terminal cursor; non-author UAT against three Phase-20/20.1/21 seeds when a fresh non-author reader becomes available; v1.6.x patches per framing §13.1 deferral register.

**Key constraints (binding for v1.5):**

- Legibility, not features. No new external libraries. Public `forge_bridge.__all__` stays at 19 unless something genuinely shifts during install audit.
- Forcing function: if `docs/INSTALL.md` doesn't work end-to-end on a clean machine, we don't ship it. Phase 20 will likely surface deployment gaps that get fixed in-flight (and may add code-fix plans alongside doc plans).
- Internal codebase audit + workflow articulation — no external research phase.
- RECIPES-04 (multi-step Flame chat) and RECIPES-05 (staged-ops approval) require a working Flame setup — assume assist-01 or equivalent operator workstation.
- Phase 23 DIAG-05 (`forge doctor` parity) may require in-flight polish — "polish forge doctor if gaps surface" is in Phase 23 scope, not a separate phase.
- All 16 planted seeds in `.planning/seeds/SEED-*.md` are out of v1.5 scope (feature work for v1.6+: auth, chat enhancements, model bumps, tool-call optimizations, staged-ops follow-on, CMA memory).
- Done state: user can sit down, follow the docs, and use forge-bridge in daily VFX workflow without re-deriving the deployment topology each time.

## Performance Metrics

**Velocity (v1.0–v1.3 baseline):**

- Total plans completed: 93 (across milestones v1.0–v1.3)
- v1.0 phases: 3 phases, 13 plans
- v1.1 phases: 3 phases, 13 plans
- v1.2 phases: 3 phases (7, 07.1, 8), 12 plans, 17 tasks
- v1.3 phases: 4 phases (9, 10, 10.1, 11), 20 plans

**By Phase (v1.0):**

| Phase | Plans | Status |
|-------|-------|--------|
| 1. Tool Parity & LLM Router | 7 | Complete |
| 2. MCP Server Rebuild | 3 | Complete |
| 3. Learning Pipeline | 3 | Complete |

**v1.1 Phase 5 Metrics:**

| Plan | Duration | Tasks | Files |
|------|----------|-------|-------|
| Phase 05 P03 | ~15min | 2 tasks | 3 files |
| Phase 05 P04 | ~12min | 1 task  | 1 file  |
| Phase 07.1 P02 | 9min | 2 tasks | 2 files |
| Phase 07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat P03 | 8min | 1 tasks | 1 files |
| Phase 16.2 P01 | 3min | 2 tasks | 4 files |
| Phase 16.2 P02 | 2min | 1 tasks | 1 files |
| Phase 16.2 P03 | 1min | 1 tasks | 1 files |

## Accumulated Context

### Roadmap Evolution

- Phase 07.1 inserted after Phase 7: startup_bridge graceful degradation hotfix + deployment UAT (URGENT) — Phase 7 UAT surfaced a deployment-blocking bug in forge-bridge.mcp.server.startup_bridge; exception from _client.start() escapes the try/except intended to guard wait_until_connected. Latent in v1.2.0. Fix + v1.2.1 hotfix + re-UAT via real MCP client before closing v1.2 milestone.
- v1.3 roadmap written 2026-04-22: 4 phases (9-12), 37 requirements across 8 categories. Phase 12 (LLM Chat) explicitly velocity-gated — may defer to v1.4 if Phases 9-11 run long.
- v1.4 pre-design dated 2026-04-23 in ROADMAP.md alongside v1.3 close — FB-A..FB-D scoped against projekt-forge v1.5 declared deps (consumer-driven naming). Phase 12 superseded by FB-D in same audit.
- v1.4 roadmap formalized 2026-04-25 by gsd-roadmapper: FB-C success criteria grew from 4→7 (absorbed LLMTOOL-04 repeat-call detection, LLMTOOL-05 8KB result truncation, LLMTOOL-06 sanitization boundary, LLMTOOL-07 recursive-synthesis guard — all surfaced by targeted FB-C research). FB-D success criteria grew from 4→5 (absorbed CHAT-05 external-consumer parity with projekt-forge Flame hooks). Total v1.4 requirements: 19 (STAGED 7 + LLMTOOL 7 + CHAT 5).
- Phase 16.2 inserted after Phase 16.1: Bug D — chat tool-call loop renders raw JSON instead of executing tools and synthesizing answer (URGENT) — surfaced in Phase 16.1 fresh-operator UAT on assist-01 2026-04-28; v1.4 milestone close blocked until Bug D fixed and CHAT-04 fresh-operator UAT records PASS. Investigation scope: LLMRouter agentic loop, chat handler tool dispatch, UI rendering, plus strengthen Strategy B chat E2E assertion to reject tool-call-only responses.
- v1.5 roadmap formalized 2026-04-30 by gsd-roadmapper: 4 phases (20-23), 19 requirements across 4 categories (DOCS/INSTALL/RECIPES/DIAG). Phase numbering continues from v1.4.x (last shipped phase 19). Phase 20 is a forcing function — INSTALL.md must pass fresh-machine UAT before the milestone ships. DOCS-02 assigned to Phase 20 (not Phase 21) because the install audit walk-through discovers the CLAUDE.md ground-truth gaps.
- Phase 20.1 inserted after Phase 20 on 2026-05-01: Install Bootstrap Script + Systemd Daemon (v1.5 ship blocker) (URGENT) — Phase 20 Track A author-walked UAT surfaced 13 gaps confirming `docs/INSTALL.md` as prose is not shippable to artists. Architecture works (cross-host LLM via assist-01 Ollama validated end-to-end); install procedure does not. 20.1 spine: two systemd units (`forge-bridge-server.service` for `:9998` bus, `forge-bridge.service` for `:9996` MCP+Console with `Requires=` ordering), `scripts/install-bootstrap.sh` (Postgres bootstrap + pg_hba alignment + role+db + alembic + units install), `/etc/forge-bridge/forge-bridge.env` (system env file with `EnvironmentFile=`), INSTALL.md reshape collapsing Steps 3/5/6 to script + edit + systemctl. Primary acceptance criterion: a Flame artist with no Linux/Postgres knowledge can complete the install. Validation requires actual non-author UAT, not author-walked-with-deviation. Capture: `.planning/phases/20-reality-audit-canonical-install/20-PHASE-20.1-CANDIDATE.md`.

- Phase 20.1 CLOSED 2026-05-12 — INSTALL-01 v1.5 ship-blocker requirement met. flame-01 walk against `242996b` rerun pass clean: 6 real surfaces green (mcp / console / ws_server / daemon_state / llm_backend.local / flame_bridge); 4 expected warnings (storage_callback degraded-tolerant / llm_backend.cloud Anthropic not configured / jsonl_parseability first-run / sidecar+probation_dir first-run). Idempotency contract held under rerun (pg_hba already aligned via marker-managed block; both units unchanged via diff-before-restart; env file preserved; "could not change directory" warnings suppressed via psql_as_postgres wrapper; doctor invocation succeeded via 15s wait-for-:9996 loop). Session contribution: 3 commits — `e1037d7` refactor(20.1) pg_hba marker-managed + diff-before-restart (convergence-pass per brief v2 §A7 + §C; preserves 447-line operational archaeology per `feedback_brief_examples_as_behavioral_reference_shapes`) + `d2895ad` docs(20.1) README Quick Start fix (parity with INSTALL.md Step 6c) + `242996b` fix(20.1) three D-04 walk-surfaced gaps (psql wrapper + env_just_created flag + :9996 wait loop). D-04 in-flight discipline operationalized cleanly (walk-surfaced gaps fixed as separate commit, no amendment of upstream commits). No close artifact in `.planning/phases/...` — operator-confirmed: commit chain + brief v2 + walk transcript carry the archaeology.

- Phase 23 CLOSED 2026-05-14 — DIAG-01..04 catalogue authored in `docs/TROUBLESHOOTING.md` (575 lines / 4 failure-mode sections) + DIAG-05 doctor-coverage parity achieved via 2 code-touching commits during the same writer's-room arc. **6 commits this phase, NOT yet pushed:** `6619b53` docs(23) TROUBLESHOOTING.md scaffold + DIAG-04 cold-start recovery (137 lines; symptom-first ordering; doctor-as-visual-anchor framing; recipe cross-linking; reassurance sentence at Symptom→Doctor boundary; archaeology in "Why this happens" not Recovery; coverage-commitment encoded as explicit prose) → `9bbd250` docs(23) DIAG-03 Ollama-unreachable (+121 lines; ~75s wait + HTTP 500 fingerprint distinguished from DIAG-04's 60-125s + HTTP 504 fingerprint; SEED-FAST-FAIL-LLM-CONNECT-TIMEOUT-V1.5+ + SEED-EXTERNAL-DEPENDENCY-PREFLIGHT-PROBES-V1.5+ cross-referenced; doctor blind spot acknowledged + queued for follow-up) → `8e0562d` docs(23) DIAG-02 Postgres-unavailable (+144 lines; JSONL-authoritative + SQL-mirror substrate framing; 8-surface "degraded vs dead" behavior table; substrate/consumer pattern manifesting at failure-mode level; Recipe 3 explicitly named as workflow-that-WON'T-break under this failure mode; second doctor blind spot acknowledged) → `6446356` **feat(23,doctor) Commit A: llm_backends.detail reachability clarification** (observability clarification — existing signal, existing concept, better operator wording; "reachable; model=..." / "unreachable; model=..." replaces previous "model=..." that was constant whether reachable or not; 2 tests added pinning detail-string semantics; credential-leak rule honored — local_error str(exc) NOT propagated; first half of doctor polish; 78 LOC / 13 passing in test_console_health.py + 43/43 cross-cutting) → `7786c83` **feat(23,doctor) Commit B: postgres reachability row + orthogonality archaeology** (observability extension — new probe, new health semantics, new subsystem cognition, new recovery affordance; `_check_postgres()` via `get_session() → SELECT 1` within 2.0s bounded timeout; parallel-gathered with other I/O probes; classified degraded-tolerant per operational-survivability invariant; orthogonal to storage_callback — 4-state truth table preserved in commit body + read_api.py docstring + TROUBLESHOOTING.md DIAG-02 + memory; 5 semantic tests in test_console_health.py + 1 CLI mapping test in test_cli_doctor.py; 322 LOC / 62 passing across health-adjacent surfaces) → `d88ec45` docs(23) DIAG-01 Flame-hook-unreachable (+131 lines; graceful-degradation framing aggressive — "Flame integration surface unavailable" not "bridge down"; 3 symptom variants surfaced; 3 doctor detail shapes named; 5 likely causes Flame-side-first; Phase 07.1 graceful-degradation contract cross-referenced as classification origin). This STATE.md update is the phase-close commit (7th). **The maturation event:** observability stopped being status reporting and became architectural teaching. Doctor output now renders forge-bridge's middleware architecture at the orientation surface — **five degraded-tolerant rows** (`flame_bridge`, `ws_server`, `storage_callback`, `postgres`, `llm_backend.*`) collectively teach **degradation boundaries** (which subsystem owns which failure mode), **subsystem ownership** (model / persistence / DCC / orchestration layers cleanly partitioned), **substrate/consumer separation** (rendered directly by the storage_callback × postgres 4-state truth table), and **operational survivability** (the invariant: doctor severity reflects what's usable, not what's impaired). Future contributors who ask "why isn't postgres surfaced as fail?" find the answer in commit body + read_api.py docstring + doctor.py degraded-tolerant comment + test_health_postgres_fail_flips_overall_to_degraded_not_fail — four redundant archaeology sites, deliberately. **Diagnostic-grammar consistency** (symptom → doctor signal → diagnosis → recovery → verification → recipe re-entry → why this happens → cross-references) repeated across all four DIAG sections with intentional cadence consistency — repetition becomes operator interface. Operators can mentally partition failures correctly across four layers and recover within ~2 minutes per the doctrine baked into every section. **Doc-driven observability evolution arc** — first true instance: docs surfaced two observability mismatches (DIAG-03 + DIAG-02 both flagged doctor blind spots); both mismatches clarified semantically BEFORE code changed (orthogonality table evaluated; trivial-query semantic chosen over TCP-reachable / migration-current / callback-registered alternatives; degraded-tolerant classification anchored to operational-survivability invariant); code change preserved architectural truths docs established (storage_callback wording untouched; postgres semantics narrow + concrete; SELECT 1 named explicitly in detail); resulting doctor output now teaches the system model rather than merely listing subsystem status. Maturity step worth preserving as `project_state_2026_05_14_phase_23_closed.md` memory cursor. **No close artifact in `.planning/phases/...`** — mirrors Phase 20.1 + Phase 21 + Phase 22 patterns; commit chain + STATE cursor + memory cursor carry the archaeology. Phase directory `.planning/phases/23-diagnostics-recovery/` does not exist (writer's-room phases ship without phase dirs by precedent). **v1.5 milestone-close evaluation is a separate governance question** — intentionally unbound at this cursor.

- Phase 22 CLOSED 2026-05-13 — RECIPES-01..06 authored under writer's-room cadence matching Phase 21. **12 commits this phase, all pushed at close:** `2893879` scaffold + RECIPES-01 → `6e22860` GETTING-STARTED.md relink → `0ec24a8` RECIPES-02 Claude Desktop wiring → `be4e008` RECIPES-02 Step 3 fix (cold-read #1) → `52cd0ad` RECIPES-03 observe synthesis pipeline → `31fe7bb` CLAUDE.md substrate/consumer (synthesis) → `a6bbc3c` RECIPES-03 Step 5 manifest jq fix (cold-read #2) → `e818701` RECIPES-06 inspect synthesis manifest → `cc52250` STATE.md mid-arc cursor → `076f880` RECIPES-04 Drive Flame from chat → `94f4a6d` RECIPES-05 Approve a staged operation → `7cebada` CLAUDE.md substrate/consumer (staged ops). This STATE.md update is the phase-close commit (13th). **Recipes form a coherent operational arc, not 6 standalone tutorials:** bring-up (1) → wiring (2) → observability (3) → inspectability (6) → orchestration (4) → authority (5). The reader who walks them in operator-traversal order builds the mental model needed to trust the bridge progressively — first as a runtime, then as a surface set, then as an observable system, then as an inspectable artifact registry, then as a chat-driven orchestrator, then as a destructive-action authority gate. Each recipe deepens what came before. Recipe numbering follows v1.5 ROADMAP requirement IDs; readable traversal jumps to 6 after 3 per psychological-coherence framing operator-validated at Recipe 6 framing. **Substantive architectural truth surfaced + propagated, dual-subsystem manifestation:** during Recipe 3 authoring, `ExecutionLog.record()` was found to have zero in-package callers — synthesis pipeline ships as substrate. Operator framing: "forge-bridge intentionally ships reusable orchestration substrate while consumers decide what operational telemetry and authority flows to emit into it" — middleware architecture behaving correctly, NOT incomplete. Dormant-on-stock-install IS the architectural shape. During Recipe 5 authoring, the same pattern was found in the staged-ops platform: bridge ships approval surface + state machine + audit log; consumer ships propose-side MCP tools (`forge_stage_rename`, `forge_stage_publish_shots`, `forge_stage_set_startframes` — live in projekt-forge). Both subsystems dormant on stock install; both correct behavior. Framing now load-bearing across (1) `CLAUDE.md` Learning pipeline subsystem bullet (synthesis substrate/consumer at `31fe7bb`), (2) `CLAUDE.md` Staged operations platform subsystem bullet (approval-surface-only at `7cebada`), (3) `docs/RECIPES.md` Recipe 3 + Recipe 5 framing, and (4) project memory `project_forge_bridge_substrate_not_producer.md` (generalized dual-subsystem; Phase 22 rewrite). **Demo-driver canonicalized as documentation pattern:** two instances landed — Recipe 3 synthesis driver (~20 lines using `ExecutionLog` + `SkillSynthesizer` + `normalize_and_hash` + `get_count` + `mark_promoted`), Recipe 5 staged-ops driver (~12 lines using `StagedOpRepo.propose()` via `get_session()`). The methodological shape — *expose substrate, inject minimal producer, observe lifecycle deterministically* — is reusable for any future subsystem that follows the substrate/consumer split. Methodology candidate worth preserving for future docs work. **Three-host topology operator-stated 2026-05-13:** `assist-01` = dedicated LLM/orchestration node; `flame-01` = dedicated DCC workstation; `portofino` = converged operator workstation. Recipes 4 + 5 use portofino as canonical (LLM + bridge + Flame on one host) with brief distributed-deployment notes acknowledging the other topology shapes — keeps operator focus on workflow / authority / trust / orchestration rather than distributed-infra debugging. forge-bridge does not require co-location; surfaces can be distributed; different studios may choose different topology shapes. **2 in-flight cold-read fixes landed during expansion** (no formal UAT pass needed — fix-as-discovered loop substituted, matching Phase 21 cadence): `be4e008` RECIPES-02 Step 3 cross-platform JSON validation, `a6bbc3c` RECIPES-03 Step 5 manifest jq pipeline + provenance field correction. **Recipe 3 reframed mid-arc** from "Watch a tool get synthesized" → "Observe the synthesis pipeline operate" per operator framing (infrastructural + observable, not synthesis-spectacle). **Phase 23 (Diagnostics + recovery → `docs/TROUBLESHOOTING.md`) naturally positioned:** after setup, workflows, orchestration, approvals, and auditability, the next natural reader question is "what do I do when something breaks?" Phase sequencing itself proving coherent — Phase 23 isn't an arbitrary follow-on, it's the architectural successor to the trust progression Phase 22 built. **No close artifact in `.planning/phases/...`** — mirrors Phase 20.1 + Phase 21 patterns; commit chain + STATE cursor + project memory carry the archaeology. Phase directory `.planning/phases/22-daily-workflow-recipes/` exists but contains no artifacts.

- Phase 21 CLOSED 2026-05-13 — DOCS-01 (README "What This Is" rewrite) + DOCS-03 (new `docs/GETTING-STARTED.md` surface-map concept doc) + DOCS-04 (projekt-forge relationship statement landed in both README lede H2 subsection and GETTING-STARTED.md closing note) covered. **5-commit writer's-room arc:** `5d155a2` docs(21) README lede rewrite + new GETTING-STARTED.md concept doc (115 lines; operator-authored four-paragraph framing + five-surface bullet list + canonical Relationship-to-projekt-forge subsection; stale `## Architecture` ASCII diagram removed; stale "extracted from projekt-forge" Relationship section removed) → `fb01681` docs(21) CLAUDE.md DOCS-02 refresh (4 operator-flagged stale sections + 1 bundled Repository-Layout drift-correction; ground truth verified against `forge_bridge/cli/main.py` + `pyproject.toml [project.scripts]` per `feedback_ground_specs_in_actual_files`) → `cd51ac1` docs(21) fbridge verb-usage canonicalization (cold-read UAT finding: `forge-bridge` conflated as both project-noun and CLI-verb; rule applied — verbs → `fbridge`, nouns → `forge-bridge`; back-compat alias preserved with single footnote in README Quick Start; structural rename `console doctor` → `doctor` and `console tools` → `actions` applied per top-level surface) → `f4272f7` docs(21) README status table refresh (rows 34 + 41 — MCP server entry + CLI surface row — compressed to current-surface inventory matching existing "Shipped (vX; expanded through vY)" pattern) → `b7297bc` docs(21) three forthcoming-RECIPES.md unlinks (link-check companion; intentional dead-link click cost removed while preserving named-future-surface signal). **Author-walked cold-read UAT performed** under writer's-room discipline (linear top-to-bottom read of both reader docs; one concrete friction point surfaced = verb/noun conflation; rest passed). **Formal non-author UAT deferred** — no non-author reader available at phase close; gap archaeologically recorded in `SEED-PHASE-21-NON-AUTHOR-UAT-V1.6+.md` with one concrete friction point as evidence (weaker than ideal — the author's mental model masks reader-comprehension gaps the author cannot see by definition). **DOCS-02 phase-mapping note (operator-stated):** DOCS-02 (CLAUDE.md ground-truth refresh) was assigned to Phase 20 in the original v1.5 roadmap, rationale: install audit walk-through is what surfaces CLAUDE.md divergence from reality. Stale sections were not fully caught at Phase 20 close; ground-truth refresh landed in Phase 21 under `fb01681` per D-04 fix-as-discovered cadence. Requirement→phase mapping in ROADMAP.md unchanged; commit-arc archaeology records the actual landing point. No formal GSD plans generated — operator declared at phase open "we don't need to use GSD; we've had a pretty good run of using the writer's room." Phase directory `.planning/phases/21-surface-map-concept-docs/` exists but contains no artifacts (mirrors Phase 20.1's no-close-artifact pattern; commit chain + STATE cursor + seed carry the archaeology).

### Decisions

Recent decisions affecting current work:

- [v1.0 Phase 3]: Synthesized tools must use bridge.execute(), never import flame — tools run in MCP server process, not inside Flame
- [v1.0 Phase 3]: Manifest-based file validation in watcher prevents arbitrary code execution from rogue synthesized files
- [v1.1 Roadmap]: Phase ordering is strict — Phase 5 cannot start until Phase 4 is complete; Phase 6 cannot start until both are done
- [Plan 05-00]: Four projekt-forge fixes ported upstream (query_lineage/query_shot_deps/media_scan builders, entity_list narrowing, ref_msg_id correlation fallback, timeline T0 gap-fill) — released as forge-bridge v1.0.1 so projekt-forge can delete local duplicates in Wave B
- [Plan 05-00]: `project_name` kwarg on AsyncClient.__init__ stays in projekt-forge fork (NOT upstreamed) — forge-specific multi-project routing, per RESEARCH §D-09b
- [Plan 05-01]: Single atomic commit over rename-aware 1597-line diff chosen over the 2-commit split heuristic — the work is mechanical/programmatic and reviewers benefit more from `git diff --find-renames` on the combined rewrite than from an artificial rename-first/sed-second separation
- [Plan 05-01]: sed rewrite scope expanded beyond plan's `from forge_bridge\.` pattern to cover (1) bare `from forge_bridge import bridge` imports in projekt_forge/tools/, (2) quoted string-literal `unittest.mock.patch("forge_bridge...")` targets across 9 test files, (3) `import forge_bridge.X as Y` statements, and (4) filesystem-path string literals in 2 static-analysis tests — all variants of the same local-package-reference rewrite the plan intended
- [Plan 05-01]: Cosmetic `forge_bridge` mentions (log channel name, FastMCP display name, argparse prog, usage-text `python -m forge_bridge`) intentionally left for Wave C's server/mcp.py + __main__.py rewrite — no runtime/test impact in Wave A
- [Plan 05-02]: D-09b resolved to branch-b (preserve local clients) — projekt-forge's db_server relies on extended HELLO project_name for per-project DB routing; swapping to canonical pip AsyncClient would silently break DB routing. All 3 client files retained: client/__init__.py, client/async_client.py, client/sync_client.py.
- [Plan 05-02]: Two-commit execution instead of single — a precondition fix(hooks) commit (4d2b579) for the sys.path.insert→append collision at 17 sites, then the atomic refactor(projekt_forge) RWR-02 commit (9856376). The plan wrote "single atomic commit" but the hook issue was discovered during Wave B execution and its fix is mechanically separate from RWR-02 (pip-dep + deletes + import flips remain atomic in commit 9856376).
- [Plan 05-02]: tests/test_switch_grade_mcp.py pared from 22 to 15 tests per user-approved Option 1 — deleted 7 projekt-forge-specific tests asserting on fork-only SwitchGradeInput fields (alternative_path/alternative_start_frame/alternative_duration/entity_id), server-side openclip writer (_write_openclip_server_side / forge_openclip_writer / _switch.clip naming), and UUID path construction. Rewrote 4 tests to use pip's media_path field. Coverage of pip surface maintained and expanded.
- [Plan 05-02]: D-12 discovered during execution — Flame hook scripts' sys.path.insert(0, scripts_dir) shadows pip forge_bridge/ because scripts dir contains sibling forge_bridge.py hook module. Resolution: insert→append at 17 sites; 05-04's autouse conftest fixture is the long-term guard.
- [Plan 05-02]: Pytest invocation standardized to `pytest tests/` (explicit path) — default `pytest` collection in projekt-forge pollutes from flame_hooks/forge_tools/forge_bridge/scripts/forge_llm_test.py (matches *_test.py AND sys.exit(1) on Ollama offline). Pre-existing issue noted in SUMMARY but not fixed in 05-02.
- [Phase 05]: [Plan 05-03]: Expanded Task 2 scope to cover projekt_forge/server/__init__.py as a Rule 3 blocking auto-fix -- the package __init__ re-exported 'main' from server.mcp which Task 1 explicitly removed, so without the three-file atomic commit the package becomes unimportable mid-wave. The alternative (split the __init__ fix into a separate commit) would have broken RWR-03's atomic-commit requirement.
- [Phase 05]: [Plan 05-03]: Introduced _run_mcp_only(args) helper in __main__.py instead of leaving the two 'from projekt_forge.server.mcp import main as mcp_main' callers dangling -- the helper configures the bridge via forge_bridge.bridge.configure and calls mcp.run() directly, so the canonical FastMCP lifespan owns startup_bridge/shutdown_bridge per Phase 4 API-04/API-05 and projekt-forge never duplicates the lifecycle.
- [Phase 05]: [Plan 05-03]: Swept the Wave A cosmetic leftovers in __main__.py (argparse prog label, module docstring, logger channel name) from 'forge_bridge' to 'projekt_forge' in the same atomic commit as the architectural rewire -- Plan 05-01 SUMMARY flagged these as Wave C's responsibility.
- [Phase 05]: [Plan 05-04]: D-06 editable-shadow reconciliation handled per plan STEP 4 — uninstalled the editable forge-bridge shadow (0.1.0 from /Users/cnoellert/Documents/GitHub/forge-bridge) and installed forge-bridge 1.0.1 from the git tag into site-packages before committing the fixture. The fixture's site-packages assertion is by design incompatible with the editable dev loop; if the user reinstates the editable shadow for forge-bridge development, pytest will fail with a clear remediation message. This is the guard working as intended, not a regression.
- [Phase 05]: [Plan 05-04]: Duplicate `import pytest` omitted from the appended conftest block — plan STEP 3 explicitly permits this reconciliation when pytest is already imported at file top (it is, at line 17). `pathlib` and `forge_bridge` are new and stay inline for section cohesion per the plan's guidance. This is plan-approved, not a deviation.
- [Phase 05]: [Plan 05-04]: projekt-forge pyproject.toml missing `tool.hatch.metadata.allow-direct-references = true` prevents `pip install -e /path/to/projekt-forge` from resolving the forge-bridge git-URL dep. Worked around in this plan by installing forge-bridge directly from its git tag. Adding the hatchling flag to projekt-forge's pyproject.toml is a projekt-forge packaging concern flagged as follow-up — out of scope for RWR-04.
- [Phase 06 scope]: 2026-04-18 — SQL persistence backend for ExecutionLog (EXT-03) stays deferred to v1.1.x. Phase 6 focuses on LRN-01..04 wiring only: JSONL log path injection, LLMRouter construction from forge_config.yaml, storage callback hook, pre_synthesis_hook. Rationale: (1) v1.1 milestone goal is "wire it, don't redesign it" — integration, not persistence redesign; (2) projekt-forge already has its own DB layer that can consume the storage callback directly, so SQL-in-bridge isn't blocking; (3) Phase 5 scope already stretched three days — keeping Phase 6 focused minimizes schedule risk; (4) EXT-03 gets a dedicated v1.1.x patch when a consumer actually needs it.
- [Phase 07.1]: v1.2.1 release ceremony mirrors Phase 7-04 v1.2.0 precedent (0987525); annotated tag v1.2.1 locks downstream identity for projekt-forge @ git+... re-pin
- [Phase 07.1]: Historical changelog comment in tests/test_public_api.py preserved across releases (1.0.0 → 1.0.1 → 1.1.0 → 1.2.0 → 1.2.1) — minor departure from Plan 02's strict grep-zero acceptance criterion, intentional convention-preservation
- [Phase 07.1-startup-bridge-graceful-degradation-hotfix-deployment-uat]: Approved Option A editable-shadow remediation before cross-repo pin bump: pip uninstall -y forge-bridge in forge conda env, then pip install -e .[dev,test] from projekt-forge to resolve fresh from @v1.2.1 tag. Shadow source was byte-identical to tag source (HEAD f069407 is one docs-only commit ahead of v1.2.1 at abd047c), so remediation is a clean no-behavior-change operation that restores direct_url.json identity lock.
- [v1.3 Roadmap, 2026-04-22]: Console serves on a separate uvicorn task on `:9996` inside `_lifespan` — NOT via FastMCP.custom_route (only works in `--http` mode; stdio is the locked default and custom_route would break Claude Desktop/Claude Code configurations).
- [v1.3 Roadmap, 2026-04-22]: MFST-02 and MFST-03 (MCP resource + tool fallback shim) ship in the same Phase 9 plan — P-03 prevention; Cursor and Gemini CLI do not support resources and the shim costs one function.
- [v1.3 Roadmap, 2026-04-22]: Phase 12 (LLM Chat) is velocity-gated — explicitly deferrable to v1.4 if Phases 9-11 run long; must be an explicit scope decision before Phase 11 closes, not a silent drop.
- [Phase 11, 2026-04-24]: Phase 12 velocity gate decision honored — Phase 12 superseded by FB-D in ROADMAP. v1.3 closes with Phases 9, 10, 10.1, 11 (LLM Chat work moves to v1.4 FB-D).
- [Phase 11, 2026-04-24]: D-04 W-01 stays open — `execs --tool` runs client-side with locked stderr note. Server-side `/api/v1/execs?tool=...` is a v1.4 API extension; Phase 11 deliberately consumed Phase 9/10 API as-is and added zero server endpoints.
- [Phase 11, 2026-04-24]: Soft UAT gate (D-08) is the right tool for technical CLI surfaces — developer-as-operator with the "can I decipher" criterion produced a useful PASS plus one v1.4-deferred UX note (manifest/tools render visually indistinguishable). Bundle the manifest caption + cross-link fix with Phase 10.1 humanized-timestamps follow-up in a v1.4 polish pass.
- [v1.4 Roadmap, 2026-04-25]: Phase letter scheme `FB-A..FB-D` is LOCKED — consumer-driven by projekt-forge v1.5 declared deps. NOT renumbered to phases 12..15. Pre-discussed at v1.4 open and re-confirmed at roadmap formalization.
- [v1.4 Roadmap, 2026-04-25]: FB-C success criteria expanded from pre-design's 4 to 7 to absorb LLMTOOL-04 (repeat-call detection), LLMTOOL-05 (8 KB result truncation), LLMTOOL-06 (sanitization boundary), LLMTOOL-07 (recursive-synthesis guard) — all surfaced by targeted FB-C research. FB-C is the largest phase by criterion count.
- [v1.4 Roadmap, 2026-04-25]: FB-D success criteria expanded from pre-design's 4 to 5 to add CHAT-05 (external-consumer parity with projekt-forge Flame hooks). FB-D is the v1.5 cross-consumer integration point, not just the Web UI chat panel.
- [v1.4 Roadmap, 2026-04-25]: FB-A and FB-C are parallelizable — no shared dependency. FB-B → FB-A (entity before its surface). FB-D → FB-C (loop before chat endpoint). Sequencing FB-A and FB-C concurrently is a legitimate execution-order option.
- [v1.4 Roadmap, 2026-04-25]: Sanitization patterns consolidate into a single source of truth in FB-C — Phase 7's `_sanitize_tag()` and FB-C's `_sanitize_tool_result()` share `forge_bridge/_sanitize_patterns.py` (or equivalent). This is a FB-C scope item, not a new phase.
- [v1.4 Roadmap, 2026-04-25]: `LLMLoopBudgetExceeded` exported from `forge_bridge.__all__`. Barrel grows 16→17. Required so callers (FB-D chat endpoint) can catch it and translate to HTTP 504/408.
- [v1.4 Roadmap amendment, 2026-04-25]: Dual-naming applied — `Phase 13..16` numeric IDs added alongside preserved `FB-A..FB-D` aliases. Forced by `gsd-tools` impedance: `normalizePhaseName()` only strips letter prefixes when followed by a digit, so `FB-A` cannot resolve via `find-phase` and the discuss/plan/execute pipeline silently fails. Numeric mapping skips 12 (taken by superseded "LLM Chat" — 12 still exists in v1.3 history as superseded). The original locked decision (no renumber) was about preserving the cross-repo contract with projekt-forge v1.5; the amendment satisfies that intent (alias preserved everywhere) while adding the numeric plumbing tooling needs.
- [Phase 16.2]: [Phase 16.2-01]: Bug D D-03 hypothesis CONFIRMED with reproduce-from-disk evidence — captured Ollama response from assist-01 shows tool_calls=null and content='{"name": "forge_tools_read", "arguments": {"name": "synthesis-tools"}}'. RED adapter test landed at tests/llm/test_ollama_adapter.py::TestOllamaToolAdapterBugDFallback::test_text_content_tool_call_salvaged with explicit Bug D regression message. Plan 02 GREEN fix unblocked.
- [Phase 16.2]: [Phase 16.2-01]: Captured fixture lives in BOTH operator-readable JSON in planning tree AND verbatim Python constant in test file; equality asserted at verify time per threat T-16.2.01-01 to prevent drift. Pattern reusable for future captured-fixture testing.
- [Phase 16.2]: [Phase 16.2-02]: Bug D GREEN fix landed in OllamaToolAdapter. _try_parse_text_tool_call helper at module scope salvages a _ToolCall from message.content when message.tool_calls is empty AND content matches the canonical {name, arguments} JSON shape. Helper never raises (returns None on parse failure). Salvage hook in send_turn resets text="" on success to prevent double-emit. Plan 01's RED test flipped to PASSED; full tests/llm/ suite 91/91 green; router.py + handlers.py byte-identical to main.
- [Phase 16.2]: [Phase 16.2-02]: Helper placement decision — _try_parse_text_tool_call lives at module scope between _TurnResponse (line 114) and _ToolAdapter Protocol (line 195) because it constructs _ToolCall (line 86). Plan-compliant; the plan's 'after _OLLAMA_KEEP_ALIVE' hint was a module-level placement directive, not a strict line constraint. Reusable pattern for future adapter-layer salvage helpers.
- [Phase 16.2-03]: Strategy B chat E2E (test_chat_canonical_uat_prompt_under_60s) strengthened with two additive D-06 assertions: regex-reject raw tool-call JSON (^\s*\{\s*"name"\s*:) as terminal content, AND assert agentic loop iterated (>=1 role=tool turn AND >=2 role=assistant turns). Module-top compiled regex _BUG_D_TERMINAL_JSON_RE; module-top import re. Fixture body byte-identical; no router/adapter mocks introduced. CONTEXT D-07 'natural prose detection' heuristic explicitly rejected — too brittle, false-positives on legitimate one-word answers. Default pytest skips cleanly (1 skipped, 0 failed).
- [v1.5 Roadmap, 2026-04-30]: DOCS-02 (CLAUDE.md ground-truth refresh) assigned to Phase 20, not Phase 21. Rationale: the install audit walk-through is what surfaces the CLAUDE.md divergence from reality — the discovery and fix are co-located in the same phase. Phase 21 (surface map) can then rely on already-accurate ground truth.
- [v1.5 Roadmap, 2026-04-30]: Phase 20 is the milestone forcing function. INSTALL.md must pass a fresh-machine non-author UAT walk-through before the phase closes. Deployment gaps found during authoring are fixed in-flight as Phase 20 plans — not deferred.
- [v1.5 Roadmap, 2026-04-30]: RECIPES-04 and RECIPES-05 carry a prerequisite assumption: assist-01 or equivalent operator workstation with Flame running. This is documented in Phase 22 success criteria and must be stated explicitly in the recipe text.
- [v1.5 Roadmap, 2026-04-30]: Phase 23 DIAG-05 scope includes in-flight `forge doctor` polish — gaps surfaced during recipe or troubleshooting authoring are closed in Phase 23, not as a separate decimal phase.

### Pending Todos

None. Roadmap formalized; ready to plan Phase 20.

### Blockers/Concerns

- **None.** All 19 v1.5 requirements mapped (INSTALL-01..04 + DOCS-02 → Phase 20; DOCS-01,03,04 → Phase 21; RECIPES-01..06 → Phase 22; DIAG-01..05 → Phase 23). Coverage 19/19.

## Deferred Items

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| EXT | Tool provenance in MCP annotations (EXT-02) | Shipped v1.2.0 (Phase 7) | — |
| EXT | SQL persistence backend for ExecutionLog (EXT-03) | Shipped v1.3.0 (Phase 8) | — |
| EXT | Shared synthesis manifest between repos (EXT-01) | Pulled into v1.3 Artist Console (MFST-06) | — |
| v1.4 | SSE/WebSocket streaming push | Deferred — poll-first for v1.3 | v1.3 roadmap |
| v1.4 | Multi-project console view | Deferred — single-bridge/single-project in v1.3 | v1.3 roadmap |
| v1.4 | Promotion sparklines / rich historical charts | Deferred | v1.3 roadmap |
| v1.4 | Admin/mutation actions (quarantine, promote, kill) | Deferred — paired with auth milestone | v1.3 roadmap |
| v1.4 | Maya/editorial manifest producers | Deferred — Flame only in v1.3 | v1.3 roadmap |
| v1.4 | LLM Chat (Phase 12) | Superseded by FB-D — velocity gate decided 2026-04-23, confirmed at Phase 11 close 2026-04-24, formalized in FB-D 2026-04-25 | v1.3 roadmap |
| v1.4 | CLI manifest/tools differentiation | Deferred — Phase 11 D-08 UAT noted manifest + tools render visually indistinguishable; bundle with Phase 10.1 humanized-timestamps follow-up | Phase 11 UAT 2026-04-24 |
| v1.4.x | Manifest/tools CLI visual differentiation, 10.1-HUMAN-UAT items, W-01 server-side filter, real-time streaming push, multi-project view | Pre-discussed at v1.4 open — patch milestone after core v1.4 ships | v1.4 open 2026-04-25 |
| v1.5 | Auth (caller-identity rate limiting, multi-user) | SEED-AUTH-V1.5 planted at v1.4 open | v1.4 open 2026-04-25 |
| v1.5 | Parallel tool execution within single LLM turn | FB-C ships `parallel: bool = False` advertising the v1.5 path; passing True raises NotImplementedError | v1.4 FB-C scope |
| v1.5 | Cross-provider sensitive fallback | Loop state is provider-specific; mid-loop fallback would require state reconstruction | FB-C research §5.4 |
| v1.5 | Message-history pruning (summarize old turns, drop early tool results) | SEED-MESSAGE-PRUNING-V1.5 — for v1.4, ingest-time truncation at 8KB per result is sufficient | FB-C research §6.2 |
| v1.5 | Tool examples (Anthropic `input_examples` field) | SEED-TOOL-EXAMPLES-V1.5 | FB-C research §2.1 |

## Session Continuity

Last session: 2026-05-13T23:30:00.000Z
Stopped at: Phase 22 CLOSED (writer's-room 12-commit arc + this close commit; 6 of 6 recipes authored; substrate/consumer dual-subsystem truth surfaced + propagated; demo-driver pattern canonicalized; in-flight cold-read fixes substituted for formal UAT)
Resume reference: in-tree close artifacts — 12 docs commits (chain enumerated above at Phase 22 CLOSED Roadmap Evolution entry) + this STATE cursor. Out-of-tree memory cursor for Phase 22 close to be authored at session boundary at `project_state_2026_05_13_phase_22_closed.md`. Predecessor terminal: `project_state_2026_05_13_phase_21_closed.md` (Phase 21 surface-map + concept-docs archaeology). Parallel-track terminal: `project_state_2026_05_12_a_5_3_2_phase_closed_pushed.md` (V1.6+ Layer 2 scope; sibling not predecessor). **Phase 23 (Diagnostics + recovery → `docs/TROUBLESHOOTING.md`; DIAG-01..05) architecturally positioned as next** — natural reader question "what do I do when something breaks?" after the trust progression Phase 22 built.
